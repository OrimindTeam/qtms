/// موجّه العمليات المستدعاة بالمسار — تنفيذ حسم `IQ-019` (**الخيار أ**).
///
/// ★★ **القرار المعتمد (2026-08-25):** **خدمة Cloud Run واحدة** تحمل كل
/// العمليات المستدعاة، **واختيار العملية من مسار الطلب** — ⟵ **فيبقى
/// `QTMS_FUNCTIONS_BASE_URL` عنواناً واحداً لكل بيئة**، ⛔ **ولا تدخل أسماء
/// الخدمات ولا بصمات النشر ولا معرّفات Cloud Run في التطبيق.**
///
/// ⚠️⚠️ **ولماذا لزم هذا الملف أصلاً — تعارضٌ رُصد بالتشغيل لا بالقراءة:**
/// `CallableClient` يبني `POST <base>/<اسم العملية>`، **بينما
/// `FUNCTION_TARGET` هدفٌ واحد لكل حاوية ولا يوجّه بالمسار**. ⟵ ⛔ **فحاويةٌ
/// بهدفٍ واحد كانت ستردّ على `/deleteRole` باستجابة `grantPermissions`**
/// — ★ **استجابةَ عمليةٍ غير المطلوبة بلا خطأ ظاهر**، وهو أسوأ من الفشل
/// الصريح. **وهذا الملف يُغلق تلك الفجوة.**
///
/// ═══════════════════════════════════════════════════════════════════════
/// ⛔★★ **القواعد الأربع المُلزِمة من نصّ القرار — ⛔ ولا تُخفَّف:**
///
///   ① **كل مسار معروف يرتبط بمعالج واحد محدد** — ⛔ **ولا توجيه ضمني.**
///   ② **أي مسار غير معروف يُرفَض صراحةً** — ⛔ **ولا `fallback` إلى معالج
///      افتراضي**، ★ **ولا إلى أول عملية في القائمة.**
///   ③ **`FUNCTION_TARGET` يبقى هدف الحاوية الواحدة** — والاختيار بالمسار.
///   ④ **كل معالج يحتفظ بفحص مصادقته وتفويضه وتدقيقه** — ⛔ **والموجّه
///      لا يفحص شيئاً منها ولا ينوب عنها** (`ADR-0013` القاعدة 3).
/// ═══════════════════════════════════════════════════════════════════════
library;

/// العمليات المستدعاة — ★ **اسم القيمة هو المسار حرفياً**.
///
/// ★★ **ولا جدول تحويل بين الاسم والمسار** — بنفس منطق `Permission`:
/// `CallableOperation.deleteRole.name` **يُنتج `deleteRole`** تماماً كما
/// يبنيه `CallableClient` في التطبيق. ⟵ **فلا موضع ينزلق فيه أحدهما عن
/// الآخر**، ⛔ **ولا مسار يُكتب مرتين بصيغتين.**
///
/// ⚠️ **ولا تُضاف قيمة هنا بلا سطر في `api-overview.md` §3.1** — نفس منطق
/// `BR-M1-07` في مفاتيح الصلاحيات: **القائمة المزدوجة تفترق عند أول إضافة**.
enum CallableOperation {
  /// `grantPermissions` — منح صلاحيات ونطاق (`permission_sync_handler.dart`).
  grantPermissions,

  /// `setSourceScope` — تحديد نطاق المصادر وحده.
  setSourceScope,

  /// `bootstrapOwnerPermissions` — ★ **إجراء تشغيلي** ⛔ لا يُستدعى من التطبيق.
  bootstrapOwnerPermissions,

  /// `createUser` — إنشاء حساب وبطاقة.
  createUser,

  /// `updateUser` — تعديل بيانات مستخدم **بسببٍ نصّي**.
  updateUser,

  /// `disableUser` — تعطيل في البطاقة وخدمة المصادقة معاً.
  disableUser,

  /// `createRole` — إنشاء دور بقالب صلاحياته.
  createRole,

  /// `updateRole` — تعديل دور **بسببٍ نصّي**.
  updateRole,

  /// `deleteRole` — ★★ **حذف فعلي لدورٍ غير مُسنَد** (`IQ-018`).
  deleteRole,

  // ── البيانات المرجعية (`WU-002`) — `api-overview.md` §3.1-د ──
  // ⛔★★ **ولا كتابة مباشرة عليها بعد اليوم:** `sources` · `suppliers` ·
  //    `dealers` · `items` · `app_settings` **وسجلات الحراسة الأربعة**
  //    صارت `allow create, update: if false` — ⟵ **فالمستند وحارسُ تفرّده
  //    وقيدُ تدقيقه في معاملة واحدة** (`ADR-0013` القاعدة 1).

  /// `createSource` — ينشئ مصدراً، ⚙️ **ويُطلِق تهيئة الحسابات** (`FR-M2-04`).
  createSource,

  /// `updateSource` — يعدّل مصدراً **بسببٍ نصّي** (`FR-M2-03`).
  updateSource,

  /// `createSupplier` — ينشئ رعوياً (`FR-M3-01`).
  createSupplier,

  /// `updateSupplier` — يعدّل رعوياً **بسببٍ نصّي** (`FR-M3-13`).
  updateSupplier,

  /// `createDealer` — ينشئ مقوتاً (`FR-M4-01`) — ⛔ **بلا حقل مصدر**.
  createDealer,

  /// `updateDealer` — يعدّل مقوتاً **بسببٍ نصّي**، ★ **وبإقرارٍ عند تعطيل
  /// مقوتٍ له رصيد** (`FR-M4-09` · `E-39`).
  updateDealer,

  /// `createItem` — ينشئ نوعاً — ⛔ **ولا يُنشئ «السكرب»** (`FR-M5-05`).
  createItem,

  /// `updateItem` — يعدّل نوعاً — ⛔ **ويُرفَض على «السكرب»** (`FR-M5-05`).
  updateItem,

  /// ★★ `writeAppSettings` — الإعداد التأسيسي **مرة واحدة** (`FR-M21-03`)،
  /// ⚙️ **ومعه النوع الافتراضي «السكرب»** في المعاملة نفسها (`IQ-012`).
  writeAppSettings,

  // ── المخزون والتوريد (`WU-003`) — `api-overview.md` §3.1-هـ ──
  // ⛔★★ **ولا كتابة مباشرة عليها:** `incoming_count` · `inventory_ledger` ·
  //    `item_daily_balances` مغلقة في القواعد — ⟵ **فالمستند وحركاتُه
  //    وأرصدتُه وقيدُ تدقيقه في معاملة واحدة** (`ADR-0013` القاعدة 1).

  /// `createCountedIntake` — ينشئ وارداً عدداً ⚙️ **بتاريخ مخزونٍ من الخادم**
  /// ورقمٍ من العدّاد (`FR-M6-01` · `FR-M6-02`).
  createCountedIntake,

  /// `amendCountedIntake` — يعدّل مستنداً معتمداً **بسببٍ نصّي**، ⚙️ **ويُعيد
  /// بناء الرصيد من الدفتر** (`FR-M6-11` · `A-14`).
  amendCountedIntake,

  /// `cancelCountedIntake` — ★ **يَسِم المستند وحركاته «ملغى»** بسببٍ نصّي
  /// (`FR-M6-13`) — ⛔ **وليس حذفاً** (`FR-M6-14` · `GR-07`).
  cancelCountedIntake,

  // ── الوارد جواني (`WU-004`) — `api-overview.md` §3.1-ز ──
  // ⛔★★ **ولا كتابة مباشرة عليها:** `sacks` و`sacks/{id}/finance` مغلقتان
  //    في القواعد — ⟵ **فالمستند وحركاتُه وأرصدتُه وقيدُ تدقيقه في معاملة
  //    واحدة** (`ADR-0013` القاعدة 1).
  //
  // ★★★ **وسبع عمليات لا واحدة — وهو نصّ `FR-M7-27`:** «**مسارات كتابة
  //    منفصلة لكل مجموعة حقول … كل منها يشترط صلاحيته المطابقة**».
  //    ⟵ **فمسارٌ جامع كان يُلزم من يُدخل الضريبة بصلاحية إدخال الأنواع.**

  /// ★★★ `createSack` — ينشئ رأس الجونية ⚙️ **بتسلسلٍ يوميٍّ مستقل لكل مصدر**
  /// (`FR-M7-04`)، ⚙️ **ويُورِّد السكرب فوراً** (`FR-M7-09` · `AT-07`).
  createSack,

  /// `enterSackLines` — ★ **مسارٌ يقتصر على مصفوفة السطور** (`FR-M7-12`).
  enterSackLines,

  /// `enterSackTax` — 🔒 **يكتب في `finance/current` وحدها** (`ADR-0011`).
  enterSackTax,

  /// `renameSack` — ⛔ **ولا يمسّ الرقم المتسلسل** (`FR-M7-05` · `ADR-0007`).
  renameSack,

  /// `enterSackScrapWeight` — ⚙️ **ويُعيد توليد سطر مخزون السكرب**.
  enterSackScrapWeight,

  /// ★★ `confirmSackLostWeight` — **الفعل الصريح وحده يُسجِّل الوزن الضائع**
  /// (`FR-M7-19` · `BR-M7-12`) — ⛔ **ولا يدخل المخزون** (`FR-M7-20`).
  confirmSackLostWeight,

  /// `amendSack` — يعدّل جونية معتمدة **بسببٍ نصّي** (`FR-M7-26`).
  amendSack,

  /// `cancelSack` — ★ **يَسِم المستند وحركاته «ملغى»** — ⛔ **وليس حذفاً**.
  cancelSack,

  // ── التسعير اليومي (`WU-005`) — `api-overview.md` §3.1-و ──
  // ⛔★★ **ولا كتابة مباشرة عليها:** `daily_prices` **`allow create, update:
  //    if false`** — ⟵ **فالسجلات وقيدُ تدقيقها في معاملة واحدة**
  //    (`ADR-0013` القاعدة 1).

  /// `writeDailyPrices` — ★ **يكتب أسعار اليوم دفعةً لمصدرٍ واحد**
  /// (`FR-M9-01` … `FR-M9-08`)، ⚙️ **واليوم من المنصّة** (`GR-54`).
  ///
  /// ⚠️ **وعمليةٌ واحدة تُنشئ وتُعدِّل وتُفرِّغ** — ★ **والتمييز على السطر
  /// لا على المسار**: `FR-M9` **لا يعرف «إلغاء تسعير»** بل **تفريغاً**،
  /// ⛔ **ولا مساراً منفصلاً للتعديل** (`FR-M9-11`).
  writeDailyPrices,

  // ── التوزيع والضمار (`WU-006`) — `api-overview.md` §3.1 ──
  // ⛔★★ **ولا كتابة مباشرة عليها:** `distributions` و`pricing/current` و
  //    `dealer_ledger` و`dealer_balances` **كلها `allow write: if false`**
  //    — ⟵ **فالمستند وأسعارُه وحركاتُه المخزنية وقيدُه المدين وأرصدتُه
  //    وقيدُ تدقيقه في معاملة واحدة** (`ADR-0013` القاعدة 1 · `FR-M10-13`).

  /// ★★★ `createDistribution` — ينشئ توزيعةً ⚙️ **بمعرّفٍ مركّب يفرض `GR-18`**
  /// (`FR-M10-01`) ⚙️ **وتاريخِ مخزونٍ من المنصّة** (`FR-M10-03`).
  ///
  /// ⚠️ **ويُرفَض إن وُجدت توزيعةٌ بنفس المفتاح** بـ`ERR_DIST_001` —
  /// ★ **والواجهة تفتح الموجودة للتعديل** (`E-04`) ⛔ **ولا تُنشئ ثانية.**
  createDistribution,

  /// `amendDistribution` — يعدّل توزيعةً معتمدة **بسببٍ نصّي**، ⚙️ **ويُعيد
  /// بناء الرصيد وقيمة الضمار معاً** (`FR-M10-16` · `A-14` · `AT-57`).
  amendDistribution,

  /// `cancelDistribution` — ★ **يَسِم المستند وحركاته وقيده «ملغى»** بسببٍ
  /// نصّي — ⛔ **ويُرفَض إن سُدِّد الضمار** (`FR-M10-18` · `E-15`).
  cancelDistribution,

  // ── المقبوضات وحساب المقوت (`WU-007`) — `api-overview.md` §3.1 ──
  // ⛔★★ **ولا كتابة مباشرة عليها:** `receipts` و`deposit/current` و
  //    `dealer_ledger` و`dealer_balances` و`dealer_surplus` **كلها
  //    `allow create, update: if false`** — ⟵ **فالسند وحركاتُه الدائنة
  //    وتسويةُ ضماراته وأرصدتُه وقيدُ تدقيقه في معاملة واحدة**
  //    (`ADR-0013` القاعدة 1 · `FR-M12-08` · `GR-51`).

  /// ★★★ `createReceipt` — ينشئ سند قبضٍ ⚙️ **بحركةٍ دائنة لكل سطر**
  /// (`FR-M12-07`) ⚙️ **وتاريخٍ يقبل السابق بمفتاحه ويرفض المستقبلي مطلقاً**
  /// (`FR-M12-02`).
  ///
  /// ⚠️ **والتوزيع التلقائي لا يمرّ من هنا** — `FR-M12-14`: **يُحسَب محلياً
  /// ويُعرَض للمراجعة** ⛔ **ولا يُحفَظ تلقائياً**، ⟵ **فما يصل هو ما أقرّه
  /// المستخدم** ⛔ **لا مخرَجُ الخوارزمية مباشرةً.**
  createReceipt,

  /// `amendReceipt` — يعدّل سنداً معتمداً **بسببٍ نصّي اختياري** (`ADR-0020`)،
  /// ⚙️ **وتُعاد حالة الضمارات احتساباً** (`FR-M12-21` · `AT-57`).
  amendReceipt,

  /// `cancelReceipt` — ★ **يَسِم السند وحركاته «ملغى»** ⚙️ **ويردّ المسدَّد
  /// إلى ضماراته** — ⛔ **بلا حركةٍ عكسية** (`A-14` · `GR-06`).
  cancelReceipt,

  /// ★★ `confirmReceiptDeposit` — **مسارٌ مقيَّد بحقول الإيداع وحدها**
  /// (`FR-M12-18` · [`ADR-0017`]) يشترط `receiptDepositConfirm`
  /// **و`receiptDepositView` وملاحظةً غير فارغة** (`AT-36`).
  ///
  /// ⛔⛔★★ **ومنفصلٌ عن [amendReceipt] عمداً** — ⟵ **فمن يملك تأكيد الإيداع
  /// لا يُعدِّل مبلغاً**، ★ **ومن يُعدِّل السند لا يُقرّ إيداعاً.**
  confirmReceiptDeposit,

  // ── الخصومات (`WU-013`) — `api-overview.md` §3.1 ──
  // ⛔★★ **ولا كتابة مباشرة عليها:** `discounts` و`dealer_ledger` و
  //    `dealer_balances` **كلها `allow create, update: if false`** —
  //    ⟵ **فالسند وحركاتُه الدائنة وتسويةُ ضماراته وأرصدتُه وقيدُ تدقيقه
  //    في معاملة واحدة** (`ADR-0013` القاعدة 1 · `FR-M13-11` · `GR-51`).
  //
  // ⛔⛔★★★ **وثلاثُ عملياتٍ بلا أي أثرٍ نقدي** (`FR-M13-03`) — ★ **وهذا
  //    فارقُها الجوهري عن المقبوضات**: ⟵ **دَينٌ يسقط ولا ريالَ يدخل**،
  //    ⛔ **ولا سجلَّ فائضٍ ولا إيداعَ بنكياً إطلاقاً** (`FR-M13-05`).

  /// ★★★ `createDiscount` — ينشئ سند خصمٍ ⚙️ **بحركةٍ دائنة لكل سطر**
  /// (`FR-M13-03`) ⚙️ **وتاريخٍ يقبل السابق بمفتاحه ويرفض المستقبلي مطلقاً**
  /// (`FR-M13-06`).
  ///
  /// ⛔⛔★★★ **ويُرفَض بـ`ERR_DIST_009` إن حمل حقلَ فائضٍ بأي صورة**
  /// (`FR-M13-05` · `AT-35`) — ★ **والحقل غير موجودٍ في الشاشة أصلاً**،
  /// ⟵ **فوصولُه يعني طلباً من خارج التطبيق.**
  ///
  /// ⛔⛔★★ **ومسارٌ منفصلٌ عن [createReceipt] قطعاً** (`FR-M15-06-أ`) —
  /// ★ **بمجموعةٍ وصلاحيةٍ ونوعِ قيدٍ منفصلة**: ⟵ **فما دفعه المقوت نقداً
  /// وما أسقطه المالك عنه رقمان مختلفان لا يجوز خلطهما.**
  createDiscount,

  /// `amendDiscount` — يعدّل سنداً معتمداً **بسببٍ نصّي اختياري** (`ADR-0020`)،
  /// ⚙️ **وتُعاد حالة الضمارات احتساباً** (`FR-M13-10` · `AT-57`).
  amendDiscount,

  /// `cancelDiscount` — ★ **يَسِم السند وحركاته «ملغى»** ⚙️ **ويردّ المخصوم
  /// إلى ضماراته** — ⛔ **بلا حركةٍ عكسية** (`A-14` · `GR-06`).
  cancelDiscount,

  // ── البيع النقدي المباشر (`WU-012`) — `api-overview.md` §3.1 ──
  // ⛔★★ **ولا كتابة مباشرة عليها:** `cash_sales` **`allow create, update:
  //    if false`** — ⟵ **فالسندُ وحركاتُه المخزنية وأرصدتُه وقيدُ تدقيقه في
  //    معاملة واحدة** (`ADR-0013` القاعدة 1).
  //
  // ⛔⛔★★★ **وثلاثُ عملياتٍ بلا مقوتٍ ولا دفترِ ذمم** (`FR-M11-03` · `GR-33`)
  //    — ★ **وهذا فارقُها الجوهري عن التوزيع**: ⟵ **مخزونٌ يخرج ونقدٌ يدخل**،
  //    ⛔ **ولا قيدَ على أحد.**

  /// ★★★ `createCashSale` — ينشئ سند بيعٍ نقدي ⚙️ **بتاريخ مخزونٍ من المنصّة**
  /// (`A-10` · `GR-14`) ⚙️ **ورقمٍ من عدّاد اليوم** (`CSH-YYYYMMDD-####`).
  ///
  /// ⛔⛔★★ **ويُرفَض إن كان سعرُ سطرٍ دون الحد الأدنى بلا
  /// `cashSaleBelowMinimum`** بـ`ERR_PRICE_002` (`FR-M11-05` · `GR-34`) —
  /// ★ **والحدُّ من `daily_prices` داخل المعاملة** ⛔ **لا من الجهاز.**
  createCashSale,

  /// `amendCashSale` — يعدّل سنداً معتمداً **بسببٍ نصّي اختياري** (`ADR-0020`)،
  /// ⚙️ **ويُعيد بناء الرصيد من الدفتر** (`FR-M11-13` · `A-14` · `AT-57`).
  amendCashSale,

  /// `cancelCashSale` — ★ **يَسِم السند وحركاته «ملغى»** — ⛔ **وليس حذفاً**
  /// (`GR-07`).
  ///
  /// ⛔★★ **ولا حارسَ تسويةٍ له بخلاف التوزيعة** (`E-15`) — ★ **فلا ذمّةَ
  /// أنشأها أصلاً** (`FR-M11-03`).
  cancelCashSale,

  // ── الإرسال والتصدير (`WU-010`) — `api-overview.md` §3.1 ──
  // ⛔★★ **ولا مسارَ إرسالٍ هنا إطلاقاً:** `FR-M20-15` و`FR-M20-16` —
  //    **الإرسال إجراء واجهة فقط، ولا تسجيل لمحاولاته** (`AT-64`).
  //    ⟵ **والتصدير وحده يُسجَّل** (`FR-M19-04`).

  // ── السحبيات والخرجيات (`WU-014`) — `api-overview.md` §3.1 ──
  // ⛔★★ **ولا كتابة مباشرة عليها:** `outflows` و`outflow_ledger` و
  //    `inventory_ledger` و`item_daily_balances` **كلها `allow create,
  //    update: if false`** — ⟵ **فالسندُ وسطورُ الدفتر الرابع وحركاتُه
  //    المخزنية وأرصدتُه وقيدُ تدقيقه في معاملة واحدة** (`ADR-0013`
  //    القاعدة 1 · `FR-M22-13` · `GR-51`).
  //
  // ⛔⛔★★★ **وثلاثُ عملياتٍ لسجلَّين — والمفتاحُ يتبع السجل لا العملية**
  //    (`GR-43` · `FR-M22-03`): ★ **`ledgerType` وسيطٌ إلزاميٌّ في الحمولة**،
  //    ⟵ **ومنه يختار `outflowPermission` أحدَ ستةِ مفاتيح.**
  //    ⛔⛔ **وفي التعديل والإلغاء السجلُّ **المخزَّن** هو الحَكَم** ⛔ **لا
  //    المُرسَل**: ⟵ **وإلا لعدّل حاملُ `expenseAmend` سحبيةً لا يملك حتى
  //    رؤيتها** — ★ **وهو تصعيدُ امتيازٍ صامت.**
  //
  // ⛔⛔★★★ **ولا حركةَ في دفتر المقاوته في أيٍّ منها** (`FR-M22-04` ·
  //    `GR-44`) — ★ **وهذا فارقُها الجوهري عن التوزيع والقبض:** ⟵ **مالٌ
  //    وبضاعةٌ يخرجان**، ⛔ **ولا ذمّةَ على أحد.**

  /// ★★★ `createOutflow` — ينشئ سحبيةً أو خرجية ⚙️ **بتاريخ سندٍ يقبل
  /// السابق بمفتاحه** (`FR-M22-09`) ⚙️ **وتاريخِ مخزونٍ من المنصّة**
  /// (`A-10` · `GR-14`) ⚙️ **ورقمٍ من عدّاد سجلِّه** (`WDR-…` · `EXP-…`).
  ///
  /// ⛔⛔★★ **ويُرفَض بلا مصدرٍ بـ`ERR_OUT_001`** (`GR-42` · `E-28`) —
  /// ★ **حتى لسحبيات المالك الشخصية.**
  createOutflow,

  /// `amendOutflow` — يعدّل سنداً معتمداً **بسببٍ نصّي اختياري** (`ADR-0020`)،
  /// ⚙️ **ويُعيد بناء الرصيد من الدفتر** (`FR-M22-18` · `A-14` · `AT-57`).
  amendOutflow,

  /// `cancelOutflow` — ★ **يَسِم السند وحركاته وسطورَ دفتره «ملغى»** —
  /// ⛔ **وليس حذفاً** (`GR-07`).
  cancelOutflow,

  /// ★★★ `logExport` — **يكتب قيد «تصدير» وحده** (`IQ-032` الخيار أ).
  ///
  /// ⛔⛔★★ **وهي العملية الوحيدة في النظام التي تكتب قيداً بلا مستندٍ
  /// مصاحب** — ★ **لأن التصدير لا يُغيِّر بياناً**: ⟵ **`AuditedWrite.auditOnly`
  /// عَلَمٌ صريحٌ يقرؤه حارسُ `_commit`**، ⛔ **لا إسقاطٌ للحارس للجميع.**
  logExport;

  /// المسار الذي يُطابق هذه العملية — **بشرطة مائلة بادئة**.
  String get path => '/$name';
}

/// ★ يستخرج العملية من مسار الطلب — و`null` تعني **مساراً غير معروف**.
///
/// ★★ **دالة خالصة عمداً** — ⟵ **فالتوجيه يُختبَر بلا سحابة ولا شبكة**،
/// وهو حارسٌ حقيقي لا تفصيل نقل: **خطأٌ فيه يُسلّم الطلب لمعالجٍ آخر.**
///
/// **ما تتسامح معه صراحةً:** الشرطة البادئة والزائدة (`/createUser` ·
/// `createUser` · `/createUser/`) — ★ **فبوابات الوسط تُطبّع المسارات
/// اختلافاً بينها**، ⛔ **والتشدّد هنا يكسر الاستدعاء بلا سبب أمني.**
///
/// ⛔★★ **وما لا تتسامح معه إطلاقاً:**
///   · **اختلاف حالة الأحرف** (`/createuser`) — ⟵ **مساران مختلفان**،
///     ★ **والتطبيع كان سيُخفي خطأً برمجياً في العميل.**
///   · **مسارٌ مركّب** (`/v1/createUser`) — ⛔ **ولا يُقتطَع آخرُ مقطع**:
///     الاقتطاع يجعل `/anything/deleteRole` يُنفِّذ حذفاً، ⟵ **وهو توجيهٌ
///     ضمني تمنعه القاعدة ①.**
///   · **المسار الجذر** (`/`) — ⛔ **ولا يُعامَل عمليةً افتراضية.**
CallableOperation? resolveCallableOperation(String path) {
  // ⛔ **الشرطة البادئة والزائدة وحدهما تُقصّان** — ★ ولا شيء غيرهما.
  final String trimmed = path.replaceAll(RegExp(r'^/+|/+$'), '');
  if (trimmed.isEmpty) return null;
  for (final CallableOperation operation in CallableOperation.values) {
    // ★ **مطابقة تامة حسّاسة لحالة الأحرف** — راجع الترويسة.
    if (operation.name == trimmed) return operation;
  }
  return null;
}
