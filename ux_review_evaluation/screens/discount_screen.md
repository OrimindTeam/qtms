## الخصومات — التقييم: 7/10
بنية توأم مدروسة بعناية لشاشة "المقبوضات" بفوارق معنوية موثَّقة بدقة (تسمية الحقل "الخصم" لا "الواصل"، إعلان صريح للمبلغ غير الموزَّع بقيمته بدل تجاهله بصمت) — لكنها ترث نفس فجوتي القرين المباشر.

### المشاكل الحالية
- **معرّف المصدر الخام معروض بدل اسمه** — نفس نمط `receipt_screen.md` حرفياً، مؤكَّد باللقطة الفعلية ("ضمار 2026/09/05 — مصدر SRC-001") (المحور 2).
- **`errorText` "يتجاوز المتبقي" لا يُعطِّل زر الحفظ** — نفس نمط `receipt_screen.md` (المحور 9).
- **سطر حالة الكتابة نص عارٍ بلا ثلاثية لون رغم حمله ثلاث درجات شدة فعلية** (`✅`/`⚠️`/`❌`) — أخطر قليلاً من نظيره في المقبوضات لأن هذه الشاشة تحديداً تستعمل ثلاث حالات شدة مختلفة (نجاح، تحذير عدم استيعاب، فشل) في نفس السطر النصي العاري، فيخسر المستخدم فرصة التمييز اللوني بين "نجح لكن انتبه" و"فشل تماماً" (المحور 3).

### التوصيات
**مكاسب سريعة (Quick Wins — أثر عالٍ وجهد تنفيذ منخفض):**
- استبدل `sourceId` الخام بالاسم المقروء (نفس توصية `receipt_screen.md` حرفياً).
- استبدل `Text` الحالة بـ`QtmsActionStatus` مع إضافة حالة ثالثة وسيطة له (`.warning` بثلاثية `warning` وأيقونة `report_problem_outlined`) لرسالة "لم تُوزَّع" تحديداً — بما أن هذه الشاشة تحتاج فعلاً ثلاث درجات لا درجتين فقط (نجاح/فشل)، فإن أمكن توسيع `QtmsActionStatus` بمصنعٍ ثالث `.warning` فهذا يخدم الشاشة بدقة أكبر من إجبارها على درجتين فقط.

**تحسينات بنيوية (Structural — تتطلب إعادة هيكلة أو استبدال مكوّن):**
- اربط `enabled` على زر "حفظ سند الخصم" بنفس شرط منع تجاوز المتبقي المطبَّق في البيع النقدي (مطابق تماماً لتوصية `receipt_screen.md`).
