<!-- UPDS-DOCGEN:BEGIN — كتلة يديرها upds-docgen آلياً. لا تُحرِّرها يدوياً؛ حرِّر مستندات docs/ وأعد التشغيل. -->

## سياق المشروع (يديره upds-docgen)

**المشروع:** QTMS — نظام محاسبي لإدارة المخزون والذمم والنقد في تجارة القات، على أندرويد بقاعدة سحابية مُدارة بلا خادم وسيط.
**المرحلة الحالية:** **التنفيذ.** التوثيق 100٪ · **24 ADR معتمداً** · **29 وحدة عمل**. ✅★★★ **عشرون منجَزة** (`WU-000` · `WU-026` · `WU-001` … `WU-016` · `WU-027` · `WU-028`) · ★ **البوابة الآلية 2395 · ٠ فاشلاً** · ⛅ **و`AM-009` و`AM-012` و`ADR-0024` مُثبَتةٌ حيّاً.** ⛔⛔★★★ **و`IQ-040` مفتوحٌ يحجب `WU-017`** (**39/40** — ★ **مفتاحا تفويضِ كشف الحساب في `FR-M17` §3**) · ✅ **وكلُّ `CR-004`…`CR-012` و`ADR-0023` و`ADR-0024` معتمدة** — ⛔ **ولا بندَ حَوْكمةٍ من الفئة 1 معلَّق.** ✅★★★ **والمرحلة الأولى: اختبارُها التكاملي (§د.3) نُفِّذ 2026-09-02 وكشف أربعةَ عطلٍ — وكلُّها سُدَّت ومُثبَتةٌ حيّاً** (آخرُها [`DEBT-86`]) ⟵ ✅★★★ **والمرحلةُ الأولى مغلقةٌ رسمياً (2026-09-02): بوابتُها 40/40 بدليلٍ مُسمّى** (`IQ-038` **الخيار أ: التقسيمُ بـ`RTM` §4 لا بالمدى**) **وبروتوكولُ `UPDS-05` §7 مُنفَّذ.** ⚠️ **و`DEBT-66` و`DEBT-67` و[`DEBT-92`] وحدَها مفتوحة** (⑤ **تفكيكُ أرقام البطاقة** · ⑥ **مشاركةُ ملخّص اليوم** — **مؤجَّلان مُعلَنان إلى `WU-018`**) — ✅ **و`DEBT-70` و`DEBT-79` و`DEBT-82`…`DEBT-91` و`DEBT-93` مُغلَقة.**
**منهجية التطوير:** تزايدية (Incremental) (وحدة العمل: **زيادة** = شريحة رأسية كاملة تُنتج قدرة قابلة للاستخدام · ⛔ لا طبقة أفقية)
→ التفاصيل: `docs/21-project-management/development-methodology.md`

## الأوامر الفعلية

★ **قواعد الحماية — تعمل فعلياً** (بلا سحابة وبلا إنترنت):

```bash
cd test/security-rules && npm test
```

بوابتان: **تغطية الصلاحيات** (تحليل نصي) ثم **236 اختبار سلوك على المحاكي**.
⚠️ يحتاج **Java 21+** — والمُشغِّل يختاره ويوجّه مقابس `AF_UNIX` تلقائياً (`DEBT-10`). ⛔★★ **وشقُّه الثاني يفشل على Windows بـ`'firebase' is not recognized` متى اكتُشف `JAVA_HOME`** (`DEBT-60`) — ★ **والبديلُ المقيس: `firebase emulators:exec --only firestore --project demo-qtms` بنفس المحيط.**

★ **ثلاث حزم Dart لا واحدة** (`ADR-0012`) — ⛔ **والأخيرتان لا تعتمدان `flutter`**:

```bash
cd packages/qtms_domain && dart analyze && dart test   # طبقة النطاق — 493 اختباراً
cd functions          && dart analyze && dart test   # العمليات السحابية — 506 اختباراً
```

★ **مشروع Flutter قائم — وهذه تعمل الآن:**

```bash
flutter pub get        # الاعتماديات
flutter analyze        # فحص ساكن صارم — التحذير يُعامَل خطأً
flutter test           # الاختبارات
```

★★ **وبوابات التصميم — 28 اختباراً** (★ **الطبقةُ الأولية** · **التباين** · ★ **خطُّ العرض** · الاتجاه · الوضع الواحد · اللغة الواحدة · خُطّاف الاختبار) **تعمل ضمن `flutter test`** — ★ **ولتشغيلها وحدها:**

```bash
flutter test test/core/design/design_gates_test.dart
```

★★ **وأيقونة التطبيق مولَّدة من شعار العميل** — ⛔ **ولا تُحرَّر `mipmap-*` يدوياً:**

```bash
flutter build apk --flavor production   # البناء — ⚠️ يحتاج شرطين، راجع أدناه
dart run flutter_launcher_icons         # عند تغيّر assets/icons/ — لا في كل بناء
```

> ⚠️ **البناء يحتاج:** ① `android/app/google-services.json` (خارج المستودع عمداً)
> ② ★ **توجيه `TEMP` خارج `%LOCALAPPDATA%`** وإلا فشل Gradle بـ
> `Unable to establish loopback connection` (`DEBT-11`).
> التفاصيل: `docs/07-development/build-and-run-instructions.md` §2.1 و§3.

## معرّفات المشروع المعتمدة

| البند | القيمة |
|---|---|
| مشروع الإنتاج | `qtms-orimind-c001` — ✅ **قائم** |
| المشروع التجريبي | `qtms-orimind-master` — ✅ **قائم** |
| موقع قاعدة البيانات | ★ **`me-central1` (الدوحة)** — نهائي لا رجعة فيه · **نُقل من `me-central2`** بـ`ADR-0014` |
| معرّف التطبيق | `dev.orimind.qtms.c001` · التجريبي `.staging` |
| ★★ الاسم الظاهر | ★ **«وكالة محمد المحامي»** · التجريبي «… — تجريبي» (`AM-002`) — ⛔ **بلا بادئة `QTMS`** · ★ **مصدره `appDisplayName` و`app_name` معاً بحارسٍ آلي** |
| ★★ الخطّان المعتمَدان | **IBM Plex Sans Arabic** — الجسم والجداول ★ **وكلُّ رقم** (أرقامه اللاتينية وحدها ثابتة العرض) · ★★ **Tajawal** (800/900) — **نصُّ العناوين الحرفي وحده** (`DS-002` · `AM-007`) ⛔ **ولا رقمَ به: أرقامه بتسعة عروضٍ وبلا `tnum`** — `docs/19-assets/fonts/README.md` §4 |
| ★ القواعد المنشورة | ⛅★★ **التجريبية على إصدار المستودع** (37799 بايت · `WU-002` · **مُتحقَّق بايتاً ببايت**) · ⛔ **والإنتاج على إصدار `WU-026`** (36497) — **عمداً: لا خدمة عمليات هناك** |
| ★★ خدمة العمليات المستدعاة | `qtms-callables` — **حاويةٌ واحدة توجّه بالمسار** (`IQ-019`) · ✅ **التجريبية فقط** · ⛅★★★ **و47 عملية منشورة ومقيسة** (`callables:wu015` · **المراجعة `00028-z2s`** · 2026-09-03 — ★ **تحمل مُحتسِبَ مالية الجواني** ⛔ **بلا عمليةٍ جديدة**) — ★ **المساراتُ ردّت `401` ومجهولٌ ردّ `404`** · ⚠️★★ **والعدد يُقرأ من `CallableOperation` و`describe`** ⛔ **لا من ذاكرة تشغيلٍ سابق** · ⛔⛔ **ولا تُنشَر بـ`--no-allow-unauthenticated`** |

★ **معرّفا حساب المالك الأول والحالة التفصيلية:** `docs/11-devops-and-infrastructure/environments.md` §1.3

## المكدس المعتمد

**Flutter/Dart** · **Cloud Firestore** · **قواعد الحماية = طبقة التفويض الوحيدة** ·
**Riverpod** لإدارة الحالة · **`go_router`** للملاحة · **`freezed` + `json_serializable`** للنمذجة ·
★ **العمليات السحابية بلغة Dart** في `functions/` بطبقة النطاق نفسها — `packages/qtms_domain/`.
→ التفاصيل: `docs/03-architecture/technology-stack.md`

## اقرأ هذه قبل تعديل أي كود

| الموضوع | المستند |
|---|---|
| ★ كل المعادلات المالية | `docs/04-design/design-overview.md` §2 |
| ★ خطة وحدات العمل وحالتها | `docs/00-governance/upds-workflow/implementation-plan.md` |
| أسلوب الكود والتسمية | `docs/04-design/coding-standards.md`, `docs/04-design/naming-conventions.md` |
| مبادئ العمارة | `docs/03-architecture/architecture-principles.md` |
| تصميم الوحدة المعنيّة | `docs/04-design/module-design/<module>-design.md` |
| ★ جدول تتبع الصلاحيات | `docs/09-security/security-requirements.md` |
| ⛔⛔★★★ **أي شاشة أو مكوّن عرض** — **قبل `module-design` لا بعده** | `docs/18-ux-ui/design-system.md`, `docs/18-ux-ui/design-tokens.md`, `docs/18-ux-ui/ui-guidelines.md` — ★ **بروتوكول التشغيل §ح** · ⛔ **وصفرُ قيمةِ لونٍ أو حجمِ خطٍّ أو نصفِ قطرٍ خارج التوكنز** |
| التفرّع ورسائل الـ Commit | `docs/07-development/branching-strategy.md`, `docs/07-development/commit-message-conventions.md` |
| تعريف "منجَز" (على مستوى الزيادة) | `docs/07-development/definition-of-done.md` |
| إدارة الأسرار | `docs/09-security/secrets-management-policy.md` |
| خارطة الدخول الكاملة | `docs/22-onboarding/developer-onboarding.md` |

## أهم القرارات المعمارية

- بلا خادم وسيط: أندرويد + قاعدة مستندية سحابية مباشرة — `ADR-0001`
- قواعد الحماية هي طبقة التفويض الوحيدة؛ وأي قاعدة عمل بلا مقابل فيها تُعتبر غير مُنفَّذة — `ADR-0002`
- اتصال إلزامي والتخزين المؤقت المحلي مُعطَّل صراحةً لمنع وهم الحفظ الناجح — `ADR-0003`
- تعديل مباشر بلا قيود عكسية، بقيد تدقيق مفروض — `ADR-0004` (⛔★★ **وشرطُه 2 «لا تعديل بلا سبب» أُلغي بـ`ADR-0020`**)
- الرصيد مشتقّ من الدفتر دائماً، والملخصات مبنية مسبقاً ولا تُقرأ كمصدر حقيقة — `ADR-0008`
- ★ أربع طبقات بنطاق نقي على ست قدرات، **والعمليات السحابية بلغة النطاق نفسها** — `ADR-0009`
- ★ إدارة حالة تصريحية بحقن اعتمادية صريح، والشاشة بلا منطق أعمال إطلاقاً — `ADR-0010`
- ⛔⛔★★★ **وشريطٌ علويٌّ موحّد في كل شاشة بلا استثناء** (`AM-008` · `ui-guidelines.md` §3-أ): **`QtmsTopBar` مكوّنٌ مركزيٌّ واحد** ⛔ **ولا `AppBar` مبنيٌّ يدوياً** — ★ **واسمُ الشاشة مُدخَلٌ إلزامي في التوقيع** · ⛔⛔ **وحالةُ الاتصال تُقاس من بثِّ Firestore** (`isFromCache`) ⛔ **لا من واجهة شبكةٍ على الجهاز**، ★ **و«متصل» لا تُقال قبل قياس** (`ADR-0003`) ⟵ **ولذلك للحالة نوعٌ ثلاثي.** ⛔⛔★★★ **ولا زرَّ رجوعٍ فيه ولا أيقونةَ خروج** (`AM-009` ①) — ★ **والخروجُ في ورقةِ جلسةٍ تفتحها الصورةُ الرمزية**، ⛔ **وإسقاطُه بلا بديلٍ يترك التطبيقَ بلا مخرج.** ★ **تحرسها ستُّ بوابات آلية.**
- ★ عزل الحقول المالية في مستندات فرعية بشرط قراءة مستقل — `ADR-0011`
- ★ **طبقة النطاق في حزمة Dart نقية مستقلة** (`packages/qtms_domain/`) يشاركها التطبيق والسحابة — `ADR-0012`
- ★ **الكتابة عبر دالة سحابية مستدعاة** تكتب المستند وقيد التدقيق في **معاملة واحدة** — `ADR-0013`
- ★ **موقع القاعدة `me-central1`** لأن `me-central2` لا تحمل العمليات السحابية — `ADR-0014`
- ★ **كل مبلغ عدد صحيح بالريال** ⛔ **بلا كسور ولا تخزين بأصغر وحدة** — `ADR-0015`
- ★ **الصلاحيات تخرج من رمز الدخول إلى `users/{userId}`** (`role` و`sourceScope` يبقيان) — `ADR-0016`
- ★ **حالة الإيداع البنكي معزولة في `receipts/{id}/deposit/current`** بشرط قراءة — `ADR-0017`
- ★★ **موضع تقريبٍ ثالث: قيمة السطر الوزني** (`الكمية كجم × سعر صحيح`) — `ADR-0019` (**يُوسِّع إحصاء `ADR-0015` القاعدة 4 ولا يُلغيها**)
- ★★ **والسبب النصي اختياريٌّ في مسارَي التعبئة المؤجَّلة للجونية وحدهما** — `ADR-0018` (⚠️ **مُستوعَبٌ في `ADR-0020`**)
- ⛔⛔★★★ **والسبب النصي اختياريٌّ في كل العمليات بلا استثناء** — `ADR-0020` (**يُلغي `ADR-0004` الشرط 2 ويُعمِّم `ADR-0018`**)
- ⛔⛔★★ **ولا طباعةَ حرارية ولا بلوتوث — المخرَجُ PDF ومشاركتُه ورسالةٌ نصية** — `ADR-0023` (**قرارُ نطاقٍ صريح** ⛔ **لا متطلبٌ مؤجَّل**)
- ⛔⛔★★★ **والدخولُ بالبصمة عبر بيانات اعتمادٍ في `Android Keystore`** — `ADR-0024` (**بأحدَ عشرَ شرطاً مجتمعةً**): ⛔ **ولا تُقرأ الخزنةُ بلا مصادقةٍ بيومترية** · ⛔ **وتُمسَح فوراً عند الإطفاء وعند أول فشل** · ⛔⛔ **ولا صلاحيةَ تُشتقّ منها ولا تلتفّ على تعطيل الحساب** — ★ **و`firestore.rules` لم تُمَسّ.**

## قواعد حَوْكمة ملزِمة في هذا المشروع

- ★ **كل `ADR-0001`…`ADR-0023` معتمد** — ⛔ **لا تُعدِّل أياً منها**؛ الانحراف يحتاج ADR جديداً يشير للقديم.
- لا تُعدِّل `docs/02-requirements/srs.md` بعد اعتماده — عبر `change-requests/CR-*.md` فقط.
- لا تكتب أي سرّ في الكود أو الإعدادات — متغيرات بيئة فقط وفق سياسة الأسرار.
- لا تحذف ملفات نهائياً — النقل إلى `docs/99-archive/` وبطلب صريح.
- ★ لا حذف بيانات ولا تعديل لسجل التدقيق داخل النظام — لأي مستخدم بمن فيهم المالك.
- ★ **كل مبلغ `int` بالريال** ⛔ **ولا فاصلة عائمة ولا نوع عشري ولا «أصغر وحدة»** (`ADR-0015`) · **والتقريب في مواضعه الثلاثة وحدها** (ضريبة الجونية · متوسط سعر الكيلو · ★ **قيمة السطر الوزني** — `ADR-0019`) · **وإدخال المستخدم يُرفض لا يُقرَّب**
- ★ ولا جمع بين الحبات والأوزان، ولا خلط بين `stockDate` و`entryDate`.
- ⛔⛔★★ **ولا تُكتب قيمة سرّ في أي مستند ولا كود ولا التزام ولا ردّ نصّي — ولو جزئياً** (`secrets-management-policy.md`) · ★★ **واستثناءٌ واحد مُوثَّق صراحةً بـ`AM-004` عدّل قاعدة «إدخالُ كلمات المرور ممنوعٌ على الأداة»:** ⛅ **حساب اختبار (QA) كامل الصلاحيات على التجريبية `qtms-orimind-master` وحدها** (`environments.md` §1.5 · `RB-staging-qa-account`) — ★ **يُسجَّل به الدخول آلياً بتمرير مسار الملف السرّي `.secrets/qtms-staging-qa-account.env` أو اسم متغيّره فقط** ⛔ **ولا تمرّ القيمة في سياقك ولا تفكيرك الظاهر ولا مخرجاتك أبداً** · ⛔⛔ **ولا نظير له على الإنتاج** — ★ **ولماذا عُدِّلت: المنع المطلق كان يُوقف اختبار §د الوظيفي عند شاشة الدخول، والغاية (ألّا تطّلع على سرّ المالك) تتحقق بالتمرير غير المباشر بلا ذلك الثمن.**
- ⛔⛔★★ **وصلاحيات ذلك الحساب = كل مفاتيح §2 من `permissions-catalog.md` تعريفاً** — ★ **فكل مفتاح جديد تُضيفه يُسنَد له في نفس التشغيل** (البند ⑤ في §6 من الكتالوج) ⛔ **بلا انتظار طلبٍ من المالك** · ★ **وصيانتُه واستخدامُه فئةٌ 3** (البروتوكول §ب.1) ⛔ **بلا أي مساسٍ بالفئة 1.**
- ⛔⛔★★ **ولا يُنشَر شيء على الإنتاج بلا أن يكتب المالك حرفياً `اعتمد-النشر-الإنتاجي` في نفس المحادثة** — ★ **مهما نجحت كل الفحوص** · ★ **وتُجهَّز `uat-signoff.md` و`release-checklist.md` ذاتياً بلا حقل التوقيع، ويُعرَض ملخصٌ ختامي واحد** (البروتوكول §ب.2) — ⛔ **والتجهيز ليس اعتماداً.**
- ★ **لا تكرار لأي معادلة خارج طبقة النطاق** — لا في شاشة ولا مستودع ولا عملية سحابية.
- ★ **منح الصلاحيات من العملية السحابية حصراً** — ⛔ ولا من التطبيق مهما كان دور المستخدم.
- ⛔⛔★★ **وتعطيلُ مقوتٍ رصيدُه ≠ 0 يشترط ثلاثةً معاً** (`FR-M4-09` · `IQ-020`): **`dealerDisableWithBalance`** — ★ **مفتاحٌ مستقل** ⛔ **لا `dealerWrite`** — **+ إقرارٌ نصّي غير فارغ + رصيدٌ مقيسٌ من `dealer_balances` داخل المعاملة** ⛔ **لا قيمةٌ من الجهاز** · ★ **ورصيدُ الصفر لا يشترط المفتاح.**
- ★ الخصم يُدخَل كخصم لا كمبلغ واصل — وبطاقة ضمار المالك تعرض «قبل الخصم» و«بعد الخصم»، والصافي النهائي على الثاني.
- ★ الزيادة لا تُعلَن منجَزة قبل أن تعمل من طرف إلى طرف بقواعد حمايتها واختباراتها وتوثيقها.
- ★ **ولا تُعلَن منجَزة بتغطية نصية** — التغطية تُثبت أن الشرط **مذكور** لا أنه **صحيح**. شغّل الاختبارات فعلياً.
- ⛔⛔★★★ **لم يبقَ مسارُ كتابةٍ مباشر واحد للتطبيق بعد `WU-002`** — **كل مجموعة `allow create, update: if false`**، ★ **والقراءة وحدها مباشرة** (`ADR-0013` القاعدة 4). ⟵ **فأي زيادة جديدة تكتب عبر دالة مستدعاة** ⛔ **ولا تُفتَح قاعدة كتابة من جديد.**
- ⛔⛔ **ولا تُنشَر `firestore.rules` قبل نشر خدمة العمليات** الحاملة للعمليات المستدعاة — ★ **ترتيبٌ مُلزِم لا يُعكَس**: القواعد تُغلِق المسار القديم فوراً، **وعكسُه يترك النظام بلا مسارٍ لكتابة بياناته المرجعية.**
- ⛔⛔★★★ **ولا دالةَ كاتبةٍ تفرض سبباً نصياً على أي عملية** (`ADR-0020` · 2026-08-27) — ★ **اختياريٌّ إنشاءً وتعديلاً وإلغاءً وإتلافاً وحذفاً**، ⛔ **ولا تُرفَض عمليةٌ لغيابه.** ⚠️★★ **وكان إلزامياً حتى ذلك التاريخ** (`ADR-0004` الشرط 2 · `CR-002` · `DEBT-21` ①) — ★ **والتاريخ يُقرأ ولا يُبنى عليه.** ⛔⛔★★★ **والقاعدة الوحيدة الباقية — وهي الآن الحارس الوحيد: لا يُعبِّئ التطبيق ولا السحابة سبباً نيابةً عن المستخدم أبداً** — **ما لم يكتبه إنسانٌ لا يُرسَل ولا يُخزَّن**، ★ **والفراغات تُقرأ غياباً لا نصّاً فارغاً** (`blankToNull` في الطرفين). ⛔⛔ **وحقلا `balanceAcknowledgement` (`FR-M4-09`) و`disableReason` (`FR-M1-12`) خارج القرار — إلزاميّان كما كانا.**
- ⛔⛔ **لا تنشر `firestore.rules` قبل تنفيذ** `docs/13-operations/runbooks/RB-backfill-user-isactive.md` — ★ **`perm()` تشترط `isActive`**، **وعكسُ الترتيب يقفل المالك خارج نظامه**.
- ★★ **الحذف ممنوع في كل السجلات — واستثناءٌ وحيد معتمد: قالب الدور غير المُسنَد** (`IQ-018`). ⛔ **ولا يُقاس عليه مستخدمٌ ولا حركة ولا قيد** · ★ **و«غير مُسنَد» يُقاس باستعلامٍ فعلي على `users` داخل معاملة الحذف** ⛔ **لا بحقل مخزَّن ولا كاش** · ⛔ **ولا حقل حالة للدور ولا تعطيل له**.
- ★★ **هوية العميل من مصدر واحد** (`AM-002`): الاسم الظاهر والشعار في `lib/core/design/brand.dart` و`res/values/strings.xml` — ⛔ **ولا نصّ اسم ولا مسار أصل محفور في أي شاشة**، ★ **والأيقونة وشاشة البداية وشاشة الدخول مساحة العميل حصراً** ⛔ **يُمنع فيها شعار جهة التطوير** (`developer-identity.md` §5). ⛔⛔★★★ **والهويةُ البصرية كلُّها مشتقّةٌ من شعار العميل بـ`DS-002`** (`AM-007` · **زيتوني/طيني/ذهبي على ورقيٍّ دافئ**) — ★ **قرارٌ مسجَّلٌ في `design-source-selection.md`** ⛔ **لا ذوقٌ في شاشة**، ★ **والطبقةُ الأولية ممنوعةٌ خارج `design_tokens.dart`** ⟵ **تحرسها بوابةٌ آلية.**
- ★★ **وضعٌ واحد فقط: الفاتح** (`AM-003`) — ⛔ **لا `ThemeMode` ولا `darkTheme` ولا قراءة لتفضيل النظام ولا قيمة مشروطة بوضع** · ★ **وآلية الثيم باقية بنيوياً بمجموعة قيم واحدة** · ★ **والقيم الداكنة الملغاة في `docs/99-archive/2026/` سجلاً تاريخياً** ⛔ **لا تُستورَد منه قيمة**.
- ★★ **لغةٌ واحدة: العربية باتجاه RTL ثابت** (`AM-003`) — ⛔ **لا إعداد لغة ولا مبدِّل ولا `Locale` غير `ar` ولا نص واجهة لاتيني** · ★ **والأرقام لاتينية (0-9) قيمةً وحيدة ثابتة** ⛔ **لا خياراً** · ★ **وقواعد جودة العربية باقية كاملة** (أرقام جدولية · ضبط اتجاه الرقم داخل الجملة · تاريخ مقروء · سطر ≥ 1.6 · تباعد = 0).
- ⛔⛔★★ **وإنشاءُ رأس الجونية يشترط `sackCreate`** (`FR-M7` §5 · `UC-001` · `IQ-021`) — ★ **مفتاحٌ تاسع مستقل في §2.2** (الكتالوج **85 مفتاحاً**) · ⛔ **ولا يُغني عنه مفتاحٌ حقلي** (`sackLinesEnter` وأخواتُه **مساراتٌ تقتصر على حقلٍ واحد**) ⛔ **ولا `sackView`** — **صلاحيةُ قراءة**.
- ⛔⛔★★★ **ولا يُستنتَج وزنُ حبةِ نوعٍ وزني من الوزن الكلي في أي حال إطلاقاً** (`FR-M7-14` · `E-08`) — ★ **والحالة تُقرَّر من `ItemNature` لا من امتلاء الحقول** · ★ **والوزن الضائع لا يُسجَّل إلا بزرٍّ صريح** (`BR-M7-12`) · ★ **والسكرب يدخل المخزن عند حفظ الرأس قبل أي نوع آخر** (`FR-M7-09`).
- ⛔⛔★★★ **وكل قراءةٍ على مجموعةٍ شرطُ قراءتها يعتمد `resource.data` تُقيّد ذلك الحقل صراحةً** — ★ **مقيسٌ خمساً:** `IQ-024` (`sources`) · `WU-008` (`audit_log`) · [`DEBT-89`] (`outflows` **بـ`ledgerType`**) · `DEBT-40` و⛔⛔ [`DEBT-91`] (`daily_summaries` · `WU-016`): ⟵ **الشرط يُقيَّم على قيود الاستعلام لا على كل مستند**، ⛔ **فاستعلامٌ لا يُقيّده يُرفَض كاملاً ولو ملك القارئُ كلَّ المفاتيح.** ⛔⛔★★★ **وقراءةُ مستندٍ *غائبٍ* بمعرّفه تُرفَض كذلك** — ★ **فالغائبُ بلا `resource` أصلاً**، ⟹ **والغيابُ حالةٌ طبيعية** (يومٌ بلا حركة · ضمارٌ لم يُنشأ): ⛔ **فلا يُقرأ بمعرّفه بل باستعلامٍ مقيَّد يُرجِع صفراً.** ★ **والعلاج تقييدُ الحقل وفهرسٌ ببادئته** ⛔ **لا تخفيفُ القاعدة** — ⚠️★★ **والقارئُ السحابي لا يكشفه: يتخطّى القواعد بحساب خدمة.**
- ⛔⛔★★★ **وكلُّ قائمةِ اختيارِ نوعٍ في عمليةِ صرفٍ تُبنى من أرصدة دفتر اليوم** (`stockOptionsProvider`) ⛔ **لا من كتالوج الأنواع** ([`DEBT-86`] · `ADR-0007` · `ADR-0008`) — ★ **سطرُ الجونية لا سجلَ نوعٍ له**، ⟹ **وبناؤها من الكتالوج يُخفيه من كل قائمة فيُرفَض صرفُه «الكمية غير كافية»** · ★ **والاسمُ المعروض هو المفتاح المركّب** (`ledgerItemDisplayName`) ⛔ **لا `itemName` المجرَّد المخزَّن** · ⛔⛔ **و`sackId` تشتقّه السحابةُ من حركة الدخول في المعاملة** (`ledgerSackIds`) ⛔ **ولا يُصدَّق من الحمولة**: ★ **لا حقلَ له في أي شاشة**، ⟵ **وغيابُه يُسقِط الجونيةَ من إيرادها بصمت.**
- ★ لا تخمّن قاعدة غامضة — أنشئ بنداً في `docs/00-governance/upds-workflow/implementation-questions.md`.
- ⚠️★★ **واستثناءٌ صريح لحجم هذا الملف** (`AM-005`): **250 سطراً مقابل حدٍّ إرشادي 200** (★ **كان 242 قبل إغلاق إعادة التحقق 2026-08-28** — ⟵ **و`+7` صافيةٌ: قسمُ `DEBT-55` مضغوطاً، وقد كُتب في خمسة أسطر لا اثني عشر**) — ★ **بعد حذفِ تكرارٍ فعلي بلغ 19 سطراً** (قسمُ بروتوكول التشغيل كان مكرَّراً حرفياً داخل الكتلة المُدارة وخارجها) ⟵ ★ **والمتبقّي نموٌّ مشروع لا تكرار**: قواعدُ حَوْكمةٍ من الفئة 1 لا تُختصَر بلا فقدِ حارس. ⛔ **ولا يُطبَّق صامتاً** — ★ **ويُعاد النظر فيه عند أول قاعدةٍ متخصصةِ النطاق تصلح لـ`.claude/rules/`.** ✅★★ **وجلسةُ `DEBT-58` (2026-08-28) صافيةُ صفر:** ★ **أُضيفت القاعدة (و) وحُدِّثت العدادات، وقوبل ذلك بإعادةِ سبكِ أقسام `IQ-027` و`WU-006` و`WU-008` بلا حذف حقيقةٍ واحدة** — ⟵ **فالسقف 250 قائمٌ لم يُخرَق.** ✅★★ **وجلسةُ حَوْكمةِ IAM (2026-08-28) صافيةُ صفر كذلك:** ★ **أُضيفت القاعدة (ز) داخل الفقرة نفسها بإعادة سبكها في أحد عشر سطراً كما كانت** — ⟵ **250 سطراً بلا زيادة ولا حذفِ حقيقة** (`git diff --numstat` = `12 12`). ✅★★ **وجلسةُ إغلاق `AM-007` (2026-08-29) صافيةُ −1 — 249 سطراً:** ★ **أُضيفت القاعدة (ح) وصفُّ الهوية البصرية في جدول القراءة وخطُّ العرض الثاني وعدّاداتٌ محدَّثة**، ★ **وقوبل ذلك بدمجِ كتلتَي أمرٍ في واحدة وسبكِ سطرِ بوابات التصميم** — ⛔ **بلا حذفِ حقيقةٍ واحدة.** ✅★★ **وجلسةُ إغلاق `WU-007` (2026-08-29) صافيةُ صفر:** ★ **حُدِّثت العدّادات وسطرُ المرحلة، واستُبدل قسمُ `IQ-027` بقسمِ `WU-007` و`DEBT-61` في العدد نفسِه من الأسطر** ⛔ **بلا حذفِ حقيقةٍ واحدة** — ⟵ **والسقف 250 قائم.** ✅★★ **وجلسةُ `AM-008` (2026-08-31) صافيةُ −2 — 244 سطراً:** ★ **أُضيفت قاعدةُ الشريط العلوي الموحّد وحُدِّثت العدّادات وسطرُ المرحلة وأُضيف مؤشّرُ `amendment-log`**، ★ **وقوبل ذلك بسبكِ قسمِ `DEBT-55` في سطرين لا سبعة** ⛔ **بلا حذفِ حقيقةٍ واحدة.** ✅★★ **وجلسةُ `AM-009` (2026-08-31) صافيةُ +1 — 245 سطراً:** ★ **أُضيف سطرُ تنبيه المحاكي وحدَه، وضُمَّت قاعدتا «لا رجوع ولا خروج» وقسمُ `AM-009` إلى سطورٍ قائمة** ⛔ **بلا حذفِ حقيقةٍ واحدة** — ⟵ **والسقف 250 قائم.** ⛔⛔★★★ **وجلسةُ [`DEBT-86`] (2026-09-02) بلغت السقفَ تماماً — 250 سطراً:** ★ **أُضيفت قاعدةُ «الخياراتُ من الدفتر» سطراً واحداً**، ★ **وقوبلت بدمج سطرَي قسمِ `DEBT-86` في واحد** ⛔ **بلا حذفِ حقيقةٍ واحدة** — ⛔⛔ **ولا هامشَ بعدها:** ⟵ **فأولُ قاعدةٍ تالية تستوجب نقلَ المتخصص إلى `.claude/rules/` لا تجاوزَ السقف.** ✅★★ **وجلسةُ `WU-016` (2026-09-03) صافيةُ −1 — 249 سطراً:** ★ **أُضيف قسمُ `WU-016` وحُدِّثت العدّادات ووُسِّعت قاعدةُ الاستعلام المُقيَّد لتشمل قراءةَ المعرّف الغائب**، ★ **وقوبِل ذلك بسبكِ أقسام §د.3 و`WU-010` و`WU-004`** ⛔ **بلا حذفِ حقيقةٍ واحدة** — ⛔⛔ **والهامشُ سطرٌ واحد.**
- التزم بالمعايير أعلاه حرفياً؛ أي استثناء يُوثَّق صراحةً ولا يُطبَّق صامتاً.

## حالة التنفيذ

**✅★★★ عشرون وحدة منجَزة (20 من 29 · 69.0٪): `WU-000` · `WU-026` ·
`WU-001` … `WU-016` · `WU-027` · `WU-028`.** ⛔ **ولا تحتاج إعادة تحقق.** ⛔⛔★★★ **و`WU-028` كشفها فحصُ «لا متطلب يتيم» لا الخطة**: ★ **`FR-SYS-28`…`30` كانت للمرحلة الأولى بلا وحدةِ عملٍ تحملها** — ⟵ **ولولاه لأُغلقت المرحلةُ ومتطلبٌ منها بلا سطرِ كود.** ★ **وقاعدتُها المتخصصة في** `.claude/rules/developer-identity.md` (`AM-005`).

★ **مقاسة 2026-09-03 بعد `WU-016` — 2395 ناجحاً · ٠ فاشلاً:** **253 حماية**
(+ بوابة التغطية) · **703 سحابة** · **715 نطاق** · **724 تطبيق** (★ **38 بوابةَ تصميم ضمنها**) · ✅ **و`analyze` نظيف في الحزم الثلاث** · ✅ **وبوابةُ النزاهة صفرُ مخالفات.**

### ⛔⛔★★★ `WU-012` — البيع النقدي · **وقاعدةٌ تغيّرت بقرار المالك**

⛔⛔★★★ **`M11` لا يمسّ دفتر المقاوته بحرف** (`FR-M11-03`) — ★ **غيابٌ بنيويٌّ لا شرطٌ يُفحَص**، ⛔ **ولا `pricing/current` هنا** (★ **`ت-12` قاعدةُ التوزيع وحده**). ⛔⛔★★★ **والبيعُ دون الحد الأدنى ممنوعٌ للجميع** ([`CR-007`] ⏳ **ينتظر التوقيع**) — ⚠️ **يخالف نصَّ `FR-M11-05` («إلا بصلاحية»)**: ★ **الكودُ يسبق النصَّ بقرارٍ موثَّق** ⛔ **لا بانحرافٍ صامت** ⟹ **و`cashSaleBelowMinimum` لم يعد يمنح شيئاً** ⛔ **ولم يُحذَف** (81 كما هو). ✅ **و`IQ-035` حُسم: لا إيصالَ إرسالٍ** ([`CR-008`] ⏳).

### ✅★★★ اختبارُ المرحلة الأولى (§د.3) — 2026-09-02 · **نُفِّذ وعطلُه سُدَّت**

★★ **دورةٌ يوميةٌ كاملةٌ على `Pixel_6_API_36`:** `INC` ⟶ `SCK` (★ **`AT-06`: 45.000−6.500−1.200 = 37.300** · **`AT-08`: 100×200جم = 20.000**) ⟶ **تسعير 3/3** ⟶ `DST` ⟶ `RCP` ⟶ **سجل التدقيق** ⟶ **المعلّقة** ⟶ **PDF** ⟶ **التقارير.** ⛔⛔★★★ **وأربعةُ عطلٍ لم يكشفها 2151 اختباراً آلياً:** ✅ **[`DEBT-85`] ضمارٌ يُنشأ عادةً لا يُقبَض منه أبداً** (**62,600 في `dealer_balances` مقابل 350 على الشاشة** — ★ **`settlementStatus` كان يُكتَب في مسار الفائض وحدَه**) · ✅ **[`DEBT-87`] تعديلُ توزيعةٍ يُفرِّغ سعرَها صامتاً** · ✅ **[`DEBT-88`] منسدلٌ يبدو فارغاً والنوعُ مختار** · ✅ **و[`DEBT-86`] أدناه.**
✅★★★ **و[`DEBT-86`] سُدَّ بتشغيلٍ خاصٍّ به — بثلاثة أشطرٍ لا واحد** (⚠️ **رابعُ وجوهِ `DEBT-55`: المفتاحُ المركّب يُنسى في كل طبقةٍ جديدة**): ① ★★★ **`stockOptionsProvider` يبني الخيارات من أرصدة الدفتر** ⟵ **تقرؤه شاشاتُ `M10` و`M11` و`M22`** · ② ⛔⛔★★★ **`ledgerSackIds` تشتقّ `sackId` من حركة الدخول في المعاملة** — ★ **والتطبيقُ لم يُرسِله قطّ**: ⟹ **ففتحُ المفتاح وحدَه كان سيُنتج خروجاً بلا مرجعِ جونية** ⟵ **فتسقط من إيرادها بصمت** · ③ ★★ **`ledgerItemDisplayName` تعرض المركّب** — ⛔ **إذ تُخزَّن الحركةُ بالاسم المجرَّد في `itemName`.** ⛅★★★ **ومقيسٌ حيّاً بالمراجعة `00027-z5h`:** **`DST` صار «2 نوع»** · **`CSH` سكربٌ وزنيٌّ 0.500كجم×250=125** · **`WDR` خمسُ حبات** — ⟹ **وثلاثُ حركاتِ خروجٍ تحمل `sackId`** ⛔ **بلا أن يُرسِله التطبيق** · ★ **و24 اختبارَ ارتداد.**

### ⛔⛔★★★ `AM-012` (2026-09-02) — ستةُ بنودٍ · و`WU-027` قيد التنفيذ

⛔⛔★★★ **شاشةٌ واحدة «التوريد مخزني» بتبويبين حلّت محلّ شاشتَي التوريد** — ★ **بمرشِّح مصدرٍ واحدٍ يقرؤه التبويبان** ⛔ **ولا مرشِّحَ مستقل**، ★ **و«كل المصادر» متاحةٌ فيهما بقرارك** ⟵ **فحقلُ المصدر صار في نموذج رأس الجونية** (نظيرُ `AM-009` ⑤). ⛔⛔★★★ **والإنشاءُ في سجل التدقيق بلا صفوف «قبل/بعد»** — ★ **والحارسُ في `describeAuditChanges`** ⛔ **لا في شاشة**، ★ **وسطرُ الهدف «اسم النوع: الرقم»** · **وبريدُ المُنفِّذ حقلٌ جديد تكتبه السحابة** ⛔ **وغائبٌ في كل قيدٍ أقدم فيُسقَط سطرُه.** ★★ **والتاريخُ في الشريط `DD / MM / YYYY`** ⛔ **و`formatReadable()` لم تُمَسّ** (لها قارئٌ سحابي). ⛔⛔★★★ **و`DEBT-63` قائمٌ:** `Size.fromHeight` **تُنتج `minWidth` لا نهائياً فلا يُخطَّط ابنُ `Row` أبداً** ⛔ **بلا خطأٍ مسموع** — ★ **والصوابُ `Size(minTouch, minTouch)`**، ⚠️ **و`filledButtonTheme` باقٍ عليها بخطرٍ مُعلَن.** ✅★★ **واعتُمدت البواباتُ الثلاث (`CR-011` · `CR-012` · `ADR-0024`) بـ«اعتمدها»** — ⛔⛔ **وليست إذنَ نشرٍ إنتاجي.**

### ✅★★★ `WU-016` (2026-09-03) — ضمار المالك وحركة النقد · **منجَزة**

⛅★★★ **باني الملخصات `buildDailySummaries` داخل الحاوية بعد الالتزام** (⛔ **لا مشغّلَ Eventarc** — نظيرُ `recomputeSackRevenue`) **موصولٌ بستةِ معالِجات** — ⛔⛔★★★ **وعلى يوم *الضمار* في مسارَي القبض والخصم لا على تاريخ السند** (`FR-M15-05` · `AT-49`): ⟵ **فسندُ اليوم لضمار أمس يُعيد بناءَ بطاقةِ أمس**، ★ **وأزواجٌ لا زوجٌ واحد.** ★★ **و`daily_summaries` يُبنى من الصفر كلَّ مرة** ⛔ **ولا يُراكِم** (`PAT-08`) · ★ **ويكتب `owner_ledger_trends` في الحدث نفسِه** (`IQ-030`). ⛔⛔★★ **والنطاقُ يُفحَص في فرعَي `daily_summaries` معاً بعد اليوم** — ★ **وكان الفرعُ التجميعي يتخطّاه** ⟹ **فيُسرِّب أرقامَ كل المصادر لمن نطاقُه مصدر** (`FR-M15-09` · `E-36`) · ⛅ **مقيسٌ حيّاً بالمراجعة `00029-mz5`: `netFinal: 6800`.**

### ✅★★★ `WU-013` و`WU-014` (2026-09-01) — الخصومات والسحبيات · **منجَزتان**

✅★★★ **والقياسُ الحيّ أثبت البندين اللذين لا يُثبتهما اختبار:** `DSC-20260901-0001` ⟹ **قيدٌ دائنٌ بنوع «خصم» و`discountedAmount: 200` و`remaining: 350`** ⛔ **و`settledAmount` لم يُمَسّ** (`ت-04`) · **و`WDR-20260901-0002`** (**سندُ مبالغَ خالص**) · **و`item_daily_balances` = 192 بعد الإلغاء.** ⛅ **ونُشرت `callables:wu014b` ذاتياً بـ`AM-010`.** ⛔⛔★★★ **و`DEBT-77` سُدِّد في طبقته: `readTime` يُملأ من `batchGet` كذلك** (`audited_transaction.dart`) — ⟵ **فكلُّ عمليةٍ بلا استعلامٍ واحد تنجو من الفخّ**، ⛔ **لا باستعلامٍ صوريّ في `outflow_handler`.** ⚠️★★ **ودرسُه `DEBT-37` عاشرَ مرة:** ★ **`AuditedTransaction` لم تكن مُختبَرةً قطّ** ⟹ ⛔ **و`documents.get` لا تحمل `readTime` إطلاقاً.**

### ✅★★ `WU-006` … `WU-009` — التوزيع والضمار والمقبوضات والرقابة

★ **`GR-18` و`E-13` أثرُ المفتاح لا شرطٌ يُفحَص** · ★★ **و`ت-12` مُنفَّذ بأقوى صورة: السعر يُرسَل ويُحتسب كاملاً لمن لا يراه** ⟵ **والحارس على مَن يُغيِّره.**
✅★★★ **وبنودُ المركز المعلّق تُكتَب وتُمحى داخل معاملة مستندها نفسِها** في **أربعة معالِجات** (`M7` · `M9` · `M10`) — ⛔⛔ **و`M22` بلا كاتبٍ بعد** (`WU-014`).
⛔⛔ **و`firestore.rules` لم تُمَسّ بحرف في أيٍّ منها** · ⛅ **ونُشرت خدمة العمليات على التجريبية.**
✅★★★ **واختبارُ المحاكي كشف في كلِّ زيادةٍ ما لم تكشفه الاختبارات الآلية** (`DEBT-40` · `DEBT-41` · `DEBT-61` · `DEBT-68`) — ★ **ولكلٍّ اختبارُ ارتداد الآن.** ⚠️ **وديْنان معلَنان:** `DEBT-42` (**حدُّ 500 قيد**) · `DEBT-43`.

### ✅★★ `WU-010` (2026-08-31) — الإرسال والتصدير · **منجَزة**

★★ **القوالبُ الأربعة ثوابتُ نطاقٍ خالصة** (`FR-M20-06` · نصُّها قرارُ المالك في `IQ-031`)، ★ **والمستندُ المُصدَّر يُبنى بالدوال نفسِها** ⟵ **فيطابق الرسالة رقماً برقم.** ★★ **ومفتاحٌ خامسٌ وسبعون `documentExport`** (`IQ-032`) **وعمليةٌ سحابية `logExport`** — ⛔⛔ **أولُ عمليةٍ تكتب قيداً بلا مستندٍ مصاحب، بعَلَمٍ صريحٍ مُسمّى** ⛔ **لا بإسقاطِ الحارس للجميع** · ⛔⛔ **و`firestore.rules` لم تُمَسّ.**
✅★★★ **وثلاثةَ عشرَ سيناريو §د.2 مقيسةٌ حيّاً** — **ومنها: قيدُ «تصدير» ظاهرٌ بمصدره الصحيح** · ⛔⛔ **ورفضُ التسجيل يُعرَض ولا يُبتلَع ولا قيدَ وهمياً يُكتَب** (**مقيسٌ بقطع الشبكة**). ⛔⛔★★★ **وحدُّ `CR-004` المُعتمَد ضيّقٌ لا يُقاس عليه: اسمُ المصدر يسقط من ترويسة الرسالة وحدها** — ✅ **ويبقى أوّلَ صفٍّ في ترويسة المستند المُصدَّر وفي `sourceId` بكل قيد وفي تقسيم البيانات** (`ADR-0005`) · ★ **ومُختبَرٌ من طرفيه.** ⛔⛔★★★ **وسابعُ أوجه `DEBT-37` هنا: حزمةُ `pdf` تُسقِط فراغَ كلمةٍ عند عكس الاتجاه** ⟵ **«مصدر الاختبارالأول» في مستندٍ يصل يدَ العميل** — ⛔ **وفراغٌ غير فاصل لم يُصلحه (مقيس)**، ✅ **والعلاج صفٌّ بفراغاتٍ نرسمها بنسبةٍ من حجم الخط** (`pdf_tokens.dart`).

### ⚠️★★ أثران معلَنان من `WU-004` — ⛔ لا سهو

⚠️★★ **`DEBT-38`:** ⛔★★ **شطرُ السبب منه سقط بـ`ADR-0020`** — ★ **والباقي شطرُ الصلاحية وحده:** **`enterSackLines` يُغيِّر سطوراً قائمة بلا `sackAmend`.** ✅ **و`enterSackTax` صار له واجهةٌ ومقيسٌ حيّاً** (`WU-015` · `sack_tax_sheet.dart`).

### ✅★★ النشر — التجريبية وحدها (2026-08-26)

★★ **`callables:debt55` · المراجعة `00018-78c` · 100٪ من المرور** (2026-08-28) —
★ **والقواعد منشورةٌ عليها كذلك.** ⛔⛔★★ **ولا تُنشَر هذه الخدمة بـ`--no-allow-unauthenticated`**
إطلاقاً — ★ **تحرسُ نفسَها برمز Firebase**، ⟵ **والراية تُزيل `allUsers` فيسقط التطبيق كلُّه.**

⛔⛔★★ **والإنتاج لم يُنشَر عليه شيء — مقيسٌ لا سهو** (واجهة Cloud Run
معطَّلة عليه). ★ **وأربعة شروط تسبقه:** **App Check** ⟵ **الخدمة** ⟵
**إقلاع المالك بمعرّفه هو** ⟵ **ثم القواعد.**

### ⛔⛔ درسٌ مُلزِم — عطلان لم يكشفهما 581 اختباراً

**`DEBT-24`** (كل صلاحية تُقرأ صفراً) · **`DEBT-25`** (`500` خام) — ✅ **سُدِّدا**
⟵ ⛔★★ **ولا يُفترَض شكلُ مخرجات مكتبة — يُقاس.** ★★ **وأُلحِق بهما (2026-08-26)
`sourceDocType` المحفور** الذي وَسَمَ حركاتِ الجواني «وارداً عدداً» حين صار
للدفتر كاتبٌ ثانٍ — ⟵ ⛔ **ولا يُفترَض ثابتٌ لأن كاتبه اليوم واحد.**

⛔⛔★★★ **و`DEBT-37` ثالثتُها** (`pieceWeightOrigin` «يدويٌّ» دائماً) ⟵ ⛔★★ **فاختبارُ الطبقة لا يُغني عن اختبار ما يعبر بينها** · **`DEBT-40`** (قراءةُ مستندٍ غائبٍ تُرفَض لأن شرطها يعتمد `resource.data`) · **`DEBT-41`** (`double` مجرَّدٌ في قيدٍ أسقط المعاملة) · **`DEBT-61`** (المتبقي لا يُبَثّ من مستندٍ فرعي).
⛔⛔★★★ **وسادستُها (2026-08-30 · `WU-010`): `https://wa.me` يجعل `canLaunchUrl` تصدُق أبداً** ⟵ **فيُفتَح المتصفّحُ بدل واتساب ولا يقع فرعُ «غير مثبَّت» قطّ** — ✅ **والصواب مخطَّط `whatsapp://`** · ★★ **وخامستُها `DEBT-68`:** ★ **كتابةُ مزوّدٍ داخل `initState` تُسقِط الشاشة كاملةً** (`Tried to modify a provider while the widget tree was building`) — ⛅ **كشفها المحاكي وحده بعد 1594 اختباراً ناجحاً**، ✅ **والعلاج `addPostFrameCallback`** ⛔ **لا كتابةٌ في دورة حياة الويدجت.** ⛔⛔★★★ **وسابعتُها [`DEBT-90`] (2026-09-03 · `WU-015`): `FutureProvider` قراءةٍ عند الطلب بلا `autoDispose` يُخزِّن نتيجتَه بمفتاح العائلة** ⟹ **فأعاد تفكيكاً قديماً بعد توزيعةٍ حقيقية: الإجمالي 20000 والورقةُ تقول «لم يخرج منها شيء بعد»** — ⛔⛔ **مخالفةٌ لـ`ADR-0008`**، ★ **والقاعدة: كلُّ قراءةٍ عند الطلب `autoDispose`** ⛔ **ولا `autoDispose` واحدةٌ في `lib/` قبل اليوم.**

⚠️ **و`PermissionGate` إخفاءٌ لا حماية** — ★ **كل استعمال له يقابله شرط في
`firestore.rules` مُختبَرٌ على المحاكي** (`RISK-02`) · ★ **و`QTMS_OWNER_UID`
لكل بيئة بمعرّفها هي** ⛔ **وخلطُه تصعيدُ امتياز.**

الحالة الدقيقة: `docs/00-governance/upds-workflow/implementation-plan.md`
والتقرير: `docs/00-governance/upds-workflow/implementation-report-2026-08-30-run12.md`
★ **وآخرُ تعديلٍ مطلوب: `AM-012`** في `docs/00-governance/upds-workflow/amendment-log.md`
⚠️⚠️★★★ **وشاشاتُ `AM-009` الخمس لم تُختبَر على المحاكي بعد** — ★ **بالآلي وحده** (§د).

<!-- UPDS-DOCGEN:END -->

<!-- USER-ADDED (خارج نطاق upds-docgen — لا تُحرَّر ولا تُحذَف آلياً بأي تحديث للإضافة؛ فقط بطلب صريح من مالك المشروع) -->

## بروتوكول تشغيل إضافي لـ /upds-docgen:implement

عند تنفيذ `/upds-docgen:implement` في هذا المشروع تحديداً، اقرأ **أولاً
وقبل أي خطوة أخرى** — بالإضافة إلى كل ما يطلبه `SKILL.md` نفسه، لا بدلاً
عنه —:

`docs/00-governance/upds-workflow/implementation-run-protocol.md`

يحوي **سبع** قواعد تشغيل اعتمدها المالك لهذه المهارة هنا تحديداً: (أ) تسلسل
تشغيلات بالمرحلة الكاملة لا بالوحدة، (ب) حدود اعتماد ذاتي موسّعة للقرارات
التنفيذية الروتينية مع إبقاء `IQ` وكل بوابات UPDS-05 بلا تغيير، (ج) تسليم
فوري لأوامر البيئة المتعذّرة بدل التجريب، (د) **اختبار إلزامي على المحاكي
`Pixel_6_API_36`** لكل زيادة قبل تعليمها منجَزة، ثم شاملٌ للمشروع،
★ **(و) ⛔⛔ لا ادّعاء توثيقيّ مجرَّد**: كل قولٍ بأن أمراً
سُجِّل أو وُثِّق يُرفَق بمسار الملف ورقم السطر أو `diff` فعلي، ⛔⛔★★ **و(ز)
النشرُ وصلاحياتُ IAM حصرُ المالك — على الإنتاج وحده** (`AM-010` · 2026-09-01):
⛅ **فأوامرُ التجريبية تُنفَّذ ذاتياً وتُسجَّل بمخرَجها**، ⛔⛔ **ويقف كلُّ ما يمسّ
`qtms-orimind-c001` حتى تصل كلمةُ `اعتمد-النشر-الإنتاجي`** — ★ **وعند الشكّ في
البيئة يُعامَل إنتاجاً**، ⛔⛔★★★ **و(ح) أي تشغيل يمسّ شاشة يقرأ `18-ux-ui/design-system.md`
و`design-tokens.md` و`ui-guidelines.md` أولاً** (`DS-002` · `AM-007`) ⛔ **لا
`module-design` وحده.** **ولا تخالف ممنوعات UPDS-05 ولا تُضعف بوابة التحقق ولا `IQ` — بل تزيدها شرطاً.**

<!-- /USER-ADDED -->