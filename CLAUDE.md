<!-- UPDS-DOCGEN:BEGIN — كتلة يديرها upds-docgen آلياً. لا تُحرِّرها يدوياً؛ حرِّر مستندات docs/ وأعد التشغيل. -->

## سياق المشروع (يديره upds-docgen)

**المشروع:** QTMS — نظام محاسبي لإدارة المخزون والذمم والنقد في تجارة القات، على أندرويد بقاعدة سحابية مُدارة بلا خادم وسيط.
**المرحلة:** التنفيذ. التوثيق 100٪ · 24 ADR معتمداً · 29 وحدة عمل، **20 منجَزة** (`WU-000`،`WU-026`،`WU-001`…`WU-016`،`WU-027`،`WU-028` — 69٪) · البوابة الآلية: **2395 ناجحاً، 0 فاشلاً** (253 حماية + بوابة تغطية · 703 سحابة · 715 نطاق · 724 تطبيق منها 38 بوابة تصميم) · `analyze` نظيف في الحزم الثلاث · بوابة النزاهة صفر مخالفات.
**محجوز حالياً:** `IQ-040` يحجب `WU-017` (39/40 — مفتاحا تفويض كشف الحساب، `FR-M17` §3).
**مفتوح ومؤجَّل:** `DEBT-66`،`DEBT-67`،`DEBT-92` (تفكيك أرقام البطاقة · مشاركة ملخص اليوم) — مؤجَّلان إلى `WU-018` بقرار معلَن.
**المرحلة الأولى:** مغلقة رسمياً (2026-09-02)، بوابتها 40/40 بدليل مسمّى. اختبارها التكاملي على المحاكي كشف 4 أعطال — كلها سُدّت ومُثبَتة حياً. تفاصيل السرد الكامل والمراجعات: `docs/00-governance/upds-workflow/claude-md-archive.md`.
**منهجية التطوير:** تزايدية (وحدة العمل = شريحة رأسية كاملة تُنتج قدرة قابلة للاستخدام، لا طبقة أفقية) → `docs/21-project-management/development-methodology.md`

## الأوامر الفعلية

**قواعد الحماية** (بلا سحابة وبلا إنترنت):
```bash
cd test/security-rules && npm test
```
بوابتان: تغطية الصلاحيات (تحليل نصي) ثم 236 اختبار سلوك على المحاكي. يحتاج Java 21+. على Windows إذا اكتُشف `JAVA_HOME` يفشل الشق الثاني بـ`'firebase' is not recognized` (`DEBT-60`) — البديل: `firebase emulators:exec --only firestore --project demo-qtms`.

**ثلاث حزم Dart** (`ADR-0012`) — الأخيرتان بلا اعتماد على `flutter`:
```bash
cd packages/qtms_domain && dart analyze && dart test   # طبقة النطاق — 493 اختباراً
cd functions          && dart analyze && dart test   # العمليات السحابية — 506 اختباراً
```

**مشروع Flutter:**
```bash
flutter pub get
flutter analyze        # تحذير = خطأ
flutter test
flutter test test/core/design/design_gates_test.dart   # بوابات التصميم وحدها (28 اختباراً)
```

**أيقونة التطبيق مولَّدة من شعار العميل** — لا تُحرَّر `mipmap-*` يدوياً:
```bash
flutter build apk --flavor production   # يحتاج شرطين، انظر أدناه
dart run flutter_launcher_icons         # فقط عند تغيّر assets/icons/
```
شرطا البناء: ① `android/app/google-services.json` (خارج المستودع عمداً) ② توجيه `TEMP` خارج `%LOCALAPPDATA%` وإلا فشل Gradle بـ`Unable to establish loopback connection` (`DEBT-11`). التفاصيل: `docs/07-development/build-and-run-instructions.md` §2.1، §3.

## معرّفات المشروع المعتمدة

| البند | القيمة |
|---|---|
| مشروع الإنتاج | `qtms-orimind-c001` |
| المشروع التجريبي | `qtms-orimind-master` |
| موقع قاعدة البيانات | `me-central1` (نهائي — `me-central2` لا تحمل العمليات السحابية، `ADR-0014`) |
| معرّف التطبيق | `dev.orimind.qtms.c001` · التجريبي `.staging` |
| الاسم الظاهر | «وكالة محمد المحامي» · التجريبي «… — تجريبي» — بلا بادئة `QTMS` (`AM-002`)، مصدره `appDisplayName` و`app_name` معاً بحارس آلي |
| الخطّان المعتمَدان | **IBM Plex Sans Arabic** — الجسم والجداول وكل رقم (اللاتينية ثابتة العرض) · **Tajawal** (800/900) — نص العناوين الحرفي وحده، بلا رقم به (`DS-002`،`AM-007`) — `docs/19-assets/fonts/README.md` §4 |
| القواعد المنشورة | التجريبية = إصدار المستودع (37799 بايت، `WU-002`) · الإنتاج = إصدار `WU-026` (36497 بايت) عمداً — لا خدمة عمليات هناك |
| خدمة العمليات | `qtms-callables` — حاوية واحدة توجّه بالمسار (`IQ-019`)، التجريبية فقط، 47 عملية منشورة — لا تُنشَر بـ`--no-allow-unauthenticated` أبداً |

معرّفا حساب المالك والحالة التفصيلية: `docs/11-devops-and-infrastructure/environments.md` §1.3

## اقرأ هذه قبل تعديل أي كود

| الموضوع | المستند |
|---|---|
| كل المعادلات المالية | `docs/04-design/design-overview.md` §2 |
| خطة وحدات العمل وحالتها | `docs/00-governance/upds-workflow/implementation-plan.md` |
| أسلوب الكود والتسمية | `docs/04-design/coding-standards.md`، `docs/04-design/naming-conventions.md` |
| مبادئ العمارة | `docs/03-architecture/architecture-principles.md` |
| تصميم الوحدة المعنيّة | `docs/04-design/module-design/<module>-design.md` |
| جدول تتبع الصلاحيات | `docs/09-security/security-requirements.md` |
| **أي شاشة أو مكوّن عرض** — قبل `module-design` لا بعده | `docs/18-ux-ui/design-system.md`، `design-tokens.md`، `ui-guidelines.md` — صفر قيمة لون أو حجم خط أو نصف قطر خارج التوكنز |
| التفرّع ورسائل الـ Commit | `docs/07-development/branching-strategy.md`، `commit-message-conventions.md` |
| تعريف "منجَز" | `docs/07-development/definition-of-done.md` |
| إدارة الأسرار | `docs/09-security/secrets-management-policy.md` |
| خارطة الدخول الكاملة | `docs/22-onboarding/developer-onboarding.md` |
| السرد الكامل لتاريخ التنفيذ (WU بالتفصيل، جنائيات الأعطال، المراجعات) | `docs/00-governance/upds-workflow/claude-md-archive.md` |

## القرارات المعمارية المعتمدة (لا تُعدَّل — الانحراف يحتاج ADR جديداً يشير للقديم)

- `ADR-0001` بلا خادم وسيط: أندرويد + قاعدة مستندية سحابية مباشرة
- `ADR-0002` قواعد الحماية هي طبقة التفويض الوحيدة؛ أي قاعدة عمل بلا مقابل فيها = غير منفَّذة
- `ADR-0003` اتصال إلزامي، والتخزين المؤقت المحلي معطَّل عمداً لمنع وهم الحفظ الناجح
- `ADR-0004` تعديل مباشر بقيد تدقيق مفروض (شرط "لا تعديل بلا سبب" أُلغي بـ`ADR-0020`)
- `ADR-0008` الرصيد مشتقّ من الدفتر دائماً؛ الملخصات مبنية مسبقاً ولا تُقرأ كمصدر حقيقة
- `ADR-0009` أربع طبقات بنطاق نقي على ست قدرات؛ العمليات السحابية بلغة النطاق نفسها
- `ADR-0010` إدارة حالة تصريحية بحقن اعتمادية صريح؛ الشاشة بلا منطق أعمال إطلاقاً
- `AM-008` شريط علوي موحّد `QtmsTopBar` في كل شاشة — لا `AppBar` يدوي؛ حالة الاتصال تُقاس من بثّ Firestore (`isFromCache`) لا من واجهة شبكة الجهاز؛ لا زر رجوع ولا أيقونة خروج (الخروج من ورقة جلسة تفتحها الصورة الرمزية) — 6 بوابات آلية تحرسه
- `ADR-0011` عزل الحقول المالية في مستندات فرعية بشرط قراءة مستقل
- `ADR-0012` طبقة النطاق في حزمة Dart نقية مستقلة (`packages/qtms_domain/`) يشاركها التطبيق والسحابة
- `ADR-0013` الكتابة عبر دالة سحابية مستدعاة تكتب المستند وقيد التدقيق في معاملة واحدة
- `ADR-0014` موقع القاعدة `me-central1`
- `ADR-0015` كل مبلغ عدد صحيح بالريال — بلا كسور ولا أصغر وحدة
- `ADR-0016` الصلاحيات تخرج من رمز الدخول إلى `users/{userId}`
- `ADR-0017` حالة الإيداع البنكي معزولة في `receipts/{id}/deposit/current`
- `ADR-0019` موضع تقريب ثالث: قيمة السطر الوزني (يوسّع `ADR-0015` ولا يلغيه)
- `ADR-0018`→`ADR-0020` السبب النصي اختياري في كل العمليات بلا استثناء (يلغي شرط `ADR-0004` الثاني)
- `ADR-0023` لا طباعة حرارية ولا بلوتوث — المخرَج PDF ومشاركته ورسالة نصية (قرار نطاق صريح)
- `ADR-0024` الدخول بالبصمة عبر بيانات اعتماد في `Android Keystore` بأحد عشر شرطاً مجتمعة: لا تُقرأ الخزنة بلا مصادقة بيومترية، تُمسَح فوراً عند الإطفاء وأول فشل، لا صلاحية تُشتقّ منها ولا التفاف على تعطيل الحساب

## قواعد حَوْكمة ملزِمة

1. لا تُعدِّل `docs/02-requirements/srs.md` بعد اعتماده — عبر `change-requests/CR-*.md` فقط.
2. لا سرّ في الكود أو الإعدادات — متغيرات بيئة فقط. **استثناء وحيد موثَّق (`AM-004`):** حساب QA كامل الصلاحيات على التجريبية `qtms-orimind-master` وحدها؛ يُسجَّل دخوله آلياً بتمرير مسار الملف السرّي أو اسم متغيّره فقط — القيمة نفسها لا تمرّ في السياق أو التفكير أو المخرجات أبداً؛ لا نظير له على الإنتاج. كل مفتاح صلاحية جديد يُسنَد له تلقائياً في نفس التشغيل.
3. لا تحذف ملفات نهائياً — النقل إلى `docs/99-archive/` وبطلب صريح فقط.
4. لا حذف بيانات ولا تعديل سجل التدقيق داخل النظام — لأي مستخدم بمن فيهم المالك. الاستثناء الوحيد المعتمد: قالب دور غير مُسنَد (`IQ-018`)، و"غير مُسنَد" يُقاس باستعلام فعلي على `users` داخل معاملة الحذف لا بحقل مخزَّن.
5. كل مبلغ `int` بالريال — بلا فاصلة عائمة ولا نوع عشري. التقريب في ثلاثة مواضع فقط (ضريبة الجونية، متوسط سعر الكيلو، قيمة السطر الوزني)؛ إدخال المستخدم يُرفض لا يُقرَّب. لا جمع بين الحبات والأوزان، ولا خلط بين `stockDate` و`entryDate`.
6. لا يُنشَر شيء على الإنتاج بلا أن يكتب المالك حرفياً **"اعتمد-النشر-الإنتاجي"** في نفس المحادثة، مهما نجحت الفحوص. `uat-signoff.md` و`release-checklist.md` تُجهَّز ذاتياً بلا حقل التوقيع — التجهيز ليس اعتماداً.
7. لا تكرار لأي معادلة خارج طبقة النطاق — لا في شاشة ولا مستودع ولا عملية سحابية.
8. منح الصلاحيات من العملية السحابية حصراً — لا من التطبيق مهما كان دور المستخدم.
9. تعطيل مقاوته رصيدُه ≠ 0 يشترط ثلاثةً معاً (`FR-M4-09`،`IQ-020`): مفتاح `dealerDisableWithBalance` المستقل (لا `dealerWrite`) + إقرار نصي غير فارغ + رصيد مقيس من `dealer_balances` داخل المعاملة نفسها لا من الجهاز. رصيد الصفر لا يشترط المفتاح.
10. الخصم يُدخَل كخصم لا كمبلغ واصل؛ بطاقة ضمار المالك تعرض "قبل الخصم" و"بعد الخصم"، والصافي النهائي على الثاني.
11. لا تُعلَن أي وحدة عمل منجَزة قبل أن تعمل من طرف إلى طرف بقواعد حمايتها واختباراتها وتوثيقها، ولا بتغطية نصية — التغطية تثبت أن الشرط **مذكور** لا أنه **صحيح**؛ شغّل الاختبارات فعلياً.
12. لا مسار كتابة مباشر للتطبيق بعد `WU-002` — كل مجموعة `allow create, update: if false`؛ القراءة وحدها مباشرة. أي زيادة جديدة تكتب عبر دالة مستدعاة، لا تُفتَح قاعدة كتابة من جديد.
13. لا تُنشَر `firestore.rules` قبل نشر خدمة العمليات الحاملة للعمليات المستدعاة — القواعد تُغلِق المسار القديم فوراً، وعكس الترتيب يترك النظام بلا مسار لكتابة بياناته المرجعية. كذلك لا تُنشَر قبل تنفيذ `docs/13-operations/runbooks/RB-backfill-user-isactive.md` (لأن `perm()` تشترط `isActive`، وعكس الترتيب يقفل المالك خارج نظامه).
14. لا دالة كاتبة تفرض سبباً نصياً على أي عملية (`ADR-0020`) — اختياري إنشاءً وتعديلاً وإلغاءً وإتلافاً وحذفاً. القاعدة الباقية: لا يُعبِّئ التطبيق ولا السحابة سبباً نيابةً عن المستخدم أبداً؛ الفراغ يُقرأ غياباً لا نصاً فارغاً. حقلا `balanceAcknowledgement` و`disableReason` إلزاميّان كما كانا، خارج هذا القرار.
15. هوية العميل من مصدر واحد (`AM-002`): الاسم الظاهر والشعار في `lib/core/design/brand.dart` و`res/values/strings.xml` — لا نص اسم ولا مسار أصل محفور في أي شاشة. الأيقونة وشاشة البداية وشاشة الدخول مساحة العميل حصراً — يُمنع فيها شعار جهة التطوير (انظر قسم "بصمة جهة التطوير" أدناه). الهوية البصرية كلها مشتقّة من شعار العميل بـ`DS-002` (`AM-007` — زيتوني/طيني/ذهبي على ورقي دافئ)، مسجَّلة في `design-source-selection.md` لا ذوقاً في شاشة؛ الطبقة الأولية ممنوعة خارج `design_tokens.dart` وتحرسها بوابة آلية.
16. وضع واحد فقط: الفاتح (`AM-003`) — لا `ThemeMode` ولا `darkTheme` ولا قراءة لتفضيل النظام؛ آلية الثيم باقية بنيوياً بمجموعة قيم واحدة، والقيم الداكنة الملغاة أرشيف تاريخي في `docs/99-archive/2026/` لا يُستورَد منه شيء.
17. لغة واحدة: العربية باتجاه RTL ثابت (`AM-003`) — لا إعداد لغة ولا `Locale` غير `ar` ولا نص لاتيني في الواجهة؛ الأرقام لاتينية (0-9) ثابتة لا خياراً؛ قواعد جودة العربية باقية كاملة (أرقام جدولية، ضبط اتجاه الرقم داخل الجملة، تاريخ مقروء، سطر ≥1.6، تباعد=0).
18. إنشاء رأس الجونية يشترط `sackCreate` (مفتاح مستقل، `FR-M7` §5) — لا يُغني عنه مفتاح حقلي ولا `sackView` (صلاحية قراءة فقط).
19. وزن حبة نوع وزني لا يُستنتَج من الوزن الكلي في أي حال (`FR-M7-14`) — الحالة تُقرَّر من `ItemNature` لا من امتلاء الحقول؛ الوزن الضائع لا يُسجَّل إلا بزر صريح؛ السكرب يدخل المخزن عند حفظ الرأس قبل أي نوع آخر.
20. كل قراءة على مجموعة شرطُ قراءتها يعتمد `resource.data` يجب أن تُقيّد ذلك الحقل صراحةً في الاستعلام نفسه — استعلام غير مقيَّد يُرفَض كاملاً ولو ملك القارئ كل المفاتيح. قراءة مستند **غائب** بمعرّفه تُرفَض كذلك (الغياب حالة طبيعية) — لا تُقرأ بمعرّفها بل باستعلام مقيَّد يُرجِع صفراً؛ العلاج تقييد الحقل وفهرس ببادئته، لا تخفيف القاعدة.
21. كل قائمة اختيار نوع في عملية صرف تُبنى من أرصدة دفتر اليوم (`stockOptionsProvider`) لا من كتالوج الأنواع؛ الاسم المعروض هو المفتاح المركّب (`ledgerItemDisplayName`) لا `itemName` المجرَّد؛ `sackId` تشتقّه السحابة من حركة الدخول في المعاملة (`ledgerSackIds`) — لا يُصدَّق من الحمولة، ولا حقل له في أي شاشة.
22. لا تخمّن قاعدة غامضة — أنشئ بنداً في `docs/00-governance/upds-workflow/implementation-questions.md`.
23. `PermissionGate` إخفاء لا حماية — كل استعمال له يقابله شرط في `firestore.rules` مُختبَر على المحاكي (`RISK-02`). `QTMS_OWNER_UID` لكل بيئة بمعرّفه هو — خلطه تصعيد امتياز.
24. التزم بالمعايير أعلاه حرفياً؛ أي استثناء يُوثَّق صراحةً ولا يُطبَّق صامتاً.

## بصمة جهة التطوير (النطاق: `lib/core/identity/**`، أي شاشة تعرض الإسناد، `README.md`، `LICENSE`، `pubspec.yaml`)

**القرار المعتمد `DI-001` = `discreet`:** حاضرة لمن يبحث عنها، غائبة عمّن لا يبحث — ومساحة العميل ملك العميل. العقد الكامل: `docs/19-assets/developer-identity.md`.

**مصدر حقيقة واحد ولا قيمة تواصل محفورة:**
`assets/branding/developer-identity.json` (المصدر الوحيد) ← يقرؤه `lib/core/identity/developer_identity.dart` (طبقة الوصول الوحيدة) ← يعرضه `lib/core/identity/developer_attribution.dart` (المكوّن الوحيد). **لا بريد ولا هاتف ولا موقع مكتوب نصاً في أي شاشة أو مكوّن أو اختبار أو مستند** — الشاشة تستدعي الطبقة الدلالية ولا تلمس القيمة الأولية أبداً.

**المواضع المعتمدة (خمسة، ولا سادس):**

| # | الموضع | الحالة |
|:-:|---|---|
| 1 | شاشة «حول التطبيق» — قسم سفلي بعد فاصل | ✅ `about_screen.dart` |
| 2 | تذييل الإعدادات بجوار رقم الإصدار | ✅ `settings_screen.dart` |
| 3 | حقل المؤلّف في `pubspec.yaml` | ⛔ لا ينطبق — `author` مهجور في Dart وينتج تحذيراً يُعامَل خطأً |
| 4 | قسم «طُوِّر بواسطة» في `README.md` | ✅ بالاسم وحده، بلا قيمة تواصل |
| 5 | سطر الحقوق في `LICENSE` | ⏳ معلَّق بـ`IQ-039` — لا ملف ترخيص بعد |

**ممنوعات مطلقة:** لا ترويسة HTTP تكشف جهة التطوير (تُرفَض ولو طُلبت صراحةً) · لا شعار مطوّر في أيقونة التطبيق أو الشريط العلوي · لا شعار مطوّر كعنصر رئيسي في شاشة البداية أو الدخول · لا ظهور للبصمة أكثر من مرة في الشاشة الواحدة · لا شاشة مستقلة اسمها «المطوّر» · **لا بصمة في أي مخرَج مُصدَّر** (PDF، رسالة — كشف الحساب وسند القبض مستندات يسلّمها المالك لمقاوته، وليست مساحة إعلانية) · لا حفر لأي قيمة تواصل نصياً · لا بصمة تعترض المستخدم بنافذة أو إشعار.

**التبعية البصرية (منفَّذة في الكود لا موصوفة):** الشعار ≤32٪ من شعار العميل (`developerMarkRatio = 0.32`، مشتقّ لا محفور) · لون الإسناد `textSecondary` لا الأساسي · لون العلامة محايد ثانوي عبر `ColorFiltered` · الدرجة `caption` (أصغر درجة متاحة) · مرة واحدة أسفل المحتوى بعد `Divider` يسبق المحتوى دائماً. الاختبار العملي: لو نظر مستخدم للشاشة ثانية واحدة يجب أن يرى هوية المنتج لا بصمة المطوّر.

**ثلاث بوابات آلية تحرس هذا كلّه — لا تُضعَّف أي منها لتمرير كود، العلاج في الكود لا في الحارس:**

| # | البوابة | ما تفحصه |
|:-:|---|---|
| ① | `test/core/identity/developer_identity_test.dart` | بحث نصي قاطع عن البريد والهاتف والموقع في `lib/`،`test/`،`assets/`،`packages/`،`functions/lib/`،`README.md`،`LICENSE`،`CLAUDE.md` |
| ② | `test/core/design/design_gates_test.dart` | لا إسناد في شريط علوي ولا دخول ولا بداية ولا مُصدَّر PDF · ولا يقرأ ملف الهوية إلا طبقة الوصول |
| ③ | `test/capabilities/master_data/presentation/about_screen_test.dart` | مرة واحدة فقط · اللون والدرجة والترتيب مقيسة فعلاً |

## دروس هندسية ملزِمة (مكثّفة — السرد والأرقام الكاملة في `claude-md-archive.md`)

- لا يُفترَض شكل مخرجات مكتبة — يُقاس (`DEBT-24`،`DEBT-25`).
- لا يُفترَض ثبات قيمة/نمط بعد إضافة كاتب ثانٍ لنفس البيانات (`sourceDocType`).
- اختبار الطبقة لا يُغني عن اختبار ما يعبر بين الطبقات (`DEBT-37`).
- قراءة مستند غائب بمعرّفه تُرفَض لأن شرط القاعدة يعتمد `resource.data` (`DEBT-40`).
- `double` مجرَّد في قيد يمكن أن يُسقِط المعاملة كاملة (`DEBT-41`).
- المتبقي يُبثّ من مستنده الفرعي الفعلي، لا يُستنتَج (`DEBT-61`).
- `canLaunchUrl` تصدُق دائماً على `https://wa.me` ولو واتساب غير مثبَّت — يفتح المتصفح بدلاً منه؛ الصواب مخطَّط `whatsapp://`.
- كتابة مزوّد داخل `initState` تُسقِط الشاشة كاملة — استخدم `addPostFrameCallback` لا كتابة في دورة حياة الويدجت (`DEBT-68`).
- كل قراءة عند الطلب تحتاج `autoDispose` — غيابه يُبقي نتيجة قديمة مخزَّنة بمفتاح العائلة فيخالف `ADR-0008` (`DEBT-90`).
- لا يكتمل إغلاق أي وحدة عمل بلا اختبار فعلي على المحاكي — تغطية 2151 اختباراً آلياً لم تكشف 4 أعطال كشفها المحاكي وحده (§د.3).

## حالة التنفيذ — ملخص

20 من 29 وحدة عمل منجَزة (69٪) — `WU-000`،`WU-026`،`WU-001`…`WU-016`،`WU-027`،`WU-028`. `WU-012`: البيع دون الحد الأدنى ممنوع للجميع حالياً — مفتاح `cashSaleBelowMinimum` قائم في الكود لكن لا يمنح شيئاً بانتظار توقيع `CR-007`. `WU-017` محجوب بـ`IQ-040`. `DEBT-66`،`DEBT-67`،`DEBT-92` مؤجَّلة إلى `WU-018`. الإنتاج لم يُنشَر عليه شيء بعد — أربعة شروط تسبقه: App Check ← الخدمة ← إقلاع المالك بمعرّفه هو ← القواعد.

الحالة الدقيقة: `docs/00-governance/upds-workflow/implementation-plan.md` · التقرير: `implementation-report-2026-08-30-run12.md` · آخر تعديل: `amendment-log.md` (`AM-012`) · سجل الجلسات والأرقام الكاملة: `docs/00-governance/upds-workflow/claude-md-archive.md`.

<!-- UPDS-DOCGEN:END -->

<!-- USER-ADDED (خارج نطاق upds-docgen — لا تُحرَّر ولا تُحذَف آلياً بأي تحديث للإضافة؛ فقط بطلب صريح من مالك المشروع) -->

## بروتوكول تشغيل إضافي لـ /upds-docgen:implement

عند تنفيذ `/upds-docgen:implement` في هذا المشروع، اقرأ **أولاً وقبل أي خطوة أخرى** — إضافةً لكل ما يطلبه `SKILL.md` نفسه، لا بدلاً عنه —: `docs/00-governance/upds-workflow/implementation-run-protocol.md`.

يحوي سبع قواعد تشغيل اعتمدها المالك لهذه المهارة هنا تحديداً: (أ) تسلسل تشغيلات بالمرحلة الكاملة لا بالوحدة · (ب) حدود اعتماد ذاتي موسّعة للقرارات التنفيذية الروتينية مع إبقاء `IQ` وكل بوابات `UPDS-05` بلا تغيير · (ج) تسليم فوري لأوامر البيئة المتعذّرة بدل التجريب · (د) اختبار إلزامي على المحاكي `Pixel_6_API_36` لكل زيادة قبل تعليمها منجَزة، ثم شامل للمشروع · (و) لا ادّعاء توثيقي مجرَّد — كل قول بأن أمراً سُجِّل أو وُثِّق يُرفَق بمسار الملف ورقم السطر أو `diff` فعلي · (ز) النشر وصلاحيات IAM حصر المالك على الإنتاج وحده (`AM-010`) — أوامر التجريبية تُنفَّذ ذاتياً وتُسجَّل بمخرَجها، ويقف كل ما يمسّ `qtms-orimind-c001` حتى تصل كلمة **"اعتمد-النشر-الإنتاجي"**؛ عند الشك في البيئة تُعامَل إنتاجاً · (ح) أي تشغيل يمسّ شاشة يقرأ `18-ux-ui/design-system.md` و`design-tokens.md` و`ui-guidelines.md` أولاً، لا `module-design` وحده.

لا تخالف ممنوعات `UPDS-05` ولا تُضعف بوابة التحقق ولا `IQ` — بل تزيدها شرطاً.

## قاعدة صيانة هذا الملف (اعتُمدت 2026-09-04 — تحلّ محل قاعدة `AM-005` القديمة)

**القرار:** `CLAUDE.md` و`.claude/rules/developer-identity.md` دُمجا في ملف واحد؛ مجلد `.claude/rules/` أُزيل. السرد التاريخي التفصيلي (قصة كل وحدة عمل، جنائيات الأعطال، أرقام المراجعات، سجل تعديلات هذا الملف عبر الجلسات) نُقل إلى `docs/00-governance/upds-workflow/claude-md-archive.md` ولم يُحذَف — فقط لم يعد يُحمَّل في كل جلسة.

**السبب الفني:** `.claude/rules/*.md` بلا ترويسة `paths:` في الأعلى يُحمَّل بالكامل في كل جلسة تماماً كملف `CLAUDE.md` — فلا يوفّر توكنز بذاته؛ التحميل الشرطي الحقيقي يحتاج ترويسة `paths:` فعلية بأنماط glob. و`/doctor` يحذّر عندما يتجاوز حجم `CLAUDE.md` وحده 40,000 حرف.

**سقف الحجم الجديد:** هدف تشغيلي ~20,000 حرف، وسقف صارم 35,000 حرف لهذا الملف وحده (هامش أمان تحت حد تحذير `/doctor`). عند الاقتراب من السقف: انقل السرد والتفاصيل التاريخية إلى `claude-md-archive.md` بالإشارة فقط — لا تُضغَط القواعد التشغيلية نفسها ولا تُحذَف حقيقة واحدة منها.

**تنبيه لمن يشغّل `/upds-docgen:implement`:** إن كانت أداة `upds-docgen` تُعيد توليد الكتلة المُدارة أعلاه من مولّدها القديم بالصيغة المطوَّلة السابقة، حدِّث قالب التوليد ليطابق هذا المستوى من الإيجاز قبل أول تشغيل تالٍ — وإلا سيعود الانتفاخ مع أول توليد آلي.

<!-- /USER-ADDED -->
