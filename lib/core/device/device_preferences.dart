/// ★★★ **تفضيلاتُ العرض على الجهاز** — `AM-012` §4.4.
///
/// ═══════════════════════════════════════════════════════════════════════
/// ⛔⛔★★★ **ولماذا لا تعيش في `app_settings` — وهو مانعٌ نصّيٌّ لا اختيار:**
///
/// ★ **`FR-M21-03`:** «يُسمح بإنشاء البندين **مرة واحدة فقط ومن المالك
/// حصراً**، **والتعديل والحذف مرفوضان نهائياً لكل المستخدمين بمن فيهم
/// المالك**» — ⟹ ⛔⛔ **فمفتاحُ تبديلٍ هناك لا يُبدَّل أبداً بعد أول حفظ**،
/// ★ **ويصير زرَّاً معطَّلاً إلى الأبد** ⛔ **وهو نقيضُ ما طُلب.**
/// ★ **و`FR-M21-05` يمنع «مفتاحاً ثالثاً في الإعدادات» أصلاً.**
///
/// ★★★ **والصوابُ دلالياً أنها محلية:** ⟵ **تفضيلُ عرضٍ يخصّ من ينظر إلى
/// الشاشة** ⛔ **لا سياسةَ منشأةٍ تُفرَض على كل مستخدميها** — ★ **ولذلك لا
/// حقلَ لها في `users/{userId}` كذلك** (⛔ **ولا كتابةَ سحابيةٍ ولا قاعدةَ
/// حمايةٍ تُمَسّ**).
///
/// ⚠️⚠️ **وأثرُه معلَن:** ★ **التفضيلُ لا يتبع المستخدم إلى جهازٍ آخر** —
/// ⛔ **وهو مقبولٌ لتفضيلِ عرضٍ تجميليّ**، ★ **ونصُّ الطلب نفسُه يسمّيه
/// «تجميلي/عرضي فقط».**
///
/// ★★ **والتخزينُ في الخزنة الآمنة نفسِها لا حزمةٍ ثالثة** — ⛔ **ولا
/// `shared_preferences` لأجل قيمةٍ منطقيةٍ واحدة:** ★ **`dependency-management-policy.md`
/// §1 البند 2 يرفض حزمةً توفّر أقل من ~100 سطر بسيط** ⟵ **والخزنةُ مضافةٌ
/// أصلاً للبصمة** ([`ADR-0024`])، ★ **والتشفيرُ فائضٌ لا ضارّ.**
/// ═══════════════════════════════════════════════════════════════════════
library;

/// ★ تفضيلاتُ العرض المحفوظة على الجهاز.
abstract interface class DevicePreferences {
  /// ★★ **هل يظهر وزنُ الحبة بجانب اسم النوع؟** — `AM-012` §4.4.
  ///
  /// ⛔⛔★★★ **وافتراضُه `false`** — ★ **بنصّ الطلب: «عند التعطيل يظهر اسم
  /// النوع فقط»**: ⟵ **فالحالةُ القائمة اليوم هي الافتراض**، ⛔ **ولا
  /// يتغيّر شكلُ كلِّ شاشةٍ على مستخدمٍ لم يطلب شيئاً.**
  Future<bool> showPieceWeightWithItemName();

  /// ★ يضبط التفضيل — ⛔ **ولا يُقرأ من مكانٍ آخر.**
  Future<void> setShowPieceWeightWithItemName({required bool enabled});
}
