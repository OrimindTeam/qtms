/// ★ **رقمُ إصدار التطبيق المعروض** — `FR-SYS-28` (تذييل الإعدادات).
///
/// ⛔⛔★★ **ومصدرُ حقيقته `pubspec.yaml` وحدَه** — ★ **وهذا الثابتُ صورةٌ منه
/// يحرسها اختبارٌ يقرأ الملفَّ نفسَه** (`app_version_test.dart`): ⟵ **فتعديلُ
/// الإصدار في `pubspec.yaml` بلا تعديلٍ هنا يُسقِط الاختبار**، ★ **والعكسُ
/// كذلك.**
///
/// ★★ **ولماذا ثابتٌ محروسٌ ولا حزمةُ `package_info_plus`:** ⟵ **الحزمةُ
/// تُضيف اعتماديةَ طرفٍ ثالثٍ ومسارَ قناةٍ أصليّةً كاملاً** ⛔ **لأجل سطرٍ
/// واحدٍ في شاشةٍ واحدة**، ★ **والحارسُ يُغني عنها بنفس الضمانة** — ⟵ **وهو
/// نفسُ أسلوبِ حارسِ توكنز `primary` وحارسِ أوزان الخط في هذا المشروع.**
library;

/// ★ الإصدارُ المعروض — **بلا رقم البناء** (`versioning-scheme.md`).
///
/// ⛔ **ولا يُعرَض `+1`** — ★ **رقمُ البناء بيانٌ تقنيٌّ لا يعني المستخدم.**
const String appVersion = '1.0.0';

/// ★ سطرُ الإصدار كما يُعرَض في التذييل.
const String appVersionLabel = 'الإصدار $appVersion';
