name: qtms_domain
description: "طبقة النطاق المشتركة لـQTMS — يستوردها التطبيق والعمليات السحابية معاً."
# ⛔ حزمة داخلية — لا تُنشَر على pub.dev.
publish_to: 'none'
version: 1.0.0

# ★ ADR-0012 القاعدة 1: هذه الحزمة **لا تعتمد `flutter`** ولا أي حزمة منصة
#   سحابية — وهذا يُفحَص **بالبناء لا بالمراجعة**: الحزمة تُصرَّف بـ`dart`
#   وحده، فاستيراد حزمة منصة فيها **يفشل التصريف** بدل أن ينتظر مراجعة بشرية.
#   وهو ما يجعل البند 4 من `coding-standards.md` §5 خطأ تصريف لا ملاحظة مراجعة.

environment:
  sdk: ^3.11.5

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.0
