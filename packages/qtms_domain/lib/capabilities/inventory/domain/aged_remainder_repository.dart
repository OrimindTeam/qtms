/// عقدُ قراءةِ المتبقي المتأخر — **قراءةً فقط** (`WU-019`).
///
/// ⛔★★ **ولا مستودعَ كتابةٍ له إطلاقاً** — ★ **والسببُ بنيوي لا سهو:**
/// `aged_remainders` **`allow write: if false`** في `firestore.rules`،
/// ⟵ **والبندُ *مشتقٌّ* يكتبه راصدُ السحابة داخل معاملة الحركة التي غيّرت
/// الرصيد** (`database-overview.md` §2: «**السحابة فقط**»). ★ **فلا فعلَ
/// مستخدمٍ يكتب بنداً**: **يُصرَّف المخزون فيختفي البند** ⛔ **ولا يُحذف
/// البند فيختفي المخزون.**
///
/// ⚠️ **والاستعلامُ مقيَّدٌ بالمصدر إلزاماً** — ★ **شرطُ القراءة يعتمد
/// `resource.data.sourceId`** (`storedInScope()`): ⟵ **واستعلامٌ لا يُقيّده
/// يُرفَض كاملاً ولو ملك القارئُ نطاقاً شاملاً** (`IQ-024` · `DEBT-40` ·
/// `WU-016`). ⛔ **والعلاجُ تقييدُ الحقل ثم الدمجُ في الذاكرة** — ★ **لا
/// تخفيفُ القاعدة.**
library;

import '../../../core/calendar_day.dart';
import 'aged_remainder.dart';

/// دليلُ المتبقي المتأخر — **تدفّقٌ حيٌّ لا قراءةٌ مفردة**.
abstract interface class AgedRemainderDirectory {
  /// ★★ بنودُ المتبقي في مصدرٍ واحد **قبل [today]** — `FR-M8-09`.
  ///
  /// ⚠️ **و[today] حدُّ الاستعلام لا مرشِّحُ عرض** — ★ **فالبنودُ المتأخرة
  /// وحدَها تصل**، ⛔ **ورصيدُ اليوم لا يُقرأ هنا أصلاً** (`FR-M8-05`).
  Stream<List<AgedRemainderCard>> watchAgedRemainders({
    required String sourceId,
    required CalendarDay today,
  });
}
