# تصميم وحدة: البيانات المرجعية (`master-data`)

| البند | القيمة |
|---|---|
| **الحالة** | ✅ **منفَّذ** — `WU-002` (2026-08-25) |
| **الوحدات المغطّاة** | `M2` المصادر · `M3` الرعية · `M4` المقاوته · `M5` الأنواع · `M21` الإعداد التأسيسي |
| **يعتمد على** | `identity-access` |
| **المتطلبات** | `FR-M2-*` · `FR-M3-*` · `FR-M4-*` · `FR-M5-*` · `FR-M21-*` |

---

## 0. ★★★ مسار الكتابة — **دالة سحابية مستدعاة** (`WU-002`)

> ⚠️⚠️ **قرار تنفيذي موثَّق — يُقرأ قبل أي تعديل على هذه الوحدة.**

★ **الكتابة على `sources` و`suppliers` و`dealers` و`items` و`app_settings`
وسجلات الحراسة الأربعة مغلقة في `firestore.rules`** بـ
`allow create, update: if false` — ⟵ **وتمرّ حصراً بتسع عمليات مستدعاة**
(`api-overview.md` §3.1-د). ✅ **والقراءة لم تُمَسّ** (`ADR-0013` القاعدة 4).

**ولماذا — ثلاثة نصوص مجتمعةً لا تتحقق إلا بهذا المسار:**

| # | النصّ | المصدر |
|:-:|---|---|
| ① | تعديل الاسم أو الرقم أو إلزامية الرعوي **«يُسجَّل بالقيمة قبل وبعد»** | `FR-M2-03` · `FR-M3-13` · `FR-M4-13` |
| ② | **«كتابة السجل تتم عبر السحابة حصراً، والكتابة عليه من التطبيق ممنوعة نهائياً»** | `FR-M18-04` |
| ③ | **المستند وقيده معاً أو لا شيء** — ⛔ ولا مشغّل يكتب القيد بعد الالتزام | `audit-log-design.md` §2 الشرط 3 · `ADR-0013` القاعدة 1 |

⟵ ★ **فالكتابة المباشرة من التطبيق كانت ستُنتج تعديلاً بلا قيد.**

> ⚠️ **وهذا يوسّع ما أغلقه `IQ-009`** (عشر مجموعات تمسّ المال أو المخزون)
> ⛔ **ولا ينقضه:** ذاك حدَّد **الحدّ الأدنى** الواجب إغلاقه، **وهذا يضيف
> مجموعةً سادسةً بمبرِّرها هي**. ✅ **والأثر أقوى لا أضعف:** حارسٌ في
> القواعد **وحارسٌ في الكود** (`ADR-0013` القاعدة 3).

⛔⛔ **ولمن يعدّل الدالة الكاتبة:** كل قيدٍ حذفناه من القواعد صار **بند
قبولٍ** في `functions/lib/src/master_data.dart` (`DEBT-21` ①) — **وقت
الخادم · التفرّد · رفض حقول السعر · حصانة النوع الافتراضي · قفل الوحدة ·
الكتابة الواحدة للإعداد التأسيسي**. ★ **إسقاطُ أيٍّ منها ثغرة صامتة** لأن
القاعدة لم تعد تحرسه.
⛔★★ **والسبب النصي خرج من هذه القائمة بـ`ADR-0020` (2026-08-27)** — ★ **لا
سهواً بل بقرارٍ معتمد**: **اختياريٌّ الآن في كل عملية.**

### 0.1 ترتيب المعاملة — ملزم ولا يُعكَس

| # | المرحلة | لماذا هذا الموضع |
|:-:|---|---|
| ① | **البوابة قبل المعاملة** — الحالة ثم الصلاحية ثم `requestId` | ⛔ فلا أقفال تُحجَز لطلبٍ مرفوض أصلاً |
| ② | **كل القراءات داخل المعاملة** — المستند القائم · **سجل الحراسة** · **عدّاد الكود** · **أرصدة المقوت** | ★ §3 نصّاً: «وبلا ذلك يستطيع مستخدمان إنشاء نفس الرقم في نفس اللحظة» |
| ③ | **الكتابات وقيد التدقيق في التزام واحد** | `ADR-0013` القاعدة 1 — ⛔ ولا كودَ يُستهلَك ثم تفشل الكتابة فتبقى فجوة |

### 0.2 ★ كود الكيان — خطوة داخلية لا عملية مستقلة

`SRC-001` · `SUP-0001` · `MQT-0001` · `ITM-0001` **تُولَّد من عدّاد يُقرأ
ويُكتب داخل المعاملة نفسها** — تطبيقاً لحسم `IQ-009` (الخيار أ) على أكواد
الكيانات، ⛔ **ولا `allocateEntityCode` منفصلة**. ★ **ومستند العدّاد
`document_counters/entity_{kind}`** — راجع `data-dictionary.md` §1.

### 0.3 ⚠️ أثر معلَن — **الاسم القديم يبقى محجوزاً**

سجل الحراسة **لا يُنقَض** (`firestore.rules` §10: «التفرد لا يُنقض»)، ⟵
**فتغييرُ اسم مصدرٍ يكتب مفتاحاً جديداً ويترك القديم محجوزاً**. ★ **مقصود
لا سهو:** إتاحةُ إعادة استعمال اسمٍ سابق تفتح باب الالتباس في سجلٍ تاريخي
لا يُحذف منه شيء.

---

## 1. المسؤوليات

تعريف الكيانات المرجعية وضمان **تفرّدها**، **وإطلاق التوليد التلقائي
للحسابات والأنواع الافتراضية** عند أي إضافة.

## 2. الكيانات

| الكيان | الحقول الحاكمة | قيد التفرد |
|---|---|---|
| `Source` | `name` · `normalizedName` · **`requiresSupplierOnIntake`** · `isActive` | **الاسم المُطبَّع** |
| `Supplier` | ⚠️★★★ `name` · `phone` · `normalizedPhone` · `notes` · `isActive` — ⛔⛔ **ولا يحمل مصدراً بعد `CR-006`** (2026-08-31) | **الرقم المُطبَّع** · ★ **ويتبع كل المصادر الحالية والمستقبلية تلقائياً** — ★ **تماماً كـ`Dealer`** |
| `Dealer` | `name` · `phone` · `normalizedPhone` · `isActive` · `disableReason` | **الرقم المُطبَّع** · **ولا يحمل مصدراً** — ★★ **وهو السابقةُ التي قِيسَ عليها `Supplier` في `CR-006`** |
| `Item` | **`sourceIds: List`** · `name` · `normalizedName` · `nature` · `pieceWeightGrams?` · **`unit`** · **`isSystemDefault`** · `isActive` | **الاسم المُطبَّع على مستوى النظام** |
| `AppSettings` | بيانات المنشأة · العملة والأرقام | **بندان فقط · كتابة واحدة** |

## 3. التطبيع (Normalization) — قاعدة التفرد

```text
تطبيع الاسم  : حذف التشكيل · حذف التطويل · توحيد حوامل الهمزة ·
               ة ⟵ ه · ى ⟵ ي · توحيد حالة الأحرف · إزالة المسافات الزائدة
تطبيع الهاتف : توحيد الأرقام للاتينية · إسقاط كل ما ليس رقماً · حذف مفتاح
               الدولة اليمني · حذف الأصفار البادئة ⟵ **المحلي المجرَّد**
```

> **الفحص يجري على القيمة المُطبَّعة حصراً** — عبر **سجلات حراسة تفرد**
> معرّفها **هو القيمة المُطبَّعة نفسها**، وتُكتب **داخل نفس المعاملة
> الذرّية** التي تُنشئ الكيان. وبلا ذلك يستطيع مستخدمان إنشاء نفس الرقم
> في نفس اللحظة.

### 3.1 القرار المعتمد — **تطبيع عربي كامل** (`IQ-013` · 2026-08-24)

★ **القواعد السبع بترتيبها، وهي مُلزِمة حرفياً:**

| # | القاعدة | مثال يفترق بدونها |
|:-:|---|---|
| ① | **حذف التشكيل** | «السُّكْرَب» ⟷ «السكرب» |
| ② | **حذف التطويل** `ـ` | «السكــرب» ⟷ «السكرب» |
| ③ | **توحيد حوامل الهمزة** — راجع §3.2 | «مؤسسة» ⟷ «موسسه» |
| ④ | **`ة` ⟵ `ه`** | «فاطمة» ⟷ «فاطمه» |
| ⑤ | **`ى` ⟵ `ي`** | «يحيى» ⟷ «يحيي» |
| ⑥ | **توحيد حالة الأحرف** | `SCRAP` ⟷ `scrap` |
| ⑦ | **إزالة المسافات الزائدة** — بالطرفين وبالداخل | «عود  ممتاز» ⟷ «عود ممتاز» |

### 3.2 حوامل الهمزة — ★ **كلها لا ألفاتها وحدها**

```text
أ إ آ ٱ ⟵ ا        ؤ ⟵ و        ئ ⟵ ي        ء ⟵ تُحذف
```

> ★ **والصيغة المُركَّبة تلتقي المُدمَجة:** حذفُ التشكيل يشمل `U+0654`
> (الهمزة فوق) و`U+0655` (الهمزة تحت)، ⟵ **فـ`ا` + همزة مُركَّبة تُنتج
> المفتاح نفسه الذي تُنتجه `أ` المدمجة.**

### 3.3 موضع القاعدة — ⛔ **ولا نسخة ثانية منها**

★ **دالة واحدة في طبقة النطاق:** `packages/qtms_domain/lib/core/text_normalization.dart`
→ `normalizeName()` — يشاركها **التطبيق والعمليات السحابية** (`ADR-0012`).

⛔ **ولا يُسمح بنسخة خاصة داخل `functions/` ولا داخل الواجهة**
(`coding-standards.md` §2.2 · `P-10`): نسختان **تفترقان عند أول تعديل**،
⟵ **والافتراق هنا يُنتج كيانين متطابقين في نظر المستخدم مختلفين في نظر
النظام** — ★ **فسادُ بيانات صامت لا خطأ يظهر.**

★ **وتسري على كل قيمة يقوم عليها تفرّد أو مطابقة مُطبَّعة:**
`sources.normalizedName` · `items.normalizedName` · `unique_source_names` ·
وأي حارس تفرّد لاحق.

### 3.4 ⚠️ الأثر المقبول — **معلَن لا مُكتشَف**

★ **قد يدمج التطبيع الكامل اسمين مختلفين في حالات نادرة** — ✅ **وهذا مقبول
بقرار صريح من صاحب المشروع**، لأن هدف `normalizedName` **منعُ الازدواج
الصامت في البيانات الأساسية** ⛔ **لا إثباتُ التطابق اللغوي الكامل.**

### 3.5 تطبيع الهاتف — **المحلي المجرَّد** (`IQ-014` · 2026-08-24)

★ **أساس القرار مُعلَن:** **أرقام هذا النظام يمنية دائماً لا دولية** — قرار
صاحب المشروع. ⟵ **فالمفتاح يُخزَّن محلياً مجرَّداً بلا مفتاح دولة.**

| # | الخطوة | مثال |
|:-:|---|---|
| ① | **توحيد الأرقام إلى اللاتينية** — العربية الهندية `٠…٩` والفارسية `۰…۹` | `٧٧٧١٢٣٤٥٦ ⟵ 777123456` |
| ② | **إسقاط كل ما ليس رقماً** — مسافات · شرطات · أقواس · `+` | `(0777) 123-456` |
| ③ | **حذف مفتاح الدولة اليمني** — `+967` · `00967` · `967` | `967777123456 ⟵ 777123456` |
| ④ | **حذف الأصفار البادئة** | `0777123456 ⟵ 777123456` |

★ **فخمس صيغ لرقم واحد تُنتج مفتاحاً واحداً** — وهو الغرض كله.

> ⚠️ **وحارس طول على ③:** لا يُحذف `967` إلا إن بقي بعده **7 أرقام فأكثر**
> — ⛔ **وإلا بُتر مُدخَل قصير خاطئ فأنتج مفتاحاً لرقم لم يكتبه أحد.**
> ★ **والحذف آمن أصلاً:** ⛔ **لا رقم يمني محلي يبدأ بـ`967`** — المحمول
> يبدأ بـ`7` والثابت برمز منطقة `1…7`.

> ★★ **والبند ① ليس تجميلاً:** لوحة المفاتيح العربية تكتب `٧٧٧` واللاتينية
> `777` ⟵ **فالرقم الواحد مفتاحان بلا توحيد.** ✅ **والتحويل آمن بنيوياً**
> لأن التقابل **واحد لواحد** — ⛔ **فيستحيل أن يدمج رقمين مختلفين.**

> ⚠️ **وأثر الخيار معلَن:** **رقمٌ غير يمني يُخزَّن بأرقامه كما كُتبت بلا
> حذف مفتاحه** (`+966…` ⟵ `966…`) — ★ **فمفتاحه متفرّد صحيح**، ⛔ **لكنه
> غير جاهز للإرسال الدولي مباشرةً.** ⟵ **وهو مقبول لأن القرار نصّ على أن
> الأرقام يمنية دائماً.**

⛔ **ولا تتحقق دالة التطبيع من صحة الرقم ولا طوله** — ★ **التحقق عند حدود
الإدخال**، والدالة تُنتج **المفتاح وحده**.

## 4. التوليد التلقائي (عمليات سحابية)

| المُشغِّل | ما يُنشَأ | المرجع |
|---|---|---|
| **إضافة مصدر** | حساب مالي **لكل مقوت** · حساب مالي **لكل رعوي** · **نسخة «السكرب»** لهذا المصدر | `BR-M2-04` · `AT-37` |
| **إضافة مقوت** | حساب له في **كل** المصادر القائمة | `BR-M4-05` |
| **إضافة رعوي** | حساب له في **كل** المصادر القائمة | `BR-M3-07` |

> ⚠️ **تُنفَّذ في السحابة بتجميع** — إضافة مصدر مع 50 مقوتاً و10 رعية =
> **60 كتابة** لا تُترَك لجهاز قد ينقطع.

## 5. النوع الافتراضي «السكرب»

| الخاصية | القيمة |
|---|---|
| الإنشاء | **من السحابة فقط** — لا من أي واجهة |
| الطبيعة والوحدة | **وزني · كيلوجرام** |
| التعديل / الحذف / التعطيل | **مرفوض دائماً** |
| المعاملة | **كأي نوع** في المخزون والتسعير والتوزيع والبيع والجرد — **والفارق الوحيد أن وحدته الكيلوجرام** |

## 6. الحالات الحدّية

| # | الحالة | المعالجة |
|---|---|---|
| 1 | حذف كيان له حركات | **مرفوض دائماً** — التعطيل بديلاً |
| 2 | تعطيل مصدر له ضمارات مفتوحة أو مخزون غير مصرَّف | **صلاحية مدير + إقرار نصي**، والرسالة تعرض العدد والكمية |
| 3 | تعطيل مقوت رصيده ≠ 0 | ✅★★ **مُنفَّذ بثلاثة شروط مجتمعة** (`IQ-020` الخيار أ · 2026-08-25): **`dealerDisableWithBalance`** — ★ **مفتاحٌ مستقل** ⛔ **لا `dealerWrite`** — **+ إقرارٌ نصّي غير فارغ + رصيدٌ مقيسٌ من `dealer_balances` داخل المعاملة** ⛔ **لا قيمةٌ من الجهاز**. ★ **ورصيدُ الصفر لا يشترط المفتاح** |
| 4 | تغيير `requiresSupplierOnIntake` | **لا يؤثر على المستندات السابقة إطلاقاً** — الجديدة فقط |
| 5 | إزالة مصدر من مصادر رعوي/نوع | **لا يؤثر على الحركات السابقة** — يمنع الجديد فقط |
| 6 | **تغيير وحدة نوع له حركات** | **مرفوض** — يفسد كل الأرصدة التاريخية |
| 7 | كتابة الإعداد التأسيسي مرتين | **مرفوض** — حتى من المالك |
| 8 | إضافة نوع بسعر | **قاعدة الحماية ترفض أي حقل سعر** على سجل الأنواع |

## 7. نقاط الانتباه للمنفِّذ

> ⚠️ **`Dealer` لا يحمل حقل مصدر إطلاقاً** — حساباته **سجلات مستقلة**
> مفتاحها `{dealerId}_{sourceId}`. وإضافة حقل مصدر للمقوت **خطأ بنيوي**
> يهدم `ADR-0005`.

> ⚠️ **`unit` جزء من هوية النوع** — يُخزَّن **داخل كل سطر مستند وكل حركة**
> لحظة الإنشاء، فلا يتغيّر المعروض تاريخياً.

## 8-ب. ★ جسر جهات الاتصال — **مُنتقي النظام** ⛔ لا حزمة طرفٍ ثالث

`FR-M3-03` و`FR-M4-03` يطلبان زرّاً **يملأ الاسم والرقم معاً بضغطة واحدة**.

★ **والمُنفَّذ قناة منصّة مكتوبة بأيدينا** (`lib/capabilities/master_data/infrastructure/contact_picker.dart`
+ `MainActivity.kt`) تفتح `ACTION_PICK` — ⟵ **فيختار المستخدم جهةً واحدة
في واجهة النظام**، ⛔ **ولا إذن `READ_CONTACTS` ولا قراءة للقائمة كلها.**

**ولماذا لا حزمة جاهزة — تطبيقٌ حرفي لسياسة الاعتماديات:**

| # | سؤال `dependency-management-policy.md` §1 | الجواب |
|:-:|---|---|
| 1 | هل يُغني عنها الإطار؟ | ✅ **نعم** |
| 2 | كم سطراً توفّر؟ | **أقل من 100** ⟵ «**اكتبه بنفسك**» |
| 5 | هل تقرأ بيانات؟ | ⚠️ **نعم — تدقيق إلزامي**، ⟵ **وتفاديناه بالكامل** |

> ⚠️ **والزر إثراءٌ لا مسار إلزامي:** تعذّرُه يُخفيه ⛔ **ولا يمنع الإدخال
> اليدوي** ولا يُسقِط النموذج.

### 8-ب-1. ⛔⛔★★★ عقدُ التعذّر — **خطأٌ صريحٌ لا `null` صامتة** (`AM-020` · 2026-09-15)

★ **و«لا يمنع الإدخال اليدوي» لا تعني «لا يُقال شيء»** — ⟵ **وكان الجسرُ
يبتلع التعذّرَ في الطرفين معاً**، ⛔ **فيضغط المستخدمُ الزرَّ ولا يحدث شيءٌ
إطلاقاً**: ★ **لا حقلٌ يُملأ ولا رسالةٌ تفسّر** (رفضُ إذن؟ جهازٌ بلا تطبيق
جهات اتصال؟ عطلٌ تقني؟).

| الطبقة | قبل | بعد |
|---|---|---|
| `MainActivity.kt` | `ActivityNotFoundException` ⟶ `result.success(null)` | ⟶ `result.error(ERR_NO_CONTACT_PICKER, …)` |
| `contact_picker.dart` | `PlatformException` و`MissingPluginException` ⟶ `return null` | **يُتركان يصعدان** |
| ورقةُ الرعوي/المقوت | — | `try/catch` ⟶ `ContactPickerFailureBanner` (`QtmsInlineBanner` بثلاثية `warning`) |

★★★ **والعقدُ الآن قاطع:** `pickOne()` **تُعيد `null` للإلغاء وحدَه** —
⛔ **ولا تخلط الإلغاءَ بالتعذّر**، ⟵ **فالشاشةُ تعرف الفرقَ وتقوله.**

> ★ **ولماذا `warning` لا `danger`** (`design-system.md` §3.4): ⟵ **لا عمليةَ
> فشلت ولا بيانٌ ضاع** — ★ **مسارُ إثراءٍ تعذّر**، ⛔ **والإدخالُ اليدوي باقٍ
> كما هو** ⟹ **فالنصُّ يقول ما يُفعَل: «تأكد من منح التطبيق إذن الوصول».**
>
> ★ **ومحاولةٌ جديدة تمسح تحذيرَ السابقة** — ⛔ **فلا تحذيرٌ معلَّقٌ بعد زوال
> سببِه.**

## 8. الاختبارات المرتبطة

`TC-M2-*` · `TC-M3-*` · `TC-M4-*` · `TC-M5-*` · `TC-M21-001` ·
`AT-01` … `AT-05` · `AT-37` · `AT-65` · `E-37` · `E-38` · `E-39`

## ★★★ ما أضافه `AM-018` — الإعدادُ التأسيسي والإعداداتُ وحول التطبيق (2026-09-14)

★ **ثلاثُ شاشاتٍ من هذه الوحدة في نطاق `AM-018`** — ⛔ **ولا قاعدةَ عملٍ
واحدةٍ تتغيّر**: ⛔ **و`FR-M21-03` بحرفه** (**مرةً واحدةً ولا تعديل**)،
⛔ **ولا `firestore.rules` تُمَسّ** ⛔ **ولا معاملةَ الكتابة.**

### 1. الإعدادُ التأسيسي — رفضٌ يُسمّي حقلَه ونجاحٌ يُرى

| # | البند | العقد |
|:-:|---|---|
| ① | ⛔⛔★★★ **رفضُ التحقق المحلي يُسمّي الحقل** | ★ **`validateAppSettings` تُرجِع رمزَ رفضٍ مُصنَّفاً لكل حقل** — ⟵ **نظيرُ `PasswordChangeRejection` حرفياً** (`FR-M1-19`): **اسمُ المحل · رمزُ العملة · فاصلُ الآلاف · العنوان** ⛔ **لا رسالةٌ عامةٌ واحدة** |
| ② | ★★ **أثناء الحفظ** | ★ **نصُّ الزر «جارٍ الحفظ…» مع تعطيله** (`design-system.md` §6-ي) |
| ③ | ★★ **بعد النجاح** | ★ **شريطُ `success` بنصّ «تم حفظ إعداد المنشأة»** ⟵ **ثم مهلةٌ قصيرةٌ قبل خروج الشاشة** (⟵ **فتُقرأ**) |
| ④ | ★ **فاصلٌ تحت «بيانات المنشأة»** | ★ **مطابقٌ لشاشة الإعدادات** (`ui-guidelines.md` نمط 7) |

> ⛔⛔★★★ **ولماذا رمزُ الحقل لا رسالةٌ عامة** — ★ **الشاشةُ لا رجعةَ فيها**
> (`FR-M21-03`): ⟵ **ومستخدمٌ رُفض إدخالُه برسالة «تعذّر تنفيذ العملية» أمام
> خمسةِ حقولٍ لا يعرف أيَّها يُصحِّح**، ★ **فيعبث بالصحيح ويترك المعيب.**
> ⛔ **وهذا نظيرُ `FR-M6-03`/`BR-M6-05` في «الوارد عدداً» حرفياً.**
>
> ⛔⛔★★ **والرموزُ في طبقة النطاق لا في الشاشة** (`ADR-0009` · `ADR-0010`
> القاعدة 1) — ★ **والشاشةُ تترجمها نصّاً عربياً** ⛔ **ولا تُعيد فحصَ شيء.**
>
> ⚠️★★ **والخروجُ يبقى بيد الموجّه** — ★ **`redirect` يُخرج الشاشةَ فور وصول
> المستندين**: ⟵ **والمهلةُ القصيرة تُتيح قراءةَ شريط النجاح قبل ذلك**،
> ⛔ **ولا `go` ولا `pop` يُضاف** (⟵ **وملاحةٌ يدويةٌ هنا تتسابق مع الموجّه**).

### 2. الإعدادات — تذييلٌ مثبَّتٌ عند القاع

★ **عمودٌ بارتفاع الشاشة ومباعدةٌ مرنةٌ بين المحتوى والتذييل** —
⛔ **لا ارتفاعٌ محفور** (`ui-guidelines.md` نمط 7).

> ★ **والمحتوى يبقى قابلاً للتمرير** — ⟵ **فعند تكبير النصّ إلى `1.3` أو على
> شاشةٍ قصيرة لا يُقتطَع شيء**، ⛔ **ولا تُفرَض مباعدةٌ سالبة.**
> ⛔⛔ **ولا مفتاحٌ ولا صفُّ تنقّلٍ يُضاف لملء الفراغ** — `FR-M21-05`.

### 3. حول التطبيق — سطورُ تواصلٍ قابلةٌ للنقر

★ **العقدُ الكامل في** [`../../19-assets/developer-identity.md`](../../19-assets/developer-identity.md)
**§4** ⛔ **ولا يُكرَّر هنا** — ★ **وخلاصتُه: `https:`/`mailto:`/`tel:` من
القيمة المقروءة، بلون `textSecondary` مسطَّراً** ⛔ **لا بلون المنتج الأساسي.**

## ★★★ ما أضافه `AM-020` — العائلةُ الرباعيةُ ببنيةٍ واحدةٍ وحرسٍ واحد (2026-09-15)

★ **أربعُ شاشاتٍ من هذه الوحدة في نطاق `AM-020`** (المصادر · الرعية ·
المقاوته · الأنواع) — ⛔ **ولا قاعدةَ عملٍ واحدةٍ تغيّرت**: ★ **ما تغيّر
**متى** يُقال الرفضُ ومَن يقوله**، ⛔ **لا ما يُرفَض.**

### 1. ⛔⛔★★★ زرُّ الحفظ يُعطَّل لغياب «سبب التعطيل»

★ **القاعدةُ قائمةٌ في طبقة النطاق منذ `WU-002`** — `_disableReason`:
**إلزاميٌّ عند التعطيل وحدَه** (`FR-M2-07` · `FR-M4-09`)، ⛔ **والواجهةُ
كانت تُظهر الحقلَ ولا تحرسه**: ⟵ **فيُضغَط الزرُّ ثم يُردّ الطلبُ برسالةٍ
عامة** (`operationFailed`) **لا تُسمّي الحقلَ الناقص.**

| البند | التفصيل |
|---|---|
| **الدالةُ المشتركة** | `masterDataFormBlocked({requiresDisableReason, disableReason})` في [`master_data_widgets.dart`](../../../lib/capabilities/master_data/presentation/master_data_widgets.dart) |
| **المكوّنُ الحامل** | `MasterDataSubmitButton` — ★ **يستمع لحقل السبب بـ`ValueListenableBuilder`** ⛔ **لا `setState` على كل محرفٍ تُعيد بناءَ الورقة كلِّها** |
| **الشرطان مستقلان** | `submitting` (أثناء النداء) · و`blocked` (اكتمالُ النموذج) — ★ **ولا يُغني أحدُهما عن الآخر** (`ui-guidelines.md` نمط 4 · `AM-015` ⑥) |
| **العقد** | [`design-system.md`](../../18-ux-ui/design-system.md) §6-ط البند ④ **ممتدّاً إلى الحقل الشرطي الإلزامي** |

⛔⛔ **ولا نسخةَ خامسةً من القاعدة:** ★ **دالةٌ واحدة تقرؤها النماذجُ
الأربعة** — ⟵ **وأربعُ نسخٍ كانت تفترق عند أول تعديل** (`coding-standards.md`
§2.2).

⚠️⚠️ **وهذا عرضٌ لا حماية** (`RISK-02`) — ★ **والرفضُ الحقيقيُّ في
`validateSource`/`Supplier`/`Dealer`/`Item` وفي الدالة السحابية كما هو**،
⛔ **ولم يُخفَّف حارسٌ واحد.**

### 2. ★★ بطاقةُ النوع — `inline` كأخواتها الثلاث

★ **بطاقةُ النوع أخفُّ الأربعة حِملاً** (**زرٌّ واحدٌ يغيب كلياً للسكرب**)،
⛔ **وكانت وحدَها `stacked`**: ⟵ **صفٌّ كاملٌ وفاصلٌ شعريٌّ لعنصرِ تفاعلٍ
واحد**، ★ **بينما بطاقةُ المصدر الأثقلُ منها `inline` بنجاح.** ⟹ **وصارت
العائلةُ الرباعيةُ ببنيةٍ واحدة** ([`design-system.md`](../../18-ux-ui/design-system.md) §6-د).

★ **والزرُّ أيقونيٌّ صامتٌ بـ`tooltip`** كما في الثلاث — ⛔ **لا
`TextButton.icon`**: ⟵ **الزرُّ النصّيُّ المُدمَج ينمو بمقياس الخط فيعصر اسمَ
الكيان**، ★ **وهو عطلُ `DEBT-48` بعينه.**

### 3. ⛅★★ مدخلُ كشف الحساب من بطاقة المقوت

★ **`IconButton` بأيقونة `receipt_long_outlined`** — **أيقونةُ «كشف حساب
المقوت» نفسُها في لوحة اليوم** (`design-system.md` §6-ج: **أيقونةٌ واحدةٌ
لكلِّ فعلٍ عبر الشاشات**) — ★ **بهدف لمسٍ 48×48**، ⟵ **ويختصر فتحَ الكشف ثم
اختيارَ المقوت من قائمةٍ منسدلة إلى ضغطةٍ واحدة.**

| القاعدة | كيف طُبِّقت |
|---|---|
| ⛔⛔★★★ **ولا معاملَ في المسار** | `statementDealerProvider.select(dealerId)` **ثم** `context.go(dealerStatementRoute)` — ★ **حالةٌ في التطبيق** ⛔ **لا رابطٌ يُشارَك** ([`router.dart`](../../../lib/app/router.dart) نصّاً) |
| ⚠️⚠️ **والبوابةُ مفتاحُ الشاشة نفسِه** | `PermissionGate(dealerStatementView)` — ★ **كما في لوحة اليوم حرفياً**، ⛔ **فلا زرَّ يَعِد بوجهةٍ تُفتَح على رفض** (`RISK-02`) |
| ⛔⛔ **ولا رصيدَ على البطاقة** | ★ **البياناتُ المرجعيةُ لا تحمل رقماً مالياً** — ⟵ **والفجوةُ تُسَدّ بوجهةٍ** ⛔ **لا بخلطِ الاهتمامين** |

### 4. ⚙️★★★ نصُّ الأثر التلقائي في ورقة الرعوي — **سهوٌ أُغلق لا قرارٌ نُقض**

★ **ثلاثُ شاشاتٍ من هذه الوحدة لها أثرٌ تلقائيٌّ عند الإنشاء** — ⛔ **وكانت
اثنتان تُعلنانه والثالثةُ صامتة:**

| الشاشة | المتطلب | النصُّ المعروض |
|---|---|---|
| **المصادر** | `FR-M2-04` | «يُنشأ للمصدر تلقائياً حسابٌ لكل مقوت ولكل رعوي، ويُوصَل به نوع «السكرب».» |
| ★★ **الرعية** | `FR-M3-04` **(حرجة)** | ✅ **«يُنشأ للرعوي تلقائياً حسابٌ في كل مصدر قائم، وفي كل مصدر يُضاف مستقبلاً.»** — **أُضيف في `AM-020`** |
| **المقاوته** | `FR-M4-05` | «يُنشأ للمقوت تلقائياً حسابٌ في كل مصدر قائم، وفي كل مصدر يُضاف مستقبلاً.» |
| ⛔ **الأنواع** | — | ⛔ **بلا نصّ عمداً** — ★ **النوعُ لا يُنشئ حساباً لأحد** |

⛅⛅★★★ **والغيابُ قِيس سهواً ⛔ لا قراراً** — ★ **مراجعةُ تجربة الاستخدام
رجّحت أنه مقصود** («قد يكون مقصوداً — الرعوي لا يُنشئ له حسابات تلقائية بنفس
طريقة المصدر/المقوت»)، ⟹ **والقياسُ في الكود والمتطلب معاً نفى ذلك:**

| الدليل | ما يقوله |
|---|---|
| `FR-M3-04` **(حرجة)** | «يُنشأ للرعوي **حساب مالي منفصل في كل مصدر موجود تلقائياً** عند إضافته، **وفي كل مصدر يُضاف مستقبلاً**» — ★ **نظيرُ `FR-M4-05` حرفاً بحرف** |
| [`account_provisioning.dart`](../../../functions/lib/src/account_provisioning.dart) | `provisionAccountsOnPartyAdd` — **«إضافة مقوت أو رعوي ⟵ حساب له في كل المصادر القائمة»** · و`provisionAccountsOnSourceAdd` — **«حساب لكل مقوت ولكل رعوي»** |
| `CR-006` | ★ **يُشدِّد الأثرَ ولا يُلغيه** — «`FR-M3-04` باقٍ حرفاً بحرف، بل هو مبرِّرُ هذا الطلب نفسه» |

⟹ ⛔⛔★★★ **فالنصُّ ليس إضافةً تجميلية:** ★ **إنشاءُ رعويٍّ واحد يُنتج
مستنداً في كل مصدر** — ⟵ **وأثرٌ بهذا الحجم يُعلَن قبل الحفظ** (`ui-guidelines.md`
نمط 4)، ⛔ **وصمتُ شاشةٍ واحدةٍ بين ثلاثٍ يُقرأ نفياً للأثر لا سكوتاً عنه.**

> ⛔ **وعلى ورقة التعديل لا يظهر** — ★ **الأثرُ وقع مرةً عند الإنشاء**،
> ⟵ **وتكرارُه على التعديل يَعِد بما لا يحدث.**

---

## ★★★ ما غيّره `AM-027` في نموذج النوع (2026-09-17)

★★ **النموذجُ نفسُه صار قابلاً للاستدعاء من خارج شاشة «الأنواع»** — ⛔ **ولم
يُنسَخ ولم يُشقَّ إلى نسختين** (`design-system.md` §8 المحظور 11):

| # | البند | العقد المُلزِم |
|:-:|---|---|
| ① | ★★ **تهيئةٌ بمُدخَلين اختياريين** | `showItemForm(context, initialName: …, initialSourceIds: …)` — ★ **تملأ حقلَ الاسم وتُعلِّم المصادر عند الإنشاء وحدَه** ⛔ **ولا أثرَ لهما في التعديل إطلاقاً**: ⟵ **فسجلٌّ قائمٌ يُقرأ من نفسِه** ⛔ **لا من سياق مُنادٍ.** |
| ② | ✅★★ **وتُرجِع معرّفَ النوع المُنشأ** | ★ **`Future<String?>`** — ⟵ **فالمُنادي يُسنِده إلى صفِّه فوراً** ⛔ **ولا يبحث عنه في القائمة**، ★ **و`null` تعني إغلاقاً بلا إنشاء** (**أو تعديلاً**). |
| ③ | ⛔⛔★★ **حارسُ الاسم المكرَّر قبل النداء** | ★ **عند الإنشاء وحدَه: يُطبَّع الاسمُ (`normalizeName`) ويُقارَن بكل الأنواع القائمة** ⟹ **ويُرفَض بـ`CatalogMessage.itemNameExists`** ⛔ **قبل أن يُرسَل.** ⚠️⚠️ **وهو راحةُ عرضٍ لا حماية** (`RISK-02`): ⛅ **والدالةُ السحابية تُعيد الفحصَ كما هي** (`ERR_SETUP_003` · `FR-M5-01`) ⛔ **ولم تُمَسّ بحرف** — ★ **فالاسمُ المكرَّر مرفوضٌ ولو طُلب من خارج الشاشة.** ⛔ **والمقارنةُ تشمل المعطَّل والافتراضيَّ معاً** ⟵ **فالتفرّدُ على مستوى النظام لا على مستوى المعروض.** |
| ④ | ⛔ **وما لم يتغيّر** | ★ **`validateItem` بحرفها** · ★ **ولا حقلَ سعرٍ ولا حقلَ وحدة** (`FR-M5-09` · `FR-M5-03`) · ★ **ووزنُ الحبة للوزني وحدَه** (`E-09`) · ★ **ومسارُ الكتابة والتدقيق كما هو** (`ADR-0013`). |
