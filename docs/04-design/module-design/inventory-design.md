# تصميم وحدة: المخزون (`inventory`)

| البند | القيمة |
|---|---|
| **الحالة** | ★★ **منفَّذ جزئياً** (`WU-003` · `WU-005` · ✅★★★ **`WU-019`** — 2026-09-04): **`M6` الوارد عدداً** · **`M8` شاشة مخزون اليوم** · **دفتر الحركات وحساب الرصيد ومنع السالب** · ✅★★ **و`M9` التسعير اليومي** · ✅★★★ **و`M8` شاشة المتبقي المتأخر وتنبيهُها الدائم وتصريفُها على يومها الأصلي** (§5 — راجع §9-و). ✅★★★ **و`FR-M8-16` الإتلاف** (`WU-020` — 2026-09-04 · راجع §9-ز). ✅★★★ **و`M16` الجرد والتسويات وجردُ يومٍ سابق** (`WU-022` — 2026-09-05 · راجع §9-ح) |
| **الوحدات المغطّاة** | `M6` الوارد عدداً · `M8` المخزون والمتبقي المتأخر · `M9` التسعير اليومي · `M16` الجرد |
| **يعتمد على** | `master-data` |
| **المتطلبات** | `FR-M6-*` · `FR-M8-*` · `FR-M9-*` · `FR-M16-*` |

---

## 1. المسؤوليات

1. **دفتر حركات المخزون** — مصدر الحقيقة الوحيد للرصيد.
2. حساب رصيد **(مصدر × نوع × تاريخ مخزون)** ومنع سلبيته.
3. **رصد المتبقي المتأخر وتصريفه على يومه الأصلي**.
4. التسعير اليومي بتصفير يومي · والجرد والتسويات.

## 2. دفتر حركات المخزون

| الحقل | ملاحظة |
|---|---|
| **`sourceId`** | إلزامي في كل سطر |
| **`stockDate`** | **اليوم الذي تنتمي إليه البضاعة** |
| `entryDate` | متى سُجِّلت فعلاً |
| `itemKey` · `itemName` | النوع أو **الاسم المركّب** |
| **`sackId?`** | إن كان من جونية — **أساس احتساب سعرها** |
| **`unit`** | حبة / كيلوجرام |
| `direction` | دخول / خروج |
| `quantity` · `balanceAfter` | — |
| `sourceDocType` · `sourceDocId` · `sourceDocNumber` | توريد · توزيع · بيع · سحبية · خرجية · إتلاف · جرد |
| **`movementTag`** | عادية / **تسوية جرد** / **إتلاف** |
| **`isCancelled`** | **بديل الحركة العكسية** |
| `lastAmendedAt` · `amendedBy` · `amendReason` | **لأن الحركة تُعدَّل في مكانها** |

## 3. حساب الرصيد

```text
رصيد (المصدر × النوع × تاريخ المخزون) = Σ(دخول) − Σ(خروج)   [غير الملغاة]
```

| مصادر الدخول | مصادر الخروج |
|---|---|
| وارد عدداً · وارد جواني **والسكرب** · تسوية جرد بالزيادة | توزيع · بيع نقدي · **سحبيات وخرجيات (بند قات)** · **إتلاف** · تسوية جرد بالنقص |

> **الرصيد يُقرأ داخل المعاملة الذرّية نفسها لا من الملخص** — لأن دقة
> التوفّر لحظة الاعتماد شرط لا يقبل التأخّر (`ADR-0008`).

## 4. منع الرصيد السالب — طبقتان

| الطبقة | الدور |
|---|---|
| **المعاملة الذرّية** | تقرأ الرصيد وتتحقق **قبل** الكتابة، **وتُعاد تلقائياً** إن تغيّر |
| **قواعد الحماية** | **ترفض نهائياً أي كتابة تُنتج رصيداً سالباً** — حتى من خارج التطبيق |

> **الطبقتان معاً لا إحداهما.** المعاملة تعطي تجربة صحيحة، وقاعدة الحماية
> تعطي **ضماناً**.

## 5. المتبقي المتأخر

```text
بند متبقٍ متأخر = رصيد (مصدر × نوع × تاريخ مخزون سابق) > 0
عمر البند       = تاريخ اليوم − تاريخ المخزون
تدرّج لوني      : 🟡 1–2 يوم · 🟠 3–4 · 🔴 5 فأكثر
```

**إجراءات التصريف الثلاثة** — وكلها تُحتسب **في اليوم الأصلي**:

| الإجراء | ما يحدث | القيمة المالية |
|---|---|---|
| **توزيع** | يفتح `M10` **مثبَّتة على التاريخ القديم** ⟵ يُوسِّع ضمار ذلك اليوم أو يُنشئ ضماراً بتاريخه | ✅ تدخل سعر الجونية |
| **بيع نقدي** | يفتح `M11` مثبَّتة ⟵ يُحتسب في **«نقدي» ذلك اليوم** | ✅ |
| **إتلاف** | خروج مخزني **بصلاحية** ★★ **وسببٍ اختياري** (`ADR-0020`) | ❌ **بلا أي قيمة ولا استحقاق للرعوي** |

> **هذا هو المسار الوحيد المصرَّح به لقبول تاريخ مخزون أقدم من اليوم**
> (`§9.6`)، ويُطلق **إعادة بناء ملخصات اليوم الأصلي** ووسمها
> **«⟳ مُحدَّث بأثر رجعي»**.

## 6. التسعير اليومي

| القاعدة | التنفيذ |
|---|---|
| **التصفير اليومي** | سعر اليوم مفتاحه `{sourceId}_{itemKey}_{date}` — **ولا يُنسَخ للغد تلقائياً** |
| **لا يظهر إلا ما له كمية اليوم** | الاستعلام على **رصيد اليوم > 0** لا على قائمة الأنواع |
| **مصدر السعر واحد** | ما يُدخَل هنا وما يُدخَل في سطر الجونية **يكتبان نفس الحقل** |
| **وحدة السعر تتبع وحدة النوع** | سعر حبة / **سعر كيلو للسكرب** |
| **تجميد السعر** | يُنسَخ **داخل سطر المستند** لحظة الاعتماد |
| **المتبقي المتأخر لا يظهر هنا** | سعره يُدخَل لحظة التصريف |

## 7. الجرد

- **يقع على مصدر واحد وتاريخ مخزون واحد**، ويُجمَّد الرصيد الدفتري عند البدء.
- ★★ **كل فرق ≠ 0 يقبل سبباً نصياً — اختيارياً** (`ADR-0020`).
- **حركات التسوية تُوسَم `movementTag = adjustment`** وتُستثنى من تقارير
  المبيعات **ومن احتساب سعر الجونية ومن استحقاق الرعوي**.
- **جرد يوم سابق بصلاحية** ⟵ يُعيد بناء ملخصات ذلك اليوم بأثر رجعي.

## 8. الحالات الحدّية

| # | الحالة | المعالجة |
|---|---|---|
| `E-01` | مستخدمان يوزّعان آخر كمية | الأول ينجح · **الثاني يُرفض** · **ولا رصيد سالب** |
| `E-22` … `E-25` | متبقٍّ متأخر وتصريفه وإتلافه | يُحتسب في **اليوم الأصلي** · مع إعادة بناء بأثر رجعي |
| `E-31` | جمع حبات مع أوزان | **ممنوع** — إجماليان منفصلان |
| `E-33` | نوع لا كمية له اليوم | **لا يظهر في التسعير** |
| `E-34` | بداية يوم جديد | الأسعار مصفَّرة · الرصيد من صفر · **ومتبقي أمس يبقى في يومه** |
| `AT-66` | تسوية جرد | **لا تُحتسب في سعر أي جونية ولا في المبيعات** |

## 9. نقاط الانتباه للمنفِّذ

> ⚠️ **كل استعلام وفهرس مخزني يُبنى على `stockDate` لا `entryDate`.**
> التاريخان متساويان في 99٪ من الحالات، **فلا يظهر الخطأ إلا عند التصريف
> المتأخر** — ويكون قد أنتج تقارير خاطئة **بصمت** (`RISK-07`).

> ⚠️ **لا حجز مؤقت للكميات** — التحقق **لحظة الاعتماد فقط داخل المعاملة**.

> ⚠️ **غياب سجل الرصيد يعني صفراً** — ولا يُنشأ سجل بصفر بلا داعٍ.

## 9-ب. ★★ ما نُفِّذ فعلاً في `WU-003` — ومواضعه

| ما في هذا المستند | الكود المنفِّذ |
|---|---|
| §2 دفتر الحركات | `packages/qtms_domain/lib/capabilities/inventory/domain/inventory.dart` (`StockMovement` · `MovementTag` · `SourceDocumentType`) |
| §3 حساب الرصيد (§2.1) | ★ **`computeItemBalance`** — ⛔ **دالة واحدة يشاركها التطبيق والسحابة** (`ADR-0012`) |
| §3 أعمدة الملخص | ★ `computeItemDailyFlow` ⟵ **يكتب `item_daily_balances`** |
| §4 منع الرصيد السالب | ★★ `validateNonNegativeBalance` **داخل معاملة `functions/lib/src/inventory.dart`** — ⚠️ **الطبقة الثانية صارت `allow write: if false` المطلق** بعد `WU-026` لا شرطاً في القاعدة |
| §8 `E-01` | ★ **مقيسٌ من الدفتر داخل المعاملة** ⟵ الثاني يُرفَض بـ`ERR_STOCK_001` |
| §8 `E-31` | ★★ **مفروضٌ بالنوع** — `StockQuantity` مُغلَق، ⛔ **ولا دالة تجمع وحدتين** |
| §9 «كل استعلام على `stockDate`» | ★ **كل استعلام في السحابة والتطبيق** — ومُختبَرٌ صراحةً (`inventory_screens_test.dart`) |

> ✅★★★ **وحدّان كانا معلَنَين بلا كود يوم `WU-003` — وقد أُغلِقا كلاهما:**
> **§5 (المتبقي المتأخر) بـ`WU-019`** (راجع §9-و) · **و§7 (الجرد) بـ`WU-022`**
> (راجع §9-ح). ✅★★ **و§6 (التسعير اليومي) نُفِّذ بـ`WU-005`** — راجع §9-ج أدناه.

> ★★ **وتعديلٌ معلَن على §3:** المستند يقول «**الرصيد يُقرأ داخل المعاملة
> الذرّية نفسها لا من الملخص**» — ✅ **ونُفِّذ حرفياً**: استعلامٌ على
> `inventory_ledger` بـ**(مصدر × نوع × تاريخ مخزون)** داخل المعاملة، ★ **ثم
> يُعاد بناء `item_daily_balances` منه بالكامل** ⛔ **بلا تراكم**
> (`coding-standards.md` §2.7).

## 9-ج. ★★ ما نُفِّذ فعلاً في `WU-005` (§6 التسعير اليومي) — ومواضعه

| ما في §6 من هذا المستند | الكود المنفِّذ |
|---|---|
| **التصفير اليومي** — مفتاح `{sourceId}_{itemKey}_{date}` | ★★ `dailyPriceId` في `packages/qtms_domain/lib/capabilities/inventory/domain/daily_price.dart` — ⟵ **واليوم جزءٌ من المفتاح**، ⛔ **فلا حقل «سارٍ حتى» يُرحِّل سعر أمس** |
| **لا يظهر إلا ما له كمية اليوم** — «الاستعلام على **رصيد اليوم > 0** لا على قائمة الأنواع» | ★★★ **مُنفَّذ في الطبقتين:** `pricingRowsProvider` يبني من الرصيد (التطبيق) · **و`_todayStockGate` في `functions/lib/src/daily_pricing.dart` يرفض نوعاً بلا كمية** — ★ **مقيساً من الدفتر داخل المعاملة بـ`computeItemBalance`** ⛔ **لا من `item_daily_balances`** (`ADR-0008`) |
| **مصدر السعر واحد** | ★ **مستندٌ واحد بمفتاح واحد** — ⟵ **وسطر الجونية في `M7` سيكتبه نفسه** (`FR-M9-04` · `BR-M7-15`) |
| **وحدة السعر تتبع وحدة النوع** | ★ `item.unit != line.unit ⟵ ERR_SETUP_009` — **والوحدة من سجل النوع** ⛔ **لا من الحمولة** |
| **تجميد السعر** | ⛔ **ليس هنا** — ★ **موضعه المستند الذي يستهلكه** (`WU-006` · `WU-012` · `WU-014`) |
| **المتبقي المتأخر لا يظهر هنا** | ★ **مُنفَّذ بالبناء** — الاستعلام على **تاريخ اليوم وحده**، ⟵ **فلا يبلغ الشاشةَ متبقٍّ سابق** |

> ★★ **وتعديلٌ معلَن على §6:** المستند لا يذكر **مَن يُسعِّر ولا بأي شرط**.
> ✅ **والمُنفَّذ:** **`dailyPriceWrite` + نطاق المصدر**، **ومصدرٌ معطَّل
> يُرفَض** (`ERR_DIST_003`) بنفس منطق التوريد الجديد. ★ **والقراءة بلا
> مفتاح** — `isSignedIn() && storedInScope()` كما في القاعدة المنشورة.
>
> ★★ **وقرارُ تنفيذٍ مُعلَن:** **السعر موجبٌ تماماً** — ⛔ **والصفر مرفوض**
> (`BR-M9-06` في الكود). ★ **وسببه أن حدّاً أدنى بصفر يُسقِط حارس
> `FR-M9-08` بصمت** بينما يبدو النوع «مسعَّراً»، ⟵ **والغياب (`null`) هو
> التعبير الصحيح عن «بلا سعر»**. ★ **وهو نفس قاعدة `validateCountedIntake`
> ③** («موجبٌ تماماً — والصفر ليس توريداً») ⛔ **لا اجتهادٌ جديد.**
>
> ⛔⛔★★ **والسبب النصّي اختياريٌّ في الحالتين معاً** — `ADR-0020`
> (2026-08-27). ★ **وكان إلزامياً على التغيير وحده** (`ADR-0004` · `CR-002`
> · `FR-M9-11`) ⛔ **ولم يكن يوماً على تسعيرٍ أوّل**: `FR-M9-01` يجعل **كل
> يوم يبدأ بلا أسعار**.
> ✅★★ **والتمييز بين «أوّل» و«تغيير» باقٍ لغرضه الآخر ولم يسقط:** ★ **فعلُ
> القيد `create` أو `amend`** ⛔ **لا اشتراطُ سبب** — ★ **والحَكَم سجلٌّ
> يُقرأ داخل المعاملة** (`changesStoredPrice`) ⛔ **لا قيمةٌ من الجهاز**،
> ★ **وإعادةُ إرسال نفس الأسعار ليست تعديلاً.**

## 9-د. ★★★ ما غيّره `AM-009` في شاشة الوارد عدداً (2026-08-31)

| # | التغيير | العقد والمبرّر |
|:-:|---|---|
| ③ | ★★ **مرشِّحُ المصدر يحمل «كل المصادر»** | ★ **مرشِّحُ *عرضٍ* مستقلٌّ عن سياق العملية** (`sourceListFilterProvider`) — ⛔⛔ **ولا يُدمَج مع `selectedSourceProvider`**: ⟵ **`null` في الأول تعني «كلَّها» وفي الثاني «لا مصدرَ في النطاق»**، ★ **ودمجُهما كان يجعل `null` تعني الشيءَ ونقيضَه.** ⛔⛔★★★ **والاستعلامُ يبقى مقيَّداً بالمصدر** — ★ **استعلامٌ لكل مصدرٍ ثم دمجٌ في الذاكرة** (`IQ-024` · `WU-008`) · ★ **واسمُ المصدر يظهر في السطر الثاني لكل بطاقة في وضع «الكل»** ⛔ **فصفٌّ بلا مصدرٍ في قائمةٍ تجمع مصادرَ لا يُقرأ** (`A-01`) |
| ⑤ | ★★ **المصدرُ حقلٌ في النموذج** | ⟵ **فالإضافةُ لا تعتمد على مرشِّح الشاشة**، ⛔ **ولا يُكتَب مستندٌ على مصدرٍ اختاره مرشِّحُ عرض** · ⛔ **ومقفلٌ في التعديل** (`sourceUnchanged()` · `ADR-0005`) · ★★ **وتبديلُه يُفرِغ السطور والرعوي** ⟵ **فالأنواعُ والرعية مفلترون بالمصدر** (`FR-M6-05`) |
| ④ | ★★★ **الأنواعُ صفٌّ يُنشأ بالطلب** | ★ **`QtmsItemLineRow`** (`design-system.md` §6-ب) — ⛔⛔ **ولا تُعرَض أنواعُ المصدر كلُّها صفوفَ إدخال** · ★★ **و«لا سطران لنفس النوع» صار بنيوياً** (`FR-M6-07`) ⟵ **فالنوعُ المختار يسقط من خيارات إخوته**، ★ **والفحصُ في طبقة النطاق باقٍ حارساً ثانياً** (`BR-M6-05`) |
| ⑥ | ★★ **نصُّ الخيار يحمل المتبقّي** | «**عوارض (80 حبة)**» — `FR-M10-06` · ★ **ومصدرُه `item_daily_balances`** (`remainingStockProvider`) ⛔ **وغيابُ النوع من الخريطة يعني صفراً** (`ADR-0008` القاعدة 5) · ⚠️ **وهو رصيدُ عرضٍ لا حارس** — ★ **والرفضُ على تجاوز المتاح في الدالة السحابية داخل المعاملة** (`FR-M10-11`) |

> ⛔⛔ **ولا يتغيّر شيءٌ في `FR-M6` نفسه:** ★ **لا حقلَ تاريخٍ ولا وزنَ ولا
> سعر** (`FR-M6-02` · `FR-M6-10`) · ★ **والرعوي مشروطٌ بالمصدر كما كان**
> (`FR-M6-03`) · ⛔ **ولا زرَّ حذفٍ إطلاقاً** (`FR-M6-14` · `GR-07`).

## 9-هـ. ★★★ ما غيّره `AM-012` §2 — دمجُ شاشتَي التوريد (2026-09-02)

⛔⛔★★★ **شاشةٌ واحدة باسم «التوريد مخزني» بتبويبين** — **بطلب المالك نصّاً:**
«**تُدمج شاشة الوارد عدداً وشاشة الوارد جواني في شاشة جديدة واحدة**».

| # | ما تغيّر | الموضع |
|:-:|---|---|
| ① | ★★★ **شاشةٌ حاويةٌ جديدة** — الشريطُ العلوي ورأسُ السياق وشريطُ التبويبات وزرُّ الإضافة | `lib/capabilities/inventory/presentation/supply_intake_screen.dart` |
| ② | ★★ **`CountedIntakeScreen` ⟶ `CountedIntakeTab`** — ★ **القائمةُ ونموذجُها بلا مساس** | `counted_intake_screen.dart` |
| ③ | ★★ **`SackIntakeScreen` ⟶ `SackIntakeTab`** — ★ **القائمةُ ونماذجُها بلا مساس** | `sack_intake_screen.dart` |
| ④ | ⛔⛔★★★ **مرشِّحٌ واحدٌ للتبويبين** (`sourceListFilterProvider`) — ⛔ **ولا مرشِّحَ مستقل** | كلاهما |
| ⑤ | ★★★ **وتبويبُ الجواني صار يقبل «كل المصادر»** — ★ **وكان يشترط مصدراً واحداً** | `sackListProvider` |
| ⑥ | ★★★ **وحقلُ المصدر داخل نموذج رأس الجونية** — ★ **نظيرُ `AM-009` ⑤** | `SackHeaderFormSheet` |
| ⑦ | ★★ **مسارٌ واحد `supplyIntakeRoute`** — ⛔ **والمساران القديمان لم يعودا يُعرَّفان** | `router.dart` |
| ⑧ | ★★ **ومدخلٌ واحد في الصدَفة ببوابة `AnyPermissionGate`** | `home_shell.dart` |

### ⚠️⚠️★★★ التعارض الذي عُرض على المالك ولم يُحسَم اجتهاداً

★ **الشاشتان كانتا تستعملان مرشِّحَي مصدرٍ *مختلفَي الدلالة قصداً*:**

| الشاشة | المرشِّح | دلالة `null` |
|---|---|---|
| «الوارد عدداً» | `sourceListFilterProvider` (`AM-009` ③) | **«كل المصادر»** |
| «الوارد جواني» | `selectedSourceProvider` (`A-01`) | ⛔ **«لا مصدرَ في النطاق» ⟹ تُوقَف الكتابة** |

⟹ ⛔⛔ **ودمجُهما في مرشِّحٍ واحدٍ يجعل `null` تعني الشيءَ ونقيضَه** —
★ **فطُرح السؤال صريحاً** (`AM-012` §2 السؤال ②)، ⟹ ⛅ **وقرارُ المالك:**
**«الكل» في التبويبين + حقلُ مصدرٍ في نموذج الجونية** ⛔ **لا إسقاطَ «الكل»
من التبويب الأول.**

### ⛔⛔★★★ و`A-01` مصونٌ بحرفه — والاتّساعُ في القراءة وحدها

`A-01`: «⛔ **لا يُجمَع بين مصدرين في أي عملية**» — ★ **وهو قائمٌ كما هو:**

| البعد | الحكم بعد الدمج |
|---|---|
| ★ **الكتابة** | ⛔ **مصدرٌ واحدٌ لكل مستند** — ★ **يُختار في النموذج** (`_sourceId`) |
| ★ **القراءة** | ✅ **تتّسع إلى كل المصادر** — ⛔ **باستعلامٍ مقيَّدٍ لكلِّ مصدرٍ ثم دمج** ⛔ **لا استعلامٌ واحدٌ غير مقيَّد** (`IQ-024` · `DEBT-40`) |
| ★ **العرض** | ⛔⛔ **واسمُ المصدر إلزاميٌّ في كل صفٍّ في وضع «الكل»** — ⟵ **فصفٌّ بلا مصدرٍ في قائمةٍ تجمع مصادرَ لا يُقرأ** |
| ★ **الرصيد والحساب** | ⛔ **بلا مساس إطلاقاً** — ★ **`FR-M8-04`: رصيدُ كل نوعٍ مستقلٌّ في كل مصدر** |

⛔⛔ **ولا قاعدةَ حمايةٍ تُمَسّ** — ★ **ولا مفتاحَ صلاحيةٍ يُضاف** (`BR-M1-07`).

## 9-و. ★★★ ما نُفِّذ فعلاً في `WU-019` (§5 المتبقي المتأخر) — ومواضعه

| ما في §5 من هذا المستند | الكود المنفِّذ |
|---|---|
| **بند متبقٍّ = رصيد (مصدر × نوع × تاريخ مخزون سابق) > 0** | ★★★ **قائمةٌ مبنيةٌ مسبقاً `aged_remainders`** يكتبها ويمحوها [`aged_remainder.dart`](../../../functions/lib/src/aged_remainder.dart) **من كتابات الرصيد نفسِها داخل المعاملة** — ⛔ **لا بمشغّلٍ بعد الالتزام ولا بجدولة** (نظيرُ `pending_entries.dart` حرفياً) · ★ **موجودٌ ⟺ الرصيد موجبٌ تماماً** |
| **عمر البند = تاريخ اليوم − تاريخ المخزون** | ★★ [`agedRemainderAgeInDays`](../../../packages/qtms_domain/lib/capabilities/inventory/domain/aged_remainder.dart) — ⛔⛔ **ويُحسَب عند القراءة ولا يُخزَّن**: ★ **العمرُ دالّةٌ في اليوم الحالي يشيخ بلا كاتب** ⟵ **وحقلٌ مخزَّنٌ له يُلوِّن بندَ خمسةِ أيامٍ بلون «يوم–يومان»** (⚠️ **وصُحِّح `data-dictionary.md` §4 تبعاً لذلك**) |
| **تدرّج لوني 🟡 1–2 · 🟠 3–4 · 🔴 5 فأكثر** | ★ `agedRemainderSeverityOf` **بحدود المتطلب حرفياً** — ⚠️★★ **وثلاثُ درجاتٍ بعائلتين وأيقونتين** (`aged_remainder_screen.dart`): ⛔ **لا عائلةَ برتقاليةَ في لوحة التوكنز**، ★ **وإدخالُ لونٍ خارج الشعار يحتاج قرارَ مالكٍ مسجَّلاً كـ`DS-003`** ⟵ **والعمرُ مكتوبٌ رقماً في كل صفٍّ فلا معنى يُنقَل باللون وحده** |
| **إجراءات التصريف: توزيع · بيع نقدي** | ★★★ **تفتح `M10`/`M11` نفسَها مثبَّتةً** (`pinnedStockDate` · `pinnedItemKey`) — ⛔ **ولا شاشةَ تصريفٍ بديلة ولا مسارَ كتابةٍ ثانٍ** · ★ **والمصدرُ مقفلٌ وشريطُ التنبيه بنصّ `FR-M8-12` حرفياً** |
| **«المسار الوحيد المصرَّح به لتاريخ مخزون أقدم»** | ★★★ [`agedClearanceRejection`](../../../functions/lib/src/aged_remainder.dart) — **دالةٌ واحدة يشاركها التوزيعُ والبيعُ النقدي** (⛔ **لا نسخةَ ثانية**): **مساوٍ ⟵ قبول · أقدم ⟵ `agedRemainderClear` · أحدث ⟵ رفضٌ للجميع** · ★ **وتُقارَن بيوم المنصّة المقروء داخل المعاملة** ⛔ **لا بساعة الحاوية** |
| **«ويُطلق إعادة بناء ملخصات اليوم الأصلي ووسمها ⟳»** | ✅ **مُنفَّذ بلا آليةٍ جديدة:** ★ **`buildDailySummariesAfterCommit` يُمرَّر له *يومُ المنصّة* لا يومُ المخزون** ⟵ **فـ`markRetro = date < today` يصدُق** ⛅ **ومقيسٌ حيّاً: `retroUpdatedAt` على `daily_summaries/SRC-001_20260830`** |
| **الإتلاف** | ✅★★★ **مُنفَّذٌ في `WU-020`** (2026-09-04) — ★ **زرٌّ ثالثٌ في بطاقة البند يفتح شاشةَ الإتلاف مثبَّتةً بنفس الآلية** (`agedClearanceFocusProvider`) ⛔ **لا بمسارِ كتابةٍ ثانٍ**: ⟵ **راجع §9-ز** |

> ⛔⛔★★★ **وقيدُ التدقيق يحمل التاريخين معاً** — `AT-52` · `FR-SYS-18` · `FR-M18-08`:
> ★ **الفعلُ `agedRemainderClear` باسمه** ⛔ **لا «إنشاءً» عادياً**، ★ **و`occurredAt`
> تاريخُ الإدخال و`stockDate` تاريخُ المخزون** — ⟵ **ويفترقان في هذا المسار وحده.**

> ⚠️⚠️ **وحدٌّ معلَن — [`DEBT-96`]:** ★ **القائمةُ تُبنى بالكتابة**، ⟵ **فأرصدةٌ
> كُتبت قبل نشر الراصد لا يظهر لها بندٌ حتى تُلمَس بعمليةٍ جديدة**. ⛔ **ولا رقمَ
> خاطئ:** ★ **ما يظهر صحيحٌ تماماً** — **والمسحةُ الأولى جزءٌ من `WU-021`.**

## 9-ز. ★★★ ما نُفِّذ فعلاً في `WU-020` (الإتلاف) — ومواضعه

| ما في §5 و`FR-M8-16` | الكود المنفِّذ |
|---|---|
| **خروجٌ مخزنيٌّ بصلاحية «الإتلاف»** | ★★ [`disposal.dart`](../../../packages/qtms_domain/lib/capabilities/inventory/domain/disposal.dart) **نطاقاً** · ★★★ [`disposal.dart`](../../../functions/lib/src/disposal.dart) **سحابةً** (`planDisposal` — **ثلاثةُ مفاتيحَ مستقلة**: `disposalCreate` · `disposalAmend` · `disposalCancel`) |
| ⛔ **بلا أي قيمة ولا استحقاق للرعوي** | ⛔⛔★★★ **غيابٌ بنيويٌّ لا شرطٌ يُفحَص:** ★ **لا نوعَ في طبقة النطاق يحمل مبلغاً** — ⟵ **فلا مسارَ يُمرِّره**، ★ **ولا `dealer_ledger` ولا `supplier_ledger` في كتاباتها** (`A-15` · `GR-29`) ⛅ **مقيسٌ حيّاً بعدّ المجموعات المكتوبة** |
| ⛔ **ولا يدخل سعرَ الجونية ولا المبيعات** | ★★ **حزامٌ وحمّالة:** ⟵ **`readSackMovementDocuments` لا تقرأ `disposals` أصلاً** (**فما لا يُقرأ لا يُجمَع**) · ★ **والحركةُ تُوسَم `movementTag = disposal`** ⛔ **فأيُّ قارئٍ لاحقٍ يراها مستثناةً بالوسم** |
| **سببٌ نصيٌّ اختياري** (`ADR-0020`) | ★ **حقلٌ مَوْسومٌ «اختياري» في الشاشة** ⛔ **ولا يُعبَّأ نيابةً عن المستخدم** — ★ **والفراغاتُ تُقرأ غياباً في الطرفين** |
| **والنافذة تحمل تحذيراً صريحاً** | ★★ [`disposal_screen.dart`](../../../lib/capabilities/inventory/presentation/disposal_screen.dart) — **شريطٌ تنبيهيٌّ بعائلة `warning` بأيقونةٍ ونصّ** (⛔ **لا لونٌ وحده** — `design-system.md` §8) |
| **إتلافُ متبقٍّ متأخر** (`E-25` · `FR-M8-11` ③) | ★★★ **يُفتَح مثبَّتاً بنفس `agedClearanceFocusProvider`** — ⟵ **وحارسُه `agedClearanceRejection` نفسُه** ⛔ **لا حارسٌ ثانٍ**: ★ **ورقمُ المستند يحمل تاريخَ المخزون** (`DSP-20260830-…`) |
| **نسبة الإتلاف ٪** (`design-overview.md` §2.10) | ★ [`disposalRatePercent`](../../../packages/qtms_domain/lib/capabilities/inventory/domain/disposal.dart) — ★ **للعرض فقط** (§2.11) · ⛔ **و`null` لواردٍ صفري** (⛔ **لا صفرٌ يُقرأ «لا إتلاف»**) · ⛔ **ولا تجمع وحدتين** |
| **تقرير `R-07`** | ★★ `buildWasteAndDisposalReport` — **الوزنُ الضائع والسكربُ والإتلاف في جدولٍ واحد** (`FR-M19` §2) |

> ⛔⛔★★★ **ولا تُطلَق بعده إعادةُ احتساب سعر الجونية ولا بناءُ ملخصات ضمار
> المالك** — ★ **والغيابُ مقصودٌ ومقروء في ترويسة `disposal_handler.dart`**:
> ⟵ **استدعاءٌ بلا أثرٍ يُوهِم القارئَ أن للإتلاف مدخلاً فيهما.**
> ✅ **ويُطلَق راصدُ المتبقي المتأخر وبنودُ `M9` المعلّقة داخل المعاملة نفسِها**
> — ⛅ **مقيسٌ حيّاً: البندُ 87 ⟶ 84 بعد إتلافِ ثلاثِ حبات.**

## 9-ح. ★★★ ما نُفِّذ فعلاً في `WU-022` (§7 الجرد) — ومواضعه

| ما في §7 و`FR-M16` | الكود المنفِّذ |
|---|---|
| **مصدرٌ واحدٌ وتاريخُ مخزونٍ واحد** | ★★ [`stocktake.dart`](../../../packages/qtms_domain/lib/capabilities/inventory/domain/stocktake.dart) **نطاقاً** · ★★★ [`stocktake.dart`](../../../functions/lib/src/stocktake.dart) **سحابةً** (`planStocktake` — **أربعةُ مفاتيحَ مستقلة**: `stocktakeWrite` · `stocktakeApprove` · `stocktakeAmend` · `stocktakeCancel`) |
| ★★★ **ويُجمَّد الرصيدُ الدفتري عند البدء** | ⛔⛔★★★ **مستندٌ بمرحلتين لا نداءٌ واحد:** ★ **`startStocktake` يقرأ الرصيدَ من الدفتر بـ`computeItemDailyFlow` ويكتبه في سطور المستند** ⛔ **بلا حركةٍ واحدة** (`touchesLedger == false`) — ★★ **و`approveStocktake` يحسب الفرقَ على الرصيد *المُجمَّد*** ⛔ **لا على رصيدٍ يُعاد قراءتُه**: ⟵ **وحركةٌ وقعت أثناء العدّ تبقى محسوبة**، ★ **وإلا فقد التجميدُ معناه كلَّه** |
| ⛔ **ولا رصيدَ دفتريٍّ يصعد من الجهاز** | ★★★ [`readFrozenLines`](../../../functions/lib/src/stocktake.dart) **تقرؤه من المستند** — ⛔ **والحمولةُ تحمل `actualCount` وحدَه** ([`functions_stocktake_repository.dart`](../../../lib/capabilities/inventory/infrastructure/functions_stocktake_repository.dart) — **لا `bookBalance` ولا `difference`**): ⟵ **وقبولُه كان يجعل الفرقَ رقماً يختاره المُرسِل** |
| **الفرق = العدُّ − الدفتر بإشارته** (`FR-M16-01`) | ★ [`stocktakeDifference`](../../../packages/qtms_domain/lib/capabilities/inventory/domain/stocktake.dart) — ⛔ **ولا قيمةَ مطلقة تُخفي اتجاهه** · ★★ **و[`stocktakeAdjustment`](../../../packages/qtms_domain/lib/capabilities/inventory/domain/stocktake.dart) تقلبه حركةً**: **الموجبُ دخول والسالبُ خروجٌ بكميةٍ مطلقة** ⛔⛔ **والصفرُ بلا حركةٍ أصلاً** ⛔ **لا حركةٌ صفرية** |
| ⛔ **ولا تُطرَح حبّةٌ من كيلوجرام** (`GR-19` · `E-31`) | ★ **`stocktakeDifference` تُرجِع `Failure` بـ`ruleCode: 'GR-19'`** — ★ **وتُترجَم `ERR_SETUP_009` في السحابة** ⛔ **لا سقوطٌ صامت** |
| **الوسمُ `movementTag = adjustment`** | ★★ [`stocktake_handler.dart`](../../../functions/lib/src/stocktake_handler.dart) — **كلُّ حركةِ تسويةٍ مَوْسومة** ⛔ **وبلا `sackId` إطلاقاً**: ⟵ **فما لا يحمل جونيةً لا يدخل تسعيرَها** |
| ⛔ **وتُستثنى من سعر الجونية واستحقاق الرعوي** (`BR-M16-03` · `AT-66`) | ⛔⛔★★★ **غيابٌ بنيويٌّ مقروءٌ في ترويسة المنفّذ:** ★ **لا `revalueSacksAfterCommit` ولا `buildDailySummariesAfterCommit` ولا `dealer_ledger` ولا `supplier_ledger`** — ⛅ **مقيسٌ حيّاً بعدّ المجموعات المكتوبة** ([`verify_stocktake_write.py`](../../../tools/staging/verify_stocktake_write.py)) |
| ✅ **وما يُطلَق فعلاً — وعند الاعتماد وحده** | ★ **راصدُ المتبقي المتأخر وبانى البنود المعلّقة داخل المعاملة نفسِها** — ⛔⛔ **والمسوّدةُ لا تُشغِّل أياً منهما**: ★★ **والمِعيارُ `StocktakeAccepted.touchesLedger`** ⟵ **كتاباتُ الخطة لا اسمُ العملية**، ⛔ **وشرطٌ على الاسم كان ينزلق عند أول عمليةٍ خامسة** |
| ★★ **سببُ الفرق اختياريٌّ** (`FR-M16-04` · `ADR-0020`) | ★ **حقلٌ مَوْسومٌ «اختياري» لكل سطر** ⛔ **ولا يُعبَّأ نيابةً عن المستخدم** — ★ **والفراغاتُ تُقرأ غياباً في الطرفين** (`blankToNull`) |
| ⛔⛔ **ولا جردان مفتوحان على يومٍ واحد** (`FR-M16-06`) | ★★★ **استعلامٌ داخل المعاملة على (`sourceId` × `stockDate`)** — [`stocktakeSameDayQuery`](../../../functions/lib/src/stocktake_handler.dart) بحدٍّ 20: ★ **والمسوّدةُ وحدَها تحجب** (`ERR_STOCK_006`) ⛔ **والمعتمَدُ والملغى لا يقفلان اليوم** |
| ★★ **جردُ يومٍ سابقٍ بصلاحية** (`FR-M16-08`) | ★★★ **مفتاحٌ تاسعٌ وثمانون `stocktakePriorDay`** — ★ **و`stockDate` من الحمولة لا من المنصّة عند البدء وحدَه** (⛔ **بخلاف `outflow_handler.dart`**): ★ **وغيابُه هو الحالُ الأصلي (جردُ اليوم)** ⛔ **ونصٌّ مشوَّه رفضٌ صريح لا سقوطٌ صامتٌ إلى «اليوم»** (`RISK-07`) |
| ⟵ **ويُعاد بناءُ ذلك اليوم بأثرٍ رجعي** | ✅ **داخل المعاملة:** `item_daily_balances` **و**`aged_remainders` — ★★ **وملخصاتُ اليوم بـ`rebuildDayRetroactively`** (`WU-021` · `api-overview.md` §3.1-ن) |
| **حالةُ المستند حارسٌ صريح** | ★★ [`_existenceGate`](../../../functions/lib/src/stocktake.dart) — **الاعتمادُ يحتاج مسوّدة والتعديلُ يحتاج معتمَداً** (`ERR_STOCK_007`) · **والملغى يُرفَض بـ`ERR_AMEND_006`** ⛔ **ولا حذف** (`GR-07`) |
| **الشاشة** | ★★ [`stocktake_screen.dart`](../../../lib/capabilities/inventory/presentation/stocktake_screen.dart) — **مرحلتان في شاشةٍ واحدة**: ★ **نموذجُ البدء ثم نموذجُ العدّ بفارقٍ حيٍّ لكل سطر** · ★★ **والاسمُ المعروض `ledgerItemDisplayName`** ⛔ **لا `itemName` المجرَّد** ([`DEBT-86`] ③) |

> ⛔⛔★★★ **وأخطرُ ما قِيس على المحاكي — ثلاثةُ عيوبٍ ظهرت هناك وحدها:**
> ① **اسمُ سطرِ الجونية كان يُعرَض مجرَّداً** (`عتود` لا `عتود - جونية رقم 1`)
> ⟵ **فيلتبس سطران في شاشة العدّ** · ② **وشريطُ النجاح كان يضيع** لأن الاعتماد
> يُغلق المسوّدة فيُستبدَل النموذجُ الحاملُ للحالة ⟵ **فرُفعت الحالةُ فوق
> النموذجين** · ③ **والزرُّ كان يقول «جارٍ الاعتماد…» أثناء الإلغاء** ⟵ **فصار
> [`_InFlight`](../../../lib/capabilities/inventory/presentation/stocktake_screen.dart)
> نوعاً ثلاثياً.** ★★ **ولكلٍّ منها اختبارُ انحدارٍ مضاف** — ⛔ **ولا واحدٌ منها
> كان يظهر في `flutter test`.**


## 9-ط. ★★★ ما غيّره `AM-021` — أربعُ شاشاتٍ من مراجعة تجربة الاستخدام (2026-09-15)

⛔⛔★★★ **ولا قاعدةَ عملٍ واحدةٍ مُسَّت** — ★ **كلُّ ما تغيّر في طبقة العرض**:
⟵ **ما تغيّر هو *متى* يُقال الشيءُ وبأيّ وضوح**، ⛔ **لا ما يُقال ولا ما يُرفَض.**

| # | الشاشة | ما تغيّر | ★ المبرّر من داخل النظام |
|:-:|---|---|---|
| ① | **الوارد عدداً** (`CountedIntakeTab`) | ★★★ **الملخّصُ التشغيلي صار العنوان** (`titleSm`) — «**مصدر رداع · 1 نوع · 120 حبة**» — ★ **ورقمُ المستند سطراً ثانياً** (`bodyMd` · `textSecondary`) | `design-system.md` §6-د (**هرمُ بطاقة الكيان**) · `ui-guidelines.md` §1: ⟵ **«ضع الجواب في الأعلى»** — ★ **والسؤالُ في قائمةِ واردِ اليوم «كم دخل ومن أين»** ⛔ **لا «ما رقمُ المستند»**: ⟵ **الرقمُ يُعرَف بعد إيجاد الصفّ لا قبله.** ⚠️ **وهو قلبٌ مُعلَنٌ لترتيبِ `AM-009` ③** — ⛔ **لا نقضٌ له:** ★ **شرطُه «واسمُ المصدر ظاهرٌ في وضع الكل» قائمٌ بحرفه**، ★ **وقد ارتفع رتبةً بدل أن ينزل** |
| ② | **التسعير اليومي** | ★★ **زرُّ «حفظ أسعار اليوم» صار ثنائيَّ الحالة** — «**جارٍ الحفظ…**» أثناء النداء مع تعطيلٍ فعلي | `design-system.md` §6-ي **البند ②** نصّاً: ⛔ **«وزرٌّ معطَّلٌ بلا بديلٍ يُقرأ ممنوعاً لا مشغولاً»** — ⟵ **والتعطيلُ كان قائماً وحدَه** (البند ①)، ★ **فاكتمل العقدُ بأربعته** |
| ③ | **التسعير اليومي** | ⛔⛔★★★ **وفراغُ المرشِّح فُصل عن فراغ البيانات** — ★ **«كل الأنواع مسعَّرة بالفعل ضمن هذا المرشِّح» بإجراءٍ أساسيٍّ واحد: «اعرض الكل»** | `design-system.md` §هـ (**الحالة الفارغة**): ⟵ **«لا شيء يُسعَّر بعد» فوق يومٍ سُعِّر بالكامل تقول عكسَ الحقيقة** ⛔ **وتدفع المستخدمَ يسجّل وارداً لا يحتاجه** · ★ **والشرطُ مقيسٌ لا مُفترَض:** **القائمةُ غيرُ المرشَّحة غيرُ فارغة** (`pricingRowsProvider`) **والمعروضةُ فارغة** (`filteredPricingRowsProvider`) |
| ④ | **الإتلاف** | ★★ **سطرُ النتيجة صار `QtmsActionStatus`** — **`.success` و`.rejection` بثلاثيةٍ كاملة** ⛔ **بعد أن كان `Text` عارياً يحمل `✅`/`❌`** | `design-system.md` §6-ز (**سطرُ نتيجة النداء**) · §8 المحظور 12: ⟵ **الإيموجي يتبع خطَّ الجهاز ولا يُلوَّن**، ★ **وسطرا النجاح والفشل كانا يُقرآن سواءً** ⛔ **على شاشةٍ تُخرِج قاتاً بلا مقابل** (`A-15`) |
| ⑤ | **الجرد** | ⛔⛔★★★ **إلغاءُ الجرد صار يمرّ بـ`showQtmsDestructiveSheet`** — **عنوانٌ برقم المستند · وجملةُ أثرٍ صريحة · وسببٌ اختياري · وزرُّ تأكيدٍ `danger.ink` منفصلٌ عن «تراجع»** | `ADR-0021` `P6` · `design-system.md` §6-و: ⟵ **كان `TextButton` أحمرَ ينفّذ بضغطةٍ واحدة مباشرة** ⛔ **بلا تأكيدٍ ولا مخرَج**، ★ **بينما إلغاءُ الوارد والجونية والتوزيعة والبيع النقدي كلُّها بالورقة** — ⟹ **فمستوى الحمايةِ كان يتفاوت بين خمسِ عملياتٍ متكافئةِ الخطر** |
| ⑥ | **الجرد** | ★★★ **وحرسُ اكتمال العدّ صار يُسمّي السطرَ الناقص** — «**أدخل العدّ الفعلي لـ«عتود - جونية رقم 1» (وسطرٌ آخرُ ناقص)**» ★ **مع حدٍّ لونيٍّ `danger` حول حقل كل سطرٍ ناقص** | `ui-guidelines.md` §6 (**رسالة خطأ مفيدة**): ⛔ **«أدخل العدّ لكل نوع» على جردٍ بعشرين سطراً تترك المستخدمَ يمسح القائمةَ يدوياً** — ★ **والاسمُ المعروض `ledgerItemDisplayName`** ([`DEBT-86`] ③) ⛔ **لا `itemKey` خاماً** (§8 المحظور 13) |

> ⛔⛔★★★ **وحقلُ سبب الإلغاء لم يُجعَل إلزامياً** (`ADR-0020`) — ★ **الورقةُ
> ترسمه «(اختياري)» ولا تُعطِّل زرَّها لغيابه**، ⛔ **ولا يُعبَّأ نيابةً عن
> المستخدم**: ⟵ **و`blankToNull` في الطرفين كما كانت.**
>
> ⚠️★★ **والسببُ المشترك بين ⑤ وحقل «ملاحظة على الجرد» حُسم بنيوياً:** ★ **الحقلُ
> في جسم النموذج يخصّ *الاعتماد* وحدَه بعد `AM-021`** — ★ **وسببُ الإلغاء يُكتَب
> في الورقة نفسِها**، ⛔ **وكان الحقلُ الواحدُ يُرسَل للعمليتين معاً**: ⟵ **فمن
> كتب ملاحظةً على العدّ ثم ألغى، أُرسلت ملاحظتُه سبباً للإلغاء** — ★ **وهو ما
> تمنعه القاعدةُ الوحيدة الباقية من `ADR-0004`**: ⛔ **«ما لم يكتبه إنسانٌ *لهذه
> العملية* لا يُرسَل».**


## 10. الاختبارات المرتبطة

`TC-M6-*` · `TC-M8-001` … `TC-M8-005` · `TC-M9-*` · `TC-M16-*` ·
`AT-05` · `AT-16` … `AT-19` · `AT-51` … `AT-54` · `AT-66`

---

## 9-ي. ★★★ ما غيّره `AM-027` في شاشة الوارد عدداً (2026-09-17)

| # | البند | العقد المُلزِم بعد `AM-027` |
|:-:|---|---|
| ① | ✅★★★ **«إضافة نوع جديد» بجوار «إضافة نوع»** | ★ **الزرّان مختلفان لا مكرَّران:** «إضافة نوع» ⟵ **صفٌّ لنوعٍ قائم** · «إضافة نوع جديد» ⟵ **سجلُّ النوع نفسُه** — ★ **يفتح `showItemForm` مُهيَّأةً باسم النوع كما كُتب في منسدل الصفِّ الأخير وبمصدر المستند مُختاراً** ⟹ **فالنوعُ يُولَد مرتبطاً بمصدره** (`FR-M6-05`: **القائمةُ مفلترةٌ بالمصدر حصراً**) ⛔ **ولا يُنشأ ثم يغيب عن القائمة نفسِها.** |
| ② | ⛔⛔★★ **ويظهر ولو كانت القائمةُ فارغة** | ★ **وهو موضعُ العطل المقيس:** ⟵ **«لا توجد أنواع مرتبطة بهذا المصدر.» كانت تُعرَض بلا أيِّ زرّ** (**`if (items.isNotEmpty)` يُخفي زرَّ الإضافة**) ⟹ ⛔ **طريقٌ مسدودٌ يُخرِج المستخدمَ من النموذج إلى شاشة «الأنواع» ثم يُعيده.** ★ **والرسالةُ باقيةٌ كما هي** — ⛔ **لا تُحذَف**: ⟵ **تقول سببَ الفراغ، والزرُّ يقول ما الفعلُ التالي** (`design-system.md` §هـ: **فراغٌ بلا إجراءٍ ممنوع**). |
| ③ | ✅★★ **والنوعُ المُنشأ يُسنَد فوراً إلى صفِّه** | ★ **يحلّ في الصفِّ الفارغ الأخير إن وُجد، وإلا أُنشئ له صفٌّ جديد** ⟹ **فالإنشاءُ ينتهي بسطرٍ جاهزٍ للعدد.** |
| ④ | ⛔⛔ **ومنعُ التكرار في طبقتين** | ① ★ **حارسُ عرضٍ في النموذج قبل النداء** (`CatalogMessage.itemNameExists`) · ② ⛅ **والدالةُ السحابية تُعيد الفحصَ كما هي** (`ERR_SETUP_003`) ⛔ **ولم تُمَسّ.** |
| ⑤ | ★ **الصلاحية** | `PermissionGate(Permission.itemWrite)` — ⛔ **لا مفتاحَ جديد** · ⚠️ **وإخفاءٌ لا حماية.** |
| ⑥ | ⛔ **وما لم يتغيّر** | ★ **تصفيةُ `intakeItemsProvider` بحرفها** (**نشطٌ · `piece` · غيرُ افتراضيٍّ · مرتبطٌ بالمصدر**) — ⟵ **والنوعُ المُنشأ يمرّ بها كبقيّته**: ⛔ **فإن أُنشئ بلا هذا المصدر لم يظهر**، ★ **ولذلك يُهيَّأ النموذجُ بالمصدر مُختاراً أصلاً.** ⛔ **ولا مساسَ بالكمية ولا بالتحقق ولا بمسار الكتابة.** |
