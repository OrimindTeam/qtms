# تصميم وحدة: مالية الجواني وحساب الرعوي (`sack-valuation`)

| البند | القيمة |
|---|---|
| **الحالة** | ✅ **منفَّذ** (`WU-015` — 2026-09-03) · ⏳ **بانتظار اختبار المحاكي §د.2** |
| **الوحدات المغطّاة** | `M14` |
| **يعتمد على** | `sack-intake` · `distribution` · `outflow` |
| **المتطلبات** | `FR-M14-01` … `FR-M14-15` |

> ⚠️ **إحدى أغلى عمليتين في النظام** — راجع §6.

---

## 1. المسؤوليات

1. **احتساب سعر الجونية من الإيراد الفعلي المتحقق** (`A-13`).
2. احتساب ضريبتها وصافي الرعوي **في مصدرها**.
3. الكتابة في **دفتر حسابات الرعية**.

## 2. الخوارزمية الملزمة

```text
عند أي تغيير يمسّ نوعاً من جونية
(توزيع · بيع نقدي · سحبية · خرجية · تسعير · تعديل · إلغاء · تصريف متأخر):

① تُجمع كل حركات الخروج المرتبطة بمعرّف تلك الجونية
② تُستبعَد:  الحركات الملغاة  ·  الإتلاف  ·  تسويات الجرد
③ لكل حركة باقية يُقرأ سعرها الفعلي المسجَّل في مستندها
④ سعر الجونية = Σ (الكمية × السعر الفعلي)   [للمسعَّرة فقط]
⑤ ضريبة الجونية = round(قيمة ضريبة الكيلو × الوزن الكلي)   ← لأقرب ريال
⑥ الصافي = سعر الجونية − الضريبة
⑦ يُكتب سطر جديد في دفتر حسابات الرعية على حساب الرعوي
   في مصدر الجونية، برقم إعادة احتساب متزايد
⑧ يُحدَّث رصيد الرعوي في ذلك المصدر
⑨ إن بقيت حركات غير مسعَّرة تُوسَم الجونية «⏳ سعر غير نهائي»
```

## 3. جدول ما يدخل الاحتساب وما لا يدخل

| الحالة | يدخل سعر الجونية؟ | يستحق الرعوي؟ |
|---|:-:|:-:|
| توزيع مسعَّر | ✅ | ✅ |
| بيع نقدي | ✅ | ✅ |
| **سحبية أو خرجية (بند قات مسعَّر)** | ✅ | ✅ |
| **إيراد السكرب** | ✅ | ✅ |
| **تصريف متبقٍ متأخر** | ✅ **متى صُرِّف ولو بعد أيام** | ✅ |
| سطر غير مسعَّر بعد | ❌ حتى يُسعَّر | — |
| **كمية لم تُصرَف وبقيت في المخزن** | ❌ | ❌ |
| **الإتلاف** | ❌ | ❌ |
| **الوزن الضائع** | ❌ | ❌ |
| **تسوية الجرد** | ❌ | ❌ |
| حركة ملغاة | ❌ | ❌ |
| الثلج | ❌ | ❌ |

> **المبدأ الجامع (`A-15`):** **كل قات يخرج من المخزن يستحق الرعوي ثمنه —
> والاستثناء الوحيد الإتلاف والوزن الضائع.**

## 4. دفتر حسابات الرعية

| الحقل | ملاحظة |
|---|---|
| `supplierId` · `supplierName` | — |
| **`sourceId`** | **إلزامي — لكل رعوي حساب في كل مصدر** |
| `sackId` · `sackDisplayName` | — |
| `sackRevenue` · `sackTax` · **`supplierNet`** | ★ **الدفتر كله محكوم بـ`supplierFinanceView`** في قاعدة الحماية · ومصدر هذه القيم على الجونية صار **`sacks/{sackId}/finance/current`** (`ADR-0011`) |
| `entryDate` · `memo` | — |
| **`recalcVersion`** | **رقم إعادة احتساب متزايد** — لأن الصافي **رقم حيّ** |
| `isCancelled` · `lastAmendedAt` | — |

> **`recalcVersion` ليس ترفاً:** الصافي يتغيّر عشرات المرات في عمر الجونية،
> **والرقم المتزايد يمنع تطبيق حساب قديم فوق حساب أحدث** عند تنفيذ
> المشغّلات خارج الترتيب.

## 5. رصيد الرعوي

```text
إجمالي سعر الجواني في المصدر = Σ(سعر كل جونية له في هذا المصدر)
إجمالي الضريبة في المصدر     = Σ(ضريبة كل جونية له فيه)
صافي الرعوي في المصدر        = الأول − الثاني

الإجمالي عبر كل المصادر = Σ(صوافيه في كل مصدر)
                          ← للعرض فقط · ولا يُنشئ حساباً موحّداً
```

## 6. الأداء — نقطة الانتباه الحرجة

> ⚠️ **هذه من أغلى عمليتين في النظام** (مع إعادة البناء بأثر رجعي).
> فهي تُطلَق عند **كل** توزيع وبيع وسحبية وتسعير وتعديل وإلغاء وتصريف —
> **وتقرأ كل حركات الجونية في كل مرة**.

**قواعد التخفيف الملزمة:**
1. **التنفيذ بتجميع (Batching)** لا بعد كل حركة منفردة حيث أمكن.
2. **فهرس مركّب `sackId ↑ · stockDate ↓`** — بلاه تصير العملية مسحاً كاملاً.
3. **قابلة للتكرار بلا أثر جانبي** — تُعيد البناء من الحركات بالكامل
   **ولا تُراكِم** على قيمة سابقة (`PAT-08`).
4. **لا تُنفَّذ على الجهاز** — قراءة ثقيلة لا تُحمَّل عليه.

## 7. النطاق — تنبيه ملزم

> **لا توجد في هذا الإصدار أي عملية تسديد للرعوي.** الصافي **يُعرض
> ويُقيَّد فقط**، وشاشة التسديد **مؤجَّلة صراحةً للمرحلة الرابعة** —
> **ويجب إبلاغ الرعية بذلك** تفادياً لسوء الفهم (`stakeholders-register.md`).

## 8. الحالات الحدّية

| # | الحالة | المعالجة |
|---|---|---|
| `E-20` | تعديل سعر سطر بعد الاحتساب | **إعادة احتساب تلقائية** للسعر والصافي |
| `E-21` | كمية لم تُصرَف | **لا تُحتسب** — الجونية تُقيَّم بما تحقق منها |
| `E-26` | سحب المالك قاتاً | **يدخل السعر** فيستحق الرعوي ثمنه |
| `E-27` | بند قات بلا سعر | الجونية **«⏳ سعر غير نهائي»** |
| `AT-53` | تصريف متأخر من الجونية | يُضاف لسعرها **بأثر رجعي** |
| `AT-47` | رعوي في مصدرين | **حسابان منفصلان تماماً** |

## 9-أ. ✅★★★ حاشيةُ التنفيذ (`WU-015` — 2026-09-03)

★ **الخوارزمية §2 مُنفَّذة حرفياً بثلاث طبقات:**

| الطبقة | الملف | ما يحمله |
|---|---|---|
| **النطاق** | [`sack_valuation.dart`](../../../packages/qtms_domain/lib/capabilities/inventory/domain/sack_valuation.dart) | `computeSackRevenue` (④) · `computeSupplierNet` (⑥) · `computeSupplierSourceTotals` (§5) · `computeAverageKiloPrice` (الموضع ②) |
| **النطاق** | [`sack_movements.dart`](../../../packages/qtms_domain/lib/capabilities/inventory/domain/sack_movements.dart) | `contributionsForSack` (① و②) — ★ **يشاركه التطبيق والسحابة** (`ADR-0012`) |
| **السحابة** | [`sack_valuation.dart`](../../../functions/lib/src/sack_valuation.dart) · [`sack_valuation_handler.dart`](../../../functions/lib/src/sack_valuation_handler.dart) · [`sack_movement_reader.dart`](../../../functions/lib/src/sack_movement_reader.dart) | ⑦ و⑧ و⑨ — **الكتابة والقراءة والتطبيع** |
| **التطبيق** | [`sack_finance_screen.dart`](../../../lib/capabilities/inventory/presentation/sack_finance_screen.dart) · [`sack_breakdown_sheet.dart`](../../../lib/capabilities/inventory/presentation/sack_breakdown_sheet.dart) · [`sack_tax_sheet.dart`](../../../lib/capabilities/inventory/presentation/sack_tax_sheet.dart) | **العرض والتفكيك وإدخال الضريبة** |

### ⛔⛔★★★ قراران تنفيذيان مُعلَنان — ⛔ لا يُطبَّقان صامتاً

**① «سطرٌ واحدٌ لكل جونية يُعاد كتابته» لا «سطرٌ لكل احتساب»** — ★ **والخطوة
⑦ تقول «يُكتب سطر جديد … برقم إعادة احتساب متزايد»**: ⟵ **وقُرئت
استبدالاً لا إلحاقاً**، ★ **لأن تعليل `recalcVersion` نفسَه في §4 يقول
«يمنع تطبيق حساب قديم *فوق* حساب أحدث»** — ⛔ **و«فوق» لا معنى لها في
الإلحاق.** ⟵ ★ **ولأن §5 تجمع «سعر كل جونية» جونيةً لا سطراً**:
⛔ **فجمعُ السطور كان يُضاعف إيرادَ كلِّ جونيةٍ عُدِّلت مرة.**
★ **والمعرّف الحتمي `supplier_ledger/{sackId}`** — ⟵ **وهو ما يُحقّق القاعدة
3 من §6 حرفياً.**

**② المُحتسِب يعمل داخل الحاوية بعد التزام العملية الأصلية** — ⛔ **لا
بمشغّل Eventarc.** ★ **و`DEBT-16` يُسمّي `recomputeSackRevenue` ضمن
المشغَّلات المحجوبة به**، ⟵ **والمشروع عالج نظائرَها كلَّها** (`writeAuditEntry`
· `recalculate*` · `syncPendingEntries`) **بنقلها إلى مسار الكتابة نفسِه**:
★ **وهذا تطبيقٌ لسابقةٍ مقيسة** ⛔ **لا اختراعُ آلية.**
⛔⛔ **وفشلُه لا يُبطل العملية الأصلية** — `api-overview.md` §3.3 نصّاً.

### ★★ ما نُفِّذ من §6 حرفياً

| # | القاعدة | ✅ التنفيذ |
|:-:|---|---|
| 1 | **التجميع** | ★ **الوحدةُ (المصدر × تاريخ المخزون)** — ⟵ **مستنداتُ اليوم تُقرأ مرةً واحدة لكل جواني اليوم** ⛔ **لا استعلامٌ لكل جونية** |
| 2 | **الفهرس المركّب** | ⚠️★★ **لم يُستعمَل `sackId ↑ · stockDate ↓`** — ★ **الاحتسابُ يقرأ *المستندات* لا حركاتِ الدفتر**: ⟵ **لأن الخطوة ③ تشترط «سعرها الفعلي المسجَّل في مستندها»**، ⛔ **والدفترُ بلا سعر.** ★ **والفهارسُ المستعمَلة `sourceId ↑ · stockDate ↓` على `distributions` و`cash_sales` و`sacks`** — ★ **وأُضيف نظيرُها على `outflows`** (⛅ **منشورٌ ومقيس: 40 فهرساً**) |
| 3 | **قابلٌ للتكرار بلا أثر** | ✅ **مقيسٌ حيّاً** — ⟵ **تشغيلٌ ثانٍ بلا تغيير يُبقي `recalcVersion` كما هو** |
| 4 | **لا يُنفَّذ على الجهاز** | ✅ **الاحتسابُ سحابيٌّ وحده** — ★ **والتطبيق يقرأ المخزَّن**، ⛔ **ولا يحسبه**: ★ **والتفكيكُ في الشاشة يشرح ولا يُصحّح** |

### ⏳★★ وثلاثةُ بنودٍ من §2 لا كاتبَ لها بعد — ⛔ ولا تُدَّعى منفَّذة

★ **«الإتلاف» و«تسوية الجرد» في الخطوة ②** — ⛔ **لا مستندَ لهما اليوم**
(`WU-020` · `WU-022`): ★ **والحارسُ مبنيٌّ قبل كاتبه عمداً**
(`SackMovementDocument.isExcluded`) ⟵ **فلا يتذكّره كاتبٌ جديد في وحدةٍ
أخرى** — ★ **ومُختبَرٌ من طرفيه.**
★ **و«التصريف المتأخر» (`AT-53`)** — ⟵ **يعمل بنيوياً لأن الحركة تُسجَّل على
تاريخ مخزون بضاعتها** (`GR-49`)، ⛔ **ولا مسارَ له قبل `WU-019`.**

---

## 9. الاختبارات المرتبطة

`TC-M14-001` … `TC-M14-004` · `AT-44` … `AT-47` · `E-20` · `E-21`
