# تصميم وحدة: ضمار المالك وحركة النقد (`owner-ledger-summary`)

| البند | القيمة |
|---|---|
| **الحالة** | ✅★★★ **منفَّذ** (2026-09-03 · `WU-016`) — ⛅ **ومُثبَتٌ حيّاً على التجريبية** · ✅ **`OQ-001` محسوم (الخيار ج)** |
| **الوحدات المغطّاة** | `M15` |
| **يعتمد على** | `distribution` · `settlement` · `outflow` · `sack-valuation` |
| **المتطلبات** | `FR-M15-01` … `FR-M15-23` |

---

## 1. المسؤوليات

بناء **ملخصين مختلفين لسؤالين مختلفين**:

| السؤال | الملخص |
|---|---|
| **«كم لي اليوم من ذمم ونقد، وكم بقي؟»** | **بطاقة ضمار المالك** — لكل مصدر وللكل |
| **«النقد الذي دخل اليوم فعلاً، كم بقي منه في يدي؟»** | **حركة النقد في تاريخ** |

## 2. سجل ملخص اليوم

**المفتاح:** `{sourceId}_{date}` · و`all_{date}` للتجميعي.

| الحقل | المصدر |
|---|---|
| `credit` (الآجل) | Σ قيم ضمارات اليوم |
| `cash` (النقدي) | Σ صافي البيع النقدي في اليوم |
| `totalDebt` | `credit + cash` |
| `settledOfDay` (الواصل) | Σ المسدَّد **من ضمارات هذا اليوم** |
| `discounts` | Σ المخصوم **من ضمارات هذا اليوم** — **بند مستقل** |
| `tax` | Σ ضرائب جواني هذا اليوم في هذا المصدر |
| `withdrawals` · `expenses` | Σ سحبيات/خرجيات هذا اليوم لهذا المصدر |
| ★ `netFinal` | ★ **البند 10 — مبنيّ على «بعد الخصم»** (`OQ-001`) |
| **`retroUpdatedAt?`** | **وسم «⟳ مُحدَّث بأثر رجعي» وتاريخه** |

### ✅★★★ 2.1 سجلُّ السلسلة — `owner_ledger_trends` (`IQ-030` · الخيار أ · 2026-08-29)

**المفتاح:** `{sourceId}` · و`all` للتجميعي — ⛔ **مستندٌ واحدٌ لكل مصدر لا
مستندٌ لكل يوم**، ★ **يحمل آخرَ سبعِ قيمٍ للصافي النهائي.**

| الحقل | المصدر |
|---|---|
| `points` | ★ **مصفوفةٌ بسبعةِ عناصرَ كحدٍّ أقصى** — ★ **مرتَّبةٌ تصاعدياً بالتاريخ**. ⚠️⚠️★★ **وعنصرُها `{ date, netFinal, withdrawals, expenses, retroUpdated }`** — ✅ **زِيد حقلان عند التنفيذ** (2026-09-03 · `WU-016`): ⟵ **لأن القاعدة 5 أدناه تشترط أن يخرج البندُ المحكوم من صافي الرسم كما يخرج من البطاقة**، ⛔ **وذلك يستحيل من `netFinal` مجموعٍ وحده.** ⛔⛔ **ولا كشفَ جديداً فيهما:** ★ **البندان مقروءان أصلاً في `daily_summaries` بالشرط نفسِه** (`ADR-0011` القاعدة 4 — **الملخّصُ محكومٌ على مستوى المستند**)، ★ **والقاعدة 4 أدناه تشترط الشرطَ نفسَه** |
| `sourceId` | ⛔⛔★★ **حقلٌ مطلوبٌ لا اختياري** — ★ **شرطُ القراءة يعتمد `resource.data.sourceId`** (`storedInScope()`) ⟵ **وأيُّ سردٍ لا يُقيّده يُرفَض كاملاً** (`IQ-024` · `WU-008` · `DEBT-40`) |
| `updatedAt` | ★ **وقتُ آخر بناء** |

| # | القاعدة الملزِمة |
|:-:|---|
| 1 | ★★ **يُحدَّث في نفس الحدث الذي يبني ملخّصَ اليوم** — ⛔ **لا جدولةَ ولا مهمةَ ثانية**: ⟵ **فالأثرُ الرجعي (§7) يُصلحه نفسُ الحدثِ الذي يعيد بناء الملخّص**، ⛔ **ولا يبقى الرسمُ على رقمٍ لم يعد صحيحاً** |
| 2 | ⛔⛔ **ولا يُقرأ مصدرَ حقيقة** — ★ **مشتقٌّ من الدفتر كالملخّص سواءً بسواء** (`ADR-0008`) ⟵ **وقابلٌ لإعادة البناء بالكامل منه** |
| 3 | ★ **وقراءةُ الرسم كلِّه قراءةٌ واحدة** — ⟵ **وهو سببُ وجود هذا السجل**: ⛔ **سبعُ قراءاتٍ في كل فتحةٍ للبطاقة تخالف §8** |
| 4 | ⛔⛔ **وشرطُ قراءته هو شرطُ بطاقة «كل المصادر» نفسُه** (§5) — ⛔ **لا شرطٌ جديد يُخترَع** · ★ **والكتابةُ مغلقةٌ كبقية المجموعات** (`ADR-0013`: `allow create, update: if false`) |
| 5 | ★ **والبندُ المحكومُ بصلاحيةٍ يخرج من `netFinal` كما يخرج من البطاقة** — ⟵ **فلا يكشف الرسمُ ما تُخفيه البطاقة** (§5 · §10.1 القاعدة 2) |

✅★★★ **ونُفِّذ في `WU-016`** (2026-09-03) — ★ **مع باني الملخّص نفسِه**
([`owner_ledger_summary_handler.dart`](../../../functions/lib/src/owner_ledger_summary_handler.dart))
· [`firestore.rules`](../../../firestore.rules) `match /owner_ledger_trends/{trendId}` ·
[`owner_ledger_trend_chart.dart`](../../../lib/capabilities/financial_outflow/presentation/owner_ledger_trend_chart.dart).
⛅ **ومقيسٌ حيّاً:** `owner_ledger_trends/SRC-001` **و**`/all` **كُتبا في نفس حدثِ بناء الملخّص**
⛔ **بلا جدولةٍ ولا مهمةٍ ثانية.**

## 3. البنود العشرة والمعادلات

```text
1) إجمالي الضمار               = الآجل + النقدي
1-أ) منه آجل                   = Σ ضمارات اليوم
1-ب) منه نقدي                  = Σ البيع النقدي في اليوم
2) الواصل                      = Σ المسدَّد نقداً من ضمارات هذا اليوم
3) الخصومات                    = Σ المخصوم من ضمارات هذا اليوم   [بند مستقل]
4) باقي الضمار قبل الخصم       = إجمالي الضمار − النقدي − الواصل
5) باقي الضمار بعد الخصم       = (4) − (3)        ★ الذمم المفتوحة الفعلية
6) إجمالي الضريبة              = Σ ضرائب جواني هذا اليوم
7) الباقي بعد الخصم والضريبة   = (5) − (6)
8) السحبيات                    = Σ سحبيات اليوم لهذا المصدر
9) الخرجيات                    = Σ خرجيات اليوم لهذا المصدر
10) الصافي النهائي             = (7) − (8) − (9)
```

> **لماذا يُطرح «النقدي» في البند 4؟** لأن البند 1 يشمله، **والنقدي محصَّل
> فوراً** فلا يبقى ذمةً على أحد.

## 4. ★ قرار `OQ-001` — محسوم (الخيار ج)

> ★ **قرار صاحب المشروع: يُعرض الصفّان معاً، ويُبنى الصافي النهائي على «بعد الخصم».**

| الصف | المعنى | كيف يُقرأ |
|---|---|---|
| **4 — قبل الخصم** | ما بقي من ضمار اليوم **قبل أي إسقاط** | ★ **حجم الذمة الأصلية** — ويطابق نص المصدر حرفياً |
| ★ **5 — بعد الخصم** | ★ **الذمم المفتوحة الفعلية** | ★ **الرقم القابل للتحصيل** — وعليه يُبنى الصافي النهائي |

**ما كان التعارض:** المعادلة الحرفية تُنتج **368,600** بينما الذمم المفتوحة
فعلياً **363,600** في سيناريو المصدر (رداع · 20/08) — ★ **والفارق 5,000 هو
الخصم بالضبط.** ★ **وعرض الصفّين يُظهر الرقمين بمعنييهما ولا يُخلَط أحدهما بالآخر.**

### 4.1 ★ قيد إدخال ملزم مرافق

```text
★ الخصم يُدخَل عبر مسار الخصم (DSC-) — ⛔ ولا يُدخَل كمبلغ واصل (RCP-) إطلاقاً.
```

> ★ **المبرّر:** ★ **التفريق بين ما دفعه المقوت فعلاً وما أسقطه المالك عنه.**
> ★ **وإدخال الخصم كواصل يُظهر المقوت وكأنه سدّد مالاً لم يدفعه** — ★ **فيُفسِد
> سجل التزامه، ويُفقد المالك معرفة كم أسقط فعلاً.**
>
> ★ **الإنفاذ:** ★ **مساران منفصلان للكتابة بصلاحيتين منفصلتين ومستندين
> مختلفين** — والفصل مفروض في قواعد الحماية لا في الواجهة.

### 4.2 أثر القرار على الأرقام المرجعية

| البند | قبل القرار | ★ بعد القرار |
|---|---|---|
| باقي الضمار (رداع · 20/08) | 368,600 | ★ **368,600 قبل الخصم · 363,600 بعد الخصم** |
| **الصافي النهائي (رداع)** | 335,475 | ★ **330,475** |
| **الصافي النهائي (كل المصادر)** | 410,275 | ★ **405,275** |

## 5. البناء حسب صلاحيات القارئ

> **البطاقة تُبنى حسب صلاحيات قارئها** — لا تُبنى مرة وتُفلتَر عرضاً:

| البند | يظهر لمن يملك |
|---|---|
| **السحبيات** | **«عرض سحبيات المالك»** — وإلا **يختفي البند كلياً** |
| **الخرجيات** | «عرض الخرجيات» |
| **بطاقة «كل المصادر»** | «عرض بطاقة كل المصادر» |
| **بطاقات المصادر** | **مصادر نطاقه فقط** — ولا تتسرب أرقام غيرها لبطاقة «الكل» عنده |

> ⚠️ **الأثر التنفيذي:** «الصافي النهائي» الذي يراه مستخدمان مختلفا
> الصلاحيات **قد يختلف رقمه** — وهذا **سلوك مقصود لا خلل**.

## 6. شاشة حركة النقد في تاريخ

```text
▸ الداخل
   المقبوض من المقاوته في التاريخ    ← بتاريخ السند لا الضمار
        منه: لضمارات هذا اليوم / لأيام سابقة / فائض لم يُسدَّد
   المبيعات النقدية المُدخلة في التاريخ
   ─────────────────────────────
   إجمالي النقد الداخل

▸ الخارج
   السحبيات في التاريخ   (نقداً وقاتاً مفصَّلين)
   الخرجيات في التاريخ
   ─────────────────────────────
   إجمالي الخارج

💰 صافي النقد المتبقي في اليد = الداخل − الخارج
📈 نسبة تغطية السحبيات من المقبوض
🏦 من المقبوض: أُودع / لم يُودع
✂️ الخصومات في التاريخ — تُعرض للعلم ولا تُطرح
```

> **`GR-47`:** **تُطرح السحبيات والخرجيات من «المقبوض في التاريخ» لا من
> «الواصل»** — لأنها تُصرف من النقد الموجود فعلاً لا من ذمة اليوم.

## 7. الأثر الرجعي

> **بطاقة أي يوم قابلة للتغيّر بأثر رجعي** إن صُرِّف متبقٍ من ذلك اليوم
> لاحقاً. فتُعاد بناؤها تلقائياً وتُوسَم **«⟳ مُحدَّثة بأثر رجعي»** مع
> تاريخ آخر تحديث — **ويظهر الوسم في كل تقرير يخص ذلك اليوم**.

## 8. الأداء

| القاعدة | لماذا |
|---|---|
| **تُبنى الملخصات في السحابة وتُقرأ جاهزة** | البطاقة تُفتَح عشرات المرات يومياً · وتجميعها لحظياً **قراءات ضخمة متكررة** |
| **تُحدَّث لحظياً بالحدث** | لا انتظار جدولة |
| **قابلة لإعادة البناء بالكامل من الدفاتر** | **الدفتر مصدر الحقيقة والملخص مشتقّ** (`ADR-0008`) |
| **كل رقم قابل للتفكيك** | الضغط عليه يعرض حركاته |
| ✅★★ **ورسمُ آخر سبعةِ أيامٍ قراءةٌ واحدة** (`IQ-030`) | ★ **سجلُّ `owner_ledger_trends` يحمل السلسلةَ جاهزة** (§2.1) — ⛔ **لا سبعُ قراءاتٍ في كل فتحة**: ⟵ **وهو بالضبط ما بُنيت الملخصاتُ المسبقة لتفاديه** |

## 9. الحالات الحدّية

| # | الحالة | المعالجة |
|---|---|---|
| `AT-49` | قبض اليوم لضمار أمس | **الواصل ≠ المقبوض** — رقمان مختلفان |
| `E-29` | مستخدم بلا صلاحية السحبيات | البند **يختفي كلياً** من البطاقة والنقد والتقارير |
| `E-36` | نطاق مصدر واحد | بطاقة «الكل» **من مصادره فقط** |
| `AT-53` | تصريف متأخر | إعادة بناء + **وسم «⟳»** |
| — | صافي النقد < «لم يُودع بعد» | **تنبيه فجوة النقد** بمراجعة السحبيات |

## 10. ★★ عقد العرض — **بُني بصرياً قبل الوحدة** (`AM-007`)

★ **بطاقة الضمار عُرضت على المالك نموذجاً واعتُمدت قبل بناء أي ملخّص**
(`prototypes/README.md` §2) ⟵ ★ **فبُني مكوّنُها البصري بعقدِ بياناتٍ حقيقيٍّ
مشتقٍّ من §2 و§3 أعلاه**، ⛔ **ولم يُوصَل ببيانات:** ★ **الملخصاتُ تُبنى في
السحابة داخل `WU-016`** — ⟵ **وعرضُ أرقامٍ وهميةٍ في شاشةٍ حقيقية اختلاقٌ
محرَّم.**

| البند | القيمة |
|---|---|
| **المكوّن** | `lib/capabilities/financial_outflow/presentation/owner_ledger_card.dart` |
| **العقد** | `OwnerLedgerSummaryView` — **حقولٌ منسَّقةٌ نصّاً** ⛔ **لا أعدادٌ خام** |
| **عقدُ الشكل الكامل** | [`../../18-ux-ui/design-system.md`](../../18-ux-ui/design-system.md) §7.1 |
| ✅ **ما نُفِّذ في `WU-016`** | ✅ **بناءُ الملخّص سحابياً** ([`owner_ledger_summary_handler.dart`](../../../functions/lib/src/owner_ledger_summary_handler.dart)) **+ مزوّدُ القراءة** ([`owner_ledger_providers.dart`](../../../lib/capabilities/financial_outflow/application/owner_ledger_providers.dart)) **+ توصيلُ العقد** ([`owner_ledger_format.dart`](../../../lib/capabilities/financial_outflow/presentation/owner_ledger_format.dart)) — ⛔ **ولم يُمَسّ شكلُ البطاقة بحرف** |

### 10.1 ⛔★★ ما يفرضه العقد على مَن يبنيه لاحقاً

| # | القاعدة |
|---|---|
| 1 | ★ **كلُّ مبلغٍ يصل نصّاً منسَّقاً مسبقاً** — ⟵ **فالتنسيقُ قاعدةُ مجالٍ لا شأنَ للعرض به** (`ADR-0015`) |
| 2 | ★ **وكلُّ بندٍ محكومٍ بصلاحيةٍ يصل `null` لمن لا يملكها** — ⛔ **لا صفراً ولا نصّاً فارغاً**: ⟵ **الصفرُ معلومةٌ في هذا النظام** (§9)، ★ **و«يختفي كلياً» يعني غيابَ القيمة لا قيمةً صفريّة** |
| 3 | ★ **وصفَّا «قبل الخصم» و«بعد الخصم» حقلان مستقلان إلزاميان** — §4 |
| 4 | ★ **ووسمُ الأثر الرجعي يصل تاريخاً جاهزاً منسَّقاً** — §7 |

### ✅★★★ 10.2 رسمُ آخر سبعةِ أيام — محسومٌ بـ`IQ-030` (الخيار أ · 2026-08-29)

★ **النموذج المرجعي يعرض «رسمَ الصافي — آخر سبعة أيام» أسفل البطاقة.**
⛔ **ولم يكن له مصدرٌ موثَّق** — ★ **السجلُّ المُوصَّف في §2 مفتاحُه
`{sourceId}_{date}` ليومٍ واحد**، ⟵ **وقراءةُ سبعةِ مستنداتٍ في كل فتحةٍ
تخالف §8.**

✅ **والقرار: سجلٌّ ثانٍ للسلسلة** — `owner_ledger_trends/{sourceId}`
(**§2.1 أعلاه بقواعده الخمس**): ★ **قراءةٌ واحدةٌ للرسم كلِّه** · ★ **والأثرُ
الرجعي يُصلحه نفسُ الحدثِ الذي يعيد بناء الملخّص.**

✅★★ **وبُني الرسمُ في `WU-016`** ([`owner_ledger_trend_chart.dart`](../../../lib/capabilities/financial_outflow/presentation/owner_ledger_trend_chart.dart))
— ★ **من السلسلة الحقيقية وحدها**، ⛔ **ولا رقمَ وهميٌّ فيه.** ⛔⛔ **ولا يُرسَم
بنقطةٍ واحدة أصلاً** — ★ **الرسمُ مقارنة**، ⟵ **وعمودٌ وحيدٌ يُوهم بمعنىً ليس فيه.**

## 10.3 ⛔⛔★★★ حركةُ النقد تُجمَع عند القراءة — قرارُ تنفيذٍ مُسجَّل (`WU-016`)

★ **`daily_summaries` مُعرَّفٌ بالبنود العشرة وحدها** (`schema/daily-summaries.md`)
— ⛔ **ولا حقلَ فيه لحركة النقد**، ⟵ **وإضافةُ حقولٍ إليه اختراعُ مخطَّطٍ لا
يُقرّه مستند.** ★ **وتبريرُ البناء المسبق في §8 نصُّه «البطاقة تُفتَح عشرات
المرات يومياً»** — ⟵ **وشاشةُ النقد تُفتَح عند الطلب بتاريخٍ يختاره المستخدم**
(`FR-M15-22`): ★ **فالتجميعُ عند القراءة اتساقٌ مع `ADR-0008`** ⛔ **لا مخالفةٌ
لقيد الأداء.**

⚠️⚠️★★★ **وثلاثةُ تواريخَ مختلفةٍ في شاشةٍ واحدة — وهو أخطر ما فيها:**

| # | البند | التاريخ الحاكم | المرجع |
|:-:|---|---|---|
| ① | سندُ القبض | **`date`** — تاريخُ السند | `GR-41` · `FR-M15-15` |
| ② | البيعُ النقدي | ⛔⛔ **`entryDate`** — **تاريخُ الإدخال** | `FR-M15-16` نصّاً |
| ③ | السحبياتُ والخرجيات | **`documentDate`** | `GR-49` |

⛔⛔★★ **و② عكسُ ما تفعله بطاقةُ الضمار بالضبط** (`schema/cash-sales.md` القاعدة
8: **«إن كان من مخزون يوم سابق فيُحتسب في نقدي ذلك اليوم»**) — ★ **والفرقُ مقصود:**
⟵ **البطاقةُ ذمّةُ يومِ المخزون، والنقدُ صندوقُ يومِ الإدخال.**

⛔⛔★★ **والفائضُ العام لا يُنسَب لمصدر** (`FR-M12-12`: **`sourceId` فيه `null`
عمداً**) — ★ **فيظهر في «كل المصادر» وحدها**، ⛔ **ونسبتُه لمصدرٍ مُخمَّن
تُضخِّم صندوقَه بمالٍ لا يخصّه.**

★ **وحالةُ الإيداع تُقرأ لمن يملك `receiptDepositView` وحدَه** (`ADR-0017`) —
⛔ **ولا يُعرَض بندُها صفراً لمن لا يملكه:** ⟵ **فصفرُ الإيداع معلومةٌ مختلفةٌ
عن «لا تُقرأ حالةُ الإيداع أصلاً»** (§9).

## 10.4 ⛔⛔★★★ الغيابُ يُقرأ باستعلامٍ مقيَّد — [`DEBT-91`]

⛔⛔ **وقراءةُ ملخّصٍ *غائبٍ* بمعرّفه تُرفَض بـ`PERMISSION_DENIED`** — ★ **إذ
يقرأ شرطُ القراءة `resource.data.sourceId`**، **والمستندُ الغائب بلا `resource`
أصلاً.** ⟵ ★ **والغيابُ هنا هو الحالة الطبيعية تماماً: يومٌ لم تقع فيه عمليةٌ
بعد** (§9: **«يومٌ بلا حركةٍ يُعرَض بأصفارٍ صريحة»**).

★ **والعلاجُ تقييدُ الاستعلام** (`sourceId` **و**`date` **صراحةً**) ⛔ **لا
تخفيفُ القاعدة** — ★ **وهو علاجُ `DEBT-40` نفسُه حرفياً**، ✅ **والقاعدة سليمة
ولم تُمَسّ بحرف.**

---

## 11. الاختبارات المرتبطة

`TC-M15-001` … `TC-M15-005` · `AT-48` · `AT-49` · `AT-50` · `AT-53` ·
`E-29` · `E-36` — **واختبار توازن:** مجموع بطاقات المصادر = البطاقة التجميعية.
