# دفتر: `supplier_ledger` — حسابات الرعية

| البند | القيمة |
|---|---|
| **الغرض** | ★ **مصدر الحقيقة الوحيد لصافي كل رعوي في كل مصدر** — `GR-03` |
| **الطبيعة** | **دفتر · لا يُحذف منه شيء** |

## الحقول
| الحقل | ملاحظة |
|---|---|
| `supplierId` · `supplierName` | — |
| ★ `sourceId` | **إلزامي — لكل رعوي حساب في كل مصدر** (`A-11`) |
| `sackId` · `sackDisplayName` | — |
| `sackRevenue` · `sackTax` · ★ `supplierNet` | — |
| `entryDate` · `memo` | — |
| ★ **`recalcVersion`** | **رقم إعادة احتساب متزايد** — **لأن الصافي رقم حيّ**، ولمنع تطبيق حساب قديم فوق أحدث عند تنفيذ المشغّلات خارج الترتيب |
| `isCancelled` · `lastAmendedAt` | — |

## المعادلات
```text
سعر الجونية = Σ(توزيعات أنواعها) + Σ(بيعها النقدي) + Σ(ما سُحب أو صُرف منها)
              [للحركات المسعَّرة غير الملغاة فقط]
              [ولا يدخل: الإتلاف · الوزن الضائع · تسوية الجرد · ما لم يُصرَف]

ضريبة الجونية = round(ضريبة الكيلو × الوزن الكلي)   ← لأقرب ريال (ADR-0015)
صافي الرعوي   = سعر الجونية − ضريبة الجونية        ← طرح عددين صحيحين، لا تقريب ثانٍ

صافي الرعوي في المصدر = Σ(صوافي جوانيه في ذلك المصدر)
الإجمالي عبر المصادر  = Σ(صوافيه) ← للعرض فقط بلا حساب موحّد
```

## القواعد الحرجة
1. ★ **كل قات يخرج من المخزن يستحق الرعوي ثمنه** — توزيعاً أو بيعاً أو **سحبيةً أو خرجيةً**. **والاستثناء الوحيد: الإتلاف والوزن الضائع** (`A-15`).
2. ★ **إيراد السكرب يدخل سعر الجونية كأي نوع** (`ت-09`).
3. **الكمية التي لم تُصرَف لا تُحتسب** — الجونية تُقيَّم **بما تحقق منها فعلاً**.
4. **الجونية ذات حركات غير مسعَّرة تُوسَم «⏳ سعر غير نهائي»**.
5. ⛅ **يكتبه المُحتسِب السحابي حصراً** — ⚠️ **بتجميع** (من أغلى عمليتين).
6. ★ **لا توجد شاشة تسديد للرعوي في هذا الإصدار** — الصافي يُعرَض ويُقيَّد فقط.

## ✅★★ حاشيةُ التنفيذ (`WU-015` — 2026-09-03)

★ **الكاتبُ الوحيد:** `SackValuationHandler`
([`functions/lib/src/sack_valuation_handler.dart`](../../../functions/lib/src/sack_valuation_handler.dart))
— ⛅ **يُطلَق بعد التزام كل عملية تمسّ جونيةً** (توزيع · بيع نقدي · سحبية ·
خرجية · جونية) ⛔ **ولا كتابةَ مباشرة** (`allow write: if false`).

⛔⛔★★★ **والمعرّف `{sackId}` — سطرٌ واحدٌ لكل جونية يُعاد كتابته:**
★ **وهو ما يجعل `recalcVersion` ذا معنى** («يمنع تطبيق حساب قديم **فوق**
أحدث») — ⛔ **والإلحاقُ كان يُضاعف إيرادَ كلِّ جونيةٍ عُدِّلت مرة في §5.**

⛅★★★ **ومقيسٌ حيّاً على التجريبية** (2026-09-03 · `callables:wu015` ·
المراجعة `qtms-callables-00028-z2s`): `SCK-20260903-0001` ⟵
`sackTax: 900` (30 × 30.000) · `sackRevenue: 0` · `supplierNet: -900`
(★ **السالبُ حالةٌ واقعية لجونيةٍ لم تُصرَف** · `FR-M14-04`) ·
`recalcVersion: 2` — ★ **واحدٌ من `createSack` وثانٍ من `enterSackTax`**،
✅ **وتشغيلٌ ثالثٌ بلا تغيير أبقاه 2** ⛔ **فلا يتضخّم بلا سبب.**

## الفهرس
★ **`supplierId ↑ · sourceId ↑ · entryDate ↓`**

## المرتبطات
`FR-M14` · [`sacks.md`](sacks.md) · [`sack-valuation-design`](../../04-design/module-design/sack-valuation-design.md)
