# سجل المخاطر المعمارية والدَّين التقني — QTMS

| البند | القيمة |
|---|---|
| **الإلزامية** | **M** |
| **الطبيعة** | **LIVING** — يُضاف إليه عند ظهور خطر أو دَين جديد |
| **المالك** | المهندس المعماري (ARCH) · ويقترح Claude Code |

> **نطاق هذا المستند:** المخاطر **التقنية والمعمارية**. أما المخاطر
> **الإدارية** فمكانها `docs/21-project-management/risk-register.md`.

---

## 1. المخاطر المعمارية

**مقياس الشدّة:** 🔴 حرجة (توقف/خسارة مالية) · 🟠 عالية · 🟡 متوسطة.

### `RISK-01` 🔴 — إغفال حقل «تاريخ المخزون» في المرحلة الأولى

| البند | التفصيل |
|---|---|
| **الوصف** | خارطة الطريق تسمح ببناء المرحلة الأولى **بافتراض «لا متبقي متأخر»**. فإن أُغفل حقل «تاريخ المخزون» في نموذج البيانات، **يستحيل تنفيذ المرحلة الثالثة بلا هجرة بيانات كاملة** لكل حركة مخزون وكل توزيعة سابقة. |
| **الاحتمال** | **متوسط–عالٍ** — الحقل يبدو زائداً في المرحلة الأولى لأنه يساوي تاريخ اليوم دائماً. |
| **الأثر** | **حرج** — هجرة بيانات مالية حيّة مع خطر فقدان أو تشويه. |
| **التخفيف** | **إلزام صريح: الحقل موجود في نموذج البيانات وكل فهرس منذ اليوم الأول**، ولو لم يُستخدَم سلوكياً بعد. **وبند قبول في مراجعة `M-1`.** |
| **المالك** | ARCH |
| **الحالة** | مفتوح — **مُدرَج في `product-roadmap.md` §3 و`project-charter.md` §10** |

### `RISK-02` 🔴 — قاعدة عمل بلا مقابل في قواعد الحماية

| البند | التفصيل |
|---|---|
| **الوصف** | 55 قاعدة عامة + مئات قواعد الوحدات. **وأي قاعدة تُنفَّذ في كود التطبيق وحده = غير مُنفَّذة أمنياً** — لأن التطبيق يعمل على جهاز المستخدم. |
| **الاحتمال** | **عالٍ** — الطريق الأسهل دائماً هو `if` في الشاشة. |
| **الأثر** | **حرج** — ثغرة تفويض صامتة تُكتشَف بعد خسارة. |
| **التخفيف** | **جدول التتبع 9.6 بند قبول لا توثيق** (`09-security/security-requirements.md`) · **اختبار قواعد الحماية على محاكٍ محلي كما يُختبَر الكود** · **ستة فحوص من خارج التطبيق في بوابة الخطوة صفر** · ومراجعة كل `CR` بسؤال «ما مقابلها في طبقة الحماية؟». |
| **المالك** | **SEC** |
| **الحالة** | مفتوح |

### `RISK-03` 🟠 — تضخّم تكلفة إعادة الاحتساب

| البند | التفصيل |
|---|---|
| **الوصف** | **إعادة احتساب سعر الجونية** تُطلَق عند **كل** توزيع وبيع وسحبية وتسعير وتعديل وإلغاء وتصريف متأخر. **وإعادة البناء بأثر رجعي** تُطلَق عند كل تصريف متأخر أو جرد يوم سابق. **وكلتاهما موصوفتان في المصدر بأنهما مكلفتان.** |
| **الاحتمال** | **عالٍ** في أيام الذروة. |
| **الأثر** | فاتورة متضخّمة · وتأخّر ملحوظ في تحديث الملخصات. |
| **التخفيف** | **تنفيذ بتجميع (Batching)** لا بعد كل حركة منفردة · **ملخصات جاهزة** (`ADR-0008`) · **تنبيهات ميزانية من اليوم الأول** (بند إلزامي في الخطوة صفر) · ومراجعة دورية لأكثر الاستعلامات استهلاكاً. |
| **المالك** | DEVOPS + ARCH |
| **الحالة** | مفتوح — مُخفَّف بالتصميم |

### `RISK-04` 🟠 — موقع قاعدة البيانات لا رجعة فيه

| البند | التفصيل |
|---|---|
| **الوصف** | **الموقع الجغرافي يُختار مرة واحدة ولا يُغيَّر بعدها إطلاقاً**؛ وتغييره يستلزم مشروعاً جديداً وهجرة كاملة. واختيار موقع بعيد يعني زمن استجابة أعلى **دائماً** — ما يصطدم بهدف «< 2 ثانية». |
| **الاحتمال** | **متوسط** — قد يُختار بلا تفكير أثناء الإعداد السريع. |
| **الأثر** | **عالٍ ودائم**. |
| **التخفيف** | ✅ **حُسم:** ★ **`me-central1` (الدوحة)** — من أقرب المتاح جغرافياً. ⚠️ **ونُقل إليه من `me-central2` في 2026-08-22 والقاعدتان فارغتان** (`ADR-0014`)، لأن الأخيرة **لا تحمل العمليات السحابية**. |
| **المالك** | DEVOPS |
| **الحالة** | ✅ **مُغلَق** — ★ **يبقى قياس زمن الاستجابة الفعلي من موقع المنشأة عند أول تشغيل، للتحقق لا للاختيار.** |

### `RISK-05` 🟠 — سجل التدقيق نقطة فشل حرجة

| البند | التفصيل |
|---|---|
| **الوصف** | بعد اعتماد **التعديل المباشر بلا حركات عكسية** (`ADR-0004`)، صار **سجل التدقيق هو الحافظ الوحيد للتاريخ**. **فإن ضعف شرط واحد من شروطه سقط المبرر كله** وصار الدفتر قابلاً للتغيير بلا أثر. |
| **الاحتمال** | منخفض إن نُفِّذ صحيحاً · **ومرتفع الأثر إن لم يُنفَّذ**. |
| **الأثر** | **حرج** — فقدان القدرة على إثبات من غيّر ماذا. |
| **التخفيف** | **الكتابة من السحابة حصراً** والكتابة من التطبيق **ممنوعة نهائياً** · **لا ينجح تعديل بلا قيد مقابل** · **لا تعديل ولا حذف على السجل لأحد** · وكل ذلك **بند في بوابة الخطوة صفر**. |
| **المالك** | **SEC** |
| **الحالة** | مفتوح |

### `RISK-06` 🟠 — الاعتماد الكامل على خدمات المنصة على الجهاز

| البند | التفصيل |
|---|---|
| **الوصف** | **خدمات Google Play شرط تشغيلي إلزامي**؛ والأجهزة بدونها غير مدعومة. وقد يشتري المالك جهازاً بلا هذه الخدمات فلا يعمل النظام إطلاقاً. |
| **الاحتمال** | متوسط. |
| **الأثر** | عالٍ — جهاز غير قابل للاستخدام. |
| **التخفيف** | **رسالة واضحة عند غيابها لا فشل صامت** (`NFR-OPS-03`) · وتوثيق الشرط في **متطلبات الجهاز** ضمن دليل المستخدم وقائمة الشراء. |
| **المالك** | DEV |
| **الحالة** | مفتوح |

### `RISK-09` 🟡 — استنتاج إجمالي التوزيعة من أسعار اليوم

| البند | التفصيل |
|---|---|
| **الوصف** | `ADR-0011` عزل أسعار التوزيعة وإجمالياتها في مستند فرعي محكوم بـ`distributionPriceView`. **لكن `daily_prices` تبقى مقروءة** لمن لا يملك تلك الصلاحية — ومن يقرأها **مع الكميات الظاهرة في المستند الأب** يستطيع **استنتاج الإجمالي حسابياً**. |
| **الاحتمال** | **منخفض–متوسط** — يتطلب قصداً وربطاً يدوياً بين مصدرين. |
| **الأثر** | متوسط — كشف مبالغ لمن مُنع من رؤية عمود السعر (`ت-12`). |
| **التخفيف** | ⛔ **لم يُغلَق عمداً:** `ت-12` يخصّ **شاشة التوزيع** (`M10`)، وأسعار اليوم شاشة ووحدة أخرى (`M9`)، **والتوثيق لا يُعرِّف صلاحية «عرض التسعير اليومي»** — وإنشاؤها من عندنا **اختلاق صلاحية** يكسر `BR-M1-07` من الجهة الأخرى. |
| **الحالة** | **مفتوح — ينتظر قراراً**. إن أُريد إغلاقه فهو **صلاحية جديدة** تمرّ بدورة `permissions-catalog.md` §6 كاملةً. |

### `RISK-07` 🟡 — الخلط بين تاريخ المخزون وتاريخ الإدخال في الاستعلامات

| البند | التفصيل |
|---|---|
| **الوصف** | كل استعلام وفهرس مخزني **يجب أن يبنى على تاريخ المخزون لا تاريخ الإدخال**. والخلط بينهما ينتج تقارير خاطئة **بصمت** — تبدو صحيحة ولا تُطابق الواقع. |
| **الاحتمال** | **متوسط–عالٍ** — التاريخان متساويان في 99٪ من الحالات، **فلا يظهر الخطأ إلا عند التصريف المتأخر**. |
| **الأثر** | متوسط–عالٍ — أرقام خاطئة بلا إنذار. |
| **التخفيف** | **تسمية صريحة لا تقبل اللبس** في نموذج البيانات (`naming-conventions.md`) · **وحالة اختبار إلزامية لكل تقرير مخزني تتضمن تصريفاً متأخراً**. |
| **المالك** | DEV |
| **الحالة** | مفتوح |

### `RISK-08` 🟡 — تباعد الملخص عن الدفتر

| البند | التفصيل |
|---|---|
| **الوصف** | الملخصات مشتقّة تبنيها السحابة (`ADR-0008`). وفشل عملية تحديث واحدة قد يترك ملخصاً **لا يطابق دفتره** — فيرى المالك رقماً خاطئاً بلا أي إشارة. |
| **الاحتمال** | منخفض–متوسط. |
| **الأثر** | متوسط — قرار مالي مبني على رقم خاطئ. |
| **التخفيف** | **الدفتر مصدر الحقيقة وأي تعارض يُحسَم لصالحه** · **الملخص قابل لإعادة البناء بالكامل** · **فحص اتساق دوري** يقارن عينة ويُبلِّغ عن أي فارق (`13-operations/health-checks.md`). |
| **المالك** | DEVOPS |
| **الحالة** | مفتوح |

### `RISK-09` 🟡 — الارتباط بمزوّد بعينه

| البند | التفصيل |
|---|---|
| **الوصف** | تغيير سياسة تسعير أو إيقاف خدمة لدى المزوّد يفرض إعادة بناء طبقة البيانات والتفويض بالكامل. |
| **الاحتمال** | منخفض. |
| **الأثر** | عالٍ. |
| **التخفيف** | **تنازل واعٍ ومقبول** (`ADR-0001`). التخفيف الوحيد: **عزل قواعد العمل والمعادلات في طبقة نطاق نقية** (`ADR-0009`) — فتبقى قيمة النظام قابلة للنقل ولو تغيّرت البنية التحتية. |
| **المالك** | ARCH |
| **الحالة** | مقبول بوعي |

---

## 2. سجل الدَّين التقني

> **حالة هذا السجل (2026-08-22):** بدأ التنفيذ فعلاً (`WU-000` قيد التنفيذ).
> فأغلب البنود أدناه **دَين مُخطَّط ومعروف سلفاً**، ★ **وبعضها صار مرصوداً
> من الكود والبيئة فعلاً** (`DEBT-04` · `DEBT-11` · `DEBT-13`) لا متوقَّعاً.

| # | البند | النوع | متى يُسدَّد | الأولوية |
|---|---|---|---|:-:|
| `DEBT-01` | **أداة إعادة بناء شاملة للملخصات** من الدفاتر — لازمة لعلاج `RISK-08` ولأي هجرة | أداة ناقصة | قبل النشر الإنتاجي | **عالية** |
| `DEBT-02` | **فحص اتساق دوري** يقارن عينة ملخصات بدفاترها ويُبلِّغ | مراقبة ناقصة | مع `13-operations/health-checks.md` | **عالية** |
| `DEBT-03` | بناء المرحلة الأولى بافتراض «لا متبقي متأخر» **مع وجود الحقل** | **دَين مقصود مؤقت** | المرحلة الثالثة | **حرجة** — راجع `RISK-01` |
| ★ `DEBT-04` | **فحص آلي في التكامل المستمر** يمنع طبقة النطاق من استيراد حزم المنصة — ⏳ **خُفِّف جزئياً (2026-08-22)** بـ`functions/test/no_flutter_dependency_test.dart` (يقرأ الرسم المحلول كاملاً فيرى غير المباشر). ⚠️ **ولم يسقط:** `ADR-0012` افترض أن القيد **يفرضه البناء**، ★ **ورصدت الطفرة عكس ذلك محلياً** — جهاز التطوير فيه Flutter مثبَّت **فيَحُلّ `sdk: flutter` بنجاح تام بلا أي إنذار**، ولا يفشل إلا داخل الحاوية. **فالحارس النصي ليس ترفاً بل هو المُنفِّذ الفعلي محلياً** | بوابة جودة ناقصة | مع أول خط تكامل مستمر | ★ **عالية** *(رُفعت — راجع أعلاه)* |
| ~~`DEBT-05`~~ | ~~**مجموعة اختبارات لقواعد الحماية** على محاكٍ محلي~~ — ✅ **سُدِّد (2026-08-22):** **٤٥ اختباراً تعمل فعلياً على المحاكي · ٤٥ ناجحاً · ٠ فاشلاً**، تغطي الفحوص ①…⑤ كاملةً و⑥ **جزئياً** (شطر القواعد فقط — شطر التزامن ينتظر العملية السحابية) | ~~اختبار ناقص~~ | ~~الخطوة صفر~~ | ✅ **مُسدَّد** |
| `DEBT-06` | **تحويل سيناريو اليوم الكامل (القسم 15) إلى اختبار قبول آلي شامل** — سيناريو متوازن حسابياً يصلح حالة مرجعية | اختبار ناقص | نهاية المرحلة الثانية | عالية |
| `DEBT-07` | **أهداف رقمية للمؤشرات التشغيلية** (معدل تصريف · نسبة تحصيل · نسبة متبقٍ) | معلومات ناقصة | بعد شهر تشغيل | متوسطة |
| ~~`DEBT-08`~~ | ~~**سقف تنبيه الميزانية الشهري**~~ — ✅ **سُدِّد (2026-08-22):** **2$ شهرياً لكل بيئة** بعتبات 50/90/100٪ وبريد المالك مستلماً. ⚠️ **ويُراجَع بعد شهر تشغيل** — راجع `environments.md` §1.3 | ~~قيمة ناقصة~~ | ~~قبل الخطوة صفر~~ | ✅ **مُسدَّد** |
| ~~`DEBT-10`~~ | ~~**تعطّل محاكي المنصة على جهاز التطوير**~~ — ✅ **سُدِّد (2026-08-22).** ⚠️ **والتشخيص السابق كان خاطئاً:** لا جدار حماية ولا مكافح فيروسات ولا حجب اتصال محلي. **السبب الحقيقي سببان:** ① `firebase-tools` ≥ 15 يرفض Java قبل 21 · ② منذ JDK 16 يبني `Selector.open()` آلية إيقاظه على مقبس `AF_UNIX` داخل `TEMP`، و`AF_UNIX` على هذا الجهاز **ينجح `bind` ويفشل `connect`** بـ`Invalid argument` لكل مسار تحت `%LOCALAPPDATA%` — وإليه يشير `TEMP`. **الحل:** [`run-rules-tests.mjs`](../../test/security-rules/run-rules-tests.mjs) يختار JDK 21 ويوجّه مجلد المقابس خارج `%LOCALAPPDATA%` | ~~بيئة تطوير معطَّلة~~ | ~~فوراً~~ | ✅ **مُسدَّد** |
| ~~`DEBT-12`~~ | ~~**تغطية اختبارية غير مكتملة لمفاتيح الصلاحيات**~~ — ✅ **سُدِّد (2026-08-22):** `permission-matrix.test.js` يغطي **61 مفتاحاً × اتجاهين = 122 اختباراً ناجحاً**، كل زوج **لا يختلف إلا في المفتاح المُختبَر**، ويحرسه اختبار تطابق مع القواعد في الاتجاهين (مُختبَر بالطفرة) | ~~اختبار ناقص~~ | ~~مع كل زيادة~~ | ✅ **مُسدَّد** |
| ★ `DEBT-11` | ★ **`AF_UNIX` معطوب تحت `%LOCALAPPDATA%` على جهاز التطوير** — ⚠️ **وأثره أوسع مما ظُنّ (2026-08-22):** ① Gradle كان يفشل بـ`Unable to establish loopback connection` فلا يُبنى APK — **عولج بتوجيه `TEMP`** · ② ★ **وحتى بعد التوجيه يعجز مُصرِّف Kotlin عن الاتصال بخادمه** (`Could not connect to Kotlin compile daemon`) **فيسقط إلى التصريف داخل العملية**: البناء ينجح لكن **أبطأ**، والخطأ يمرّ في وسط السجل بلا إفشال. ⛔ **والسبب الجذري ما يزال غير محدَّد** | **بيئة تطوير معطوبة جزئياً** | ★ **مُخفَّف لا مُسدَّد** — قبل توسّع العمل على أندرويد | ★ **عالية** |
| ✅★ `DEBT-13` | ✅★★ **سُدِّد بحسم `IQ-016` (2026-08-24)** — ★ **واعتُمد [`error-codes-catalog.md`](../05-api/error-codes-catalog.md) مرجعَ نصّ العرض** (`LIVING`)، و[`error-handling-strategy.md`](../04-design/error-handling-strategy.md) §4 (`STATIC`) **مرجعاً تاريخياً لأسلوب الصياغة** ⛔ **لا يُعدَّل لإضافة رسالة**. ⟵ ★ **فزال التعارض من أصله: مرجعٌ واحد لا اثنان.** ✅ **وأُضيفت معه الرسالتان المفقودتان بنصٍّ اعتمده صاحب المشروع صراحةً**: `ERR_AUTH_008` (بيانات دخول غير صحيحة) و`ERR_AUTH_009` (فشل دخول غير متوقَّع) — ⛔ **لا بصياغة مُخترَعة**. ★ **و`signInRejectionMessage` صار عائدُه غير قابل للعدم** فصارت «حالةُ رفضٍ بلا نصّ» **خطأَ تصريف لا صمتاً في الشاشة**، **واختبار الصمت المتعمَّد قُلِب إلى اختبار ظهور ومُثبَت بالطفرة** | ✅ **مسدَّد** | ✅ **2026-08-24** | — |
| ~~`DEBT-09`~~ | ~~**عيّنة إثبات مسار العمليات السحابية بلغة Dart**~~ — ✅ ★ **سُدِّد كاملاً (2026-08-22) بتشغيل حقيقي من طرف إلى طرف على `qtms-orimind-master`:** ① **البناء** (Cloud Build من `dart:3.11` — ⟵ **دليل قاطع أن المسار يُبنى بلا Flutter**) · ② **النشر** على Cloud Run · ③ ★ **تسليم Eventarc حقيقي** (كُتب مستند `sources` فعلي، فوصل الحدث بمعرّف **UUID مولَّد من المنصة لا من يدنا**) · ④ **الاستقبال والفكّ** · ⑤ ★ **والكتابة في القاعدة الحقيقية** (`observedAt` = **`createTime` الفعلي للمستند** لا ساعة الحاوية). ⚠️ **ويبقى `DEBT-16` منفصلاً** — فكّ الحقول لا يزال ناقصاً | ~~تحقق تقني ناقص~~ | ~~الخطوة صفر~~ | ✅ **مُسدَّد** |

| ~~`DEBT-14`~~ | ~~**إزالة كل أثر عيّنة `DEBT-09`**~~ — ✅★★ **سُدِّد كاملاً (2026-08-24) بأشطره الثلاثة، ومُتحقَّق منه بالجرد بعد الحذف لا بالادّعاء:** ① **برمجياً:** `spike_probe.dart` واختباراته الخمسة ونقطة دخوله **أُزيلت** · ② **سحابياً بالترتيب المُلزِم** (المشغّل ← الخدمة ← حساب الخدمة): `qtms-debt09-source-created` ثم `qtms-functions-debt09` ثم `qtms-functions-spike@…` · **وصورتا `…/qtms/functions` حُذفتا** · ③ **وبياناتٍ بإذن صريح:** مستند `spike_debt09` · `sources/SRC-EVENTARC-PROBE` · وعدّادات `SRC-CHECK6-*` الأربعة. ✅ ★ **والتحقق بجردٍ فعلي بعد الحذف:** لا مشغّل عيّنة ولا خدمة ولا حساب ولا صورة، **والمجموعات الست فارغة تماماً** — ⛔ **ولم يُحذَف مورد واحد خارج العيّنة**. ★ **وأثر فحص الطرف إلى الطرف نُظِّف بنفسه أيضاً** | ~~كود ومورد مؤقت مقصود~~ | ~~قبل إغلاق `WU-000`~~ | ✅ **مُسدَّد** |
| ★ `DEBT-17` | ★ **ازدواج فحص التفويض — ✅ وأُعيد وصفه بعد جواب `IQ-009` (2026-08-24)** · ⚠️ **الوصف السابق لم يعد دقيقاً:** كان يقول إن كل قاعدة صلاحية تصير مكتوبة «مرتين». ★ **والنموذج المعتمد الآن يفصل بينهما لا يُكرِّرهما:** **① القراءة** — تفويضها في `firestore.rules` وحدها (`perm()` · شروط `ADR-0011`) · **② الكتابة** — تفويضها **في الدالة السحابية وحدها**، والقاعدة تصير `allow create, update: if false` (`ADR-0013` القاعدة 2 · تُنفَّذ في **`WU-026`**). ⟵ ★ **فالازدواج الحقيقي ينحصر في المفاتيح نفسها لا في الشروط** — ✅ **وهو مُعالَج أصلاً:** `Permission` في طبقة النطاق **مصدر واحد** يحرسه اختبار تطابق ثنائي الاتجاه مع الكتالوج (مُثبَت بالطفرة). ⚠️ **والمخاطرة المتبقية:** أن يفترق **شرط قراءة** في القاعدة عن **شرط كتابة** في الدالة لنفس السجل — ★ **والتخفيف: اختبارات تُشغَّل على الاثنين معاً** على غرار `permission-matrix.test.js` | **خطر ازدواج — ✅ مُضيَّق** | ★ **مع أول دالة كاتبة (بعد `WU-026`)** | متوسطة |
| ★ `DEBT-20` | ★ **`ADR-0013` القاعدة 2 لم تُطبَّق على `firestore.rules` بعد** — اعتُمد الـADR في **2026-08-22** وينصّ على أن الكتابة المباشرة من التطبيق **«تصير `allow create, update: if false`»**، ⛔ **والقواعد ما تزال تفتح `allow create`** بشروط تفصيلية على `incoming_count` · `sacks` · `distributions` · `cash_sales` · `receipts` · `discounts` · `outflows` · `stocktakes`. ⚠️ **و`DEBT-17` أعلاه يصف الوضع بأن الشرط مكتوب «مرتين» — وهو وصفٌ يطابق الواقع الحالي ولا يطابق ما يفرضه `ADR-0013`.** ★ **مُصعَّد في `IQ-009`** لأن الإجابة تمسّ **66 موضع `perm` و172 اختباراً** — ⛔ **ولا يُعاد بناؤها باجتهاد** | **انحراف عن قرار معتمد — ✅ مُخطَّط للسداد** | ★ **`WU-026`** — ✅ **أُجيب `IQ-009` (الخيار أ) وأُنشئت الزيادة**، والسداد تنفيذها | ★ **عالية** |
| ★★ `DEBT-21` | ★★ **قيودٌ كانت القواعد تفرضها وصارت على الدالة الكاتبة — وهي مواصفة قبولٍ مُلزِمة لكل دالة** (`WU-026` · `ADR-0013` القاعدة 2). ⛔ **إسقاط أيٍّ منها = ثغرة صامتة**، لأن القاعدة لم تعد تحرسها: ~~①~~ ⛔⛔★★★ **البند ① أُسقِط بـ[`ADR-0020`](adr/ADR-0020-optional-amendment-reason.md) (2026-08-27)** — ★ **كان «السبب النصي غير الفارغ لكل تعديل وإلغاء» (`nonEmpty`)** ⟵ **والسبب اختياريٌّ الآن في كل عملية**، ⛔ **فلم يبقَ ما يُحرَس هنا** · ② **وقت الخادم** في كل حقل يُخزَّن (`serverTime` · `GR-54` · `E-41`) · ③ **تاريخ المخزون = اليوم** ولا أقدم إلا بصلاحية التصريف المتأخر (`todayStockDate`) · ④ **لا تاريخ مستقبلي مطلقاً** (`notFutureDate`) · ⑤ ★ **`GR-18` المعرّف المركّب للتوزيعة بترتيبه** (`compositeIdMatches`) · ⑥ **المسارات المقيَّدة على الجونية** — كل مفتاح يمسّ حقوله وحدها (`onlyFields`) · ⑦ **الحقول المالية لا تعيش في المستند الأب** (`noSackFinanceFields` · `noPriceFields` · `ADR-0011`) · ⑧ **الرقم المتسلسل لا يُمَسّ بعد الإنشاء** (`cloudOnlyUnchanged`) · ⑨ **المصدر لا يتغيّر** فلا يُنقَل مستند خارج النطاق (`sourceUnchanged`) · ⑩ **نطاق المصادر** على كل كتابة (`incomingInScope`) · ⑪ ★★ **`ADR-0015`: كل مبلغ عدد صحيح** (`money`) · ⑫ ★ **`ADR-0017`: حقول الإيداع تُكتب في `receipts/{id}/deposit/current` داخل معاملة الأب نفسها** — بصلاحية `receiptDepositConfirm` **وملاحظة غير فارغة**، ⛔ **ولا حقل إيداع واحد في المستند الأب**. ★ **ونصّ كل قيد محفوظ تعليقاً في `firestore.rules` في موضعه**، ولم يُحذَف. ★★ **وأُضيف بـ`CR-002` (2026-08-24) بندُ قبولٍ مُلزِم لأول دالة كاتبة تفرض `amendReason`:** ⛔ **الفحص الرابع في `product-roadmap.md` §0 («تعديل حركة بلا سبب تُرفض») انتقل إثباته إلى هنا** — ★ **ولا يُحتسَب مستوفىً حتى تفشل فيها حالةُ «تعديل بلا سبب» فعلياً بتشغيل حقيقي**، ⛔ **ولا يُدَّعى تحقّقه قبل ذلك** | ★★ **إنفاذ مفقود مؤقتاً — ⛔★★ والبند ① أُسقِط بـ`ADR-0020` لا سُدِّد** | ★ **بند قبول في كل دالة كاتبة — ولا دالة تُقبَل بدونه** | ★★ **حرجة** · ✅★★★ **والبند ① سُدِّد فعلياً (2026-08-24) في أول دالة كاتبة** كما نصّ `CR-002` حرفياً: `updateUser` و`updateRole` **ترفضان التعديل بلا `amendReason` غير فارغ** برمز `ERR_AMEND_002`، **والفراغات وحدها ليست سبباً** (نصّ `nonEmpty()` منقولاً لا مُعاد صياغته). ✅ **ومُثبَت بالطفرة:** بحذف الحارس **سقطت ثلاثة اختبارات** ثم استُعيد. ⟵ ★ **فالفحص الرابع في `product-roadmap.md` §0 («تعديل حركة بلا سبب تُرفض») مستوفىً الآن على مستوى المستخدمين والأدوار** · ★★ **وأُلحِقت به `deleteRole` (2026-08-25 · `IQ-018`)** — **الحذف يُرفَض بلا `amendReason` غير فارغ**، ✅ **ومُثبَت بالطفرة** — ★★ **وأُلحِقت به عمليات البيانات المرجعية التسع (2026-08-25 · `WU-002`)** — `updateSource` · `updateSupplier` · `updateDealer` · `updateItem` **ترفض التعديل بلا سبب**، ✅ **ومُثبَت بالطفرة** (بتعطيل الحارس سقطت اختبارات فعلاً) · ✅★★ **والبند ② سُدِّد كذلك في مسار البيانات المرجعية:** `createdAt` يُكتب **بتحويل `REQUEST_TIME` داخل الالتزام نفسه** (`PendingDocument.serverTimestampFields`) ⛔ **لا بساعة الحاوية** — ★ **واختبارٌ يُثبت أنه لا يُذكَر في الحقول ولا في القناع** · ★★ **وأُضيفت إليه سبعةُ قيودٍ جديدة تخصّ هذه المجموعة وحدها** ولا سطر لها في القائمة الأصلية: **تفرّدُ الاسم والرقم عبر سجل حراسة يُقرأ ويُكتب في المعاملة نفسها** · **رفضُ أي حقل سعر على سجل الأنواع** (`FR-M5-09`) · **حصانةُ النوع الافتراضي** (`FR-M5-05`) · **قفلُ الوحدة** (`FR-M5-04`) · **الكتابةُ الواحدة للإعداد التأسيسي** (`FR-M21-03`) · **إقرارُ تعطيل مقوتٍ له رصيد** (`FR-M4-09`) · **واشتقاقُ الوحدة و`isSystemDefault` في الكود** ⛔ لا من الحمولة — ⟵ ★ **وكلها مُختبَرة في `functions/test/master_data_test.dart`** · ✅★★★ **وسُدِّدت أربعةٌ أخرى بـ`WU-003` (2026-08-25) في أول دالةٍ كاتبةٍ لمستندٍ مخزني** (`functions/lib/src/inventory.dart`): **③ تاريخ المخزون = اليوم من الخادم** — ★ **ومن زمن المنصّة داخل المعاملة (`readTime`) ⛔ لا من ساعة الحاوية**، **والحاوية تقترح والمنصّة تحكم** (`inventory_handler.dart` الترويسة · `DEBT-29`) · **⑨ المصدر لا يتغيّر** — ★ **والمصدرُ المخزَّن هو الحَكَم لا المُرسَل**، ⛔ **وإلا فُحِص النطاق على مصدرٍ وعُمِل على مستند آخر** · **⑩ نطاق المصادر على كل كتابة** — `inventoryGate` **قبل فتح المعاملة وداخل التخطيط معاً** · ★★ **وأُضيف إليها ما لا سطر له في القائمة الأصلية: منعُ الرصيد السالب مقيساً من الدفتر داخل المعاملة** (`FR-M8-01` · `GR-11`) — ⟵ **وهو الحارس الوحيد الآن بعد أن صارت القاعدة `allow write: if false` مطلقاً.** ✅ **ومُثبَتة بالطفرة: ستّ طفرات، خمسٌ قُتلت فوراً، وواحدةٌ نجت فكشفت فجوةَ تغطيةٍ حقيقية** (فلترةُ الملغاة كانت في موضعين) — ★ **فأُعيد بناء `computeItemDailyFlow` بموضعٍ واحد وأُضيف اختبارُ «حركة خروجٍ ملغاة»، ثم قُتلت.** · ⛔ **وبقيت ④ و⑤ و⑥ و⑦ و⑧ و⑪ و⑫ تنتظر دوالَّها الكاتبة.** ⛔⛔★★★ **وتحديثٌ حاسم (2026-08-27): البند ① لم يعد قائماً إطلاقاً** — ★ **`ADR-0020` جعل السبب اختيارياً في كل العمليات**، ⟵ **فكلُّ ما سُجِّل أعلاه من سدادٍ وطفراتٍ على `updateUser` و`updateRole` و`deleteRole` وعمليات البيانات المرجعية التسع يبقى سجلاً تاريخياً** ⛔ **ولا يُقرَأ مواصفةَ قبولٍ سارية**، ★ **وحُذفت حراسُه من الكود وأُبدِلت اختباراتُه باختبارات قبولٍ تُثبت أن الغياب يمرّ** ⛔ **وأن التطبيق لا يُعبِّئ سبباً نيابةً عن أحد.** |
| ✅★★ `DEBT-22` | ✅★★★ **سُدِّد بحسم `IQ-017` (2026-08-24) — والترتيب المُلزِم مُحترَم:** ① **كل مسار كتابة بطاقة صار يكتب `isActive`** (`permission_sync.dart` · `owner_bootstrap.dart`)، **وقيمتُه مشتقّة من حالة الحساب في خدمة المصادقة** (`!disabled`) ⛔ **لا `true` ثابتة** — ⟵ **فمنحُ صلاحيةٍ لا يُعيد تفعيل معطَّل**. ② ★★ **وبوابة «تمّ سابقاً» في الإقلاع صارت تنظر إلى `isActive` كما تنظر إلى الصلاحيات** — ⟵ **فصار `bootstrapOwnerPermissions` نفسُه أداةَ ترحيل بطاقة المالك**، ⛔ **وبدونه كانت البطاقة المُقلَعة قبل الحقل تُرجِع «تمّ سابقاً» فلا تُكتب أبداً ⟵ فيُقفَل المالك خارج نظامه** (حلقة إقلاع ثانية). ③ **ثم أُضيف الشرط إلى `perm()`**: `userCard().data.get('isActive', false) == true` — ★ **في `perm()` وحدها فحرس الـ66 موضعاً معاً**. ④ ★ **والتعطيل يقع في الطبقتين معاً** (`IQ-017` الخيار ج): البطاقة تمنع الرمزَ **القائم**، و`disableUser` تعطّل الحساب في **خدمة المصادقة** فتمنع إصدارَ الجديد. ✅ **ومُثبَت بالطفرة:** بجعل `activeCard()` تُرجِع `true` **سقط اختباران** ثم استُعيدت. ⚠️★★ **وبقي بندٌ تشغيلي وحيد قبل النشر:** [`RB-backfill-user-isactive.md`](../13-operations/runbooks/RB-backfill-user-isactive.md) — ⛔ **ولا يُنشَر شرط `perm()` قبل تنفيذه**، وعكسُ الترتيب يقفل المالك | ✅ **مسدَّد كوداً واختباراً** · ⚠️ **وترحيلٌ تشغيلي مُعلَّق** | ★★ **الترحيل قبل النشر** | ✅ **زالت الحرجية من الكود** |
| ★ `DEBT-23` | ★★ **نافذة تزامن في `deleteRole` — إسنادٌ يقع أثناء الحذف** (`IQ-018` · 2026-08-24). ★ **القياس صحيح ومتسق:** الاستعلام `users where roleId == …` يجري **داخل معاملة الحذف نفسها**، ⟵ **فأي مستند قرأته المعاملة وتغيّر يُبطلها**. ⚠️ **لكن Firestore لا تحجز مستنداً لم يوجد بعد ولا مستنداً لم تقرأه** — ⟵ **فإسنادُ الدور لمستخدمٍ عبر `updateUser` في اللحظة نفسها قد لا يُبطل الحذف**. ★★ **والأثر محدود ومعروف ومقيس:** يبقى المستخدم **بمرجع دورٍ محذوف**، ⟵ **وتعرضه الواجهة «بلا دور»** (`FirestoreRoleDirectory` يعرض المعرّف والشاشة تعرض «بلا دور»)، ⛔ **ولا يفقد صلاحية واحدة** — فالصلاحيات في **بطاقته** لا في الدور (`ADR-0016`)، **ولا يُمنَع من الدخول ولا تنكسر قاعدة أمنية**. ★ **والسداد المقترح حين يُبنى `WU-002`:** أن تقرأ `createUser`/`updateUser` مستندَ `roles/{roleId}` **داخل معاملتها** حين تُسنِد دوراً — ⟵ **فيصير المستندان طرفَي قفلٍ واحد** وتُبطَل إحدى المعاملتين حتماً. ⛔ **ولم يُنفَّذ الآن** لأنه يُضيف مسارَ رفضٍ («دورٌ لا وجود له») **لا سطر له في `FR-M1`** — ★ **ولا تُخترَع قاعدة** | ⚠️ **نافذة تزامن معلَنة — ⛔ لا ثغرة تفويض** | ★ **مع `WU-002`** (أول عملية تُسنِد دوراً بمعاملتها) | **منخفضة** |
| ✅★★★ `DEBT-24` | ✅★★★ **سُدِّد يوم رصده (2026-08-25) — وهو عطلٌ لم يكشفه اختبارٌ واحد بل أول استدعاء حقيقي على Cloud Run:** `Value.toJson()` في `googleapis` **سطحيٌّ لا متعدٍّ** — ⟵ **يُعيد `{'mapValue': Instance of MapValue}`** ⛔ لا خريطة JSON عادية. ★★ **فكان كل فحص `is Map` على القيمة المتداخلة يسقط ويعود `null` صامتاً** — ⟵ ⛔⛔ **بطاقة المالك تحمل تسع صلاحيات وكل عملية سحابية تقرؤها صفراً**، **فيُرفَض كل استدعاء إداري بـ`ERR_AUTH_001`** ★ **بلا استثناء ولا سجل ولا أي أثر يدلّ على السبب.** ✅ **والسداد `firestore_decode.dart` (جديد): فكٌّ من النموذج المُصنَّف مباشرةً** (`value.mapValue` · `value.arrayValue`) ⛔ **بلا المرور بـ`toJson()` إطلاقاً**، ⟵ ★ **فالمترجم نفسه يحرس البنية.** ✅ **ومُثبَت بالطفرة** (طفرتان قُتلتا) **وبأربعة اختبارات تبني القيمة بـ`googleapis` نفسها** ⛔ لا بخريطة يدوية تُثبت ما نظنّه. ★★ **والدرس المُلزِم:** ⛔ **لا يُفترَض شكلُ مخرجات مكتبة** — **يُقاس** | ✅ **مسدَّد كوداً واختباراً وطفرةً** | — | ✅ **زالت** |
| ✅★ `DEBT-25` | ✅★ **سُدِّد يوم رصده (2026-08-25):** جسمُ طلبٍ ببايتات ليست UTF-8 صالحة كان يرمي `FormatException` **خارج أي `try`** ⟵ **فيصل العميلَ `500 Internal Server Error` نصّاً خاماً بلا رمز كتالوج** — ⛔ **وهو ما يمنعه `error-handling-strategy.md` §3 القاعدة 1.** ⚠️ **والنظام عربيٌّ كلّه** فكل سببٍ ووصفٍ يعبر بمحارف متعددة البايتات، ⟵ **وبوابةٌ وسيطة تُعيد الترميز كانت تُسقِط العملية بلا تفسير.** ✅ **والسداد حارسٌ في `parseCallableRequest` يُرجِع `ERR_CALL_400`** — **باختبارٍ ببايتات حقيقية غير صالحة** | ✅ **مسدَّد** | — | ✅ **زالت** |
| ✅★★ `DEBT-26` | ✅★★★ **سُدِّد (2026-08-25) بحسم `IQ-020` الخيار أ** — **أُضيف المفتاح المستقل `dealerDisableWithBalance`** إلى `permissions-catalog.md` §2.1 (**73 مفتاحاً لا 72**)، ⟵ **فاكتمل `FR-M4-09` بشطريه** ⛔ **ولم يُعدَّل نصُّ المتطلب ولا أُنشئ `CR`**: القرار **استكمالُ الكتالوج لتنفيذ المتطلب كما هو**. ★★ **وتعطيلُ مقوتٍ رصيدُه ≠ 0 يشترط الآن ثلاثةً معاً:** المفتاح المستقل + إقرارٌ نصّي غير فارغ + **رصيدٌ مقيسٌ من `dealer_balances` داخل المعاملة** ⛔ لا من الجهاز. ★ **و`dealerWrite` بقي مسؤولاً عن الإضافة والتعديل والتعطيل العادي** — ⛔ **والمفتاح الجديد ليس بديلاً عنه**، **واختبارٌ صريح يُثبت أن رصيد الصفر لا يشترطه**. ★ **ورمزا الرفض يفترقان:** `ERR_AUTH_001` لنقص المفتاح · `ERR_SETUP_006` لنقص الإقرار. ✅ **ومُثبَت بالطفرة مرتين** (طبقة النطاق · العملية السحابية) | ✅ **مسدَّد — والمتطلب مُنفَّذ بالكامل** | ✅ **2026-08-25** | — |
| ★ `DEBT-27` | ★★ **جسر جهات الاتصال غير مُثبَت على جهاز حقيقي** (`FR-M3-03` · `FR-M4-03` · `WU-002`). ✅ **المُنفَّذ قناةُ منصّة مكتوبة بأيدينا** — `contact_picker.dart` + `MainActivity.kt` بنيّة `ACTION_PICK`، ★ **بلا إذن `READ_CONTACTS` وبلا حزمة طرفٍ ثالث** (تطبيقاً لـ`dependency-management-policy.md` §1 البندين 1 و2). ✅ **والطرف الدارت مُختبَر بمُنتقٍ مزيّف** (الزر يملأ الاسم والرقم معاً · ويختفي بلا مُنتقٍ · والإلغاء لا يُسقِط النموذج). ⛔ **والطرف الأصلي (Kotlin) لم يُشغَّل على جهاز ولا محاكٍ بعد** — ⟵ **فسلوك `ACTION_PICK` وقراءةُ الرقم واستئنافُ النشاط غير مقيسة**. ★ **والأثر محصور:** الزر **إثراءٌ لا مسار إلزامي**، ⟵ **وتعذّرُه يُخفيه ولا يمنع الإدخال اليدوي** ⛔ ولا يُسقِط حفظاً. ★ **والسداد: تشغيلٌ على محاكي أندرويد ضمن فحص الطرف إلى الطرف** | ⚠️ **مسارٌ غير مقيس — ⛔ لا عطل معروف** | ★ **مع أول تشغيل على جهاز** | **منخفضة** |
| ⚠️★★★ `DEBT-29` | ⚠️⚠️★★★ **«اليوم» في هذا النظام يومٌ بتوقيت UTC — وهو افتراضٌ مُعلَن لا قرارٌ صريح من صاحب المشروع** (`WU-003` · 2026-08-25). ★ **وأساسه ثلاثة نصوص قائمة لا اجتهاد:** ① `firestore.rules` §4 تُعرّف تاريخ اليوم بـ`request.time.date()` **وهي UTC**، **وهي منشورةٌ ومُختبَرةٌ بـ224 اختباراً منذ `WU-000`** · ② `CalendarDay` **مشتقّتُه الوحيدة `fromUtc`** (`WU-000`) · ③ ⛔ **ولا مستندٍ واحدٍ في `docs/` يذكر منطقةً زمنية إطلاقاً**. ⚠️⚠️ **والأثر مقيسٌ ومحدود ويجب أن يُقال صراحةً:** اليمن **UTC+3**، ⟵ **فكل ما يقع بين 00:00 و03:00 بالتوقيت المحلي يُنسَب إلى اليوم السابق** — **في `stockDate` وفي رقم المستند وفي مفتاح الرصيد `{sourceId}_{itemKey}_{stockDate}` وفي كل تقرير يُبنى عليها.** ★★ **وهذه النافذة خارج ساعات العمل الموثَّقة**: `persona-storekeeper.md` ينصّ على أن أول من يفتح التطبيق يفعل ذلك «**قرابة السادسة صباحاً**» (= 03:00 UTC)، ⟵ **فالانزلاق لا يقع في المسار الموثَّق**. ⛔⛔ **لكنه ليس مستحيلاً**: توريدٌ يبدأ قبل الفجر **يُكتب على يوم أمس بصمت** — ★ **ولا رسالةَ خطأ ولا أثرَ يدلّ عليه**، وهو **نفس صنف العطل الصامت الذي يحذّر منه `RISK-07`**. ✅ **والتخفيف المُنفَّذ:** **موضع القرار واحدٌ لا يتكرر** — `CalendarDay.fromUtc` في طبقة النطاق، **ويستدعيه `inventory_handler.dart` وحده على زمن المنصّة** ⟵ ★ **فتغييره إلى توقيت محلي تعديلٌ في موضعين لا في كل استعلام.** ★★ **والقرار المطلوب من صاحب المشروع:** **هل يبدأ يوم المخزون منتصف ليل UTC (= 03:00 محلياً) أم منتصف الليل المحلي؟** ⛔ **ولم يُصعَّد `IQ` لأن النصوص الثلاثة أعلاه تُرجِّح UTC ترجيحاً كافياً للتنفيذ** — ★ **وهذا السطر هو الإعلان الصريح للأثر، لا طيُّه.** | ⚠️ **افتراضٌ مُعلَن — ⛔ لا عطل معروف داخل ساعات العمل** | ★★ **قبل أول بياناتٍ حيّة على الإنتاج** | ★★ **متوسطة — وترتفع إن ثبت عملٌ قبل الفجر** |
| ★ `DEBT-30` | ★ **«مقارنة بآخر سعر فعلي مستخدم» لم تُنفَّذ** (`FR-M9-13` · `WU-005`). ★ **وسببها بنيوي لا إغفال:** «السعر الفعلي المستخدم» **يعيش داخل سطر التوزيع أو البيع النقدي أو السحبية** لحظة تجميده (`FR-M9-09` · `GR-32`) — ⟵ ⛔ **ولا مستند من الثلاثة موجود بعد** (`WU-006` · `WU-012` · `WU-014`)، ★ **فلا مصدر تُقرأ منه المقارنة أصلاً.** ✅ **وما نُفِّذ من البند نُفِّذ كاملاً:** **«نسخ أسعار أمس»** (نسخٌ صريح يملأ الحقول ⛔ ولا يحفظ) · **و«التسعير الجماعي»** (الشاشة دفعةٌ بطبيعتها ⛔ لا سطرٌ سطر). ★ **والأثر محصور:** المقارنة **اقتراحٌ للعرض لا يُكتب إلا بموافقة المستخدم** بنصّ المتطلب نفسه، ⟵ **وغيابُها لا يمنع تسعيراً ولا يُنتج رقماً خاطئاً** ⛔ **ولا يمسّ قاعدة عمل واحدة.** ★ **والسداد: مع `WU-006`** — أول مستندٍ يُجمِّد سعراً | ⚠️ **ميزة مساعدة مؤجَّلة — ⛔ لا قاعدة عمل ناقصة** | ★ **مع `WU-006`** | **منخفضة** |
| ⚠️★★ `DEBT-32` | ⚠️⚠️★★ **`WU-004` لم تُختبَر وظيفياً على المحاكي — والفحوص الآلية وحدها اجتازتها** (2026-08-26). ★ **والسبب محدَّد لا عام:** تشغيلُ الزيادة من طرف إلى طرف **يتطلّب تسجيل دخولٍ ببريدٍ وكلمة مرور حسابٍ حقيقي**، ✅★★ **وقد رُفع سببُ التعذّر بـ`AM-004` (2026-08-26):** ⛔ **لم يعد المنع مطلقاً** — ★ **بل صار حساب اختبار (QA) كامل الصلاحيات على التجريبية** (`environments.md` §1.5 · `RB-staging-qa-account`) **يُسجَّل به الدخول آلياً بتمرير مسار الملف السرّي وحده** ⛔ **بلا أن تمرّ قيمةُ الاعتماد في سياق الأداة ولا مخرجاتها.** ✅★★ **وبُني خُطّاف الملء المسبق فعلاً (`AM-005` · `DEBT-31` سُدِّد).** ⚠️⚠️ **لكن البند يبقى مفتوحاً لسببٍ واحدٍ متبقٍّ:** ⛔ **حساب الاختبار نفسه لم يُنشأ بعد** — ★ **وإنشاؤه فعلٌ بشري في وحدة التحكم** (`RB-staging-qa-account` §3.1)، ⟵ **فالخطوة ما تزال مُسلَّمة للمستخدم** (بروتوكول التشغيل §ج). ✅ **وما ثبت فعلاً:** الحزمة **بُنيت وثُبِّتت وأقلعت على `Pixel_6_API_36` حتى شاشة الدخول** · **والخدمة منشورة والتوجيه مُثبَتٌ حيّاً 15/15**. ⛔ **وما لم يثبت:** أن الأوزان والسطور والسكرب والضريبة **تُكتب فعلاً من واجهة المستخدم** وتعود صحيحة. ⚠️⚠️ **والدرس المكتوب في `DEBT-24` و`DEBT-25` ينطبق حرفياً:** عطلان حقيقيان مرّا من **581 اختباراً** ولم يكشفهما إلا التشغيل الحقيقي — ⟵ ⛔ **فلا تُعلَن `WU-004` منجَزة قبل هذا الاختبار.** ★ **والسداد: `RB-staging-qa-account` §3 (إنشاء الحساب) ⟵ ثم `flutter run --flavor staging --dart-define-from-file=…` ⟵ ثم تشغيل السيناريوهات المذكورة في تقرير `implementation-report-2026-08-26.md` §الخطوة التالية** · ⚠️⚠️★★ **وحُوول تنفيذُه في تشغيلَي `2026-08-26-run2` و`run3`:** ✅★★ **وفي `run3` أُنشئ حساب الاختبار فعلاً (`mjmK8owk4ZaciYT2yj1711jRgWB3`) وسُجِّل الدخول به وأُقلِع المالك بنجاح** — ⛔⛔★★ **ثم تعطّل المنح بحلقة إقلاعٍ مغلقة:** **المالك بلا `sourceScope` فلا يمنح أحداً ولا نفسه** (`IQ-022` مفتوح — **مقيسٌ حيّاً بثلاث استدعاءات**). ★ **والحاجز انتقل إذن من «لا حساب» إلى «لا مسار لأول نطاق في النظام»** ⟵ ✅★★ **ثم حُسم `IQ-022` ونُشر إصلاحُه (`callables:iq022` · `00008-dfp`) فصار للمالك نطاق** ⟵ ⛔⛔★★ **والحاجز اليوم `IQ-023`: لا جذرَ للصلاحيات التشغيلية** (`BR-M1-03` تسقُف المنح بصلاحيات المُنفِّذ · والمالك يملك التسعة الإدارية وحدها). ✅ **ومُثبَتٌ حيّاً أن كل ما عدا السقف يعمل: منحُ التسعة لحساب الاختبار ردّ `200`.** ⟵ ★ **وأصلُ التعذّر في `run2` كان:** ⛔ **حسابُ الاختبار لم يُنشأ بعد** — ★ **وإنشاؤه فعلٌ بشري في وحدة التحكم** (`RB-staging-qa-account` §3.1)، ⛔⛔ **وإنشاءُ الحسابات وكتابةُ كلمات المرور ممنوعان على الأداة بحدٍّ لم يُعدِّله `AM-004`** — ★ **فهو أباح **قراءة** اعتمادٍ قائم بلا اطّلاع** ⛔ **لا **إنشاءَه**.** ✅★★ **وما أُغلِق في ذلك التشغيل بدلاً عنه:** **بندا التدقيق ① و②** — ★ **اختبارُ ارتدادٍ سلوكي لـ`sourceDocType` مُثبَتٌ بإعادة العطل عمداً ثم إزالته** · ★ **وفحصٌ حيّ `30/30` يُثبت أن `WU-003` و`WU-005` منشورتان فعلاً** | ⛔ **بند قبولٍ مفتوح — والوحدة `[قيد التنفيذ]`** | ★★ **قبل تعليم `WU-004` منجَزة** | ★★ **عالية — فهي بالضبط الفجوة التي كشفت `DEBT-24`** |
| ★★ `DEBT-28` | ★★★ **لم يبقَ مسارُ كتابةٍ مباشر واحد للتطبيق بعد `WU-002`** — ⟵ **و`perm()` لم تعد تُستدعى إلا في شروط القراءة**. ⚠️⚠️ **والخطر ليس أمنياً بل خطرُ اختبارٍ يمرّ لسببٍ خاطئ:** حين يُرفَض **كل** طلب كتابة، يصير «مرفوض» تفسيراً محتملاً لنجاح أي اختبار إغلاق **بلا أن يُثبت شيئاً**. ✅ **والضابط المُنفَّذ:** اختبارٌ صريح يُثبت أن **القراءة تعمل فعلاً** (`ADR-0013` القاعدة 4) وأن **الفرق هو النطاق وحده** لا الرفض الشامل — `rules.test.js`: «✅⛔ والقراءة لم تُمَسّ». ★ **وضابطٌ ثانٍ**: الضابط الموجب في اختبارات `ADR-0016` صار **قراءةً تشترط مفتاحاً** (`dealerBalanceView`) ⛔ لا كتابةً تنجح. ⚠️ **ويجب أن يبقى هذان الضابطان قائمين** — ⛔ **وإسقاطُ أحدهما يُفرِغ 30 اختبار إغلاق من معناها** | ⚠️ **خطر تآكل تغطية — ✅ ومحروس** | ★ **يُراجَع مع كل زيادة تُغلِق مسار كتابة** | **متوسطة** |
| ✅★★★ `DEBT-34` | ✅★★★ **سُدِّد فور رصده (2026-08-26) — عطلٌ كشفه التشغيل على المحاكي وحده.** ⛔⛔ **شاشةُ «الوارد جواني» (`WU-004`) كانت غيرَ قابلةٍ للوصول من الواجهة إطلاقاً:** ★ **مبنيّةٌ ومسارُها `/home/sacks` مسجَّلٌ في `router.dart`** ⛔ **ولا زرَّ واحد في `home_shell.dart` يصل إليها** — ⟵ **فالزيادة كلها ميتةٌ عملياً.** ⚠️⚠️ **ولا اختبارَ آليٍّ واحدٍ كان يفشل بذلك:** ★ **اختبارات الشاشة تبنيها مباشرةً ولا تمرّ بالصدَفة** ⟵ **فالفجوة بين «الشاشة تعمل» و«المستخدم يصل إليها».** ✅ **المعالجة:** **مدخلٌ في الصدَفة محروسٌ بـ`sackCreate`** (`IQ-021` — ⛔ **لا `incomingCountWrite` ولا `sackView`**) · ✅★★ **واختبارُ ارتدادٍ يسرد مداخل الزيادات الأربع** ⟵ **فأي زيادةٍ قادمة بلا مدخلٍ تُفشِل البناء** · ✅ **ومُثبَتٌ بإزالة المدخل عمداً فأفشل الاختبار ثم أُعيد** | ✅ **مُسدَّد** | ✅ **—** | ✅ **—** |
| ✅★★★ `DEBT-35` | ✅★★★ **سُدِّد فور رصده (2026-08-26) — وكان يُعطِّل `WU-004` كلها.** ⛔⛔ **كلُّ إنشاء جونية يسقط بـ`500` خام:** ★ **`_entry` في `sack_intake.dart` كان «يُسطِّح» `DecimalValue` إلى `double` خام للقيد**، ⟵ **ثم يُرمِّزه `_auditWrite` فيرفضه المُرمِّز** (`ADR-0015`). ★★ **والتسطيحُ لم يكن يُنجز غايتَه أصلاً:** `DecimalValue` **تُخزَّن `doubleValue` في القاعدة** ⟵ **فالقيد يُقرأ رقماً عادياً في الحالتين**، ★ **والغايةُ المكتوبة («لا يحمل أنواعَ ترميزٍ داخلية») متحققةٌ في المخزَّن لا في نوع الوسيط.** ⚠️⚠️ **والاختبارُ القائم كان يحرس العطل لا يمنعه** (`expect(valuesAfter['totalWeight'], 45.0)`) — ⛔ **ولا واحدَ يُمرِّر القيد بالمُرمِّز.** ✅ **المعالجة:** إزالةُ التسطيح · ✅★★ **واختبارٌ يُرمِّز القيد فعلاً** · ✅ **ومُثبَتٌ بإعادة العطل عمداً ثم إزالته** · ✅★★ **وحيّاً: `createSack` ردّ `200` بعد أن كان `500`** | ✅ **مُسدَّد** | ✅ **—** | ✅ **—** |
| ✅★★ `DEBT-36` | ✅★★★ **سُدِّد فور رصده (2026-08-26) — عطلُ تخطيطٍ مرئي.** ⛔⛔ **بطاقةُ الجونية تُرسَم حرفاً في كل سطر مع شريط التجاوز الأصفر فور إضافة أول سطور**: ★ **`InventoryTile` تضع `...badges` في الصفّ بلا قيدٍ واحد** ⟵ **وشارةٌ ثانية تظهر عندئذٍ** («⏳ سعر غير نهائي») **تعصر عمودَ النص حتى الصفر** (**تجاوزٌ مقيسٌ 149 بكسل**). ⚠️ **ولم يفشل به اختبار:** ★ **الاختبارات تبني البطاقة بشارةٍ واحدة وبسطحٍ واسع.** ✅ **المعالجة:** **`Flexible` + `Wrap`** ⟵ **الشارات تلتفّ ولا تُزاحِم** · ✅★★ **واختبارُ ارتدادٍ على عرض هاتفٍ حقيقي بشارتين** · ✅ **ومُثبَتٌ بإعادة العطل عمداً فأفشل الاختبار بالرسالة نفسها** | ✅ **مُسدَّد** | ✅ **—** | ✅ **—** |
| ✅★★★ `DEBT-33` | ✅★★★ **سُدِّد فور رصده (2026-08-26) — عطلٌ حقيقي كشفه التشغيل على المحاكي وحده.** ⛔⛔ **كلُّ إنشاءِ نوعٍ وزنيٍّ بوزن حبةٍ مهيّأ كان يسقط بـ`500` خام بلا رمز كتالوج** — ★ **والسبب:** `master_data.dart` → `_itemFields` **كانت تمرّر `pieceWeightGrams` `double` خاماً**، ⟵ **والمُرمِّز يرفض `double` المجرَّد عمداً** («لأن المُرمِّز لا يعرف أمبلغٌ هذا أم وزن، يرفض الاثنين ويُلزم المُستدعي بالتصريح» · `ADR-0015`). ⛔⛔★★ **والحقلُ نفسه كان يُكتب بطريقتين من كاتبَين:** `sack_intake.dart` **تُصرِّح به `DecimalValue` صحيحاً** ⛔ **و`master_data.dart` لا تُصرِّح** — ★ **وهو حرفياً درسُ `sourceDocType`: كاتبان لحقلٍ واحد يفترقان.** ✅ **المعالجة:** `DecimalValue(item.pieceWeightGrams!)` · ✅★★ **واختبارُ ارتدادٍ يُمرِّر الخطة بالمُرمِّز فعلاً** ⟵ ⛔ **وكلُّ الاختبارات القائمة كانت تفحص الخطة ولا واحد يُرمِّزها** — ★ **فالفجوة كانت بين الطبقتين تماماً** (**درس `DEBT-25` حرفياً**) · ✅ **ومُثبَتٌ بإعادة العطل عمداً فأفشل الاختبار ثم أُزيل** | ✅ **مُسدَّد** | ✅ **—** | ✅ **—** |
| ✅★★★ `DEBT-37` | ✅★★★ **سُدِّد فور رصده (2026-08-26) — عطلُ توثيقٍ تدقيقي لا يُغيِّر ريالاً واحداً ولا يظهر في أي شاشة.** ⛔⛔ **حالةُ «تهيئة» من `pieceWeightOrigin` لم تكن تُكتَب قط:** ★ **`_lineFields` في [`functions_sack_repository.dart`](../../lib/capabilities/inventory/infrastructure/functions_sack_repository.dart) كانت تُرسِل وزنَ الحبة في حقلٍ واحد** (`pieceWeightGrams`) **مهما كان مصدره**، ⟵ **فيأخذ `resolveSackLine` في السحابة فرعَ «اليدوي» دائماً** — ★ **والحقلُ أحدُ ثلاثةٍ يوثّقها `sack-intake-design.md` §4 نصّاً** («تهيئة/يدوي/مستنتج») **ويصفها بأنها «تجعل مصدر الرقم قابلاً للتدقيق».** ⛔⛔★★ **وشقٌّ ثانٍ أخطر:** `_LineDraft.fromLine` **كانت تطوي صلةَ التهيئة في `null`** ⟵ **فأيُّ تعديلٍ لسببٍ آخر يُعيد وسمَ كلِّ سطرٍ وزنيٍّ لم يُمَسّ «يدوياً»** — ★ **وهو غسلُ مصدرٍ لا حفظُه.** ⚠️⚠️ **ولا اختبارَ واحدٍ من 1042 كان يفشل بذلك:** ★ **النطاق يشتقّ الحالات الثلاث صحيحةً ومُختبَرةً**، ⛔ **والفجوة في الحمولة بين الطبقتين** — **درسُ `DEBT-25` و`DEBT-33` حرفياً للمرة الثالثة.** ✅ **المعالجة:** **وزنُ الحبة يعبر في حقلِ حالته** (`configuredPieceWeightGrams` مقابل `pieceWeightGrams`) ⛔ **ولا يُرسَل `pieceWeightOrigin` نفسه** (**مشتقٌّ لا مُدخَل**) · **ومصدرُ السطر المخزَّن مُجمَّدٌ مع رقمه** (`FR-M7-15`) · ✅★★ **وأربعةُ اختباراتِ ارتداد** (3 على الحمولة + 1 على إعادة الإرسال) · ✅ **ومُثبَتٌ بإعادة كلا الشقّين عمداً فأفشل كلٌّ اختبارَه ثم أُزيلا** · ⛅ **ورُصد حيّاً على التجريبية:** `SCK-20260826-0004` **مكتوبٌ `manual` ووزنُه من التهيئة بلا مساسٍ** | ✅ **مُسدَّد** | ✅ **—** | ✅ **—** |
| ✅★★ `DEBT-31` | ✅★★★ **سُدِّد (2026-08-26 · `AM-005`) — والخُطّاف مبنيٌّ ومحروسٌ ومُختبَر.** ~~خُطّاف الدخول التلقائي في التطبيق لم يُبنَ بعد~~ (`AM-004`). ★ **الآلية المعتمَدة موثَّقة كاملةً** (`environments.md` §1.5 · `RB-staging-qa-account` §5): **يُمرَّر `--dart-define-from-file=<مسار الملف السرّي>`** ⟵ **فتصل قيمتا `QTMS_STAGING_QA_EMAIL` و`QTMS_STAGING_QA_PASSWORD` ثوابتَ تصريفٍ للتطبيق** ⛔ **بلا مرورها في أي أمرٍ مكتوب.** ✅ **والكود الذي يقرؤها:** [`staging_qa_credentials.dart`](../../lib/core/startup/staging_qa_credentials.dart) ⟵ `login_screen.dart` (`initState`). ✅★★ **والشرط الأمني مُنفَّذ بحارسين مستقلين لا بواحد:** ① **`kDebugMode`** — ★ **ثابتُ تصريف يُقتلع الفرع كاملاً في بناء الإصدار** · ② **القيمتان فارغتان ما لم تُمرَّرا صراحةً** ⛔ **وبناءُ الإنتاج لا يُمرِّرهما.** ⛔⛔★★ **ولا إرسال تلقائياً — الملء يُهيّئ الحقلين والضغط يبقى فعلاً صريحاً**، ⟵ ★ **فلا مسار دخولٍ ذاتيّ الحركة في أي بناء.** ✅★★ **ويحرسه ثلاثةَ عشرَ اختباراً** (7 وحدة + 3 بوابة ساكنة + 3 ودجات) — ★ **منها بوابةٌ تُفشِل البناء إن سقط أحد الحارسين أو قُرئ الثابت من شاشة أو أُضيف إرسالٌ تلقائي**. ⛔⛔★★★ **ولم يُكتفَ بها — بل قِيس على الحزمة نفسها** (`AM-005` · بوابة التحقق §7 بند 2): ★ **بُنيت حزمةُ إنتاجٍ حقيقية والاعتمادُ مُمرَّرٌ إليها عمداً بقيمتين مُختلَقتين** (`flutter build apk --flavor production --release --dart-define=…`)، ⟵ ✅ **فغاب الاعتماد كلياً عن `libapp.so` للمعماريات الثلاث وعن كل ملفٍّ في الحزمة** (`0`)، ★★ **بينما نفسُ القيمتين ظهرتا فعلاً في بناء `staging/debug`** (`kernel_blob.bin` = `1`). ⛔⛔★★ **وشاهدٌ مضاد يمنع «الصفر الأعمى»:** نصوصٌ لاتينية معلومةٌ قِيست في نفس اللقطة الإنتاجية (`firebase`=35 · `users`=6 · `sacks`=3 · `daily_prices`=1) — ⟵ **فالأداة مُبصِرة والغيابُ حقيقي** | ✅ **مُسدَّد** | ~~مع أول تشغيل §د.2~~ | ✅ **—** |
| ⚠️★★ `DEBT-38` | ⚠️★★ **صاحبُ `sackLinesEnter` يُغيِّر سطوراً دخلت الدفتر بلا `sackAmend`** (`WU-004` · 2026-08-26). ★★ **وهو أثرٌ مُعلَنٌ لقرار المالك في `IQ-025`** ([`ADR-0018`](adr/ADR-0018-deferred-entry-reason-optional.md) — الخيار ب-1) ⛔ **لا سهوٌ ولا اكتشافٌ لاحق:** ★ **عُرض عليه التعارض بنصّه قبل أي تنفيذ فاختاره صراحةً.** ⚠️ **والوضعُ سابقٌ على القرار جزئياً:** `enterSackLines` **كان يقبل الكتابة فوق سطورٍ قائمة قبله** — ⛔ **لكنه كان يشترط سبباً نصياً**، ⟵ **فصار الآن يقع بلا سببٍ وبلا مفتاح التعديل.** ⛔⛔★★ **وما لم يُمَسّ:** **الشرطان 1 و3 من `ADR-0004` نافذان كاملَين** — **لا كتابة بلا صلاحيةٍ ما** (`sackGate` ②) **ولا كتابة بلا قيدٍ في المعاملة نفسها** (`ADR-0013` القاعدة 1) ⟵ **فالقيد يحمل القيم قبل وبعد والمُنفِّذ والوقت والمصدر**، ★ **و«من غيّر ماذا ومتى» مُجابٌ كاملاً** ⛔ **و«لماذا» وحدَه قد يغيب.** ✅ **والمعالجة المقترحة — بندُ صلاحيةٍ لا بندُ سبب:** ★ **أن يشترط `enterSackLines` مفتاح `sackAmend` حين تكون `lines` غيرَ فارغة** (`FR-M7-26` · `FR-M7-27`) ⛔ **بلا مساسٍ باختيارية السبب** ⟵ **فيُغلَق الالتفاف على المفتاح وحده.** ⛔ **ولا يُطبَّق بتقديري** — ★ **يحتاج قرار المالك لأنه يمسّ تفكيك الصلاحيات الستَّ عشرة في `FR-M7` §5.** ⛔⛔★★★ **وتحديثٌ حاسم (2026-08-27 · `ADR-0020`): شطرُ السبب من هذا البند سقط نهائياً** — ★ **إذ صار وقوعُ التغيير بلا سبب هو السلوك المعتمد في كل النظام لا انحرافاً في هذا المسار.** ⟵ ⚠️★★ **والبند يبقى مفتوحاً بشطره الثاني وحده — شطرِ الصلاحية:** **`enterSackLines` يُغيِّر سطوراً قائمة بلا `sackAmend`**، ★ **وهو التفافٌ على تفكيك المفاتيح** ⛔ **لا علاقةَ له بالسبب** | ⚠️ **متوسط — تفويض** | ★ **قبل إغلاق المرحلة الأولى** | ⚠️ **مفتوح — بشطر الصلاحية وحده** |

| ✅★★★ `DEBT-39` | ✅★★★ **سُدِّد فور رصده (2026-08-26) — ★ رصدَه اختبارُ المحاكي وحده، ⛔ ولم يكشفه أيٌّ من 1062 اختباراً آلياً.** ⛔⛔ **سببُ تعديلٍ سابق يبقى عالقاً على تعديلٍ لاحقٍ ليس سببَه:** ★ `_partialWrite` في [`sack_intake.dart`](../../functions/lib/src/sack_intake.dart) **كان يشتقّ `updateMask` من `fields.keys`** — ⟵ **وحين يغيب السبب يغيب من القناع أيضاً**، ⛔ **فتبقى القيمة المخزَّنة كما هي.** ⚠️⚠️ **ومقيسٌ حيّاً على `SCK-20260826-0006`:** **`amendCount: 3` و`amendedBy` و`lastAmendedAt` جديدةٌ كلُّها** ⛔ **و`amendReason: «REASON-TEST-4»` من التعديل الذي قبله** — ⟵ ★★ **وهو عينُ عطل `IQ-025` بثوبٍ آخر:** **نصٌّ يقرؤه المدقّق سبباً لما ليس سببَه.** ★★ **وأشدُّ ما فيه أنه تناقضٌ داخلي:** **المعالجة الصحيحة كانت مكتوبةً فعلاً في `_planTax`** (القناع يحمل `amendReason` دائماً فيمحوه الإدخالُ التالي) ⛔ **ولم تُطبَّق على مستند الجونية** — ★ **فمسارانِ متجاورانِ افترقا في القاعدة نفسها.** ✅ **المعالجة:** **`amendReason` يبقى في القناع عند كل تعديلٍ لاحق ولو غاب** ⟵ **فيُمحى المخزَّن** · ⛔ **وأوّلُ تعبئةٍ لا تمسّه أصلاً** (**لا تعديلَ سابقاً يُمحى سببُه**) · ★ **والقيدُ يحفظ الأصل لكل تعديل.** ✅★★ **واختبارا ارتداد** — ⛔⛔ **ولم يُكتفَ بكتابتهما:** ★ **أُعيد العطلُ عمداً فأفشل الاختبار** (`+30 -1`) **ثم أُزيل فعاد أخضر (333)** · ⛅★★ **ومُثبَتٌ حيّاً بعد النشر** (`00014-pxq`): **تعديلٌ خامسٌ بلا سبب ⟵ `amendCount: 5` والحقلُ مُحيَ تماماً** | ✅ **مُسدَّد** | ✅ **—** | ✅ **—** |
| ✅★★★ `DEBT-40` | ✅★★★ **سُدِّد فور رصده (2026-08-27 · `WU-006`) — ★ رصدَه اختبارُ المحاكي وحده، ⛔ ولم يكشفه أيٌّ من 1265 اختباراً آلياً.** ⛔⛔★★★ **قراءةُ مستندٍ غائبٍ بمعرّفه تُرفَض بـ`PERMISSION_DENIED`:** ★ شرطُ القراءة على `distributions` هو `isSignedIn() && storedInScope()`، **و`storedInScope()` يقرأ `resource.data.sourceId`** — ⟵ **والمستندُ الغائب بلا `resource` أصلاً فيُقيَّم الشرط `false`.** ⛔ **والغياب هو الحالة الطبيعية قبل أول توزيعة**، ⟵ **فمسارُ `E-04` كان يفشل في أشيع حالاته** وتبقى الشاشة على الهيكل العظمي أبداً. ★★ **وهي مقايسةُ `IQ-024` و`WU-008` الثالثة حرفياً** (`CLAUDE.md`): **كل استعلامٍ شرطُ قراءته يعتمد `resource.data` يجب أن يُقيّد ذلك الحقل صراحةً** — ⟵ ★ **والعلاج تقييدُ الاستعلام** (`dealerDistributionProvider` يشتقّ الوجود من قائمة اليوم المقيَّدة بـ`sourceId` و`stockDate` بفهرسٍ قائم) ⛔ **لا تخفيفُ القاعدة**: ★ **القاعدة سليمة ولم تُمَسّ بحرف.** ✅ **ولها اختبارُ ارتداد** في `distribution_screen_test.dart` | ✅ **سُدِّد** | — | ✅ **مُغلَق** |
| ✅★★★ `DEBT-41` | ✅★★★ **سُدِّد فور رصده (2026-08-27 · `WU-006`) — ★ رصدَه اختبارُ المحاكي وحده كذلك.** ⛔⛔★★ **قيدُ تدقيقٍ حمل `double` مجرَّداً فأسقط الالتزام كلَّه:** ★ `_auditSafe` في [`distribution.dart`](../../functions/lib/src/distribution.dart) كان **يفكّ `DecimalValue` إلى `double`** قبل بناء `AuditEntry` — ⟵ **والمُرمِّز يرفض الفاصلة العائمة المجرَّدة رفضاً مقصوداً** (`ADR-0015` القاعدة 1 · `firestore_value.dart`)، ⛔ **فسقطت المعاملة كاملةً برمزٍ عام** بينما **الخطةُ نفسها كانت صحيحة الحقول**. ★★ **وهو درسُ `DEBT-37` نفسه بوجهٍ ثانٍ:** ⛔ **اختبارُ الطبقة لا يُغني عن اختبار ما يعبر بينها** — ⟵ **كل اختبارات الخطة كانت خضراء.** ✅ **والعلاج تمريرُ القيم كما هي** (الوزن يبقى `DecimalValue` مغلَّفاً)، ✅ **ولها اختبارُ ارتداد يُرمِّز كل مستندٍ وكلَّ قيدٍ فعلاً** في `functions/test/distribution_test.dart` | ✅ **سُدِّد** | — | ✅ **مُغلَق** |
| ⚠️★★ `DEBT-42` | ⚠️★★ **حدُّ قراءة دفتر المقوت 500 قيدٍ في المصدر الواحد** (`dealerLedgerLimit` في [`distribution_handler.dart`](../../functions/lib/src/distribution_handler.dart) · `WU-006`). ★ **ورصيدُ المقوت يُجمَع من الدفتر داخل المعاملة** (`ADR-0008`) — ⟵ **وقيودُه تنمو مع عمر الحساب لا مع نشاط اليوم**، ⛔ **بخلاف دفتر المخزون المحدود بيومٍ ونوعٍ ومصدر.** ★ **والتجاوز لا يُبتلَع:** **يُرفَض الطلب صراحةً** ⛔ **ولا يُجمع رصيدٌ من مجموعةٍ مبتورة** — ★ **فالرقمُ الكاذب في دفتر ذمم أخطر من الرفض.** ⚠️ **والأثر العملي:** **حسابٌ يتجاوز 500 قيدٍ في مصدرٍ واحد يتوقف عن قبول التوزيع.** ✅ **والعلاج المقترح رصيدٌ متدرّج مُرحَّل** (نقطةُ بداية دورية تُجمع منها القيود اللاحقة وحدها) ⛔ **لا رفعُ الحد** — ★ **ورفعُه يؤجّل العطل ولا يمنعه.** ⛔ **ولا يُخمَّن حلُّه في هذه الزيادة**: ★ **موضعه المقبوضات** (`WU-007`) **وأعمار الديون** (`WU-023`) حيث يُقرأ الدفتر نفسه | ⚠️ **متوسط — قابلية التوسّع** | ★ **قبل إغلاق المرحلة الثانية** | ⚠️ **مفتوح** |
| ✅★★ `DEBT-43` | ✅★★★ **سُدِّد (2026-08-28) بقرار المالك في `IQ-027` — الخيار أ.** ★ **المبالغُ الثلاثة (`settledAmount` · `discountedAmount` · `remaining`) نُقلت إلى `distributions/{key}/pricing/current`** المحكوم بـ`distributionPriceView`، ★ **و`settlementStatus` وحده بقي في الأب** (**حالةٌ لا رقم**) ⟵ **فالفهرس `dealerId ↑ · settlementStatus ↑ · stockDate ↑` صالحٌ بلا تغيير** ⛔ **ولا مفتاحَ صلاحيةٍ جديداً** (الكتالوج **74** كما هو). ★ **وطُبِّق قبل أول كتابةٍ حيّة** ⟵ ⛔ **بلا هجرة بيانات — والكلفة صفر كما خُطِّط.** ★ **والقراءة انتقلت في** [`readStoredSettlement`](../../functions/lib/src/distribution_handler.dart) **باختبار ارتدادٍ مستقل** ([`stored_settlement_test.dart`](../../functions/test/stored_settlement_test.dart) — **7 اختبارات**) ★ **يسقط وحدَه لو عادت القراءةُ إلى الأب** | ✅ **مُغلَق** | ✅ **أُنجز قبل `WU-007`** | ✅ **مُغلَق (2026-08-28)** |
| ⚠️★ `DEBT-59` | ⚠️★★ **حدُّ قراءة قيود دفتر المقوت داخل المعاملة** — ★ **500 في مسار التوزيع** (`dealerLedgerLimit`) **و1500 في مسار القبض** (`receiptDealerLedgerLimit` — **لأنّه يقرأ كلّ مصادر المقوت لا مصدراً واحداً**، `FR-M12-04`). ⛔⛔ **وتجاوزُه رفضٌ صريح لا جمعٌ ناقص** — ★ **لأن رصيداً مجموعاً من مجموعةٍ مبتورة رقمٌ كاذب يُطالَب به مقوت** (`ADR-0008`). ⟵ **فحسابٌ يتجاوز الحدّ يتوقف عن قبول القبض أو التوزيع.** ★ **والعلاج رصيدٌ متدرّج مُرحّل** ⛔ **لا رفعُ الرقم** — ⟵ **فالرفع يؤجّل العطل ويُبطئ كل معاملة.** ⚠️ **وهو حدّ معلَن لا مطويّ** (`WU-007` · 2026-08-29) | ⚠️ **متوسط — يتوقف عند حسابٍ طويل العمر** | ★ **قبل أول حسابٍ يقارب الحد** (`WU-017`) | ⚠️ **مفتوح** |
| ⚠️★★ `DEBT-44` | ⚠️★★ **دوالُّ السبب في `firestore.rules` بقيت تصف شرطاً أُلغي** (2026-08-27 · `ADR-0020`) — ★ **`nonEmpty()` و`canAmend()` و`canCancel()` ما تزال في الملف** وتشترط `amendReason`/`cancelReason` **غيرَ فارغ**. ✅★★★ **وأثرُها التشغيلي صفرٌ مقيس:** ⛔ **لا تُستدعى من أي قاعدة** — **كل مجموعة `allow create, update: if false` منذ `WU-002`** (`ADR-0013` القاعدة 2)، ★ **ومُثبَتٌ نصّياً: لا موضع استدعاء واحد لـ`canAmend` ولا `canCancel` في الملف كله**، ⟵ **و236 اختبار حماية تمرّ بلا تغيير حرفٍ واحد.** ⚠️ **والخطر إدراكيٌّ لا وظيفي:** ★ **قارئٌ للملف يظنّ الشرط قائماً فيبني عليه.** ★★ **وتُرِكت عمداً لا سهواً:** `ADR-0020` §د نصّ أن **«`firestore.rules` لا تُمَسّ بحرف»**، ★ **وممارسة المشروع تحفظ نصّ كل قيد في موضعه ولا تحذفه** (`DEBT-21`). ✅ **والمعالجة المقترحة: سطرُ تعليقٍ واحد فوقها يقول «مُلغى بـ`ADR-0020` — محفوظٌ سجلاً»** ⛔ **بلا حذف** — ★ **يُنفَّذ مع أول تعديلٍ لاحق يمسّ الملف لسببٍ آخر** ⟵ **فلا يُفتَح نشرُ قواعدَ لتعليق.** | ⚠️ **منخفض — إدراكي** | ★ **مع أول تعديل لاحق على `firestore.rules`** | ⚠️ **مفتوح** |
| ⚠️★ `DEBT-45` | ⚠️★ **تعليقٌ في التطبيق يستشهد بشرطٍ أُلغي** (2026-08-27 · `ADR-0020`) — ★ **شقيقُ `DEBT-44` في الطرف الآخر:** التعليق فوق حقل سبب التعديل في [`distribution_screen.dart:292`](../../lib/capabilities/sales_receivables/presentation/distribution_screen.dart) يقول **«(`ADR-0004` الشرط 2)»**، ⛔ **وذلك الشرط ألغاه `ADR-0020` إلغاءً لا تقييداً.** ✅★★ **والادّعاء نفسه ما يزال صحيحاً** — «⛔ ولا يُعبَّأ آلياً أبداً» **هو القيد ① الباقي في `ADR-0020`**، ⟵ **فالخلل في الاستشهاد وحده لا في القاعدة.** ⚠️ **وأثرُه التشغيلي صفر** ⛔ **وإدراكيٌّ محض:** ★ **قارئٌ يتبع المرجع يصل إلى شرطٍ مرفوع فيظنّ الإلزام قائماً** — ⟵ **وهو بعينه خطرُ `DEBT-44`.** ✅ **والمعالجة: تصحيح المرجع إلى `ADR-0020` القيد ①** ⛔ **بلا مسّ نصّ الادّعاء** — ★ **يُنفَّذ مع أول تعديلٍ لاحق يمسّ الشاشة** (`P8` في `MASTER.md` §5b · `ADR-0021`) ⟵ **فلا تُفتَح الشاشة لتعليق.** ⚠️ **ورُصد بجرد الشاشات (2026-08-27)** ⛔ **لا باختبارٍ آلي** — ★ **ولا اختبارَ يرصد استشهاداً ميتاً أصلاً**. ⛔⛔★★ **وموضعٌ ثانٍ رُصد في اليوم نفسه أثناء شريحة `P6`:** ★ **ترويسةُ [`role_form_screen.dart`](../../lib/capabilities/identity_access/presentation/role_form_screen.dart) كانت تنصّ على «التعديل يفرض حقل سبب نصّي غير فارغ» (`ADR-0004` · `DEBT-21` ①) وعلى «الحذف يفرض سبباً بحسم `IQ-018`»** — ⛔ **والشرطان ساقطان معاً بـ`ADR-0020`** ⟵ **والكودُ تحتهما اختياريٌّ فعلاً**: ★ **فالتعليقُ كان يُطمئن قارئَه إلى عكس ما ينفّذ.** ✅ **وسُدِّد هذا الموضع نصّياً في نفس الشريحة** (⛔ **بلا مساسٍ بمنطق**) — ⚠️ **ويبقى `distribution_screen.dart:292` مفتوحاً.** ⟵ ★★ **والدرس أن الاستشهاد الميت ليس موضعاً واحداً بل نمطاً**: ⛔ **فيُفتَّش عنه عند كل لمسةٍ لملفٍّ قديم.** | ⚠️ **منخفض — إدراكي** | ★ **مع أول تعديل لاحق على `distribution_screen.dart`** | ⚠️ **مفتوح جزئياً — موضعٌ سُدِّد وآخر باقٍ** |
| ⚠️★ `DEBT-46` | ⚠️★ **مسارُ إلغاءِ توزيعةٍ وحيدٌ بلا حارس `blankToNull`** — [`distribution_screen.dart:427`](../../lib/capabilities/sales_receivables/presentation/distribution_screen.dart) يمرّر **`cancelReason: _reason.text` خاماً**، ★ **بينما ثلاثة عشر مسارَ سببٍ في التطبيق كلُّها تمرّ بـ`blankToNull`** (مقيسٌ نصّياً 2026-08-27) — ⟵ **فهو الشذوذ الوحيد.** ✅★★★ **والأثر على البيانات صفرٌ مقيسٌ لا مُقدَّر:** ⛔ **والادّعاء الأول أنه «يُخزِّن نصّاً فارغاً» خاطئ** — ★ **`CallableRequest.readString` تُرجِع `null` لكل نصٍّ فارغ أو مسافات** ([`callable.dart:433`](../../functions/lib/src/callable.dart))، ⟵ **فالفراغ يُقرأ غياباً في السحابة قبل أن يصل حقلَ المستند.** ⚠️ **والخطر أن الضمانة صارت مسنودةً إلى طرفٍ واحد:** ★ **`CLAUDE.md` تنصّ على `blankToNull` «في الطرفين»**، ⛔ **وطرفٌ ساقط اليوم لا يُرى لأن الآخر يستره** — ⟵ **فأيُّ مسارٍ مستقبليٍّ يقرأ السبب بغير `readString` يكشفه فجأة.** ✅ **والمعالجة سطرٌ واحد:** `blankToNull(_reason.text)` ⛔ **ولا تغييرَ سلوكٍ يصحبه** — ★ **يُنفَّذ مع `P8`** (`ADR-0021`) ⟵ **ويُسنَد باختبارٍ يثبت أن الفراغ يعبر غياباً** ⛔ **لا نصّاً.** ★★ **ودرسُه درسُ `DEBT-37`:** **اختبارُ الطبقة لا يُغني عن اختبار ما يعبر بينها** | ⚠️ **منخفض — اتساقٌ ودفاعٌ في العمق** | ★ **مع أول تعديل لاحق على `distribution_screen.dart`** | ⚠️ **مفتوح** |
| ✅★★ `DEBT-48` | ✅★★ **زرُّ المرشِّحات النصّي كان يعصر اسمَ المصدر في رأس السياق** (2026-08-27 · `ADR-0021` نمط `P3`) — ★ **رصده المحاكي عند المقياس الطبيعي لا عند التكبير:** **ثلاثةُ عناصرَ في صفٍّ واحد** (مصدر + يومٌ مقفل + `TextButton.icon` بنصّ «مرشِّحات») ⟵ **فاقتُطع الاسمُ إلى «مصدر الاخت…»** على `DailyPricingScreen`. ⛔⛔ **وأثرُه ليس تجميلياً:** ★ **كلُّ رقمٍ في تلك الشاشة يخصّ مصدراً بعينه** (`A-01` — لا جمع بين مصدرين)، ⟵ **ومستخدمٌ لا يعرف المصدرَ المختار يُسعِّر على المصدر الخطأ.** ⚠️⚠️ **و13 اختبارَ ويدجت لم تكشفه** — ★ **لأن أياً منها لم يُمرِّر مرشِّحاتٍ واسماً طويلاً معاً.** ✅ **عولج بجعل الزرّ أيقونياً بشارةِ عدد** (`Badge.count`) ⟵ **فسقط ثالثُ العناصر النصّية وبقي الصفُّ واحداً والسقفُ 96dp محفوظاً**، ★ **والعددُ لم يسقط بل انتقل إلى الشارة، والنصُّ إلى `tooltip`** ⟵ **فشرطُ §5b «وعددُ النشط يظهر في زرّ الطيّ» محفوظٌ بحرفه.** ★ **وله اختبارُ ارتداد** (`context_header_test.dart`) **وأُعيد الفحص على المحاكي نفسه بعد الإصلاح.** | ✅ **مُعالَج** | ★ **سُدِّد في يومه** | ✅ **مُسدَّد** |
| ⛔⛔★★★ `DEBT-49` | ⛔⛔★★★ **حفظُ الأسعار يُخفق حيّاً على التجريبية — ولم يُشخَّص** (2026-08-27) — ★ **رصده اختبارُ المحاكي لـ`P4`:** **إدخالُ سعرَي نوعٍ ثم «حفظ أسعار اليوم» يردّ** «تعذّر إتمام العملية. أعد المحاولة.» (`CatalogMessage.operationFailed`). ⛔⛔ **وليس أثراً لتغيير العرض:** ★ **`_submit` ومستودعُه والعميلُ المستدعى لم يُمَسّ منها حرفٌ في شريحة `P4`** — ⟵ **والتغييرُ نقلَ الزرَّ واللافتة موضعاً لا غير.** ★★ **وما قِيس فعلاً:** ✅ **الخدمة منشورة وحيّة** (`qtms-callables-00016-65v`) · ✅ **والمسارُ موجودٌ ويحرس نفسه** (`POST /writeDailyPrices` بلا رمزٍ ⟵ `401`، ومسارٌ مجهول ⟵ لا اتصال) · ✅ **و`allUsers` مربوطةٌ بـ`roles/run.invoker`** ⟵ ⛔ **فليست راية `--no-allow-unauthenticated`** (تحذيرُ `CLAUDE.md`). ⚠️⚠️ **وما لم يُقَس:** ⛔ **لم يظهر طلبُ التطبيق في سجل Cloud Run في نافذة الفحص إطلاقاً** — ★ **فالإخفاق قد يسبق الخروجَ إلى الشبكة** (رمزُ هوية غير مُرفَق · App Check · استثناءٌ قبل النداء) ⛔ **ولم يُحسَم.** ⛔⛔★★ **ولم يُلاحَق عمداً:** ★ **تشخيصُه يمسّ مسارَ كتابةٍ ومصادقة** — ⟵ **وهو خارج التفويض التلقائي صراحةً** (بروتوكول التشغيل §ج: تسليمُ أمر البيئة بدل التجريب المتكرر). ★★ **وهو أحدُ وجوه البند المفتوح أصلاً على `WU-005`** — «نشرُ خدمة العمليات ثم اختبارُ المحاكي حيّاً» ⟵ **فالمسارُ لم يُختبَر حيّاً قبل اليوم قطّ**، ⛔ **والإخفاق مُرجَّحٌ سابقٌ للشريحة لا ناتجٌ عنها.** ⛔⛔★★★ **وتحديثٌ مقيسٌ في اختبار `P6` (2026-08-27) يُوسِّع المدى ويحسم المنشأ:** ★ **الإخفاق ليس في التسعير وحده** — ⟵ **`إنشاء دور` بمُدخَلٍ صحيح ومفتاحٍ مُختار ردَّ الرسالة نفسَها حرفياً**، ★ **و`حفظ التعديل` على دورٍ قائم كذلك** — ⛔⛔ **وكلاهما مسارٌ لم تمسّه شريحةُ الأنماط بحرف واحد** (`role_form_screen.dart` لم يتغيّر فيه `_submit` ولا مستودعُه). ⟵ ★★★ **فالإخفاق عامٌّ على كل نداءٍ كاتبٍ لا خاصٌّ بعمليةٍ** — ⛔ **وينتفي نهائياً أن يكون أثراً لـ`ADR-0021`.** ⚠️★★ **وأثرُه التشغيلي المباشر: عطَّل التحقّقَ الحيَّ لورقتَي `RoleDeleteSheet` و`CancelSackSheet`** — ★★ **وثالثةٌ قِيست 2026-08-28: سطرُ «قيمة الضمار» في [QtmsLiveSummary.emphasis] بـ`DistributionScreen`** (`P8` البند ⑦): ⟵ **مصدرُه `pricing.value?.debtValue` وهو يشترط تسعيراً يومياً**، ⛔ **وحفظُ التسعير هو عينُ ما يُخفقه هذا الديْن** — ★ **فالإجماليان مُتحقَّقان حيّاً والسطرُ الثالث مُتحقَّقٌ باختبارَي ودجت وحدهما** (الإيجابُ والسلب معاً)، ⛔ **ولا يُغلَق حيّاً قبل `DEBT-49`.** — ★ **إذ لا دورَ غيرَ مُسنَدٍ ولا جونيةَ اليوم**، ⟵ **وإنشاءُ أيٍّ منهما يُخفق بهذا العطل نفسِه.** ⛔ **ولم يُلاحَق كذلك.** | ⛔ **عالٍ — مسارُ كتابةٍ لا يعمل حيّاً** | ★ **قرارُ المالك — راجع «قرارات معلّقة»** ⛔⛔★★★ **وحُسم نهائياً 2026-08-28 — والسببُ الجذري ليس في المنتج إطلاقاً:** ⛔ **`QTMS_FUNCTIONS_BASE_URL` لم يُمرَّر إلى بناء الاختبار**، ⟵ **فارتدّ `CallableClient.call` من أول سطرٍ فيه قبل `http.post`** ([callable_client.dart:70](../../lib/core/callable/callable_client.dart)) — ★ **وهو بعينه ما فسّر «سجلُّ Cloud Run خالٍ تماماً»: الطلبُ لم يُرسَل.** ✅★★★ **ومُثبَتٌ تجريبياً لا استنتاجاً:** **بناءٌ واحد بالعَلَم الصحيح ⟵ `DEBT49-PROOF` أُنشئ فعلاً**، ⛅ **وسجلُّ Cloud Run:** `2026-08-28T01:45:17Z POST /createRole 200` — ★ **بعد ضغط «إنشاء» بثلاث ثوانٍ.** ✅ **والطبقاتُ الثلاث الأخرى مُبرَّأةٌ بقياسٍ مستقل:** `curl` **بلا رمز على أربع عمليات حقيقية ⟵ `401` بكتالوجنا `ERR_AUTH_003`** (⟵ **الخدمة حيّة · التوجيه بالمسار يعمل · `allUsers` سليمة فليست راية `--no-allow-unauthenticated`**)، **ومسارٌ مجهول ⟵ `404` `ERR_CALL_404`** — ⛔⛔ **و`firestore.rules` لم تُبلَغ أصلاً فلا شأن لها.** ⛔⛔★★ **وسابقٌ لكل عمل تصميمي بيقين لا بترجيح:** ★ **موثَّقٌ حرفياً في [`run3`](../00-governance/upds-workflow/implementation-report-2026-08-26-run3.md) §7 بتاريخ 2026-08-26** — **قبل اعتماد `ADR-0021` (2026-08-27) بيوم** ⟵ **فينتفي نهائياً أن يكون أثراً للشرائح.** ⚠️⚠️★★★ **وتكرّر أربع مرات لأن ضمانةَ منعِه لم تُنشأ:** ⛔ **تقرير 2026-08-27 §4 ادّعى تسجيلَه في `troubleshooting-guide.md` والملفُ لم يكن موجوداً** — ✅ **وأُنشئ الآن فعلاً** ([`troubleshooting-guide.md`](../13-operations/troubleshooting-guide.md) §1 و§4). ✅ **و`QTMS_FUNCTIONS_BASE_URL` أُضيف إلى `.secrets/qtms-staging-qa-account.env`** ⚠️ **والملفُ مُستبعَدٌ من المستودع فيعود ناقصاً على أي نسخةٍ جديدة** — ★ **منصوصٌ عليه في الدليل §1.** ✅★★★ **وسُدَّ الجذرُ نفسُه في 2026-08-28 بـ`ADR-0022`** — ⛔ **لا بالتوثيق وحده**: ★ **القيمة صارت في ملفٍّ عامٍّ مُلتزَمٍ به** [`config/qtms-public-defines.env`](../../config/qtms-public-defines.env) **يصحب `git clone`**، ⟵ **فلا نسخةَ مشروعٍ تبدأ بلا العنوان بعد اليوم.** ⛔⛔ **و`.secrets/` لم يُمَسّ بحرف** — ★ **والملفّان يُمرَّران معاً بعَلَمين والأخصُّ آخِراً.** ⛔ **ولا قيمةَ افتراضية محفورةٌ في Dart** (`ADR-0022` القاعدة 5) — ★ **فبناءُ الإنتاج بلا عَلَمٍ يُخفق ظاهراً** ⛔ **ولا ينادي التجريبيةَ صامتاً.** ⛔ **وبقي منه ديْنٌ مستقل: `DEBT-52`** — **الرسالةُ العامة التي أخفت التشخيص وكلّفت أربع جولات.** | ✅ **مُغلَق — 2026-08-28** |
| ⚠️★ `DEBT-50` | ⚠️★★ **حقلُ `totalPieces` يُعرَض خاماً في فرق سجل التدقيق** — ★ **رصده اختبارُ المحاكي لـ`P7`** (2026-08-28): **قيدُ تعديلٍ على مستندِ جوانٍ عرض** «`totalPieces`: 5 ← 85» ⛔ **بلا اسمٍ عربي**، ★ **بينما جاره `lines` عُرِض «السطور»**. ⛔⛔ **وليس عطلاً في آلية العرض:** ★ **`auditFieldLabel` تردّ المفتاح كما هو عمداً** (`_fieldLabels[field] ?? field`) — ⟵ **وهو الخيار الموثَّق صراحةً في [audit_labels.dart](../../lib/core/messages/audit_labels.dart): «إخفاءُ تغييرٍ وقع فعلاً يُسقِط مبرر `ADR-0004` كلَّه» فالمقايضة بين «ناقصٌ صامت» و«كاملٌ خشن» والسجل يختار الثاني.** ⟵ ★★ **فالعطلُ نقصُ مدخلةٍ في الخريطة لا خللٌ في المنطق** — **82 مفتاحاً فيها و`totalPieces` ليس منها.** ⚠️★★ **ودلالتُه أوسع من مفتاحه:** ⛔ **الخريطةُ تُصان يدوياً ولا حارسَ آلي يربطها بحقول المستندات فعلاً** — ⟵ **فكلُّ حقلٍ جديد يدخل مستنداً يظهر خاماً حتى يتذكّره إنسان**، ★ **وهذا وجهٌ رابع لدرس `DEBT-37`: اختبارُ الطبقة لا يُغني عن اختبار ما يعبر بينها.** ✅ **والمعالجة سطرٌ للمفتاح المرصود، والأولى بوابةٌ تُقايس مفاتيح الخريطة بحقول النماذج.** ⛔⛔ **ولم يُعالَج في شريحة `QtmsFilterBar`** — ★ **خارج نطاقها: الشريحةُ لم تمسّ `audit_labels.dart` بحرف.** | ⚠️ **منخفض — عيبُ عرضٍ لا فقدَ بيانات: القيمةُ ظاهرةٌ صحيحةً والاسمُ وحده خام** | ★ **مع أول لمسٍ لـ`audit_labels.dart`** | ⚠️ **مفتوح** |
| ⚠️★★ `DEBT-51` | ⚠️★★★ **بوابةُ «لا حبّةَ حالة مرسومة محلياً» تُمسك نصفَ الحالات** (§8 المحظور الحادي عشر) — ★ **رُصد أثناء توحيد بطاقتَي `UsersScreen` و`RolesScreen`** (2026-08-28): **الاختبار يبحث عن `BorderRadius.circular(Radii.pill)` وحده** ([design_gates_test.dart:471](../../test/core/design/design_gates_test.dart)) — ⟵ ⛔⛔ **فحبّتان محليّتان عاشتا تحت أنفه** (`_DisabledBadge` في `users_screen.dart` و`_AssignedBadge` في `roles_screen.dart`): ★ **كلتاهما `Container` بـ`Primitives.dangerSoft`/`infoSoft` وحدٍّ ونصّ** — **أي حبّةُ حالةٍ بكل معنى** — ⛔ **لكن بـ`Radii.field` لا `Radii.pill`**، ⟵ **فمرّتا.** ✅ **وقد أُزيلتا الآن واستُبدلتا بـ`StatusPill`** — ★ **والحبّتان لم تعودا موجودتين.** ⛔⛔★★ **والديْنُ ليس فيهما بل في البوابة:** ⟵ **الحارسُ يُقاس بما يمنعه لا بما مرّ عليه سالماً**، ★ **وبوابةٌ تُلتفّ حولها بتغيير نصف قطرٍ واحد ليست حارساً** — ⛔ **وأيُّ حبّةٍ محلية جديدة بـ`Radii.field` تمرّ اليوم كما مرّتا.** ⚠️ **ولم تُوسَّع فوراً عمداً:** ★ **توسيعُها إلى `Radii.field` يُنذر كاذباً على كل حقلٍ مشروع** — ⟵ **والصياغةُ الصحيحة تُقايس البنية لا الرقم** (`Container` + `*Soft` + حدّ + `Text` بلا تفاعل)، ⛔ **وهي تغييرُ بوابةٍ يستحق شريحتَه هو.** ★★ **ودرسُه درسُ `DEBT-24` نفسِه: لا يُفترَض أن البوابة تحرس — يُقاس ما تمسكه فعلاً.** | ⚠️ **متوسط — حارسٌ يوهم بالحماية أخطرُ من غيابه: يُسكِت المراجعة البشرية** | ★ **شريحةٌ مستقلة لبوابات §8** | ⚠️ **مفتوح — والحبّتان المرصودتان مُزالتان** |
| ⛔★★★ `DEBT-52` | ⛔⛔★★★ **الحارسُ ناطقٌ في الكود وصامتٌ عند المستخدم — رسالةُ `operationFailed` العامة تبتلع تشخيصاً دقيقاً موجوداً فعلاً** (2026-08-28) — ★ **رصده تشخيصُ `DEBT-49`**، ⟵ **وهو سببُ كلفته لا سببُ وقوعه.** ★★ **والسلسلة مقروءةٌ من طرفٍ إلى طرف:** `CallableClient.call` **تُرجِع** `InfrastructureError('QTMS_FUNCTIONS_BASE_URL غير مضبوط في هذا البناء')` ([callable_client.dart:70](../../lib/core/callable/callable_client.dart)) ⟵ **و`appErrorMessage` تُمرِّر نصَّه إلى `callableErrorMessage`** ([error_messages.dart:340](../../lib/core/messages/error_messages.dart)) ⟵ **وهي تُقايسه بأسماء رموز الكتالوج وحدها** (`ERR_AUTH_001`…) **فلا يطابق** ⟵ ⛔ **فيسقط في `_ => CatalogMessage.operationFailed`** ([:309](../../lib/core/messages/error_messages.dart)) ⟵ **«❌ تعذّر إتمام العملية. أعد المحاولة.»** ⛔⛔★★★ **والضرر ليس في الغموض بل في التضليل الإيجابي:** ★ **«أعد المحاولة» فعلٌ لا يمكن أن ينجح** — **العطلُ في البناء لا في اللحظة** — ⟵ **فالرسالةُ توجّه المستخدم إلى طريقٍ مسدود بثقة**، ⛔ **وهو أسوأ من رسالةٍ تقول «لا أعرف».** ⛔⛔★★ **ويُناقض تعليقَ الملف نفسِه نصّاً:** ★ **[callable_client.dart:37](../../lib/core/callable/callable_client.dart) ينصّ «بناءٌ بلا إعداد يفشل ظاهراً لا صامتاً»** — ⟵ **والقياس يقول إنه صامتٌ تماماً**: ★ **فالتعليقُ يصف نيّةً لم تتحقق**، ⛔ **ووثيقةٌ تُطمئن على ضمانةٍ غير قائمة أخطرُ من غيابها.** ⚠️★★ **ومداه أوسع من هذا المفتاح:** ★ **كلُّ `InfrastructureError` بنصٍّ تشخيصي غيرِ كتالوجي يُصيبه المصيرُ نفسُه** — ⟵ **«استجابة 200 بلا حقل result» مثلُه حرفياً** ⛔ **فلا يصل المستخدمَ ولا السجلَّ منه شيء.** ★★ **ودرسُه درسُ `DEBT-51` نفسِه في طبقةٍ أخرى: الحارسُ يُقاس بما يبلغه فعلاً لا بما كُتب فيه.** ⛔⛔ **ولم يُعالَج في جلسة `DEBT-49` عمداً** — ★ **نطاقُها تصحيحُ إعدادِ بناءٍ وتوثيق** (قرار المالك 2026-08-28)، ⟵ **وعلاجُه يمسّ طبقةَ الرسائل ويستحق شريحتَه هو.** | ⛔ **عالٍ — يُضلِّل المشخِّص لا المستخدم وحده: كلّف أربع جولاتِ تشخيص** | ✅★★★ **سُدِّد 2026-08-28 — [CatalogMessage] صار نوعاً مُغلَقاً بشكلين** ([error_messages.dart](../../lib/core/messages/error_messages.dart)): ★ **[CatalogEntry] لمداخل الكتالوج** (⛔ **ونصوصُها لم يتغيّر منها حرف**) · ★★★ **و[DiagnosticMessage] لكل رمزٍ خارجه** ⟵ **يُعرَض بنصّه: «تعذّر إتمام العملية — التفصيل التقني: `<الرمز>`»**، ⛔ **بلا «أعد المحاولة»** (فعلٌ لا يمكن أن ينجح على عطلٍ بنيوي) ⛔ **وبلا ابتلاع**: ★ **والفارغُ وحده يسقط إلى `ERR_CALL_500`**، ★ **والطويلُ يُقصّ عند 140 محرفاً بعلامة قطع.** ⛔⛔★★ **وهو انحرافٌ صريح مُوثَّق عن `error-codes-catalog.md` §3 القاعدة 1** («لا يُعرَض رمز تقني للمستخدم») — ★ **مُعلَّلٌ في رأس النوع نفسِه**، ⛔ **ولا يُطبَّق صامتاً.** ✅ **وستةُ اختبارات ارتداد** ([error_messages_test.dart](../../test/core/messages/error_messages_test.dart)) — ★ **وأحدُها يُثبت النفي صراحةً: الرمزُ المجهول ⛔ لا يساوي نصَّ `operationFailed`.** ⚠️★★ **واختبارٌ قائم انقلب عمداً** (`user_form_screen_test.dart`): ★ **كان يُثبت الرسالة العامة، وصار يُثبت ظهور `ERR_XYZ_999` وغيابَها** — ⟵ **فالسلوكُ القديم صار ارتداداً مُختبَراً لا ذكرى.** | ✅ **مُغلَق — 2026-08-28** |
| ⛔⛔★★★ `DEBT-53` | ⛔⛔★★★ **الخدمة المنشورة على التجريبية متأخّرة عن `ADR-0020` — وتفرض سبباً نصياً ألغاه القرار** (2026-08-28) — ★ **رصده أوّلُ اختبارٍ حيٍّ حقيقي لمسار كتابةٍ في تاريخ المشروع**، ⟵ **وهو ما أتاحه إغلاقُ `DEBT-49` مباشرةً.** ★★ **والقياس مزدوجٌ لا يحتمل تأويلاً:** ⛅ **«حفظ التعديل» على دورٍ قائم بوصفٍ جديد وبلا سبب ⟵ الشاشة تردّ «❌ يجب إدخال سبب التعديل قبل الحفظ.»** (`ERR_AMEND_002`)، ⛅ **وسجلُّ Cloud Run في اللحظة نفسها:** `2026-08-28T01:57:17Z POST /updateRole **400**` — ⟵ ⛔⛔ **فالرافضُ هو الخادمُ قطعاً لا العميل.** ✅★★★ **والكودُ المحلي يقبلها — مقيسٌ لا مُدَّعى:** **`dart test` في `functions/` يمرّ باختبارٍ صريح اسمُه «✅★ وتعديل الدور كذلك — القاعدة على الفعل لا على الكيان»** ضمن مجموعة «★★★ `ADR-0020` — تعديل بلا سبب يُقبَل» (`user_admin_test.dart`) — ⟵ **فالفجوةُ بين المستودع والمنشور لا في المنطق.** ⛔⛔★★ **والجذر تسلسلٌ زمني:** ★ **الإصدارُ الحيُّ `qtms-callables-00016-65v` نُشر `2026-08-27T05:22:06Z`** (`callables:wu006b` · `WU-006`) — **و`ADR-0020` اعتُمد في اليوم نفسه بعده** ⟵ **فالصورةُ المنشورة تسبق تعديلَ `functions/` الذي أنفذ القرار**، ⛔ **ولم تُعَد بعده.** ⚠️⚠️★★★ **وأثرُه أوسع من الأدوار بكثير:** ★ **`ADR-0020` قرارٌ عابرٌ للعمليات كلِّها** — **إنشاءً وتعديلاً وإلغاءً وإتلافاً وحذفاً** — ⟵ **فكلُّ مسارٍ كاتبٍ على التجريبية اليوم يفرض سبباً ألغاه المالك**: ⛔ **والتجريبيةُ تختبر سلوكاً لم يعد سلوكَ النظام.** ⛔⛔★★★ **وخطرُه الحقيقي في الاتجاه المعاكس:** ★ **أيُّ اختبارٍ حيٍّ يُجرى عليها الآن يُثبت العكسَ ويُوثَّق نجاحاً** — ⟵ **فتُغلَق زياداتٌ على شهادةِ بيئةٍ متأخّرة.** ⚠️★★ **والواجهةُ تناقض نفسَها أمام المستخدم:** ★ **الحقل معنونٌ «سبب التعديل (اختياري)» حرفياً** — ⛔ **والحفظُ يُرفَض لغيابه**: ⟵ **فيُقرأ عطلاً في التطبيق لا تأخّراً في النشر.** ⛔⛔ **ولم يُعالَج:** ★ **العلاجُ نشرُ خدمةٍ — فعلٌ خارج التفويض التلقائي** (بروتوكول التشغيل §ج · وقرارُ المالك على نطاق جلسة 2026-08-28: **تصحيحُ إعدادِ بناءٍ وتوثيقٌ فقط**) — ⟵ **يُسلَّم للمالك.** ★★ **ودرسُه جديدٌ يُضاف للسجل: `dart test` أخضرُ لا يقول شيئاً عمّا يعمل في السحابة** — ⛔ **والمنشورُ يُقاس بمعرّف إصداره وتاريخِه لا بحالة المستودع.** | ⛔⛔ **عالٍ جداً — بيئةُ التحقق تُصادق على سلوكٍ مُلغى، فتُغلَق زياداتٌ بشهادةٍ باطلة** | ★ **قرارُ المالك — إعادةُ نشر `qtms-callables` على التجريبية** | ⛔ **مفتوح — بانتظار قرار نشر** |
| ⛔⛔★★★ `DEBT-54` | ⛔⛔★★★ **`.gcloudignore` لم يستبعد `/.secrets/` — فكلمةُ مرور حساب الاختبار رُفعت إلى سلّة Cloud Build** (رُصد 2026-08-28 قبل النشر مباشرةً) — ★ **والعلّة قاعدةٌ في الأداة تُغفَل:** ⛔⛔ **وجودُ `.gcloudignore` يُلغي `.gitignore` تماماً في `gcloud builds submit`** — ⟵ **فسطرُ `/.secrets/` في `.gitignore` لا يسري على الرفع البتّة**، ★ **والملفُ لم يحمل نظيراً له.** ⚠️⚠️★★ **والتعرّضُ وقع فعلاً ولم يُمنَع — مقيسٌ بالتواريخ لا مُقدَّر:** **`.secrets/` أُنشئ `2026-08-26 07:30`** · **و`.gcloudignore` كُتب `2026-08-27 00:33`** · **وصورةُ `callables:wu006b` بُنيت `2026-08-27T05:22Z`** ⟵ ⛔ **فالبناءُ الأخير على الأقل رفع المجلَّد كاملاً**، **ويُرجَّح أن `wu004` و`wu002` كذلك.** ★★ **والوجهةُ سلّةُ مصدرِ البناء في المشروع نفسِه** (`gs://qtms-orimind-master_cloudbuild/source/`) — ⟵ **فليس تسريباً لطرفٍ ثالث**، ⛔ **لكنه خروجُ سرٍّ من حدوده المصرَّح بها إلى مخزنٍ دائمٍ غيرِ مقصود**، ★ **وهو تعرّضٌ بحكم `secrets-management-policy.md`** ⛔ **لا يُصنَّف سليماً لأن الوجهة مملوكة.** ✅★★ **والسرُّ المعنيُّ محدود:** **حساب اختبار (QA) على التجريبية وحدها** (`AM-004` · `RB-staging-qa-account`) — ⛔⛔ **ولا نظيرَ له على الإنتاج**، ★ **ولا سرَّ مالكٍ ولا مفتاحَ خدمةٍ في المجلَّد.** ✅ **والثغرة أُغلقت فوراً قبل أي رفعٍ في هذه الجلسة:** ★ **أُضيف `/.secrets/` و`**/.secrets/` و`/.buildtmp/` إلى `.gcloudignore`** — ⛅ **ومُثبَتٌ بالقياس: بناءُ `callables:adr0020` رفع 143 ملفاً بـ2.4 م.ب** ⛔ **بدل مجلَّدٍ يضمّ الأسرار و902 م.ب من مؤقتات Gradle.** ⛔⛔ **والقرارُ الباقي للمالك وحده: تدويرُ كلمة مرور حساب الاختبار** — ★ **لم أُنفِّذه** (⛔ **إدخالُ كلمات المرور ممنوعٌ على الأداة** — و`AM-004` يستثني التمريرَ غيرَ المباشر لا التدوير). ★★ **ودرسُه: كلُّ ملفِّ استبعادٍ يُقاس بما يستبعده فعلاً في أداته هو** — ⛔ **ولا يُقاس بنظيره في أداةٍ أخرى.** | ⛔⛔ **عالٍ — تعرّضُ سرٍّ فعليٌّ وقع، ومحدودٌ بحساب اختبارٍ تجريبي** | ★ **قرارُ المالك — تدويرُ كلمة مرور حساب QA** | ⚠️ **الثغرة مُغلَقة · والتدويرُ مفتوحٌ بانتظار قرار** |
| ⛔⛔★★ `DEBT-55` | ⛔⛔★★★ **`writeDailyPrices` يُرفَض بـ`400` من الخادم — والسببُ الدقيق محجوبٌ بـ`DEBT-52`** (2026-08-28) — ★ **رصده أوّلُ تشغيلٍ للتسعير بعد إغلاق `DEBT-49` و`DEBT-53` معاً**، ⟵ **فهو ثالثُ عطلٍ مستقلٍّ كان مختبئاً خلفهما.** ⛅★★ **والقياس يفصله عن سابقَيه قطعاً:** `2026-08-28T03:10:10Z POST /writeDailyPrices **400** qtms-callables-00017-tp9` — ⟵ ⛔ **فالطلبُ وصل** (⟵ **ليس `DEBT-49`**) **والإصدارُ هو الجديد** (⟵ **ليس `DEBT-53`**): ★ **رفضٌ حقيقيٌّ ثالث.** ★★ **والرمز مُستنتَجٌ من الحالة لا مقروءاً:** **`400` = `INVALID_ARGUMENT` = `ERR_CALL_400`** — ⛔⛔ **و`403` مستبعَدٌ يقيناً فـ`sourceOutOfScope` و`permissionMissing` كلاهما `httpStatus: 403`** ([callable.dart:52](../../functions/lib/src/callable.dart)) ⟵ **فليست صلاحيةً ولا نطاقاً.** ★ **والحرّاسُ الثلاثة المرشَّحة محصورةٌ في [daily_pricing_handler.dart](../../functions/lib/src/daily_pricing_handler.dart):** ① `requestId`/`sourceId` غائبان (`:98`) · ② `lines` فارغة (`:114`) · ③ `_readLines` تردّ `null` لسطرٍ مشوَّه — **`itemId` فارغ أو مبلغٌ لا يُقرَأ عدداً** (`:300`). ⚠️⚠️ **وتحديدُه بينها لم يتمّ:** ⛔ **الخادم لا يسجّل جسم الخطأ** (السجل `WARNING` بلا `textPayload`)، ★ **والعميل يطويه في الرسالة العامة** — ⟵ ⛔⛔ **فلا طرفَ يقول الرمز**: **وهذا `DEBT-52` يُكلِّف جولةَ تشخيصٍ ثانية بعينها.** ⛔ **ولم يُلاحَق أبعدَ من ذلك:** ★ **تحديدُه يحتاج تعديلَ كودٍ أو تسجيلاً إضافياً** — **وهو خارج نطاق جلسة 2026-08-28 صراحةً** (قرارُ المالك: نشرٌ وتحقّقٌ وتوثيقٌ فقط). ⚠️★★ **وأثرُه المباشر: يحجب البند ③ من إعادة التحقق** — **سطرُ «قيمة الضمار» في `P8`** ⟵ **مصدرُه `pricing.value?.debtValue` ولا تسعيرَ يُحفَظ**، ⛔ **فانتقل حجبُه من `DEBT-49` إلى هذا.** ✅★★★ **وحُسم نهائياً 2026-08-28 — والحارسُ الرابع لم يكن في القائمة أصلاً:** ⛔⛔ **الرفض من `_validate` داخل المعاملة لا من الحرّاس الثلاثة المرشَّحة** ([daily_pricing_handler.dart](../../functions/lib/src/daily_pricing_handler.dart)) — ★ **`items[line.itemId] == null` ⟵ `ValidationError('FR-M9-01')` ⟵ `AbortTransaction(invalidArgument)`** ⟵ **`400`.** ★★★ **والسببُ الجذري بنيويٌّ لا عارض: `itemKey` في الدفتر «النوعُ أو الاسمُ المركّب»** (`inventory-ledger.md` · `ADR-0007`) — ⟵ **وسطرُ الجونية يدخل المخزن بمفتاحٍ مركّب (`سلة - جونية رقم 2`)** ⛔ **لا بمعرّف سجل نوع**، ★ **والمعالِج كان يقرأ `items/{itemKey}` فيردّ غياباً.** ⛅★★ **ومقيسٌ على بيانات التجريبية نفسِها:** **مخزونُ 2026-08-28 كلُّه من جونية** (`سلة - جونية رقم 1` · `السكرب - جونية رقم 1`) ⟵ **فسقط كلُّ سطرٍ**، ⛔⛔ **بينما `FR-M9-02` ينصّ حرفياً على تسعير ما ورد «عدداً أو جواني أو كان سكرباً».** ✅ **والعلاج [withLedgerItems](../../functions/lib/src/inventory_handler.dart):** ★ **الاسمُ والوحدة من حركة الدفتر نفسِها** — ⟵ **بيانٌ كتبته السحابة في معاملةٍ سابقة** (`ADR-0008`)، ⛔ **لا من الحمولة**: **فشرطُ `FR-M9-06` «الوحدة المخزَّنة لا المُرسَلة» مصونٌ حرفياً**، ⛔ **ولا هويةَ من حركةٍ ملغاة** (`A-14`)، ⛔ **ولا يُستبدَل سجلُّ نوعٍ قائم** (⟵ **وإلا عاد نوعٌ معطَّلٌ نشطاً**). ★★ **وأُصلح المسار الثاني معه بالمقايسة: `distribution_handler`** — ⛔ **إذ لم يكن يُوزَّع سطرُ جونيةٍ إطلاقاً** (`FR-M10-13`). ✅★★★ **وإثباتٌ حيٌّ لا استنتاج:** **جونيةٌ أُنشئت وسُعِّرت على التجريبية بالمفتاحين المركّبين** ⟵ ⛅ `2026-08-28T04:47:46Z POST /writeDailyPrices **200** qtms-callables-00018-78c` — ★ **والمستندان مكتوبان فعلاً:** `SRC-001_سلة - جونية رقم 2_20260828` (`piece`) · `SRC-001_السكرب - جونية رقم 2_20260828` (**`kilogram`** ⟵ **`FR-M9-06` مُثبَتٌ بالقياس**). ★★ **ودرسُه ثالثُ وجوه `DEBT-37`:** ⛔ **`daily_pricing_test.dart` يبني خريطة الأنواع في الاختبار نفسِه** — ⟵ **فلم يرَ قطُّ كيف يبنيها المعالِج**، ★ **ولذلك اختبارُ الارتداد في طبقة المعالِج لا في التخطيط** ([ledger_item_records_test.dart](../../functions/test/ledger_item_records_test.dart)). | ⛔ **عالٍ — مسارُ كتابةٍ لا يعمل، ويحجب تحقّقاً مؤجَّلاً** | ✅ **سُدِّد — `withLedgerItems` + اختبارُ ارتدادٍ في طبقة المعالِج** | ✅ **مُغلَق — 2026-08-28** |
| ⚠️★★ `DEBT-56` | ⚠️★★★ **الإقلاعُ البارد يعرض «لا يوجد مصدر ضمن نطاقك. راجع المدير.» ثم يعمل عند الدخول الثاني** (2026-08-28) — ★ **مقيسٌ على `DailyPricingScreen`:** **أوّلُ دخولٍ بعد إقلاعٍ بارد ⟵ حالةُ منعٍ كاملة** (أيقونةُ قفل · «نطاق مصادرك لا يشمل أي مصدر — راجع المدير لمنحك نطاقاً»)، ⟵ ★ **وخروجٌ وعودةٌ فوراً بلا أي تغيير ⟵ الشاشة تعمل كاملةً** والمصدر حاضر. ⛔⛔★★ **والعطل تصنيفيٌّ لا بصري:** ★ **حالةُ «لم يصل بعد» تُعرَض حالةَ «مُنِعت»** — ⟵ **والفرق بينهما هو الفرق بين انتظارٍ وقرار.** ⛔⛔ **والنصُّ قابلٌ للتصرف وخاطئ معاً:** «راجع المدير» ⟵ **يدفع المستخدم إلى فعلٍ لا لزوم له**، ★ **ويدفع المديرَ إلى منح نطاقٍ ممنوحٍ أصلاً** — ⛔ **فقد يُوسِّع نطاقاً لعلّةٍ غير قائمة**: ⟵ ★★ **وهو أذى تصعيدِ امتيازٍ بحسن نيّة.** ★★ **ودرسُه درسُ `DEBT-52` في طبقةٍ أخرى: رسالةٌ واثقةٌ عن حالةٍ لم تُحسَم أسوأُ من مؤشّر انتظار.** ⚠️ **والمدى غيرُ مقيس:** ⛔ **لم يُفحَص هل يعمّ كلَّ شاشةٍ تقرأ النطاق أم يخصّ التسعير** — ★ **ويُرجَّح العموم لأن المصدر واحد.** ⛔ **ولم يُعالَج — خارج نطاق الجلسة.** | ⚠️ **متوسط — يُوهم بمنعٍ غير قائم ويستدعي إجراءً إدارياً خاطئاً** | ★ **شريحةُ حالات الانتظار في قراءة النطاق** | ⛔ **مفتوح** |
| ⛔⛔★★★ `DEBT-57` | ⛔⛔★★★ **نداءُ العمليات بلا مهلة — وانقطاعُ الشبكة يُجمِّد الإجراء أبداً بلا رسالة** (2026-08-28) — ★ **رصده محاولةُ التحقق من شقّ `EntityTile.rejection`**، ⟵ **فكشفت عطلاً أخطرَ ممّا قصدتُ قياسه.** ⛅★★ **والقياس الحيّ:** **قطعُ الواي‑فاي والبيانات ⟵ «حذف الدور» ⟵ الأزرارُ تُعطَّل** (`_busy = true`) **وتبقى كذلك** — ⛔ **بلا مؤشّر انتظار وبلا رسالةٍ وبلا مهلة**: ★★ **قِيست أربع دقائق ثم أُعيدت الشبكة، فلم يستأنف النداءُ ولم يُخفق** — ⟵ ⛔⛔ **عالقٌ حتى يُعاد بناءُ الشاشة.** ✅★★★ **والجذر مقيسٌ في الكود لا مُستنتَج:** ⛔ **لا `timeout` ولا `Duration` في [callable_client.dart](../../lib/core/callable/callable_client.dart) البتّة** (مقيسٌ نصّياً) — ★ **و`http.post` بلا مهلةٍ ينتظر مهلةَ نظامِ التشغيل** ⟵ **وهي دقائقُ أو لا تنتهي.** ⛔⛔★★ **والحارسُ الموجود يُخدَع:** ★ **`CallableClient` يلتقط الاستثناء ويردّ `ConnectivityError`** (`ERR_CONN_001`) — ⟵ **وهو صحيحٌ تماماً لكنه لا يُنادى أبداً لأن النداء لا يرمي**، ⛔ **فالمعالجةُ المكتوبة لا تُبلَغ.** ⚠️⚠️★★★ **والأثر أخطرُ من الانتظار — على المدمّرات خاصة:** ★ **المستخدم لا يعلم أوقعَ الحذفُ أم لا** ⟵ **فيُعيد المحاولة على مستندٍ قد يكون حُذف**، ⛔ **و`ADR-0003` يمنع التخزينَ المؤقت لئلا يُوهَم بحفظٍ لم يقع** — ★★ **وهذا يُوهم بالعكس: بعملٍ جارٍ وهو ميت.** ⛔⛔ **ويحجب `EntityTile.rejection` عن التحقق الحيّ:** ⟵ **فالشقُّ يُملأ من مسار الإخفاق، والمسارُ لا يكتمل.** ★ **ودرسُه: الحارسُ يُقاس بأنه يُنادى فعلاً** — ⛔ **لا بأنه مكتوب** (وهو `DEBT-52` من زاويةٍ ثالثة). ⛔ **ولم يُعالَج — خارج نطاق الجلسة.** | ⛔⛔ **عالٍ — يُجمِّد إجراءً مدمّراً بلا خبر، ويُبطِل معالجةَ انقطاعٍ مكتوبة** | ✅★★★ **سُدِّد 2026-08-28 — `callableTimeout = 20s` و`.timeout(_timeout)` على كل نداء** ([callable_client.dart](../../lib/core/callable/callable_client.dart)): ★ **و[TimeoutException] تدخل مصيدةَ `on Object` القائمة** ⟵ ⛔⛔ **فـ`ConnectivityError` صارت تُنادى فعلاً بعد أن كانت مكتوبةً لا تُبلَغ.** ⚠️ **و20 ثانية لا أقل عمداً:** ★ **إقلاعُ حاويةٍ باردة على Cloud Run يبلغ عشرة ثوانٍ** — ⛔ **ومهلةٌ أقصر تُخفق نداءً سليماً.** ⛅★★★ **وإثباتٌ حيٌّ بحجب صامت لا بقطعٍ رامٍ** — ★ **`iptables -I OUTPUT -p tcp --dport 443 -j DROP` على `Pixel_6_API_36`** (⟵ **الشبكةُ قائمةٌ والحزمُ تُبتلَع**، ⛔ **لا وضعُ طيرانٍ يهدم المقبس فيرمي فوراً**): **«حفظ أسعار اليوم» ⟵ مشغولٌ عند t=19s ⟵ و`ERR_CONN_001` والأزرارُ حيّةٌ عند t=22s** — ★ **والحدُّ العشرونيّ مقيسٌ بين الإطارين.** ✅★★ **وثلاثةُ اختبارات ارتداد بزمنٍ مُزيَّف** (`fake_async`) — ⛔ **بلا انتظارٍ حقيقي في البوابة.** ✅★★★ **وأزال حجبَه عن البند ④ فاكتمل:** **`EntityTile.rejection` مُتحقَّقٌ حيّاً** — ★ **الرسالةُ داخل بطاقة `DEBT57-PROBE` نفسِها** ⛔ **لا لافتةً على مستوى الشاشة.** | ✅ **مُغلَق — 2026-08-28** |
| ✅★★ `DEBT-58` | ⛔⛔★★★ **رفضُ بطاقةٍ يُهاجر إلى بطاقةٍ أخرى حين تختفي صاحبتُه — حالةُ الصفّ تُعاد استعمالها بلا مفتاح** (2026-08-28) — ★ **رصده إثباتُ `DEBT-57` الحيّ**، ⟵ **فهو عطلٌ رابعٌ كان مختبئاً خلف التعليق الأبدي.** ⛅★★ **والقياس الحيّ خطوتان:** ① **حذفُ `DEBT57-PROBE` أُخفق بانقطاعٍ صامت ⟵ الرسالةُ ظهرت في بطاقته هو** (صحيح) · ② **ثم أُعيدت الشبكة وحُذف فعلاً ⟵ ⛔⛔ والرسالةُ انتقلت إلى بطاقة «دور مُسنَد» الباقية** — ★ **وهي بريئةٌ تماماً ولم يُحاوَل حذفُها قطّ.** ✅★★ **والجذر مقيسٌ في الكود:** ⛔ **`_RoleTile` بلا `key` في `itemBuilder`** ([roles_screen.dart:129](../../lib/capabilities/identity_access/presentation/roles_screen.dart)) — ⟵ **فحين يقصر السرد يُعيد Flutter استعمال `State` نفسِه لعنصرٍ آخر**، ★ **و`_rejection` و`_busy` يسافران معه.** ⛔⛔★★ **والضرر ليس تجميلياً:** ★ **رسالةُ رفضٍ على بطاقةٍ لم تُمَسّ تُفهَم إخفاقاً وقع عليها** — ⟵ **فيُعيد المديرُ محاولةَ عملٍ لم يُخفق**، ★ **وهو درسُ `DEBT-56` بعينه: رسالةٌ واثقةٌ عن حالةٍ غير قائمة.** ⚠️ **ومداه غيرُ مقيس:** ⛔ **لم يُفحَص كلُّ سردٍ يحمل حالةَ رفضٍ في صفّه** — ★ **ويُرجَّح العموم لأن النمط واحد** (`sack_intake_screen` كذلك). ~~⛔ **ولم يُعالَج — خارج نطاق جلسة `DEBT-52` و`DEBT-57` صراحةً.**~~ ✅★★★ **سُدِّد 2026-08-28.** ★ **العلاج مفتاحٌ ثابت:** `key: ValueKey<String>(roles[index].roleId)` **على `_RoleTile` في `itemBuilder`** ([roles_screen.dart:140](../../lib/capabilities/identity_access/presentation/roles_screen.dart)) — ⛔ **لا تصفيرُ الرسالة عند إعادة البناء.** ✅★★ **ومداه مقيسٌ الآن لا مُرجَّح — والترجيحُ السابق كان خاطئاً:** ★ **جُرِد كل `itemBuilder` في `lib/`** ⟵ **و`_RoleTile` هو الصفُّ المُحافِظُ على حالةٍ الوحيد**: ⛔ **و`_SackTile` و`_UserTile` و`_IntakeTile` و`_DealerTile` و`_ItemTile` و`_SourceTile` و`_SupplierTile` كلُّها `ConsumerWidget` و`_PricingTile` `StatelessWidget`** — ⟵ **فلا `State` يُورَّث فيها أصلاً**، ★ **و`_PermissionsEditor` (`_rejection` كذلك) مُفردٌ بمسارٍ لا صفٌّ في سرد.** ✅★★ **واختبارُ ارتدادٍ يُعيد العطل عمداً:** ★ **بحذف المفتاح وحده يسقط الاختبار عند سطر «⛔ ولا أثرَ لرفضٍ على البطاقة البريئة»**، ⟵ **فهو يقيس العطل لا يصفه** ([roles_screen_test.dart](../../test/capabilities/identity_access/presentation/roles_screen_test.dart)). ⛅★★★ **وإثباتٌ حيٌّ بسيناريو الاكتشاف نفسِه على `Pixel_6_API_36`:** ① **`DEBT58-KEY-PROBE` أُنشئ** ⟵ `2026-08-28T20:00:41Z POST /createRole 200` · ② **وأُخفق حذفُه تحت حجب الشبكة ⟵ الرسالة في بطاقته هو** («لا يوجد اتصال بالإنترنت…») ⛔ **ولا طلبَ في السجل** (⟵ **حارسُ `DEBT-57` عمل**) · ③ **ثم أُعيدت الشبكة وحُذف فعلاً** ⟵ `2026-08-28T20:06:40Z POST /deleteRole 200` — ✅★★★ **وبطاقةُ «دور مُسنَد» الباقيةُ نظيفةٌ تماماً بلا أثرِ رفضٍ واحد.** | ⛔ **متوسط‑عالٍ — يَنسب إخفاقاً إلى سجلٍّ بريء** | ✅ **`ValueKey(roleId)` + اختبارُ ارتداد + إثباتٌ حيّ** | ✅ **مُغلَق — 2026-08-28** |
| ⛔★★ `DEBT-47` | ⛔★★ **الحاسبة الحيّة تختفي عند رفض الأوزان بدل أن تُظهر سببه** — [`sack_intake_screen.dart:466`](../../lib/capabilities/inventory/presentation/sack_intake_screen.dart): **`Failure<ValidatedSackWeights>() => const SizedBox.shrink()`**. ★ **و`validateSackWeights` تُستدعى مع كل ضغطة مفتاح** (`:312`) **ونتيجتها تُغذّي `_ClaimableSummary` وحده** (`:370`) — ⟵ **فمن يُدخل وزن ثلجٍ أكبر من الكلي يرى الملخّصَ يتبخّر:** ⛔ **لا رقم، ولا سبب، ولا لافتة.** ⚠️⚠️ **و`_rejection` لا يُملأ إلا عند الحفظ**، ★ **فلا شيء يسدّ الفراغ أثناء الكتابة** ⟵ **والمستخدم يقرأ الاختفاء عطلاً في الشاشة لا رفضاً لمُدخَله.** ⛔⛔★★★ **والأخطر أن التعليق فوق السطر نفسه يصف السلوك المعاكس حرفياً:** «⚠️ **والرفض يُعرَض حيّاً لا عند الحفظ وحده** — ⟵ فيُصحِّح المستخدم الرقم وهو ينظر إليه (`FR-M7-29`)» — ★ **فالنيّة موثَّقة والتنفيذ عكسُها**، ⟵ ⛔ **وقارئُ الملف يُطمئنّه التعليقُ فلا يفحص.** ✅ **والمعالجة: يعرض الملخّصُ رسالةَ الرفض من `sackRejectionMessage` مكانَ القيمة** ⛔ **ولا يختفي إطلاقاً** — ★ **وهي قاعدة `P5` البند ⑦ في `MASTER.md` §5b** (`ADR-0021`)، ⟵ **فيُسدَّد الدَّينُ بتطبيق النمط لا بترقيعٍ موضعي.** ⚠️ **ورُصد بجرد الشاشات (2026-08-27)** ⛔ **لا باختبارٍ آلي** — ★ **ولا اختبارَ ويدجت واحد يغطي مسار الأوزان غير المنطقية حيّاً**، ⟵ **ويُسنَد العلاجُ باختبارٍ يُثبت ظهورَ السبب لا اختفاءَ الملخّص** | ⛔ **متوسط — حالةُ رفضٍ صامتة على نموذجٍ مالي** | ★ **مع `P5`** (`ADR-0021`) **أو أول تعديل لاحق على `sack_intake_screen.dart`** | ⚠️ **مفتوح** |

| ~~`DEBT-15`~~ | ~~**`me-central2` مغلقة لكل ما عدا قاعدة البيانات**~~ — ✅ ★ **سُدِّد (2026-08-22):** نُقلت قاعدتا البيئتين إلى **`me-central1` (الدوحة)** **وهما فارغتان**، وأُعيد نشر القواعد و**33 فهرساً** على كلٍّ منهما **بتحقق بعدي مطابق للملف المحلي**. ★ **وأُنشئ مشغّل Eventarc فعلاً وسلَّم حدثاً حقيقياً** — وهو ما كان مستحيلاً قبل النقل. `ADR-0014` | ~~حاجز بنيوي~~ | ~~قبل أي بيانات حقيقية~~ | ✅ **مُسدَّد** |
| ★ `DEBT-18` | ★ **مبالغ سطور المستندات (`lines[]`) خارج فحص قواعد الحماية** — `ADR-0015` يفرض أن **كل مبلغ عدد صحيح**، ✅ **والقاعدة تفرضه فعلاً على كل الحقول المالية العليا** (`money()` · `daily_prices` · `sacks/finance` · `distributions/pricing` · `cash_sales` · `receipts` · `discounts` · `outflows` — **مُختبَرة بالطفرة**). ⚠️ **لكن لغة القواعد لا تمرّ على عناصر مصفوفة**، فمبالغ السطور (`unitPrice` · `amount` · `lineTotals[]`) **لا تُفحَص هناك**. ★ **والتغطية المطلوبة: فحصها في الدالة السحابية الكاتبة** (`ADR-0013`) — ⛔ **لا في التطبيق** (`RISK-02`). ★★ **وتحديث `WU-026` (2026-08-24): لم تعد السطور وحدها** — بإغلاق الكتابة صارت `money()` **غير مُستدعاة إطلاقاً**، ⟵ ⛔ **فلا مبلغ واحد يُفحَص في القواعد الآن**، وكل ما كان يحرسه `ADR-0015` انتقل إلى `DEBT-21` | ⚠️ **تغطية إنفاذ ناقصة — ★ واتّسع مداه بـ`WU-026`** | ★ **مع أول دالة كاتبة** | ★★ **حرجة** |
| ★ `DEBT-16` | ★ **فكّ حمولة protobuf لأحداث قاعدة البيانات** — رُصد بالتشغيل: Eventarc **يرفض `application/json` لأحداث القاعدة** ولا يقبل إلا `application/protobuf`، والتنفيذ الحالي يقرأ JSON. **يلزم توليد أنواع `DocumentEventData`.** ⛔ **ولا ينطبق على العمليات المستدعاة** (§3.1) فهي HTTP بحمولة JSON. ★★ **وضُيِّق مداه بدقة (2026-08-24) بعد فحصٍ فعلي لا تقدير:** ⛔ **لا يحجب كل مشغَّل بل المشغَّلات المحتاجة لـ«قيم الحقول» وحدها.** ★ **ومسار المستند ونوع التغيير يصلان في `subject` و`type` خارج الحمولة**، فتلتقطهما `FirestoreDocumentEvent.tryParseAttributesOnly` ⛔ **بلا فكّ حقل واحد**. ⟵ ✅ **فـ`provisionAccountsOnSourceAdd` و`provisionAccountsOnPartyAdd` غير محجوبتين به إطلاقاً** — **وقد بُنيت طبقتهما الخالصة فعلاً**. ⚠️ **ويبقى حاجزاً حقيقياً** لـ`writeAuditEntry` وكل مشغَّل يحتاج **قيمة حقل** (`recalculate*` · `syncPendingEntries` · `recomputeSackRevenue`). ✅★★★ **وضُيِّق مداه ثانيةً (2026-09-03 · `WU-015`): `recomputeSackRevenue` لم يعد محجوباً به** — ⛔ **ولم يُفَكّ الحاجز بل دِيرَ حولَه:** ★ **العمليةُ تعمل داخل الحاوية بعد التزام العملية الأصلية** ([`sack_valuation_handler.dart`](../../functions/lib/src/sack_valuation_handler.dart)) ⛔ **لا بمشغّل Eventarc** — ⟵ **وهو بالضبط ما فُعل بـ`writeAuditEntry` و`recalculate*` و`syncPendingEntries` قبلها**: ★ **فالحاجزُ لم يُوقف مشغَّلاً واحداً عملياً حتى اليوم**، ⛔ **ويبقى قائماً لأي مشغَّلٍ يلزمه أن يعمل *خارج* مسار الكتابة** (مثال: إعادةُ بناءٍ دورية) | تنفيذ ناقص — ★ **ومداه أضيق مما كان مسجَّلاً** | **مع أول مشغّل يحتاج قيم الحقول** | عالية |
| ⚠️★★ `DEBT-60` | ⚠️★★ **مُشغِّلُ اختبارات قواعد الحماية يبتر `PATH` على Windows** (2026-08-29 · `AM-007`) — ★ **مقيسٌ لا مُخمَّن:** [`run-rules-tests.mjs`](../../test/security-rules/run-rules-tests.mjs) يبني محيطَ العملية الوليدة بـ`const env = { ...process.env }` **ثم `env.PATH = javaBin + (env.PATH ?? '')`** — ⛔ **والنسخُ بالبسط يُفقد لا-حساسيةَ حالة الأحرف التي يوفّرها `process.env` على Windows**، ⟵ **فالمفتاح القائم `Path` يبقى بقيمته والمفتاح الجديد `PATH` يصير مجلَّدَ Java وحده.** ⟹ ⛔ **فلا يُعثر على `firebase.cmd`** ويفشل الشقُّ الثاني بـ`'firebase' is not recognized` **بعد نجاح بوابة التغطية.** ⚠️ **ولا يظهر إلا متى اكتُشف `JAVA_HOME`** — ★ **ولذلك كان مستوراً في التشغيلات السابقة.** ✅ **والعلاجُ سطرٌ واحد:** الكتابة على **مفتاح `PATH` القائم فعلاً** (`env.Path` أو مفتاحٌ محسوبٌ لا حرفي). ⛔ **ولم يُصلَح في `AM-007` لأنه خارج نطاق تعديلٍ عرضيٍّ محض** — ★ **والاختباراتُ الـ236 شُغِّلت بالبديل المقيس** (`firebase emulators:exec --only firestore --project demo-qtms` بنفس المحيط) ⟵ **فالبوابةُ لم تُتخطَّ** | ⚠️ **متوسط — بيئةُ تطوير لا إنتاج** | ★ **عند أول تشغيلٍ يمسّ `test/security-rules/`** | ⚠️ **مفتوح** |
| ✅★★★ `DEBT-61` | ⛔⛔★★★ **«المتبقي» في شاشة المقبوضات يبقى على قيمته قبل القبض — والشاشةُ شاشةُ تحصيلٍ نقدي** (2026-08-29 · `WU-007` · **كشفه اختبارُ المحاكي `Pixel_6_API_36` وحده**). ⛅★★ **مقيسٌ حيّاً لا مُخمَّن:** ★ **سندُ `RCP-20260829-0002` بـ3000 على ضمارٍ متبقيه 5000 نجح فعلاً** — **`remaining` صار 2000 و`settledAmount` 8000 في `distributions/MQT-0001_SRC-001_20260829/pricing/current`** ⛔ **والشاشةُ ظلّت تعرض «المتبقي: 5000» ولافتةَ «5000 ريال (1 ضمار مفتوح)» — حتى بعد الخروج من الشاشة والرجوع إليها**، ★ **ولم تصحّ إلا بإقلاعٍ جديد للتطبيق.** ⛔⛔★★★ **والجذرُ حدٌّ بين طبقتين:** [`openDebtLotsProvider`](../../lib/capabilities/sales_receivables/application/receipt_providers.dart) **`StreamProvider` يبثّ مستنداتِ `distributions` الأب**، **والمتبقي يُقرأ قراءةً مفردةً `await _remainingOf(doc.id)` من `pricing/current`** ([firestore_receipt_directory.dart:90](../../lib/capabilities/sales_receivables/infrastructure/firestore_receipt_directory.dart)) — ⟵ **وسندُ القبض لا يمسّ الأب إطلاقاً** (`IQ-027` · `ADR-0011` **نقلا المبالغ الثلاثة إلى المستند الفرعي**): ⛔ **فلا حدثَ بثٍّ يقع، ولا يُعاد قراءةُ المتبقي.** ⛔⛔★★ **والضررُ ماليٌّ لا تجميلي:** ★ **يُقرأ ديناً قائماً بقيمته القديمة** ⟵ **فيُقبَض مرتين.** ⛔⛔★★★ **ولم يكشفه أيٌّ من 1507 اختباراً آلياً** — ★ **لأن بديلَ الاختبار يبثّ ما يُملى عليه فيُخفي الحدَّ نفسَه**: ⟹ **وهو رابعُ وجوه درس `DEBT-37`** (`DEBT-40` · `DEBT-41` قبلَه) — **اختبارُ الطبقة لا يُغني عن اختبار ما يعبر بينها.** ✅★★★ **سُدِّد 2026-08-29:** `_refreshDerivedReads()` **تُبطِل `openDebtLotsProvider` و`availableSurplusProvider` بعد كتابةٍ ناجحة وحدها** ([receipt_screen.dart](../../lib/capabilities/sales_receivables/presentation/receipt_screen.dart)) — ⛔ **ولا تُبطِل بعد فشل** (⟵ **قراءةٌ بلا موجب**). ✅★★ **واختبارا ارتدادٍ يقيسان العطل لا يصفانه** ([receipt_screen_test.dart](../../test/capabilities/sales_receivables/presentation/receipt_screen_test.dart)) — ★ **مُثبَتان بطفرة: بحذف سطر النداء وحده يسقط الاختبار** (`+14 -1`). ⛅★★★ **وإثباتٌ حيٌّ بعد الإصلاح على المحاكي:** **`RCP-20260829-0003` بـ800 ⟵ الشاشةُ هبطت فوراً إلى «المتبقي: 1200» بلا إقلاع**، **و`pricing/current` = `settledAmount 8800 · remaining 1200`** — ⛔ **ولا سطرَ خطأ في `adb logcat`.** ⚠️★★ **وحدٌّ معلَنٌ باقٍ:** ★ **هذا علاجُ الكاتب لنفسه** ⛔ **لا للكتابة الآتية من جهازٍ آخر** — ⟵ **تلك تبقى بلا بثٍّ حتى إعادة الدخول**، ★ **والعلاجُ الكامل بثُّ `pricing/current` نفسِه** ⚠️ **بكلفة استماعٍ لكل ضمار** (`owner-ledger-summary-design.md` §8) | ⛔ **عالٍ — تحصيلٌ نقدي مزدوج** | ✅ **إبطالٌ بعد الكتابة + اختبارا ارتداد + إثباتٌ حيّ** | ✅ **مُغلَق — 2026-08-29** |
| ✅★★★ `DEBT-62` | ⛔⛔★★★ **شاشةُ التسعير اليومي تُرسَم فارغةً عند الدخول إليها مرةً ثانية في نفس تشغيل التطبيق** (2026-08-29 · `WU-005`). ⛅★★ **مقيسٌ ومُكرَّرٌ على `Pixel_6_API_36`:** ★ **الدخولُ الأول يعمل كاملاً** · ⛔ **والثاني شريطُ تطبيقٍ وحده وجسمٌ فارغ** — **بلا رأسِ سياقٍ ولا هيكلٍ عظمي ولا حالةٍ فارغة ولا رسالةِ خطأ** ⟵ **ويبقى كذلك دقائق**، ✅ **ولا يصحّ إلا بإقلاعٍ جديد.** ⛔ **والتسعيرُ بوابةُ التوزيع كلِّه** (`FR-M9`) ⟵ **فالأثرُ توقّفُ العمليات اليومية.**

⛔⛔★★★ **وجذرُه لم يُحدَّد بعد — ويُقال صراحةً** (بروتوكول §و): ★ **وما يلي مقيسٌ بأدواتِ تشخيصٍ حقيقيةٍ على الجهاز، ⛔ لا مُستنتَجٌ بالقراءة.**

| # | ما قِيس في الدخول الثاني | الدلالة |
|:-:|---|---|
| 1 | ★ **`build` يعمل ببياناتٍ صحيحة** — `sources=2 · sourceId=SRC-001 · rows.hasValue=true · n=1` | ⛔ **فليست مشكلةَ مزوّدٍ ولا حالةٍ ولا بيانات** |
| 2 | ★ **`initState` يعمل** بعد `dispose` سليم | ⛔ **ولا حالةٌ بائتةٌ تُعاد** |
| 3 | ★ **`LayoutBuilder` يتلقّى قيوداً كاملة** `0≤w≤411.4 · 0≤h≤809.5` | ⛔ **فليس انهياراً في التخطيط ولا ارتفاعاً صفرياً** |
| 4 | ★ **`addPostFrameCallback` ينطلق** | ★ **فالإطاراتُ تكتمل فعلاً** |
| 5 | ⛔⛔ **`CustomPainter.paint` للجسم لا ينطلق إطلاقاً** | ⟹ **الجسمُ لا يُرسَم البتّة** |
| 6 | ⛔ **ولا يستجيب للمس** (نقرةٌ على موضع منتقي المصدر لا تفعل شيئاً) | ⟹ **وخارجَ فحصِ اللمس كذلك** |
| 7 | ✅★★ **بينما `paint` لشريط التطبيق في نفس `Scaffold` ينطلق** | ⟹ **فالعطلُ في الجسم وحده لا في الصفحة كلِّها** |
| 8 | ⛔⛔★★★ **وكلُّ أسلافِ الجسم عالقون على `debugNeedsPaint=true`** (وفي الدخول الأول `false`) | ⟹ **طلبُ الرسم لا يُصرَّف أبداً** |
| 9 | ★ **والمسار هو الحالي والأعلى** — `isCurrent=true · isActive=true · /home/pricing` | ⛔ **فليس حجباً بمسارٍ فوقه** |
| 10 | ★ **وسلسلةُ الأسلاف إطاريةٌ محضة** (`Scaffold` · `InkFeatures` · `PhysicalModel` · `RepaintBoundary` · `IgnorePointer` · `FractionalTranslation`) | ⛔ **ولا `Offstage` ولا `Opacity` من كودنا فيها** |
| 11 | ⛔ **ولا استثناءَ Dart ولا استثناءَ إطارٍ في `adb logcat`** | ⛔ **فلا `ErrorWidget` مُبتلَع** |
| 12 | ⛔ **ولا علاقةَ لـImpeller** — ★ **يتكرّر حرفياً بعد تعطيله** (`EnableImpeller=false`) | ⛔ **فليس خلفيةَ تصيير** |
| 14 | ✅★★★ **والمُسبِّبُ عُزل بالقطع (2026-08-29 · التشغيل 41):** ⛔ **`_CopyYesterdayButton`** — ★ **أربعُ قطعاتٍ على الجهاز:** ① **حذفُ `_BulkBar`** ⟹ ✅ **تُرسَم** · ② **إبقاءُ الشريط بـ`action: null`** ⟹ ✅ **تُرسَم** · ③ **استبدالُ `_PricingForm` بنصٍّ** ⟹ ✅ **تُرسَم** · ④ **وإعادةُ الزرِّ** ⟹ ⛔ **تُظلم** | ⟹ ★★ **والمُشترَكُ اشتراكٌ ثانٍ على `dailyPricesProvider`** (**أسعارُ أمس** — مفتاحٌ مغايرٌ لمفتاح اليوم) |
| 15 | ⛔⛔★★★ **وفرضيةُ «موضعُ القراءة» جُرِّبت وفشلت — تُسجَّل ولا تُطوى:** ★ **رُفعت القراءة من داخل باني [InventoryAsyncView] إلى `_PricingFormState` وصار الزرُّ `StatelessWidget` يتلقّى بياناتَه** ⟹ ⛔ **والشاشةُ ظلّت تُظلم** | ⟹ ★★ **فالمُشكِل ليس تعشيشَ القراءة بل الاشتراكَ الثاني نفسَه** — ⛔ **وأُعيد الكود إلى حاله** (⟵ **إعادةُ هيكلةٍ بلا فائدةٍ مُثبَتة لا تبقى**) |
| 16 | ⚠️ **وشاهدٌ مرافق غيرُ مفسَّر:** ★ **الزرُّ لا يظهر حتى في الدخول الأول** — **رغم أن مزوّدَه يردّ `n=3`** | ⟹ ★ **فالميزةُ `FR-M9-13` معطَّلةٌ عملياً أصلاً** ⛔ **لا في الدخول الثاني وحده** |
| 13 | ⛔ **ولا يتكرّر في اختبار ودجات** — **لا بتبديلٍ مباشر ولا بملاحة `go_router` متداخلة مطابقة** | ★ **لأن اختبار الودجات لا يُشغّل المُركِّب الحقيقي** ⟹ **فالحارسُ الآلي عاجزٌ عنه بنيوياً** |

⛔⛔★★ **ولم يُكتب له إصلاح — عمداً:** ★ **كلُّ إصلاحٍ الآن تخمينٌ** (`implementation-playbook.md` §5: **لا تخترع**) — ⟵ **وإصلاحٌ بلا آليةٍ مفهومة يُخفي العَرَض ويُبقي العلّة**، ⛔ **وهو أسوأ من تركه ظاهراً.**
★★ **والخطوة التالية المقترحة — بعد أن ضاق النطاق إلى سببٍ واحد:** ① **فحصُ اشتراكَي `dailyPricesProvider` معاً** (**اليوم وأمس**) ⟵ **هل يُتخلَّص من اشتراك أمس عند الخروج؟** ★ **وتجريبُ `autoDispose` عليه، أو قراءةٌ لمرة واحدة (`Future`) بدل بثٍّ دائم** — ⟵ **فأسعارُ أمس ثابتةٌ لا تتغيّر** ⛔ **فبثُّها الدائم بلا موجب أصلاً** · ② **وتشغيلُ الشاشة تحت `flutter run` مع DevTools ومعاينةُ شجرة الطبقات** ⟵ **لتحديد الآلية لا المُسبِّب** · ③ **وقياسُه على جهازٍ حقيقي لا محاكٍ.** ⛔ **وسقط من الترشيح:** **`QtmsContextHeader`** (⟵ **`مخزون اليوم` يستعمله ويعمل**) · **وصفُّ المرشِّحات** (⟵ **`filters: null` والعطلُ باقٍ**).
✅★★ **ومداه صار مقيساً لا مُرجَّحاً** (2026-08-29): ★ **`مخزون اليوم` يُعاد الدخول إليه فيعمل كاملاً** ⟵ **وهو يستعمل `QtmsContextHeader` نفسَه** ⛔ **فالرأسُ بريء.** ★★ **والعطلُ محصورٌ في `التسعير اليومي` وحدها** — ⟵ **وهي الشاشةُ الوحيدةُ التي تشترك في `dailyPricesProvider` مرتين.** ⚠️ **وبقي غيرَ مفحوصٍ:** `counted_intake` · `sack_intake` · `distribution` — ⛔ **ولا يُدَّعى لها سلامةٌ ولا عطل** ✅★★★ **سُدِّد 2026-08-29 (التشغيل 42) — والعلاجُ سطرٌ واحد:** `isAutoDispose: true` **على** [`dailyPricesProvider`](../../lib/capabilities/inventory/application/inventory_providers.dart) — ⟵ **فيسقط اشتراكُ أمس عند مغادرة الشاشة ويُعاد نظيفاً عند العودة.** ★ **ومقيسٌ أن أثرَه محصورٌ في مفتاح أمس:** ⟵ **مفتاحُ اليوم يبقى حيّاً لأن `pricingRowsProvider` و`suggestedDistributionPricesProvider` دائمان يراقبانه.**

⛅★★★ **والتحقّقُ حيٌّ على `Pixel_6_API_36` ببناءٍ نظيف وبنفس تسلسل الأساس:**

| # | المعيار | النتيجة المقيسة |
|:-:|---|:-:|
| 1 | **الدخولُ الثاني يُرسَم** | ✅ **نعم** — ★ **والثالثُ كذلك** |
| 2 | **أسعارُ أمس صحيحةٌ ومتاحةٌ لفعل النسخ** | ✅ **`n=3` بالمفاتيح الثلاثة نفسِها بعد التخلّص وإعادة الاشتراك** (`loading=true ⟶ n=3`) ⛔ **ولا بياناتٍ بائتة** |
| 3 | **لا انحدارَ في `مخزون اليوم`** | ✅ **يُعاد الدخول إليه فيعمل كاملاً** |
| 4 | **ولا انحدارَ في بقية المستهلكين** | ✅★★ **شاشةُ التوزيع تحسب «قيمة الضمار: 10000 ريال» لخمسِ حبات** — ⟵ **5 × 2000 من `dailyPricesProvider` حيّاً** ⛔ **لا رقمٌ مخزَّن** |
| 5 | **البوابة الآلية** | ✅ **1509 · ٠ فاشلاً** · `analyze` نظيف في الحزم الثلاث |
| 6 | **سجلُّ الجهاز** | ✅ **نظيف** — ⛔ **لا `E/flutter` ولا رفضٌ ولا طلبُ فهرس** |

✅★★ **وله حارسُ انحدارٍ مُثبَتٌ بطفرة** ([`daily_pricing_screen_test.dart`](../../test/capabilities/inventory/presentation/daily_pricing_screen_test.dart)) — ⚠️★★ **وهو حارسُ الشرط لا حارسُ العَرَض، ويُقال بصدق:** ★ **تعذُّرُ الرسم لا يتكرّر في اختبار ودجاتٍ إطلاقاً** (**جُرِّب بتبديلٍ مباشر وبملاحة `go_router` متداخلةٍ مطابقة**) ⟵ **فالحارسُ يمنع إسقاطَ `isAutoDispose`** ⛔ **ولا يُثبت أن الرسمَ يعمل** — ★ **وذلك برهانُ §د.2 وحدها.**

⛔⛔★★ **والآليةُ لم تُفسَّر بالكامل — ولا يُدَّعى خلافُ ذلك:** ★ **المعروفُ أن المُطلِقَ اشتراكٌ ثانٍ حيٌّ على العائلة نفسِها، وأن إسقاطَه يُزيل العطل قطعاً** ⛔ **والسببُ الذي يجعل اشتراكاً ثانياً يوقف طَورَ الرسم يبقى مجهولاً** — ⚠️ **فإن ظهر العَرَضُ نفسُه في شاشةٍ أخرى تشترك مرتين، فهذا بابُه.** ⛔ **وفرضيةٌ سابقة جُرِّبت وفشلت وسُجِّلت:** **رفعُ القراءة إلى `_PricingFormState`** ⟵ **العطلُ باقٍ** ⟹ ★ **فليس موضعَ القراءة بل الاشتراكَ نفسَه** | ⛔ **كان عالياً — يحجب عمليةً يوميةً حاكمة** | ✅ **سُدِّد ومُتحقَّقٌ حيّاً** | ✅ **مُغلَق — 2026-08-29**
| ✅★★★ `DEBT-63` | ✅★★★ **سُدِّد وسببُه مقيسٌ لا مُستنتَج** (2026-08-30). ⛔⛔★★★ **والسببُ سطرٌ واحد في الثيم:** **`minimumSize: Size.fromHeight(Sizes.minTouch)`** في `outlinedButtonTheme` ([`app_theme.dart`](../../lib/core/design/app_theme.dart)) — ★ **و`Size.fromHeight(h)` هي `Size(double.infinity, h)`** ⟹ **`minWidth` لا نهائي.** ⟵ ⛔⛔ **وابنُ `Row` غيرُ المرن يُخطَّط بعرضٍ غير محدود** ⟹ **قيدٌ مستحيل: الزرُّ يُبنى ويُربَط ولا يُخطَّط أبداً** (`hasSize=false` **و`needsLayout=false` معاً**) ⛔ **ولا يُرسَم ولا يُخطئ بصوتٍ مسموع.** ★★ **والعطلُ كان يعمّ التطبيق كلَّه** — ⛔ **لا شاشةَ التسعير وحدها**: ★ **كلُّ زرٍّ محاطٍ في سياقٍ غير محدود العرض**، ⟵ **وزرُّ «نسخ أسعار أمس» ضحيّتُه الظاهرة وحدها لأنه الوحيد الذي يظهر بعد الإطار الأول.** ⛅★★★ **والتنصيفُ الذي أوصل إليه — سبعُ ضوابطَ مقيسة:** **(أ) عيّنةُ عزلٍ خارج المستودع بثلاث طبقات** (`setState` · `Riverpod` · مسارٌ مدفوع) ⟵ ✅ **الزرُّ يظهر** ⟹ ⛔ **فليس عطلَ إطار** · **(ب) ثلاثةُ ضوابطَ داخل شاشة التسعير** (خارج `AsyncStateView` · داخله · الزرُّ الحقيقي) ⟵ ⛔ **الثلاثة تسقط** ⟹ **فلا `AsyncStateView` ولا `_PricingForm` ولا مصدرُ البيانات** · **(ج) ضابطٌ على شاشةٍ أخرى** (`home_shell`) ⟵ ⛔ **يسقط** ⟹ **فالعطلُ عامٌّ للتطبيق** · **(د) صفٌّ عارٍ بلا `QtmsBulkActionBar`** ⟵ ⛔ **يسقط** ⟹ **فليس المكوّن** · **(هـ) ⛅★★★ والفاصلُ القاطع: صندوقٌ عاديٌّ بدل الزرّ في الصفِّ نفسِه** ⟵ ✅ **يُخطَّط `120x40` والصفُّ يصير `40`** ⟹ ⛔⛔ **فالعلّةُ في `OutlinedButton` وحده لا في الصفِّ ولا في الإدراج المؤجَّل.** ✅★★ **والإصلاحُ مُثبَتٌ بقلب الحالة:** **`Size(Sizes.minTouch, Sizes.minTouch)`** ⟵ **والضوابطُ الثلاثةُ الساقطة انقلبت في البناء نفسِه** (`row ×22.0` ⟶ `×48.0`) · ⛅ **والميزةُ مُتحقَّقةٌ حيّاً:** **الزرُّ ظاهرٌ ويعمل** — **ضغطُه ملأ الحقلين بسعرَي أمس** (`2000 ⟶ 1700` · `فارغ ⟶ 1300`) ⛔ **بلا حفظ** (`BR-M9-02`) · ✅ **و`DEBT-62` سليم في الدخول الثاني.** ★★ **وله حارسُ انحدارٍ مُثبَتٌ بطفرة** ([`design_gates_test.dart`](../../../test/core/design/design_gates_test.dart)) — **يرفض أي `minWidth` لا نهائي على الأزرار المحاطة والنصّية والأيقونية.** ⚠️★★ **ويبقى `filledButtonTheme` على `Size.fromHeight` عمداً** — ★ **مقصودةٌ هناك: الزرُّ الأساسي يملأ العرض وموضعُه دائماً محدود** — ⛔ **ولو وُضع في `Row` لسقط السقوطَ نفسَه** ⟹ ⚠️ **خطرٌ كامنٌ مُعلَن لا عطلٌ قائم** | ★ **كان: ميزةُ توفيرٍ معطَّلة** | ✅ **أُنجز** | ✅ **مغلق** |
| ⚠️ `DEBT-64` | ⚠️★★ **الحالةُ الفارغة تحت المرشِّح تنصح بنصيحةٍ خاطئة** (2026-08-30 · **رُصد في §د.2 لـ`WU-005`**). ⛅ **مقيس:** ★ **باختيار «لم يتم» بعد تسعير كل الأنواع تظهر «لا شيء يُسعَّر بعد — التسعير يتبع المخزون، سجّل وارداً أولاً ثم عُد لتسعيره»** ⛔ **والمخزونُ قائمٌ فعلاً** (**95 حبة**) — ⟵ **فالسببُ المعروض خاطئ: الفراغُ أثرُ المرشِّح لا غيابُ المخزون** ([`daily_pricing_screen.dart`](../../lib/capabilities/inventory/presentation/daily_pricing_screen.dart) — **`emptyTitle`/`emptyLabel` واحدةٌ للحالتين**). ⛔ **ويخالف `design-system.md` §هـ:** **«الفارغة تعرض سببَ الفراغ لا وصفَه»** — ★ **والنصيحةُ هنا تدفع المستخدم لتسجيل واردٍ لا يحتاجه.** ⚠️ **ولم يُعالَج عمداً:** ★ **عطلٌ سابقٌ مستقلٌّ عن `DEBT-62`/`DEBT-63`** ⛔ **وإصلاحُه خارج نطاق التحقق** — ★ **والعلاجُ المقترح: نصٌّ ثانٍ للحالة المفلترة وزرُّ «عرض الكل».** | ⚠️ **منخفض — إرشادٌ مضلِّل لا خطأُ بيانات** | ★ **مع أول تشغيلٍ يمسّ هذه الشاشة** | ⚠️ **مفتوح** |
| ⚠️★★ `DEBT-65` | ⚠️⚠️★★★ **ثلاثةُ معايير قبولٍ في `WU-005` أُغلقت الوحدةُ دونها — ⛔ تُقرأ ولا يُبنى عليها أنها مُتحقَّقة** (2026-08-30 · **قرارُ المالك الصريح بعد عرضِها عليه** — ⟵ `implementation-report-2026-08-30-run9.md` §4). ★ **وسُجِّلت بنداً مستقلاً لئلّا تسقط بإغلاق الوحدة:** ① ✅★★ **`AT-17` · `FR-M9-06` — مُغلَق 2026-08-30 في §د.2 لـ`WU-009`:** ⛅ **قِيس حيّاً — «السكرب - جونية رقم 1 · 10.000 كجم» بحقلَي «سعر التوزيع لكل كيلوجرام» و«الحد الأدنى لكل كيلوجرام»** ⟵ **فالوحدةُ الوزنية تظهر وتُسعَّر فعلاً.** ⚠️ **وكان متعذّراً حينها:** ⛅ **لا مخزونَ وزنياً يومَ اختبار `WU-005`** (**«إجمالي الوزني: 0.000 كجم»** مقيسٌ في «مخزون اليوم») ⟹ ★ **ويحتاج سكرباً عبر «الوارد جواني»** (`M7`) · ⚠️ **وله تغطيةٌ آلية** (`testWeightBalance`) ⛔ **لا تُغني عن §د.2** · ② ⛔ **`AT-18` · `FR-M9-01` — التصفير في يومٍ جديد:** ⛅ **يحتاج انقلابَ يومٍ بتوقيت UTC** ⟹ **عائقُ بيئةٍ لا عطلُ كود** (البروتوكول §ج) · ③ ⛔ **مسارُ منع الصلاحية** (`dailyPriceWrite` غائبة): ⛅ **حسابُ الاختبار يملك كل مفاتيح الكتالوج تعريفاً** (§6 البند ⑤) ⟹ **ويحتاج حساباً مقيَّداً** · ⚠️ **وله تغطيةٌ على المحاكي في قواعد الحماية.** ⛔⛔★★ **والأثرُ المُعلَن:** ★ **بوابةُ §د.2 لهذه الوحدة مُستوفاةٌ جزئياً** ⟹ ⛔ **فلا يُستشهَد بها دليلاً على تغطيةٍ كاملة**، ★ **وتُستكمَل عند أول تشغيلٍ يتوفّر فيه سكربٌ في المخزون أو حسابٌ مقيَّد.** | ⚠️ **متوسط — ثغرةُ تغطيةٍ لا عطلٌ قائم** | ★ **البند ① أُغلق 2026-08-30 · و② و③ عند أول تشغيلٍ تتوفّر فيه شروطُهما** | ⚠️ **مفتوحٌ جزئياً — 1 من 3 مُغلَق** |
| ⚠️ `DEBT-66` | ⚠️★★ **زرُّ [ إدخال ] في المركز المعلّق يفتح الشاشة على سياق البند ⛔ ولا يفتح الورقة السفلية للحقل نفسِه** (2026-08-30 · `WU-009`). ★ **ما نُفِّذ فعلاً:** **يُضبَط المصدرُ دائماً** (`A-01`)، **ومرشِّحُ «لم يُسعَّر» لبند `M9`**، **والمقوتُ المقصود لبند `M10`** ([`pending_entries_screen.dart`](../../lib/capabilities/oversight/presentation/pending_entries_screen.dart) `_open`) — ⟵ **فبندُ التسعير يصل حقلَه فعلاً.** ⛔ **وما لم يُنفَّذ:** **بندُ `M7` يصل قائمةَ الجواني لا نموذجَ الضريبة** — ★ **ونصُّ `FR-SYS-04` «ينقل مباشرةً إلى نفس الحقل».** ⛅★★ **والأهمُّ محقَّقٌ كاملاً:** ⛔⛔ **«لا شاشة إدخالٍ بديلة»** (`pending-entries-design.md` §6) — ⟵ **فلا مسارَ كتابةٍ ثانٍ بقواعد تحقّقٍ منفصلة**، ★ **وهو المحظورُ الذي سمّاه المستند «مخالفةً تصميمية».** ★ **والعلاجُ المقترح: تمريرُ [`PendingFocus`] إلى شاشة الجواني لتفتح ورقةَ الحقل بعد أول إطار** — ⚠️ **وكلفتُه حالةٌ إضافية في شاشةٍ من 1153 سطراً.** | ⚠️ **منخفض — نقرةٌ إضافية لا خطأُ بيانات** | ★ **مع أول تشغيلٍ يمسّ شاشة الجواني** | ⚠️ **مفتوح** |
| ⚠️ `DEBT-67` | ⚠️ **فلترُ التاريخ في مركز الإدخالات المعلّقة لم يُبنَ شريحةً** (2026-08-30 · `WU-009`) — ★ **`FR-SYS-10` يذكر «المصدر · التاريخ · نوع المستند»**، ⛅ **والمنفَّذ المصدرُ والنوع.** ★ **والمبرّر المُعلَن:** **القائمة مرتَّبةٌ بالتاريخ تنازلياً والتاريخُ ظاهرٌ في كل بطاقة**، ★ **والصفحةُ الواحدة 100 بند** ([`pendingEntriesPageSize`](../../packages/qtms_domain/lib/capabilities/oversight/domain/pending_entry.dart)) ⟵ **فمدىً زمنيٌّ عليها زينةٌ لا أداة اليوم.** ⚠️ **ويصير أداةً متى تجاوزت البنودُ صفحةً** — ★ **والعلاجُ قيدُ مدىً على `date` بالفهرس القائم نفسِه** (`sourceId ↑ · date ↓`) ⛔ **بلا فهرسٍ جديد.** | ⚠️ **منخفض — نقصُ ميزةِ تصفية** | ★ **متى تجاوز المركزُ صفحةً واحدة** | ⚠️ **مفتوح** |
| ✅★★★ `DEBT-68` | ⛔⛔★★★ **شاشةُ التوزيع تسقط كاملةً عند فتح بندِ توزيعةٍ من المركز المعلّق** (2026-08-30 · `WU-009` · **كشفه اختبارُ المحاكي `Pixel_6_API_36` وحده**). ⛅ **مقيسٌ حيّاً:** ★ **الضغطُ على [ إدخال ] في بند «توزيعة رعوي الاختبار» أظهر شاشةً حمراء** بـ**«Tried to modify a provider while the widget tree was building»** ⛔ **بدل شاشة التوزيع.** ⛔⛔★★★ **والجذرُ سطرٌ واحد:** `_DistributionBodyState.initState` **كان يستدعي [`PendingFocusState.take`] مباشرةً** ([`distribution_screen.dart`](../../lib/capabilities/sales_receivables/presentation/distribution_screen.dart)) — ★ **و`take()` *تكتب* حالةً لا تقرؤها**، ⟵ **وRiverpod يمنع الكتابة في دورة حياة الويدجت** (`build` · `initState` · `dispose` · …). ⚠️⚠️ **والمفارقة أن العلاج كان مكتوباً ثم أُزيل:** ★ **صيغةٌ أولى بمكوّنٍ مستقل تستهلك الوجهة في `addPostFrameCallback`** ⛔ **استُبدلت بـ`initState` لتقليل إعادة المحاذاة في ملفٍ قائم** ⟵ **فعاد العطلُ من حيث سُدَّ.** ✅★★★ **سُدِّد 2026-08-30:** **الاستهلاك في `addPostFrameCallback` مع `setState` وحارسِ `mounted`** — ⛅ **ومُثبَتٌ حيّاً بعد الإصلاح: الشاشة تُفتَح على المقوت المقصود وعلى التوزيعة القائمة `DST-20260830-0001` بلا استثناء.** ✅★★ **وأربعةُ حرّاسِ ارتدادٍ يقيسون العطل لا يصفونه** ([`distribution_screen_test.dart`](../../test/capabilities/sales_receivables/presentation/distribution_screen_test.dart)) — ★ **مُثبَتون بطفرة: بإعادة الاستهلاك إلى `initState` تسقط الأربعة** (`+19 -4`). ⛔⛔★★★ **ولم يكشفه أيٌّ من 1594 اختباراً آلياً** — ★ **لأن اختبار شاشة التوزيع لا يصلها ووجهةٌ مضبوطة**: ⟹ **وهو الوجهُ الخامس لدرس `DEBT-37`** (`DEBT-40` · `DEBT-41` · `DEBT-61` قبلَه). | ⛔ **عالٍ — شاشةٌ تسقط كاملةً** | ✅ **إصلاحٌ + أربعةُ حرّاسٍ + إثباتٌ حيّ** | ✅ **مُغلَق — 2026-08-30** |
| ⚠️★★★ `DEBT-69` | ⛔⛔★★★ **حارسُ النزاهة في `track_implementation.py` لا يرى الوحداتِ غيرَ المنجَزة — فبقيت ستَّ عشرةَ وحدةً «مخطَّطة» تحمل شهادةَ نجاحٍ كاذبة** (2026-08-30 · **التشغيل 11**). ⛅★★ **مقيسٌ لا مُخمَّن:** ★ **جسدُ `WU-006` كاملاً — 73 سطراً — كان منسوخاً حرفياً ومطابقاً بايتاً ببايت داخل `WU-010` … `WU-025` كلِّها** ([`implementation-plan.md`](../00-governance/upds-workflow/implementation-plan.md)): ⟵ **فكلُّ وحدةٍ مخطَّطةٍ كانت تعرض `**فحوصات التحقق:**` بتسعةِ أسطرٍ «✅ نجح» بتاريخ 2026-08-27، و`**نتيجة التحقق:** نجح`، وجدولَ اختبارِ محاكٍ بثمانيةِ سيناريوهات ومعرَّفَ مستندٍ حيٍّ `DST-20260827-0001`، وقائمةَ ملفاتٍ تخصّ التوزيع** — ⛔ **لوحداتٍ لم يُكتب لها سطرُ كودٍ واحد.** ⛔⛔★★★ **والسببُ الجذري في الأداة لا في الملف:** ★ **`integrity_violations` تفحص وحداتِ `[منجز ✅]` وحدَها** (`implementation-playbook.md` §7 البند 5: «**أي وحدة `[منجز ✅]` بلا فحوص … تظهر في `integrity_violations`**») — ⟵ **والستَّ عشرةَ `[مخطط]`**: ⟹ ⛔⛔ **فبقيت البوابةُ تُبلِّغ «`integrity_violations` فارغة» وهي تحمل ستَّ عشرةَ شهادةَ نجاحٍ كاذبة**، ★ **ولا يكشفها إلا قارئٌ يفتح الملف بعينه.** ⛔⛔ **وهذا حرفياً ما يمنعه بروتوكول التشغيل §و** («**الادّعاءُ غيرُ المتحقَّق أسوأ من غياب التوثيق: غيابُه يُبقي المشكلة مرئية، وادّعاؤه يُغلقها كذباً**») — ★ **والضررُ تراكمي:** ⟵ **كلُّ تشغيلٍ قادمٍ كان سيبدأ من رقمٍ خاطئ ويظنّ فحوصَ وحدتِه مُشغَّلةً سلفاً.** ✅★★ **والعلاجُ نُفِّذ في التشغيل نفسِه:** ★ **حُذف المقطعُ المنسوخ من الستَّ عشرةَ جميعاً وحلَّ محلَّه حقلان صريحان** («⏳ **لم تُنشأ بعد**» · «⏳ **لم تُشغَّل بعد**») ⛔ **بلا مساسٍ بحقلٍ خاصٍّ بوحدة** (**النطاق · معيارُ القبول · المتطلباتُ المرتبطة · المستنداتُ الحاكمة · «يلمس نطاقاً حسّاساً» · الملاحظات — كلُّها كما هي**) · ⛅ **والملفُّ انكمش من 4080 سطراً إلى 2992** ⟵ **1088 سطرَ حشوٍ صافية** · ✅ **و`track_implementation.py` بعده يقرأ `checks: 0` و`verification_result: ''` لكلِّ وحدةٍ مخطَّطة** ⛔ **بعد أن كان يقرأ تسعةً و«نجح».** ⚠️★★ **وحدٌّ معلَنٌ باقٍ — ⛔ والدَّينُ الحقيقي هو هذا لا النسخُ نفسُه:** ★ **الحارسُ ما يزال أعمى عن الوحدات غير المنجَزة** ⟵ **فلا شيءَ يمنع تكرارَ العطل نفسِه غداً**، ★ **والعلاجُ المقترح فحصٌ ثانٍ في الأداة: «وحدةٌ ليست `[منجز ✅]` تحمل `**نتيجة التحقق:**` أو فحصاً بنتيجةٍ ناجحة ⟵ مخالفةُ نزاهة»** — ⚠️ **وهو تعديلٌ في `upds-docgen` نفسِها لا في المشروع** ⟵ **فيُرفَع لمالك الإضافة.** | ⛔ **عالٍ — يُفسد رقمَ البداية لكلِّ تشغيلٍ قادم** | ✅ **الملفُّ أُصلح 2026-08-30** · ⚠️ **وحارسُ الأداة يُطلَب من مالك الإضافة** | ⚠️ **مفتوحٌ جزئياً — البياناتُ صحيحةٌ والحارسُ ما يزال ناقصاً** |
| ✅★★★ `DEBT-70` | ⛔⛔★★ **قراءةُ `dealer_balances` و`dealer_surplus` بلا قيدِ نطاقِ مصادر** (رُصد 2026-08-30 · `WU-010`). ★ **الشرط اليوم `isSignedIn() && perm('dealerBalanceView')` وحدَه** ([`firestore.rules`](../../firestore.rules)) — ⛔ **بلا `storedInScope()`**، ⟵ **فمستخدمٌ نطاقُه مصدرٌ واحد يستطيع قراءة رصيد المقوت في مصدرٍ خارج نطاقه**، ★ **وهو ما يمنعه `GR-23` و`FR-M19-02` نصّاً.** ⚠️ **وكان بلا أثرٍ عملي حتى اليوم لأن لا قارئ للمجموعة** — ⟵ **و`WU-010` أولُ قارئ:** ★ **قراءةٌ بمعرّفٍ واحد `{dealerId}_{sourceId}` للمصدر المختار في الشاشة** ⛔ **لا استعلامَ سردٍ**، ⟹ **فلا تسريبَ فعلياً اليوم** ⛔ **والفجوةُ قائمة.** ★★ **ولم تُعالَج في هذا التشغيل بقرارٍ مُعلَن:** ⛔ **تعديلُ `firestore.rules` بلا نشرٍ يقابله يكسر التطابقَ البايتيَّ المُثبَت بين المستودع والتجريبية** (`CLAUDE.md`)، ★ **والنشرُ حصرُ المالك** (بروتوكول التشغيل §ز). ★ **والعلاج سطرٌ واحد:** إضافةُ `&& storedInScope()` إلى شرطَي القراءة **مع زوجَي اختبارٍ في `permission-matrix.test.js`** — ⛔ **ويُنفَّذ في أول تشغيلٍ ينشر القواعد.** ✅★★★ **وسُدِّد فعلاً (2026-09-02 · بقرار المالك بتفويضٍ صريح):** ★ **`dealer_balances` صار `storedInScope() && perm('dealerBalanceView')`** — ★ **نظيرُ `supplier_balances` حرفياً** · ⛔⛔★★★ **و`dealer_surplus` عُولج بدقّةٍ لا بنسخِ الشرط:** ★ **الفائضُ العام يُكتب `sourceId: null` عمداً** (`FR-M12-12` · `AT-32`) ⟵ **و`storedInScope()` كانت ستحجبه عن الجميع** (**الحقلُ موجودٌ فيصدُق `in` ثم يسقط `inScope(null)`**) ⟹ ★ **فالشرط `resource.data.sourceId == null || inScope(...)`** — ⛅★★ **ومقيسٌ بخمسة اختباراتٍ سلوكية جديدة** (**241 بعد أن كانت 236**) · ✅★★★ **واختبارُ طفرةٍ أثبت أنه ليس شكلياً:** **بإسقاط `storedInScope()` سقط الاختبارُ وسمّاه، ثم أُعيد فنجح.** ⛔⛔★★ **وعطلٌ ثانٍ كشفه العلاج:** ★ **بذرتا الاختبار في `rules.test.js` و`permission-matrix.test.js` كانتا تكتبان المستندَ بلا `sourceId`** ⟵ **وهو ما لا تكتبه السحابةُ أبداً** ⟹ ⛔ **فكانتا تُخفيان غيابَ فحص النطاق أصلاً** — ★ **وصُحِّحتا.** | ⚠️ **كان متوسطاً — قراءةٌ خارج النطاق لبيانات ماليةٍ مجمَّعة** | ✅ **سُدِّد 2026-09-02** | ✅ **مُغلَق** |
| ⚠️★★ `DEBT-71` | ⛔⛔★★ **مفاتيحُ عائلات التقارير الستّة بواباتُ شاشةٍ لا حارسَ قراءة** (2026-08-31 · `WU-011` · `IQ-034` الخيار ب). ★ **العائلاتُ المالية والبيعية محكومةٌ فعلاً في القاعدة** — `dealer_ledger` و`dealer_balances` ⟵ `dealerBalanceView` · `distributions/{id}/pricing` ⟵ `distributionPriceView` — ⛔ **أما المخزنية والرقابية فمجموعاتُها شرطُها النطاقُ وحده** (`inventory_ledger` · `item_daily_balances` · `incoming_count` · `pending_entries`): ⟵ **فمن لا يملك `reportInventoryView` يُخفى عنه التقرير** ⛔ **ولا يُمنَع من قراءة المجموعة نفسِها بأي مسارٍ آخر** — ★ **وهو حرفياً ما يحذّر منه `RISK-02`.** ★★ **ولا يُدَّعى غيرُ ذلك في أي موضع:** ✅ **مكتوبٌ في الكتالوج §2.11 وفي `security-requirements.md` §1 وفي بوابة التغطية وفي `permission.dart`** ⟵ **فالفجوةُ معلَنةٌ لا مكتومة** (نفسُ نهج `RISK-09`). ⛔⛔ **والعلاجُ ليس تشديدَ شرطٍ في القاعدة:** ★ **تلك المجموعاتُ تقرؤها شاشاتٌ منجَزة** (مخزونُ اليوم · التسعير · مركزُ الإدخالات) ⟵ **وتقييدُها بمفتاح تقريرٍ يكسرها**، ⛔ **بل قرارٌ معماريٌّ على مستوى تلك المجموعات نفسِها** — **ويُرفَع بندَ `IQ` عند أول تشغيلٍ يمسّها.** | ⚠️ **متوسط — إخفاءُ واجهةٍ يُقرأ حمايةً وهو ليس كذلك** ⛔ **بلا تصعيدِ امتياز: القارئُ يقرأ ما تسمح له القاعدةُ به أصلاً** | ★ **قرارٌ معماري عند أول تشغيلٍ يمسّ شروطَ تلك المجموعات** | ⚠️ **مفتوح — معلَن** |
| ⚠️★ `DEBT-72` | ⚠️★★ **ستةُ فلاترَ من `FR-M19` §2 لم تُبنَ في `WU-011` — ⛔ وتُقال صراحةً لا تُطوى** (2026-08-31): **① الرعوي في `R-03` و`R-04`** · **② الجونية في `R-04`** · **③ المقوت في `R-08` و`R-10`** · **④ النوع في `R-02` و`R-03` و`R-05`** · **⑤ المستخدم في `R-14`** · **⑥ الحدُّ الأدنى للرصيد في `R-17`**. ★ **والمبنيُّ منها فعلاً: المصدرُ والفترةُ في كلِّ تقرير، والنوعُ في `R-01`، والمقوتُ في `R-19`، وحالةُ التسوية والإيداع والتوفّر ورصيدِ المقوت ونوعِ المستند المعلَّق.** ★★ **وأربعةٌ منها فلترةٌ محليةٌ رخيصة** (الرعوي · المقوت · النوع · الحدُّ الأدنى) — ⟵ **تُبنى بشريحةٍ في شريط التصفية بلا استعلامٍ جديد**، ⚠️ **و`⑤ المستخدم` وحدَه يحتاج ما ليس قائماً:** ★ **`createdBy` مكتوبٌ في السند** ⛔ **لكن ترجمتَه اسماً تحتاج `userView`** ⟵ **فيُبنى مع من يملكه أو يُعرَض المعرّف** — ★ **وكلاهما قرارُ عرضٍ يُحسَم عند بنائه.** ⛔⛔ **ولا يُوصَف التقريرُ بما لا يفعله:** ✅ **سطرُ الفلاتر تحت كلِّ تقريرٍ في القائمة يذكر المتاحَ فعلاً** ([`reports_screen.dart`](../../lib/capabilities/oversight/presentation/reports_screen.dart)) ⛔ **لا ما يَعِد به المتطلب.** | ⚠️ **منخفض — تقاريرُ تعمل بفلاترها الأساسية** ⛔ **ولا رقمَ خاطئ فيها** | ✅★★ **سُدِّد أربعةٌ من ستة في `WU-018`** (2026-09-04): **① الرعوي في `R-03` و`R-04` · ② الجونية في `R-04` · ③ المقوت في `R-08` و`R-10` · ④ النوع في `R-02` و`R-03` و`R-05`** — ★ **كلُّها ترشيحٌ محليٌّ على الصفحة المقروءة** ⛔ **بلا استعلامٍ جديد ولا فهرسٍ ثالث**، ★ **وثلاثُ مجموعاتٍ مشتركة في الشاشة** (`_supplierGroup` · `_itemGroup` · `_dealerGroup`) ⛔ **لا نسخةٌ لكل تقرير.** ⏳ **والباقيان:** **⑤ المستخدم في `R-14`** — ⛔ **`createdBy` مكتوبٌ لكن ترجمتَه اسماً تحتاج `userView`** ⟵ **وهو قرارُ عرضٍ يُحسَم عند بنائه** · **⑥ الحدُّ الأدنى للرصيد في `R-17`** — ✅ **البانِي يقبله** (`minimumBalance`) ⛔ **ولا مُدخَل رقميّاً في شريط التصفية**: ★ **الشريطُ شرائحُ لا حقول** (`filter_bar.dart`) ⟵ **وإضافةُ نمطِ إدخالٍ إليه قرارُ تصميمٍ لا فلترٌ** | ⏳ **مفتوحٌ جزئياً — معلَن** |
| ⛔⛔★★★ `DEBT-73` | ⛔⛔★★★ **Riverpod 3 يُعيد المحاولة تلقائياً على أي مزوّدٍ يرمي — فحالةُ الخطأ لا تصل الشاشة أبداً** (2026-08-31 · `WU-011` · **كشفه اختبارُ ويدجت لا مراجعةُ كود**). ⛅★★ **مقيسٌ بمزوّدٍ مجرَّد:** `FutureProvider<int>((ref) async => throw Exception('boom'))` — ★ **الحالةُ تبقى `AsyncLoading` بعد 50 مللي ثانية وبعد استماعٍ فعلي**، ⛔ **و`await ref.read(p.future)` لا يعود إطلاقاً** (**قِيس: مهلةٌ 30 ثانية بلا نتيجة**). ⟵ ⛔⛔★★★ **والأثرُ في الواجهة أن المستخدم يبقى أمام هيكلٍ شبحيٍّ أبداً** ⟵ **يقرؤه بطئاً في الشبكة**، ★ **بينما الحقيقةُ رفضُ صلاحيةٍ لن يزول بإعادة المحاولة.** ✅★★★ **والعلاجُ هو القاعدةُ الموثَّقة أصلاً:** `error-handling-strategy.md` §3 القاعدة 3 (**«الأخطاء المتوقّعة تُرجَع كنتيجة لا تُرمى»**) — ⟵ **فـ`reportTableProvider` يُرجِع `Outcome<ReportTable?>`** ([`report_providers.dart`](../../lib/capabilities/oversight/application/report_providers.dart)) **ولا يرمي**، ★ **والرفضُ يُترجَم برسالة الكتالوج** (`PermissionError` ⟵ «ليس لديك صلاحية…»). ✅ **وحارسُ ارتداد يقيس ذلك** ([`report_screens_test.dart`](../../test/capabilities/oversight/presentation/report_screens_test.dart)). ⚠️⚠️★★ **والدَّينُ الباقي أن القاعدة تسري على كلِّ `FutureProvider` قادم** — ★ **ولا حارسَ آليٌّ يمنع مزوّداً جديداً من الرمي**: ⟵ **والمقترَح بندٌ في `design_gates_test.dart` يرفض `FutureProvider` لا يُرجِع `Outcome`** ⛔ **أو مراجعةٌ يدوية تُنسى.** ★ **والمزوّداتُ القائمةُ كلُّها `StreamProvider`** ⟵ **وأخطاءُ التدفّق أحداثٌ لا رميٌ في الإنشاء** ⛔ **فلا أثرَ رجعيٌّ عليها.** | ⛔ **عالٍ — يُخفي كلَّ رفضٍ خلف تحميلٍ لا ينتهي** | ✅ **العلاجُ مُطبَّقٌ في `WU-011`** · ⚠️ **وحارسُ البوابة يُبنى مع أول `FutureProvider` ثانٍ** | ⚠️ **مفتوحٌ جزئياً — الحالةُ الحاضرة سليمة والحارسُ ناقص** |
| ⛔⛔★★★ `DEBT-74` | ⛔⛔★★★ **الشاشةُ تعرض جدولَ التقرير السابق بإجمالياته تحت عنوان التقرير الجديد** (2026-08-31 · `WU-011` · ⛅★★ **كشفه اختبارُ المحاكي §د.2 وحدَه بعد 1738 اختباراً ناجحاً**). ★★ **السبب:** `AsyncValue` **تحتفظ بقيمة الطلب السابق أثناء إعادة الحساب** (`isLoading == true` **و`value` غيرُ خالٍ**) — ⟵ **وفرعُ التحميل كان يُقاس بـ`value: null` وحدَه**، ⛔ **فلا يلتقط إلا أولَ بناء.** ⛅ **والمقيسُ حيّاً: فتحُ «الضمارات» بعد «التوزيعات» أظهر أعمدةَ التوزيعات وإجمالييها — «إجمالي الحبات» و«عدد التوزيعات 3» — ثوانيَ كاملة تحت عنوان «الضمارات»**، ★ **ثم استقرّ على «إجمالي الضمارات 59,250».** ⛔⛔★★★ **وخطرُه أنه رقمٌ خاطئ يبدو صحيحاً** — ⟵ **وزرُّ التصدير كان حيّاً في تلك النافذة** ⟹ **مستندُ PDF يحمل أرقامَ تقريرٍ آخر إلى يد العميل.** ✅★★ **والعلاج شطران:** ① **فرعُ التحميل يُقاس بـ`isLoading` أولاً** ② **و`table` يُقرأ فارغاً وهو قيد الحساب** ⟵ **فيختفي زرُّ التصدير كذلك** ([`report_view_screen.dart`](../../lib/capabilities/oversight/presentation/report_view_screen.dart)). ✅ **وحارسُ ارتدادٍ يقيسه بتأخيرٍ مُصطنَع في الدليل الوهمي** ([`report_screens_test.dart`](../../test/capabilities/oversight/presentation/report_screens_test.dart)) — ⛔ **وبلا التأخير لا تقع النافذة أصلاً فلا يُقاس شيء.** | ⛔ **عالٍ — رقمٌ خاطئ يُعرَض ويُصدَّر بلا أي إشارة** | ✅ **سُدِّد في `WU-011`** | ✅ **مغلق** |
| ⛔⛔★★★ `DEBT-75` | ⛔⛔★★★ **«الرصيد الختامي» في كشف حساب المقوت كان صفراً وهو 49,800** (2026-08-31 · `WU-011` · ⛅★★ **كشفه اختبارُ المحاكي §د.2 وحدَه**). ★★ **السبب حقلٌ مخزَّنٌ لا يكتبه كلُّ كاتب:** ⛅ **كاتبُ التوزيع يكتب `balanceAfter` في قيد الدفتر** ([`distribution.dart:945`](../../functions/lib/src/distribution.dart)) ⛔ **وكاتبُ القبض لا يكتبه إطلاقاً** ([`receipt.dart`](../../functions/lib/src/receipt.dart) — **حقولُ القيد ليس فيها الحقل**)، ⟵ **فكلُّ قيدِ قبضٍ يُقرأ صفراً**، ⟹ **وآخرُ قيدٍ في الكشف قبضٌ** ⟹ **والختامي صفر.** ⛅ **والمقيسُ حيّاً: مدين 59,250 · دائن 9,450 · وعمودُ «الرصيد بعد الحركة» يعرض 48,000 ثم 0 ثم 53,000 ثم 0 …** — ⛔⛔ **رقمٌ خاطئ في تقريرٍ مالي يُصدَّر إلى يد العميل**، ★ **ويناقض `R-17` في الجلسة نفسِها** (**49,800**). ✅★★★ **والعلاجُ هو `ADR-0008` حرفياً — «الرصيد مشتقٌّ من الدفتر دائماً»:** ★ **الرصيدُ الجاري يُحسَب في البناء بالمرور على القيود غير الملغاة** ([`report_builders.dart`](../../packages/qtms_domain/lib/capabilities/oversight/domain/report_builders.dart))، ⛔ **وحقلُ `balanceAfter` أُسقط من `DealerLedgerRowCard` ومن مُحوِّله** ⟵ **فلا يبقى بابٌ لقراءته** · ★ **والملغى يُعرَض بشرطةٍ لا برقم** (`A-14`). ✅ **وحارسا ارتدادٍ يقيسانه** ([`report_builders_test.dart`](../../packages/qtms_domain/test/capabilities/oversight/domain/report_builders_test.dart)). ⚠️⚠️★★ **والدَّينُ الباقي على السحابة لا على التقرير:** ⛔ **`balanceAfter` في `dealer_ledger` حقلٌ يكتبه كاتبٌ من اثنين** — ★ **فإما يكتبه كاتبُ القبض كذلك وإما يُحذف من العقد**، ⟵ **وتركُه كما هو فخٌّ لأي قارئٍ قادم** ⛔ **ولا يُصلَح هنا: تصحيحُه يلمس معالِج المقبوضات ويشترط نشراً**، ★ **والنشرُ حصرُ المالك** (بروتوكول التشغيل §ز). | ⛔ **عالٍ — رقمٌ مالي خاطئ يُعرَض ويُصدَّر ويناقض تقريراً آخر** | ✅ **جانبُ التقرير سُدِّد في `WU-011`** · ⚠️ **وجانبُ الكاتب السحابي مفتوح** | ⚠️ **مفتوحٌ جزئياً — العرضُ صحيحٌ الآن والعقدُ السحابي ناقص** |
| ⚠️★★ `DEBT-76` | ⚠️★★ **منتقي التاريخ يعرض أرقاماً عربيةً-هنديةً في ترويسته وفي اسم الشهر** (2026-08-31 · **التشغيل 2 لـ`WU-011`** · ⛅★★ **كشفه اختبارُ المحاكي §د.2 وحدَه**). ⛅ **المقيسُ حيّاً: «الاثنين، ٣١ أغسطس» و«أغسطس ٢٠٢٦» في الترويسة** — ⚠️ **بينما شبكةُ الأيام نفسُها لاتينية** (`1`…`31`)، ⟵ ⛔ **فالنظامان يظهران في حوارٍ واحد.** ⛔⛔★★ **وهذا يخالف قاعدةً موثَّقةً صراحةً:** «**والأرقام باللاتينية (0-9) قيمةً وحيدة ثابتة** ⛔ **لا خياراً**» ([`ui-guidelines.md`](../18-ux-ui/ui-guidelines.md) §8) — ★ **وعلّتُها مذكورةٌ هناك: اللاتينيةُ ثابتةُ العرض فتتحاذى الجداول** ([`localization-strategy.md`](../17-localization-and-i18n/localization-strategy.md) §4.1). ★★ **والمصدرُ ليس كودَ المشروع:** `GlobalMaterialLocalizations` **مع `Locale('ar')`** ([`main.dart`](../../lib/main.dart)) — ⟵ **فالأثرُ عابرٌ لكل شاشةٍ تفتح `showDatePicker`** ⛔ **لا التقاريرُ وحدها.** ⛔⛔ **ولم يُعالَج في التشغيل 2 بقرارٍ مُعلَن:** ★ **العلاجُ مندوبٌ يلفّ `MaterialLocalizations` ويُحوِّل أرقامَ كل نصٍّ راجع** — ⚠️ **وواجهتُها تتجاوز مائةَ عضو**، ⟵ **فكتابتُها في تشغيلٍ يُغلق مرحلةً مخاطرةٌ على كل حوارٍ في التطبيق**، ★ **ومحلُّها زيادةٌ لها حارسُها الآلي** (بندٌ في `design_gates_test.dart` **يرفض أيَّ رقمٍ عربيٍّ-هنديٍّ في شجرةِ حوارٍ مبنيّة**). ⛔ **ولا يُقاس عليه نصُّ التطبيق نفسُه:** ✅ **كلُّ رقمٍ تكتبه شاشاتُنا لاتينيٌّ ومُختبَر.** | ⚠️ **منخفض — اتّساقٌ بصريٌّ لا رقمٌ خاطئ** ⛔ **ولا أثرَ على قيمةٍ محسوبة** | ★ **زيادةٌ مستقلة بحارسها الآلي** ⛔ **لا في تشغيلٍ يُغلق مرحلة** | ⚠️ **مفتوح — معلَن** |
| ⚠️⚠️★★★ `DEBT-78` | ⛔⛔★★★ **أرضيةُ أندرويد صارت API 24 ولم يُشغَّل التطبيق على API 24/25 ولا مرةً واحدة** (2026-09-01 · [`AM-011`](../00-governance/upds-workflow/amendment-log.md) · [`CR-010`](../02-requirements/change-requests/CR-010-android-floor-api-24.md)). ★ **ما أُثبِت فعلاً:** ⛅ **حزمةُ التثبيت تُعلن `minSdkVersion:'24'`** (`aapt2 dump badging`) · **ودمجُ المانفست بصفر خطأ** · **و66 مصنوعةً أندرويديّة في الرسم المحلول لا واحدةَ فيها تتجاوز API 23** · **و2071 اختباراً ناجحاً.** ⛔⛔ **وما لم يُثبَت — وهو المهم:** ★ **إقلاعُ التطبيق فعلاً على جهازٍ بـAPI 24/25**: ⟵ **رسمُ Flutter بمسار OpenGLES بدل Vulkan** · **واتصالُ Firestore عبر gRPC بمخزنِ ثقةِ أندرويد 7** · **وسلوكُ ورقة المشاركة و`FileProvider`** · **وشكلُ الأيقونة غيرِ المتكيّفة على المُشغِّل.** ⚠️★★ **والسبب مُعلَنٌ لا مُهمَل:** ⛔ **لا جهازَ ولا محاكيَ API 24/25 لدى المالك**، ★ **وقد طلب صراحةً ألّا يُنزَّل أيُّ محاكٍ** — ⟵ **فبوابةُ §د من بروتوكول التشغيل غيرُ مستوفاةٍ لهذه الأرضية**، ⛔ **وهذا فارقٌ يُقال لا يُطوى.** ★★ **والعلاجُ الوحيد المقبول: تشغيلٌ حيٌّ واحد على `google_apis;android-24;x86_64`** — ★ **يُغطّي المسارات الخمسة أعلاه** ⛔ **ولا يُغني عنه اختبارٌ آلي.** ✅★★ **وأقَرّه المالك صراحةً (2026-09-02)** — ★ **الدَّينُ مقبولٌ معروفاً لا مكتشفاً لاحقاً**، ⛔⛔★★★ **وإقرارُه ليس إذنَ نشرٍ إنتاجي:** ★ **تلك تحتاج الكلمةَ الحرفية وحدها** (بروتوكول التشغيل §ب.2). | ⚠️ **متوسط — لا عطلَ مرصود، وأرضيةٌ مُعلَنةٌ بلا تشغيلٍ واحد يسندها** | ★ **قبل أول نشرٍ إنتاجي يَعِد بدعم أندرويد 7** | ⚠️ **مفتوح — معلَن** |
| ✅★★★ `DEBT-79` | ✅★★★ **مُغلَق (2026-09-02) — مسارا الدخول بالبصمة قِيسا حيّاً على `Pixel_6_API_36`.** ★ **والحاجزُ كان بيئياً لا برمجياً:** ⛔ **المحاكي كان بلا قفلِ شاشةٍ ولا بصمةٍ مسجَّلة** ⟹ **`isDeviceSupported()` تُرجِع `false`**. ✅ **والعلاجُ المقيس:** `adb shell locksettings set-pin` **ثم `android.settings.FINGERPRINT_ENROLL` وإتمامُ التسجيل بـ`adb emu finger touch 1`** ⟵ **و`dumpsys fingerprint` أثبت `count:1`** (⛔ **والحيلةُ ليست HAL الافتراضية `persist.vendor.fingerprint.virtual.*`: مستشعرُ المحاكي `fingerprint-service.ranchu` لا يقرؤها — مقيسٌ لا مفترَض**). ✅★★★ **وأحدَ عشرَ سيناريو قِيست:** **التفعيل بكلمةٍ صحيحة ثم بصمة ⟵ «فُعِّل الدخول بالبصمة على هذا الجهاز»** · **وبكلمةٍ خاطئة ⟵ «❌ كلمة المرور غير صحيحة» بلا طلبِ بصمة** (الشرط 5) · **وإلغاءُ البصمة ⟵ «❌ تعذّر التحقق من البصمة»** · **والدخولُ بالبصمة ⟵ لوحةُ اليوم** (الشرط 2) · **والإطفاءُ ⟵ «أُوقف الدخول بالبصمة ومُسحت بياناته من هذا الجهاز» ثم اختفاءُ الزرّ من شاشة الدخول** (الشرط 6 — **دليلُ المسح**) · **وإعادةُ التفعيل من خزنةٍ نظيفة.** ⚠️★★ **وما بقي غيرَ مقيسٍ حيّاً يُقال لا يُطوى:** ⛔ **الشرط 7 (المسحُ التلقائي عند فشل الدخول السريع) والشرط 8 (تحديثُ الخزنة بعد تغييرٍ ناجحٍ للكلمة)** — ★ **لكلٍّ منهما اختبارُ ارتدادٍ آلي في `biometric_providers_test.dart`**، ⚠️ **وقياسُهما حيّاً يستلزم تدويرَ كلمةِ حساب QA المُدارة في `.secrets/`** ⟹ ⛔ **وهو ما تمنعه `secrets-management-policy.md`** — ★ **فالفارقُ مُعلَنٌ لا مطويّ.** | ✅ **مُغلَق** | — | ✅ **مغلق (2026-09-02)** |
| ✅★★★ `DEBT-82` | ⛔⛔★★★ **إلغاءُ المستخدم للبصمة كان يُسرِّب استثناءً غيرَ ممسوك إلى الشاشة** (2026-09-02 · **مقيسٌ على `Pixel_6_API_36` لا مفترَض** · [`ADR-0024`](adr/ADR-0024-biometric-unlock-with-os-protected-credentials.md)). ★ **الجذر:** **`local_auth` 3.x ترمي `LocalAuthException`** ⛔ **لا `PlatformException`** — ★ **وعقدُها نصّاً:** «*returns false … For other failures cases, throws a `LocalAuthException`*» ⟵ **و`userCanceled` منها**، ⛔ **و[`local_auth_biometric_gateway.dart`](../../lib/core/device/local_auth_biometric_gateway.dart) كان يمسك `PlatformException` وحدَها.** ⟹ ⛔⛔ **والأثرُ المقيس:** `Unhandled Exception: LocalAuthException(code userCanceled)` **في `logcat`** ⟵ **فلا تكتمل `_toggle`**: ★ **يبقى المفتاح معطَّلاً** ⛔ **وتبقى رسالةُ «❌ كلمة المرور غير صحيحة» السابقة معروضةً بعد كلمةٍ صحيحة** — ★ **كذبٌ صريحٌ على المستخدم.** ⛔⛔★★ **وهو ثاني عشرَ أوجهِ `DEBT-37` حرفياً:** ★ **`LocalAuthBiometricGateway` لم تكن مُختبَرةً قطّ** — ⟵ **ولا يُفترَض شكلُ مخرجات مكتبة، يُقاس.** ✅ **والعلاج:** **مسكُ `LocalAuthException` في `authenticate` و`availability` معاً** (⛔ **و`PlatformException` باقيةٌ حارساً ثانياً**) · ✅ **و10 اختباراتِ ارتدادٍ جديدة** [`local_auth_biometric_gateway_test.dart`](../../test/core/device/local_auth_biometric_gateway_test.dart) · ⛅ **ومُثبَتٌ حيّاً بعد الإصلاح: «❌ تعذّر التحقق من البصمة» والمفتاحُ يعمل.** | ✅ **مُغلَق** | — | ✅ **مغلق (2026-09-02)** |
| ✅★★★ `DEBT-83` | ⛔⛔★★★ **مؤشّرُ الاتصال يعلَق على «غير متصل» إلى آخر عمر العملية بعد أول تسجيل خروج** (2026-09-02 · **مقيسٌ على `Pixel_6_API_36`** · `AM-008` ① · `ADR-0003`). ★ **الجذر:** **تسجيلُ الخروج يُفشِل مُصغيَ `users/{uid}` بـ`PERMISSION_DENIED`** (⟵ **القاعدة تشترط `request.auth.uid == userId`** — **مقروءٌ في `logcat` نصّاً**)، ★ **والخطأُ يصعد من `asyncExpand` فيقتل البثَّ الخارجي كلَّه** ⟹ ⛔⛔ **فلا يُعاد بناءُ المُصغي عند الدخول التالي أبداً.** ★ **مقيسٌ في الاتجاهين:** **بدايةٌ نظيفة ⟵ «متصل»** · **وبعد خروجٍ ودخولٍ في العملية نفسِها ⟵ «غير متصل» أبداً** ⛔ **بينما البياناتُ تُقرأ من الخادم فعلاً وشاشةُ المصادر تعرضها.** ⛔⛔★★ **وهو أسوأُ ما يصيب هذا المؤشّر بالذات:** ★ **وُجد ليمنع ادّعاءً كاذباً** ⟵ **فصار يكذب في الاتجاه الآخر**، ⛔ **ومؤشّرٌ عالقٌ يتجاهله المستخدم حين يصدق.** ⛔⛔★★ **وثالثَ عشرَ أوجهِ `DEBT-37`:** ★ **طبقةُ الاتصال لم تكن مُختبَرةً قطّ** (⛔ **صفرُ اختبارٍ قبل اليوم**). ✅ **والعلاج:** **فصلُ `watchProbes` عن `Firestore` وقراءةُ فشلِ المُصغي وسمَ انقطاعٍ يُبَثّ** ⛔ **لا استثناءً يُنهي المراقب** — ★ **والدلالةُ المعتمدة كما هي** ([`firestore_connection_monitor.dart`](../../lib/core/connectivity/firestore_connection_monitor.dart)) · ✅ **و4 اختباراتِ ارتداد** [`firestore_connection_monitor_test.dart`](../../test/core/connectivity/firestore_connection_monitor_test.dart) · ⛅ **ومُثبَتٌ حيّاً: بعد خروجٍ ودخولٍ ⟵ «متصل».** ⚠️★ **ودرسٌ ثانوي مقيس:** **`yield*` تُمرِّر خطأ البثِّ الداخلي إلى الخارج بلا `catch`** ⟵ **والصوابُ `await for`** — ★ **كشفه اختبارٌ فاشل لا مراجعةٌ بالعين.** | ✅ **مُغلَق** | — | ✅ **مغلق (2026-09-02)** |
| ✅★★ `DEBT-84` | ⚠️★★ **منسدلُ سطر الجونية وحدَه لا يُطبّق تفضيلَ «إظهار وزن الحبة»** (2026-09-02 · **مقيسٌ على `Pixel_6_API_36`** · `AM-012` §4.4). ★ **ونصُّ التعديل صريح:** «**يظهر اسمه في كل الأماكن**» — ⟵ **وخمسُ قوائمَ تُطبّقه** (`items_screen` · `counted_intake` · `distribution` · `cash_sale` · `outflow`) ⛔ **وواحدةٌ لا** (`sack_intake_screen._optionsFor`): ⟹ ★ **فيقرأ المستخدم «عتود» في سطر الجونية و«عتود وزن (200 جرام)» في الوارد عدداً للنوع نفسِه** — ⛔ **وعرضٌ لا يتّسق مع نفسه أسوأ من عرضٍ لا يُظهر شيئاً.** ✅ **والعلاج:** **تمريرُ التفضيل إلى `_optionsFor`** ([`sack_intake_screen.dart`](../../lib/capabilities/inventory/presentation/sack_intake_screen.dart)) · ✅ **واختبارا ارتدادٍ** ([`sack_intake_screen_test.dart`](../../test/capabilities/inventory/presentation/sack_intake_screen_test.dart)). ⛔⛔ **وعرضٌ محضٌ لا يمسّ ما يُخزَّن** — ★ **`SackLineInput.itemName` يبقى الاسمَ المجرَّد.** ⚠️★★ **ويبقى معلَناً ما لم يُعالَج:** ⛔ **شاشتا «مخزون اليوم» و«التسعير اليومي» تعرضان الاسمَ من حركة الدفتر** (`ADR-0008`) ⟵ **ولا وزنَ حبةٍ فيها تُشتقّ منه اللاحقة**: ★ **فبقيتا بلا لاحقة** ⛔ **وهذا فارقٌ يُقال لا يُطوى.** | ✅ **مُغلَق** | — | ✅ **مغلق (2026-09-02)** |
| ✅★★★ `DEBT-85` | ⛔⛔★★★ **ضمارٌ يُنشأ عادةً لا يُقبَض منه ولا يُخصَم عليه أبداً** (2026-09-02 · **مقيسٌ على التجريبية رقماً برقم** · `FR-M12-03` · `BR-M12-11`). ★ **الجذر:** **`settlementStatus` كان يُكتَب داخل `_surplusApplicationWrites` وحدَها** ([`distribution.dart`](../../functions/lib/src/distribution.dart)) — **وهي تعود فارغةً متى لا فائض** ⟹ ⛔ **فالحقلُ غائبٌ عن كل ضمارٍ أُنشئ بلا فائض**، ★ **واستعلامُ الضمارات المفتوحة يُقيّد `settlementStatus whereIn [open, partiallyOpen]`** ([`firestore_receipt_directory.dart`](../../lib/capabilities/sales_receivables/infrastructure/firestore_receipt_directory.dart)) ⟵ **والحقلُ الغائب لا يطابق شرطاً.** ⛔⛔★★★ **والأثر المقيس:** **`dealer_balances/MQT-0001_SRC-001` = 62,600 ريال** ⛔ **بينما شاشةُ المقبوضات تقول «إجمالي الديون على المقوت: 350 ريال (1 ضمار مفتوح)»** — ★ **و`runQuery` على `distributions` يقرأ خمسةً من ستةٍ بلا الحقل إطلاقاً**، ⟵ **والوحيدُ الظاهر ضمارٌ وسَمَه خصمٌ سابق.** ⛔⛔ **ولم يكشفه أيُّ اختبار — بل كان اختبارٌ يُثبّته نصّاً** («⛔ ولا سجلَ فائض ⟵ لا كتابةَ تسوية إطلاقاً»). ✅ **والعلاج `_settlementWrites` تُستدعى في كل إنشاءٍ وتعديل** — ★ **والمُطبَّقُ سابقاً يُصان من `storedSettlement`** ⛔ **فلا يُمحى قبضٌ وصل** · ⛔ **وضمارُ الصفر بلا سابقةٍ لا يُكتب له سجل** (`ADR-0008` القاعدة 5 · `E-05`). ⛅★★★ **ومُثبَتٌ حيّاً على `qtms-callables-00026-w4v`:** **`settlementStatus = open`** ⟵ **والشاشة صارت «5350 ريال (2 ضمار مفتوح)»** ⟹ ✅ **ثم قُبض 2,000 فصار 3,350** (`RCP-20260902-0001`). | ✅ **مُغلَق** | — | ✅ **مغلق (2026-09-02)** |
| ✅★★★ `DEBT-86` | ⛔⛔★★★ **ما يدخل المخزنَ من جونية لا يمكن توزيعُه ولا بيعُه ولا صرفُه إطلاقاً** (2026-09-02 · **مقيسٌ على `Pixel_6_API_36`** · [`ADR-0007`](adr/ADR-0007-composite-item-naming.md) · `FR-M10-06`). ★ **الجذر:** **سطرُ الجونية يدخل الدفترَ بمفتاحٍ مركّب** (`{اسم النوع} - جونية رقم N`) ⛔ **بينما منسدلاتُ التوزيع والبيع النقدي والسحبيات تُبنى من كتالوج الأنواع بمعرّفاتِ `itemId`** — ⟵ **فلا يظهر المفتاحُ المركّب في أي قائمة اختيار**، ★ **ولا رصيدَ يقابله في خريطة `remaining`** ⟹ ⛔⛔ **والسحابة ترفض بـ«❌ الكمية غير كافية — المتوفر أقل من المطلوب».** ★★ **مقيسٌ في الاتجاهين:** **«مخزون اليوم» يعرض «عتود — وارد 100 حبة»** ⛔ **وتوزيعُ 10 منها يُرفَض**، ✅ **بينما «سلة» (واردٌ عدداً بمفتاحٍ مجرَّد) تُوزَّع فوراً** (`DST-20260902-0001`). ⛔⛔★★★ **وهو نقضٌ لنصِّ `ADR-0007` نفسِه** — ★ **الخيار 2 رُفض حرفياً لأن «المستخدم لا يرى الفرق في قوائم الاختيار»**، ⟵ **والخيارُ المعتمَد وعد بأن «التمييز مرئي في كل قائمة اختيار وكل شاشة مخزون»**: ⛔ **والقائمةُ اليوم لا تحمله.** ★ **والعلاجُ المستنتَج من `ADR-0007` نفسِه: تُبنى خياراتُ الصرف من أرصدة الدفتر (`itemKey` + `itemName`) لا من الكتالوج** — ★ **مع بقاء `sackId` في السطر** (`distribution-design.md` §2)، ⛔ **ويمسّ ثلاث زياداتٍ منجَزة** (`WU-006` · `WU-012` · `WU-014`) ⟹ **فيستحق تشغيلاً خاصاً به.** ✅★★★ **سُدِّد 2026-09-02 في تشغيلٍ خاصٍّ به — وبثلاثة أشطر لا واحد:** ① ★ **`stockOptionsProvider` يبني الخيارات من أرصدة الدفتر** (`inventory_providers.dart`) — ⟵ **وتقرؤه الشاشاتُ الثلاث**، ⛔ **وحُذفت مزوّداتُ الكتالوج الثلاثة وحلَّ محلَّها شاهدٌ يشرح العلّة.** ② ⛔⛔★★★ **و`ledgerSackIds` تشتقّ `sackId` من حركة الدخول داخل المعاملة** (`inventory_handler.dart`) — ★ **وهو وجهٌ ثانٍ للعطل لم يكن ظاهراً:** **التطبيق لم يُرسِل `sackId` قطّ**، ⟹ **فكان فتحُ المفتاح المركّب وحدَه سيُنتج حركاتِ خروجٍ بلا مرجعِ جونية** ⟵ **فتسقط من احتساب إيرادها بصمت** (`ADR-0007` ⑤ · `FR-M10-14`). ③ ★★ **و`ledgerItemDisplayName` تعرض الاسمَ المركّب** (`sack_intake.dart` — **بانٍ وقارئٌ في ملفٍ واحد**): ⟵ **لأن الحركة تُخزِّن المجرَّد في `itemName` والمركّبَ في `itemKey`**، ⛔ **فجونيتان من نوعٍ واحد كانتا تُعرَضان سطرين متطابقين** — ★ **وهو عينُ الخيار 2 المرفوض.** ★★ **ومقيسٌ حيّاً على `Pixel_6_API_36` بالمراجعة `qtms-callables-00027-z5h`:** **`DST-20260902-0001` صار «2 نوع»** · **`CSH-20260902-0001` سكربٌ وزنيٌّ 0.500 كجم × 250 = 125 ريال** · **`WDR-20260902-0001` خمسُ حبات** — ⟹ **وثلاثُ حركاتِ خروجٍ تحمل `sackId: SCK-20260902-0001`** ⛔ **بلا أن يُرسِله التطبيق.** ★ **و24 اختبارَ ارتداد.** | ⛔⛔ **حرج — قدرةٌ أساسيةٌ في المرحلة الأولى معطَّلةٌ كلياً** (مسارُ `M7` ⟶ `M10`) | ✅ **سُدِّد 2026-09-02** | ✅ **مُغلَق** |
| ✅★★★ `DEBT-87` | ⛔⛔★★★ **تعديلُ توزيعةٍ يُفرِّغ سعرَها صامتاً** (2026-09-02 · **مقيسٌ على `Pixel_6_API_36`** · [`ADR-0011`](adr/ADR-0011-financial-fields-isolation.md)). ★ **الجذر:** **`_seedFrom` كان يبذر النوعَ والكميةَ وحدَهما** ⟵ **وحقلُ السعر يُفتَح فارغاً**، ⛔⛔ **وحفظُه يُرسِل `unitPrice: null`** ⟹ **فيُقرأ «تفريغَ سعرٍ قائم»** (`distributionPriceClear`) ★ **فتنهار قيمةُ الضمار** — ⛔ **ومن عدّل الكمية وحدَها خسِر السعر.** ⚠️★★ **وأولُ علاجٍ لم يكفِ — ويُقال:** ★ **البذرُ من `card.lines[].unitPrice` لا يقع أبداً** ⟵ **فالمستندُ الأب بلا أسعارٍ بالتصميم** (`ADR-0011`): ✅ **والصوابُ البذرُ من `pricing/current` عبر `distributionPricingProvider`** ⛔ **مع تأجيل البذر ما دام يُحمَّل** (`isLoading`) ★ **وإلا قُفل البذرُ على فارغ.** ✅ **وثلاثةُ اختباراتِ ارتداد** ([`distribution_screen_test.dart`](../../test/capabilities/sales_receivables/presentation/distribution_screen_test.dart)) · ⛅ **ومُثبَتٌ حيّاً: الحقل يفتح على `500`.** | ✅ **مُغلَق** | — | ✅ **مغلق (2026-09-02)** |
| ✅★★ `DEBT-88` | ⛔⛔★★ **منسدلُ النوع يبدو فارغاً والنوعُ مختارٌ فعلاً** (2026-09-02 · **مقيسٌ على `Pixel_6_API_36`** · `AM-009` ④). ★ **الجذر:** **`DropdownMenu` يقرأ `initialSelection` عند إنشائه وحدَه** — ⟵ **وقائمةُ الخيارات تصل من بثِّ الأنواع بعده**: ⛔ **فلا يُحدَّث النصُّ المعروض أبداً.** ⚠️ **وأثرُه في نموذج التعديل:** ★ **الكميةُ والسعرُ مبذوران والنوعُ يبدو فارغاً** ⟹ **فيُقرأ فقداناً للمُدخَل** ⛔ **ويُعيد المستخدم اختيارَه أو يحفظ ظانّاً أنه فارغ.** ✅ **والعلاج مفتاحٌ يجمع المختارَ وعددَ الخيارات** ([`item_line_editor.dart`](../../lib/core/ui/item_line_editor.dart)) — ⛔ **لا نصَّ الفلترة**: ⟵ **فلا يُهدَم المنسدلُ وهو يُكتَب فيه.** ✅ **وثلاثةُ اختباراتِ ارتداد** ([`item_line_editor_test.dart`](../../test/core/ui/item_line_editor_test.dart)) — ★ **وهذه الطبقةُ لم تكن مُختبَرةً قطّ: رابعَ عشرَ أوجهِ `DEBT-37`.** | ✅ **مُغلَق** | — | ✅ **مغلق (2026-09-02)** |
| ✅★★★ `DEBT-89` | ⛔⛔★★★ **ثالثُ وجوه `IQ-024`: سردُ `outflows` بلا تقييد `ledgerType` يُرفَض كاملاً** (2026-09-03 · **مقيسٌ على `Pixel_6_API_36`** · `WU-015`). ★ **العَرَض:** **ورقةُ تفكيك سعر الجونية ردّت «تعذّر عرض البيانات»** ⛔ **بحساب QA يملك المفاتيح الخمسة والثمانين كلَّها** — ★ **فبدا رفضَ صلاحيةٍ وليس كذلك.** ★ **الجذر:** **شرطُ قراءة `outflows` يتفرّع على `resource.data.ledgerType`** (`withdrawalView` أو `expenseView` — [`firestore.rules`](../../firestore.rules)) ⟵ **والشرطُ يُقيَّم على قيود الاستعلام لا على كل مستند**: ⛔ **فاستعلامٌ يُقيّد `sourceId` و`stockDate` وحدَهما لا يُثبِته فيُرفَض كاملاً.** ⚠️⚠️★★★ **ولم يكشفه واحدٌ من 2306 اختباراً — وهذا أهمُّ ما فيه:** ★ **القارئُ السحابي `sack_movement_reader.dart` يعمل بحساب خدمةٍ يتخطّى القواعد** ⟹ **فالمخزَّن كان صحيحاً تماماً والشاشةُ وحدَها تفشل** — ★ **وهو درسُ `DEBT-37` في وجهه الخامسَ عشرَ: اختبارُ الطبقة لا يُغني عن اختبار ما يعبر بينها.** ✅ **والعلاج تقييدُ الحقل صراحةً — استعلامان لا واحد** ([`firestore_sack_valuation_directory.dart`](../../lib/capabilities/inventory/infrastructure/firestore_sack_valuation_directory.dart)) ⛔⛔ **لا تخفيفُ القاعدة** (`CLAUDE.md`) — ⛅ **ومقيسٌ حيّاً: الاستعلامان ردّا `200`** ⛔ **بلا فهرسٍ جديد** (**تساوٍ محض**). ✅ **وثلاثةُ اختباراتِ ارتداد في طبقة القواعد نفسِها** ([`rules.test.js`](../../test/security-rules/rules.test.js)) — ★ **ومنها أن مالكَ مفتاحٍ واحد لا يسرد شقَّ الآخر.** | ✅ **مُغلَق** | — | ✅ **مغلق (2026-09-03)** |
| ✅★★★ `DEBT-90` | ⛔⛔★★★ **ورقةُ تفكيك السعر تُعيد لقطةً قديمة بعد أول فتحة** (2026-09-03 · **مقيسٌ على `Pixel_6_API_36`** · `WU-015` · [`ADR-0008`](adr/ADR-0008-derived-balances-and-prebuilt-summaries.md)). ★ **العَرَض المقيس حرفياً:** **فُتحت الورقةُ وهي فارغة، ثم سُجِّلت توزيعةٌ فعلية بـ20000 من نوعٍ في الجونية، ثم أُعيد فتحُها** ⟹ ⛔ **فقالت «لم يخرج من هذه الجونية شيء بعد» بينما `finance/current` يقول `sackRevenue: 20000`** — ★ **رقمان لحقيقةٍ واحدة على شاشةٍ واحدة.** ★ **الجذر:** **`sackBreakdownProvider` كان `FutureProvider.family` بلا `autoDispose`** ⟵ **ووسيطُه ثابتٌ** (الجونية والمصدر واليوم)، ⟹ **فالقراءةُ تُحسَب مرةً وتُخزَّن بمفتاح العائلة ما بقي لها مستمع.** ⛔⛔★★ **وهذا يناقض `ADR-0008` في جوهره** — ★ **«الرصيد مشتقٌّ من الدفتر دائماً»**: ⟵ **فلا يُعرَض رقمٌ لا يُعاد اشتقاقُه.** ✅ **والعلاج `autoDispose`** ([`sack_valuation_providers.dart`](../../lib/capabilities/inventory/application/sack_valuation_providers.dart)) — ★ **الورقةُ مستمعُها الوحيد**: ⟹ **إغلاقُها يُسقِط الحالة وفتحُها يقرأ من جديد.** ✅ **واختبارُ ارتدادٍ يَعُدُّ القراءات فعلاً** (`breakdownCallCount` في [`fake_sack_valuation.dart`](../../test/support/fake_sack_valuation.dart)) ⛔ **لا يكتفي بالنصّ المعروض** — ★ **ويسقط على الكود القديم.** ⚠️★★ **ودرسُه أوسعُ من موضعه:** ★ **لا `autoDispose` واحدةٌ في `lib/` كلِّها** ⟵ **فكلُّ `FutureProvider` قراءةٍ عند الطلب يُضاف لاحقاً يرث الفخَّ نفسَه.** | ✅ **مُغلَق** | — | ✅ **مغلق (2026-09-03)** |
| ✅★★★ `DEBT-91` | ⛔⛔★★★ **بطاقةُ ضمار المالك تبقى على الهيكل العظمي أبداً في أول يومٍ من كل يوم** (2026-09-03 · `WU-016` · **كشفه اختبارُ المحاكي `Pixel_6_API_36` وحده**). ⛅★★ **مقيسٌ حيّاً لا مُخمَّن:** ★ **`adb logcat` أعطى `PERMISSION_DENIED` على `daily_summaries/all_20260903` وعلى `owner_ledger_trends/all` معاً** — ⛔ **والقارئُ حسابُ QA بكل المفاتيح الـ85 ونطاقٍ شامل.** ⛔⛔★★★ **والجذرُ أن المستند *غائب*:** ★ **شرطُ القراءة يعتمد `resource.data.sourceId`** (`storedInScope()`)، ⟵ **والمستندُ الغائب بلا `resource` أصلاً فيُقيَّم الشرطُ `false`** — ⛔ **والغيابُ هنا هو الحالةُ الطبيعية تماماً: يومٌ لم تقع فيه عمليةٌ بعد**، ★ **وهو اليومُ الذي يشترط `§9` أن يُعرَض فيه بأصفارٍ صريحة.** ⚠️⚠️★★ **وهو `DEBT-40` نفسُه ثانيَ مرة** — ★ **ودرسُه أن العلاج لم يكن قاعدةً عامةً محفوظة**: ⟵ **بل عولج في `distributions` وحدها فعاد في مجموعةٍ جديدة.** ✅★★ **والعلاج تقييدُ الاستعلام** (`sourceId` **و**`date` **صراحةً بفهرسٍ قائم**) ([`firestore_owner_ledger_directory.dart`](../../lib/capabilities/financial_outflow/infrastructure/firestore_owner_ledger_directory.dart)) ⛔ **لا تخفيفُ القاعدة** — ✅ **والقاعدة سليمة ولم تُمَسّ بحرف.** ✅★★ **وثلاثةُ حرّاسِ ارتدادٍ على المحاكي** ([`rules.test.js`](../../test/security-rules/rules.test.js)) **يقيسون العطلَ والعلاج معاً:** ★ **قراءةُ المعرّف الغائب تُرفَض · والاستعلامُ المقيَّد يُرجِع صفراً** — ⛔ **لا وصفاً.** ⛅★★★ **وإثباتٌ حيٌّ بعد الإصلاح:** **البطاقةُ عُرضت بأصفارٍ صريحة لكل المصادر ولمصدرٍ بلا حركة** ⛔ **ولا سطرَ رفضٍ في `adb logcat`.** | ⛔ **عالٍ — أهمُّ بطاقةٍ للمالك لا تُعرَض** | ✅ **تقييدُ الاستعلام + ثلاثةُ حرّاس + إثباتٌ حيّ** | ✅ **مُغلَق — 2026-09-03** |
| ✅★★ `DEBT-93` | ⛔⛔★★ **فهرسُ سرد السحبيات والخرجيات كان ناقصاً منذ `WU-014`** (رُصد 2026-09-03 · `WU-016`). ★ **`firestore_outflow_directory.watchOutflows` يُقيّد `sourceId` و`ledgerType` ثم يُرتّب بـ`documentDate`** — ⟵ **وذلك يستلزم فهرساً مركّباً `sourceId ↑ · ledgerType ↑ · documentDate ↓` على `outflows`**، ⛔ **والفهرسُ الموجود بذلك الشكل كان على `outflow_ledger` لا على `outflows`.** ⚠️⚠️★★ **ولم يكشفه شيء:** ★ **محاكي Firestore لا يفرض الفهارس إطلاقاً**، ⟵ **فمرَّ اختبارُ `WU-014` الحيُّ كاملاً** — ⛔ **والفشلُ كان سيقع على أول قاعدةٍ حقيقيةٍ يُفتَح فيها سردُ السندات.** ★ **ورصدَه تعليقُ الدليل نفسِه وهو يدّعي أن الفهرس قائم** (بروتوكول التشغيل §و: **الادّعاءُ يُتحقَّق منه لا يُصدَّق**). ✅ **والعلاج إضافةُ الفهرس ونشرُه** ([`firestore.indexes.json`](../../firestore.indexes.json) — **44 فهرساً مقيسةً بـ`gcloud firestore indexes composite list`**). | ⚠️ **متوسط — سردٌ يفشل على قاعدةٍ حقيقية** | ✅ **فهرسٌ مُضاف ومنشور ومُتحقَّقٌ منه** | ✅ **مُغلَق — 2026-09-03** |
| ⏳★★ `DEBT-92` | ⚠️★★ **بندان من `FR-M15` مؤجَّلان معلَنَين** (2026-09-03 · `WU-016`) — ⛔ **لا سهو:** ① **`FR-M15-11` «كل رقم في البطاقة قابل للتفكيك»** — ★ **البطاقةُ تقبل `onRowTap` في عقدها بالفعل** ([`owner_ledger_card.dart`](../../lib/capabilities/financial_outflow/presentation/owner_ledger_card.dart))، ⛔ **ولا ورقةَ تفكيكٍ موصولةٌ به بعد**: ⟵ **والتفكيكُ يحتاج سردَ الحركات المكوِّنة لكل بند** (**أربعةُ استعلاماتٍ لكل صف**) ★ **وهو نظيرُ `sack_breakdown_sheet.dart` في `WU-015`.** ② **و`FR-M15-23` «مشاركةُ ملخص اليوم صورةً أو PDF»** — ★ **البنيةُ قائمةٌ من `WU-010`** (`documentExport` · `logExport` · `pdf_tokens.dart`)، ⛔ **ولا قالبَ لملخّص اليوم في `message_templates.dart`**: ⟵ **وقوالبُ `FR-M20-06` أربعةٌ محسومةٌ بـ`IQ-031` ليس فيها هذا** — ★ **فنصُّه قرارُ مالكٍ لا اجتهاد.** ✅ **وما نُفِّذ من `FR-M15-23`:** **مقارنةُ الأمس** (`deltaLabel`) · **ورسمُ آخر سبعة أيام** · **وتنبيهُ فجوة النقد** — ⛔ **والمشاركةُ وحدها المتبقية.** | ⚠️ **منخفض — عرضٌ لا صحّةُ رقم** | ⏳★★ **وأُعيد توجيهُه في `WU-018`** (2026-09-04) — ⛔ **ولم يُسدَّد فيه:** ★ **البندان من `FR-M15` لا من `FR-M19`** ⟵ **وسطرُ نطاق `WU-018` تقاريرُ `M19` وحدها** (`R-07` · `R-09` · `R-11`…`R-16` · `R-20`…`R-27`)، ⛔ **وضمُّهما كان توسيعَ نطاقِ زيادةٍ بلا قرارٍ من المالك.** ⚠️ **و② منهما قرارُ مالكٍ أصلاً** — **نصُّ قالبِ ملخّص اليوم** (`IQ-031` حسم أربعةَ قوالبَ ليس فيها هذا): ⟹ **فبناؤه اجتهاداً يُخالف `ADR-0020` القاعدةَ الباقية** ⛔ **«ما لم يكتبه إنسان لا يُرسَل».** ★ **يُسدَّدان مع أولِ زيادةٍ تمسّ بطاقةَ `M15` نفسَها أو بطلبٍ صريح** | ⏳ **مفتوح** |
| ⏳★★ `DEBT-94` | ⚠️★★ **`R-16` و`R-24` تقريرا يومٍ واحد — و«من–إلى» مؤجَّلٌ معلَناً** (2026-09-04 · `WU-018`) ⛔ **لا سهو:** ★ **`FR-M19` §2 يعرض لـ`R-16` «تاريخ / من–إلى» ولـ`R-24` «الفترة»**، ⛔ **والمبنيُّ تاريخٌ واحدٌ في كليهما.** ★★ **والسببُ مقيسٌ لا تقديري:** ⟵ **حركةُ النقد تُجمَع من الدفاتر عند القراءة لا من ملخّصٍ مبنيٍّ مسبقاً** (`daily_summaries` **مُعرَّفٌ بالبنود العشرة وحدها ولا حقلَ فيه لحركة النقد** — `schema/daily-summaries.md`)، ★ **وتجميعُها خمسةُ استعلاماتٍ لكل يومٍ لكل مصدر** (`FirestoreCashMovementDirectory`): ⟹ **ومدىً شهريٌّ لمصدرٍ واحد يُنتج مئةً وخمسين قراءة** ⛔ **بلا سقفٍ موثَّق** — ★ **والعتبةُ الرقمية غيرُ موثَّقة فتكون بند `IQ`** (`implementation-playbook.md` §5) ⛔ **لا رقماً يُخترَع في الكود.** ✅ **والمبنيُّ نظيرُ `R-02` و`R-05` حرفياً** — ★ **يومٌ واحدٌ بمنتقي تاريخٍ واحد** (`isSingleDayReport`)، ✅ **ومعلَنٌ في سطر فلاتر التقرير نفسِه** («المصدر · التاريخ») ⛔ **فلا يَعِد بما لا يفعل** (`DEBT-72` **نفسُ المبدأ**). ⚠️ **والعلاجُ حين يُطلَب أحدُ أمرين لا ثالث:** **① عتبةُ مدىً يحسمها المالك بـ`IQ`** · **② أو حقلُ حركةِ نقدٍ في ملخّص اليوم** ⟵ **وهو توسيعُ مخطَّطٍ يحتاج قراراً.** | ⚠️ **منخفض — تقريرٌ يعمل بمداه المعلَن** ⛔ **ولا رقمَ خاطئ فيه** | ★ **بـ`IQ` من المالك أو بتوسيع مخطَّط الملخّص** | ⏳ **مفتوح — معلَن** |
| ⚠️★★ `DEBT-95` | ⛔⛔★★ **جونيةٌ ملغاةٌ وسطرُها في دفتر الرعية يقول `isCancelled: false`** — ★ **رصدَه تشغيلُ `WU-018` بقراءةٍ مباشرة لا بافتراض** (2026-09-04 · التجريبية). ⛅★★ **والقياسُ حرفياً:** `sacks/SCK-20260903-0002` ⟵ `status: cancelled` · `supplier_ledger/SCK-20260903-0002` ⟵ `isCancelled: false` — ★ **والكاتبُ يكتب `isCancelled: sack.isCancelled`** ([`sack_valuation.dart`](../../functions/lib/src/sack_valuation.dart) — `_ledgerWrite`): ⟹ ★ **فالأرجحُ أن إلغاءَ الجونية لا يُطلِق إعادةَ الاحتساب**، ⛔ **ولم يُقَس ذلك في هذا التشغيل** (**خارج نطاق `WU-018`: الكاتبُ من `WU-015`**). ⚠️ **والأثرُ الرقمي اليومَ صفر** — ★ **إيرادُ تلك الجونية `0`**: ⟵ **لكنه لن يكون صفراً لجونيةٍ صُرف منها ثم أُلغيت**، ⛔ **فتبقى في `عدد الجواني` وفي `supplier_balances` كأنها حيّة** (`A-14`). ✅★★ **وتقريرا `R-25` و`R-26` بريئان من هذا** — ★ **يقرآن الحقلَ كما كُتب ويُقصيان الملغاة فعلاً** ⛔ **ولا يخترعان حالة**: ⟵ **فالعلاجُ في الكاتب لا في القارئ.** ★ **والسدادُ:** ① **يُقاس أولاً بإلغاء جونيةٍ ذاتِ إيرادٍ على التجريبية وقراءةُ سطرها** · ② **ثم يُصلَح مُطلِقُ إعادة الاحتساب إن ثبت** | ⚠️ **متوسط — رقمُ رعويٍّ قد يتضخّم بجونيةٍ ملغاة** ⛔ **ولا أثرَ اليومَ لأن إيرادَها صفر** | ★ **مع أولِ زيادةٍ تمسّ `M14` أو بطلبٍ صريح** | ⚠️ **مفتوح — معلَن** |
| ⚠️★★ `DEBT-80` | ⚠️⚠️★★★ **كلمةُ مرورٍ حيّةٌ تُخزَّن على الجهاز عند تفعيل البصمة** (2026-09-02 · [`ADR-0024`](adr/ADR-0024-biometric-unlock-with-os-protected-credentials.md) §العواقب). ⛔⛔ **وهذا ليس عطلاً بل مقايضةٌ اختارها المالك صراحةً** بعد عرض البديل عليه (`AM-012` §2 السؤال ④) — ★ **والبديلُ المرفوض كان «قفلاً محلياً بلا تخزين»**، ⛔ **وعيبُه أنه لا يعمل بعد «تسجيل خروج» صريح** ⟵ **وهي الحالةُ الشائعة لا النادرة بعد `AM-009` ①.** ★★ **والتخفيفُ مُنفَّذٌ ومُختبَر:** **`Keystore` بمفتاحٍ لا يغادر العتاد** (الشرط 1) · **ولا قراءةَ بلا مصادقةٍ بيومترية** (الشرط 2) · **ولا عرضَ ولا ملءَ حقل** (الشرط 3) · **ومسحٌ فوريٌّ عند الإطفاء وعند أول فشل** (6 و7). ⚠️ **والخطرُ الباقي معلَن:** ★ **جهازٌ مسروقٌ مأصولٌ (Rooted) يُضعف `Keystore`** — ⛔ **ولا تخفيفَ كاملاً له**، ★ **وهو نفسُ نموذج كل تطبيقٍ بنكيٍّ على المنصّة.** ⛔⛔ **ويُعاد النظر فيه إن تغيّرت طبيعةُ الأجهزة الميدانية** ⛔ **لا قبل ذلك.** | ⚠️ **متوسط — سطحٌ هجوميٌّ أوسع على جهازٍ مفقود** ⛔ **محصورٌ بمن يملك الجهاز وبصمةَ صاحبه معاً** | ★ **مراجعةٌ عند تغيّر نموذج الأجهزة** — ⛔ **لا إجراءَ الآن** | ⚠️ **مقبولٌ بقرارٍ — معلَن** |
| ⚠️★ `DEBT-81` | ⚠️★★ **`flutter_secure_storage` مثبَّتٌ على `10.0.0` بقيدِ بناءٍ لا باختيار** (2026-09-02 · `AM-012`). ⛔ **`11.x` يفرض `compileSdk 37`** ⟹ **سقط البناءُ فعلاً** (`Failed to find target with hash string 'android-37'`) — ⛔⛔ **ورفعُ `compileSdk` يخالف [`AM-011`](../00-governance/upds-workflow/amendment-log.md) نصّاً** («**compileSdk/targetSdk يبقيان كما هما**»). ⛔ **و`9.x` يتعارض مع `share_plus 13.x` عبر `win32`.** ⟹ ✅ **و`10.0.0` وحدَه يجمع الثلاثة.** ⚠️ **والأثرُ المعلَن:** ★ **الحزمةُ ستتقادم** ⟵ **وأولُ تشغيلٍ يرفع `compileSdk` إلى 37 يجب أن يرفعها معه**، ⛔ **ولا يُترك القيدُ يُكتشَف عند أول `pub upgrade`.** ★ **والقيدُ مكتوبٌ في ترويسة `secure_credential_vault.dart` وفي §7 من سياسة الاعتماديات** ⟵ **فلا يُقرأ تفضيلاً.** | ⚠️ **منخفض — بناءٌ يعمل اليوم** ⛔ **ولا أثرَ وظيفي** | ★ **مع أول رفعٍ لـ`compileSdk`** | ⚠️ **مفتوح — معلَن** |
| ★ `DEBT-19` | ★ **تصنيف أخطاء خدمة المصادقة يعتمد مطابقة نصّ الرسالة** — `classifyIdentityApiError` في [`identity_gateway.dart`](../../functions/lib/src/identity_gateway.dart) يقارن رسالة المنصة بقائمة صريحة (`INVALID_ID_TOKEN` · `TOKEN_EXPIRED` · …) لأن الواجهة **لا تُرجِع رمز خطأ مُصنَّفاً** بل نصّاً. ⚠️ **فتغيير المنصة لنصّ الرسالة يُسقِط التصنيف صامتاً**، فيعود المستخدم يرى `HTTP 500` بدل `ERR_AUTH_003`. ★ **والاتجاه الآمن مقصود:** ما لا يُصنَّف **يُعاد رميه خاماً** ⛔ ولا يُقنَّع جلسةً منتهية — فالعطل يبقى ظاهراً. ★ **والتخفيف المطلوب: فحص حيّ دوري** على رمز منتهٍ فعلاً يُثبت أن التصنيف ما يزال يعمل (`authentication-policy.md` §7 البندان 1 و2) | **اعتمادية على تفصيل غير موثَّق** | ★ **قبل أول نشر إنتاجي** | ★ **عالية** |

> ★ **لماذا `DEBT-09` حرج:** `ADR-0009` ② اعتمد **Dart لغةً وحيدة للعمليات
> السحابية** لأن أي لغة أخرى تعني **كتابة كل معادلة مالية مرتين** — وهو ما
> يمنعه `coding-standards.md` §2.2 صراحةً. لكن Dart **ليست بيئة تشغيل من
> الدرجة الأولى** في Cloud Functions، فالمسار عبر Cloud Run + Eventarc.
> **بناء الدوال السبع قبل إثبات المسار مخاطرة إعادة عمل كاملة.**
> ⛔ **وإن فشلت العيّنة، الانتقال للبديل يحتاج ADR جديداً** يفرض **متجهات
> اختبار مشتركة** تُشغَّل على التنفيذين وتفشل البناء عند أي اختلاف — لا
> انتقالاً صامتاً.

---

## 3. ✅ بنود كانت محجوبة — كلها مغلقة

| البند | القيمة المعتمَدة |
|---|---|
| ✅ **المنهجية** | ★ **تزايدية** — [`../21-project-management/development-methodology.md`](../21-project-management/development-methodology.md) |
| ✅ **معادلة باقي الضمار** | ★ **صفّان: قبل الخصم وبعده** — والصافي على الثاني |
| ✅ **بيئات المشروع والموقع** | `qtms-orimind-c001` · `qtms-orimind-master` · ★ **`me-central1`** *(`ADR-0014`)* |
| ✅ **معرّف الحزمة والاسم** | `dev.orimind.qtms.c001` · ★ «وكالة محمد المحامي» *(`AM-002`)* |
| ✅ **الأيقونة والخط** | من شعار العميل · **IBM Plex Sans Arabic** |

### 3.1 ⚠️ مخاطر متبقية بعد الإغلاق

| البند | الحالة |
|---|---|
| ⚠️ **الأرقام الهندية متغيّرة العرض في الخط المعتمَد** | ★ **خيار الأرقام الهندية ملغى** — [`../19-assets/fonts/README.md`](../19-assets/fonts/README.md) §3 |
| ⚠️ **نص الأيقونة يتجاوز الدائرة الآمنة جزئياً** | **انحراف موثَّق ومقبول بقرار العميل** |
| ⚠️ **أيقونة البيئة التجريبية** | ★ **مطلوبة قبل أول نشر تجريبي** — تمنع الخلط بين التطبيقين |
| ⚠️ **الوزن 700 غير مضمَّن** | يُضاف عند بناء أول شاشة تحتاجه |

---

## 4. المراجعة الدورية

| الدورة | النشاط |
|---|---|
| **أسبوعياً** | مراجعة الدَّين التقني وتحديث حالته |
| **عند كل بوابة معلم** | **تصعيد أي دَين حرج لم يُعالَج قبل الانتقال** (UPDS-05 §7) |
| **سنوياً** | مراجعة شاملة لـ`architecture-overview.md` وإعادة تقييم `RISK-09` |
