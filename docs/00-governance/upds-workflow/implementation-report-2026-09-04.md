# تقرير تنفيذ برمجي — UPDS DocGen — 2026-09-04

**رقم تشغيل التنفيذ:** 34 (**الأول في 2026-09-04**)
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة — شريحة رأسية كاملة**)
**تقدّم التنفيذ:** 72.4٪ (21 منجَزة من 29)
**حالة التوثيق وقت هذا التشغيل:** 100.0٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** 1 (`IQ-040` — تحجب `WU-017` وحدها)

---

## 1. ملخص تنفيذي

★★ **بُنيت `WU-018` كاملةً وأُغلقت:** **أربعةَ عشرَ تقريراً جديداً** (`R-09` ·
`R-11` · `R-12` · `R-13` · `R-15` · `R-16` · `R-20` · `R-21` · `R-22` · `R-23` ·
`R-24` · `R-25` · `R-26` · `R-27`) ⟹ **فكتالوجُ التقارير صار 25 من 31**،
★ **وسُدِّد أربعةٌ من فلاتر [`DEBT-72`] الستة** (الرعوي · الجونية · المقوت ·
النوع). ⛔⛔ **وقرارا نطاقٍ معلَنان لا سهوَ فيهما:** `R-07` **مؤجَّلٌ إلى
`WU-020`** (⛔ **لا كاتبَ للإتلاف بعد، وعنوانُه المعتمد يَعِد به**) ·
و`R-16` و`R-24` **بتاريخٍ واحدٍ لا مدى** ([`DEBT-94`]).

⛅★★★ **والزيادةُ مُثبَتةٌ حيّاً لا آلياً وحسب:** **19 سيناريو نجاح + 6 مسارات
حدّية على `Pixel_6_API_36`** ببياناتٍ حقيقيةٍ من التجريبية — ✅ **وصفرُ
`PERMISSION_DENIED` وصفرُ `FAILED_PRECONDITION` في `adb logcat`** (مقيسٌ
بالعدّ). ★★ **وثلاثةُ أرقامٍ تقاطعت من مساراتِ بناءٍ مختلفة** (`R-20` و`R-23`
⟵ **6,800** · `R-25` و`R-27` ⟵ **20,000**) — ⟵ **وهو أقوى ما في هذا التشغيل.**

⚠️ **ورصد التشغيلُ عطلاً خارج نطاقه ولم يُصلحه** ([`DEBT-95`]): **جونيةٌ ملغاةٌ
وسطرُها في دفتر الرعية يقول `isCancelled: false`** — ★ **والعلّةُ في كاتب
`WU-015` لا في قارئ `WU-018`.**

---

## 2. بوابة الدخول — نتيجة الفحص

### 2.1 البوابة العامة (٨ شروط — `implementation-playbook.md` §1.1)

| # | الشرط | متحقق؟ | المسار المفحوص |
|:-:|---|:-:|---|
| 1 | المنهجية مُصرَّح بها | ✅ | `docs/21-project-management/development-methodology.md` |
| 2 | العمارة والمكدّس | ✅ | `docs/03-architecture/architecture-overview.md` · `technology-stack.md` |
| 3 | أسلوب الكود والتسمية ومعالجة الأخطاء | ✅ | `docs/04-design/coding-standards.md` · `naming-conventions.md` · `error-handling-strategy.md` |
| 4 | تعريف «منجَز» والتفرّع والالتزام والإصدار | ✅ | `docs/07-development/definition-of-done.md` · `branching-strategy.md` · `commit-message-conventions.md` · `docs/12-deployment-and-release/versioning-scheme.md` |
| 5 | استراتيجية الاختبار | ✅ | `docs/08-testing-and-qa/test-strategy.md` |
| 6 | سياسة الأسرار | ✅ | `docs/09-security/secrets-management-policy.md` |
| 7 | `srs.md` بمتطلبات مرقّمة | ✅ | `docs/02-requirements/srs.md` |
| 8 | ⛔ **لا `ADR` بحالة «مقترح» يحكم ما نُفِّذ** | ✅ | **24 `ADR` — كلُّها «مقبول/معتمد»** (فُحصت واحداً واحداً بحقل الحالة) |

### 2.2 بوابة الوحدة `WU-018` (٥ شروط — §1.2)

| # | الشرط | متحقق؟ | المسار |
|:-:|---|:-:|---|
| 1 | تصميمُ الوحدة | ✅ | `docs/04-design/module-design/reporting-design.md` |
| 2 | المتطلب بمعايير قبول | ✅ | `docs/02-requirements/functional-requirements/FR-M19.md` |
| 3 | عقودُ الواجهات (إن مسّت API) | ✅ | ⛔ **لا واجهةَ جديدة** — **الزيادةُ قراءةٌ محضة** (`ADR-0013` القاعدة 4) |
| 4 | التخزين | ✅ | `docs/06-database/erd.md` · `data-dictionary.md` · `schema/supplier-ledger.md` · `schema/cash-sales.md` |
| 5 | المصادقة والصلاحيات | ✅ | `docs/09-security/authentication-policy.md` · `permissions-catalog.md` §2.7 و§2.11 |

★★ **وبروتوكولُ التشغيل §ح مُطبَّق:** **`design-system.md` و`design-tokens.md`
و`ui-guidelines.md` قُرئت قبل الكتابة** — ⟵ **والشاشتان لم تُضيفا قيمةَ لونٍ ولا
حجمَ خطٍّ ولا نصفَ قطرٍ واحدة**: ★ **كلُّ ما أُضيف شرائحُ `QtmsFilterOption`
وأعمدةُ `QtmsDataTable` القائمة** ⛔ **ولا طبقةَ أوليةَ لُمست** (✅ **وبوابةُ
`design_gates_test.dart` ضمن `flutter test` الناجح**).

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

| WU | العنوان | الملفات المنتَجة/المعدَّلة | الفحوصات التي شُغِّلت ونتيجتها الفعلية |
|---|---|---|---|
| `WU-018` | **زيادة: تقارير المرحلة الثانية** | ★ **النطاق:** `report_catalog.dart` · `report_repository.dart` · `report_builders.dart` · `cash_sale_repository.dart` · `sack_valuation.dart` · ★ **التطبيق:** `firestore_report_directory.dart` · `report_providers.dart` · `report_view_screen.dart` · `reports_screen.dart` · `firestore_cash_sale_directory.dart` · `firestore_sack_valuation_directory.dart` · `sack_breakdown_sheet.dart` · ★ **الفهارس:** `firestore.indexes.json` · ★ **اختباران جديدان** | ✅ **`dart analyze` × 3 حزم — نظيفٌ كلُّه** · ✅ **`dart test` (نطاق) 761** · ✅ **`dart test` (سحابة) 703** · ✅ **`flutter analyze` نظيف** · ✅ **`flutter test` 738** · ✅ **`npm test` (قواعد) 253** · ✅ **`check:coverage` صفرُ فجوة** · ⛅ **نشرُ الفهارس والتحقق البعدي — 45** · ⛅★★ **المحاكي: 19 نجاح + 6 حدّية** |

★★ **الإجمالي المقيس: 2455 اختباراً · ٠ فاشلاً** (**761 نطاق + 703 سحابة +
738 تطبيق + 253 حماية**) — ⟵ **و+61 عن التشغيل السابق** (47 نطاق + 14 تطبيق).

### 3.2 وحدات قيد التنفيذ

⛔ **لا وحدة** — ★ **`WU-018` فُتحت وأُغلقت في هذا التشغيل نفسِه.**

### 3.3 وحدات محجوبة (ولماذا)

| WU | العنوان | محجوب بسبب | ما الذي يلزم لرفع الحجب |
|---|---|---|---|
| `WU-017` | كشف حساب المقوت من طرف إلى طرف | `IQ-040` | ★ **قرارُ المالك في مفتاحَي «عرض كشف الحساب» و«كشف موحّد لكل المصادر»** — ⛔ **ولا يُخمَّن مفتاحُ تفويضٍ في نظام ذمم** (`IQ-021` نصّاً) |

⚠️★★ **وحلقةُ المرحلة انتهت هنا بنصّ بروتوكول التشغيل §أ.3:** ★ **كلُّ وحدات
المرحلة الثانية صارت `[منجز ✅]` عدا `WU-017` المحجوبة** — ⛔ **ولا وحدةَ
`[مخطط]` قابلةٌ للبدء في هذه المرحلة**، ⛔⛔ **ولا تبدأ وحداتُ المرحلة الثالثة
في هذا التشغيل** (§أ.5).

⛔⛔★★★ **والمرحلةُ الثانية لم تُعلَن مكتملة** — ★ **وشرطُها في §أ.5 صريح:**
**اختبارٌ تكامليٌّ شاملٌ للمرحلة على المحاكي ثم مطابقةُ `AT-26`…`AT-28` و
`AT-33`…`AT-35` و`AT-38`…`AT-50`** ⟵ **وذلك يستلزم `WU-017` المحجوبة**:
⟹ **فالمرحلةُ تبقى مفتوحةً حتى يُحسَم `IQ-040`.**

---

## 4. مخالفات النزاهة المرصودة آلياً

✅ **لا مخالفات** — `track_implementation.py` أرجع `integrity_violations: null`
بعد إغلاق الوحدة: ★ **كلُّ وحدة معلَّمة منجَزة تقابلها فحوصاتٌ ناجحةٌ فعلاً
و`نتيجة التحقق: نجح` صريحة.**

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

⛔ **ولا بندَ `IQ` جديد في هذا التشغيل** — ★ **وهذا مقصودٌ لا صدفة:** كلُّ ما
واجهته الزيادةُ من قراراتٍ كان **قابلاً للاستنتاج بثقة من مستندٍ قائم**:

| القرار | المرجّح الذي حسمه — ⛔ فلم يكن تخميناً |
|---|---|
| **`R-07` يُؤجَّل ولا يُبنى جزئياً** | `DEBT-72` («⛔⛔ **ولا يُوصَف التقريرُ بما لا يفعله**») + `report_table.dart` (⛔ **لا وسمٌ لا يظهر أبداً**) + `UPDS-05` §4 (⛔ **لا تعديلَ لمتطلبٍ معتمد بلا `CR`**) ⟹ **خيارٌ واحدٌ مشروع** |
| **`R-16` و`R-24` بتاريخٍ واحد** | ★ **العتبةُ الرقمية وحدَها هي بند `IQ`** (playbook §5) — ⟵ **وبناؤهما يوماً واحداً نظيرَ `R-02` و`R-05` لا يحتاج عتبةً أصلاً**، ✅ **والتأجيلُ معلَنٌ بـ[`DEBT-94`]** |
| **صفُّ `R-13` مستندٌ لا سطر** | `ADR-0011` (**الأسعارُ معزولةٌ بشرطِ قراءة**) + **المستندُ الأب لا يحمل إلا `unpricedLineCount`** ⟹ **تسميةُ السطر تلزمها صلاحيةُ الأسعار** ⛔ **وتقريرُ الملاحقة يجب أن يعمل لمن يلاحق النواقص** |
| **مفاتيحُ `R-27` هي `withdrawalView` و`expenseView`** | ★ **[`DEBT-89`] نصّاً:** **قراءةُ `outflows` تتفرّع على `ledgerType`** ⟵ **والتفكيكُ يستعلمهما معاً** ⟹ **فبلا المفتاحين يُرَدُّ التقريرُ دائماً** |
| **مدى `R-25` على `entryDate`** | `schema/supplier-ledger.md` (⛔ **ولا `stockDate` في سطره أصلاً**) + **سابقةُ `R-19` حرفياً** (**دفترٌ مالي لا مخزني**) |

★ **والبندُ المفتوح الوحيد `IQ-040`** — **من تشغيلٍ سابق** ⟵ **يحجب `WU-017`
وحدَها**، ⛔ **ولا علاقة له بـ`WU-018`.**

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

| # | الموضع | ماذا يقول المستند | ماذا فرض التنفيذ | كيف عولج |
|:-:|---|---|---|---|
| ① | `FR-M19` §2 — `R-07` | **«الوزن الضائع والسكرب والإتلاف»** | ⛔ **لا كاتبَ للإتلاف قبل `M21`** | ✅ **تأجيلٌ معلَن** — ⛔ **ولا تعديلَ للمتطلب ولا `CR`**: ★ **المتطلبُ صحيحٌ والزمنُ وحدَه لم يحن** |
| ② | `FR-M19` §2 — `R-16` و`R-24` | **«تاريخ / من–إلى»** و**«الفترة»** | ⛔ **كلفةُ القراءة بلا عتبةٍ موثَّقة** | ✅ **[`DEBT-94`] معلَن** — ★ **ومكتوبٌ في سطر فلاتر التقرير نفسِه** ⛔ **فلا يَعِد بما لا يفعل** |
| ③ | `report_table.dart` — ترويسة | **«وسمُ الأثر الرجعي ليس هنا ولا كاتبَ له»** | ✅ **صار له كاتبٌ في `WU-016`** | ✅ **بُني في `R-20`** — ★ **والتعليقُ صار متجاوَزاً بحدثٍ لا برأي** |
| ④ | `firestore_sack_valuation_directory.dart` | `_rowOf` **خاصّة** | **يقرأ `R-25` المجموعةَ نفسَها** | ✅ **كُشفت `rowOf`** — ★ **بنفس سابقة `balanceOf`** ⛔ **لا نسخةٌ ثانيةٌ تفترق** (`coding-standards.md` §2.2) |
| ⑤ | ⛅ **التجريبية** — `supplier_ledger` | **الكاتبُ يكتب `isCancelled: sack.isCancelled`** | ⛅ **جونيةٌ ملغاةٌ وسطرُها `false`** | ⚠️ **سُجِّل [`DEBT-95`] ولم يُصلَح** — ⛔ **خارج نطاق الزيادة**: ★ **الكاتبُ من `WU-015`** |

⛔ **ولا انحرافٍ صامتٍ واحد** — ★ **وكلُّ ما سبق مكتوبٌ في سجل المخاطر أو في
جسم الوحدة.**

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

⛔ **لا بند** — ★ **ولم يُنشأ `ADR` ولا `RFC` ولا `CR` في هذا التشغيل**،
⛔ **ولم يُعتمَد شيءٌ ذاتياً.**

⛅★★ **وما نُفِّذ ذاتياً على التجريبية** (الفئة 3 — بروتوكول التشغيل §ب و§ز.1):

| # | الأمر | المخرَج الفعلي |
|:-:|---|---|
| ① | `firebase deploy --only firestore:indexes --project qtms-orimind-master` | ✅ `Deploy complete!` |
| ② | `firebase firestore:indexes --project qtms-orimind-master` | ✅ **45 فهرساً** — ★ **ومنها `supplier_ledger: sourceId ↑ · entryDate ↓`** |
| ③ | **تسجيلُ الدخول بحساب QA على المحاكي** | ✅ **بتمرير مسار الملف السرّي وحدَه** — ⛔ **ولا قيمةَ سرٍّ مرّت في أي مخرَج** |
| ④ | **قراءاتٌ مباشرة للتجريبية** (`runQuery` مُقيَّدٌ بالمصدر) | ✅ **للتحقق من مطابقة المعروض للمخزَّن** — ⛔ **ولا كتابةَ حرفٍ واحد** |

⛔⛔★★★ **ولا شيءَ مسّ الإنتاج `qtms-orimind-c001`** — ★ **ولا يُمَسّ حتى تصل
كلمةُ `اعتمد-النشر-الإنتاجي`** (§ز.2).

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

| WU | لماذا حسّاسة | ما الذي يجب أن يراجعه إنسان |
|---|---|---|
| `WU-018` | ⛔ **أرقامُ قرارٍ مالي محكومةٌ بنطاق المصادر** — ★ **ومنها ما يُسلَّم للرعوي** (`R-25` · `R-26`) و**ما يُقرأ صندوقاً** (`R-16` · `R-24`) | ① ★★ **حقلُ `extraPermissions` لكلِّ تقريرٍ من الأربعةَ عشر** — ⟵ **خطأٌ فيه يُخفي تقريراً يعمل أو يعرض تقريراً يُرَدُّ استعلامُه** · ② **معادلاتُ `R-23`** (**الأثرُ النهائي = مجموعُ صوافي الأيام**) · ③ ⛔⛔ **أن `R-25` لا يُدخِل ضريبةً معلّقةً في جمع** · ④ **أن `R-16` يفصل «المقبوض» عن «الواصل»** (`GR-41`) |

⚠️⚠️★★ **ولماذا لا يُغني اختبارُ المحاكي الناجح عنها:** ★ **جرى بحساب QA
كامل المفاتيح الـ85** — ⟹ **فهو لا يمارس حالةَ نقصِ مفتاحٍ واحدة**:
⛔ **وتلك مُختبَرةٌ آلياً وحدها** (`report_screens_phase2_test.dart` — **ستة
اختباراتٍ تُثبت الإخفاء**)، ★ **والاختبارُ الآلي يُثبت السلوكَ في بيئةٍ مزيَّفة
لا في قاعدةٍ حقيقية.**

★ **والوحداتُ الحسّاسةُ السابقة تبقى كما هي** (`WU-004` · `WU-016` وغيرُهما) —
⛔ **ولم يُمَسّ شيءٌ من مراجعاتها.**

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| `docs/00-governance/upds-workflow/implementation-plan.md` | ★ **إغلاقُ `WU-018`** بفحوصها وجدولِ محاكيها وقرارَي نطاقها |
| `docs/04-design/module-design/reporting-design.md` | ★ **الحالة من «11 من 31» إلى «25 من 31»** · ★★ **و§7.2 جديد بحالة القواعد التسع مقيسةً** |
| `docs/02-requirements/requirements-traceability-matrix.md` | ★ **عمود «الكود» لـ`FR-M19`** — **من «مُنفَّذٌ جزئياً ولم يُثبَت حيّاً» إلى «مُنفَّذٌ ومُثبَتٌ حيّاً»** |
| `docs/06-database/indexing-strategy.md` | ★ **صفُّ الفهرس الجديد** `supplier_ledger: sourceId ↑ · entryDate ↓` **بعلّته** |
| `firestore.indexes.json` | ★ **فهرسٌ واحد جديد — 45** (⛅ **منشورٌ ومُتحقَّقٌ منه على التجريبية**) |
| `docs/03-architecture/risks-and-technical-debt-register.md` | ★ **[`DEBT-94`] جديد** · ★ **[`DEBT-95`] جديد** · ★ **[`DEBT-72`] سُدِّد أربعةٌ من ستة** · ★ **[`DEBT-92`] أُعيد توجيهُه بعلّته** |

⛔ **وما لم يُحدَّث ولماذا:** `openapi.yaml` و`05-api/endpoints/` (⛔ **لا واجهةَ
جديدة — الزيادةُ قراءةٌ محضة**) · `erd.md` و`data-dictionary.md` (⛔ **لا حقلَ
جديدٌ في القاعدة — `lineTotal` مكتوبٌ أصلاً في `schema/cash-sales.md` وإنما
صار مقروءاً**) · `migrations-log.md` (⛔ **لا هجرة**) · `troubleshooting-guide.md`
(⛔ **ولا مشكلةَ متكررةٍ جديدةٍ حُلَّت**) · `developer-onboarding.md` (⛔ **ولا
مستندَ رئيسيٍّ جديد**).

---

## 10. حالة `CLAUDE.md`

⛔ **لم يُحدَّث — ولا سطرَ فيه لُمس.** ★ **والسببُ مقيسٌ لا كسل:**

★ **نقطةُ التحوّل وقعت فعلاً** (`claude-md-management.md` §2 البند 5 — **اكتمالُ
وحدة عمل**) — ⛔ **لكنها لا تُغيِّر حرفاً في الكتلة المُدارة هنا:** ⟵ **قسمُ
«حالة التنفيذ» في هذا المشروع سطرُ إحالةٍ واحد بلا اسمِ وحدةٍ ولا رقمِ تقدّم**
(`AM-013` — **حدُّ ثلاثةِ أسطر**)، ★ **ومصدرُ الحقيقة `implementation-plan.md`
وقد حُدِّث فعلاً.** ⛔ **ولا أمرَ بناءٍ جديد ولا `ADR` معتمدٌ جديد ولا تغيّرت
المنهجية** — ⟹ **فالكتابةُ كانت ستُنتج الملفَّ نفسَه حرفياً.**

⛅★★ **ومخرَجُ `sync_claude_md.py --check` الفعلي (2026-09-04):**

```json
{"has_managed_block": true, "managed_block_lines": 60,
 "exceeds_block_line_limit": false, "implementation_status_lines": 1,
 "exceeds_status_line_limit": false, "max_single_line_chars": 386,
 "exceeds_line_char_limit": false, "current_total_lines": 89,
 "human_content_lines_preserved": 20}
```

⚠️⚠️★★ **وتنبيهٌ للتشغيل القادم:** ★ **الكتلةُ عند سقفها تماماً — 60 من 60**،
⟹ **فأيُّ سطرٍ يُضاف إليها يُرفَض بـ`rejected-size-limit`**: ⛔ **والعلاجُ
نقلُ القاعدة إلى `.claude/rules/`** (`AM-013` — **القاعدة العملية**) ⛔ **لا
`--force`** (**ممنوعٌ ذاتياً بلا تأكيدٍ بشريٍّ صريح**).

---

## 11. الخطوة التالية الموصى بها

| # | الخطوة | لماذا الآن |
|:-:|---|---|
| ① | ⛔⛔★★★ **حسمُ `IQ-040`** — **مفتاحا «عرض كشف الحساب» و«كشف موحّد لكل المصادر»** | ★ **هو وحدَه ما يحجب `WU-017`**، ⟹ **وبه تكتمل المرحلة الثانية فتُفتَح بوابتُها** (`AT-26`…`AT-50`) |
| ② | ★ **ثم `/upds-docgen:implement`** | ⟵ **يُنجز `WU-017` ثم يُجري الاختبار التكاملي للمرحلة** (§أ.5) |
| ③ | ⚠️ **مراجعةُ `WU-018` البشرية** (§8) | ⛔ **شرطٌ سابقٌ لأي نشرٍ إنتاجي** — ★ **ولا يُغني عنه نجاحُ 2455 اختباراً** |
| ④ | ⏳ **قرارٌ في [`DEBT-94`]** — **مدى «من–إلى» لـ`R-16` و`R-24`** | ★ **يحتاج عتبةً رقمية من المالك أو توسيعَ مخطَّط الملخّص** |

---

## 12. ★★ ملاحق هذا التشغيل

### 12.1 ⛅★★★ اختبار المحاكي — الجدول الكامل

★ **الجدولُ التفصيليُّ بالتسعةَ عشرَ سيناريو والستةِ المسارات الحدّية في
[`implementation-plan.md`](implementation-plan.md) تحت `WU-018`** — ⛔ **ولا
يُكرَّر هنا** (`AM-013` — **مصدرُ حقيقةٍ واحد**).

★★ **وثلاثةُ تقاطعاتٍ رقمية هي أقوى دليلٍ في هذا التشغيل:**

| # | الرقم | المساران المستقلان اللذان أنتجاه |
|:-:|---|---|
| ① | **6,800** | `R-20` (**صفٌّ يوميٌّ من `daily_summaries`**) · `R-23` (**تجميعُ الفترة**) |
| ② | **20,000** | `R-25` (`sackRevenue` **المخزَّن**) · `R-27` (**جمعُ [computeSackRevenue] من حركات الدفتر**) |
| ③ | **12,000** | `R-20` (**بند السحبيات في الملخّص**) · `R-21` (**سندُ `WDR-20260901-0001` بقاته**) |

### 12.2 ⚠️ فحص «الحشر بدل الاختصار» (`SKILL.md` الخطوة 2 · playbook §10)

★ **`plan.has_field_char_warnings: true`** — **23 وحدةً فيها حقلٌ يتجاوز 400
حرف.** ⛔⛔ **ولم تُختصَر ولا واحدة في هذا التشغيل** — ★ **وهذا قرارٌ مقيسٌ لا
إهمال:**

★ **فُحصت الوحدةُ الأكبر عيّنةً** (`WU-000` — **38,807 حرفاً في حقولٍ متجاوزة**):
⟹ **تبيّن أن الحقولَ المتجاوزة فيها هي عشرةُ `**فحوصات التحقق — <جولة مؤرَّخة>:**`
تحمل مخرجاتٍ مقيسة** — ★ **وتلك «دليلُ تحققٍ فعلي»** ⛔ **و§10 البند 3 يمنع
حذفَه صراحةً** («⛔ **لا يُحذف أي دليل تحقق فعلي، ولا أي قاعدة حَوْكمة/أمان**»).
⟵ ★★ **والسردُ التحليليُّ الذي يستهدفه §10 حُذف من هذا الملف أصلاً في التشغيل 11**
(**إصلاحُ النزاهة: 1088 سطرَ حشوٍ منسوخ**).

⏳ **والمرشَّحُ الوحيدُ المشروع المرصود:** **جولاتُ `WU-000` المؤرَّخة العشر
تُستبدَل بسطرِ إحالةٍ إلى تقاريرها** (`implementation-report-2026-08-22` …
`-2026-08-26-run3`) ⟹ **بتوفيرٍ يُقدَّر بـ116 سطراً (≈2٪ من الملف)** —
★ **يُنفَّذ في تشغيلٍ مخصَّصٍ له** ⛔ **لا في تشغيلٍ يمسّ الكود**: ⟵ **فخلطُ
تحريرِ سجلِّ أدلةٍ بتحريرِ كودٍ يجعل مراجعةَ أيِّهما أصعب.**

### 12.3 ★ أرشفةُ التقارير القديمة

**نُفِّذت في نهاية التشغيل — النتيجةُ في §13.**

---

## 13. الأرشفة التلقائية لتقارير التنفيذ

✅ **نُفِّذت** (`archive_implementation_reports.py --keep 5`):

| البند | القيمة |
|---|---|
| **أُرشِف** | **59 تقريراً قديماً** — ⟵ `docs/99-archive/2026/implementation-reports/` |
| **بقي نشطاً** | **5 تقارير** — `2026-09-04` · `2026-09-03-run3` · `2026-09-03-run2` · `2026-09-03` · `2026-09-02-run4` |
| ⛔ **حذفٌ نهائي** | **لا شيء** — ★ **نقلٌ فقط** (`UPDS-05` §4 البند 5): ✅ **و`git status` يُظهر 59 `D` في المجلد النشط يقابلها 59 ملفاً في الأرشيف** |

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
