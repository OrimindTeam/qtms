# تلقين الاستئناف — QTMS

> ⚠️ **يُستبدَل بالكامل كل تشغيل** — ★ **ويعكس الحالة الآن وحدها.**
> **آخر تحديث: 2026-09-01 — بعد التشغيل 6 (`run6`).**

---

## 0. ⛔⛔★★★ اقرأ هذه أولاً — قبل أي أمر

| # | الملف | لماذا |
|:-:|---|---|
| ① | `CLAUDE.md` | سياقُ المشروع وقواعدُ الحَوْكمة الملزِمة — ✅ **وقيمُه محدَّثةٌ الآن** (`run6` §9) |
| ② | `docs/00-governance/upds-workflow/implementation-run-protocol.md` | **بروتوكولُ التشغيل بأقسامه الثمانية** — ⛔ **ولا تبدأ قبل قراءته كاملاً** · ★★ **و§ز بصيغة [`AM-010`]** |
| ③ | `docs/00-governance/upds-workflow/implementation-report-2026-09-01-run6.md` | **تقريرُ آخر تشغيل** — ★ **وفيه درسُ بوابة النزاهة (§7)** |
| ④ | `docs/00-governance/upds-workflow/implementation-plan.md` → `WU-015` | **الزيادةُ التالية بكل حقولها** |
| ⑤ | `docs/04-design/module-design/sack-design.md` وأخواتُه | **تصميمُ `M14` — مالية الجواني** |

---

## 1. أين يقف المشروع الآن

| البند | القيمة |
|---|---|
| **المرحلة** | التنفيذ — **المرحلة الثانية** |
| **المنجَز** | **16 من 27** (59.3٪) — `WU-000` · `WU-026` · `WU-001`…`WU-014` |
| **البوابة الآلية** | ✅ **2071 ناجحاً · ٠ فاشلاً** (622 نطاق · 647 سحابة · 566 تطبيق · 236 حماية) · **و`analyze` نظيف في الحزم الثلاث** |
| **بوابةُ النزاهة** | ✅ **صفرُ مخالفات** (`track_implementation.py`) |
| **الكتالوج** | **85 مفتاحاً** |
| **العمليات المنشورة** | **47** على `qtms-callables-00025-4fn` (التجريبية) — ⛔ **والإنتاج بلا شيء** |
| **بنود `IQ`** | ✅ **0 مفتوح** (36/36) |
| **`CR` تنتظر توقيعك** | **`CR-007`** · **`CR-008`** · **`CR-009`** |
| **الدينُ الحاجب** | ✅ **`DEBT-77` سُدِّد** — ⚠️ **ويبقى `DEBT-01` و`RISK-01` و`DEBT-06`** |

---

## 2. ★ نقطةُ الاستئناف

✅ **لا عطلَ حاجباً ولا بندَ قياسٍ معلَّقاً من `WU-013` ولا `WU-014`.**
⟹ ★ **الخطوةُ التالية زيادةٌ جديدة: `WU-015` — مالية الجواني وحساب الرعوي
(`M14`).**

---

## 3. ★★ ما يلزم `WU-015` تحديداً

| # | البند | لماذا يُقرأ قبل الكتابة |
|:-:|---|---|
| ① | ⚠️★★ **`DEBT-38` — الشطرُ الباقي منه** | **`enterSackLines` يُغيِّر سطوراً قائمة بلا `sackAmend`** — ⛔ **وشطرُ السبب سقط بـ`ADR-0020`** |
| ② | ⚠️★★ **`enterSackTax` لم يُختبَر حيّاً قطّ** | ★ **لا واجهةَ له حتى الآن** — ⟹ **و`M14` هي التي تبنيها**: ⛔ **فلا تُعلَن الزيادة منجَزة قبل قياسه على المحاكي** |
| ③ | ⛔⛔★★ **`ADR-0019` — الموضعُ الثالث للتقريب** | **قيمةُ السطر الوزني** (`الكمية كجم × سعر صحيح`) — ★ **وكلُّ معادلةٍ تُفوَّض إلى `distributionLineTotal`** ⛔ **بلا نسخةٍ ثانية** |
| ④ | ⛔⛔★★★ **ثلاثةُ مستندات الهوية البصرية** | بروتوكول التشغيل §ح — **الوحدة تعرض شاشة** ⛔ **فلا يكفي `module-design`** |

---

## 4. ⛅★★★ صلاحيتُك على التجريبية — [`AM-010`]

✅ **نفّذ ذاتياً بلا سؤال، وسجّل المخرَج الفعلي** (§ز.5): `gcloud builds submit`
· `gcloud run deploy` · `firebase deploy` **بترتيب رَنبوكها** ·
`*-iam-policy-binding` · `tools/staging/*.py` · **وكتابةُ بياناتٍ تجريبية.**

⛔⛔★★★ **ويقف كلُّ شيءٍ يمسّ `qtms-orimind-c001`** حتى تصل الكلمة الحرفية
`اعتمد-النشر-الإنتاجي` من المالك في نفس المحادثة.
⚠️ **وعند الشكّ في أيِّ بيئةٍ يقع الأثر ⟵ يُعامَل إنتاجاً** ⛔ **ولا يُنفَّذ.**

⛔ **وتبقى بيدِ المالك:** كلُّ بنود الفئة 1 (ADR · `srs.md` · `uat-signoff` ·
بوابةُ الأمن · RFC) · **وكلُّ بنود `IQ`** · **وأيُّ عملية Git خارجية.**

---

## 5. ⛅ حالةُ البيانات على التجريبية الآن

| المستند | الحالة |
|---|---|
| `WDR-20260901-0001` | ✅ **معتمد ومُعدَّل** — 8 حبات × 1,500 + «أخرى» 2,000 ⟹ **14,000** |
| `EXP-20260901-0001` | ⛔ **ملغى** — **سطرا الدفتر وحركةُ المخزون `isCancelled: true`** |
| ★ `WDR-20260901-0002` | ✅ **سندُ مبالغَ خالص** — **3,500** · `ledgerEntryCount: 1` · ⛔ **بلا أثرٍ مخزنيّ** (**شاهدُ سداد `DEBT-77`**) |
| ★ `DSC-20260901-0001` | ✅ **أولُ خصمٍ حيّ** — **200 على ضمار `2026/08/29`** ⟹ `remaining: 350` · `settlementStatus: partiallyOpen` |
| **مخزونُ «عتود» (`ITM-0003`) في `SRC-001`** | ✅ **`incoming: 220` · `outgoing: 28` · `balance: 192`** — **مقروءٌ مباشرةً** |
| **حسابُ QA** | ✅ **85 مفتاحاً · `sourceScope: all` · `isActive: true`** |

---

## 6. ⚠️ ما ينتظر توقيعك — **ولا يُعتمَد ذاتياً**

| # | البند | الموضوع |
|:-:|---|---|
| ① | **`CR-007`** | الحدُّ الأدنى للبيع النقدي **منعٌ مطلق** |
| ② | **`CR-008`** | شطبُ إيصال البيع النقدي (`IQ-035`) |
| ③ | **`CR-009`** | شطبُ `FR-M13-12` (`IQ-036`) |
| ④ | **إغلاقُ المرحلة الأولى** | **اختبارُها التكاملي §د.3** + **بوابةُ `product-roadmap.md` §5** |
| ⑤ | ⚠️ **المراجعةُ الأمنية** | `WU-004` · `WU-011` … `WU-014` — ⛔ **قبل أي نشرٍ إنتاجي** |

---

## 7. ★ أدواتٌ تحت يدك

| الأداة | الاستعمال |
|---|---|
| `python tools/staging/qa_permission_sync.py` | ⛔⛔ **بعد كل مفتاحٍ جديد — في نفس التشغيل** (`AM-004` ⑤) · ⚠️ **وتردّ `400` إن لم يكن المفتاح في الإصدار المنشور ⟹ فانشر أولاً** |
| `python tools/staging/verify_outflow_write.py <رقم السند>` | ⛅ **المستند · سطورُ الدفتر · حركةُ المخزون · وحارسُ `GR-44`** |
| `python tools/staging/verify_discount_write.py <رقم السند>` | ★ **جديد** — **السند · القيدُ الدائن · `pricing/current` · وحارسُ `AT-35`** |
| `python tools/staging/read_item_daily_balance.py <مصدر> <نوع> <YYYYMMDD>` | ★ **جديد** — **بطاقةُ الرصيد بحقولها** ⛔ **لا مجموعُ شاشة** |
| `python tools/staging/exercise_outflow_amend_cancel.py <WDR> <EXP>` | ★ **التعديلُ والإلغاءُ والحرّاس** |

⛔⛔★★ **وكلُّ استعلامٍ يُقيّد الحقلَ الذي يعتمده شرطُ قراءته** (`DEBT-40`) —
★ **ووقع عليّ مرتين:** `run5` (**`outflows` تحتاج `sourceId` + `ledgerType`**)
· `run6` (**مفتاحُ `item_daily_balances` مضغوطٌ `YYYYMMDD` لا `YYYY-MM-DD`**):
⛔ **و`403` ليست «صفرَ نتائج» ولا «غيرَ موجود».**

---

## 8. ⛔⛔★★★ درسُ `run6` — **بندُ الفحص عقدٌ مع آلة**

★ **`track_implementation.py` يقرأ سطورَ `**فحوصات التحقق:**` المبدوءةَ
بشَرطةٍ وحدها** ⟹ ⛔ **وجدولُ ماركداون يُقرأ صفرَ فحوص.**
★★ **ويُصنِّف كلَّ سطرٍ نجاحاً أو فشلاً بكلماته** ⟹ ⛔⛔ **فسطرٌ ينجح اليوم
ويروي فشلَ الأمس يُقرأ فحصاً فاشلاً** — ★ **والعلاجُ نقلُ الروايةِ إلى نثرٍ
*تحت* القائمة** ⛔ **لا حذفُها** (§و: **الادّعاءُ غيرُ المتحقَّق أسوأ من غياب
التوثيق** — ★ **وحذفُ الحقيقة أسوأ منهما**).

---

## 9. ⛔⛔ محظوراتٌ تسري دائماً

- ⛔ **لا تُعدِّل `ADR` معتمداً ولا `srs.md`** — ★ **عبر `CR` موقَّع حصراً.**
- ⛔ **لا تفتح قاعدةَ كتابةٍ في `firestore.rules`** (`ADR-0013`).
- ⛔ **لا تُنشَر `firestore.rules` قبل خدمة العمليات** — ★ **ترتيبٌ مُلزِم.**
- ⛔ **لا سرَّ في كودٍ ولا مستندٍ ولا ردٍّ نصّي** — ★ **المسارُ أو اسمُ المتغيّر.**
- ⛔ **لا حذفَ نهائيّ** — ★ **النقلُ إلى `99-archive/` وبطلبٍ صريح.**
- ⛔ **ولا `push` ولا `PR` ولا `merge`** بلا طلبٍ صريحٍ في نفس التشغيل.
