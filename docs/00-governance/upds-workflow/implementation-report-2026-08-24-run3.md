# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-24 (تشغيل 3)

**رقم تشغيل التنفيذ:** 15
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة**)
**تقدّم التنفيذ:** 0٪ (0 منجَزة من 27)
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** ★ **0 — ولا بند واحد**

---

## 1. ملخص تنفيذي

★ **تشغيل ترحيلِ قرارَين، ولم يُكتب سطر كود واحد — عن قصد.**

✅ **أُجيب `IQ-009` (الخيار أ)** — تطبيق `ADR-0013` حرفياً: تُغلَق الكتابة
المباشرة في القواعد، ⛔ **وأُلغي `allocateDocumentNumber` و
`allocateDailySackNumber` كعمليتين مستقلتين**، وصار تخصيص الرقم **خطوة
داخلية** في المعاملة التي تنشئ المستند.

✅ ★ **واعتُمد `ADR-0016`** بقرار صريح — الصلاحيات تخرج من رمز الدخول إلى
`users/{userId}`.

★ **وأهم قرار تخطيطي في هذا التشغيل: القراران يُنفَّذان معاً في زيادة واحدة
جديدة — `WU-026`** — لأن **كليهما يعيد كتابة `firestore.rules` نفسها (66
موضع `perm` في 814 سطراً) والـ172 اختباراً نفسها**. ⛔ **وفصلهما يعني المرور
على الطبقة الأخطر في النظام مرتين بلا فائدة.**

✅ **ورُفع الحجب عن الوحدتين**: `WU-000` عادت `[قيد التنفيذ]` **بنطاق أضيق**،
و`WU-001` عادت `[مخطط]` **معتمِدةً على `WU-026`**.

**327 اختباراً ما تزال ناجحة · ٠ فاشلاً · 0 بند مفتوح · 0 وحدة محجوبة.**

---

## 2. بوابة الدخول — نتيجة الفحص

✅ **البوابة العامة (٨ شروط) متحققة.**

★ **والشرط 8 تغيّر حاله للأفضل في هذا التشغيل:** كان `ADR-0016` **«مقترحاً»**
فحجب `WU-001`؛ ✅ **واعتُمد الآن**، فصارت **الستة عشر ADR كلها «مقبول»** ولا
واحد بحالة «مقترح».

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

⛔ **لا شيء** — التشغيل تخطيطي وحَوْكمي بالكامل.

### 3.2 وحدات قيد التنفيذ

| WU | العنوان | الحالة بعد هذا التشغيل |
|---|---|---|
| `WU-000` | بوابة التأسيس التقني | ✅ **رُفع الحجب** · ★ **وضاق نطاقها**: لم يبقَ من المخرَج 7 إلا **مُنشئ الحسابات** (`DEBT-16`) **وإزالة أثر العيّنة** (`DEBT-14`) |

### 3.3 وحدات محجوبة (ولماذا)

✅ ★ **لا وحدة محجوبة إطلاقاً** — لأول مرة منذ بدء التنفيذ.

### 3.4 ★ وحدة جديدة أُضيفت للخطة

| WU | العنوان | ترتيبها |
|---|---|---|
| ★ **`WU-026`** | **زيادة: إعادة تشكيل طبقة التفويض والكتابة** | ★ **بعد `WU-000` و⛔ قبل `WU-001`** — **حاجز إلزامي: لا زيادة تشغيلية قبلها** |

★ **ورقمها متأخر عن ترتيبها عمداً** — الترقيم **تراكمي لا يُعاد**، وإعادة
ترقيم 25 وحدة تكسر كل إشارة إليها. **والترتيب التنفيذي مُثبَت في مخطط
التبعية وفي نصّ الوحدة معاً.**

**نطاقها — قراران في ملف واحد:**

| # | المصدر | ما يُنفَّذ |
|:-:|---|---|
| ① | `ADR-0013` القاعدة 2 (`IQ-009` أ) | `allow create, update: if false` على **ثماني مجموعات** |
| ② | `ADR-0016` (`IQ-008` ب) | `perm()` تقرأ من `users/{userId}` بـ`get()` واحدة |

**ومعيار قبولها ستة بنود**، أشدّها: ★ **اختبار طفرة على `perm()`**،
و★★ **قياس الأرقام الثلاثة في `ADR-0016` §«كلفة القراءة» فعلياً** — ⛔ **ولا
تُغلَق الزيادة قبل قياسها.**

**الفحوصات التي شُغِّلت فعلاً (2026-08-24):**

| الفحص | النتيجة |
|---|---|
| `cd functions && dart analyze` · `dart test` | ✅ **`No issues found!`** · **80 · ٠ فاشلاً** |
| `track_implementation.py` · `track_completion.py` | ✅ **0 مخالفة · 0 سطر غير مُحلَّل · 0 بند مفتوح** |

⛔ **ولم يُعدَّل ملف كود واحد** — التغييرات كلها في `docs/` و`CLAUDE.md`.
★ **ولذلك لم تُعَد الفحوص الأربعة كاملةً** (Flutter 9 · قواعد الحماية 172):
**لا شيء يمكن أن يُفشِلها لم يُمَسّ.**

---

## 4. مخالفات النزاهة المرصودة آلياً

✅ **لا مخالفات** — `integrity_violations: []` · `unparsed_lines: []`.

---

## 5. أسئلة التنفيذ المُصعَّدة

★ **لا بند مفتوح — والتسعة كلها محسومة:**

| IQ | الحالة |
|---|---|
| `IQ-001` … `IQ-006` | ✅ **حُلَّت ونُفِّذت** سابقاً |
| `IQ-007` | ✅ **حُلَّ ونُفِّذ** (تشغيل 13) |
| `IQ-008` | ✅ **حُلَّ (الخيار ب)** ⟵ **`ADR-0016` — اعتُمد الآن** |
| ★ `IQ-009` | ✅ **حُلَّ (الخيار أ)** ⟵ **`WU-026`** |

---

## 6. تعارضات وثغرات توثيق عولجت في هذا التشغيل

### ① تعارض `allocateDocumentNumber` — ✅ **حُسم بقرارك**

`api-overview.md` §3.1 يجعله عمليةً مستقلة، و`ADR-0013` يُغلق الكتابة
المباشرة. ✅ **والقرار: تطبيق `ADR-0013` حرفياً** — تُلغى العمليتان،
والتخصيص خطوة داخلية.

⛔ **ولم يُعدَّل `api-overview.md` §3.1** بقرارك الصريح — **يُقرأ مقروناً
بـ`ADR-0013`**، تماماً كما صنع الـADR نفسه بـ§2. ★ **وهو المسار الصحيح
حَوْكمياً** فالمستند STATIC (`UPDS-05` §5).

### ② `DEBT-17` — ✅ **أُعيد وصفه كما طلبت**

**الوصف السابق:** «كل قاعدة صلاحية تصير مكتوبة **مرتين**: شرطاً في القاعدة
وفحصاً في الدالة».

★ **والوصف الجديد يعكس النموذج المعتمد — وهو يفصل لا يُكرِّر:**

| الطبقة | موضع تفويضها |
|---|---|
| **القراءة** | `firestore.rules` وحدها (`perm()` · شروط `ADR-0011`) |
| **الكتابة** | ★ **الدالة السحابية وحدها** — والقاعدة `if false` |

⟵ ★ **فالازدواج الحقيقي ينحصر في المفاتيح لا في الشروط**، ✅ **وهو مُعالَج
أصلاً** بمصدر واحد في طبقة النطاق يحرسه اختبار تطابق ثنائي الاتجاه.
**وخُفِّضت خطورته من «عالية» إلى «متوسطة».**

### ③ `DEBT-20` — ✅ **رُبط بزيادة سدادِه**

الانحراف قائم (الكتابة ما تزال مفتوحة على ثماني مجموعات)، ✅ **لكنه صار
مُخطَّطاً للسداد في `WU-026`** بدل أن يكون انحرافاً بلا خطة.

---

## 7. بنود بوابة الحَوْكمة

| البند | الحالة |
|---|---|
| ★★ **`ADR-0016`** | ✅ **اعتُمد في 2026-08-24 بقرار صريح من صاحب المشروع** («اعتمد ADR-0016») — ووُثِّق في §«الاعتماد» بنفس نمط `ADR-0013`. ⛔ **ولم يُعتمَد ذاتياً** (`UPDS-05` §4.6) |
| **تعديل ADR معتمد** | ⛔ **لم يحدث** — `ADR-0016` كان **«مقترحاً»**، وتحويله إلى «مقبول» **انتقالُ دورةِ حياةٍ عادي** لا تعديلٌ لقرار معتمد |
| **تعديل `srs.md` أو `api-overview.md`** | ⛔ **لم يحدث** |
| **ADR جديد** | ⛔ **لم يُنشأ** — `IQ-009` **لا ينقض `ADR-0013` بل يطبّقه**، فلا يحتاج ADR |

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

★★ **`WU-026` هي أخطر زيادة في المشروع كله** — تعيد كتابة **طبقة التفويض
الوحيدة** (`ADR-0002`) **ونموذج الكتابة** معاً، في ملف واحد تحرسه 172
اختباراً. ⛔ **مراجعة بشرية إلزامية — والتحقق الآلي شرط ضروري لا كافٍ.**

| # | ما يحتاج عيناً بشرية | لماذا |
|:-:|---|---|
| 1 | ★★ **`WU-026` عند تنفيذها** | **طبقة التفويض ونموذج الكتابة معاً** |
| 2 | ★ **تبعية جديدة يفرضها `ADR-0016`** | ⛔ **غياب `users/{userId}` = فقدُ كل الصلاحيات فوراً** — اتجاه آمن، **لكن يجب أن يكون مقصوداً لا مفاجأة** |
| 3 | ★★ **`bootstrapOwnerPermissions`** | تمنح صلاحياتٍ **بلا فحص صلاحية** — الهوية وحدها هي التفويض |
| 4 | ★ **`QTMS_OWNER_UID` في البيئتين** | خلطُه **تصعيدُ امتياز** |
| 5 | ★ **`DEBT-19`** | تصنيف أخطاء المصادقة بمطابقة نصّ — يحتاج **فحصاً حيّاً** |

> ⛔ **وثلاثة بنود لا تُنسى:** **App Check بفرض مُطفأ** · **حساب المالك ما
> يزال بلا صلاحيات** حتى يُشغَّل رَنبوك الإقلاع · **`DEBT-14`**.

---

## 9. مستندات UPDS التي حُدِّثت

| المستند | سبب التحديث |
|---|---|
| ★ `docs/03-architecture/adr/ADR-0016-*.md` | ✅ **«مقترح» ← «مقبول»** + قسم **الاعتماد** الموثَّق |
| `docs/00-governance/upds-workflow/implementation-questions.md` | **إغلاق `IQ-009`** بجدول أثره |
| ★ `docs/00-governance/upds-workflow/implementation-plan.md` | ★ **`WU-026` جديدة** · رفع الحجب عن `WU-000` و`WU-001` · **تضييق نطاق `WU-000`** · **مخطط التبعية** |
| `docs/03-architecture/risks-and-technical-debt-register.md` | ★ **`DEBT-17` أُعيد وصفه** · `DEBT-20` رُبط بـ`WU-026` |
| `CLAUDE.md` | راجع §10 |

⛔ **ولم يُحدَّث `authentication-and-authorization.md` ولا `authentication-policy.md`
ولا `data-dictionary.md`** — ★ **عمداً:** نصوصها تتغيّر بأثر `ADR-0016`
**بعد نجاح التنفيذ لا قبله**، ⛔ **وتحديثها الآن يجعل التوثيق يصف واقعاً غير
قائم**. **وهي مُدرَجة صراحةً في ملاحظات `WU-026`.**

---

## 10. حالة `CLAUDE.md`

✅ **حُدِّث** عبر `sync_claude_md.py` حصراً — `managed-block-updated` ·
**158 سطراً** (تحت 200) · `human_content_lines_preserved: 0`.

★ **نقطة تحوّل صريحة** (`claude-md-management.md` §2 البند 6): **ADR انتقل من
«مقترح» إلى «مقبول»**. وأُضيف `ADR-0016` إلى قائمة القرارات، وصار النصّ
«**كل `ADR-0001`…`ADR-0016` معتمد**»، وحُدِّث عدد الوحدات إلى **27**.

---

## 11. الخطوة التالية الموصى بها

★ **لا قرار ينتظرك — والطريق مفتوح لأول مرة منذ بدء التنفيذ.**

1. ★★ **`WU-026` أولاً** — إعادة تشكيل طبقة التفويض والكتابة.
   ⚠️ **وهي أخطر ما في المشروع:** 66 موضع `perm` · 172 اختباراً · وطبقة
   التفويض الوحيدة. ★ **ويُنصَح ببدئها وحدها في تشغيل مخصَّص.**
2. **ثم إكمال `WU-000`:** مُنشئ الحسابات (`DEBT-16`) ← **`DEBT-14`** ←
   وعندها **وحدها** تُرفَع إلى `[منجز ✅]`.
3. **ثم `WU-001`** — أول زيادة تشغيلية بشاشات.

⚠️ **وتذكير بشرط لا يسقط:** الأرقام الثلاثة في `ADR-0016` §«كلفة القراءة»
**تُقاس فعلياً على المحاكي والقاعدة الحقيقية** — ⛔ **ولا يُدَّعى ما لم
يُشغَّل.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
