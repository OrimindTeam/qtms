# تقرير تنفيذ برمجي — UPDS DocGen — 2026-09-02 (تشغيل [`DEBT-86`])

**رقم تشغيل التنفيذ:** 15 — ⛔ **تشغيلٌ خاصٌّ بعطلٍ واحد** لا بزيادةٍ جديدة
**المنهجية المعتمدة:** تزايدية (Incremental) — وحدة العمل: **زيادة = شريحة رأسية كاملة**
**تقدّم التنفيذ:** 60.7٪ (17 منجَزة من 28) — ⛔ **بلا تغيير: لم تُفتَح زيادةٌ جديدة**
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** 0 من 37

---

## 1. ملخص تنفيذي

★★★ **هذا التشغيل يعالج عطلاً واحداً: [`DEBT-86`]** — **الحاجزَ الوحيد الباقي
أمام بوابة المرحلة الأولى** بعد اختبارها التكاملي (§د.3) صباحَ اليوم نفسِه.
⛔⛔★★★ **والعطل: ما يدخل المخزنَ من جونية لا يُوزَّع ولا يُباع ولا يُصرَف
إطلاقاً** — ★ **سطرُ الجونية يدخل الدفترَ بمفتاحٍ مركّب** (`{النوع} - جونية
رقم N` — [`ADR-0007`]) ⛔ **بينما منسدلاتُ الصرف الثلاثة كانت تُبنى من كتالوج
الأنواع بمعرّفاتِ `itemId`**: ⟹ **فلا يظهر المفتاحُ في أي قائمة اختيار، ولو
أُدخِل بالمعرّف المجرَّد ردّت السحابة «الكمية غير كافية».**

⛔⛔★★★ **وانكشف عند العلاج شطران لم يكونا في وصف العطل أصلاً** — ★ **وهذا
أهمُّ ما في هذا التشغيل**: ② **التطبيق لم يُرسِل `sackId` في أي سطرٍ قطّ**
⟹ **ففتحُ المفتاح المركّب وحدَه كان سيُنتج حركاتِ خروجٍ بلا مرجعِ جونية**
⟵ **فتسقط الجونيةُ من احتساب إيرادها بصمت** (`ADR-0007` ⑤ · `FR-M10-14`) ·
③ **والحركةُ تُخزِّن الاسمَ المجرَّد في `itemName` والمركّبَ في `itemKey`**
⟹ **فجونيتان من نوعٍ واحد كانتا تُعرَضان سطرين متطابقين** — ★ **وهو حرفياً
الخيارُ 2 الذي رفضه [`ADR-0007`] لأن «المستخدم لا يرى الفرق».**

✅★★★ **والأشطرُ الثلاثة نُفِّذت ومُثبَتةٌ حيّاً على `Pixel_6_API_36`**،
★ **و24 اختبارَ ارتداد** موزَّعةً على أربع طبقات · **2193 اختباراً · ٠ فاشلاً.**

---

## 2. بوابة الدخول — نتيجة الفحص

| الشرط | متحقق؟ | المسار المفحوص |
|---|:-:|---|
| `implementation-plan.md` قائم وبحالاتٍ صحيحة | ✅ | `docs/00-governance/upds-workflow/implementation-plan.md` |
| المنهجية مُصرَّح بها | ✅ | `docs/21-project-management/development-methodology.md` |
| بنود `IQ` مفتوحة تعطّل العمل | ✅ **لا يوجد** (37/37 محسومة) | `implementation-questions.md` |
| مخالفات نزاهةٍ آلية | ✅ **صفر** | `track_implementation.py` |
| تعارضُ كودٍ/توثيقٍ يحتاج تسوية | ✅ **مُعالَج بلا انحرافٍ صامت** — راجع §6 | — |

⚠️ **وهذا تشغيلُ إصلاحٍ لا تشغيلُ زيادة** — ★ **فبوابةُ الوحدة (٥ شروط) لا
تنطبق**: ⛔ **ولم تُفتَح ولم تُغلَق أيُّ وحدةِ عمل.**

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

⛔ **لا وحدةَ جديدة** — ★ **والوحداتُ الثلاث التي مسّها العلاج
(`WU-006` · `WU-012` · `WU-014`) كانت وبقيت `[منجز ✅]`**: ⟵ **فمعيارُ قبول
كلٍّ منها كان متحقّقاً ومقيساً**، ⛔ **و[`DEBT-86`] عطلٌ عابرٌ للزيادات في
*نموذج اختيار النوع* لا في أيٍّ منها وحدَها.**

★ **وما تغيّر فيها فعلياً:**

| الزيادة | الشاشة | التغيير |
|---|---|---|
| `WU-006` | `M10` — التوزيع | **الخياراتُ من `stockOptionsProvider`** ⛔ **لا `distributionItemsProvider`** |
| `WU-012` | `M11` — البيع النقدي | **المصدرُ نفسُه** — ⛔ **و`cashSaleItemsProvider` سقط** |
| `WU-014` | `M22` — السحبيات والخرجيات | **المصدرُ نفسُه** — ⛔ **و`outflowItemsProvider` سقط** |

### 3.2 وحدات قيد التنفيذ

⛔ **لا شيء.**

### 3.3 وحدات محجوبة (ولماذا)

⛔ **لا شيء** — ★ **و`blocked: 0` بمخرَج `track_implementation.py`.**

---

## 4. مخالفات النزاهة المرصودة آلياً

✅ **لا مخالفات — كل وحدة معلَّمة منجَزة تقابلها فحوصات ناجحة فعلياً.**
(`"integrity_violations": []` · `"has_integrity_violations": false`)

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

⛔ **لا بندَ `IQ` جديداً في هذا التشغيل** — ★ **والقراراتُ الثلاثة التي كانت
مرشَّحةً حُسمت من نصٍّ معتمدٍ قائم لا باجتهاد:**

| القرار | من أين حُسم |
|---|---|
| **أيُّ اسمٍ يُعرَض للمفتاح المركّب؟** | [`ADR-0007`] الخيار 3 حرفياً: «**التمييز مرئي في كل قائمة اختيار وكل شاشة مخزون**» |
| **أيبقى الرصيدُ الصفر خياراً؟** | ✅ **نعم** — ⛔ **بخلاف شاشة التسعير** (`FR-M9-02` · `E-33` **نصَّا على العكس هناك**): ★ **وإسقاطُه هنا يُعيد عَرَضَ [`DEBT-88`] في نموذج التعديل** |
| **من أين يأتي `sackId`؟** | [`ADR-0013`] القاعدة 3 (**الفحص في المعاملة**) + [`ADR-0008`] (**الدفتر هو الحَكَم**) — ⟹ **يُشتقّ ولا يُصدَّق** |

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

### 6.1 ⛔⛔★★★ الكودُ كان يخالف [`ADR-0007`] — **والتسويةُ لصالح القرار**

| | |
|---|---|
| **ماذا يقول المستند** | [`ADR-0007`] رفض **الخيار 2** («سجل رصيد منفصل بمعرّف مركّب **بلا تغيير الاسم**») لسببٍ واحدٍ صريح: «**المستخدم لا يرى الفرق في قوائم الاختيار — يرى «بطوة معالم أحمر» مرتين بلا تمييز فيختار الخطأ**»، ★ **واعتمد الخيار 3** الذي وعد بأن «**التمييز مرئي في كل قائمة اختيار وكل شاشة مخزون**» |
| **ماذا كان التنفيذ يفعل** | ⛔⛔ **بنى القوائمَ من كتالوج الأنواع** ⟹ **فلم يظهر المفتاحُ المركّب في أي قائمة قطّ**، ★ **وعرض `itemName` المجرَّد في شاشتي المخزون والتسعير** ⟹ **فجونيتان من نوعٍ واحد سطران متطابقان** — ⛔⛔ **وهو الخيارُ 2 المرفوضُ نفسُه، مُنفَّذاً بلا قرار** |
| **كيف عُولج** | ✅ **تسويةُ الحالة (ب): الكودُ يُصحَّح ليطابق القرار المعتمد** — ⛔ **ولا `ADR` جديد**: ★ **لا انحرافَ يُوثَّق هنا، وإنما نقضٌ يُرفَع** |

### 6.2 ⚠️★★ ثغرةٌ توثيقية: **مصدرُ خيارات النوع لم يكن منصوصاً في أي `module-design`**

★ **`inventory-design.md` §84 ينصّ على القاعدة لشاشة التسعير وحدها**
(«**الاستعلام على رصيد اليوم > 0 لا على قائمة الأنواع**») — ⟵ **ولذلك نجت
شاشةُ التسعير وحدَها من العطل**: ⛔ **ولم يُنصّ نظيرُها لشاشات الصرف الثلاث.**
✅ **وسُدَّت الثغرة في `distribution-design.md` §③ وفي قاعدةِ حَوْكمةٍ في
`CLAUDE.md`** — ⟵ **فلا تُبنى قائمةُ صرفٍ من الكتالوج مرةً أخرى.**

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

⛔ **لا بندَ حَوْكمةٍ جديداً في هذا التشغيل** — ★ **ولا `ADR` مقترح ولا `CR`.**

⏳ **والبنودُ القائمة من قبلُ كما هي:** `CR-007` · `CR-008` · `CR-009`
**تنتظر توقيعك** — ⛔ **ولم تُعتمَد ذاتياً.**

⛔⛔★★★ **ولا إذنَ نشرٍ إنتاجيّاً طُلب ولا يُستحَقّ هنا** — ★ **ولا أمرَ
واحدٌ مسّ `qtms-orimind-c001`.**

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

⚠️★★ **والقائمةُ المنجَزة الحسّاسة كما يقرؤها `track_implementation.py` —
14 وحدة:** `WU-000` · `WU-002` … `WU-008` · `WU-010` … `WU-014` · `WU-027`
(★ **و11 وحدةً مخطَّطةً حسّاسةً بعدُ: `WU-015` … `WU-025`**).

⛔⛔★★★ **وهذا التشغيل يمسّ ثلاثاً منها في مسارٍ ماليّ مباشر** — ★ **فتُعاد
مراجعتُها بشرياً بهذا التغيير تحديداً:**

| الوحدة | لماذا يلزم النظرُ فيها ثانيةً |
|---|---|
| `WU-006` · `WU-012` · `WU-014` | ⛔⛔ **نموذجُ اختيار النوع تغيّر في الشاشات الثلاث** — ★ **وما يُرسَل صار مفتاحَ الدفتر لا معرّفَ سجلِ نوع** |
| ⛔⛔★★★ **ومعها `sackId`** | ★ **صارت السحابةُ تشتقّه وتكتبه في كل حركة خروجٍ من جونية** — ⟹ **وعليه يُبنى إيرادُ الرعوي** (`A-13`): ⛔ **فخطأٌ فيه خطأٌ في مالِ إنسان.** ⚠️ **ولا يُحسَب أثرُه كاملاً قبل `WU-015`** (`M14` — تقييم الجونية) |

⛔ **والتحققُ الآليُّ شرطٌ ضروريٌّ لا كافٍ لهذه الوحدات** — ★ **ولا يُغني عن
مراجعتك.**

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| `03-architecture/risks-and-technical-debt-register.md` | ✅ **[`DEBT-86`] ⟶ مُغلَق** بأشطره الثلاثة وقياسِه الحيّ |
| `00-governance/upds-workflow/implementation-plan.md` | ★★ **نتيجةُ بوابة §د.3 ⟶ «الحاجزُ رُفع»** · ★ **قسمٌ للتشغيل الخاصّ** · ★ **وبنودُ الأثر المُعلَن في `WU-006` و`WU-012` و`WU-014` صارت مُغلَقة** |
| `04-design/module-design/distribution-design.md` | ⛔⛔ **مصدرُ خيارات النوع** (§③) · ⛔⛔ **واشتقاقُ `sackId` في المعاملة** (جدولُ بنية السطر) |
| `11-devops-and-infrastructure/environments.md` | ⛅ **المراجعة `qtms-callables-00027-z5h` · الصورة `callables:debt86` · 47 عملية** ⛔ **بلا عمليةٍ جديدة** |
| `CLAUDE.md` | ★ **راجع §10** |

⛔ **ولم تُمَسّ:** `openapi.yaml` · `endpoints/` · `erd.md` ·
`data-dictionary.md` · `migrations-log.md` — ★ **فلا عمليةَ جديدة ولا حقلَ
جديدٌ ولا مجموعةَ جديدة**: ⟵ **و`sackId` حقلٌ قائمٌ موصوفٌ في
`schema/inventory-ledger.md` صار يُملأ فعلاً لا أكثر.**

⛔⛔★★★ **و`firestore.rules` لم تُمَسّ بحرف** — ★ **مقيسٌ:
`git status --porcelain firestore.rules` فارغ.**

---

## 10. حالة `CLAUDE.md`

✅ **حُدِّث — ونقطةُ التحوّل حقيقية:** **رفعُ الحاجز الأخير أمام بوابة المرحلة
الأولى**، ★ **وقاعدةُ حَوْكمةٍ جديدة تمنع تكرارَ العطل.**

| البند | القيمة |
|---|---|
| الأداة | `sync_claude_md.py` — ⛔ **ولا تحريرَ يدوي** |
| `action` | `managed-block-updated` |
| `human_content_lines_preserved` | **20** |
| `new_total_lines` | **250** — ★ **سقفُ [`AM-005`] تماماً** |

⛔⛔★★★ **ولا هامشَ بعد اليوم:** ★ **أُضيفت قاعدةٌ واحدة وقوبلت بدمج سطرَي
قسمِ [`DEBT-86`] في واحد** ⛔ **بلا حذفِ حقيقةٍ واحدة** — ⟵ **وأولُ قاعدةٍ
تالية تستوجب نقلَ المتخصص إلى `.claude/rules/`** ⛔ **لا تجاوزَ السقف**،
★ **وقد سُجِّل ذلك في بند [`AM-005`] نفسِه.**

---

## 11. الخطوة التالية الموصى بها

### ✅★★★ المرحلةُ الأولى: **حواجزُها مرفوعة** — ⛔ **وإغلاقُها لم يُعلَن بعد**

★ **وما بقي بابان لا واحد:**

| # | ما بقي | المرجع |
|:-:|---|---|
| ① | ★★ **مطابقةُ معايير القبول** `AT-01`…`AT-37` · `AT-55`…`AT-62` · `AT-67` | `product-roadmap.md` §5 |
| ② | ★★ **بروتوكولُ نهاية المرحلة** — **مراجعةُ التتبع · تقريرُ الحالة · `developer-onboarding.md` · فحصُ سجل الديون** | UPDS-05 §7 |

⛔⛔ **ولا تُفتَح المرحلةُ الثانية** (`WU-015` ⟶ `WU-016` ⟶ `WU-017` ⟶
`WU-018`) **قبلهما.**

⚠️★★ **وثلاثةُ ديونٍ تبقى مفتوحةً بقرارٍ مُعلَن** — ⛔ **ولا واحدَ منها حاجز:**
`DEBT-66` (نقرةٌ إضافية في المركز المعلّق) · `DEBT-67` (فلترُ تاريخٍ لم يلزم
بعد) · `DEBT-70` (⚠️ **قيدُ نطاقٍ ناقصٌ في قاعدتَي قراءة** — ★ **يُعالَج في
أول تشغيلٍ ينشر `firestore.rules`**).

⏳ **وتنتظر توقيعَك:** `CR-007` · `CR-008` · `CR-009`.

---

*تقرير مولَّد بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
