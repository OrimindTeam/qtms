# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-22

**رقم تشغيل التنفيذ:** 12
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة**)
**تقدّم التنفيذ:** 0٪ (0 منجَزة من 26)
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** 1 (`IQ-007` — **جديد**)

---

## 1. ملخص تنفيذي

★ **الفحص السادس من «الخطوة صفر» اكتمل** — وكان آخر فحوصها الستة المعلَّقة.
**12 تخصيصاً متزامناً** على العدّاد نفسه في قاعدة بيانات حقيقية أنتجت
`[1…12]`: **لا تكرار · يبدأ من ١ · بلا فجوة** — **ثلاث مرات متتالية**.

★ **وبُني نموذج الصلاحيات في طبقة النطاق:** 63 مفتاحاً منقولة حرفياً عن
الكتالوج، ونطاق المصادر، وقواعد المنح الثلاث. **ويحرس تطابقَها مع المستند
اختبارٌ ثنائي الاتجاه مُثبَت بالطفرة.**

⚠️ **وأهم ما في هذا التشغيل خطأٌ كشفه التشغيل الحقيقي في كودي أنا:** أول
تشغيل للفحص ⑥ **فشل** — إعادة المحاولة كانت **بلا تباعد**، فالخاسرون يعيدون
في اللحظة نفسها فيتصادمون مجدداً. ⛔ **ولا اختبار وحدة كان ليكشف هذا.**

⏳ **و`grantPermissions` محجوبة بـ`IQ-007`:** `api-overview.md` §3.1 يشترط
لاستدعائها «صلاحية تخصيص صلاحيات فردية»، ⛔ **ولا مفتاح لها في الكتالوج** —
ومعضلة إقلاع: **حساب المالك بلا صلاحيات إطلاقاً**.

**246 اختباراً ناجحاً · ٠ فاشلاً.** ⛔ **ولا وحدة أُعلنت منجَزة.**

---

## 2. بوابة الدخول — نتيجة الفحص

✅ **البوابة العامة (٨ شروط) متحققة** — فُحصت من جديد في هذا التشغيل،
**والشرط 8 خصوصاً**: الـ14 ADR كلها «مقبول/معتمد» ولا واحد بحالة «مقترح».
✅ **وبوابة `WU-000` (٥ شروط) متحققة.**

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

⛔ **لا شيء.** `WU-000` ما تزال `[قيد التنفيذ]` بصدق — الدوال لم تُبنَ بعد
وأثر العيّنة لم يُزَل (`DEBT-14`).

### 3.2 ما بُني وتُحقِّق منه

| المكوّن | المسار | التحقق |
|---|---|---|
| **مفاتيح الصلاحيات** | `…/identity_access/domain/permission.dart` | **63 مفتاحاً** · واسم القيمة هو المفتاح حرفياً فلا جدول تحويل |
| **نطاق المصادر** | `…/identity_access/domain/source_scope.dart` | `all` أو قائمة · `canAccessSource` · `scopeIsWithin` |
| **قواعد المنح** | `…/identity_access/domain/permission_grant.dart` | `BR-M1-03` · `FR-M1-08` · **ومنع تعديل صلاحيات النفس** |
| **مفاتيح العدّادات** | `…/core/counter_key.dart` | المستند بالنوع واليوم · ★ **والجونية بالمصدر واليوم** |
| **مُخصِّص الترقيم** | `functions/lib/src/counter_allocator.dart` | معاملة ذرّية · إعادة محاولة **بتباعد أسّي وتشويش** |
| **أداة الفحص ⑥** | `functions/tool/check_six_concurrent_allocation.dart` | ⛔ **ترفض الإنتاج** بفحص لاحقة المشروع |

**الفحوصات التي شُغِّلت فعلاً:**

| الفحص | النتيجة |
|---|---|
| `dart test` — طبقة النطاق | ✅ **46 اختباراً** (16 + **30 جديدة**) |
| `dart test` — العمليات السحابية | ✅ **23 اختباراً** |
| `flutter analyze` · `flutter test` | ✅ نظيف · **9 اختبارات** |
| `dart analyze` (الحزمتان) | ✅ **`No issues found!`** |
| `npm test` — بوابتا الحماية | ✅ **168/168** |
| ★★ **الفحص ⑥ على قاعدة حقيقية** | ✅ **نجح ٣ مرات** — `[1…12]` بلا تكرار ولا فجوة |
| ★ **طفرة على حارس الكتالوج** | ✅ **فشلت ٣ فحوص** عند تحريف مفتاح، ونجحت بعد الإعادة |

**المجموع: 246 اختباراً · 246 ناجحاً · ٠ فاشلاً.**

### 3.3 وحدات محجوبة

⛔ **لا وحدة محجوبة بالكامل.** و`IQ-007` يحجب **دالتين بعينهما**:
`grantPermissions` و`setSourceScope`.
★ **وقواعد المنح نفسها غير محجوبة وقد بُنيت** — لأنها تبقى صحيحة أياً كانت
إجابة `IQ-007`.

---

## 4. مخالفات النزاهة المرصودة آلياً

**لا مخالفات** — `integrity_violations: []` · `unparsed_lines: []`.

---

## 5. أسئلة التنفيذ المُصعَّدة

| IQ | السؤال | المتعطّل | لماذا لم يكن قابلاً للاستنتاج |
|---|---|---|---|
| ★ `IQ-007` | **من يُصرَّح له باستدعاء `grantPermissions`؟** | `grantPermissions` · `setSourceScope` | `api-overview.md` §3.1 يشترط «صلاحية تخصيص صلاحيات فردية» و`FR-M1` §3 يسمّي **تسع** صلاحيات لهذه الوحدة، ⛔ **ولا مفتاح لأيٍّ منها في الكتالوج** (تُحقِّق منه آلياً: 63 مفتاحاً، ولا واحد يحوي `user`/`role`/`permission`/`scope`/`grant`). ★ **والكتالوج لم يُغفلها سهواً** — §3 يقول «ولا مفتاح يُتيحه **من التطبيق**»، وهذا يُجيب عن الكتابة المباشرة **ولا يُجيب عمّن يستدعي العملية**. ⚠️ **ومعضلة الإقلاع تجعله عاجلاً**: المالك بلا صلاحيات، فإن اشترطت الدالة صلاحيةً **قفل النظام على نفسه** |

⛔ **ولم أخترع مفتاحاً ولا مسار إقلاع** — وهذا **أخطر مسار في النظام**
(`api-overview.md` §2 الشرط 4)، واختراع نموذج تفويض له يخالف `BR-M1-07`.

---

## 6. تعارضات وثغرات اكتشفها التنفيذ

| # | الثغرة | كيف عولجت |
|:-:|---|---|
| 1 | ★★ **عيب في كودي كشفه التشغيل الحقيقي:** إعادة المحاولة بلا تباعد ⟵ **12 محاولة متزامنة تفشل جميعاً** بـ`Aborted due to cross-transaction contention` | **أُضيف تباعد أسّي بتشويش عشوائي**، ورُفع `maxAttempts` إلى 8. ✅ **ونجح الفحص بعده ثلاث مرات**. ووُثِّق العرَض في `troubleshooting-guide.md` والأداة في `build-and-run-instructions.md` §2.2.2-ب |
| 2 | ★ **`FR-M1` §3 يسمّي تسع صلاحيات لإدارة المستخدمين ولا مفتاح لأيٍّ منها** | ⛔ **لم يُخترَع مفتاح** — صُعِّد في `IQ-007` بثلاثة خيارات، **وأُضيف اختبار يفشل عمداً لو ظهر مفتاح كهذا** حتى لا يتسلّل بلا قرار |
| 3 | **مفتاح `document_counters` غير منصوص** بينما `daily_sack_counters` منصوص «المصدر × التاريخ» | ★ **استُنتج بثقة لا خُمِّن:** `naming-conventions.md` §5 يُفرد **الجونية وحدها** بجملة «مستقل لكل مصدر» — **والتخصيص بالذكر هو ما يجعله استثناءً**. فبقية المستندات عدّادها **بالنوع واليوم**، ووُثِّق التسبيب في الكود |

⛔ **ولم يُعدَّل أي مستند محكوم بحَوْكمة** — ولا `srs.md` ولا ADR معتمد ولا
`coding-standards.md` ولا `naming-conventions.md` ولا `api-overview.md`.
★ **و`permissions-catalog.md` لم يُمَسّ** رغم أنه `LIVING` — لأن الإضافة إليه
**قرار `IQ-007`** لا اجتهادي.

---

## 7. بنود بوابة الحَوْكمة

⛔ **لا شيء ينتظر اعتماداً** — `ADR-0013` و`ADR-0014` اعتُمدا في التشغيل
السابق، ولم يُنشأ ADR جديد في هذا التشغيل.
★ **و`IQ-007` لا يستدعي ADR** — الكتالوج `LIVING` وله مسار إضافة موثَّق (§6).

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

| # | البند |
|:-:|---|
| ① | ★★ **`IQ-007` — نموذج التفويض لأخطر مسار في النظام.** ⛔ **ولا تُبنى الدالة قبل حسمه** |
| ② | ★ **`DEBT-17` جديد:** `ADR-0013` يجعل كل قاعدة تفويض **مكتوبة مرتين** — ونسختان تفترقان. ✅ **وأُنجزت أولى لبنات التخفيف:** `Permission` مصدر واحد للمفاتيح بحارس ثنائي الاتجاه |
| ③ | ★ **`DEBT-14` — أثر العيّنة ما يزال قائماً** على التجريبية: كود · مجموعة · مستند وهمي · مشغّل · خدمة · حساب خدمة. ⚠️ **وأُضيفت إليه عدّادات `SRC-CHECK6-*`** من تشغيلات الفحص ⑥ |
| ④ | ★ **App Check بفرض مُطفأ** — إلزامي قبل أول نشر إنتاجي |
| ⑤ | ★ **`DEBT-13`** — رسالة «خدمات المنصة غائبة» ناقصة · و**`DEBT-16`** فكّ protobuf ناقص |
| ⑥ | **حساب المالك بلا صلاحيات** — ويظل كذلك حتى يُحسَم `IQ-007` |

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| `04-design/module-design/identity-access-design.md` | ★ **الحالة «مخطَّط» ⟵ «جزئي»** + §6-ب يسرد ما نُفِّذ فعلاً وما لم يُنفَّذ |
| `03-architecture/risks-and-technical-debt-register.md` | ★ **`DEBT-17` جديد** (ازدواج فحص التفويض) |
| `07-development/build-and-run-instructions.md` | ★ **§2.2.2-ب جديد** — أداة الفحص ⑥ وتحذير التباعد |
| `07-development/troubleshooting-guide.md` | عرَض `cross-transaction contention` بحلّه |
| `00-governance/upds-workflow/implementation-plan.md` | **الفحص ⑥ اكتمل** · 8 فحوص جديدة · الملفات المنتَجة · جدول البنود |
| `00-governance/upds-workflow/implementation-questions.md` | ★ **`IQ-007` جديد** |

⛔ **لم يُحدَّث** `erd.md` ولا `openapi.yaml` — لا تغيّر في المخطَّط ولا
Endpoint. ★ **ومجموعتا العدّادات موصوفتان في القاموس أصلاً** فلا جديد فيهما.

---

## 10. حالة `CLAUDE.md`

★ **حُدِّث** — نقطة التحوّل 5: **الفحص ⑥ اكتمل** · عدد الاختبارات · نموذج
الصلاحيات · و`IQ-007` المفتوح.

| الحقل | القيمة |
|---|---|
| `action` | `managed-block-updated` · `new_total_lines` **142** من حدّ 200 |
| `human_content_lines_preserved` | `0` |

---

## 11. الخطوة التالية الموصى بها

| # | الخطوة | مَن |
|:-:|---|---|
| **1** | ★ **أجب عن `IQ-007`** — يفتح `grantPermissions` **ويمنح المالك صلاحياته أخيراً** | **أنت** |
| 2 | بناء دوال الكتابة بنمط `ADR-0013` — وتستخدم `CounterAllocator` الجاهز | `/upds-docgen:implement` |
| 3 | إغلاق قواعد الحماية على الكتابة المباشرة + تحديث الـ168 اختباراً | `/upds-docgen:implement` |
| 4 | ★ **إزالة كل أثر عيّنة `DEBT-09` وعدّادات الفحص (`DEBT-14`)** ثم اعتماد `WU-000` | `/upds-docgen:implement` |

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
