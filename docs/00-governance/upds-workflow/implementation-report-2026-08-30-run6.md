# تقرير تشغيل التنفيذ — 2026-08-30 (التشغيل 43)

**الطلب:** معالجة `DEBT-63` — **زرُّ «نسخ أسعار أمس»** (`FR-M9-13`) —
بالبدء من فرضية `QtmsBulkActionBar` و`Row([Expanded(Text), action])` **بلا
افتراض أنها السبب**، ★ **وإثباتِ آلية الرسم بمجسٍّ صحيح المدى قبل أي إصلاح.**

---

## 1. ⛔⛔★★★ النتيجة الصريحة — لم يُصلَح، والسببُ مقيس

⛔ **`DEBT-63` ما يزال مفتوحاً** — ★ **ولم يُطبَّق أيُّ إصلاح**، ⟵ **لأن كلَّ
مرشَّحٍ جُرِّب إمّا أبطلَه القياس أو أحدَث ارتداداً.** ✅★★ **والمستودع رُدَّ
إلى حالة `DEBT-62` المُتحقَّقة بالضبط** ⛔ **بلا بقايا تجربة.**

⛅★★★ **لكنّ التشخيص تقدّم تقدُّماً حقيقياً:** ★ **الآليةُ موصوفةٌ الآن بخمس
قياسات**، ★ **وثلاثُ فرضياتٍ أُبطلت بالتجربة لا بالرأي** ⟵ **فلا يُعاد طرقُها.**

---

## 2. ⛅ الآليةُ كما قِيست — `Pixel_6_API_36` · بناءٌ نظيف

★ **المجسُّ صحيحُ المدى:** `GlobalKey` **على مستوى الملف** ⛔ **لا داخل
`build`** (درسُ التشغيل 41)، **مع `addPostFrameCallback` وعيّناتٍ مؤجَّلة.**

| # | القياس | النتيجة |
|---|---|---|
| ① | **هل تصل البيانات؟** | ✅ `n=3` **بمفاتيحَ حقيقية** — `loading=true ⟶ n=3` |
| ② | **هل يُبنى الزرُّ؟** | ✅ **نعم** — **وشجرةُ العناصر صحيحة**: ⛅ **قِيس بـ`Offstage` أن `elementTree offstage=false`** |
| ③ | **هل كائنُ الرسم ابنٌ للصفّ؟** | ✅ **نعم ومربوطٌ فعلاً** — `flexChild[1] isOurs=true` · `flexChildCount=2` |
| ④ | **هل خُطِّط؟** | ⛔⛔ **لا** — `hasSize=false` **و`needsLayout=false` معاً**، ★ **والصفُّ على تخطيطه القديم**: `flexChild[0] RenderParagraph size=379.4×22.0` ⟵ **النصُّ أخذ العرض كلَّه لأن الزرَّ أسهم بصفر** |
| ⑤ | **هل ينفع الدفعُ القسري؟** | ⛔ **لا** — `markNeedsLayout()` **على الصفّ لا يُحدِث تخطيطاً**، **والمُجدوِل خاملٌ** (`phase=idle scheduled=false`) |

⟹ ★★★ **الخلاصة:** **`RenderFlex.performLayout` لم يُعَد تشغيلُه بعد إدراج
الابن**، ⟵ **فالزرُّ يُبنى ويُربَط ولا يُخطَّط ولا يُرسَم أبداً.**
⟹ ★★ **وهو شقيقُ `DEBT-62`** — **شجرةٌ فرعية تتوقّف عن تلقّي التخطيط
والمُجدوِلُ يحسبها نظيفة** — ⛔ **لا عطلُ تخطيطٍ عادي.**

---

## 3. ⛔⛔★★★ فرضياتٌ أُبطلت — ولا يُعاد طرقُها

| الفرضية | كيف أُبطلت |
|---|---|
| **(أ) `Row([Expanded(Text), action])` يقصّ الزرَّ أو يُصفِّر قيوده** — **وهي فرضيةُ البدء** | ⛔ **باطلة:** ★ **الصفُّ يحمل الابنين معاً** (`kids=2`)، **والمقصوصُ يُخطَّط ثم يُقصّ** ⟵ **بينما هذا لم يُخطَّط أصلاً.** ★ **ويشهد لها اختبارٌ قائم:** [`filter_bar_test.dart`](../../../test/core/ui/filter_bar_test.dart) **يُثبت أن الشريط يعرض إجراءه** |
| **(ب) تبديلُ نوع الويدجت في الخانة** (`SizedBox.shrink` ⟷ زر) | ⛔ **باطلة:** ★ **`Offstage` يُثبِّت النوع** ⟵ **والعطلُ باقٍ**: `RenderOffstage size=(0,0)` **بعد ست ثوانٍ** |
| **(ج) الاشتراكُ الحيُّ الثاني على `dailyPricesProvider`** | ⛔ **باطلة:** ★ **`FutureProvider` بقراءةٍ واحدة (`.first`) يُسقِط الاشتراك** ⟵ **والعطلُ باقٍ** |

⛔⛔★★ **ومحاولةٌ رابعة رُفضت لأنها ارتداد:** ★ **رفعُ القراءة إلى `_BulkBar`
مع `ValueKey` يُجدِّد الصفّ** ⟵ ⛔ **أحدَث تعليقاً متكرِّراً للتطبيق** (**ANR
مرّتين على التوالي** — ★ **وبلا أيِّ بناءٍ للشريط أصلاً**) ⟹ ✅ **رُدَّت فوراً.**

---

## 4. ✅ ما رُدَّ — وما بقي

✅★★ **صافي التغيير على الكود: صفر.** ⛅ **مقيسٌ بـ`git diff`:**
★ **`daily_pricing_screen.dart` غائبٌ عن الفرق** (**عاد إلى حالته المُودَعة**) ·
★ **و`inventory_providers.dart` يحمل تغييرَ `DEBT-62` وحدَه** (`isAutoDispose`).

---

## 5. ✅ بوابة التحقق — شُغِّلت فعلاً

| البوابة | النتيجة |
|---|---|
| `flutter test` | ✅ **401 · All tests passed** |
| `dart test` (النطاق) | ✅ **412 · All tests passed** |
| `dart test` (السحابة) | ✅ **461 · All tests passed** |
| `flutter analyze lib/` | ✅ **No issues found** |
| **قواعد الحماية (236)** | ⚠️ **لم تُعَد** — ⛔ **عمداً**: ★ **`firestore.rules` وملفات اختبارها لم تُمَسّ بحرف في هذا التشغيل** (⛅ **مقيسٌ بـ`git status`**) |
| ⛅ **المحاكي — `DEBT-62`** | ✅ **الدخولان الأول والثاني يُرسَمان** ⛔ **بلا ANR ولا انحدار** |

★ **فالمجموع 1510 قائمٌ** — **1274 أُعيد قياسها الآن، و236 لم يتغيّر مدخلُها.**

---

## 6. ⚠️ الحالة

**27 وحدة — 9 منجَزة · 0 قيد التنفيذ · 17 مخطَّطة · 0 محجوبة · 1 تحتاج إعادة
تحقق.** ✅ `integrity_violations: []` · ✅ **`IQ` 30/30 محسومة.**

⛔ **و`WU-005` تبقى `[يحتاج إعادة تحقق]`** — ★ **فبوابةُ §د.2 لا تكتمل على
ميزةٍ لا يمكن اختبارها** (`FR-M9-13`).

---

## 7. ★ الخطوة التالية المقترحة

★ **عزلُ الآلية في عيّنةٍ صغيرة خارج المشروع** (**صفٌّ + ابنٌ يظهر بعد
`Future`**): ⟵ **فإن تكرّرت فهي عطلُ إطارٍ يُرفَع لـ`flutter` بعيّنةٍ صغيرة**،
⛔ **وإلا فالسببُ في تركيب هذه الشاشة** ⟵ **ويُقاس بـ`flutter run --profile`
وأدوات المُعايَنة بدل بناءِ `apk` كامل لكل فرضية** (★ **أرخصُ وأسرع دورة**).

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

★ **بلا تغيير:** `WU-000` · `WU-026` · `WU-001` · `WU-002` · **`WU-004`** ·
`WU-006` · **`WU-007`** · `WU-008`. ⛔ **ولم يُنشَر شيء ولم تُمَسّ صلاحيةُ IAM
في هذا التشغيل** (البروتوكول §ز).
