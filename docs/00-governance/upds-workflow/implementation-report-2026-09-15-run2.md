# تقرير تشغيل التنفيذ — 2026-09-15 · التشغيل الثاني (`AM-021`)

**المهارة:** `/upds-docgen:amend` · **الحالة:** ★ **(٢) مستندات وكود معاً** —
⭐ **صرّح بها المالكُ حرفياً في نصّ الطلب** («الحالة المطلوبة صراحة: الحالة (٢)
… أُصرّح بهذه الحالة صراحة الآن، لا تسأل عنها»)، ⛔ **فلا استنتاجَ ولا سؤال.**

**المصدر:** حزمةُ `ux_review_evaluation/` — ★ **ستُّ شاشاتٍ محدَّدةٍ بالنطاق من
عائلة المخزون** (مخزون اليوم · التوريد مخزني · التسعير اليومي · الإتلاف ·
الجرد · مالية الجواني) ⛔ **ولا بندَ خارجها**، ⛔ **ولا شريطَ تبويبٍ سفلي**
(**الاستثناءُ الدائم**).

---

## 1. ما نُفِّذ — ستةُ نطاقات

| # | النطاق | مكاسبُ سريعة | تحسيناتٌ بنيوية | الحال |
|:-:|---|---|---|---|
| ① | **مخزون اليوم** (8.5) | — | — | ✅ **قاعدةٌ نمطيةٌ إلزامية في `ui-guidelines.md` §6** ⛔ **لا تغييرَ كود** |
| ② | **التوريد مخزني** (7) | **ازدواجُ «المصدر»** | **هرمُ بطاقة الوارد والجونية** | ⛔ **المكسبُ السريع: لا تغييرَ لزم — مُغلَقٌ سلفاً بـ`AM-015` ⑤** · ✅ **والبنيويُّ مطبَّق** |
| ③ | **التسعير اليومي** (7.5) | **زرٌّ ثنائي الحالة + فراغُ مرشِّح** | — | ✅ **مطبَّق** |
| ④ | **الإتلاف** (8) | **`QtmsActionStatus`** | — | ✅ **مطبَّق** |
| ⑤ | **الجرد** (6.5) | — (**المراجعةُ نفسُها نفت وجودَ مكسبٍ سريعٍ حقيقي**) | **الورقةُ المدمّرة + حرسٌ يُسمّي سطرَه** | ✅ **مطبَّق** |
| ⑥ | **مالية الجواني** (6.5) | **هياكلُ تحميلٍ للبطاقتين** | **شارةُ «غير نهائي» على إجماليات اليوم** | ✅ **مطبَّق** |

★ **والترتيبُ كما أمر المالك: «مكاسب سريعة» أولاً ثم «بنيوية» في كل نطاق.**

---

## 2. ⚠️⚠️★★★ بندٌ لم يُنفَّذ لأنه لم يكن قائماً — وسُجِّل ولم يُدَّعَ

**التوصيةُ ② مكسباً سريعاً:** «**في `SackHeaderFormSheet`، أزل `labelText`
الداخلي من `DropdownButtonFormField` «المصدر» ما دام `_FieldLabel` الخارجي
موجوداً**».

✅ **مُغلَقٌ قبل هذا التشغيل بـ`AM-015` ⑤** — **مقيسٌ بطريقتين:**

| الدليل | النتيجة |
|---|---|
| **قراءةُ الكود** | `sack_intake_screen.dart` — `DropdownButtonFormField` **بلا `labelText`** وبتعليقٍ صريحٍ يشرح لماذا (`AM-015` ⑤) |
| **بحثٌ نصّيٌّ قاطع** | `grep "labelText: 'المصدر'"` **لا يُطابق هذا الملف إطلاقاً** — ⛔ **ويُطابق تسعَ شاشاتٍ أخرى بلا `_FieldLabel` خارجيّ فلا ازدواجَ فيها** |
| ⛅ **المحاكي** | **ورقةُ «جونية جديدة» تعرض «المصدر» *مرةً واحدة*** — التسميةُ الخارجيةُ وحدَها (لقطة) |
| **الشرطُ الثاني** («**وطبّق نفس الاختيار في `SackLinesFormSheet` إن وُجد نفس الازدواج**») | ⛔ **غيرُ متحقّق — لا `_FieldLabel` واحدةً في تلك الورقة أصلاً** |

⟹ ★★★ **ولو نُفِّذ حرفُ التوصية لكان «الإصلاحُ» إزالةَ التسميةِ الباقية
الوحيدة** — ⟵ **فتصير الورقةُ بحقلٍ بلا اسم.** ⛔ **والمراجعةُ وُضعت على حالةٍ
أسبقَ من الكود**، ★ **وهذا درسُ التشغيل الأول في `implementation-lessons.md`.**

---

## 3. ✅★★★ بندٌ كان خارج النطاق — رُفع إليك ثم اعتمدتَه فنُفِّذ

★ **في التسليم الأول رُفع إليك أن سطرَ حالة *الجرد* ما يزال `Text` عارياً
برمزِ «✅»/«❌»** (`stocktake_screen.dart` — `widget.status`)، ⛔ **ولم يُنفَّذ
صامتاً لأن نصَّ الطلب حصر البند ④ في «الإتلاف».** ✅ **ثم اعتمدتَه صراحةً
فنُفِّذ** — **وقرارُك الثاني في نفس الرسالة أغلق بندَ «ازدواج المصدر» نهائياً.**

| البند | القرار | الحال |
|---|---|---|
| ① **ازدواج «المصدر» في `SackHeaderFormSheet`** | ⛔ **مغلقٌ نهائياً — ولا يُعاد فتحه** | ⛔ **لم يُمسّ الملفُّ بحرفٍ لهذا البند** |
| ② **سطرُ حالة الجرد** | ✅ **اعتُمد التنفيذ بالنمط المرجعي نفسِه** | ✅ **مطبَّق** |

### النمطُ المرجعي المعتمَد — ⛔ ولا نمطَ موازٍ

★ **قُرئ `disposal_screen.dart` أولاً** ثم نُقل حرفاً بحرف:

| الطبقة | في الإتلاف (المرجع) | في الجرد (المُطبَّق) |
|---|---|---|
| **النموذج** | `_DisposalStatus {message, succeeded}` | `_StocktakeStatus {message, succeeded}` |
| **العرض** | `switch (_status) { null / succeeded:true ⟶ .success / _ ⟶ .rejection }` | ★ **الصيغةُ نفسُها في موضعَي العرض** (نموذجُ البدء ونموذجُ العدّ) |
| **المكوّن** | `QtmsActionStatus` من `sticky_action_bar.dart` | ★ **المكوّنُ نفسُه** ⛔ **ولم يُنشَأ شيء** (§8 المحظور 11) |

⛔⛔★★★ **ولا تغييرَ وظيفياً واحداً:** ★ **الرسائلُ الخمسُ نفسُها ومواضعُ
استدعائها نفسُها والشروطُ نفسُها** — ⟵ **وإنّما سقط الرمزُ من نصّها** (**فقد
كان هو *آليةَ العرض* القديمة**) **وصار الحكمُ حقلاً** `succeeded` **تترجمه
الشاشةُ ثلاثيةً لونية.** ⛔ **ولا `ADR` مُسّ** — ★ **القاعدةُ مكتوبةٌ سلفاً في
`design-system.md` §6-ز، وهذا تطبيقٌ لها لا تعديلٌ لها.**

### اختباراتٌ أُضيفت أو عُدِّلت

| النوع | التفصيل |
|---|---|
| **أُضيفت (7)** | **رفضُ البدء بلا نوع** (`danger` + `error_outline`) · **بدءٌ ناجح** (`success` + `check_circle_outline`) · **حرسُ العدّ الناقص** · **اعتمادٌ ناجح** · **إلغاءٌ ناجح** · **رفضُ السحابة بنصّ الكتالوج** · **وعبورُ اللافتة تبدّلَ المرحلة بثلاثيتها** |
| **عُدِّلت (3)** | **نصّا حرسِ العدّ** (بلا «❌») · **وعنوانُ اختبار «اعتمادٌ ناجح يُبقي اللافتة»** |
| **اختبارُ الطفرة** | ✅ **بحذف فرع `succeeded: true` قسراً سقطت 4 اختباراتٍ فعلاً** ثم أُعيد الفرع ⟵ **فالحارسُ ليس شكلياً** |

### الفحوص

| الفحص | النتيجة |
|---|---|
| `flutter analyze` | `No issues found!` — ✅ |
| `flutter test` | **895 اختباراً · `All tests passed!`** (**كان 868 ⟵ +27**) — ✅ **نجح** |
| `packages/qtms_domain` — `dart analyze` / `dart test` | `No issues found!` / **868** — ✅ |
| `functions` — `dart analyze` / `dart test` | `No issues found!` / **820** — ✅ |
| `test/security-rules` — `npm test` | **264 pass · 0 fail** — ✅ |
| ★ **المجموع النهائي** | **2847 اختباراً آلياً · ٠ فاشلاً** |

### ⛅ المحاكي `Pixel_6_API_36` — 4 مشاهد

| # | المشهد | النتيجة |
|:-:|---|---|
| 1 | **بدءُ جردٍ بلا نوع** | ✅ **شريطُ `danger` كاملٌ** (تعبئة · حدّ · أيقونةٌ متجهية · نصٌّ أحمر) **بـ«اختر نوعاً واحداً على الأقل لجرده.»** ⛔ **بلا «❌»** |
| 2 | **بدءٌ ناجح** | ✅ **شريطُ `success` أخضرُ بـ`check_circle_outline` و«بدأ الجرد STK-20260914-001»** ⛔ **بلا «✅»** |
| 3 | **عبورُ تبدّل المرحلة** | ✅ **اللافتةُ باقيةٌ بعد استبدال نموذج البدء بنموذج العدّ** — ★ **حارسُ ارتداد 2026-09-05 قائمٌ بالنمط الجديد** |
| 4 | **إلغاءٌ ناجح** | ✅ **شريطُ `success` بـ«أُلغي الجرد STK-20260914-001»** — ⛅ **والمسوّدةُ التجريبيةُ أُلغيت فتُركت التجريبيةُ كما كانت** |

⛔ **ولا `FATAL` ولا `E/flutter` في السجل بعد الجولة.**

### تأكيدُ انحصار الأثر

⛔⛔★★★ **ولا تغييرَ وظيفيٍّ ولا أمنيٍّ خارج هذا الإصلاح:** ★ **لم تُمَسّ
طبقةُ النطاق ولا دالةٌ سحابية ولا `firestore.rules` بحرف** · ★ **ولا بوابةُ
صلاحيةٍ أُضيفت ولا أُسقطت** (`stocktakeWrite` · `stocktakeApprove` ·
`stocktakeCancel` **كما هي**) · ★ **ولا شرطُ حفظٍ ولا حارسٌ خُفِّف** — ⟵ **حرسُ
العدّ الناقص يعمل بالشرط نفسِه، وسببُ الإلغاء يبقى اختيارياً** (`ADR-0020`) ·
★ **ولا حقلَ بياناتٍ جديد يُرسَل ولا يُقرأ** — ⟵ **`_StocktakeStatus` حالةُ
واجهةٍ محليةٌ لا تُسافر إلى السحابة إطلاقاً.**

---
## 4. الملفات المُعدَّلة

| الطبقة | الملفات |
|---|---|
| **الشاشات** | `counted_intake_screen.dart` · `sack_intake_screen.dart` · `daily_pricing_screen.dart` · `disposal_screen.dart` · `stocktake_screen.dart` · `sack_finance_cards.dart` |
| **مكوّناتٌ مشتركة** | `inventory_widgets.dart` (**`emptyActionLabel`/`onEmptyAction` على المبدِّل القائم** ⛔ **لا مبدِّلَ ثانٍ** — §8 المحظور 11) |
| **الاختبارات** | `inventory_screens_test.dart` · `sack_intake_screen_test.dart` · `daily_pricing_screen_test.dart` · `disposal_screen_test.dart` · `stocktake_screen_test.dart` · `sack_finance_screen_test.dart` — ★ **+27 اختباراً** |
| **البدائل** | `fake_inventory.dart` (**نتيجةٌ مؤجَّلة**) · `fake_stocktake.dart` (**`cancelReasons`**) · `fake_sacks.dart` و`fake_sack_valuation.dart` (**`holdFinance`/`holdBalance`**) |
| **المستندات** | `ui-guidelines.md` · `design-system.md` · `inventory-design.md` · `sack-valuation-design.md` · `sack-intake-design.md` · `implementation-plan.md` · `implementation-lessons.md` · `amendment-log.md` |

⛔⛔★★★ **ولا طبقةَ نطاقٍ مُسّت ولا دالةٌ سحابية ولا `firestore.rules` بحرف.**

---

## 5. الفحوص — بمخرَجٍ فعلي

| الفحص | النتيجة |
|---|---|
| `flutter analyze` | `No issues found!` — ✅ **نجح** |
| `flutter test` | **895 اختباراً · `All tests passed!`** (**كان 868 ⟵ +27**) — ✅ **نجح** |
| `cd packages/qtms_domain && dart analyze` | `No issues found!` — ✅ **نجح** |
| `cd packages/qtms_domain && dart test` | **868 · `All tests passed!`** — ✅ **نجح** |
| `cd functions && dart analyze` | `No issues found!` — ✅ **نجح** |
| `cd functions && dart test` | **820 · `All tests passed!`** — ✅ **نجح** |
| `cd test/security-rules && npm test` | **`# tests 264` · `# pass 264` · `# fail 0`** — ✅ **نجح** |
| ★ **المجموع** | **2847 اختباراً آلياً · ٠ فاشلاً** |

★★ **وثلاثُ بوابات تصميمٍ آليةٍ ضمن `flutter test`** (`design_gates_test.dart`)
**مرّت كما هي** — ⛔ **ولا توكنَ أوليٍّ استُدعي من شاشة، ولا قيمةَ لونٍ محفورة.**

---

## 6. ⛅ اختبار المحاكي `Pixel_6_API_36` — 20 مشهداً

**البناء:** `flutter build apk --debug --flavor staging` **بعَلَمَي التعريفات
معاً** (`config/qtms-public-defines.env` + `.secrets/qtms-staging-qa-account.env`
— ⛔ **بمسار الملف لا بقيمته**) ⟵ `√ Built app-staging-debug.apk` ·
**تثبيتٌ وإقلاع** ⟵ `topResumedActivity=…MainActivity` ⛔ **ولا `FATAL` ولا
`E/flutter` في السجل بعد الجولة كلِّها.**

| # | المشهد | النتيجة |
|:-:|---|---|
| 1 | **بطاقةُ الوارد عدداً** | ✅ **«ماويه · 1 نوع · 120 حبة» عنواناً بارزاً و`INC-20260914-0001` تحته باهتاً** |
| 2 | **ورقةُ رأس الجونية** | ✅ **«المصدر» مرةً واحدة** ⛔ **لا ازدواج** |
| 3 | **بطاقةُ الجونية** | ✅ **«جونية رقم 1» عنواناً · و«ماويه · المطالب به 50.000 كجم · 0 نوع · SCK-20260914-0001» سطراً ثانياً بالترتيب الجديد** |
| 4 | **زرُّ التسعير أثناء النداء** | ✅ **«جارٍ الحفظ…» باهتاً معطَّلاً** (لقطةٌ أثناءه) |
| 5 | **وبعده** | ✅ **«حفظ أسعار اليوم» + شريطُ «حُفظت أسعار اليوم.»** |
| 6 | **مرشِّح «لم يتم» على يومٍ مكتمل** | ✅ **«كل الأنواع مسعَّرة بالفعل ضمن هذا المرشِّح» + زرُّ «اعرض الكل»** ⛔ **لا رسالةَ الفراغ العامة** |
| 7 | **«اعرض الكل»** | ✅ **رفع المرشِّح فعلاً فعاد صفُّ «الجوده»** |
| 8 | **الإتلاف — رفضٌ** | ✅ **شريطُ `danger` كاملٌ** (تعبئة · حدّ · أيقونةٌ متجهية · نصٌّ أحمر) ⛔ **بلا «❌»** |
| 9 | **الإتلاف — نجاحٌ** | ✅ **شريطُ `success` أخضرُ بـ`check_circle_outline`** (`DSP-20260914-0001`) ⛔ **بلا «✅»** |
| 10 | **إجمالياتُ اليوم في المالية** | ✅ **«⏳ غير نهائي — 1 جونية بضريبةٍ معلّقة» *فوق* صفّ «إجمالي الضريبة: 0 ريال»** |
| 11 | **حرسُ عدّ الجرد** | ✅ **«أدخل العدّ الفعلي لـ«عوارض».»** — ★ **يُسمّي السطر** · ⚠️ **وقِيس أولاً بالرمز «❌» ثم سقط الرمزُ بالقرار ② وصار الشريطُ `danger` يحمل الدلالة** |
| 12 | **حقلُ العدّ الناقص** | ✅ **حدٌّ أحمرُ + «مطلوب — أدخل العدّ الفعلي»** |
| 13 | **ضغطةٌ واحدة على «إلغاء الجرد»** | ✅ **لم تُلغِ شيئاً — فتحت الورقةَ فقط** |
| 14 | **محتوى الورقة** | ✅ **عنوانٌ برقم المستند · جملةُ أثر · «سبب الإلغاء (اختياري)» · زرٌّ أحمر «⚠ تأكيد إلغاء الجرد» · «تراجع» منفصل** |
| 15 | **«تراجع»** | ✅ **أغلق الورقةَ والمسوّدةُ باقيةٌ مفتوحة** |
| 16 | **«تأكيد إلغاء الجرد»** | ✅ **أُلغي `STK-20260905-002` فعلاً** — ★ **بسببٍ فارغٍ رغم وجود نصٍّ في «ملاحظة على الاعتماد»** (**الفصلُ البنيوي**) |
| 17 | ⑦ **بدءُ جردٍ بلا نوع** (القرار ②) | ✅ **شريطُ `danger` كاملٌ** ⛔ **بلا «❌»** |
| 18 | ⑦ **بدءٌ ناجح** (القرار ②) | ✅ **شريطُ `success` بـ`check_circle_outline`** (`STK-20260914-001`) ⛔ **بلا «✅»** |
| 19 | ⑦ **عبورُ تبدّل المرحلة** (القرار ②) | ✅ **اللافتةُ باقيةٌ بالنمط الجديد** — ★ **حارسُ 2026-09-05 قائم** |
| 20 | ⑦ **إلغاءٌ ناجح** (القرار ②) | ✅ **شريطُ `success`** — ⛅ **والمسوّدةُ التجريبيةُ أُلغيت** |

---

## 7. ⛅⚠️ ما تغيّر في سجل التجريبية — مُعلَنٌ لا مطويّ

**`qtms-orimind-master` وحدها** (`AM-010`: ⛅ **أوامرُ التجريبية تُنفَّذ ذاتياً
وتُسجَّل بمخرَجها**) — ⛔⛔ **ولا شيءَ مسّ `qtms-orimind-c001`:**

| # | ما كُتب | لماذا |
|:-:|---|---|
| ① | `INC-20260914-0001` — **واردُ 120 حبة «الجوده» على «ماويه»** | ⟵ **بلا وارِدٍ لا بطاقةَ تُقاس هرميّتُها، ولا نوعَ يُسعَّر، ولا رصيدَ يُتلَف** |
| ② | `SCK-20260914-0001` — **جونيةٌ بوزنٍ كليٍّ 50 كجم بلا ضريبة** | ⟵ **وهي ما أظهر شارةَ «غير نهائي» فعلاً** ⛔ **لا محاكاة** |
| ③ | **أسعارُ 2026-09-14 لـ«الجوده»** (150 / 130) | ⟵ **بلا تسعيرٍ كاملٍ لا تظهر حالةُ «فراغ المرشِّح»** |
| ④ | `DSP-20260914-0001` — **إتلافُ 5 حبات** | ⟵ **وهو ما أظهر شريطَ النجاح** |
| ⑤ | **إلغاءُ المسوّدة `STK-20260905-002`** | ⟵ **مسوّدةُ اختبارٍ من 2026-09-05 لا بيانَ عميل** — ★ **وإلغاؤها هو المشهدُ الذي يُثبت مسارَ الورقة من طرفٍ إلى طرف** |
| ⑥ | **بدءُ `STK-20260914-001` ثم إلغاؤه في الجلسة نفسِها** (القرار ②) | ⟵ **وهما المشهدان اللذان يُظهِران شريطَي النجاح فعلاً** — ⛔⛔ **ولا تسويةَ واحدةً كُتبت** (**المسوّدةُ لا تمسّ الدفتر والإلغاءُ يَسِمُها ولا يحذفها**) |

---

## 8. الحَوْكمة

⛔⛔★★★ **ولا `ADR` مُسّ ولا `srs.md` ولا `uat-signoff.md`** — ★ **ولا بوابةَ
حَوْكمةٍ لزمت.** ★ **و`ADR-0021` `P6` طُبِّق على الجرد ولم يُعدَّل** (**نطاقُه
اتّسع بالتنفيذ لا بنصٍّ جديد**)، ★ **و`ADR-0020` تُقوّى لا تُضعَّف**:
**السببُ يبقى اختيارياً ولا يُعطِّل زرّاً**، ★ **والفصلُ بين حقلَي عمليتين
يمنع تعبئةً نيابةً عن المستخدم** (**وهو حرفُ القاعدة الباقية**).

**العدّاداتُ بعد التشغيل** (`track_*.py` — **حتمية لا مُقدَّرة**):
`AM` **= 21 · مطلوب 0 · مطبَّق 21** · `WU` **= 29 · منجَز 26 · مخطَّط 3 ·
`[يحتاج إعادة تحقق]` 0 · 89.7٪** · `integrity_violations` **= لا شيء** ·
`has_stale_done_units` **= `false`** (**بعد إعادة تسجيل البصمات عقب إعادة
البوابة كاملةً — بنفس منطق `AM-014` … `AM-020`**).

**لا وحدةَ حسّاسةٍ أمنياً في هذا التشغيل** — ⛔ **ولا مصادقةَ ولا صلاحياتٍ ولا
أسرارَ ولا مدفوعات**: ★ **كلُّ ما تغيّر طبقةُ عرض.**
