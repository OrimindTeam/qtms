# قرارات معلّقة — تشغيلٌ ذاتيّ 2026-08-27 (`ADR-0021`)

> ✅★★★ **حُسم البندان ① و② معاً — 2026-08-28 بقرار المالك «أصلحها وأنجزها
> فعلياً»:** ★ **الخيار ① (تشخيصٌ مأذون) نُفِّذ**، ⟵ **والعطلُ لم يكن في
> مسار الاستدعاء ولا المصادقة قطّ:** ⛔⛔ **بل `DEBT-55` — قراءةُ
> `items/{itemKey}` لمفتاحٍ مركّب.** ✅ **وأُصلح ونُشر وأُثبت حيّاً**
> (`qtms-callables-00018-78c`)، ⟵ ★★ **فعادت الوحدات التسع `[منجز ✅]`**
> ⛔ **ولم يعد لحدّ البند ② محلّ: دورةُ الحفظ تحقّقت.**
> ⛔ **ويبقى هذا الملف سجلَّ توقّفٍ كما هو — لا يُحذَف ولا يُعاد كتابته.**

> ⛔⛔★★★ **هذا الملف سجلُّ توقّفٍ لا سجلُّ إنجاز** — ★ **كلُّ بندٍ فيه أوقف
> المتابعة التلقائية في موضعه**، ⟵ **ولم يُخمَّن له حلٌّ ولم يُنفَّذ.**
> ★ **والقرار للمالك** (`UPDS-05` §4.6 · بروتوكول التشغيل §ج).

---

## ① ⛔⛔★★★ `DEBT-49` — حفظُ الأسعار يُخفق حيّاً على التجريبية

**أين وقف العمل:** ★ **بعد اكتمال شريحة `P3`+`P4` على `DailyPricingScreen`
وتحقُّقها بصرياً على المحاكي** — ⟵ **وعند اختبار دورة الحفظ كاملةً.**

**ما يحدث:** إدخالُ سعرَي نوعٍ ثم «حفظ أسعار اليوم» ⟵ «**تعذّر إتمام العملية.
أعد المحاولة.**»

**ما قِيس فعلاً — ⛔ لا مُقدَّراً:**

| # | القياس | النتيجة |
|:-:|---|---|
| ① | نشرُ الخدمة على التجريبية | ✅ **حيّة** — `qtms-callables-00016-65v` |
| ② | وجودُ المسار وحراستُه لنفسه | ✅ `POST /writeDailyPrices` بلا رمزٍ ⟵ `401` · ومسارٌ مجهول ⟵ لا اتصال |
| ③ | ربطُ `allUsers` بـ`roles/run.invoker` | ✅ **مربوطة** — ⛔ **فليست راية `--no-allow-unauthenticated`** |
| ④ | ظهورُ طلب التطبيق في سجل Cloud Run | ⛔ **لم يظهر في نافذة الفحص إطلاقاً** |

⟵ ★★ **والاستنتاج المحدود المسموح به:** **الإخفاق يقع على الأرجح قبل الخروج
إلى الشبكة** (رمزُ هوية غير مُرفَق · App Check · استثناءٌ قبل النداء)،
⛔ **ولم يُحسَم.**

**⛔ ولماذا لم يُلاحَق:** ★ **تشخيصُه يمسّ مسارَ كتابةٍ ومصادقة** — ⟵ **وهو
أولُ ممنوعات التفويض التلقائي حرفياً.**

**⚠️ وترجيحٌ مهمّ للقرار:** ⛔ **الإخفاق ليس أثراً لشريحة `P4`** — ★ **`_submit`
ومستودعُه والعميلُ المستدعى لم يُمَسّ منها حرفٌ**، ⟵ **والتغييرُ نقلَ الزرَّ
واللافتة موضعاً لا غير.** ★★ **وهو أحدُ وجوه البند المفتوح أصلاً على `WU-005`**:
«**نشرُ خدمة العمليات ثم اختبارُ المحاكي حيّاً**» — ⟵ **فالمسارُ لم يُختبَر
حيّاً قبل اليوم قطّ.**

**الخيارات المعروضة على المالك:**

| # | الخيار | الأثر |
|:-:|---|---|
| ① | **تشخيصٌ مأذون** لمسار الاستدعاء والمصادقة على التجريبية | ★ **يفتح اختبارَ الكتابة الحيّ لكل الشاشات** ⟵ **وهو شرطُ إعادة ستِّ وحدات إلى `[منجز ✅]`** |
| ② | **تأجيلُه** ومتابعةُ شرائح العرض وحدها | ⚠️ **يبقى كلُّ مسار كتابةٍ غيرَ مُختبَرٍ حيّاً** — ⛔ **والوحدات تبقى `[يحتاج إعادة تحقق]`** |

★ **وما جرى فعلاً في هذا التشغيل: الخيار ②** — ⟵ **وشرائحُ العرض تابعت،
وكلُّ شاشةٍ تحمل مسارَ كتابةٍ تُوثَّق بأن دورةَ حفظها لم تُتحقَّق حيّاً.**

---

## ② ⚠️★★ حدُّ التحقق الحيّ لكل شاشةٍ تحمل مسارَ كتابة

★★ **أثرٌ مباشر للبند ①** — ⛔ **ويُعلَن ولا يُطوى:** ⟵ **كلُّ شريحةٍ بعد
`DailyPricingScreen` في هذا التشغيل تحقّقت حيّاً في **العرض والتنقّل والحالات**
وحدها، ⛔ **ولم تتحقّق دورةُ الحفظ فيها.**

★ **وهذا لا يُضعف الشريحة نفسها:** ⟵ **الشرائح تغييراتُ عرضٍ محضة بحكم النمط**،
⛔ **لكن الادّعاء بأن الشاشة «عملت من طرف إلى طرف» غيرُ مسموحٍ به** —
★ **ولذلك تبقى كلُّ الوحدات `[يحتاج إعادة تحقق]` بلا استثناء.**
