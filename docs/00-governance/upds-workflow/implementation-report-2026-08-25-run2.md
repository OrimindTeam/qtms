# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-25

**رقم تشغيل التنفيذ:** 37
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة** — شريحة رأسية كاملة)
**تقدّم التنفيذ:** 14.8٪ (**4 منجَزة من 27**)
**حالة التوثيق وقت هذا التشغيل:** 100٪ (٠ بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** **1** — `IQ-020` ⛔ **ولا تحجب أي وحدة**

---

## 1. ملخص تنفيذي

✅★★★ **أُنجزت `WU-002` — البيانات المرجعية والإعداد التأسيسي من طرف إلى
طرف:** المصادر `M2` · الرعية `M3` · المقاوته `M4` · الأنواع `M5` · والإعداد
التأسيسي `M21` — **بطبقة نطاق مشتركة، وتسع عمليات سحابية مستدعاة، وإغلاق
الكتابة المباشرة في قواعد الحماية، وأربع شاشات قوائم وأربعة نماذج وشاشة
تأسيسية إلزامية**.

★★★ **وأخطر ما في هذه الزيادة قرارٌ تنفيذي جوهري وُثِّق ولم يُنفَّذ صامتاً:**
**الكتابة على البيانات المرجعية تمرّ بدالة سحابية مستدعاة، والقواعد تُغلَق**
— ⟵ **وبه لم يبقَ مسارُ كتابةٍ مباشر واحد للتطبيق في النظام كله.** المبرِّر
والحدود في §6.

**الأرقام:** **735 اختباراً ناجحاً · ٠ فاشلاً** (كانت 618) — **222 حماية ·
207 سحابة · 170 نطاق · 136 تطبيق**، ✅ **وكل البوابات تنجح** ·
✅ **و`analyze` بلا ملاحظة واحدة** · ★★★ **وأربع طفرات جديدة قُتلت كلها.**

⚠️ **والمرحلة الأولى لم تُستهلَك:** بقيت `WU-003`…`WU-011` بحالة `[مخطط]`
— راجع §11.

---

## 2. بوابة الدخول — نتيجة الفحص

### 2.1 البوابة العامة (٨ شروط — `implementation-playbook.md` §1.1)

| # | الشرط | متحقق؟ | المسار المفحوص |
|:-:|---|:-:|---|
| 1 | المنهجية مُصرَّح بها | ✅ | `docs/21-project-management/development-methodology.md` |
| 2 | العمارة والمكدس | ✅ | `docs/03-architecture/architecture-overview.md` · `technology-stack.md` |
| 3 | أسلوب الكود والتسمية ومعالجة الأخطاء | ✅ | `docs/04-design/coding-standards.md` · `naming-conventions.md` · `error-handling-strategy.md` |
| 4 | تعريف «منجَز» والتفرّع والـCommit والإصدار | ✅ | `docs/07-development/*` · `docs/12-deployment-and-release/versioning-scheme.md` |
| 5 | استراتيجية الاختبار | ✅ | `docs/08-testing-and-qa/test-strategy.md` |
| 6 | سياسة الأسرار | ✅ | `docs/09-security/secrets-management-policy.md` |
| 7 | `srs.md` بمتطلبات مرقّمة | ✅ | `docs/02-requirements/srs.md` |
| 8 | **لا ADR بحالة «مقترح» يحكم ما يُنفَّذ** | ✅ | **الـ17 كلها «مقبول»** — فُحصت واحداً واحداً |

### 2.2 بوابة `WU-002` (٥ شروط — §1.2)

| # | الشرط | متحقق؟ | المسار |
|:-:|---|:-:|---|
| 1 | تصميم الوحدة موجود | ✅ | `docs/04-design/module-design/master-data-design.md` |
| 2 | المتطلبات بمعايير قبول | ✅ | `FR-M2` · `FR-M3` · `FR-M4` · `FR-M5` · `FR-M21` |
| 3 | عقد العمليات | ✅ | `docs/05-api/api-overview.md` · `authentication-and-authorization.md` |
| 4 | عقود البيانات | ✅ | `docs/06-database/erd.md` · `data-dictionary.md` |
| 5 | سياسة المصادقة | ✅ | `docs/09-security/authentication-policy.md` |

✅ **البوابتان اجتازتا — والتنفيذ بدأ بحقّ.**

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

| WU | العنوان | الملفات المنتَجة (مختصرة) | الفحوصات التي شُغِّلت ونتيجتها الفعلية |
|---|---|---|---|
| ✅★★★ **`WU-002`** | زيادة: البيانات الأساسية والإعداد التأسيسي من طرف إلى طرف | **النطاق:** `master_data.dart` · `master_data_repository.dart` · `counter_key.dart` (`entityCounterId`) · **السحابة:** `master_data.dart` · `master_data_handler.dart` · `audited_transaction.dart` (`serverTimestampFields` · `queryDocuments`) · `callable.dart` (8 رموز) · `callable_router.dart` (9 عمليات) · `functions.dart` · **القواعد:** `firestore.rules` + 3 ملفات اختبار وبوابة التغطية · **التطبيق:** `master_data_providers.dart` · مستودعان · `contact_picker.dart` + `MainActivity.kt` · 6 ملفات عرض · `router.dart` · `main.dart` · `home_shell.dart` · `error_messages.dart` | ✅ `dart analyze` (النطاق) — **بلا ملاحظة** · ✅ `dart test` (النطاق) — **170 · ٠ فاشلاً** · ✅ `dart analyze` (السحابة) — **بلا ملاحظة** · ✅ `dart test` (السحابة) — **207 · ٠ فاشلاً** · ✅ `npm test` — **بوابة التغطية `PASS ③`/`PASS ⑤` بصفر فجوة** ثم **222 اختبار محاكٍ · ٠ فاشلاً** · ✅ `flutter analyze` — **بلا ملاحظة** · ✅ `flutter test` — **136 · ٠ فاشلاً** (وبوابات التصميم الأربع ضمنها) · ★★★ **أربع طفرات قُتلت** (§3.4) |

### 3.2 وحدات قيد التنفيذ

⛔ **لا وحدة قيد التنفيذ** — لم تُترَك وحدة مفتوحة عند إغلاق هذا التشغيل.

### 3.3 وحدات محجوبة (ولماذا)

⛔ **لا وحدة محجوبة.**

⚠️ **و`IQ-020` المفتوح لا يحجب شيئاً:** `WU-002` أُغلقت **بنصف `FR-M4-09`
مُنفَّذاً والانحراف معلَناً** (`DEBT-26`) — ⟵ **والبند لإتمام النصف الثاني
لا لفكّ حجب** (§5).

### 3.4 ★★★ الطفرات — **ما يُثبت أن الحرّاس ليست شكلية**

| # | الطفرة | الأثر المرصود |
|:-:|---|---|
| ① | **إعادة فتح `sources` للكتابة** في القواعد (`perm('sourceWrite')`) | ⛔ **سقطت خمسة اختبارات** — إغلاق المصفوفة · حارس المصفوفة · وقت الجهاز · إنشاء `sources` · تعديلها. ثم أُعيد الإغلاق فنجحت الـ222 كلها |
| ② | **تعطيل شرط `amendReason`** في `planMasterData` | ⛔ **سقط اختباران** ⟵ **`DEBT-21` ① مُثبَت في هذا المسار** |
| ③ | **تعطيل `isTakenByOther`** (حارس التفرد) | ⛔ **سقط اختبار** — الاسم المكرر صار يمرّ |
| ④ | **`unitOfItem` تُرجِع `piece` دائماً** | ⛔ **سقط اختبار السكرب** ⟵ **`FR-M5-03` و`E-31` مُثبَتان** |

★★ **وضابطٌ مضاد لـ«النجاح لسبب خاطئ»:** لمّا أُغلقت **كل** مسارات الكتابة
صار «مرفوض» تفسيراً محتملاً لأي اختبار إغلاق. ⟵ ✅ **فأُضيف اختبارٌ صريح
يُثبت أن القراءة تعمل فعلاً** (`ADR-0013` القاعدة 4)، **وأن الفرق في
`sources` هو النطاق وحده**، ★ **وحُوِّل الضابط الموجب في اختبارات
`ADR-0016` إلى قراءةٍ تشترط مفتاحاً** (`dealerBalanceView`) ⛔ لا كتابةٍ
تنجح. **وهذا مسجَّل `DEBT-28`.**

---

## 4. مخالفات النزاهة المرصودة آلياً

✅ **لا مخالفات** — `track_implementation.py` يُرجِع
`integrity_violations: []` و`has_integrity_violations: false`.
**كل وحدة معلَّمة `[منجز ✅]` تقابلها فحوصات ناجحة فعلياً مسجَّلة بنتائجها.**

> ⚠️ **وسطرُ فحصٍ أُعيدت صياغته لسببٍ يُقال صراحةً:** رصد السكربت في المحاولة
> الأولى مخالفةً على `flutter analyze` لأن نصّ السطر كان يحوي كلمة «خطأً»
> (في عبارة «التحذير يُعامَل خطأً») ⟵ **فقرأه فحصاً فاشلاً**. ✅ **والعلاج
> إعادة صياغة النصّ** («الفحص الساكن صارم: كل ملاحظة حاجبة») ⛔ **لا تعطيل
> الحارس ولا تجاوزه** — ★ **والنتيجة الفعلية للفحص لم تتغيّر: نجح.**

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

| IQ | السؤال | الوحدة المتعطّلة | ملف/دالة | لماذا لم يكن قابلاً للاستنتاج |
|---|---|---|---|---|
| ⚠️ **`IQ-020`** | **`FR-M4-09` يشترط صلاحيةً مستقلة «تعطيل مقوت له رصيد» ⛔ ولا مفتاح لها في الكتالوج** | ⛔ **لا وحدة** — `WU-002` أُغلقت بالانحراف معلَناً | `master_data.dart` → `validateDealerDeactivation` · `functions/.../master_data.dart` → `_planDealer` | ★ **الكتالوج مصدر الحقيقة الوحيد للمفاتيح** (`IQ-001`)، **و`BR-M1-07` يرفض أي مفتاح بلا سطر فيه** ⟵ ⛔ **فاختراع مفتاح سادس يخالفه نصّاً**، **وإسقاط الشرط بصمت يترك متطلباً حرجاً منقوصاً**. ★ **والخياران يمسّان بوابة حَوْكمة**: تعديل الكتالوج أو `CR` على `FR-M4-09` |

راجع التفاصيل الكاملة والخيارات الثلاثة في:
`docs/00-governance/upds-workflow/implementation-questions.md`

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

### 6.1 ★★★ الأهم — **مسار كتابة البيانات المرجعية**

| البند | التفصيل |
|---|---|
| **ماذا يقول المستند** | `IQ-009` (الخيار أ) أغلق الكتابة المباشرة على **عشر مجموعات تمسّ المال أو المخزون** ⛔ **ولم تكن البيانات المرجعية منها**، ★ **و`WU-026` أبقاها مفتوحة عمداً** واختبرها ضابطاً على أن «الإغلاق مُستهدَف لا شامل» |
| **ماذا فرض التنفيذ فعلياً** | ⛔ **الكتابة المباشرة تُنتج تعديلاً بلا قيد تدقيق** — ثلاثة نصوص مجتمعةً لا تتحقق إلا بمعاملة سحابية واحدة: ① `FR-M2-03`/`FR-M3-13`/`FR-M4-13` («يُسجَّل بالقيمة قبل وبعد») ② `FR-M18-04` («كتابة السجل عبر السحابة حصراً») — و`audit_log` `allow create: if false` ③ `audit-log-design.md` §2 الشرط 3 + `ADR-0013` القاعدة 1 («المستند وقيده معاً أو لا شيء») |
| **كيف عُولج** | ★ **بتوسيع الإغلاق لا بنقض قرار:** `IQ-009` حدَّد **الحدّ الأدنى** الواجب إغلاقه، **وهذا يضيف مجموعةً سادسةً بمبرِّرها هي**. ⛔ **ولا ADR جديد** — لا نقض لـ`ADR-0013` بل تطبيقٌ أوسع له، ★ **والأثر أقوى لا أضعف**: حارسٌ في القواعد **وحارسٌ في الكود** |
| **أين وُثِّق** | `master-data-design.md` **§0 كاملاً** · `api-overview.md` §3.1-د و§4 واللافتة العليا · `security-requirements.md` §1 **والبند ③** · `permissions-catalog.md` §2.1 · `firestore.rules` (لافتة في موضعها) · `implementation-plan.md` (جدول القرارات الذاتية) |

⚠️⚠️ **وأثرٌ تشغيلي مُلزِم يُقال صراحةً:** ⛔ **لا تُنشَر `firestore.rules`
قبل نشر خدمة العمليات** الحاملة للعمليات التسع — ★ **بنفس منطق
`RB-backfill-user-isactive`**: القواعد تُغلِق المسار القديم فوراً، **وعكسُ
الترتيب يترك النظام بلا مسارٍ لكتابة بياناته المرجعية.**

### 6.2 ثغرات أصغر — سُدَّت بقرارات روتينية موثَّقة

| # | الثغرة | المعالجة |
|:-:|---|---|
| ① | ⛔ **`naming-conventions.md` §5 يفرض ترقيماً سحابياً لأكواد الكيانات، ولا مجموعة عدّادٍ لها في `data-dictionary.md`** | ★ **إعادة استعمال `document_counters` بفضاء أسماء `entity_`** ⛔ **لا مجموعة جديدة** — فلا قاعدة حماية تتغيّر. ✅ **ووُثِّق في القاموس §1** |
| ② | ⛔ **القاموس لا يذكر حدّاً لطول اسم المصدر** بينما يذكره لغيره | ★ **حدٌّ مشتقّ لا مخترَع** (2–80): الأدنى من الأنواع والأقصى من الأطراف. ✅ **والافتراض موثَّق في الكود نفسه** |
| ③ | ⛔ **سجل حراسة التفرد موصوف «سجلاً فارغاً»** — ⟵ **فلا سبيل للتمييز بين تكرارٍ حقيقي وإعادة إرسال** | ★ **أُضيف `entityId`** ⟵ **فإعادةُ المحاولة بعد انقطاع لا تُرفَض كاسمٍ مكرر**. ✅ **ووُثِّق في القاموس بجدول حقوله** |
| ④ | ⚠️ **`FR-M3-03`/`FR-M4-03` يطلبان جسر جهات اتصال، ولا حزمة معتمدة في المكدس** | ★ **قناة منصّة مكتوبة بأيدينا** — تطبيقٌ حرفي لـ`dependency-management-policy.md` §1 (البند 1: «يُغني عنها الإطار» · البند 2: «أقل من 100 سطر ⟵ اكتبه بنفسك»)، ★ **وأقلّ امتيازاً**: ⛔ **بلا إذن `READ_CONTACTS` إطلاقاً**. ✅ **ووُثِّق في `master-data-design.md` §8-ب** |

### 6.3 ⚠️ انحرافٌ معلَن — **`FR-M4-09` مُنفَّذ نصفاً**

راجع §5 و`DEBT-26`. ⛔ **ولم يُنفَّذ بتخمين ولم يُسقَط بصمت** — ★ **نصفُه
مُنفَّذ ومُختبَر، ونصفُه مُصعَّد.**

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

| # | البند | ما هو مطلوب |
|:-:|---|---|
| ① | ⚠️ **`IQ-020`** — الخيار **أ** يُعدِّل `permissions-catalog.md` (مفتاح سادس)، والخيار **ب** يحتاج `change-requests/CR-*.md` على `FR-M4-09` | **إجابة صاحب المشروع** ⛔ ولم يُعتمَد أيٌّ منهما ذاتياً |
| ② | ⚠️ **نشر `firestore.rules` بصيغتها الجديدة** — على **البيئة التجريبية** يقع ضمن الفئة 3 من بروتوكول التشغيل، **وعلى الإنتاج بوابةُ حَوْكمة** (UPDS-04 §2.2) | ⛔ **لم يُنشَر شيء في هذا التشغيل** — ★ **والترتيب المُلزِم في §6.1** |

⛔ **ولم يُنشأ ADR جديد ولم يُعدَّل ADR معتمد ولا `srs.md` ولا `uat-signoff.md`.**
⛔ **ولم تُنفَّذ أي عملية Git خارجية** (لا `push` ولا `PR` ولا `merge`).

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

| WU | لماذا حسّاسة | ما يجب أن يُراجَع بشرياً تحديداً |
|---|---|---|
| ⚠️★★ **`WU-002`** | **بيانات شخصية** (أسماء وهواتف الرعية والمقاوته) · **ونطاق المصادر الذي تُبنى عليه كل الصلاحيات اللاحقة** | ★★★ **أخطر ما فيها إغلاق `firestore.rules` على خمس مجموعات وسجلاتها** — ⟵ **راجع §6.1 وترتيبَ النشر المُلزِم** · ★ **وتسع دوالّ كاتبة صارت الحارس الوحيد** لقيود كانت في القواعد (`DEBT-21`) · ★ **وجسر جهات الاتصال يمسّ بيانات الجهاز** (`DEBT-27` — لم يُشغَّل على جهاز) |
| ⚠️ **`WU-000`** · **`WU-026`** · **`WU-001`** | (من تشغيلات سابقة) | ★ **تبقى مراجعتها قائمةً بنداً متابعاً** — راجع تقارير 2026-08-24 و2026-08-25 |

> ⛔★★ **والتحقق الآلي شرط ضروري لا كافٍ لهذه الوحدات** — 735 اختباراً
> ناجحاً **لا تُغني عن عينٍ بشرية** على طبقة التفويض، ★ **والدرس مُسجَّل
> في هذا المشروع نفسه**: `DEBT-24` مرّ من 581 اختباراً وكشفه أول استدعاء
> حقيقي.

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| `docs/04-design/module-design/master-data-design.md` | ★★ **اكتمال الوحدة** — الحالة `مخطَّط` ⟵ **`منفَّذ`** · **§0 مسار الكتابة** و§0.1 الترتيب و§0.2 الكود و§0.3 الأثر المعلَن · **§8-ب جسر جهات الاتصال** |
| `docs/06-database/data-dictionary.md` | **بنية تخزين جديدة** — عدّاد `entity_{kind}` · **حقول سجل الحراسة** · لافتة إغلاق الكتابة · `disableReason` على `sources` |
| `docs/05-api/api-overview.md` | **تسع عمليات جديدة** — **§3.1-د** · §4 (الكتابة الممنوعة) · اللافتة العليا |
| `docs/09-security/security-requirements.md` | **تغيّر موضع الإنفاذ** — §1 (أربعة صفوف + الإعداد التأسيسي) · **البند ③** تحت جدول التتبع |
| `docs/09-security/permissions-catalog.md` | §2.1 — **المفاتيح الخمسة انتقل إنفاذها للدالة** · **وانحراف `FR-M4-09` معلَناً** |
| `docs/02-requirements/requirements-traceability-matrix.md` | **عمود «الكود» لخمسة صفوف** (`FR-M2`/`M3`/`M4`/`M5`/`M21`) ⟵ **🟩 منفَّذ** · وتصحيح ملاحظة الحالة العليا |
| `docs/03-architecture/risks-and-technical-debt-register.md` | **`DEBT-26`/`27`/`28` جديدة** · **و`DEBT-21` البندان ① و② سُدِّدا في هذا المسار** وأُضيفت إليه سبعةُ قيودٍ جديدة |
| `docs/00-governance/upds-workflow/implementation-plan.md` | **`WU-002` ⟵ `[منجز ✅]`** بملفاتها وفحوصاتها ونتيجتها وحدودها المعلَنة وجدول قراراتها الذاتية |
| `docs/00-governance/upds-workflow/implementation-questions.md` | **`IQ-020` جديد** |

⛔ **ولم يُحدَّث `openapi.yaml` ولا `endpoints/`** — **غير منطبقين على هذا
المشروع** (`api-overview.md` §5: لا REST ولا نقاط نهاية).
⛔ **ولم يُحدَّث `erd.md`** — **لا كيان جديد**؛ الكيانات الخمسة موصوفة فيه
أصلاً، ★ **والتغيير في مسار الكتابة لا في نموذج البيانات.**
⛔ **ولم يُحدَّث `migrations-log.md`** — **لا هجرة**؛ قاعدة مستندية بلا مخطط.
⛔ **ولم يُحدَّث `troubleshooting-guide.md`** — **لا مشكلة متكررة جديدة رُصدت**.
⛔ **ولم يُحدَّث `developer-onboarding.md`** — **لا مستند رئيسي جديد**؛
المستندات المحدَّثة كلها مذكورة فيه أصلاً.

---

## 10. حالة `CLAUDE.md`

✅ **حدثت نقطة تحوّل فعلية** — **اكتمال وحدة عمل** (`claude-md-management.md`
§2 البند 5). ⟵ **وحُدِّث عبر `sync_claude_md.py` وحده** ⛔ **بلا تحرير يدوي.**

| الحقل | القيمة |
|---|---|
| `action` | `managed-block-updated` |
| `human_content_lines_preserved` | **13** ✅ |
| `new_total_lines` | **213** |
| `exceeds_recommended_size` | ⚠️ **نعم** (الحدّ الإرشادي 200) |

> ⚠️ **والتجاوز قائم قبل هذا التشغيل** (كان **218 سطراً**) — ✅ **وقُلِّص
> إلى 213 رغم إضافة محتوى `WU-002`** بضغط قسمين تاريخيين. ★ **والسداد
> الكامل يحتاج نقل قواعد متخصصة إلى `.claude/rules/`** — ⛔ **ولم يُنشأ
> المجلد استباقياً** (`claude-md-management.md` §4).

---

## 11. الخطوة التالية الموصى بها

### 11.1 ⚠️ **المرحلة الأولى لم تُستهلَك** — وهذا يُقال صراحةً

بروتوكول التشغيل §أ يطلب **حلقةً عبر وحدات المرحلة الحالية في التشغيل
الواحد**. ⛔ **ولم تُستكمَل:** بقيت **`WU-003`…`WU-011`** بحالة `[مخطط]`
وهي **قابلة للبدء** (تبعياتها منجَزة).

★ **وسبب التوقف يُذكَر بلا تجميل:** `WU-002` أنتجت **قراراً بنيوياً يمسّ
كل زيادة قادمة** (إغلاق كل مسارات الكتابة المباشرة) **وبنداً `IQ` يمسّ
بوابة حَوْكمة**. ⟵ ★ **وبناءُ تسع زيادات فوقه قبل اطّلاع صاحب المشروع
يُراكم أثرَه بلا مراجعة** — وهو نقيض ما تخدمه آلية `IQ` أصلاً.

### 11.2 ما يُطلَب من صاحب المشروع الآن

| # | المطلوب | أين |
|:-:|---|---|
| ① | ★ **الإجابة على `IQ-020`** (الخيار أ أو ب) | `implementation-questions.md` |
| ② | ★ **مراجعة قرار §6.1** — توسيع الإغلاق إلى البيانات المرجعية | `master-data-design.md` §0 |
| ③ | ★★ **إذنٌ صريح للنشر** إن أُريد نشرُ القواعد والخدمة على التجريبية — **بالترتيب المُلزِم** | §6.1 |

### 11.3 عند الاستدعاء التالي لـ`/upds-docgen:implement`

★ **يُستأنف من `WU-003`** (الوارد عدداً والمخزون اليومي) — **أول زيادة تلمس
المال والكميات والتاريخين فعلياً**، ★ **وتُرحَّل إجابة `IQ-020` أولاً إن
كُتبت** ثم يُغلَق `DEBT-26`.

⚠️ **وتذكير:** الوحدات الحسّاسة الأربع (§8) **تحتاج مراجعة بشرية قبل أي
نشر إنتاجي** — ⛔ **والتحقق الآلي لا يُغني عنها.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
