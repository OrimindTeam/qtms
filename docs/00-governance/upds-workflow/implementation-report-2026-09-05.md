# تقرير تنفيذ برمجي — UPDS DocGen — 2026-09-05

**رقم تشغيل التنفيذ:** 67
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة** — شريحة رأسية كاملة)
**تقدّم التنفيذ:** **82.8٪** (24 منجَزة من 29)
**حالة التوثيق وقت هذا التشغيل:** **100٪** (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** **0**

> ⚠️ **وتاريخان في هذا التقرير عمداً:** ★ **العملُ كلُّه وقع يوم 2026-09-04
> بتوقيت المنصّة** (**والمستندان الحيّان `DSP-20260904-0001` و
> `DSP-20260830-0001` مكتوبان بختم الخادم**)، ⟵ **وانقلب اليومُ إلى
> 2026-09-05 أثناء إغلاق التشغيل**: ★ **فاسمُ الملف باليوم الجاري**
> ⛔ **والأدلّةُ بتواريخها التي وقعت فيها** (بروتوكول التشغيل §و).

---

## 1. ملخص تنفيذي

★★★ **بُنيت `WU-020` — زيادةُ الإتلاف — من طرف إلى طرف وأُغلقت بدليلٍ حيّ.**
شريحةٌ رأسيةٌ كاملة عبر أربع طبقات: **طبقةُ نطاقٍ بلا مبلغٍ واحد** (`disposal.dart`)
· **ثلاثُ عملياتٍ سحابية** بثلاثة مفاتيح مستقلة · **شاشةٌ بتحذيرٍ صريح وحقلِ
كميةٍ واحد** ⛔ **بلا حقل سعر** · **وتقريرُ `R-07`** الذي كان مؤجَّلاً على هذه
الزيادة بالاسم. ★★ **وثالثُ إجراءات تصريف المتبقي المتأخر صار قائماً**
(`FR-M8-11`) — ⟵ **يفتح شاشةَ الإتلاف مثبَّتةً على يوم المخزون القديم بنفس
آلية التوزيع والبيع** ⛔ **لا بمسارِ كتابةٍ ثانٍ.**

⛅★★★ **والقياسُ حيٌّ لا موصوف:** **مستندان أُنشئا من الواجهة على المحاكي**
(`DSP-20260904-0001` · `DSP-20260830-0001`) **وقُرئا من التجريبية حقلاً بحقل**:
⛔ **صفرُ حقلٍ ماليٍّ** · ⛔ **صفرُ قيدٍ في دفترَي المقاوته والرعوي** ·
★ **ووسمُ `disposal` على حركتيهما** · ★ **والمتبقي المتأخر 87 ⟶ 84 في
المعاملة نفسِها.** ★★ **والإجمالي 2641 اختباراً · ٠ فاشلاً.**

★ **والمشروعُ الآن على أربعٍ باقيات في المرحلة الثالثة:** `WU-021` ·
`WU-022` · `WU-023` · `WU-024` · `WU-025`.

---

## 2. بوابة الدخول — نتيجة الفحص

| # | الشرط (playbook §1.1) | متحقق؟ | المسار المفحوص |
|:-:|---|:-:|---|
| 1 | المنهجية مُصرَّح بها | ✅ | `docs/21-project-management/development-methodology.md` |
| 2 | العمارة والمكدّس | ✅ | `docs/03-architecture/architecture-overview.md` · `technology-stack.md` |
| 3 | معايير الكود والتسمية ومعالجة الأخطاء | ✅ | `docs/04-design/coding-standards.md` · `naming-conventions.md` · `error-handling-strategy.md` |
| 4 | تعريف «منجَز» والتفرّع والالتزام والإصدار | ✅ | `docs/07-development/*` · `docs/12-deployment-and-release/versioning-scheme.md` |
| 5 | استراتيجية الاختبار | ✅ | `docs/08-testing-and-qa/test-strategy.md` |
| 6 | سياسة الأسرار | ✅ | `docs/09-security/secrets-management-policy.md` |
| 7 | `srs.md` بمتطلبات مرقّمة | ✅ | `docs/02-requirements/srs.md` |
| 8 | ⛔ **لا `ADR` بحالة «مقترح» يحكم ما يُنفَّذ** | ✅ | **الأربعةُ والعشرون كلُّها «مقبول/معتمد»** — مقروءةً من `**الحالة:**` في كل ملف |

| # | بوابة الوحدة (playbook §1.2) — `WU-020` | متحقق؟ | المسار |
|:-:|---|:-:|---|
| 1 | تصميم الوحدة | ✅ | `docs/04-design/module-design/inventory-design.md` §5 |
| 2 | المتطلب بمعيار قبولٍ واضح | ✅ | `FR-M8-16` · `FR-M8-11` · `E-25` · `AT-54` |
| 3 | عقود الواجهة | ✅ | `docs/05-api/api-overview.md` · `authentication-and-authorization.md` |
| 4 | عقود التخزين | ✅ | `docs/06-database/erd.md` · `data-dictionary.md` §`disposals` |
| 5 | سياسة المصادقة والصلاحيات | ✅ | `docs/09-security/authentication-policy.md` · `permissions-catalog.md` §2.4 |
| — | ★★ **الهوية البصرية** (بروتوكول §ح — **الوحدة تعرض شاشة**) | ✅ | `design-system.md` · `design-tokens.md` · `ui-guidelines.md` — **قُرئت الثلاثةُ كاملةً قبل الكتابة** |

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

| WU | العنوان | الملفات المنتَجة | الفحوصات ونتيجتها الفعلية |
|---|---|---|---|
| ✅ **`WU-020`** | **زيادة: الإتلاف من طرف إلى طرف** | **13 ملفاً جديداً** (نطاق 2 · سحابة 2 · تطبيق 4 · اختبارات 4 · دعم 1) · **26 معدَّلاً** · **أداتا تجريبية** — **القائمة الكاملة في `implementation-plan.md`** | **845** نطاق · **753** سحابة · **781** تطبيق · **262** حماية = ✅ **2641 · ٠ فاشلاً** · **38 بوابةَ تصميم** · ⛅ **بناءٌ ونشرٌ وفهرس** · ⛅ **أربعةَ عشرَ سيناريو محاكٍ** |

#### ⛅★★★ ما قِيس حيّاً على التجريبية

| # | ما نُفِّذ | النتيجة المقروءة |
|:-:|---|---|
| ① | `gcloud builds submit` | ✅ **`callables:wu020` · `d2b7d9bb-ce2a-44cd-a781-ffcacaa36600` · 1M9S · SUCCESS** |
| ② | `gcloud run deploy` | ✅ **المراجعة `qtms-callables-00032-5jq` · 100٪ من المرور** · ✅ **`/createDisposal` و`/amendDisposal` و`/cancelDisposal` ⟵ `401` بلا رمز** ⛔ **مقابل `404` لمسارٍ مجهول** — ★ **فالمساراتُ حيّةٌ مقيسة** ⛔ **لا مفترَضة** |
| ③ | `firebase deploy --only firestore:indexes` | ✅ **نجح** · **والفهرس `disposals(sourceId ↑ · stockDate ↓)` قُرئ بـ`gcloud firestore indexes composite list`** (**`CREATING` لحظةَ القياس** — ★ **والاستعلامُ يعمل على المدى الصغير**) |
| ④ | **مزامنةُ حساب QA** (`AM-004` ⑤) | ✅ **`permissions_true = 87` · `isActive: True` · `sourceScope: all`** — ⛔ **ولا مفتاحَ جديدٌ في هذه الزيادة** |
| ⑤ | ⛔ **مساراتُ الرفض** (`exercise_disposal.py`) | ✅ **خمسةٌ كما هو متوقَّع**: **مستقبليٌّ ⟵ `ERR_CALL_400`** · **مشوَّه ⟵ `ERR_CALL_400`** · **كميةٌ تتجاوز الرصيد ⟵ `ERR_STOCK_001`** · **مصدرٌ لا وجودَ له ⟵ `ERR_CALL_500`** · **بلا سطر ⟵ `ERR_CALL_400`** |
| ⑥ | ⛅★★★ **الكتابةُ الحيّة** (`verify_disposal_write.py`) | ✅ `DSP-20260904-0001`: **7 حبات · `balanceAfter: 53` · `incoming 60 / outgoing 7 / balance 53`** · ✅ `DSP-20260830-0001`: **3 حبات · `stockDate 2026-08-30` · `87 ⟶ 84`** — **ولكلٍّ:** ⛔ **صفرُ حقلٍ ماليّ** · ★ **`movementTag: disposal`** · ⛔ **`dealer_ledger` = 0 · `supplier_ledger` = 0** · ★ **قيدٌ فعلُه `disposal` و`reason: null`** |
| ⑦ | ★★★ **اختبار المحاكي `Pixel_6_API_36`** | ✅ **أربعةَ عشرَ سيناريو — الكلُّ نجح** (**الجدول الكامل في `implementation-plan.md` تحت `WU-020`**) |

### 3.2 وحدات قيد التنفيذ

⛔ **لا وحدة قيد التنفيذ** — ★ **و`WU-020` أُغلقت في هذا التشغيل نفسِه.**

⚠️★★ **ولم تبدأ `WU-021`** — ★ **والسببُ مُعلَن:** **الزيادةُ استغرقت التشغيل
كاملاً** (**أربعُ طبقاتٍ وشاشةٌ وتقريرٌ ونشرٌ واختبارُ محاكٍ من طرفٍ إلى طرف**)،
★ **وحلقةُ §أ تُستأنف من `WU-021` عند الاستدعاء التالي** — ⚠️ **وقد استُدعي هذا
التشغيل بوسيطةٍ محدَّدة (`WU-020`)**: ⟵ **فالقسمُ (أ) لا ينطبق على تسلسل
المرحلة أصلاً** (البروتوكول §أ · الفقرة الأخيرة).

### 3.3 وحدات محجوبة (ولماذا)

⛔ **لا وحدة محجوبة** — ★ **ولا بند `IQ` جديد فُتح في هذا التشغيل.**

---

## 4. مخالفات النزاهة المرصودة آلياً

✅ **لا مخالفات** — `track_implementation.py` ⟵ `integrity_violations: []`
(**مقيسٌ بعد الإغلاق لا قبله**).

⚠️★★ **ورُصدت مخالفتان أثناء الإغلاق فعُولجتا في المصدر** ⛔ **لا بتخفيف
الحارس** (playbook §7 البند 3):

| # | ما رصده الحارس | العلاج |
|:-:|---|---|
| ① | **سطرُ `flutter analyze` قُرئ «فحصاً فاشلاً»** — ★ **لأن وصفَه حوى كلمة «خطأً»** (`_FAIL_WORDS`) | ✅ **أُعيدت صياغةُ الوصف إلى «بالإعدادات الصارمة»** — ⛔ **والنتيجةُ نفسُها لم تتغيّر** |
| ② | **سطرا القياس الحيّ «لم تُصرَّح نتيجتهما»** — ★ **بلا كلمةِ حكمٍ صريحة** | ✅ **أُضيفت «نجحت» صراحةً** — ★ **فالحكمُ مقروءٌ آلياً لا مستنتَجاً** |

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

⛔ **لا بند `IQ` جديد** — ★ **والواحدُ والأربعون كلُّها `[تم الحل ✅]`.**

⚠️★★ **وأقربُ ما اقترب من `IQ` في هذا التشغيل — ويُقال صراحةً:** ★ **بادئةُ
رقم مستند الإتلاف**: ⟵ **`naming-conventions.md` §5 لا يذكر مستندَ الإتلاف
إطلاقاً** ⛔ **مع أن مجموعته وصلاحياته وقاعدةَ حمايته قائمةٌ منذ `WU-000`
و`WU-026`.** ★ **ولم يُصعَّد بنداً لأن الاشتقاق حتميٌّ لا اختيارٌ بين متكافئين**
(**اختبارُ playbook §5**): **معجمُ §2 يقول الإتلاف ⟵ `disposal`** · **وهيكلُ
حروفه `DSP`** · **وغيرُ مستعمَلٍ في البادئات التسع** · **وأربعُ خاناتٍ كبقية
مستندات الحركة** — ★ **وهو نفسُ اشتقاق `WU-000` حين أضاف المجموعة نفسَها.**
✅ **وسُجِّل [`DEBT-97`] بدلَ ذلك** — ★ **لأن الأثرَ الباقي تحريرُ مستندٍ
`STATIC` وهو بيد المالك** ⛔ **لا قرارُ كودٍ معلَّق.**

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

| # | ما يقوله المستند | ما فرضه التنفيذ | كيف عولج |
|:-:|---|---|---|
| ① | **`naming-conventions.md` §5 يعدّ تسع بادئات «منقولةً حرفياً من المصدر»** — ⛔ **وليس فيها الإتلاف** | ★ **رقمُ المستند إلزاميٌّ بنيوياً**: ⟵ **`inventory_ledger` يشترط `sourceDocNumber`**، **و`stockMovementId` يُبنى منه**، **و`entityId` في القيد كذلك** | ✅ **اشتقاقٌ مُعلَنٌ لا صامت** (`DSP`) — ★ **مكتوبٌ بمبرِّره في `data-dictionary.md` وفي `DocumentKind.disposal` وفي اختبارِها** · ⛔ **ولم يُحرَّر المستندُ `STATIC` ذاتياً** (`UPDS-05` §5) ⟵ **بل سُجِّل [`DEBT-97`]** |
| ② | **`audit_log_screen.dart` يقول: «لا فعلٌ بلا كاتبٍ له اليوم» ويُسمّي الإتلافَ مؤجَّلاً** | ★ **صار للإتلاف كاتبٌ في هذه الزيادة** | ✅ **أُضيف `AuditAction.disposal` إلى الفلاتر مع كاتبه** — ★ **وهو ما تُوجِبه قاعدةُ الملف نفسِها** |
| ③ | ⚠️ **وقاعدةٌ ثالثةٌ رُصدت ولم تُعالَج — خارج النطاق** | ★ **`agedRemainderClear` فعلٌ مكتوبٌ منذ `WU-019` وبلا فلترٍ في الشاشة** | ⛔ **لم يُضَف التزاماً بحدّ الزيادة** ⟵ ✅ **وسُجِّل [`DEBT-98`] بسدادٍ من سطر** |
| ④ | ⛔ **ورُصد على المحاكي لا في مراجعة:** **حقلُ `lineCount` يُعرَض بالإنجليزية في بطاقة قيد التدقيق** | `ui-guidelines.md` §6: ⛔ **لا كلمة إنجليزية في أي شاشة** | ✅ **أُضيف اسمُه العربي** («عدد السطور») · ★★ **واختبارُ ارتدادٍ يحرس الحقولَ الأربعة الجديدة** ⛔ **فلا يتكرر** |

⛔ **ولا انحرافٌ صامتٌ واحد** — ★ **ولا `ADR` جديدٌ لزم**: ⟵ **الزيادةُ كلُّها
تنفيذٌ لقراراتٍ قائمة** (`ADR-0013` · `ADR-0020` · `ADR-0007` · `ADR-0008`).

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

| # | البند | الحال |
|:-:|---|---|
| ① | ⛔⛔★★★ **النشرُ الإنتاجي** | ⛔ **لم يُطلَب ولم يُعرَض في هذا التشغيل** — ★ **ولا شيءَ نُشر على `qtms-orimind-c001`**: ⟵ **والتشغيلُ كلُّه على التجريبية** (البروتوكول §ز.1) |
| ② | ★ **تعديلُ `naming-conventions.md` §5** (**مستندٌ `STATIC`**) | ⚠️ **ينتظر تأكيداً واعياً من المالك** — ★ **سطرٌ واحد**: `\| إتلاف \| DSP-YYYYMMDD-#### \|` · ⛔ **ولم يُحرَّر ذاتياً** ([`DEBT-97`]) |
| ③ | ⛔ **`ADR` جديد أو `RFC` أو تعديلُ `srs.md`** | ⛔ **لا شيء** — ★ **ولا لزم.** |

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

★★★ **`WU-020` معلَّمةٌ `يلمس نطاقاً حسّاساً: نعم`** — ⛔ **والتحقق الآلي شرطٌ
ضروريٌّ لا كافٍ لها:**

| # | لماذا حسّاسة | ما يُراجَع بشرياً |
|:-:|---|---|
| ① | ★★ **تخفض مخزوناً بلا مقابلٍ مالي** — ⟵ **وهي العمليةُ الوحيدة في النظام التي تُخرِج قاتاً بلا استحقاقٍ للرعوي** (`A-15`) | **أن يكون منحُ `disposalCreate` مقصوداً لمن يُمنَح** — ★ **فمن يملكه يستطيع تصفيرَ مخزونٍ بلا أثرٍ مالي يكشفه تقريرُ مبيعات** |
| ② | ★★ **تكتب على تاريخ مخزونٍ سابق** (`agedRemainderClear`) | **أن يبقى المفتاحان مشروطين معاً** — ⛔ **ولا يُغني أحدُهما عن الآخر** (`FR-M8` §5) |
| ③ | ⚠️ **ولم يُقَس «بلا المفتاح» حيّاً** — ★ **حسابُ QA يملك المفاتيح كلَّها** | **مراجعةُ الأدوار قبل النشر** — ★ **والتغطيةُ آليةٌ كاملة** (**ثلاثةُ اختبارات رفضٍ + اختبارُ شاشة**) |

★ **وسابقاتُها الحسّاسةُ قائمةٌ كما هي** — ★ **ومنها `WU-019` و`WU-014` و`WU-004`**
(⛔ **ولم يُنشَر أيٌّ منها إنتاجياً بعد**).

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| `docs/05-api/api-overview.md` | ★★ **قسمٌ جديد §3.1-م** — **ثلاثُ عملياتٍ مستدعاة** بمدخلاتها ومخرجاتها وشروطها وأخطائها (UPDS-03 §4 البند 1) |
| `docs/06-database/data-dictionary.md` | ★★ **حقولُ `disposals` استُكملت**: `documentNumber` (**بمبرِّر البادئة**) · `totalPieces`/`totalWeight` · `lineCount` — ★ **وأُضيف دليلُ «لا حقلَ ماليّ» المقيس** |
| `docs/06-database/indexing-strategy.md` | ★ **فهرسٌ جديد** `disposals(sourceId ↑ · stockDate ↓)` (UPDS-03 §4 البند 3) |
| `docs/04-design/module-design/inventory-design.md` | ★★ **حالةُ الوحدة** + **§9-ز الجديد** (**ما نُفِّذ فعلاً ومواضعه**) + **صفُّ الإتلاف في §5 صار مُنفَّذاً** |
| `docs/04-design/module-design/reporting-design.md` | ★ **26 تقريراً من 31** — **و`R-07` بُني** |
| `docs/02-requirements/requirements-traceability-matrix.md` | ★★ **عمودُ الكود لصفَّي `FR-M8-09…18` و`FR-M19`** (UPDS-03 §4 البند 7) |
| `docs/09-security/permissions-catalog.md` | ★ **`disposalCreate` صار له مسارٌ حقيقيٌّ يُفحَص فيه** ⛔ **بعد أن كان مفتاحاً بلا كاتب** |
| `docs/03-architecture/risks-and-technical-debt-register.md` | ★★ **[`DEBT-97`] و[`DEBT-98`]** (UPDS-03 §4 البند 8) |
| `docs/00-governance/upds-workflow/implementation-plan.md` | ★ **`WU-020` ⟵ `[منجز ✅]`** بفحوصها ودليلها الحيّ |

⛔ **ولم تُحدَّث `openapi.yaml`** — ★ **ولا وجودَ لها في هذا المشروع أصلاً**:
⟵ **العقدُ عملياتٌ مستدعاة موصوفةٌ في `api-overview.md` §3.1** (`ADR-0013`).
⛔ **ولم يُحدَّث `erd.md`** — ★ **ولا مجموعةَ جديدة**: `disposals` **معرَّفةٌ
منذ `WU-000`** ⟵ **والمُستكمَلُ حقولُها في قاموس البيانات.**
⛔ **ولم يُحدَّث `migrations-log.md`** — ★ **ولا هجرةَ بيانات**: **الفهرسُ
إضافةٌ لا تحويل.**

---

## 10. حالة `CLAUDE.md`

⛔ **لم يُحدَّث — ولا نقطةَ تحوّلٍ تستدعيه في هذا التشغيل** (`claude-md-management.md`
§2): ★ **المرحلةُ «تنفيذ» كما هي** · **والأوامرُ لم تتغيّر** · **ولا `ADR`
انتقل من مقترحٍ إلى مقبول** — ⟵ **واكتمالُ زيادةٍ واحدة تُحدِّث «الوحدة
الجارية» وحدَها**، ★ **وقسمُ «حالة التنفيذ» في الكتلة المُدارة يُحيل إلى
`implementation-plan.md` أصلاً** ⛔ **ولا يحمل رقمَ تقدّمٍ يُحدَّث**
(`AM-013`: **≤ 3 أسطر**).

---

## 11. الخطوة التالية الموصى بها

| # | الخطوة | الحال |
|:-:|---|---|
| ① | ★★ **استئنافُ المرحلة الثالثة من `WU-021`** (**مُعيد البناء بأثر رجعي**) — ⟵ **وهي التي تسدّ [`DEBT-01`] و[`DEBT-96`] معاً** | ⏳ **جاهزة — ولا حاجب** |
| ② | ⚠️ **قرارُ المالك في [`DEBT-97`]** — **سطرٌ في `naming-conventions.md` §5** | ⏳ **ينتظر تأكيداً واعياً** (**مستندٌ `STATIC`**) |
| ③ | ⚠️ **مراجعةٌ بشريةٌ لـ`WU-020` قبل أي نشرٍ إنتاجي** — §8 أعلاه | ⏳ **مطلوبة** |
| ④ | ⛔ **ولا بند `IQ` ينتظر إجابة** | ✅ **صفر** |

### ⚠️★★ فحصُ «الحشر بدل الاختصار» — [v1.5.1]

★ **شُغِّل الفحص فعلاً** (`plan.has_field_char_warnings = true`) — **والنتيجة
المقيسة:**

| البند | العدد |
|---|:-:|
| **وحداتٌ فيها حقلٌ يتجاوز 400 حرف** | **21** |
| ✅ **وحداتٌ اختُصرت في هذا التشغيل** | **0** |
| **أحرفٌ حُذفت** | **0** |

⛔ **ولم تُختصَر واحدة — والسببُ مقيسٌ لا تفضيل:**

| # | الفئة | الوحدات | لماذا لا تُختصَر |
|:-:|---|---|---|
| ① | ★ **متأثرةٌ بانحرافٍ مفتوح** (`track_amendments.py` ⟵ `drift.affected_units`) | `WU-000` · `WU-026` · `WU-001` · `WU-002` · `WU-003` · `WU-005` · `WU-006` · `WU-007` · `WU-008` · `WU-010` · `WU-011` · `WU-012` · `WU-013` · `WU-014` | ⛔ **ليست «مستقرة» بتعريف playbook §10** — ★ **وهذا التشغيلُ نفسُه سبَّب جزءاً من الانحراف**: ⟵ **تحديثُ `data-dictionary.md` و`indexing-strategy.md` و`permissions-catalog.md` و`inventory-design.md` و`reporting-design.md`** |
| ② | ★ **حديثةُ الإنجاز** (آخر 2–3 تشغيلات) | `WU-015` · `WU-016` · `WU-017` · `WU-018` · `WU-019` · `WU-020` · `WU-027` · `WU-028` | ★ **playbook §10 القاعدة 4 يُبقيها بتفصيلها الكامل** |
| ③ | ⏳ **غيرُ منجَزة أصلاً** | `WU-025` | ★ **الاختصارُ لوحدات `[منجز ✅]` وحدها** |
| ④ | ⚠️ **مرشَّحتان مؤجَّلتان بقرارٍ معلَن** | `WU-004` · `WU-009` | ★ **مستقرّتان فعلاً** ⛔ **لكن تقريرَي يومِهما مؤرشفان** في `docs/99-archive/2026/implementation-reports/` — ⟵ **وسطرُ الإحالة في §10 يفترض المجلدَ النشط**: ⛔ **فلا يُحذَف سردٌ قبل التحقق من مقصد الإحالة فعلياً** (بروتوكول §و) — ★ **ويُترَك لتشغيلٍ مخصَّص** |

⚠️★★ **وانحرافُ المستندات مرصودٌ ومُعلَن:** `has_drift = true` · **14 وحدةً
متأثرة** · **9 مستنداتٍ حاكمةٍ تغيّرت** — ⛔ **ومعالجتُه ليست من نطاق
`implement`**: ★ **هي عملُ `/upds-docgen:amend`** (`amendment-playbook.md` §3)،
⟵ **وأكثرُه أثرُ هذا التشغيل نفسِه على مستنداتٍ مشتركة** ⛔ **لا تغيُّرَ عقدٍ
في تلك الوحدات.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
