# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-29

**رقم تشغيل التنفيذ:** 42
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة**)
**تقدّم التنفيذ:** 33.3٪ (9 منجَزة من 27)
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند مفتوح)
**أسئلة تنفيذ مفتوحة:** 0 (30 من 30)

---

## 1. ملخص تنفيذي

✅★★★ **`DEBT-62` سُدِّد ومُتحقَّقٌ حيّاً — والعلاجُ سطرٌ واحد:**
`isAutoDispose: true` **على** `dailyPricesProvider`. ★ **وهو الخيارُ الأول الذي
وجّه إليه المالك** (`autoDispose` **قبل القراءة لمرة واحدة**) ⟵ **ونجح، فلم
يُحتَج إلى الثاني.**

⛔⛔★★★ **ولم تُعلَّم `WU-005` منجَزة** — ★ **و`DEBT-63` سببُ ذلك:** **زرُّ «نسخ
أسعار أمس» لا يظهر إطلاقاً** ⟵ **والميزةُ `FR-M9-13` معطَّلةٌ عملياً**، ★ **وهو
عطلٌ مستقلٌّ سابقٌ لـ`DEBT-62`** ⛔ **لا أثرٌ من آثاره** — ⟹ **فبوابةُ §د.2 لا
تكتمل على ميزةٍ لا يمكن اختبارُها أصلاً.**

★★ **وسطحُ التغيير ملفّان لا ثالثَ لهما:** **مزوّدٌ واحد + حارسُ انحدارٍ واحد** —
⛔ **و`daily_pricing_screen.dart` لم يُمَسّ بحرف.**

---

## 2. ★★ التحقّق من دورة حياة المزوّد قبل التغيير — ⛔ لا افتراض

★ **طلب المالك صراحةً التحقّق قبل التعديل** — ★ **وهذا ما قِيس:**

| # | ما فُحص | النتيجة المقيسة |
|:-:|---|---|
| 1 | **هل Riverpod 3 يتخلّص تلقائياً افتراضاً؟** | ⛔ **لا** — ★ **مقيسٌ من الحزمة نفسها:** `isAutoDispose = false` **افتراضاً** ([`stream_provider.dart:96,213`](../../../../)) · ★ **و`CHANGELOG` 3.0.0: بديلُ `Provider.autoDispose()` صار `Provider(isAutoDispose: true)`** ⟹ **فالخيار متاحٌ وغيرُ مُفعَّل** |
| 2 | **من يستهلك `dailyPricesProvider`؟** | ★ **ثلاثة:** `pricingRowsProvider` (**اليوم**) · `_CopyYesterdayButton` (**أمس**) · `suggestedDistributionPricesProvider` (**اليوم**) |
| 3 | **أيُّ شاشةٍ تشترك مرتين؟** | ⛔ **التسعيرُ وحدَها** — ⟵ **وهي وحدَها المصابة** |
| 4 | **هل يُسقِط `isAutoDispose` بثّاً يعتمد عليه أحد؟** | ⛔ **لا** — ★ **مفتاحُ اليوم يبقى حيّاً لأن مراقبَيه دائمان** (`Provider.family` **بلا `isAutoDispose`**) ⟹ ★★ **فالأثرُ العملي محصورٌ في مفتاح أمس** — **وهو المطلوب بالضبط** |
| 5 | **هل يُنتج بياناتٍ بائتة؟** | ⛔ **لا** — ★ **ما دام له مستمعٌ فالبثُّ حيّ**، ★ **وعند العودة يُعاد الاشتراك نظيفاً** ⟹ **أحدثُ لا أقدم** |

⟹ ✅ **فالخيارُ الأول (`autoDispose`) مناسبٌ لمعمار المزوّد** — ⛔ **ولم يُحتَج
إلى القراءة لمرة واحدة** (الخيار الثاني).

---

## 3. ✅★★★ نتيجة التحقّق على الجهاز — بناءٌ نظيف وبنفس تسلسل الأساس

⛅ **`Pixel_6_API_36`** · **بناءٌ نظيف** (`flutter build apk --debug --flavor staging`)
· **وتسلسلُ الأساس نفسُه:** **الصدَفة ⟵ التسعير ⟵ رجوع ⟵ التسعير.**

| # | معيار القبول (بنصّ المالك) | النتيجة |
|:-:|---|:-:|
| 1 | **الشاشة تُرسَم صحيحةً في الدخول الثاني** | ✅ **نعم** — ★ **والثالثُ كذلك** (**الرأس · الشريط · بطاقةُ النوع بسعر 2000 · حقلُ السبب · زرُّ الحفظ**) |
| 2 | **أسعارُ أمس صحيحةٌ ومتاحةٌ لفعل النسخ** | ✅ **مقيس:** `loading=true ⟶ n=3` **بالمفاتيح الثلاثة نفسِها** بعد التخلّص وإعادة الاشتراك ⛔ **بلا بياناتٍ بائتة** |
| 3 | **لا انحدارَ في `مخزون اليوم`** | ✅ **يُعاد الدخول إليه فيعمل** (**95 حبة · وارد 100 · صادر 5**) |
| 4 | **ولا في بقية مستهلكي المزوّد** | ✅★★ **شاشةُ التوزيع تحسب «قيمة الضمار: 10000 ريال» لخمسِ حبات** ⟵ **5 × 2000 من المزوّد حيّاً** ⛔ **لا رقمٌ مخزَّن** — ★ **وهو أقوى إثباتٍ لأنه مسارٌ مالي** |
| 5 | **البوابة الآلية خضراء** | ✅ **1510 · ٠ فاشلاً** — **401 تطبيق** (+1) · **412 نطاق** · **461 سحابة** · **236 حماية** · ✅ **`analyze` نظيف في الثلاث** |
| 6 | **لا نشرَ إنتاجي ولا تغييرَ IAM** | ✅ **لم يقع أيٌّ منهما** — ⛔ **ولا `run deploy` ولا `builds submit` ولا `*-iam-policy-binding`** |
| 7 | **سجلُّ الجهاز** | ✅ **نظيف** — ⛔ **لا `E/flutter` ولا رفضٌ ولا طلبُ فهرس** |

---

## 4. ✅ حارسُ الانحدار — ومَحدوديّتُه تُقال

✅ **أُضيف اختبارٌ يفشل بإسقاط الإصلاح** — ★ **مُثبَتٌ بطفرة:** **بحذف
`isAutoDispose: true` وحده يسقط** (`+22 -1`) **وبإعادته يمرّ** (`+23`)
([`daily_pricing_screen_test.dart`](../../../../test/capabilities/inventory/presentation/daily_pricing_screen_test.dart)).

⚠️⚠️★★★ **وهو حارسُ الشرط لا حارسُ العَرَض — ولا يُدَّعى خلافُه:**
⛔ **تعذُّرُ الرسم لا يتكرّر في اختبار ودجاتٍ إطلاقاً** — ★ **جُرِّب مرتين في
التشغيل السابق** (**تبديلٌ مباشر للشجرة · وملاحةُ `go_router` متداخلةٌ مطابقة
للمسار الحقيقي**) ⟵ **والشجرةُ حاضرةٌ في الحالتين**، ★ **لأن اختبار الودجات لا
يُشغّل المُركِّبَ الحقيقي.** ⟹ ★★ **فالحارسُ يمنع إسقاطَ الشرط الذي أزال العطل**
⛔ **ولا يُثبت أن الرسم يعمل** — ★ **وذاك برهانُ §د.2 على المحاكي وحدها.**

---

## 5. ⛔⛔ ما لم يُفسَّر — ويُقال صراحةً

★ **المعروفُ بالقياس:** **المُطلِقُ اشتراكٌ ثانٍ حيٌّ على العائلة نفسِها، وإسقاطُه
يُزيل العطل قطعاً** (**مُثبَتٌ في الاتجاهين: بإزالته يعمل، وبإعادته يُظلم**).
⛔ **والمجهولُ:** **لماذا يوقف اشتراكٌ ثانٍ طَورَ الرسم أصلاً.**
⚠️ **فإن ظهر العَرَضُ نفسُه في شاشةٍ أخرى تشترك مرتين، فهذا بابُه.**

⛔ **وفرضيةٌ سابقة جُرِّبت وفشلت وسُجِّلت** (التشغيل 41): **رفعُ القراءة إلى
`_PricingFormState`** ⟵ **والعطلُ باقٍ** ⟹ ★ **فليس موضعَ القراءة بل الاشتراكَ نفسَه.**

---

## 6. ⚠️ عطلٌ جديد مسجَّل — `DEBT-63`

⛔⛔ **زرُّ «نسخ أسعار أمس» لا يظهر إطلاقاً** — ★ **والميزةُ `FR-M9-13` معطَّلةٌ
عملياً.** ⛅ **مقيسٌ لا مُستنتَج:** **المزوّدُ يردّ `n=3` بمفاتيحَ حقيقية فيُبنى
الزرُّ فعلاً** ⛔ **ولا يُعرَض** — **لا في الدخول الأول ولا الثاني، وقبل إصلاح
`DEBT-62` وبعده سواءً** ⟹ ★ **فهو مستقلٌّ سابقٌ له.**
⛔ **ولم يُعالَج عمداً:** ★ **خارج النطاق الضيّق الذي حدّده المالك** — ⟵ **وخلطُ
عطلين في تغييرٍ واحد يُفسد إسنادَ النتيجة.**

---

## 7. مخالفات النزاهة

✅ **لا مخالفات:** `integrity_violations: []` · `unparsed_lines: []`.
**27 وحدة — 9 منجَزة · 0 قيد التنفيذ · 17 مخطَّطة · 0 محجوبة · 1 تحتاج إعادة تحقق.**

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

★ **بلا تغيير:** `WU-000` · `WU-026` · `WU-001` · `WU-002` · **`WU-004`** ·
`WU-006` · **`WU-007`** · `WU-008`.
⛔⛔★★ **والأخيرتان بتأكيدٍ متكرِّر من المالك: لا نشرَ إنتاجيّ ولا تغييرَ IAM قبل
مراجعةٍ بشرية.**

---

## 9. مستندات UPDS التي حُدِّثت

| المستند | سبب التحديث |
|---|---|
| `risks-and-technical-debt-register.md` | ✅ **`DEBT-62` أُغلق بجدول تحقّقٍ من ستة معايير** · ⚠️ **و`DEBT-63` سُجِّل** |
| `implementation-plan.md` | ★ **`WU-005`: الإصلاحُ ونتائجُه، وسببُ بقائها `[يحتاج إعادة تحقق]`** |
| `CLAUDE.md` (الكتلة المُدارة) | ⛔⛔ **كانت تقول إن `WU-005` محجوبةٌ بـ`DEBT-62` — وصار ذلك غيرَ صحيح** ⟹ ✅ **صُحِّح إلى `DEBT-63`** · **والعدّاد 1510** |
| `implementation-report-2026-08-29-run5.md` | ★ **هذا التقرير** |

⛔ **ولم يُمَسّ `openapi.yaml` ولا `erd.md` ولا `data-dictionary.md` ولا
`migrations-log.md`** — ★ **لا مخططَ ولا عقدَ واجهةٍ تغيّر.**

---

## 10. حالة `CLAUDE.md`

✅ **حُدِّث عبر `sync_claude_md.py` وحده** — `action: managed-block-updated` ·
**20 سطراً بشرياً محفوظة** · **249 سطراً** (⚠️ **تحت سقف `AM-005` البالغ 250**).
★★ **ولماذا حُدِّث وليست نقطةَ تحوّلٍ بالتعريف:** ⛔ **لأن الكتلة كانت تحمل
حقيقةً صارت كاذبة** (**«`WU-005` بـ`DEBT-62`»**) — ★ **وهي تُقرأ في كل جلسة
جديدة**، ⟵ **فتركُها كان سيوجّه التشغيلَ القادم إلى عطلٍ مُغلَق.**

---

## 11. ✅ سطحُ التغيير — ملفّان

| الملف | التغيير |
|---|---|
| `lib/capabilities/inventory/application/inventory_providers.dart` | ★ **`isAutoDispose: true` + توثيقُ السبب** |
| `test/capabilities/inventory/presentation/daily_pricing_screen_test.dart` | ★ **حارسُ الانحدار** |

⛔ **و`daily_pricing_screen.dart` لم يُمَسّ** · ⛔ **ولا `AndroidManifest.xml`** ·
⛔ **ولا `firestore.rules`** — **مقيسٌ بـ`git status`.**

---

## 12. الخطوة التالية الموصى بها

★ **`DEBT-63`** (**زرُّ نسخ أسعار أمس**) — ⟵ **وهو آخرُ ما يمنع `WU-005` من
`[منجز ✅]`**: ★ **وأولُ ما يُفحَص `QtmsBulkActionBar`** (`Row([Expanded(Text), action])`)
⛔ **بمجسِّ هندسةٍ صحيح لا `GlobalKey` داخل `build`.**
★ **ثم §د.2 كاملةً لـ`WU-005`، ثم `WU-009`.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
