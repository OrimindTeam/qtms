# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-26 (تشغيل 3)

**رقم تشغيل التنفيذ:** 40
**المنهجية المعتمدة:** تزايدية (Incremental)
**تقدّم التنفيذ:** 22.2٪ (6 منجَزة من 27) — ⛔ **بلا تغيير**
**أسئلة تنفيذ مفتوحة:** ٠ (24 من 24 مغلقة — ★ **وثلاثةٌ منها فُتحت وحُسمت في هذا التشغيل**)

---

## 1. ملخص تنفيذي

✅★★★ **الحاجز زال واختبارُ المحاكي بدأ فعلاً لأول مرة.** حسابُ الاختبار أُنشئ
ومُنِح **74 صلاحية** و`sourceScope: all`، **وخُطّاف `DEBT-31` ملأ الحقلين تلقائياً**
⛔ **بلا إرسال**، **والدخول نجح**، ✅ **وأُنشئت جوانٍ فعلية من الواجهة.**

⛔⛔★★★ **وكشف الطريقُ ستةَ أعطالٍ حقيقية — لم يكشف واحداً منها 1042 اختباراً
آلياً** — ★ **كلُّها سُدَّت ونُشرت ومُثبَتة حيّاً:**

| # | العطل | الأثر قبل الإصلاح |
|:-:|---|---|
| `IQ-022` | **المالك بعد الإقلاع بلا `sourceScope`** | ⛔ **لا مسار لأول نطاق** ⟵ بيئةٌ تقفل على نفسها |
| `IQ-023` | **لا جذر للصلاحيات التشغيلية** (`BR-M1-03`) | ⛔ **لا مستخدم يملك صلاحيةً تشغيلية أبداً** |
| `IQ-024` | **قاعدة `sources` تعمل لـ`get` لا لـ`list`** | ⛔ **منتقي المصدر فارغ في كل شاشة** |
| `DEBT-33` | **`pieceWeightGrams` يمرّ `double` خاماً** | ⛔ **كلُّ نوعٍ وزنيٍّ يسقط بـ`500`** |
| `DEBT-35` | **قيدُ الجونية «يُسطَّح» فيرفضه المُرمِّز** | ⛔ **كلُّ إنشاء جونية يسقط بـ`500`** |
| `DEBT-36` | **شاراتٌ بلا قيدٍ في `InventoryTile`** | ⛔ **البطاقة تُرسَم حرفاً في كل سطر** (تجاوز 149 بكسل) |

⛔ **و`WU-004` تبقى `[قيد التنفيذ]`** — ★ **بصدق:** **أربعةُ سيناريوهات من
أحد عشر نُفِّذت.**

---

## 2. السيناريوهات — نتيجةٌ صريحة لكل واحد

| # | السيناريو | النتيجة |
|:-:|---|---|
| 1 | إنشاء جونية `45/6.5/1.2` | ✅ **نجح** — **«المطالب به 37.300 كجم» حيّاً** · `SCK-20260826-0002` · «جونية رقم 2» **من الواجهة كاملةً** |
| 2 | السكرب يدخل المخزن فوراً (`AT-07`) | ✅ **نجح** — «السكرب - جونية رقم 1» **1.200** · «… رقم 2» **2.000** · ★★★ **و`sourceDocType: sack`** |
| 3 | التسلسل مستقلٌّ لكل مصدر (`AT-14`/`AT-15`) | ✅ **نجح** — المصدر الثاني ⟵ **«جونية رقم 1»** بينما رقم المستند `-0003` |
| 4 | سطر وزني `100 × 200جم` | ✅ **نجح** — **فُسِّر 20.000 من 23.000** · المتبقّي **3.000** · ووزنُ الحبة عاد من التهيئة |
| 5 | سطر عددي (وزن الحبة مُستنتَج) | ⚠️ **لم يُنفَّذ** |
| 6 | تأكيد الوزن الضائع | ⚠️ **لم يُنفَّذ** — ★ **والزرّ ظاهرٌ وقيمتُه تتحدّث حيّاً** (`3.000` بعد السطر) |
| 7 | `ERR_INTAKE_003` | ⚠️ **لم يُنفَّذ** |
| 8 | `ERR_INTAKE_004` | ⚠️ **لم يُنفَّذ** |
| 9 | `ERR_INTAKE_005` | ⚠️ **لم يُنفَّذ** — ★ **والنوع مُهيّأ** (`مشقر` وزنيٌّ بلا وزن حبة) |
| 10 | حفظ بلا تأكيد ⟵ يبقى «غير مفسَّر» | ⚠️ **لم يُنفَّذ** |
| 11 | `ERR_AMEND_004` | ⚠️ **لم يُنفَّذ** |

---

## 3. مخالفات النزاهة

✅ `integrity_violations: []` · `unparsed_lines: []` · `has_drift: false`.

---

## 4. الفحوص المقيسة فعلياً

**227 حماية · 278 نطاق · 325 سحابة · 212 تطبيق = ✅ 1042 · ٠ فاشلاً** ·
✅ `analyze` بلا ملاحظة في الحزم الثلاث.

★★ **وكلُّ إصلاحٍ من الستة يحرسه اختبارُ ارتدادٍ مُثبَت** — ⛔ **ولم يُكتفَ
بكتابته:** ★ **أُعيد كلُّ عطلٍ عمداً فأفشل اختبارَه ثم أُزيل فعاد أخضر.**

---

## 5. بنود بوابة الحَوْكمة

★★ **وثلاثةُ قرارات من الفئة 1 صعّدتُها ولم أحسمها** — `IQ-022` · `IQ-023` ·
`IQ-024` — ✅ **وحسمها المالك بالخيار (أ) في الثلاثة**، ⟵ **ثم نُفِّذت.**

⛔⛔ **و`BR-M1-03` لم تُستثنَ ولم تُخفَّف** — ★ **والتغيير في جذر الشجرة وحده.**
⛔ **ولا توسيعَ امتيازٍ في `IQ-024`** — ★ **صاحبُ `all` كان يقرأ كل مصدرٍ أصلاً.**

---

## 6. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

★★★ **`WU-004`** (مفتاح `sackCreate` + أوزانٌ يُبنى عليها سعرُ الجونية) ·
★★ **وخُطّاف `DEBT-31`** ⛔ **بندٌ مستقل لا يُغني عنها** ·
★★★ **وثلاثةُ تغييراتٍ في طبقة التفويض نفسها** (`IQ-022` · `IQ-023` · `IQ-024`)
⟵ ⛔⛔ **تحتاج مراجعةً بشرية مستقلة قبل أي نشر إنتاجي.**

⛔⛔ **والإنتاج لم يُمَسّ بحرف** — ★ **وشروطه الأربعة باقية**، ⛔ **ولا يُقلَع
قبل أن يحمل الإنتاجُ إصلاحَي `IQ-022`/`IQ-023`** — ★ **وإلا قفل على نفسه.**

---

## 7. الخطوة التالية

**السيناريوهات السبعة المتبقّية** (5 · 6 · 7 · 8 · 9 · 10 · 11) — ★ **والبيئة
جاهزةٌ لها بالكامل:** الحساب والصلاحيات والبيانات المرجعية والأنواع الثلاثة
(**عددي · وزنيٌّ بوزن حبة · وزنيٌّ بلا وزن حبة**) **كلُّها مُهيّأة.**

```bash
flutter build apk --flavor staging --debug \
  --dart-define-from-file=.secrets/qtms-staging-qa-account.env \
  --dart-define=QTMS_FUNCTIONS_BASE_URL=https://qtms-callables-7stgg3ngga-ww.a.run.app
```

> ⚠️★★ **ودرسٌ تشغيلي:** **`QTMS_FUNCTIONS_BASE_URL` لا يُغني عنه ملفُّ
> الاعتماد** — ★ **وبناءٌ بدونه يفشل بـ«تعذَّر إتمام العملية» بلا أثرٍ في
> سجل الخادم**، ⛔ **لأن الطلب لا يُرسَل أصلاً.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen.*
