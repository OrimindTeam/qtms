# تقرير تشغيل التنفيذ — 2026-08-30 (التشغيل 46)

**الطلب:** **تشغيل §د.2 كاملةً لـ`WU-005`** على الجهاز الحقيقي — ★ **بكل
مسارات النجاح والخطأ الموثَّقة**، ⛔ **لا باختبارات ودجات.**

---

## 1. ✅ الحصيلة — 16 سيناريو شُغِّلت، كلُّها نجحت ⛔ ولا عطلَ واحد

⛅★★★ **`Pixel_6_API_36` · بناءٌ نظيف بلا أي مجسّ** — **14 نجاحاً + خطآن.**
⛔⛔ **ولم يُكتشَف أيُّ عطلٍ جديد في المسارات المُمارَسة** ⟹ ★ **ولم يُغيَّر
سطرٌ إنتاجيٌّ واحد في هذا التشغيل.**

---

## 2. ⛅ مسارات النجاح — 14

| # | السيناريو | الدليل المقيس |
|---|---|---|
| ① | **الدخول الأول يُرسَم** | الشاشة كاملة · ⛔ بلا فراغ |
| ② | **لا يظهر إلا ما له كمية اليوم** (`FR-M9-02`·`AT-19`) | **«سلة» وحدها** — ⛅ **مقابَلٌ بـ«مخزون اليوم»: وارد 100 · صادر 5 = 95** |
| ③ | **الوحدة تتبع النوع** (`FR-M9-06` جزئياً) | **«سعر التوزيع لكل حبة» · «الحد الأدنى لكل حبة»** |
| ④ | **«نسخ أسعار أمس» يظهر ويعمل** (`FR-M9-13`) | **`2000 ⟶ 1700`** · **`فارغ ⟶ 1300`** |
| ⑤ | ⛔ **والنسخُ لا يحفظ** (`BR-M9-02`) | **الخروجُ والعودة أعادا القيم المخزَّنة** ⛔ **بلا لافتة نجاح** |
| ⑥ | **الحفظ ينجح** | **«حُفظت أسعار اليوم.»** |
| ⑦ | **العدّاد حيٌّ** (`FR-M9-05`) | **`سُعِّر 0 من 1` ⟶ `1 من 1`** بعد اكتمال السعرين |
| ⑧ | **الحفظ بلا سبب** (`ADR-0020`) | **حقلُ السبب فارغ والحفظ نجح** ⛔ **بلا رفض** |
| ⑨ | **التعديل بسبب** (`FR-M9-11`) | **`2000 ⟶ 2200`** بسبب `amend-test` — **نجح** |
| ⑩ | **التفريغ يُعيد «غير مسعَّر»** | **مسحُ الحد الأدنى ⟹ حفظٌ ناجح والعدّاد `0 من 1`** |
| ⑪ | **مرشِّح «لم يتم»** | **المسعَّر اختفى · وشارةُ العدد `1` ظهرت** |
| ⑫ | **مرشِّح «تم التسعير»** | **الصفُّ ظاهرٌ بـ`2000/1500`** |
| ⑬ | **مرشِّح «الكل»** | **الصفُّ ظاهر · والشارةُ صُفِّرت** |
| ⑭ | **البقاء بعد الخروج والعودة** (`FR-M9-03`) | **`2200/1500` عادت من القاعدة · `1 من 1`** |

★★ **وسبعُ دخلاتٍ متتالية للشاشة كلُّها تُرسَم** ⟹ ✅ **`DEBT-62` سليمٌ بلا انحدار**،
✅ **و«نسخ أسعار أمس» ظاهرٌ في كل دخلة** ⟹ **`DEBT-63` سليم.**

---

## 3. ⛅ مسارات الخطأ — 2

| # | السيناريو | الدليل المقيس |
|---|---|---|
| ⑮ | **الصفر يُرفَض** (`BR-M9-06`) | **لافتةُ رفضٍ حمراء: «المبلغ يجب أن يكون رقماً صحيحاً بالريال — بلا كسور عشرية.»** (`ERR_MONEY_001`) ⛔ **والعدّاد بقي `0 من 1`** ⟹ **لم يُحفَظ شيء** |
| ⑯ | **الكسر يُمنَع بنيوياً** (`ADR-0015` القاعدة 3) | **إدخال `1.5` أنتج `15`** — ★ **النقطةُ رُشِّحت عند الإدخال** ⛔ **ولا تقريب** |

⚠️★ **وملاحظةٌ على ⑮ — ⛔ لا عطل:** ★ **نصُّ الرسالة يتحدث عن الكسور بينما
السببُ الفعلي هو الصفر** — ⟵ ★ **وهو قرارٌ موثَّقٌ صراحةً في الكود** (**`BR-M9-06`
يُعرَض بـ`ERR_MONEY_001` بسابقة `BR-M6-06`** ⛔ **ولا يُخترَع رمزٌ ثالث**) ⟹
★ **سُجِّل ملاحظةً للمالك** ⛔ **ولم يُغيَّر.**

---

## 4. ⛔⛔★★★ ثلاثةُ معايير لم تُمارَس — ولهذا تبقى `WU-005` مفتوحة

| # | المعيار | لماذا لم يُمارَس | ما يلزم |
|---|---|---|---|
| ① | **`AT-17`·`FR-M9-06` — السكرب بسعر الكيلو** | ⛅ **لا مخزونَ وزنياً أصلاً:** **«إجمالي الوزني: 0.000 كجم»** مقيسٌ في «مخزون اليوم» | ★ **إنشاءُ سكربٍ عبر «الوارد جواني»** (`M7`) — ⛔ **كتابةٌ من زيادةٍ أخرى، لم تُفعَل** |
| ② | **`AT-18`·`FR-M9-01` — التصفير في يومٍ جديد** | ⛅ **يحتاج انقلابَ يومٍ بتوقيت UTC** ⟹ **عائقُ بيئةٍ لا عطلُ كود** (§ج) | ★ **تشغيلٌ في يومٍ تالٍ** |
| ③ | **منعُ الصلاحية** (`dailyPriceWrite` غائبة) | ⛅ **حسابُ الاختبار يملك كل المفاتيح تعريفاً** (الكتالوج §6 ⑤) | ★ **حسابٌ مقيَّد** |

⚠️★★ **وللأول والثالث تغطيةٌ آلية** (`testWeightBalance` · قواعدُ الحماية على
المحاكي) — ⛔ **والآليُّ لا يُغني عن §د.2 بنصّ البروتوكول** (**«شرطٌ إضافي فوق
الفحوص الآلية وليس بديلاً عنها»**).

⟹ ⛔⛔★★★ **فالوحدة تبقى `[يحتاج إعادة تحقق]`** — ★ **ولا تُغلَق على تحقّقٍ
جزئي**، ⛅ **والحاجزُ الوظيفي ارتفع فعلاً** (**`FR-M9-13` تعمل**) ⟹ **والباقي
حاجزُ تغطيةٍ لا حاجزُ عطل.**

---

## 5. ⚠️ عطلٌ جديدٌ واحد — مُسجَّلٌ ⛔ لم يُعالَج

★ **`DEBT-64`** — **الحالةُ الفارغة تحت المرشِّح تنصح بنصيحةٍ خاطئة:**
⛅ **باختيار «لم يتم» بعد تسعير كل الأنواع تظهر «سجّل وارداً أولاً»** ⛔ **والمخزونُ
قائمٌ (95 حبة)** ⟹ **فالسببُ المعروض خاطئ** (`design-system.md` §هـ:
**«الفارغة تعرض سببَ الفراغ لا وصفَه»**). ⚠️ **منخفضُ الأثر — إرشادٌ مضلِّل
لا خطأُ بيانات** · ⛔ **وسابقٌ مستقلٌّ عن `DEBT-62`/`DEBT-63`** ⟹ ★ **ولم
يُصلَح: خارج نطاق التحقق** (**«لا إصلاحَ صامتٌ لما لا يخصّ العطل»**).

---

## 6. ⚠️ تعارضٌ توثيقيٌّ مرصود — ⛔ لم يُعدَّل

⚠️★★ **سطرُ «المتطلبات المرتبطة» في `WU-005` يقول `AT-21`…`AT-25`** — ⛔ **وهي
اختباراتُ قبولٍ لـ`M10` (التوزيع) لا لـ`M9`** ⟵ ★ **واختباراتُ `M9` هي
`AT-16`·`AT-17`·`AT-18`·`AT-19`·`AT-20`·`AT-27`·`AT-45`.** ⛔ **ولم يُصحَّح
في هذا التشغيل** — ★ **تصحيحُ مرجعٍ في الخطة قرارُ حَوْكمةٍ يُعرَض على المالك**
⛔ **لا تعديلٌ صامتٌ أثناء تحقّق.**

---

## 7. ✅ بوابة التحقق الآلية — كما هي

★ **لم يُغيَّر كودٌ في هذا التشغيل** ⟹ **البوابةُ على نتيجة التشغيل 45:**
**403 تطبيق · 412 نطاق · 461 سحابة · `analyze` نظيف** · ⚠️ **و236 قواعدُ
حماية لم تُعَد** (**`firestore.rules` لم تُمَسّ**).

---

## 8. ⚠️ الحالة

**27 وحدة — 9 منجَزة · 0 قيد التنفيذ · 17 مخطَّطة · 0 محجوبة · 1 تحتاج إعادة
تحقق.** ✅ `integrity_violations: []`.
⛔ **ولم يُنشَر شيء ولم تُمَسّ صلاحيةُ IAM** (§ز).
