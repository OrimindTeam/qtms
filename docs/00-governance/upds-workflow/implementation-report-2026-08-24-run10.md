# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-24 (تشغيل 10)

**رقم تشغيل التنفيذ:** 22
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة**)
**تقدّم التنفيذ:** ★★ **7.4٪ (2 منجَزة من 27)** — ⟵ **`WU-000` أُغلقت**
**حالة التوثيق:** 100٪ · **أسئلة تنفيذ مفتوحة:** **1 — `IQ-013` جديد**
**المطلوب:** النشر وإثبات المسار · إجابة `IQ-012` · حذف عيّنة `DEBT-14`

---

## 1. ملخص تنفيذي

✅ ★★★ **أُغلقت `WU-000` — بوابة التأسيس التقني.** نُشر مُنشئ الحسابات
وعمل **من طرف إلى طرف على قاعدة حقيقية**، ✅ **و`DEBT-14` سُدِّد كاملاً
بأشطره الثلاثة** ومُتحقَّق منه **بجردٍ بعد الحذف لا بالادّعاء**.

★★★ **وأقوى ما في هذا التشغيل فحصٌ واحد:** زُرع **رصيد حقيقي `123456`** ثم
أُعيد إطلاق المشغّل على نفس المستند ⟵ ✅ **فصمد الرصيد ولم يُمَسّ.**
★ **وهو إثبات أن حارس «يُنشئ الناقص ولا يلمس القائم» يعمل في الإنتاج** —
⛔ **لا في اختبارٍ بمزيّف.**

✅ **ونُفِّذ `IQ-012` بالخيار أ** — ★ **مع تصحيح صريح لادّعاءٍ غير دقيق ورد
في نصّ البند نفسه**، ⟵ **و`IQ-013` يحمل ما بقي غير محسوم فعلاً.**

★ **365 اختباراً ناجحاً · ٠ فاشلاً** · ✅ **والبوابتان تنجحان.**

---

## 2. بوابة الدخول

| البوابة | النتيجة |
|---|---|
| **العامة (٨ شروط)** | ✅ **8/8** — والـ17 ADR كلها «مقبول» |
| **بوابة `WU-000` (٥ شروط)** | ✅ **5/5** |

---

## 3. ★★★ النشر والإثبات من طرف إلى طرف

### 3-أ. ما نُشر — بأقل امتياز

| المورد | الاسم |
|---|---|
| خدمة Cloud Run | `qtms-provision-accounts-on-source-add` · ⛔ **بلا وصول عام** |
| خدمة Cloud Run | `qtms-provision-accounts-on-party-add` · ⛔ **بلا وصول عام** |
| حساب خدمة واحد | `qtms-provision-accounts@…` — `datastore.user` · `eventarc.eventReceiver` · **`run.invoker`** |
| مشغّل | `qtms-provision-on-source-add` ⟵ `sources/{sourceId}` |
| مشغّل | `qtms-provision-on-dealer-add` ⟵ `dealers/{dealerId}` |
| مشغّل | `qtms-provision-on-supplier-add` ⟵ `suppliers/{supplierId}` |

★ **والصورة `dart:3.11` بلا Flutter** — ⟵ **والبناء نفسه إثبات `ADR-0012`:**
لو تسرّبت اعتمادية Flutter إلى حزمة النطاق **لفشل البناء قبل النشر.**

### 3-ب. ★★ خطران دُفعا قبل النشر لا بعده

**① هل يقبل الإطار حمولة protobuf أصلاً؟** أحداث القاعدة **لا تُسلَّم إلا
بـ`application/protobuf`** (`DEBT-16`). ⛔ **ولو رفضها الإطار لفشل كل تسليم
بعد النشر.** ✅ **فُحص في مصدر الحزمة ثم بالتشغيل المحلي: `HTTP 200`.**

**② `FUNCTION_SIGNATURE_TYPE`** — ⛔ **شرط نشرٍ لا شرط تشغيل محلي فقط**،
وبدونه **لا تقلع الخدمة إطلاقاً** فيبدو النشر ناجحاً والخدمة ميتة.

### 3-ج. ★★★ الإثبات — ستة بنود مرصودة

| # | ما جرى | الدليل |
|:-:|---|---|
| ① | كُتب `sources/SRC-E2E-CHECK` فعلاً | التسليم وصل **بعد ثانيتين** (`08:18:20` ⟵ `08:18:22`) |
| ② | الخدمة قرأت **القاعدة الحقيقية** | `nothingToDo` — والأطراف فارغة فعلاً |
| ③ | ★ **ورفعت راية النقص بدل ابتلاعه** | `scrapMissing: true` + سطر تحذير ⟵ **وهو `IQ-012` بعينه، ظاهراً في الإنتاج** |
| ④ | كُتب `dealers/DLR-E2E-CHECK` | `applied, documentsWritten: 1` |
| ⑤ | ★★ **والمستند مكتوب فعلاً** | `dealer_balances/DLR-E2E-CHECK_SRC-E2E-CHECK` · مفتاح مركّب صحيح · `sourceId` صريح · ★ **وكل مبلغ `integerValue`** ⟵ **`ADR-0015` مُثبَت في الإنتاج** |
| ⑥ | ★★★ **صمود رصيد حقيقي** | زُرع `123456` · حُذف المقوت وأُعيد إنشاؤه ⟵ `nothingToDo, documentsWritten: 0` ✅ **والرصيد كما هو** |

> ★★★ **البند ⑥ هو الإثبات الحاسم.** ⛔ **الفشل هنا كان سيمحو رصيداً
> محاسبياً بأصفار بلا أي إنذار** — ★ **وهو أسوأ ما يفعله كود مالي.**

✅ **وأثر الفحص نُظِّف كاملاً** — المستندات الثلاثة حُذفت بعد الإثبات.

---

## 4. ✅ `DEBT-14` — سُدِّد كاملاً ومُتحقَّق منه بالجرد

| الشطر | ما حُذف |
|---|---|
| **برمجي** | `spike_probe.dart` · خمسة اختبارات · نقطة الدخول |
| **سحابي** | المشغّل ← الخدمة ← حساب الخدمة **بالترتيب المُلزِم** · ★ **وصورتا `…/qtms/functions`** |
| **بيانات** | `spike_debt09` · `sources/SRC-EVENTARC-PROBE` · **أربعة عدّادات `SRC-CHECK6-*`** |

✅ ★ **والجرد بعد الحذف:** لا مشغّل عيّنة · ولا خدمة · ولا حساب · ولا صورة ·
**والمجموعات الست فارغة تماماً**. ⛔ **ولم يُحذَف مورد واحد خارج العيّنة.**

---

## 5. ✅ `IQ-012` — نُفِّذ بالخيار أ · ⚠️ **مع تصحيح على نفسي**

★ **القرار:** إنشاء النوع مسؤولية `M21`، **والتهيئة تصل وتُبلِّغ**.
✅ **وهو ما هو مُنفَّذ فعلاً — ومُثبَت بتسليم حقيقي** (البند ③ أعلاه).
★ **وخوارزمية التطبيع تُوضَع في طبقة النطاق مصدرَ حقيقة واحداً.**

> ⚠️★ **وتصحيح واجب:** ورد في نصّ `IQ-012` أن خوارزمية التطبيع **«غير
> موثَّقة في أي مستند»** — ⛔ **وهذا غير دقيق.** ✅ **وهي موثَّقة جزئياً في
> موضعين:** `master-data-design.md` §3 و`glossary.md`.
> ★ **فالحاجز أضيق مما وصفتُه:** **ثلاثٌ من ستّ محسومة** (المسافات ·
> الهمزات · حالة الأحرف)، **وأربع لا** — ⟵ **وهي وحدها `IQ-013`.**

### ★ `IQ-013` — ما بقي غير محسوم فعلاً

| # | الحالة | المثال الذي يفترق به المفتاحان |
|:-:|---|---|
| ① | **التاء المربوطة** `ة`⟷`ه` | «فاطمة» ⟷ «فاطمه» |
| ② | **الألف المقصورة** `ى`⟷`ي` | «يحيى» ⟷ «يحيي» |
| ③ | **التشكيل** | «السُّكرب» ⟷ «السكرب» |
| ④ | **التطويل** `ـ` | «السكــرب» ⟷ «السكرب» |

★ **والأثر مالي لا تجميلي:** ذمّةٌ واحدة **تنقسم على كيانين** يمرّان فحص
التفرّد كلٌّ من جهته. ⟵ **يحجب جزءاً من `WU-002`** ⛔ **ولا يحجب `WU-000`.**

---

## 6. الفحوصات التي شُغِّلت فعلاً

| الفحص | النتيجة |
|---|---|
| `npm test` (**البوابتان**) | ✅ **`exit 0`** · `PASS ③` · `PASS ⑤` · **188 · ٠ فاشلاً** |
| `functions` — `dart analyze` · `dart test` | ✅ **`No issues found!`** · **102 · ٠ فاشلاً** |
| `packages/qtms_domain` | ✅ **66 · ٠ فاشلاً** |
| `flutter analyze` · `flutter test` | ✅ **9 · ٠ فاشلاً** |
| `dart run build_runner build` | ✅ **خمسة أهداف** — ثلاث `http` واثنتان `cloudEvent` |
| `dart compile exe` | ✅ **Dart وحده بلا Flutter** |
| ★★ **تشغيل محلي بحمولة protobuf** | ✅ **`HTTP 200`** |
| ★★★ **تسليم Eventarc حقيقي** | ✅ **`applied` ثم `nothingToDo`** |
| ★★★ **صمود رصيد `123456`** | ✅ **لم يُمَسّ** |
| ★ **جرد ما بعد الحذف** | ✅ **الست مجموعات فارغة · ولا مورد عيّنة** |

★ **المجموع: 365 اختباراً ناجحاً · ٠ فاشلاً** — **وثلاث طفرات** أسقطت
اختبارات ثم أُعيدت.

---

## 7. ★ لماذا جاز إغلاق `WU-000` — بنداً بنداً

| الفحص من `product-roadmap.md` §0 | الحالة |
|---|---|
| ① كتابة بلا صلاحية تُرفض | ✅ **مُثبَت** — 188 اختباراً ومصفوفة ثنائية الاتجاه |
| ② كتابة رصيد سالب تُرفض | ✅ **مُثبَت** — الأرصدة `allow write: if false` |
| ③ مصدر خارج النطاق يُرفض | ✅ **مُثبَت** |
| ④ **تعديل بلا سبب يُرفض** | ⏳ ★ **نُقل موضع إثباته بـ`CR-002`** ⛔ **ولا يُدَّعى تحقّقه** |
| ⑤ حذف حركة يُرفض | ✅ **مُثبَت** |
| ⑥ رقمان متزامنان بلا تكرار | ✅ **مُثبَت بتشغيل حقيقي ثلاث مرات** |
| **زائداً `DEBT-09`** | ✅ **أضلعه الخمسة** |

★ **وكل بند في النطاق مُستوفى أو محسوم بقرار معتمد:** الترقيم ×2 **أُلغيا**
(`IQ-009`) · **وكاتب سجل التدقيق صار خطوةً داخل المعاملة** (`ADR-0013`
القاعدة 1 — مبنيّ في `AuditedTransaction`) · **ومزامن الصلاحيات ومُنشئ
الحسابات منشوران ومُشغَّلان.**

> ⚠️ ★ **والوحدة تُغلَق على ما أُثبت لا على ما يُفترَض:** ⛔ **الفحص الرابع
> لا يُحتسَب مستوفىً**، وهو محفوظ **بندَ قبولٍ مُلزِماً في `DEBT-21`**.

---

## 8. مخالفات النزاهة

✅ **لا مخالفات** — `integrity_violations: []` · `unparsed_lines: []`.
★ **وحدتان `[منجز ✅]` بدليل مسجَّل** · **0 قيد التنفيذ** · **0 محجوبة**.

---

## 9. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

★ **`WU-000` تلمس نطاقاً حسّاساً — نعم.** والتحقق الآلي **شرط ضروري لا كافٍ**:

| # | ما يحتاج عيناً بشرية |
|:-:|---|
| 1 | ★★ **App Check بفرض مُطفأ** — ⛔ **يُفعَّل إلزامياً قبل أول نشر إنتاجي** |
| 2 | ★★ **`DEBT-21`** — أحد عشر قيداً **وفحصٌ منقول** غير مفروضة في أي مكان حتى تُبنى الدوال الكاتبة |
| 3 | ★ **خدمتان تكتبان بامتياز إداري يتجاوز قواعد الحماية** — ✅ **بلا وصول عام**، ★ **وتفويضهما في الكود** (`ADR-0002`) |
| 4 | ★ **حساب المالك بلا صلاحيات** حتى يُشغَّل الرَنبوك · **و`QTMS_OWNER_UID` بمعرّف كل بيئة هي** ⛔ **وخلطُه تصعيدُ امتياز** |
| 5 | ⛔ **والإنتاج بلا أي من هذه الموارد بعد** — ولا يُنشَر قبل البند 1 |

---

## 10. مستندات UPDS التي حُدِّثت

| المستند | سبب التحديث |
|---|---|
| ★★★ `implementation-plan.md` | **`WU-000` ⟵ `[منجز ✅]`** بجدول إثبات وفحوصٍ بنداً بنداً |
| ★★ `implementation-questions.md` | **`IQ-012` مُغلق** (+ تصحيح) · **`IQ-013` جديد** |
| ★★ `risks-and-technical-debt-register.md` | **`DEBT-14` ⟵ مُسدَّد** |
| ★★ `environments.md` §1.4 **(جديد)** | **البنية المنشورة فعلاً** ومتغيّراتها الإلزامية |
| ★ `troubleshooting-guide.md` §2-د | **مزلقان جديدان** — ★ **وأُزيل تكرار أدخلتُه** |
| **جديد** `functions/cloudbuild.yaml` | بناء الحاوية من جذر المستودع |

⛔ **و`api-overview.md` لم يُمَسّ** · ⛔ **ولا `srs.md`** · ⛔ **ولا أي ADR.**

---

## 11. حالة `CLAUDE.md`

✅ **حُدِّث بالسكربت حصراً** — ★ **ونقطة تحوّل حقيقية هذه المرة**
(`claude-md-management.md` §2 البند 5): **اكتمال وحدة عمل.**

---

## 12. الخطوة التالية

1. ★★ **أجب `IQ-013`** ⟵ **يفكّ جزءاً من `WU-002`.**
2. ★★★ **ثم `WU-001`** — أول زيادة تشغيلية بشاشات (**الدخول والصلاحيات**).
3. ⚠️ ★ **وقبل أي نشر إنتاجي: فعّل فرض App Check** — ⛔ **وهو أقدم بند
   معلَّق في المشروع.**
4. ⛔ **ولا دالة كاتبة تُقبَل بلا استيفاء `DEBT-21`** — **أحد عشر قيداً
   وفحصٌ منقول.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
