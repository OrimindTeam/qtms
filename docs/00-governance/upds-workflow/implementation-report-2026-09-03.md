# تقرير تنفيذ برمجي — UPDS DocGen — 2026-09-03

**رقم تشغيل التنفيذ:** التشغيل الأول في 2026-09-03
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة** — شريحة رأسية كاملة)
**تقدّم التنفيذ:** 62.1٪ (18 منجَزة من 29) — ⏳ **و`WU-015` قيد التنفيذ**
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** 0

---

## 1. ملخص تنفيذي

★★★ **بُنيت `WU-015` (مالية الجواني وحساب الرعوي · `M14`) كاملةً عبر
الطبقات الأربع** — طبقةُ النطاق (المعادلات) · العملياتُ السحابية (المُحتسِب)
· القواعدُ (⛔ **لم تُمَسّ**) · التطبيق (شاشةٌ وورقتا تفصيلٍ وضريبة) —
**و84 اختباراً جديداً** رفعت البوابة الآلية من **2222** إلى **2306 · ٠
فاشلاً**، ⛅ **ونُشرت خدمةُ العمليات والفهارس على التجريبية ذاتياً**
(`AM-010`) **وقِيس الاحتسابُ حيّاً من طرفٍ إلى طرف بأرقامه.**

⛔⛔★★★ **ولم تُعلَّم الوحدة منجَزة:** ★ **اختبارُ المحاكي `Pixel_6_API_36`
(§د.2) لم يُنفَّذ** — **المحاكي سقط في منتصف السيناريو ثم لم يعد `adb` يراه**،
⟵ **وطُبِّق §ج بحرفه: محاولتان ثم توقّف** ⛔ **بلا تجريبٍ متتالٍ.** ★ **وأمرُ
الاستئناف مُسلَّمٌ للمالك في §11.**

★★ **وسقط أثرٌ معلَنٌ من `WU-004`:** **`enterSackTax` صار له واجهةٌ ومقيسٌ
حيّاً** — ⟵ **ونطاقُ `WU-015` يشمل «وضريبة الجونية» نصّاً** ⛔ **فليست
توسعةَ نطاق.**

---

## 2. بوابة الدخول — نتيجة الفحص

### 2.1 البوابة العامة (٨ شروط) — ✅ **كلُّها متحققة**

| # | الشرط | ✅ | المسار المفحوص |
|:-:|---|:-:|---|
| 1 | **المنهجية مُصرَّح بها** | ✅ | `docs/21-project-management/development-methodology.md` (**تزايدية**) |
| 2 | **`srs.md` معتمد** | ✅ | `docs/02-requirements/srs.md` |
| 3 | **العمارة والمكدس** | ✅ | `architecture-overview.md` · `technology-stack.md` |
| 4 | **قواعد الكتابة** | ✅ | `coding-standards.md` · `naming-conventions.md` · `error-handling-strategy.md` |
| 5 | **تعريف «منجَز» والتفرّع والالتزام والإصدار** | ✅ | `definition-of-done.md` · `branching-strategy.md` · `commit-message-conventions.md` · `versioning-scheme.md` |
| 6 | **استراتيجية الاختبار** | ✅ | `08-testing-and-qa/test-strategy.md` |
| 7 | **سياسة الأسرار والمصادقة** | ✅ | `secrets-management-policy.md` · `authentication-policy.md` |
| 8 | ⛔ **لا ADR بحالة «مقترح»** | ✅ | **24 ADR — كلُّها «مقبول/معتمد»** (مقيسٌ بـ`grep` على `**الحالة:**`) |

### 2.2 بوابة الوحدة `WU-015` (٥ شروط) — ✅ **كلُّها متحققة**

| # | الشرط | ✅ | الدليل |
|:-:|---|:-:|---|
| 1 | **تصميم الوحدة موجود** | ✅ | `docs/04-design/module-design/sack-valuation-design.md` |
| 2 | **المتطلبات المرتبطة موجودة** | ✅ | `docs/02-requirements/functional-requirements/FR-M14.md` |
| 3 | **التبعيات منجَزة** | ✅ | `WU-004` (الجواني) · `WU-006` (التوزيع) · `WU-012` (البيع النقدي) · `WU-014` (السحبيات) — **كلُّها `[منجز ✅]`** |
| 4 | **لا `IQ` يحجبها** | ✅ | **0 بند مفتوح** (39/39 محسومة) |
| 5 | **معيار قبولٍ قابل للفحص** | ✅ | `BR-M14-01` · `AT-44`…`AT-47` |

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

⛔ **لا وحدة** — ★ **و`WU-015` لم تكتمل بوابتُها** (راجع §3.2).

### 3.2 وحدات قيد التنفيذ

| WU | العنوان | ما بُني | ما بقي |
|---|---|---|---|
| `WU-015` | مالية الجواني وحساب الرعوي (`M14`) | ★ **12 ملفاً جديداً و11 معدَّلاً** عبر الطبقات الأربع · **84 اختباراً** · ⛅ **نشرٌ وفهارس على التجريبية** · ✅ **احتسابٌ حيٌّ مقيس** | ⛔ **اختبارُ المحاكي `Pixel_6_API_36` (§د.2)** |

#### ★★ ما بُني — مقيسٌ بالملفات

| الطبقة | الملفات |
|---|---|
| **النطاق** (جديد 3) | `sack_valuation.dart` (`computeSackRevenue` · `computeSupplierNet` · `computeSupplierSourceTotals` · `computeAverageKiloPrice`) · `sack_movements.dart` (`contributionsForSack`) · `sack_valuation_repository.dart` |
| **السحابة** (جديد 3) | `sack_valuation.dart` · `sack_valuation_handler.dart` · `sack_movement_reader.dart` |
| **السحابة** (معدَّل 6) | `firestore_writer.dart` · `functions.dart` · **وأربعةُ معالِجات تُطلق المُحتسِب بعد الالتزام** |
| **التطبيق** (جديد 6) | `sack_finance_screen.dart` · `sack_finance_cards.dart` · `sack_breakdown_sheet.dart` · `sack_tax_sheet.dart` · `sack_valuation_providers.dart` · `firestore_sack_valuation_directory.dart` |
| **التطبيق** (معدَّل 3) | `router.dart` · `home_shell.dart` · `main.dart` |
| **الاختبارات** (جديد 5 · معدَّل 1) | **32 نطاق · 32 سحابة · 20 تطبيق** |
| **الفهارس** | `firestore.indexes.json` — **`outflows: sourceId ↑ · stockDate ↓`** (39 ⟵ **40**) |

#### ✅★★★ الفحوص التي شُغِّلت فعلاً — بنتائجها المقروءة

| الفحص | النتيجة |
|---|---|
| `cd packages/qtms_domain && dart analyze` | ✅ `No issues found!` |
| `cd packages/qtms_domain && dart test` | ✅ **675 · ٠ فاشلاً** (643 ⟵ 675) |
| `cd functions && dart analyze` | ✅ `No issues found!` |
| `cd functions && dart test` | ✅ **685 · ٠ فاشلاً** (653 ⟵ 685) |
| `flutter analyze` | ✅ `No issues found!` |
| `flutter test` | ✅ **705 · ٠ فاشلاً** (685 ⟵ 705) |
| `flutter test test/core/design/design_gates_test.dart` | ✅ **38 بوابة** (§ح البند 3) |
| `cd test/security-rules && npm test` | ✅ **241 · ٠ فاشلاً** · **التغطية `PASS` بـ85 مفتاحاً** |
| **الإجمالي** | ✅ **2306 · ٠ فاشلاً** (2222 ⟵ 2306 · **+84**) |

#### ⛅★★★ ما قِيس حيّاً على التجريبية

| # | ما نُفِّذ | النتيجة المقروءة |
|:-:|---|---|
| ① | `gcloud builds submit` | ✅ **`callables:wu015` · 1M12S · SUCCESS** |
| ② | `gcloud run deploy` | ✅ **المراجعة `qtms-callables-00028-z2s` · 100٪ من المرور** · ✅ **`allUsers` على `roles/run.invoker`** ⛔ **فلا راية `--no-allow-unauthenticated`** · ✅ **`/createSack` بلا رمز ⟵ `401` · ومسارٌ مجهول ⟵ `404`** |
| ③ | `firebase deploy --only firestore:indexes` | ✅ **و`firestore:indexes` ردّ 40 فهرساً ومنها `outflows`** — **تحقّقٌ بعديٌّ مستقل** ⛔ **لا اكتفاءً برسالة النشر** |
| ④ | **احتسابٌ حيّ — جونيةٌ بلا رعوي** | ✅ `SCK-20260902-0001` ⟵ **`sackTax: 1125`** (25 × 45.000 — **`AT-13` حرفياً**) · **`sackRevenue: 7125`** · **`supplierNet: 6000`** · **`isRevenueFinal: false`** · ⛔ **ولا سطرَ دفتر رعية** (`FR-M7-03`) |
| ⑤ | **احتسابٌ حيّ — جونيةٌ برعوي** | ✅ `SCK-20260903-0001` ⟵ **`sackTax: 900`** (30 × 30.000) · **`supplierNet: -900`** · ✅ **`supplier_ledger` كامل بـ`recalcVersion: 2`** (**من `createSack` ثم `enterSackTax`**) · ✅ **و`supplier_balances/SUP-0001_SRC-001` مبنيٌّ من الدفتر** |
| ⑥ | **قابليةُ التكرار** | ✅ **تشغيلٌ ثالثٌ بلا تغيير أبقى `recalcVersion: 2`** ⛔ **ولم يكتب حرفاً** (`PAT-08`) |

#### ⛔⛔★★★ ما لم يُنفَّذ — ولماذا

★ **اختبارُ المحاكي `Pixel_6_API_36` (§د.2).**
⟵ **الشاشةُ سقطت سوداءَ في منتصف السيناريو ونافذةُ الإشعارات علقت في
التركيز** (`mCurrentFocus=NotificationShade` — **مقيسٌ بـ`dumpsys window`**)،
★ **ثم أُعيد تشغيل المحاكي فلم يعد `adb` يراه إطلاقاً** (**`emulator` و
`qemu-system-x86_64` عاملان في `Get-Process` · و`adb devices` فارغة بعد
`kill-server`/`start-server` وانتظارٍ يزيد على دقيقتين**).
★ **وطُبِّق §ج بحرفه:** **محاولتان بتصحيحٍ واضح ثم توقّف** ⛔ **بلا تجريبٍ
متتالٍ ولا تخمينِ حلولٍ بديلة** — ★ **والأمرُ في §11.**

⚠️★★ **وما قِيس بالعين قبل السقوط — يُذكَر بحدّه ⛔ ولا يُعدّ §د.2:**
★ **شاشةُ «مالية الجواني» فُتحت فعلاً من قسم «المالية» في الصدَفة**
(**شريطٌ علويٌّ موحّد باسمها · رأسُ سياقٍ بالمصدر واليوم المقفل · بطاقةُ
جونيةٍ · والتنبيهُ النطاقي `FR-M14-13` بنصّه**) — ⛔ **وهذا عرضٌ لا دورةُ
عملٍ كاملة.**

### 3.3 وحدات محجوبة (ولماذا)

⛔ **لا وحدة محجوبة** — ★ **ولا بند `IQ` جديد فُتح في هذا التشغيل.**

---

## 4. مخالفات النزاهة المرصودة آلياً

✅ **لا مخالفات** — `track_implementation.py` ردّ `integrity_violations: []`
و`unparsed_lines: []`.
★ **و`WU-015` تُقرأ `[قيد التنفيذ]` بـ17 سطرَ فحص** — **16 مصنَّفاً نجاحاً
وواحدٌ `unknown`** ⟵ **وهو سطرُ «اختبارُ المحاكي لم يُنفَّذ»** ⛔ **مكتوبٌ
عمداً بلا ادّعاء نجاح.**

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

⛔ **لا بند `IQ` جديد** — ★ **و39/39 محسومة كما كانت.**

★★ **وقراران تنفيذيان روتينيان (فئة 3) اتُّخذا ذاتياً — ⛔ ولم يُخمَّنا بل
اشتُقّا من سابقةٍ مقيسة في المشروع نفسِه:**

| # | القرار | ★ السابقة التي اشتُقّ منها |
|:-:|---|---|
| ① | **المُحتسِب يعمل داخل الحاوية بعد التزام العملية الأصلية** ⛔ **لا بمشغّل Eventarc** | ★ **`DEBT-16` يحجب المشغَّلات المحتاجة لقيم الحقول ويُسمّي `recomputeSackRevenue` بالاسم** — ⟵ **والمشروع عالج نظائرَها كلَّها بالنقل إلى مسار الكتابة** (`writeAuditEntry` · `recalculate*` · `syncPendingEntries`) |
| ② | **سطرُ دفتر الرعية واحدٌ لكل جونية يُعاد كتابته** ⛔ **لا سطرٌ لكل احتساب** | ★ **تعليلُ `recalcVersion` في `schema/supplier-ledger.md` نفسِه:** «**يمنع تطبيق حساب قديم *فوق* أحدث**» — ⛔ **و«فوق» لا معنى لها في الإلحاق** · ★ **و§5 تجمع «سعر كل جونية» جونيةً لا سطراً** |

★ **وكلاهما مسجَّلٌ في `implementation-plan.md` تحت `WU-015` وفي
`sack-valuation-design.md` §9-أ** — ⛔ **ولا يُطبَّقان صامتاً.**

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

| # | ما يقوله المستند | ما فرضه التنفيذ | كيف عُولج |
|:-:|---|---|---|
| ① | ★ **`sack-valuation-design.md` §6 القاعدة 2: «فهرس مركّب `sackId ↑ · stockDate ↓` — بلاه تصير العملية مسحاً كاملاً»** | ⛔ **الفهرسُ لم يُستعمَل** — ★ **لأن الخطوة ③ تشترط «سعرها الفعلي المسجَّل في *مستندها*»**، ⟵ **ودفترُ المخزون بلا سعر**: ★ **فالاحتسابُ يقرأ المستندات بـ(`sourceId` + `stockDate`)** | ✅ **تسويةُ الحالة (ب): حُدِّث `module-design`** (§9-أ جدول §6) ⛔ **بلا انحرافٍ صامت** — ★ **والفهرسُ يبقى قائماً ولم يُحذَف** (يخدم قرّاءَ الدفتر) |
| ② | ★ **`CLAUDE.md` (أثرا `WU-004`): «`enterSackTax` لم يُختبَر حيّاً — ⛔ لا واجهةَ له بعد (يُبنى مع `M14`)»** | ✅ **بُنيت واجهتُه في هذا التشغيل** — ★ **ونطاقُ `WU-015` يشمل «وضريبة الجونية» نصّاً** | ✅ **حُدِّث `CLAUDE.md`** ⟵ **صافي صفر سطر** (250 كما هي) |
| ③ | ⚠️ **`build-and-run-instructions.md` §2 و§2.1 ② يعرضان `flutter run`/`flutter build apk` بلا عَلَم التعريفات** — ★ **بينما §2.1 ①-ب يذكره** | ⛔ **بُني `app-staging-debug.apk` بلا العَلَم فردّ «`QTMS_FUNCTIONS_BASE_URL` غير مضبوط»** — ★ **`DEBT-49` في وجهه الخامس** ⟵ **وأهدر جولةً كاملة** | ✅ **أُضيف العَلَم إلى أمرَي البناء والتشغيل نفسَيهما** — ⟵ **فمَن ينسخ الأمر لا يحتاج قراءة ما فوقه** |
| ④ | ★ **`data-dictionary.md` §4: `supplier_balances` = «إجمالي سعر الجواني · إجمالي الضريبة · الصافي»** | ★ **لزم عدّادان يقولان *لماذا* الرقم غير نهائي** (`pendingTaxCount` · `unfinalRevenueCount`) | ✅ **حُدِّث القاموس** — **إضافةُ حقولٍ مشتقّة (AUTO · UPDS-05 §3.1)** ⛔ **بلا تغييرِ معادلة** |

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

⛔ **لا بند جديد** — ★ **ولا `ADR` جديداً ولا `RFC` ولا تعديلاً على
`srs.md`**: ⟵ **الزيادة نفَّذت `FR-M14` كما هو.**

⛔⛔★★★ **ولا نشرَ إنتاجيّاً وقع ولا طُلب:** ★ **كلُّ ما نُفِّذ على
`qtms-orimind-master` وحدها** (فئة 3 · `AM-010`) — ⛔ **و`qtms-orimind-c001`
لم تُمَسّ بحرف.**

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

| WU | لماذا حسّاسة | ما يلزم |
|---|---|---|
| `WU-015` | ⛔⛔ **يحدد ما يُستحَق للرعوي** — ★ **ورقمٌ خاطئ هنا يُدفَع أو يُمنَع مالاً حقيقياً** | ★ **مراجعةٌ بشرية للمعادلات الأربع ولجدول §3 من `sack-valuation-design.md`** · ⛔ **واختبارُ المحاكي (§د.2) شرطٌ سابقٌ لأي اعتماد** |
| `WU-004` | ★ **مفتاحُ تفويضٍ جديد وأوزانٌ يُبنى عليها السعر** | ⛔ **مراجعتُها الأمنية البشرية ما تزال قائمة** (الفئة 1 البند 5) |

★ **وكلُّ الوحدات المنجَزة الثماني عشرة تحمل الوسم نفسَه في الخطة** —
⛔ **والتحققُ الآلي شرطٌ ضروري لا كافٍ لأيٍّ منها.**

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| `04-design/module-design/sack-valuation-design.md` | **الحالة «مخطَّط» ⟵ «منفَّذ»** · **§9-أ حاشيةُ التنفيذ والقراران المُعلَنان وتسويةُ الفهرس** |
| `06-database/data-dictionary.md` | **حقولٌ جديدة في `supplier_ledger` و`supplier_balances` و`sacks/{id}/finance/current`** |
| `06-database/schema/supplier-ledger.md` | **حاشيةُ التنفيذ + الكاتبُ الوحيد + الدليلُ الحيّ** |
| `06-database/schema/sacks.md` | **`sackRevenue` و`supplierNet` صار لهما كاتب** |
| `06-database/migrations-log.md` | **أربعةُ تغييراتٍ بنيوية (17…20) + فهرسٌ منشور** ⛔ **ولا ترحيلَ بيانات** |
| `05-api/api-overview.md` §3.2 | **`recomputeSackRevenue` صار مُنفَّذاً — وموضعُ تنفيذه مُعلَن** |
| `02-requirements/requirements-traceability-matrix.md` | **عمودُ «الكود» لـ`FR-M14` 🔷 ⟵ 🟨** |
| `03-architecture/risks-and-technical-debt-register.md` | **`DEBT-16` ضُيِّق مداه ثانيةً** — ⛔ **ولم يُغلَق** |
| `07-development/build-and-run-instructions.md` | ⛔⛔ **عَلَمُ التعريفات في أمرَي البناء والتشغيل** (`DEBT-49` الوجه الخامس) |
| `00-governance/upds-workflow/implementation-plan.md` | **`WU-015` `[مخطط]` ⟵ `[قيد التنفيذ]` بملفاتها وفحوصها** |

⛔ **ولم تُحدَّث `openapi.yaml` ولا `endpoints/`** — ★ **لا عمليةَ مستدعاةً
جديدة:** ⟵ **العددُ 47 كما هو** (`CallableOperation` بلا قيمةٍ مضافة).

---

## 10. حالة `CLAUDE.md`

✅ **حُدِّث — نقطةُ تحوّل فعلية** (`claude-md-management.md` §2 البند 5:
**تقدّمٌ جوهري في التنفيذ**).

| البند | القيمة |
|---|---|
| `action` | **`managed-block-updated`** |
| `human_content_lines_preserved` | **20** |
| `new_total_lines` | **250** — ★ **صافي صفر** (`AM-005`: السقف 250) |
| `exceeds_recommended_size` | **`true`** — ★ **بالاستثناء المعتمد وحده** |

★ **وأربعةُ تعديلاتٍ لا غير:** **عدّادُ البوابة 2222 ⟵ 2306** ·
**`WU-015` قيد التنفيذ في سطر المرحلة** · **مراجعةُ الخدمة `00027-z5h` ⟵
`00028-z2s` والصورة `callables:wu015`** · **وأثرُ `enterSackTax` سقط.**
⛔ **ولا قسمَ جديد** — ★ **فالسقف بلا هامش** (`AM-005`): ⟵ **وأولُ قاعدةٍ
تالية تستوجب `.claude/rules/`.**

---

## 11. الخطوة التالية الموصى بها

### ⛔⛔★★★ ① أمرُ بيئةٍ متعذّر — مُسلَّمٌ للمالك (§ج)

★ **المحاكي `Pixel_6_API_36` لم يعد يُرى من `adb`.** ★ **نفِّذ في طرفيتك
(PowerShell) ثم ألصق المخرَجات كاملةً كما ظهرت:**

```powershell
Get-Process -Name "qemu*","emulator*" -ErrorAction SilentlyContinue | Stop-Process -Force
adb kill-server
Start-Process emulator -ArgumentList "-avd Pixel_6_API_36"
Start-Sleep -Seconds 60
adb devices
adb shell getprop sys.boot_completed
```

★ **وعند ظهور `emulator-5554  device` و`1`** — ★ **أعِد
`/upds-docgen:implement WU-015`** ⟵ **فيُستأنف من §د.2 وحده**:
⛔ **ولا يُعاد بناء شيء** (**الكودُ والاختباراتُ والنشرُ كلُّها قائمة**).

★ **والحزمةُ جاهزةٌ ومثبَّتة على المحاكي** (`app-staging-debug.apk`
**بعَلَم التعريفات**) — ⛔ **ولا يلزم بناؤها ثانيةً إلا إن مُسح المحاكي.**

### ★★ ② سيناريو §د.2 المطلوب — مُحدَّدٌ سلفاً فلا يُرتجَل

| # | الخطوة | ما يُقاس |
|:-:|---|---|
| 1 | **«المالية» ⟵ «مالية الجواني»** | ★ **الشاشة تُفتح بشريطٍ علويٍّ موحّد ورأسِ سياق** |
| 2 | **إدخالُ ضريبة الكيلو من الورقة** | ★ **المعاينةُ الحيّة = `round(الضريبة × الوزن الكلي)`** · ✅ **والحفظُ يُغلق الورقة** |
| 3 | **مرشِّحُ الرعوي ⟵ «محمد»** | ★ **بطاقةُ حساب الرعوي تظهر بإجمالياتها الثلاثة** |
| 4 | **النقرُ على بطاقة الجونية** | ★ **تفكيكُ السعر بجدوله السداسي** · ★ **والمجموعُ يطابق `finance/current`** |
| 5 | **توزيعةٌ من نوعٍ في الجونية ثم العودة** | ⛅ **السعرُ يتغيّر تلقائياً** (`FR-M14-05`) |
| 6 | **مسارُ خطأ: حفظُ ضريبةٍ بحقلٍ فارغ** | ⛔ **الزرُّ معطَّل** ⛔ **ولا نداءَ يُرسَل** |
| 7 | **مسارُ خطأ: جونيةٌ ملغاة** | ⛔ **لا مدخلَ ضريبةٍ فيها** |

★ **وتحقّقٌ بعديٌّ من القاعدة بعد كلٍّ منها:**
`python tools/staging/verify_sack_valuation.py SRC-001 <YYYYMMDD>`

### ⚠️ ③ بندٌ يُعرَض على المالك — ⛔ ولا يُقرَّر ذاتياً

★ **`FR-M14-11`** («**إضافة جونية من شاشة إدارة الجواني**») **لم يُنفَّذ** —
★ **مسارُه `M7` قائمٌ بمفتاحه `sackCreate`**، ⟵ **والمطلوب زرٌّ ثانٍ يفتح
ورقةَ `M7` نفسَها من هذه الشاشة.** ⛔ **وأُجِّل لأنه تكرارُ مدخلٍ لا قدرةٌ
ناقصة** — ★ **والقرار للمالك: أيُضاف في استئناف `WU-015` أم يبقى مدخلُ
«التوريد مخزني» وحدَه؟**

### ★ ④ بعد إغلاق `WU-015`

★ **المرحلة الثانية تتابع بترتيبها:** `WU-016` (**ضمار المالك** — ★ **أعلى
الزيادات خطراً حسابياً: اثنتا عشرة خطوة**) ⟵ `WU-017` ⟵ `WU-018`
(**بوابةُ المرحلة**). ⛔ **ولا تُفتَح المرحلة الثالثة قبل اختبارها التكاملي
(§د.3).**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
