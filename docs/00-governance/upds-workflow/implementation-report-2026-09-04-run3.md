# تقرير تنفيذ برمجي — UPDS DocGen — 2026-09-04 (التشغيل الثالث في اليوم)

**رقم تشغيل التنفيذ:** 21
**المنهجية المعتمدة:** تزايدية (Incremental) — وحدة العمل: **زيادة** (شريحة رأسية كاملة)
**تقدّم التنفيذ:** **79.3٪** (23 منجَزة من 29)
**حالة التوثيق وقت هذا التشغيل:** **100٪** (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** **0** — ⛔ **ولم يُصعَّد بندٌ جديد في هذا التشغيل**

> ★ **وسيطُ التشغيل:** `WU-019` **محدَّدةً** — ⟹ **فقسمُ «تسلسل المرحلة»**
> (بروتوكول التشغيل §أ) **لا ينطبق**: ★ **وحدةٌ واحدة نُفِّذت كاملةً**،
> ⛔ **ولم تُفتَح `WU-020`.**

---

## 1. ملخص تنفيذي

بُنيت **`WU-019` — زيادة المتبقي المتأخر وتصريفه من طرف إلى طرف** كاملةً:
**راصدٌ سحابيٌّ** يكتب قائمة `aged_remainders` ويمحوها **داخل معاملة الحركة
نفسِها** (لا بمشغّلٍ ولا بجدولة)، **وشاشةُ متبقي الأيام السابقة** مجمَّعةً
بالأيام بعمرها ودرجة حدّتها، **وتنبيهٌ دائم** في لوحة اليوم، **ومسارُ تصريفٍ
يفتح `M10`/`M11` مثبَّتةً على تاريخ المخزون القديم** بمفتاح `agedRemainderClear`
— **وهو المسار الوحيد المصرَّح به لتاريخٍ أقدم من اليوم.**

★ **وسُدَّ `DEBT-03`** (الدَّين المقصود منذ المرحلة الأولى). ⛅★★★ **والزيادةُ
مقيسةٌ حيّاً على `Pixel_6_API_36`** مقابل `qtms-callables-00031-pns`:
`DST-20260830-0002` بـ`stockDate` **2026-08-30** و`entryDate` **2026-09-04**،
**وقيدُ تدقيقٍ فعلُه `agedRemainderClear` بالتاريخين معاً**، **و`retroUpdatedAt`
على ملخّص ذلك اليوم**. ⛔⛔ **و`firestore.rules` لم تُمَسّ بحرف.**

---

## 2. بوابة الدخول — نتيجة الفحص

### 2.1 البوابة العامة (ثمانية شروط)

| # | الشرط | متحقق؟ | المسار المفحوص |
|:-:|---|:-:|---|
| 1 | المنهجية مُصرَّح بها | ✅ | `docs/21-project-management/development-methodology.md` (`methodology_declared: true`) |
| 2 | العمارة والمكدّس | ✅ | `docs/03-architecture/architecture-overview.md` · `technology-stack.md` |
| 3 | الأسلوب والتسمية ومعالجة الأخطاء | ✅ | `docs/04-design/coding-standards.md` · `naming-conventions.md` · `error-handling-strategy.md` |
| 4 | «منجَز» والتفرّع والالتزام والإصدار | ✅ | `docs/07-development/*` · `docs/12-deployment-and-release/versioning-scheme.md` |
| 5 | استراتيجية الاختبار | ✅ | `docs/08-testing-and-qa/test-strategy.md` |
| 6 | سياسة الأسرار | ✅ | `docs/09-security/secrets-management-policy.md` |
| 7 | `srs.md` بمتطلبات مرقّمة | ✅ | `docs/02-requirements/srs.md` |
| 8 | ⛔ **لا `ADR` بحالة «مقترح» يحكم ما نُفِّذ** | ✅ | `ADR-0006` · `ADR-0007` · `ADR-0008` · `ADR-0013` — **كلها معتمدة** |

### 2.2 بوابة الوحدة (خمسة شروط) — `WU-019`

| # | الشرط | متحقق؟ | المسار |
|:-:|---|:-:|---|
| 1 | تصميم الوحدة | ✅ | `module-design/inventory-design.md` §5 (**عقدُ التنفيذ**) |
| 2 | المتطلبات بمعايير قبول | ✅ | `FR-M8-09`…`FR-M8-18` · `UC-004` · **ومخطط التسلسل** `clear-aged-remainder.md` |
| 3 | عقود الواجهات | ✅ | `05-api/api-overview.md` (**حُدِّث في هذا التشغيل**) |
| 4 | مخطط البيانات | ✅ | `06-database/data-dictionary.md` §4 · `indexing-strategy.md` |
| 5 | المصادقة والصلاحيات | ✅ | `09-security/permissions-catalog.md` §2.3 (`agedRemainderClear` **قائمٌ منذ `IQ-001`**) |

### 2.3 ★★ القراءة الإلزامية للشاشات (بروتوكول التشغيل §ح)

✅ **قُرئت الثلاثة بالكامل قبل كتابة أي سطر عرض:**
`docs/18-ux-ui/design-system.md` · `design-tokens.md` · `ui-guidelines.md` —
⛔ **لا `module-design` وحدَه.** ★ **وأثرُها مقيسٌ لا مُدَّعى:** **صفُّ
«يحتاج إجراء» بدل بطاقة تنبيه** (`ui-guidelines.md` §3 نمط 1 القرار 2 يضرب
**متبقي خمسة أيام** مثالاً بعينه) · **والثلاثيةُ الدلالية وحدها** ·
**وبوابةُ التصميم أسقطت الشاشة مرتين قبل الإصلاح** (§4 أدناه).

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

| WU | العنوان | الملفات المنتَجة | الفحوصات ونتيجتها الفعلية |
|---|---|---|---|
| ✅ **`WU-019`** | زيادة: المتبقي المتأخر وتصريفه من طرف إلى طرف | **النطاق:** `aged_remainder.dart` · `aged_remainder_repository.dart` · **السحابة:** `aged_remainder.dart` (**الراصد + البوابة**) **ووصلُها في خمسة معالِجات** · **والحقل الاختياري `stockDate`** في معالِجَي التوزيع والبيع النقدي وعقدَي مستودعيهما · **التطبيق:** `aged_remainder_screen.dart` · `aged_remainder_providers.dart` · `firestore_aged_remainder_directory.dart` · **صفُّ التنبيه في `home_shell.dart`** · **مسارٌ في `router.dart`** · **فهرسٌ واحد** · **وأداتا تجريبية** | `dart analyze` × 2 ⟵ **نظيف** · `dart test` (النطاق) ⟵ **813 · ٠ فاشل** · `dart test` (السحابة) ⟵ **720 · ٠ فاشل** · `flutter analyze` ⟵ **نظيف** · `flutter test` ⟵ **771 · ٠ فاشل** · **بوابات التصميم 38** · `npm test` (القواعد) ⟵ **257 · ٠ فاشل** · ⛅ **بناءٌ ونشرٌ ⟵ `qtms-callables-00031-pns`** · ⛅★★★ **اختبار المحاكي: 7 نجاح + 4 خطأ — الكل نجح** |

★★ **والاختبارات الجديدة في هذا التشغيل: 59 — مقيسةً لا مُقدَّرة** — **15**
(طبقة النطاق) · **17** (العمليات السحابية) · **16** (شاشة المتبقي وصفُّ التنبيه)
· **4** (تثبيتُ شاشة التوزيع) · **3** (تثبيتُ شاشة البيع النقدي) · **4** (قواعد
الحماية). ★ **وإجماليُّ `flutter test` مقيسٌ بمُبلِّغ `--reporter json`:**
**771 اختباراً مرئياً · ٠ فاشلاً** ⛔ **لا بقراءة سطر الحالة وحدَه.**

### 3.2 وحدات قيد التنفيذ

**لا شيء** — ⛔ **ولا وحدة تُركت `[قيد التنفيذ]`.**

### 3.3 وحدات محجوبة (ولماذا)

**لا شيء** — ⛔ **ولم يُصعَّد بند `IQ` في هذا التشغيل.**

---

## 4. مخالفات النزاهة المرصودة آلياً

**لا مخالفات** — `track_implementation.py` ⟵ `integrity_violations: []` ·
`has_integrity_violations: false`. ★ **كل وحدة معلَّمة منجَزة تقابلها فحوصات
ناجحة فعلياً.**

> ⛔⛔★★ **وبوابةُ التصميم أسقطت الشاشة مرتين قبل تعليم الوحدة منجَزة** —
> ★ **ويُقال لأنه دليلُ أن البوابة تعمل:** **حشوٌ بأربع قيمٍ موضعية**
> (`EdgeInsets.fromLTRB`) خالف **بوابة الاتجاه** و**سلّم المسافات** معاً،
> ⟵ **فصُحِّح إلى `EdgeInsetsDirectional.only`** ⛔ **ولم تُضعَّف البوابة.**

> ⚠️★★ **واختبارٌ قائمٌ اضطُرّ للتقوية لا للتخفيف** — `users_screen_test.dart`:
> ★ **صفُّ التنبيه الجديد أزاح قسمَ «إدارة المستخدمين» تحت الطيّة**،
> ⟵ **وجسمُ الصدَفة `ListView` يبني كسولاً** ⟹ **فسقط `findsOneWidget`.**
> ✅ **والعلاجُ تمريرٌ قبل القياس في الاختبار الموجب** — ★★ **وتمريرٌ إلى
> آخر الصدَفة + شاهدٌ حيّ (`التسعير اليومي`) في الاختبار السالب**:
> ⛔⛔ **وإلا صار النفيُ صادقاً لأن القائمة لم تُبنَ لا لأن البوابة أخفت** —
> ★ **نفيٌ فارغٌ يمرّ على شاشةٍ معطوبة.**

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

**لا شيء في هذا التشغيل** — ★ **وثلاثةُ قراراتٍ اتُّخذت ذاتياً بوصفها من
الفئة 3** (بروتوكول التشغيل §ب)، ⛔ **ولم يُخمَّن أيٌّ منها صامتاً:**

| # | القرار | ★ **لماذا لم يكن بند `IQ`** (اختبار `playbook` §5) |
|:-:|---|---|
| ① | ⛔ **لا حقلَ عمرٍ مخزَّن** — يُحسَب عند القراءة | ★ **البديلُ ليس متكافئاً بل خاطئ:** **عمرٌ مخزَّنٌ يشيخ بلا كاتب** ⟵ **فيُلوِّن بندَ خمسةِ أيامٍ بلون «يوم–يومان»** ⛔ **رقمٌ خاطئٌ بصمت** — ★ **ولا مجدولةَ في المكدّس تُحدِّثه** (`WU-016` بنى الملخصات داخل المعاملة لا بمشغّل) |
| ② | ★ **ولا مفتاحَ عرضٍ جديد** — النطاقُ وحده | ★ **`FR-M8` §5 يسمّي ستّاً، وثلاثٌ منها بلا مفتاحٍ في الكتالوج أصلاً**، ⟵ **وشاشةُ مخزون اليوم شُحنت على ذلك في `WU-003`**: ⟹ **فالسابقةُ داخل المتطلب نفسِه** ⛔ **لا اجتهاد** |
| ③ | ⚠️ **ثلاثُ درجاتٍ بعائلتين وأيقونتين** | ★ **الكودُ يبقى صحيحاً ومقبولاً بافتراضٍ موثَّق:** **العمرُ مكتوبٌ رقماً في كل صفٍّ** ⟹ **فلا معنى على اللون وحده**، ★ **والتغييرُ دالّةٌ واحدة** ⟵ **قابلٌ للتراجع بلا كلفة** |

> ⚠️⚠️★★ **والقرار ③ يُعرَض على المالك صراحةً:** ★ **إن أراد ثلاثَ عائلاتٍ
> لونية، فذلك لونٌ خارج شعار العميل** ⟹ **ويحتاج بنداً في
> [`design-source-selection.md`](design-source-selection.md) نظيرَ `DS-003`**
> — ⛔ **ولا يُدخَل بلا قراره.**

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

| # | ماذا يقول المستند | ماذا فرض التنفيذ | كيف عُولج |
|:-:|---|---|---|
| ① | `data-dictionary.md` §4 يَعُدّ **«العمر بالأيام»** ضمن محتوى `aged_remainders` | ⛔ **لا كاتبَ له ولا مُحدِّث** — ★ **والعمرُ دالّةٌ في اليوم الحالي** | ✅ **صُحِّح المستند ليطابق المُنفَّذ** (UPDS-05 §3.1: `data-dictionary` **AUTO**) — ⛔ **ولا يُترك ادّعاءُ حقلٍ لا كاتبَ له**، ★ **والسببُ مكتوبٌ في الخلية نفسِها** |
| ② | `api-overview.md` كان يقول: **«التصريف المتأخر مسارٌ لم يُبنَ بعد»** | ✅ **بُني في هذا التشغيل** | ✅ **حُدِّث بجدول الأحكام الثلاثة** (غائب · مساوٍ · أقدم · أحدث) **ورموزِ فشلها** |
| ③ | `indexing-strategy.md` §4 يقترح **«فهرس `balance > 0`»** | ⛔ **لا لزومَ له** — ★ **القائمةُ تحمل الموجب وحدَه ويُمحى البندُ عند الصفر** | ✅ **حُدِّث بالفهرس الفعلي** `sourceId ↑ · stockDate ↑` — ★ **والشرطُ مفروضٌ بالوجود نفسِه** |
| ④ | `api-overview.md` كان يعِد بفتح `FR-M22-10` (سحبيةٌ من متبقٍّ متأخر) مع `WU-019` | ⛔ **لم تفتحه هذه الزيادة** — ★ **`FR-M8-11` يسمّي التوزيعَ والبيعَ النقدي وحدهما** | ✅ **صُحِّح النصُّ صراحةً** ⛔ **ولا يَعِد بما لم يقع** |

⛔⛔★★ **ولا انحرافٌ صامتٌ واحد** — ★ **ولا `ADR` جديدٌ لزم:** ⟵ **كلُّ ما
نُفِّذ داخل `ADR-0006` و`ADR-0007` و`ADR-0008` و`ADR-0013` كما هي.**

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

⛔ **لا `ADR` جديد ولا `RFC` ولا تعديل على `srs.md`** — ★ **ولم يُعتمَد شيءٌ
ذاتياً.** ★★ **وبندٌ واحد يُعرَض للقرار لا للاعتماد:** **العائلةُ اللونية
الثالثة لدرجة الحدّة** (§5 القرار ③) — ⟵ **يحتاج `DS-*` نظيرَ `DS-003` إن
أراده المالك**، ⛔ **ولا يُدخَل بلا ذلك.**

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

★ **`WU-019` حسّاسةٌ بنصّ بندها** — ⛔ **والتحقق الآلي شرطٌ ضروريٌّ لا كافٍ:**

| # | لماذا تحتاج عيناً بشرية |
|:-:|---|
| ① | ⛔⛔ **تكتب حركاتٍ بتاريخِ مخزونٍ سابق** — ★ **أولُ مسارٍ في النظام يفعل ذلك**: ⟵ **ويُعيد بناءَ ملخصاتِ يومٍ مضى** (`retroUpdatedAt`) |
| ② | ★ **وتفتح مفتاحاً تفويضياً لم يكن له مسارٌ قبل اليوم** (`agedRemainderClear`) — ⟵ **ويجب أن يُسنَد بقصدٍ لا بالوراثة** |
| ③ | ⚠️ **وسيناريو «بلا المفتاح» لم يُقَس حيّاً** — ★ **مغطّىً آلياً بأربعة اختبارات**، ⛔ **ولا حسابَ ثانٍ على التجريبية يقيسه** |

★★ **وتنضمّ إلى قائمة الوحدات الحسّاسة القائمة** (`WU-000` · `WU-001` …
`WU-018` · `WU-026` … `WU-028`) — ⛔ **ولم تُراجَع أيٌّ منها بشرياً بعد.**

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| `06-database/data-dictionary.md` §4 | ★ **حقولُ `aged_remainders` الفعلية** — ⛔ **وحذفُ ادّعاء «العمر بالأيام»** (تعارض ①) |
| `06-database/indexing-strategy.md` §4 | ★ **الفهرسُ الفعلي بدل «فهرس `balance > 0`»** (تعارض ③) |
| `05-api/api-overview.md` | ★ **الحقلُ الاختياري `stockDate` وأحكامُه الثلاثة ورموزُ فشلها** (تعارضا ② و④) |
| `04-design/module-design/inventory-design.md` | ★ **الحالة + §9-و: ما نُفِّذ من §5 ومواضعه** |
| `02-requirements/requirements-traceability-matrix.md` | ★ **صفُّ `FR-M8-09`…`18` ⟵ عمودُ الكود والحالة 🟩** |
| `09-security/permissions-catalog.md` §2.3 | ★ **موضعُ إنفاذ `agedRemainderClear` فعلاً** |
| `03-architecture/risks-and-technical-debt-register.md` | ✅ **`DEBT-03` مُسدَّد** · ⏳ **و`DEBT-96` جديدٌ معلَن** |
| `firestore.indexes.json` | ★ **فهرسٌ واحدٌ جديد** (45 ⟵ 46) — **ومنشورٌ ومقيسٌ `READY`** |

⛔⛔ **ولا `openapi.yaml` ولا `erd.md` ولا `migrations-log.md`** — ★ **لا
`REST endpoint` جديد** (**عمليةٌ مستدعاة بحقلٍ اختياري**)، ★ **ولا جدولَ ولا
هجرةَ بيانات**: **المجموعةُ مشتقّةٌ بالكامل** ⟵ **و`stockDate` قائمٌ منذ
`WU-001`.**

---

## 10. حالة `CLAUDE.md`

**لم يُحدَّث — ولا نقطةَ تحوّلٍ تستدعيه.** ★ **واكتمالُ وحدةٍ نقطةُ تحوّلٍ
بالتعريف** (`claude-md-management.md` §2 البند 5) — ⛔ **لكنّ أثرَها صفر هنا:**
⟵ **قسمُ «حالة التنفيذ» في الكتلة المُدارة سطرُ إشارةٍ إلى
`implementation-plan.md`** ⛔ **ولا يحمل اسمَ الوحدة الجارية ولا عدّاداً**
(`AM-013` — **الحدُّ ثلاثةُ أسطر**). ⟹ ★ **فلا حرفَ يتغيّر**، ⛔ **ولم
يُشغَّل `sync_claude_md.py`**: ★ **والتشغيلُ بلا تغييرٍ ضجيجٌ في تاريخ
المشروع** (§2: «**خارج هذه النقاط لا تلمس الملف**»).

---

## 11. الخطوة التالية الموصى بها

1. ★★ **`WU-020` — زيادة الإتلاف** (`FR-M8-16` · `AT-54`): ⟵ **الإجراءُ
   الثالث في شاشة المتبقي المتأخر**، ★ **وموضعُه محجوزٌ في الشاشة بتعليقٍ
   صريح** ⛔ **بلا زرٍّ لا مسارَ خلفه.**
2. ★ **ثم `WU-021`** (مُعيد البناء بأثر رجعي) — ⟵ **وفيه تُسدَّد
   [`DEBT-96`]:** **مسحةٌ أولى تكتب بنودَ الأرصدة السابقة لنشر الراصد.**
3. ⚠️ **ولا تُفتَح بوابةُ المرحلة الثالثة** (`AT-51`…`AT-54` · `AT-63`…`AT-66`
   · `AT-68`) **قبل اكتمال `WU-020`…`WU-025`** — ★ **والاختبارُ التكاملي
   للمرحلة (§د.3) بعدها لا قبلها.**
4. ⛔⛔★★ **ولا شيء نُشر على الإنتاج** — ★ **وحالةُ البيئتين كما هي في**
   `environments.md`: ⟵ **وكلُّ ما جرى هنا على `qtms-orimind-master`**،
   ★ **والنشرُ الإنتاجي يقف حتى تصل كلمة المالك الحرفية.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*

---

## 12. ★★ فحص «الحشر بدل الاختصار» — والنتيجة مقيسةٌ لا مُقدَّرة

`track_implementation.py` ⟵ `has_field_char_warnings: true` · **135 تحذيراً
على 24 وحدة.** ★ **وتصنيفُ الحقول المُحذَّر منها هو الفاصل:**

| الحقل | العدد | ★ **هل يجوز اختصاره؟** (`playbook` §10) |
|---|:-:|---|
| **فحوصات التحقق** | **33** | ⛔⛔ **لا** — §10 الخطوة 3 نصّاً: «**لا يُحذف أي دليل تحقق فعلي — الفحوصُ المُشغَّلة ونتائجها تبقى**» |
| **الملفات المنتَجة/المعدَّلة** | **22** | ⛔ **لا** — **حقلٌ من قالب §10 الخطوة 1** يُبقى صراحةً |
| **ملاحظات** | **23** | ⚠️ **جزئياً وحده** — ★ **وهو الحقلُ السرديُّ الوحيد** |
| **المستندات الحاكمة · نتيجة التحقق · النطاق · معيار القبول · يلمس نطاقاً حسّاساً** | **26** | ⛔ **لا** — **كلُّها حقولُ القالب الأساسية** |

⟹ ★★★ **فسبعةٌ وثمانون تحذيراً من 135 تقع على حقولٍ يمنع §10 نفسُه اختصارَها**،
⛔ **واختصارُها كان سيُسقِط دليلَ تحقّقٍ أو حقلَ قالب.**

**عددُ الوحدات المُختصَرة في هذا التشغيل: صفر** — ★ **والسببُ مُفصَّلٌ لا مُجمَل:**

| # | الفئة | الوحدات | لماذا لم تُختصَر |
|:-:|---|---|---|
| ① | **متأثرةٌ بانحرافٍ قائم** (`drift.affected_units`) | `WU-000` · `WU-026` · `WU-001` · `WU-002` · `WU-003` · `WU-005` · `WU-006` · `WU-007` · `WU-008` · `WU-010` · `WU-011` · `WU-012` · `WU-013` · `WU-014` | ⛔ **غيرُ مستقرةٍ بتعريف §10** — ★ **تُترَك بتفصيلها** |
| ② | **حديثةُ الإنجاز** (آخر ثلاثة تشغيلات) | `WU-016` · `WU-017` · `WU-018` · `WU-027` · `WU-028` · `WU-019` | ⛔ **§10 الخطوة 4 تستثنيها صراحةً** |
| ③ | **مستقرةٌ وتحذيرُها على حقلٍ سردي** | `WU-004` · `WU-009` · `WU-015` | ⚠️★★ **حقلُ «ملاحظات» فيها يحمل قواعدَ حَوْكمةٍ وقراراتِ `IQ` محسومة** — ⛔ **و§10 الخطوة 3 تمنع حذفَ قاعدةِ حَوْكمةٍ أو أمانٍ مسجَّلة**: ⟹ ★ **وفرزُها يحتاج تشغيلاً مخصَّصاً يقرأ كلَّ بندٍ ويحكم عليه** ⛔ **لا تمريرةً عابرةً في تشغيلٍ نطاقُه `WU-019`** — ★ **والخطرُ إسقاطُ قاعدةٍ بصمت** |
| ④ | **مخطَّطة** | `WU-025` | ⛔ **ليست `[منجز ✅]` أصلاً** |

★ **والتوصية:** **تشغيلٌ مخصَّصٌ للاختصار** يمرّ على `ملاحظات` الوحدات الثلاث
المستقرة وحدها، **ينقل السردَ التاريخي إلى تقريره المؤرشَف ويُبقي كلَّ قاعدة**.

---

## 13. ⚠️★★ انحرافٌ توثيقيٌّ *أحدثَه هذا التشغيل عمداً* — ويُقال صراحةً

⛅ **بعد تحديث المستندات التابعة (§9) صار `track_amendments.py` يَسِم أربعةَ
مستنداتٍ حاكمةٍ بأنها «تغيّر محتواها»:**

| المستند | الوحداتُ التي تشير إليه |
|---|---|
| `04-design/module-design/inventory-design.md` | `WU-003` · `WU-005` |
| `06-database/data-dictionary.md` | `WU-000` · `WU-002` |
| `06-database/indexing-strategy.md` | `WU-000` · `WU-003` · `WU-011` |
| `09-security/permissions-catalog.md` | `WU-000` · `WU-026` · `WU-010` · `WU-011` · `WU-012` |

⛔⛔★★★ **وهذا انحرافُ بصمةٍ لا انحرافُ عقد** — ★ **والفرقُ جوهري:**
⟵ **الكاشفُ يقارن بصمةَ الملف** ⛔ **ولا يقرأ *ماذا* تغيّر فيه**:
★ **وما تغيّر هنا إضافاتٌ تصف ما بُني في `WU-019`** (**صفُّ `aged_remainders`
· فهرسُه · §9-و · موضعُ إنفاذ `agedRemainderClear`**) — ⛔⛔ **ولم يُمَسّ سطرٌ
واحدٌ يصف عقدَ أيٍّ من تلك الوحدات**: ⟹ ★ **فسلوكُها لم يتغيّر، ولا يلزمها
إعادةُ تحقّق.**

⚠️ **ويبقى `reporting-design.md` و`settlement-design.md` انحرافين سابقين لهذا
التشغيل** — ⛔ **لم يُحدِثهما ولم يعالجهما**، ★ **وهما من نطاق `/upds-docgen:amend`.**

⛔⛔★★ **ولا وحدةَ نُقلت إلى `[يحتاج إعادة تحقق]`** — ★ **وقرارُ ذلك للمالك
عبر `amend`** ⛔ **لا لتشغيلٍ نطاقُه زيادةٌ واحدة**، ⟵ **والحقيقةُ مذكورةٌ
هنا كاملةً فلا تُكتشَف لاحقاً كأنها أُخفيت** (بروتوكول التشغيل §و).
