# تقرير تشغيل التنفيذ — 2026-09-15 (`AM-020`)

**المهارة:** `/upds-docgen:amend` · **الحالة:** ★ **(٢) مستندات وكود معاً** —
⭐ **صرّح بها المالكُ حرفياً في نصّ الطلب**، ⛔ **فلا استنتاجَ ولا سؤال.**

**المصدر:** حزمةُ `ux_review_evaluation/` — ★ **أربعُ شاشاتٍ محدَّدةٍ بالنطاق**
(`sources_screen.md` · `suppliers_screen.md` · `dealers_screen.md` ·
`items_screen.md`) ⛔ **ولا بندَ خارجها.**

---

## 1. ما نُفِّذ — البنودُ الخمسة

| # | البند | الصنف في المراجعة | الحال |
|:-:|---|---|---|
| ① | **حرسُ «سبب التعطيل» يُعطِّل زرَّ الحفظ** في النماذج الأربعة | **مكسبٌ سريع** (المصادر · الرعية · المقاوته) **+ تحسينٌ بنيوي** (استخلاصُ دالةٍ مشتركة) | ✅ **مطبَّق** |
| ② | **تعذّرُ مُنتقي جهات الاتصال يُعرَض شريطَ `warning`** | **مكسبٌ سريع** (الرعية · المقاوته) | ✅ **مطبَّق** — ⛅ **وبإصلاحٍ ثانٍ لم تطلبه المراجعةُ لأنها لم تره** |
| ③ | **مدخلُ كشف الحساب على بطاقة المقوت** | **مكسبٌ سريع** (المقاوته) | ✅ **مطبَّق** |
| ④ | **بطاقةُ النوع `inline` بزرٍّ أيقونيٍّ صامت** | **مكسبٌ سريع** (الأنواع) | ✅ **مطبَّق** |
| ⑤ | ⚙️ **نصُّ الأثر التلقائي في ورقة الرعوي** | **ملاحظةٌ طلبت «تأكيداً صريحاً»** — ★ **ثم أمرَ بها المالكُ بعد عرضِ الحكم** | ✅ **مطبَّق** |

★ **والترتيبُ كما أمر المالك: «مكاسب سريعة» أولاً ثم «بنيوية»** — ⛔ **ولا
بندَ من «02_home_screen_redesign_reference.md» ولا شريطَ تبويبٍ سفلي** (**الاستثناءُ
الدائم**).

★★ **والبندُ ⑤ لم يكن توصيةً تُنفَّذ:** ★ **المراجعةُ أدرجته مشكلةً وطلبت
«تأكيداً صريحاً بدل أن يُقرأ كسهو»** — ⟹ **فعُرض عليك حكماً مقيساً في نهاية
الدفعة الأولى**، ✅ **ثم أمرتَ بتنفيذه إن كان الأفضل** — ⛅ **وكان.**

### ⚙️★★★ البند ⑤ — لماذا نُفِّذ: خمسةُ أوجهٍ كلُّها تقول نعم

| الوجه | الحكم ودليلُه |
|---|---|
| ⚙️ **وظيفياً** | ✅ `provisionAccountsOnPartyAdd` **«إضافة مقوت أو رعوي ⟵ حساب له في كل المصادر القائمة»** — ★ **فالنصُّ يصف ما يقع فعلاً** ⛔ **لا وعدٌ مخترَع** |
| 📜 **متطلَّبياً** | ✅ `FR-M3-04` **(حرجة)** **نظيرُ `FR-M4-05` حرفاً بحرف** · ★ **و`CR-006` يُشدِّده لا يُلغيه** |
| 🎨 **تصميمياً** | ✅ **ثلاثُ شقيقاتٍ: اثنتان تُعلنان وواحدةٌ صامتة** ⟹ **يُقرأ سهواً** — ✅ **والأنواعُ بلا نصٍّ عمداً** (**لا تُنشئ حساباً**): ⟵ **فالاتساقُ بالقاعدة لا بالشكل** |
| 👤 **تجربةً** | ✅ `ui-guidelines.md` **نمط 4: «أثرٌ يجب أن يعرفه المستخدم قبل الحفظ»** — ★ **رعويٌّ واحد يُنتج مستنداً في كل مصدر** |
| 🔍 **تدقيقاً** | ⛅ **المراجعةُ رجّحت أنه مقصود** — ⛔⛔ **والقياسُ في الكود والمتطلب نفى ذلك** ⟹ **سهوٌ أُغلق لا قرارٌ نُقض** |

### ⛅★★★ ما لم تطلبه المراجعةُ ولزم — **ابتلاعٌ ثانٍ في طبقة المنصّة**

★ **التوصيةُ وصفت الابتلاعَ في `contact_picker.dart` وحدَه** — ⛅ **وقياسُ
الكود كشف ابتلاعاً أسبقَ منه في** [`MainActivity.kt`](../../../android/app/src/main/kotlin/dev/orimind/qtms/MainActivity.kt):
`ActivityNotFoundException` ⟶ `result.success(null)`.

⟹ **ولو أُصلح طرفُ Dart وحدَه لبقي أشيعُ سببٍ حقيقيٍّ للفشل صامتاً تماماً**
(**جهازٌ بلا تطبيق جهات اتصال · إذنٌ مرفوض**) — ★ **لأنه لا يصل Dart استثناءً
أصلاً، بل `null` التي تعني «ألغى المستخدم»** ⛔ **فيُسلَّم إصلاحٌ ناجحُ
الاختبارات لا يُغيّر شيئاً في يد المستخدم.** ✅ **فأُصلحت الطبقتان معاً،
ومُقيسٌ الأثرُ على المحاكي بتعطيل تطبيق جهات الاتصال فعلياً.**

---

## 2. المستندات التي تغيّرت فعلياً — بمسارها

| المستند | ما تغيّر |
|---|---|
| [`docs/18-ux-ui/design-system.md`](../../18-ux-ui/design-system.md) | **§6-د:** «الأنواع» ضمن شاشات `inline` · **§6-ط:** البند ④ ممتدّاً إلى **الحقل الشرطي الإلزامي** |
| [`docs/04-design/module-design/master-data-design.md`](../../04-design/module-design/master-data-design.md) | **§8-ب-1 جديد** (عقدُ تعذّر الجسر بجدول الطبقات الثلاث) · **قسمُ «ما أضافه `AM-020`»** بأربعة بنود — ★ **ومنها جدولُ نصوص الأثر التلقائي في الشاشات الأربع وأدلّةُ أن الغياب كان سهواً** |
| [`amendment-log.md`](amendment-log.md) | **بند `AM-020`** كاملاً بنصّ الطلب حرفياً وبأدلّته |
| [`implementation-plan.md`](implementation-plan.md) | **قسمُ «جولةُ `AM-020`»** تحت `WU-002` بفحوصه |
| [`implementation-lessons.md`](implementation-lessons.md) | **درسان:** الابتلاعُ في طبقتين · والحرسُ المكرَّر يُقاس بدالة |

⛔⛔★★★ **ولا `CLAUDE.md`** — ★ **لم تتغيّر حقيقةٌ ثابتة**: ⛔ **لا منهجيةَ ولا
مرحلةَ ولا أمرَ بناءٍ ولا قرارٌ معماري.**

---

## 3. الملفاتُ البرمجية المعدَّلة

| الملف | التغيير |
|---|---|
| `lib/capabilities/master_data/presentation/master_data_widgets.dart` | ★ `masterDataFormBlocked` · `MasterDataSubmitButton` · `ContactPickerFailureBanner` |
| `…/presentation/sources_screen.dart` · `suppliers_screen.dart` · `dealers_screen.dart` · `items_screen.dart` | ★ **الزرُّ المشترك** · ★ **الشريطُ في اثنتين** · ★ **المدخلُ في المقاوته** · ★ **`inline` في الأنواع** · ⚙️ **ونصُّ الأثر في الرعية** |
| `…/infrastructure/contact_picker.dart` | ⛔ **لا ابتلاع** — **و`null` للإلغاء وحدَه** |
| `android/app/src/main/kotlin/dev/orimind/qtms/MainActivity.kt` | ★ `result.error(ERR_NO_CONTACT_PICKER, …)` |
| `test/capabilities/master_data/presentation/master_data_screens_test.dart` | ★ **+23 اختباراً** في خمس مجموعات |
| `test/support/fake_master_data.dart` | ★ **`FakeContactPicker` يرمي استثناءً** |

---

## 4. الفحوص الفعلية — بمخرَجها

| الفحص | المخرَج | النتيجة |
|---|---|---|
| `flutter analyze` | `No issues found!` | ✅ |
| `flutter test` | **868 · `All tests passed!`** | ✅ |
| `dart analyze` (النطاق) | `No issues found!` | ✅ |
| `dart test` (النطاق) | **868 · `All tests passed!`** | ✅ |
| `dart analyze` (السحابة) | `No issues found!` | ✅ |
| `dart test` (السحابة) | **820 · `All tests passed!`** | ✅ |
| `npm test` (قواعد الحماية) | **`# tests 264` · `# pass 264` · `# fail 0`** | ✅ |
| ★ **المجموع** | **2820 اختباراً آلياً · ٠ فاشلاً** | ✅ |
| ★★ **اختبارا طفرة** | ① **بإرجاع `false` قسراً من `masterDataFormBlocked` سقطت 5 اختباراتٍ فعلاً** · ② **وبإخفاء نصّ الرعوي قسراً سقط اختبارُه فعلاً** — ثم أُعيد الاثنان | ✅ |
| ⛅★★ **المحاكي `Pixel_6_API_36`** | **8 سيناريوهات نجاح + 1 خطأ — الكلُّ نجح** (التفصيل في `implementation-plan.md`) | ✅ |

⛔ **و`firestore.rules` لم تُمَسّ بحرف** — مقيسٌ لا مُدَّعى.

---

## 5. الوحداتُ وإعادةُ التصنيف

⛔⛔★★★ **ولا وحدةَ نزلت إلى `[يحتاج إعادة تحقق]`** — ★ **بنفس منطق `AM-014`
… `AM-019` حرفياً:** ⟵ **المستندان الحاكمان تغيّرا، والبوابةُ أُعيدت كاملةً
في التشغيل نفسِه بمخرَجٍ فعلي**، ⛔ **ثم أُعيد تسجيلُ البصمات** — ★ **لا
لإسكاتِ راية، بل لأن الأساسَ قِيس بعد تغيّره.**

`WU-002` هي الوحدةُ المعنيّةُ بالبنود الخمسة · و`WU-017` وجهةُ المدخل الجديد
⛔ **بلا سطرِ كودٍ فيها.**

---

## 6. ما ينتظر المالك

⛔⛔★★★ **ولا بوابةَ حَوْكمةٍ ولا بندَ مُصعَّد** — ★ **لا `ADR` مُسّ، ولا
`srs.md`، ولا `uat-signoff.md`**: ⟵ **البنودُ الخمسةُ كلُّها في طبقة العرض**،
★ **وقواعدُ العمل المقابلةُ قائمةٌ بحرفها** ⛔ **لم يُخفَّف منها شرطٌ واحد.**
★★ **والبندُ ⑤ يعرض متطلَّباً قائماً** (`FR-M3-04`) ⛔ **ولا يُنشئ سلوكاً
جديداً** — ⟹ **فلا `CR` ولا `ADR`.**

⚠️★★ **و`WU-002` وحدةٌ حسّاسة أمنياً** (`**يلمس نطاقاً حسّاساً:** نعم`) —
★ **فتحتاج مراجعةً بشريةً قبل أي نشرٍ إنتاجي كما هي منذ إنشائها**، ⛔ **والنشرُ
الإنتاجيُّ موقوفٌ حتى كلمة `اعتمد-النشر-الإنتاجي`** (`AM-010`).
