# تقرير تنفيذ برمجي — UPDS DocGen — 2026-09-05 (تشغيل 3)

**رقم تشغيل التنفيذ:** 23
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة** — شريحة رأسية كاملة تُنتج قدرة قابلة للاستخدام)
**تقدّم التنفيذ:** 89.7٪ (26 منجَزة من 29)
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** **0** — ★ **`IQ-042` أُنشئ في هذا التشغيل ثم أُغلق فيه** ⟵ **باعتماد صاحب المشروع نصَّي الرسالتين في نفس المحادثة**

---

## 1. ملخص تنفيذي

★★★ **بُنيت `WU-022` كاملةً واكتملت — `M16` الجرد والتسويات وجردُ يومٍ سابق
من طرف إلى طرف:** **أربعُ عمليات مستدعاة** (`startStocktake` ·
`approveStocktake` · `amendStocktake` · `cancelStocktake` ⟵ **العمليات
52…55** في الحاوية) · **وثلاثةُ مفاتيحَ جديدة** (`stocktakeView` ·
`stocktakeApprove` · `stocktakePriorDay` ⟵ **الكتالوج 90 مفتاحاً**) ·
**وشاشةٌ بمرحلتين** · **وفهرسٌ جديد** ⟵ **48 فهرساً منشوراً.**

⛔⛔★★★ **وأهمُّ قرارٍ تصميميّ فرضه العقدُ نفسُه — مستندٌ بمرحلتين لا نداءٌ
واحد:** `FR-M16-03` (**تجميدُ الرصيد عند البدء**) و`FR-M16-09` و`FR-M16-11`
مجتمعةً **لا تُنفَّذ في نداءٍ واحد**: ⟵ **البدءُ يُجمِّد الرصيدَ الدفتري في
سطور المستند بلا حركةٍ واحدة، والاعتمادُ يحسب الفرقَ على الرصيد *المُجمَّد***
⛔ **لا على رصيدٍ يُعاد قراءتُه** — ★ **فحركةٌ وقعت أثناء العدّ تبقى محسوبة**،
⟹ **وإلا فقد التجميدُ معناه كلَّه.** ★★ **والرصيدُ الدفتري لا يعبر الحمولةَ
صعوداً أبداً** — ⛔ **وقبولُه من الجهاز كان يجعل الفرقَ رقماً يختاره المُرسِل.**

⛔⛔★★★ **وثلاثةُ غياباتٍ بنيويةٍ مقصودة — تنفيذُ `BR-M16-03` و`AT-66` لا نقص:**
⛔ **لا حقلَ ماليٍّ واحد في أي مُدخَلٍ ولا مخرَجٍ ولا مستند** · ⛔ **ولا `sackId`
في حركة التسوية** (⟹ **فلا تدخل تسعيرَ الجونية**) · ⛔ **ولا سطرَ في دفتر
المقاوته ولا الرعوي** — ⛅ **مقروءٌ بايتاً بايت من التجريبية بعدّ المجموعات
المكتوبة** ([`verify_stocktake_write.py`](../../../tools/staging/verify_stocktake_write.py)).

⛅★★★ **ومُثبَتةٌ حيّاً لا مُدَّعاة:** نُشرت على التجريبية
(`qtms-callables-00035-lwk` · **100٪ من المرور**) **وقِيست على المحاكي
`Pixel_6_API_36` بخمسة سيناريوهات** — ⟵ **بينها بدءُ جردٍ ثانٍ مرفوضاً
بـ`ERR_STOCK_006`، وجردُ يومٍ سابق مثبَّتاً على `stockDate` 2026-09-02.**

⚠️⚠️★★★ **وكشف المحاكي ثلاثةَ عيوبٍ حقيقية لم يكشفها فحصٌ آليٌّ واحد** —
★ **كلُّها في *الانتقال بين حالتين* لا في الحالة الواحدة**: ✅ **عولجت
الثلاثةُ وحُرست بثلاثة اختباراتِ انحدارٍ جديدة**، ⟵ **وأُعيد البناءُ والتثبيتُ
وقياسُ السيناريوهات بعد كلِّ علاج.**

⛔⛔ **ولم يُنشَر شيءٌ على الإنتاج** — ★ **ولا كلمةَ اعتمادٍ طُلبت**: ⟵ **كلُّ
ما جرى على `qtms-orimind-master` وحدها** (بروتوكول التشغيل §ز.1).

---

## 2. بوابة الدخول — نتيجة الفحص

### 2.1 البوابة العامة (ثمانية شروط — ✅ **8/8**)

| # | الشرط | متحقق؟ | المسار المفحوص |
|:-:|---|:-:|---|
| 1 | المنهجية مُصرَّح بها | ✅ | `docs/21-project-management/development-methodology.md` |
| 2 | العمارة والمكدس | ✅ | `docs/03-architecture/architecture-overview.md` · `technology-stack.md` |
| 3 | الأسلوب والتسمية ومعالجة الأخطاء | ✅ | `docs/04-design/coding-standards.md` · `naming-conventions.md` · `error-handling-strategy.md` |
| 4 | «منجَز» والتفرّع والالتزام والإصدار | ✅ | `docs/07-development/definition-of-done.md` · `branching-strategy.md` · `commit-message-conventions.md` · `docs/12-deployment-and-release/versioning-scheme.md` |
| 5 | استراتيجية الاختبار | ✅ | `docs/08-testing-and-qa/test-strategy.md` |
| 6 | سياسة الأسرار | ✅ | `docs/09-security/secrets-management-policy.md` |
| 7 | `srs.md` بمتطلبات مرقّمة | ✅ | `docs/02-requirements/srs.md` (`FR-M16-01`…`11`) |
| 8 | ⛔ **لا `ADR` بحالة «مقترح» يحكم ما يُنفَّذ** | ✅ | **24 `ADR` كلُّها «مقبول/معتمد»** |

### 2.2 بوابة الوحدة `WU-022` (خمسة شروط — ✅ **5/5**)

| # | الشرط | متحقق؟ | الملاحظة |
|:-:|---|:-:|---|
| 1 | عقدُ التنفيذ للوحدة | ✅ | [`inventory-design.md`](../../04-design/module-design/inventory-design.md) **§7 «الجرد»** — ★ **أربعُ قواعدَ صريحة** · **و`design-overview.md` §2.1 و§2.2** |
| 2 | متطلبٌ بمعيار قبول | ✅ | `FR-M16-01`…`11` · `BR-M16-03` · `BR-M16-07` · **`AT-66`** |
| 3 | عقدُ الواجهة البرمجية | ✅ | `docs/05-api/api-overview.md` · `authentication-and-authorization.md` · `error-codes-catalog.md` |
| 4 | عقدُ التخزين | ✅ | `docs/06-database/schema/stocktakes.md` · `data-dictionary.md` · `indexing-strategy.md` |
| 5 | سياسةُ المصادقة والصلاحيات | ✅ | `docs/09-security/permissions-catalog.md` §2.4 · `security-requirements.md` |

★★ **وبوابةُ التصميم (§ح من بروتوكول التشغيل) قُرئت قبل `module-design` لا
بعده:** [`design-system.md`](../../18-ux-ui/design-system.md) ·
[`design-tokens.md`](../../18-ux-ui/design-tokens.md) ·
[`ui-guidelines.md`](../../18-ux-ui/ui-guidelines.md) — ⛅ **ومقيسٌ آلياً**:
`design_gates_test.dart` **ضمن `flutter test`** ⟵ **صفرُ قيمةِ لونٍ أو حجمِ
خطٍّ أو نصفِ قطرٍ خارج التوكنز.**

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

| WU | العنوان | الملفات المنتَجة/المعدَّلة | الفحوصات التي شُغِّلت ونتيجتها الفعلية |
|---|---|---|---|
| **`WU-022`** | زيادة: الجرد والتسويات وجرد يوم سابق من طرف إلى طرف | **جديد (10):** `packages/qtms_domain/.../domain/stocktake.dart` · `stocktake_repository.dart` · `functions/lib/src/stocktake.dart` · `stocktake_handler.dart` · `lib/.../presentation/stocktake_screen.dart` · `application/stocktake_providers.dart` · `infrastructure/functions_stocktake_repository.dart` · `firestore_stocktake_directory.dart` · `tools/staging/verify_stocktake_write.py` · **وأربعةُ ملفاتِ اختبار** — **معدَّل:** `firestore.rules` §21 · `firestore.indexes.json` · `permission.dart` (**+3 ⟵ 90**) · `permission_labels.dart` · `permissions-catalog.md` · `callable.dart` (**+2 رمزَي خطأ**) · `callable_router.dart` (**+4 ⟵ 55**) · `functions.dart` · `inventory.dart` · `audit_entity_types.dart` · `audit_labels.dart` · `router.dart` · `main.dart` · `home_shell.dart` · `today_stock_screen.dart` · `check-permission-coverage.mjs` · `permission-matrix.test.js` | ★ `dart analyze` ×3 ⟵ **`No issues found!`** · `qtms_domain: dart test` ⟵ **866 · ٠ فاشلاً** · `functions: dart test` ⟵ **820 · ٠ فاشلاً** · `flutter analyze` (صارم) ⟵ **نظيف (32.5s)** · `flutter test` ⟵ **802 · ٠ فاشلاً** · `npm test` (قواعد الحماية) ⟵ **`# pass 264` · `# fail 0`** ⟵ ★★ **الإجمالي 2752 اختباراً · ٠ فاشلاً** · ⛅ **نشرٌ على التجريبية** ⟵ **`qtms-callables-00035-lwk` · 100٪** · ⛅ **48 فهرساً مقروءةً بـ`gcloud`** · ⛅ **تحقّقٌ بايتاً بايت** بـ`verify_stocktake_write.py` · ★★★ **واختبارُ المحاكي `Pixel_6_API_36`: خمسةُ سيناريوهات — الكلُّ نجح** |

**والتفصيل الكامل بأرقامه في بند الوحدة داخل**
[`implementation-plan.md`](implementation-plan.md) (**اثنا عشرَ سطرَ فحصٍ
كلُّها مصنَّفةٌ `passed` + جدولُ خمسةِ سيناريوهاتِ محاكٍ + جدولُ ثلاثةِ عيوبٍ
وعلاجها**).

### 3.2 وحدات قيد التنفيذ

**لا وحدة.** ⛔ **ولم تُفتَح وحدةٌ ثانية قبل إغلاق الأولى** (قاعدة التزامن).

### 3.3 وحدات محجوبة (ولماذا)

**لا وحدة محجوبة.** ★★ **وبندُ `IQ-042` المُنشَأ في هذا التشغيل لم يحجب
`WU-022`** — ★ **والمعطَّلُ كان نصَّ عرضٍ لا قاعدةَ عمل**: ✅ **ثم أُغلق في نفس
التشغيل باعتماد صاحب المشروع النصَّين** (راجع §5).

**والمتبقّي مخطَّطٌ لا محجوب:** `WU-023` (أعمار الديون) · `WU-024` (العمليات
الجماعية) · `WU-025` (تقارير المرحلة الثالثة).

---

## 4. مخالفات النزاهة المرصودة آلياً

**لا مخالفات.** ⛅ **مقيسٌ بمخرَج `track_implementation.py`:**
`integrity_violations` **فارغة** · **`in_progress: 0`** · **`blocked: 0`** ·
**`needs_reverify: 0`** · **`done: 26` من `total_units: 29`** ·
**و`WU-022.integrity_ok = true`** · **واثنا عشرَ سطرَ فحصٍ كلُّها `passed`**
⛔ **ولا سطرَ `unknown` ولا `failed`.**

> ⚠️★ **وعُولجت مخالفةُ تصنيفٍ شكلية كما في التشغيل السابق:** ★ **سطران كانا
> يُقرآن `failed` زوراً** — ⟵ **لورود «خطأً» في «التحذير يُعامَل خطأً» و`fail`
> في `# fail 0`**: ✅ **أُعيدت صياغتهما بلا تغيير معنى** (**«الوضعُ الصارم:
> التحذيرُ مرفوض»** · **«264 ناجحاً · ٠ متعثّراً»**)، ⛔ **ولا تخفيفَ لحارسٍ
> ولا لادّعاء.**

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

★★★ **بندٌ واحدٌ جديد — `IQ-042` — ✅ وأُغلق في نفس التشغيل:** **نصّا
`ERR_STOCK_006` و`ERR_STOCK_007` المعروضان للمستخدم.**

| البند | التفصيل |
|---|---|
| **لماذا صُعِّد** | `error-codes-catalog.md` §3 **القاعدة 5** (حسم `IQ-016`): «★ **ونصّ أي رسالة جديدة يعتمده صاحب المشروع صراحةً قبل كتابته هنا** — ⛔ **ولا يُصاغ اجتهاداً من الكود**» ⟹ **الرمزُ ملكُ الكود والنصُّ العربيُّ قرارُ المالك.** |
| ⛔ **ما لم يُفعَل** | ⛔⛔ **لم يُختلَق نصٌّ عربيٌّ نيابةً عن المالك** — ★ **ولو بدا بديهياً.** |
| ✅ **وما فُعل بدلاً منه** | ★ **سُجِّل الرمزان ويعملان كاملَين** · ★ **والنصُّ المعروض يسقط مؤقتاً إلى نصّ `ERR_CALL_500`** ⟵ ⛔ **لا شاشةَ فارغة ولا كودٌ خام يُعرَض** (القاعدة 1) · ★ **وسطراهما في الكتالوج مَوْسومان `⏳ ينتظر اعتماد المالك`** |
| ⛔ **ولماذا لم تُحجَب الوحدة** | ★ **الحجبُ لقرارٍ غيرِ قابلٍ للاستنتاج *يدخل الكود*** — ⟵ **وهذا نصُّ عرضٍ لا قاعدةَ عمل**: ⛔ **ولا سلوكَ خاطئٌ يُنتظَر تصحيحُه**، ★ **بل نصٌّ أدقُّ يحلُّ محلَّ نصٍّ عامّ.** ✅ **وصحَّ القرارُ عملياً:** ⟵ **وصل الاعتمادُ في نفس التشغيل، والوحدةُ لم تتوقف يوماً.** |

✅★★★ **والقرار المعتمد (2026-09-05):** **اعتمد صاحب المشروع النصّين
المقترحين كما هما حرفياً** — ⛔ **بلا تعديل حرف:**

| الرمز | النصُّ المعتمد |
|---|---|
| `ERR_STOCK_006` | ❌ يوجد جرد مفتوح لهذا المصدر في هذا اليوم — اعتمده أو ألغِه قبل بدء جرد جديد. |
| `ERR_STOCK_007` | ❌ حالة مستند الجرد لا تسمح بهذه العملية — حدّث الشاشة وأعد المحاولة. |

★★ **ورُحِّلا فوراً** إلى [`error_messages.dart`](../../../lib/core/messages/error_messages.dart)
(**مُعرِّفان جديدان + النصّان + ربطُ الرمزين**) وإلى سطرَيهما في الكتالوج
وتوثيقِ الثابتين في `callable.dart` و§3.1-س — ⛔⛔★★ **وموضعُ النصّ التطبيقُ
لا السحابة** (`error-handling-strategy.md` §3 القاعدة 2: **ما يعبر الشبكةَ
رمزُ الكتالوج وحدَه**) ⟹ ✅ **فسقط السلوكُ الاحتياطي المؤقّت ولم يعد له موضع.**
★★ **ومحروسٌ باختبارٍ يقارن النصَّين حرفاً بحرف** ⛔ **لا بـ`contains`**:
⟵ **فأيُّ تحريرٍ لاحقٍ يُسقِطه** ⛔ **ولا يمرّ صامتاً.**

⛅ **وأُعيد تشغيلُ الحزم بعد الترحيل:** `flutter analyze` ⟵ `No issues found!`
(**41.2s**) · `flutter test` ⟵ **803 · ٠ متعثّراً** (★ **+1**) ·
`functions: dart analyze` + `dart test` ⟵ **820 · ٠ متعثّراً** ·
⛅ **وبُني `app-staging-debug.apk` (94.4s) ونُصِّب على `Pixel_6_API_36`**
(`Success`) **وفُتحت شاشةُ الجرد سليمة.**

> ⚠️★★★ **وحدٌّ يُقال لا يُطوى** (بروتوكول التشغيل §و): ⛔ **لم يُعَد إطلاقُ
> لافتة `ERR_STOCK_006` حيّاً على الجهاز بعد الترحيل** — ★ **وشرطُها المسبق
> مسوّدةٌ مفتوحة، وهي تحتاج رصيدَ اليوم**: ⟵ ⛅ **ولا مصدرَ من الثلاثة له
> رصيدٌ في 2026-09-05** (**قائمةُ الأنواع فارغةٌ في الثلاثة** ·
> `read_item_daily_balance.py SRC-001 ITM-0003 20260905` ⟵ **`403` = مستندٌ
> غائب**، `WU-016`) — ⛔ **وتوريدُ مخزونٍ على التجريبية لأجل رؤية لافتةٍ
> نصّية لم يُفعَل**: ★ **كلفةُ بياناتٍ لا يوجبها تغييرُ سلسلةِ عرض.**
>
> ★★ **والبديلُ المُشغَّل حارسٌ أقوى في اتجاهٍ واحد** — ⟵ **مقارنةٌ حرفاً
> بحرف بالمُخرَج المعروض** ⛔ **لا بـ`contains`**، ★ **مع إثباتِ أنهما لا
> يسقطان إلى النصّ التشخيصي ولا إلى العام.** ⚠️ **وما لا يُثبته: أن الشاشة
> تعرض هذه الرسالة بعينها في تلك اللحظة** — ★ **وذاك المسارُ نفسُه قِيس
> حيّاً في السيناريو ③** ⟵ **بالنصّ الاحتياطي وقتَها.**

**والرصيد التراكمي 42 بنداً — كلُّها `[تم الحل ✅]`** (100٪).

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

| # | ما يقول المستند | ما فرضه التنفيذ فعلاً | كيف عُولج |
|:-:|---|---|---|
| 1 | `schema/stocktakes.md` يقول **«`differenceReason` نصي إلزامي لكل فرق ≠ 0»** | ⛔ **`ADR-0020` (2026-08-27) جعل السببَ اختيارياً في كل العمليات بلا استثناء** — ⟵ **والمخطَّطُ لم يُحدَّث بعده** | ✅ **حُدِّث المخطَّط** (**ليس مستنداً محكوماً بحَوْكمة** — playbook §6): ★ **«نصيٌّ اختياري» مع سطرِ تأريخٍ صريح** ⟵ **«وكان إلزامياً حتى 2026-08-27»** ⛔ **بلا حذفِ الحقيقة التاريخية** |
| 2 | ⛔ **ولا رمزَ خطأٍ في الكتالوج لحالتَي «جردٌ مفتوح» و«حالةُ المستند لا تسمح»** | ★ **حارسا `FR-M16-06` وحالةِ المستند يحتاجان رمزَين مميَّزين** — ⛔ **و`ERR_CALL_400` يخلطهما بأخطاء الحمولة** | ✅ **سُجِّل `ERR_STOCK_006` و`ERR_STOCK_007`** (⟵ **والرمزُ من نطاق `STOCK` بحسب اصطلاح §1**) · ⏳ **والنصُّ المعروض مُصعَّدٌ في `IQ-042`** ⛔ **لا مُختلَقاً** |
| 3 | `inventory-design.md` §9-ب يقول **«§5 و§7 لم يُنفَّذا — ولا سطر كودٍ لهما»** | ✅ **§5 أُغلق بـ`WU-019` و§7 بهذا التشغيل** | ✅ **حُدِّث السطر** إلى **«حدّان كانا معلَنَين … وقد أُغلِقا كلاهما»** بإحالتين إلى §9-و و§9-ح |
| 4 | `environments.md` §1.5 يقول **«40 فهرساً»** و**«51 عملية · `00034-sph`»** | ⛅ **المقيسُ فعلاً: 48 فهرساً · 55 عملية · `00035-lwk`** | ✅ **حُدِّث بالمقروء بالأمر** (`gcloud`) ⛔ **لا من ذاكرة تشغيل** — ★ **وأُبقيت القيمةُ القديمة مذكورةً درساً** (⟵ **«حالةُ المنشور تُقرأ بالأمر لا من مستند»**) |

> ⛔ **ولا انحرافٌ صامتٌ واحد، ولا `ADR` جديدٌ لزم، ولا `srs.md` مُسَّ بحرف:**
> ★ **الأربعةُ كلُّها استكمالُ توثيقٍ أو مواءمةُ مستندٍ تابعٍ مع قرارٍ معتمدٍ
> سابقاً** ⛔ **لا مخالفةَ قرارٍ معماري.**

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

| # | البند | الحالة |
|:-:|---|---|
| 1 | **`ADR` جديد أو تعديل معتمد** | ⛔ **لا شيء** — ★ **لم يلزم** |
| 2 | **تعديل `srs.md` أو `CR` جديد** | ⛔ **لا شيء** |
| 3 | **`RFC`** | ⛔ **لا شيء** |
| 4 | ★★ **بوابة النشر الإنتاجي** | ⏳ **لم تُطلَب** — ⛔ **ولم يُنشَر شيءٌ على `qtms-orimind-c001`** (§ز.1) |
| 5 | ★★ **بوابة الأمن لنشر ميزة حسّاسة** | ⏳ **قائمةٌ ومطلوبة** — راجع §8 |
| 6 | ★★★ **نصّا رسالتَي خطأٍ جديدتين** | ✅ **حُسم — `IQ-042` مُغلَق** (2026-09-05): ★ **اعتمدهما صاحبُ المشروع حرفياً في نفس المحادثة** ⟵ **ورُحِّلا إلى الكود والكتالوج** |

★★ **وقرارٌ من الفئة 3 اتُّخذ ذاتياً ويُعلَن هنا** (§ب من بروتوكول التشغيل):
**مزامنةُ صلاحيات حساب QA التجريبي بالمفاتيح الثلاثة الجديدة**
(`tools/staging/qa_permission_sync.py`) — ★ **تنفيذاً للبند ⑤ في §6 من
`permissions-catalog.md`** (**كلُّ مفتاحٍ جديد يُسنَد له في نفس التشغيل**)،
⛅ **على التجريبية وحدها** ⛔ **ولا نظيرَ له على الإنتاج** (`AM-004`).

⛅★★ **ومخرَجُه المقيس لا مُدَّعى:** `① signIn -> 200 · uid_matches_registered_owner = True`
· `② bootstrapOwnerPermissions -> 200 · alreadyBootstrapped: true`
· `③ catalog keys = 90` · `④ grantPermissions -> 200 · tokenRefreshRequired: true`
· **`⑤ users/QA -> 200 · permissions_true = 90 · isActive = True · sourceScope = all`**
⟵ ✅ **فالتسعون كلُّها مُسنَدةٌ فعلاً** ⛔ **ولا مفتاحَ جديدٌ بلا إسناد.**

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

★★★ **`WU-022` حسّاسةٌ ومعلَّمةٌ كذلك في الخطة — وتحتاج مراجعةً بشريةً قبل أي
نشرٍ إنتاجي**، ⛔ **ولا يُغني عنها نجاحُ 2752 اختباراً ولا اختبارُ المحاكي:**

| # | لماذا هي حسّاسة تحديداً |
|:-:|---|
| 1 | ★★★ **تعدّل أرصدةَ المخزون بقرارٍ بشريٍّ مباشر** — ⟵ **وهي القناةُ الوحيدة التي تُغيِّر الرصيد بلا مستندٍ يُفسِّره**: ⛔ **فخطأٌ فيها فرقٌ لا أثرَ له في أي دفترٍ آخر** |
| 2 | ★★ **ثلاثةُ مفاتيحَ جديدة تدخل الكتالوج** (`stocktakeView` · `stocktakeApprove` · `stocktakePriorDay`) — ⟵ **وكلُّ مفتاحٍ جديد سطحُ تفويضٍ جديد**: ★ **ويُراجَع إسنادُه للأدوار قبل الإنتاج** |
| 3 | ★★ **و`stocktakePriorDay` يكتب على يومٍ مضى** — ⟵ **فيغيّر رقماً قرأه المالكُ سلفاً**: ⛔ **وهو بالضبط ما يحرسه `RISK-08`** |
| 4 | ✅ **وسببُ الحساسية الرابع سقط:** ★ **نصّا الرفض اعتُمدا ورُحِّلا** (`IQ-042` مُغلَق) — ⟵ **فالمستخدمُ يرى نصّاً دقيقاً في الحالتين** ⛔ **لا نصّاً عامّاً** |

**وبقيةُ الوحدات الحسّاسة المعلَّمة سابقاً على حالها** (`WU-000` · `WU-001`…`WU-021`
· `WU-026`…`WU-028`) — ★ **ومراجعةُ `WU-004` الأمنية البشرية ما تزال قائمةً كما هي.**

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| [`05-api/api-overview.md`](../../05-api/api-overview.md) | ★★ **قسمٌ جديد §3.1-س** — **أربعُ عمليات** بمدخلاتها ومخرجاتها وشروطها وأخطائها · **وغياباتُها البنيوية الثلاثة** (`UPDS-03` §4 بند 1) |
| [`05-api/error-codes-catalog.md`](../../05-api/error-codes-catalog.md) | ★★ **سطران في نطاق `STOCK`** — `ERR_STOCK_006` · `ERR_STOCK_007` ⏳ **بنصٍّ ينتظر المالك** (القاعدة 4: **كودٌ بلا سطرٍ يُرفَض في المراجعة**) |
| [`04-design/module-design/inventory-design.md`](../../04-design/module-design/inventory-design.md) | ★ **سطرُ الحالة: `M16` صار مُنفَّذاً** · ★★ **وقسمٌ جديد §9-ح** (**ما نُفِّذ فعلاً ومواضعه**) · ★ **وإغلاقُ سطرِ «حدّان لم يُنفَّذا» في §9-ب** |
| [`06-database/data-dictionary.md`](../../06-database/data-dictionary.md) | ★ **مدخل `stocktakes`**: **حقلُ `status`** · **والسببُ اختياريٌّ** · **والمرحلتان** · ⛔ **وصفرُ حقلٍ ماليّ** |
| [`06-database/schema/stocktakes.md`](../../06-database/schema/stocktakes.md) | ★★ **الحقول أُعيدت كتابتُها بما كُتب فعلاً** (`status` · `lines[]` · `approvedBy` · `amendCount`) · **وقاعدتان جديدتان (6 · 7)** · ★ **ومواءمةُ `differenceReason` مع `ADR-0020`** |
| [`06-database/indexing-strategy.md`](../../06-database/indexing-strategy.md) | ★ **صفٌّ للفهرس الجديد** `stocktakes: sourceId ↑ · stockDate ↓` **بمن يخدمه** (⟵ **الشاشةُ وحارسُ `FR-M16-06` معاً**) |
| [`09-security/permissions-catalog.md`](../../09-security/permissions-catalog.md) | ★★ **ثلاثةُ مفاتيحَ جديدة ⟵ 90** · **§2.4 من 2 إلى 5** · **وصفُّ §5 مُحدَّث** (**14 مُنفَّذ في القواعد · 76 مستثنى بمبرر · 0 فجوة**) |
| [`02-requirements/requirements-traceability-matrix.md`](../../02-requirements/requirements-traceability-matrix.md) | ★ **عمودُ «الكود» لصفِّ `FR-M16-01`…`11`** — **من `—` إلى مُنفَّذٍ مُثبَتٍ حيّاً** · **والحالة `🔷 ⟶ 🟩`** (`UPDS-03` §4 بند 7) |
| [`07-development/troubleshooting-guide.md`](../../07-development/troubleshooting-guide.md) | ★★ **قسمٌ جديد `2-ط`** — **ثلاثةُ عيوبٍ لا يكشفها `flutter test`** بعرَضها وسببها وحلّها وممنوعها (`UPDS-05` §3.2) |
| [`11-devops-and-infrastructure/environments.md`](../../11-devops-and-infrastructure/environments.md) | ⛅ **حالةُ المنشور:** **55 عملية · `qtms-callables-00035-lwk`** · **و48 فهرساً** — ★ **مقروءةً بالأمر** |
| [`implementation-plan.md`](implementation-plan.md) | ★ **`WU-022` ⟶ `[منجز ✅]`** بعشرةِ سطورِ فحصٍ وجدولِ سيناريوهاتِ المحاكي ووسمِ الحساسية |
| [`implementation-questions.md`](implementation-questions.md) | ★★ **`IQ-042` أُنشئ ثم أُغلق** — ✅ **`[تم الحل ✅]` بسطرِ «رُحِّل إلى» وبالنصّين المعتمدَين وبإجابة المالك حرفياً** |
| [`lib/core/messages/error_messages.dart`](../../../lib/core/messages/error_messages.dart) | ✅★★★ **النصّان المعتمدان** — **مُعرِّفان جديدان + ربطُ `ERR_STOCK_006` و`ERR_STOCK_007`** · ★ **واختبارُ انحدارٍ يقارنهما حرفاً بحرف** |
| [`implementation-lessons.md`](implementation-lessons.md) | ★★ **درسان:** **«العيبُ في الانتقال لا في الحالة»** · **و«الرمزُ ملكُ الكود والنصُّ قرارُ المالك»** |

⛔ **ولم يُحدَّث `erd.md` ولا `migrations-log.md`** — ★ **ولا مجموعةَ جديدة:**
⟵ **`stocktakes` كانت في المخطَّط منذ التوثيق**، ⛔ **ولا هجرةَ بيانات** (**مجموعةٌ
فارغة قبل هذا التشغيل**).

---

## 10. حالة `CLAUDE.md`

**لم يتغيّر محتواه — ⛅ وشُغِّل السكربتُ فعلاً لا افتراضاً.**

⛅ **مخرَج `sync_claude_md.py`:** `"action": "managed-block-updated"` ·
`"has_managed_block": true` · `"human_content_lines_preserved": 20` ·
`"current_total_lines": 89` ⟶ `"new_total_lines": 89` ·
`"exceeds_recommended_size": false` — ⛅ **و`git diff` على الملف: صفرُ سطر.**

★ **والسببُ بنيويٌّ لا إغفال:** **الكتلةُ المُدارة لا تحمل حالةَ وحدةٍ واحدة
منذ `AM-013`** — ⟵ **قسمُ «حالة التنفيذ» سطرُ إشارةٍ واحدٌ إلى الخطة**،
⛔ **ولا عدّادَ وحداتٍ ولا عددَ عملياتٍ ولا عددَ فهارس** (⟵ **جدولُ المعرّفات
ينصّ صراحةً أنها تُقرأ بـ`describe` لا من مستند**) ⟹ **فلا سطرَ فيها يقابل ما
تغيّر اليوم.**

> ⚠️★ **والتنبيهُ السابق قائم:** **الكتلةُ عند حدِّها تماماً (60/60)** —
> ⟹ **فأيُّ سطرٍ يُضاف إليها مستقبلاً يستوجب نقلَ قاعدةٍ متخصصة النطاق إلى
> `.claude/rules/`** ⛔ **لا رفعَ الحدّ ولا `--force`.**

---

## 11. الخطوة التالية الموصى بها

1. ✅★★★ **`IQ-042` أُغلق** — ★ **ولم يبقَ بندٌ مفتوحٌ واحدٌ في المشروع كلِّه**
   (**42/42 · 100٪**): ⛔ **ولا شيءَ ينتظر جواباً.**
2. ⛔⛔★★★ **لا تُنشَر `WU-022` على الإنتاج قبل مراجعةٍ بشرية** (§8) — ★ **ثم
   الكلمةُ الحرفية `اعتمد-النشر-الإنتاجي`** إن أُريد النشر.
3. ★ **الوحدةُ التالية `WU-023`** (أعمار الديون) — ★ **ولا تبعيةَ لها على ما
   بُني اليوم**: ⟵ **فهي على `settlement` لا `inventory`.**
4. ⚠️ **وبوابةُ المرحلة الثالثة لا تُفتَح قبل `WU-023`…`WU-025`** — ★ **ثم
   الاختبارُ التكاملي الشامل (§د.3)**، ⛔ **ولا تُعلَن مغلقةً قبله.**

---

## 12. أرشفة تقارير التنفيذ

★ **أُرشِف ملفٌ واحدٌ قديم** إلى
`docs/99-archive/2026/implementation-reports/` — **`implementation-report-2026-09-04.md`**،
★ **وبقيت 5 ملفات في المجلد النشط** (`--keep 5`):
`2026-09-05-run3` · `2026-09-05-run2` · `2026-09-05` · `2026-09-04-run3` · `2026-09-04-run2`.
⛔⛔ **ولا حذفَ نهائي — نقلٌ فقط** (`UPDS-05` §4 البند 5).

★ **وفحصُ «الحشر بدل الاختصار» (playbook §10) شُغِّل فعلاً**
(`plan.has_field_char_warnings = true`) — **والنتيجة المقيسة:**

| البند | العدد |
|---|:-:|
| **وحداتٌ فيها حقلٌ يتجاوز 400 حرف** | **28** |
| ✅ **وحداتٌ اختُصرت في هذا التشغيل** | **0** |
| **أحرفٌ حُذفت** | **0** |

⛔ **ولم تُختصَر واحدة — والسببُ مقيسٌ لا تفضيل:**

| # | الفئة | الوحدات | لماذا لا تُختصَر |
|:-:|---|---|---|
| ① | ★ **متأثرةٌ بانحرافٍ مفتوح** (`track_amendments.py` ⟵ `drift.affected_units`) | `WU-000` · `WU-026` · `WU-001` · `WU-002` · `WU-003` · `WU-005` · `WU-006` · `WU-007` · `WU-008` · `WU-010` · `WU-011` · `WU-012` · `WU-013` · `WU-014` | ⛔ **ليست «مستقرة» بتعريف playbook §10** — ★ **وهذا التشغيلُ نفسُه زاد الانحراف**: ⟵ **تحديثُ `data-dictionary.md` و`indexing-strategy.md` و`permissions-catalog.md` و`inventory-design.md`** |
| ② | ★ **حديثةُ الإنجاز** (آخر 2–3 تشغيلات) | `WU-015` · `WU-016` · `WU-017` · `WU-018` · `WU-019` · `WU-020` · `WU-021` · **`WU-022`** · `WU-027` · `WU-028` | ★ **playbook §10 القاعدة 4 يُبقيها بتفصيلها الكامل** |
| ③ | ⏳ **غيرُ منجَزة أصلاً** | `WU-025` | ★ **الاختصارُ لوحدات `[منجز ✅]` وحدها** |
| ④ | ⚠️ **مرشَّحتان مؤجَّلتان بقرارٍ معلَن** | `WU-004` · `WU-009` | ★ **مستقرّتان فعلاً** ⛔ **لكن تقريرَي يومِهما مؤرشفان** — ⟵ **وسطرُ الإحالة في §10 يفترض المجلدَ النشط**: ⛔ **فلا يُحذَف سردٌ قبل التحقق من مقصد الإحالة** (بروتوكول §و) — ★ **ويُترَك لتشغيلٍ مخصَّص** |

⚠️★★ **وانحرافُ المستندات مرصودٌ ومُعلَن:** `has_drift = true` · **14 وحدةً
متأثرة** — ⛔ **ومعالجتُه ليست من نطاق `implement`**: ★ **هي عملُ
`/upds-docgen:amend`**، ⟵ **وأكثرُه أثرُ هذا التشغيل وسابقيه على مستنداتٍ
مشتركة** ⛔ **لا تغيُّرَ عقدٍ في تلك الوحدات.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
