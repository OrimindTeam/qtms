# `CR-007` — الحدُّ الأدنى للبيع النقدي: **منعٌ مطلق** ⛔ لا استثناءَ بصلاحية

| البند | القيمة |
|---|---|
| **الرقم** | `CR-007` |
| **العنوان** | إلغاءُ استثناء «البيع دون الحد الأدنى بصلاحية» — **والمنعُ للجميع** |
| **التاريخ** | 2026-09-01 |
| **مُقدِّم الطلب** | ★ **مالك المشروع** — ⛅ **بقرارٍ صريح في جلسة `WU-012`** |
| **الحالة** | ✅★★★ **معتمد** (2026-09-02) — ★ **بتفويضٍ صريحٍ من مالك المشروع في المحادثة نفسِها** (`UPDS-05` §4.6: **الاعتماد فعلٌ بشري حصري**) |
| **المتطلبات المتأثرة** | `FR-M11-05` · `FR-M11` §4 · `GR-34` |
| **الوحدة** | `M11` (`WU-012`) |

---

## 1. ما يقوله المتطلب المعتمد اليوم

`FR-M11-05` حرفياً:

> **السعر لا يقل عن سعر الحد الأدنى للبيع النقدي المسجَّل للنوع، إلا بصلاحية
> «البيع دون الحد الأدنى»** ★★ **وسببٍ نصي اختياري يُسجَّل في سجل التدقيق إن
> كُتب** (`ADR-0020`) — «`AT-27`: 600 والحد الأدنى 700 ⟵ **يُرفَض** إلا
> بصلاحية — ★ **والصلاحية وحدها هي الحارس**».

★ **ومعه مفتاحٌ في الكتالوج §2.3:** `cashSaleBelowMinimum` — «البيع دون الحد
الأدنى».

---

## 2. ما طُلب تغييره — **نصُّ قرار المالك**

> **«يُمنع الحفظ للجميع بلا استثناء»**

⟹ ⛔⛔ **يسقط الاستثناء كلُّه:** ★ **الحدُّ الأدنى يصير حدّاً فعلياً** —
⛔ **لا اقتراحاً يتجاوزه صاحبُ مفتاح.**

---

## 3. المبرِّر — **كما ظهر بالتشغيل الحيّ**

⛅★★ **رُصد على `Pixel_6_API_36` (2026-09-01):** ★ **سعرٌ 600 والحدُّ 700
حُفِظ فعلاً** — ⟵ **لأن حساب الاختبار (QA) يملك المفاتيح الـ81 ومنها
`cashSaleBelowMinimum`**، ★ **فالسلوك كان مطابقاً لـ`FR-M11-05` حرفياً.**

⛔⛔★★ **وحكمُ المالك أن هذا خطأٌ في القاعدة لا في التنفيذ:** ★ **حدٌّ أدنى
يُتجاوَز بضغطة ممن يملك مفتاحاً ليس حدّاً** — ⟵ **وأثرُه المالي مباشر:
كلُّ ريالٍ دون الحد خسارةٌ لا تُسترَدّ**، ⛔ **ولا يقابلها قيدٌ يُصحَّح.**

---

## 4. الأثر — **ما تغيّر فعلاً في الكود** (مُنفَّذ 2026-09-01)

| الموضع | قبل | بعد |
|---|---|---|
| [`functions/lib/src/cash_sale.dart`](../../../functions/lib/src/cash_sale.dart) — `_minimumPriceGate` | `if (actor.claims.has(cashSaleBelowMinimum)) return null;` **ثم الفحص** | ⛔ **الفحصُ وحده** — ★ **ولا قراءةَ للمفتاح إطلاقاً** |
| [`cash_sale_screen.dart`](../../../lib/capabilities/sales_receivables/presentation/cash_sale_screen.dart) | زرُّ الحفظ مُمكَّنٌ دائماً · **والرفضُ من السحابة بعد رحلةِ شبكة** | ★★ **الزرُّ مُعطَّلٌ ولافتةُ `ERR_PRICE_002` تظهر قبل الإرسال** — ⛔ **ولا طلبَ يُرسَل** |
| `functions/test/cash_sale_test.dart` | «✅ ومع المفتاح يمرّ» | ⛔ **«يُرفَض ولو ملك المُرسِل المفتاح»** |
| `cash_sale_screen_test.dart` | «✅ ومع المفتاح يُمكَّن الحفظ» | ⛔ **«يُعطَّل الحفظ ولو ملك المفتاح»** |

★ **وما لم يتغيّر:** ✅ **`FR-M11-06` كما هو** — **نوعٌ بلا حدٍّ مسجَّل يُباع
بتنبيه** ⛔ **ولا يقع تحت هذا المنع** · ✅ **والمساواةُ بالحد مقبولة**
(«لا يقل عن») · ⛔ **و`firestore.rules` لم تُمَسّ.**

---

## 5. ⚠️ بندٌ معلَّق يحتاج قرارك — **مصيرُ `cashSaleBelowMinimum`**

⛔⛔★★★ **المفتاح لم يعد يمنح شيئاً** — ★ **ولا يقرؤه أيُّ حارسٍ بعد اليوم.**
⟵ **وبقاؤه في الكتالوج §2.3 يجعله يَعِد بما لا يُعطيه** — ⛔ **وهو ما تمنعه
قاعدةُ الكتالوج نفسِها** (`BR-M1-07`: **لا مفتاح بلا مقابل**).

| الخيار | الأثر |
|---|---|
| **أ — يُحذَف من الكتالوج** | **81 ⟵ 80 مفتاحاً** · ⟵ **يمسّ `Permission` و`permission_labels` و`check-permission-coverage.mjs` وعدّادات ثلاثة اختبارات ومزامنةَ حساب QA و`CLAUDE.md`** |
| **ب — يبقى محجوزاً بوسمٍ صريح** | ★ **صفٌّ في §2.3 يقول «⛔ لا يمنح شيئاً بعد `CR-007`»** — ✅ **بلا مساسٍ بالعدّادات** · ⚠️ **ويبقى مفتاحٌ ميت** |
| **ج — يُعاد إحياؤه بسياسةٍ متدرّجة** | مثال: **يسمح بهامشٍ محدودٍ دون الحد** — ⛔ **ونسبتُه رقمٌ لا يذكره مستند** (نفس علّة `IQ-035` ②) |

✅★★★ **والقرار (2026-09-02): «الخيار ب» — يبقى محجوزاً بوسمٍ صريح.**

| البند | التفصيل |
|---|---|
| **ما يُنفَّذ** | ★ **صفٌّ في `permissions-catalog.md` §2.3 يقول: «⛔ لا يمنح شيئاً بعد `CR-007` — محجوزٌ ولا يُسنَد»** |
| ✅ **لماذا هو الأنسب مهنياً** | ⛔ **صفرُ مساسٍ بـ`Permission` و`permission_labels` و`check-permission-coverage.mjs` وقواعد الحماية** — ⟹ **ولا عدّادٌ يتغيّر (81 كما هو) ولا مزامنةُ حساب QA تلزم** · ★ **والحذفُ (الخيار أ) يمسّ أربعةَ مواضعَ ويُسقِط بوابةَ التغطية حتى تُزامَن كلُّها** ⛔ **بلا مكسبٍ وظيفيٍّ واحد** |
| ⛔ **ولماذا رُفض (ج)** | ★ **«هامشٌ محدودٌ دون الحد» يستلزم نسبةً رقمية** — ⛔ **ولا مصدرَ لها في أي مستند**: ⟹ **واختراعُها هو الاختلاقُ الممنوع** (playbook §5) |
| ★ **وحُجّةُ `BR-M1-07`** | ★ **«لا مفتاح بلا مقابل» تُستوفى بالوسم الصريح** — ⟵ **فالمفتاحُ لم يعد يَعِد بشيء: الكتالوجُ نفسُه يقول إنه لا يمنح** ⛔ **وهو أصدقُ من حذفٍ يمحو أثرَ القرار من السجل** |

⛅ **والأثرُ المقيس:** ★ **الكودُ اليوم لا يقرأ المفتاح فعلاً** (`cash_sale.dart`
· `cash_sale_handler.dart`) — ⛔ **والكتالوجُ يحتاج صفَّ الوسم وحدَه.**

---

## 6. التوقيع

| البند | القيمة |
|---|---|
| **قرار المالك** | ✅★★★ **معتمد** — ★ **بتفويضٍ صريحٍ من المالك في المحادثة** (2026-09-02): «**أوافق واعتمد ما ينتظر موافقتي**») |
| **التاريخ** | **2026-09-02** |

> ⛔⛔ **ولا يُعدَّل نصُّ `FR-M11-05` في** [`FR-M11.md`](../functional-requirements/FR-M11.md)
> **قبل توقيع هذا الطلب** — ★ **والمتطلب معتمدٌ بـ`srs.md`** (UPDS-05 §4 البند 3):
> ⟵ **والكودُ اليومَ يسبق النصَّ بقرارٍ شفهيٍّ موثَّق هنا**، ⛔ **وهذا الفارقُ
> يُغلَق بالتوقيع لا بالسكوت.**
