# البيئات — QTMS

| البند | القيمة |
|---|---|
| **الإلزامية** | **M** |
| **الطبيعة** | LIVING |
| **المالك** | DEVOPS |

---

## 1. البيئات الأربع

| البيئة | الغرض | البيانات | مَن يصل |
|---|---|---|---|
| ★ **المحاكي المحلي** | ★ **تطوير + اختبار قواعد الحماية** | وهمية | المطوّر |
| **التجريبية** | تكامل · اختبار قبول | ★ **وهمية حصراً** | الفريق + المالك |
| **الإنتاج** | ★ **التشغيل الفعلي — بيانات حقيقية** | ★ **حقيقية** | ★ **المستخدمون فقط + وصول إداري مُسجَّل** |
| **الأتمتة** | البناء والاختبار الآلي | وهمية | خط الأتمتة |

### 1.1 ★ المعرّفات المعتمدة

| البند | القيمة | ملاحظة |
|---|---|---|
| **مشروع الإنتاج** | `qtms-orimind-c001` | ✅ `RD-001` |
| **المشروع التجريبي** | `qtms-orimind-master` | ✅ `RD-001` |
| ★ **موقع قاعدة البيانات** | ★ **`me-central1` (الدوحة)** | ✅ ★ **نُقل من `me-central2` — `ADR-0014`** |
| **معرّف التطبيق — الإنتاج** | `dev.orimind.qtms.c001` | ✅ `RD-002` |
| **الاسم المعروض — الإنتاج** | ★ **وكالة محمد المحامي** | ✅ `RD-002` · ★ **`AM-002`** |
| 🔶 **الاسم المعروض — التجريبي** | 🔶 **وكالة محمد المحامي — تجريبي** | ★ **`AM-002`** — ★ **ليُميَّز على الجهاز نفسه** |
| 🔶 **معرّف التطبيق — التجريبي** | 🔶 `dev.orimind.qtms.c001.staging` | ★ **اصطلاح اعتمدناه** — راجع §1.2 |

> ★ **لماذا `me-central1` (الدوحة):** ★ **من أقرب المتاح جغرافياً لليمن**،
> ★ **وكل قراءة في هذا النظام تذهب للسحابة فعلاً** (لا تخزين مؤقت محلي ·
> `ADR-0003`) — ★ **فزمن الرحلة يُدفَع في كل شاشة تُفتَح، لا مرة واحدة.**
>
> ⚠️ ★ **ولماذا لم تبقَ `me-central2` (الدمّام) رغم أنها الأقرب:** رُصد
> بالتشغيل الفعلي (2026-08-22) أنها **مغلقة على هذا الحساب لكل خدمة عدا
> قاعدة البيانات نفسها** — Cloud Run وEventarc وArtifact Registry وCloud
> Build كلها ترد `LOCATION_POLICY_VIOLATED`. **ومشغّل قاعدة البيانات يجب أن
> يكون في منطقتها حرفياً** ⟹ **فالعمليات السحابية كانت غير قابلة للنشر**.
> نُقلت القاعدتان في 2026-08-22 وهما **فارغتان**، وأُعيد نشر القواعد
> و**33 فهرساً** عليهما — التفصيل في `ADR-0014`.
>
> ⛔ **والموقع الآن نهائي فعلاً** — ولا يتكرر النقل بعد أول بيانات حقيقية.

### 1.2 ✅ معرّف التطبيق التجريبي — اعتُمد وطُبِّق

> **المالك وفّر معرّف الإنتاج فقط.** ★ **والتجريبي يجب أن يختلف عنه** حتى
> يتعايش التطبيقان على الجهاز نفسه (§2 البند 5).
>
> 🔶 ★ **اعتمدنا اصطلاح اللاحقة `.staging`** — ★ **ولم نُنشئ بنداً جديداً
> لأن هذا اصطلاح قياسي مشتقّ لا معلومة غائبة.**
>
> ✅ **وأقرّه المالك وسجّله فعلياً في المنصة (2026-08-22)** — فلم يعد
> اصطلاحاً مقترحاً بل **قيمة معتمدة ومطبَّقة**.

### 1.3 ✅ حالة التأسيس الفعلي (2026-08-22)

**نُفِّذت «الخطوة صفر» 1·2·3·4·8 بشرياً** وفق
[`RB-step-zero-console-setup`](../13-operations/runbooks/RB-step-zero-console-setup.md):

| المخرَج | الإنتاج | التجريبية |
|---|:-:|:-:|
| المشروع مُنشأ | ✅ | ✅ |
| Firestore بالوضع الإنتاجي · **`me-central1`** | ✅ | ✅ |
| الخطة Blaze | ✅ | ✅ |
| المصادقة (بريد/كلمة مرور) + حساب المالك | ✅ | ✅ |
| تطبيق Android مسجَّل | ✅ | ✅ |
| App Check مسجَّل — ★ **والفرض مُطفأ** | ✅ | ✅ |
| تنبيه الميزانية | ✅ | ✅ |
| ★ **القواعد والفهارس منشورة** | ✅ **33 فهرساً** | ✅ **33 فهرساً** |

**معرّفات حساب المالك الأول** *(معرّفات لا أسرار)*:

| البيئة | UID |
|---|---|
| الإنتاج | `W7QNcPR4F5V1846SzjUAruqWD2d2` |
| التجريبية | `fj7BDnSxHkfiSwU7SjNXp7gKgXv2` |

> ★ **يلزمان العملية السحابية «مُنشئ الحسابات/مزامن الصلاحيات»** (المخرَج 7):
> حساب المالك **بلا صلاحيات إطلاقاً** الآن، ومنحها **من السحابة حصراً**
> (`authentication-policy.md` §3). ⛔ **ولا تُمنَح يدوياً من التطبيق.**

> ✅★★ **وأُقلِع مالك التجريبية فعلاً (2026-08-26):** `bootstrapOwnerPermissions`
> ردّت **`200`** · `alreadyBootstrapped: false` · `tokenRefreshRequired: false`
> ⟵ **فبطاقته الآن `isActive: true` + التسعة الإدارية** (`RB-bootstrap-owner-permissions`).
> ⛔⛔★★ **لكنه ما يزال عاجزاً عن منح أحد** — ★ **بطاقتُه بلا `sourceScope`**،
> **والفحص ③ في `permission_sync.dart` يرفض كل عملية مزامنة لفاعلٍ بلا نطاق.**
> ⟵ ⚠️⚠️ **حلقةُ إقلاعٍ مغلقة مقيسةٌ حيّاً — راجع `IQ-022`** (مفتوح).
> ✅★★★ **وحُسم `IQ-022` بالخيار (أ) ونُشر الإصلاح (2026-08-26):** الصورة
> `callables:iq022` · المراجعة **`qtms-callables-00008-dfp`** · **100٪ من المرور**
> ⟵ **وبطاقة المالك الآن: 9 صلاحيات · `isActive: true` · `sourceScope: all`.**
> ✅★★ **وحساب الاختبار مُنِح التسعة الإدارية و`sourceScope: all` فعلاً.**
> ⛔⛔★★ **لكنه لم يُمنَح الـ65 التشغيلية** — ★ **`BR-M1-03` تسقُف المنح بصلاحيات
> المُنفِّذ، والمالك لا يملك تشغيليةً واحدة** ⟵ ⚠️ **`IQ-023` مفتوح.**
> ⛔ **والإنتاج لم يُقلَع بعد** — ★ **ولا يُقلَع قبل حسم `IQ-023`**، ⟵ **وإلا تكرّر القفل هناك.**

> ### ✅★★★ وأين تعيش المعرّفات غير السرّية فعلاً — `ADR-0022` (2026-08-28)
>
> ⛔⛔ **كان `QTMS_FUNCTIONS_BASE_URL` — وهو «معرّف لا سرّ» بنصّ هذا القسم —
> يعيش في `.secrets/qtms-staging-qa-account.env` وحده**، ★ **وهو ملفٌّ
> مستبعَدٌ من المستودع** ⟵ **فكانت كلُّ نسخةٍ جديدة من المشروع تبدأ بلا
> القيمة**: ⛔ **وهو سببُ `DEBT-49` أربع مرات متتالية.**
>
> ✅ **وموضعُه اليوم ملفٌّ عامٌّ مُلتزَمٌ به:** `config/qtms-public-defines.env`
> — ★ **يُمرَّر بـ`--dart-define-from-file` قبل الملف السرّي، فيغلبه هذا عند
> التصادم** (`ADR-0022` القاعدة 4). ⛔⛔ **و`.secrets/` لم يُمَسّ بحرف.**

### 1.4 ✅★★ البنية السحابية المنشورة فعلاً — التجريبية (2026-08-24)

⚠️ **هذه الحالة الفعلية المرصودة بالجرد، لا خطةَ نشرٍ مقترحة.**

| المورد | الاسم | التفصيل |
|---|---|---|
| **خدمة Cloud Run** | `qtms-provision-accounts-on-source-add` | `me-central1` · ⛔ **بلا وصول عام** |
| **خدمة Cloud Run** | `qtms-provision-accounts-on-party-add` | `me-central1` · ⛔ **بلا وصول عام** |
| **حساب الخدمة** | `qtms-provision-accounts@…` | ★ **أقل امتياز:** `datastore.user` · `eventarc.eventReceiver` · **و`run.invoker` على الخدمتين** |
| **مشغّل Eventarc** | `qtms-provision-on-source-add` | `document=sources/{sourceId}` |
| **مشغّل Eventarc** | `qtms-provision-on-dealer-add` | `document=dealers/{dealerId}` |
| **مشغّل Eventarc** | `qtms-provision-on-supplier-add` | `document=suppliers/{supplierId}` |
| **صورة الحاوية** | `…/qtms/provision-accounts` | `dart:3.11` ⛔ **بلا Flutter** (`ADR-0012`) |
| ★★★ **خدمة Cloud Run** | **`qtms-callables`** | `me-central1` · ★★ **حاويةٌ واحدة توجّه بالمسار** (حسم `IQ-019` الخيار أ) · `FUNCTION_TARGET=callables` · ✅★★★ **وتحمل 30 عملية منذ 2026-08-26** (تسعٌ إدارية + **تسع البيانات المرجعية** `WU-002` + **ثلاث المخزون** `WU-003` + **التسعير اليومي** `WU-005` + ★★ **ثماني الجواني** `WU-004`) · **المراجعة `qtms-callables-00007-ctd`** · صورتها `callables:wu004` · ★★ **وإثبات التوجيه حيّاً: 15 مساراً معروفاً ردّت `401` ومسارٌ مجهول ردّ `404`**. ⚠️⚠️ **ورُصد عند هذا النشر أن `WU-003` و`WU-005` لم تُنشَرا قط** — ★ **الخدمة بقيت على `callables:wu002` حتى اليوم**، ⟵ **فالصورة الجديدة تحمل الثلاث معاً** |
| ★★ **حساب خدمتها** | `qtms-callables@…` | ★ **أقل امتياز:** `datastore.user` · `firebaseauth.admin` — ⛔ **ولا شيء غيرهما** |
| ★ **صورتها** | `…/qtms/callables` | نفس `Dockerfile` وسياق الجذر (`ADR-0012`) |

> ### ★★★ نموذج عنونة العمليات المستدعاة — حسم `IQ-019` (2026-08-25)
>
> ★ **خدمة واحدة لكل بيئة، والعملية مسارٌ فيها:**
>
> ```text
> POST  https://<qtms-callables>/<اسم العملية>
>       /grantPermissions · /setSourceScope · /bootstrapOwnerPermissions
>       /createUser · /updateUser · /disableUser
>       /createRole · /updateRole · /deleteRole
> ```
>
> ⛔★★ **ولا خدمة لكل عملية** — ★ **والسبب أن `FUNCTION_TARGET` هدفٌ واحد
> لكل حاوية ولا يوجّه بالمسار**، ⟵ **فخدماتٌ منفصلة كانت تعني مضيفاً لكل
> عملية**، ⛔ **وبصمةَ نشرٍ هشّة داخل بناء التطبيق.**
>
> ★ **والتطبيق يعرف عنواناً واحداً:** `--dart-define=QTMS_FUNCTIONS_BASE_URL`
> — ⛔ **ولا اسم خدمة ولا معرّف Cloud Run يدخل بناءه.**
>
> ⚠️⚠️ **ووصولها العام مقصودٌ ومطلوب — ⛔ لا سهو:** الجهاز يحمل **رمز
> Firebase** لا رمزَ IAM، ⟵ **فاشتراطُ IAM على الخدمة كان سيرفض كل استدعاء
> من التطبيق قبل أن يبلغ الحاوية.** ★ **والتفويض كلُّه في الكود**
> (`ADR-0013` القاعدة 3): **كل معالج يتحقق من الرمز وصلاحية صاحبه ويكتب
> قيده** — ⛔ **ومسارٌ مجهول يُرفَض بـ`ERR_CALL_404` قبل أي معالج.**
>
> ⚠️ **وهذا يجعل `App Check` أهمّ لا أقلّ** — ★ **وهو معلَّق بفرضٍ مُطفأ**،
> ⛔ **ويُفعَّل إلزامياً قبل أول نشر إنتاجي.**

★★ **ومتغيّرا بيئة إلزاميان على كل خدمة مشغَّلة بالكتابة:**

| المتغيّر | القيمة | ولماذا |
|---|---|---|
| `FUNCTION_TARGET` | اسم العملية كما في `api-overview.md` §3.2 | هدف واحد لكل خدمة |
| ★★ `FUNCTION_SIGNATURE_TYPE` | **`cloudevent`** | ⛔ **إلزامي — وبدونه لا تقلع الخدمة أصلاً** (`troubleshooting-guide.md` §2-د) |
| `GOOGLE_CLOUD_PROJECT` | معرّف المشروع | ★ **يُضبَط عند النشر صراحةً** لا اتّكالاً على المنصة |

> ★ **ودرسٌ موثَّق يُعاد هنا لأنه كلّف ست محاولات تسليم فاشلة في `DEBT-09`:**
> ⛔ **`eventarc.eventReceiver` وحده لا يكفي** — بلا `run.invoker` على الخدمة
> تُرفض كل عملية تسليم بـ`401`، **وتبدو كأن Eventarc لا يعمل.**

> ⛔ **والإنتاج (`qtms-orimind-c001`) بلا أي من هذه الموارد بعد** — ★ **ولا
> يُنشَر عليه قبل تفعيل فرض App Check** (§1.3).
>
> ⚠️⚠️ **ومقيسٌ لا مفترَض (2026-08-25):** **واجهة Cloud Run نفسها غير
> مُفعَّلة على مشروع الإنتاج** (`run.googleapis.com` معطَّلة) — ⟵ ★★ **فلا
> خدمة عمليات هناك إطلاقاً.** ⛔⛔ **وهذا يجعل نشرَ قواعد `WU-002` عليه
> عكساً للترتيب المُلزِم**: القواعد تُغلق كل مسارات الكتابة المباشرة،
> **وبلا خدمةٍ تستقبلها يبقى النظام بلا مسارٍ لكتابة بياناته المرجعية.**
> ★ **وقواعده اليوم على 36497 بايت** (إصدار `WU-026`) **وهي متسقة مع حالته.**

★ **ومتغيّر البيئة الذي يحمل المعرّف للعملية السحابية (2026-08-24):**

| المتغيّر | القيمة | يُقرأ في |
|---|---|---|
| ★ **`QTMS_OWNER_UID`** | معرّف مالك **هذه البيئة** من الجدول أعلاه | `bootstrapOwnerPermissions` — `functions/lib/functions.dart` |

> ⚠️ **معرّف لا سرّ** (كما ينصّ هذا القسم أعلاه)، فيُضبَط كمتغيّر بيئة عادي عند
> النشر ⛔ **ولا يدخل** [`../09-security/secrets-management-policy.md`](../09-security/secrets-management-policy.md).
> ★ **ولماذا من البيئة لا من قاعدة البيانات:** مستند الإعداد التأسيسي
> (`app_settings`) يُكتب في `WU-002` **ولا وجود له لحظة الإقلاع** — فلا يصلح
> مصدراً لهوية المالك في اللحظة التي يوجد فيها المالك وحده.
>
> ⛔ ★ **وخلطُه بين البيئتين تصعيدُ امتياز:** ضبطه على معرّف من بيئة أخرى يجعل
> الإقلاع يرفض المالكَ الحقيقي — أو أسوأ، يقبل غيره. **يُتحقَّق منه مزدوجاً**
> في [`../13-operations/runbooks/RB-bootstrap-owner-permissions.md`](../13-operations/runbooks/RB-bootstrap-owner-permissions.md) §3.

**سقف تنبيه الميزانية (`DEBT-08`):** **2 دولار شهرياً لكل بيئة**، بعتبات
50٪ · 90٪ · 100٪.

> ⚠️ **ملاحظة تشغيلية — السقف منخفض عمداً وهذا مقصود الآن:** الحصص المجانية
> اليومية (عشرات آلاف القراءات) تجعل الاستهلاك الفعلي قريباً من الصفر في
> مرحلة التطوير، **فالسقف المنخفض إنذار مبكر لا قيد إنفاق**.
> ★ **ويُراجَع بعد شهر تشغيل حقيقي** — لأن سقفاً يُطلِق تنبيهاً كل يوم
> **يُعلِّم المالك تجاهل التنبيهات**، وحينها يصير التنبيه ضرراً لا حماية.
> ⛔ **والتنبيه يُخطر ولا يوقف الخدمة** في كل الأحوال.

### 1.5 ★★ حساب اختبار (QA) كامل الصلاحيات — ⛔ التجريبية حصراً (`AM-004`)

⛔⛔★★ **هذا الحساب على `qtms-orimind-master` وحدها.** ⛔ **ولا نظير له على
الإنتاج (`qtms-orimind-c001`) — لا اليوم ولا ضمناً**، ★ **وإنشاؤه هناك يحتاج
بند `AM` جديداً صريحاً** (`AM-004` أجّله عمداً).

| البند | القيمة |
|---|---|
| **الغرض** | ★★ **تشغيل اختبار المحاكي الوظيفي** (بروتوكول التشغيل §د.2 · §د.3 · §د.4) **من طرف إلى طرف بواجهة مستخدم حقيقية** — ⛔ **لا استدعاء دوال من داخل اختبار** |
| **البيئة** | ⛅ **التجريبية حصراً** — `qtms-orimind-master` |
| **نطاق الصلاحيات** | ★★ **كل مفاتيح §2 من** [`../09-security/permissions-catalog.md`](../09-security/permissions-catalog.md) **= `true`** — ★ **مشتقّة لا منسوخة** (راجع «قاعدة المزامنة» أدناه) |
| ★ **`sourceScope`** | **`all`** — ★ **ليغطي كل المصادر بلا منح صريح لكل واحد** (§4 من الكتالوج) |
| ★ **`isActive`** | **`true`** — ⛔ **وبدونه تُصفِّر `perm()` كل صلاحية** (`RB-backfill-user-isactive`) |
| **متغيّر البريد** | ★ **`QTMS_STAGING_QA_EMAIL`** |
| **متغيّر كلمة المرور** | ★ **`QTMS_STAGING_QA_PASSWORD`** |
| **مسار الملف السرّي** | ★ **`.secrets/qtms-staging-qa-account.env`** — ⛔ **مستثنى فعلياً بـ`/.secrets/` في `.gitignore`** |
| **معرّف الحساب (UID)** | ✅ **`mjmK8owk4ZaciYT2yj1711jRgWB3`** — ★ **معرّف لا سرّ** (كما في §1.3) · **أُنشئ الحساب 2026-08-26** ⛅ **على `qtms-orimind-master`** · ★ **ومُتحقَّقٌ منه بتسجيل دخولٍ فعلي** ⛔ **بلا مرور أي قيمة اعتماد في سياق الأداة** |
| ★★ **خُطّاف الملء المسبق** | ✅★★ **مبنيٌّ ومحروسٌ ومُثبَتٌ ببناءٍ إنتاجيٍّ فعلي** (`AM-005` · `DEBT-31`) — ⛔ **غائبٌ كلياً عن حزمة الإنتاج ولو مُرِّر إليها الاعتماد عمداً** — [`staging_qa_credentials.dart`](../../lib/core/startup/staging_qa_credentials.dart) · ⛔ **ملءٌ فقط بلا إرسالٍ تلقائي** |
| **الرَنبوك التشغيلي** | [`../13-operations/runbooks/RB-staging-qa-account.md`](../13-operations/runbooks/RB-staging-qa-account.md) |

> ⛔⛔★★ **ولا قيمة سرّية واحدة في هذا الجدول ولا في أي مستند آخر** — ★ **اسم
> المتغيّر ومسار الملف فقط**، وهو نمط `required-data.md` نفسه وبنص
> [`../09-security/secrets-management-policy.md`](../09-security/secrets-management-policy.md)
> §1 و§4 البند 2. ★ **والملف يقع تحت §3 «ملف بيئة محلي مستثنى من المستودع»**
> ⟵ ⛔ **فلا حرف في تلك السياسة (STATIC) احتاج تعديلاً.**

#### ★★ قاعدة المزامنة الآلية — ⛔ لا قائمة مفاتيح ثانية

```text
★ صلاحيات هذا الحساب = كل مفاتيح §2 من permissions-catalog.md ⟵ تعريفاً
⛔ لا قائمة مخزَّنة في مستند ولا في كود ولا في هذا الملف — فلا مصدر حقيقة ثانٍ يَنحرِف
★ وكل منح يُعيد كتابة المجموعة كاملةً من الكتالوج الحالي، لا يُضيف فرقاً
```

⛔⛔★★ **وأي تشغيل يُضيف مفتاحاً جديداً إلى §2 من الكتالوج يُسنِده لهذا
الحساب في نفس التشغيل** — ★ **بلا انتظار طلبٍ من المالك**، ★ **وهو البند ⑤
في §6 من الكتالوج نفسه** (قائمة «عند إضافة صلاحية جديدة»). ⟵ ★ **فالقاعدة
تعيش حيث يقرؤها من يضيف المفتاح**، ⛔ **لا في مستند بنية تحتية قد لا يفتحه.**

#### ★★ كيف تُستخدَم كلمة المرور بلا أن يطّلع عليها أحد

⛔ **القاعدة القديمة كانت «إدخال كلمات المرور ممنوعٌ على الأداة» مطلقاً** —
⟵ ★★ **وعُدِّلت بـ`AM-004` تعديلاً معلَناً لا التفافاً صامتاً.** ★ **والبديل
المعتمَد يحفظ الغاية التي وُجد المنع لأجلها** (ألّا يطّلع مساعدٌ آليّ على سرّ
المالك) **ويُسقط الشرط الذي لا يخدمها** (ألّا يُسجَّل الدخول آلياً إطلاقاً):

| # | الضمان الملزِم |
|:-:|---|
| 1 | ★★ **القيمة تُقرأ داخل عملية التشغيل وحدها** — ⛔ **ولا تُقرأ بأداة قراءة ملفات ولا تُطبَع ولا تُردَّد** |
| 2 | ⛔⛔ **ولا تمرّ في أمرٍ يُكتب حرفياً** — ★ **يُمرَّر مسار الملف أو اسم المتغيّر فقط** (`--dart-define-from-file=<المسار>` أو `$env:QTMS_STAGING_QA_PASSWORD`) |
| 3 | ⛔⛔ **ولا تظهر في تفكير ظاهر ولا ردّ نصّي ولا مستند ولا رسالة التزام** — ★ **ولو جزئياً** |
| 4 | ⛔ **ولا تُطلَب من المالك في المحادثة إطلاقاً** — ★ **يملأ الملف بنفسه** (`RB-step-zero-console-setup` §6 يبقى سارياً حرفياً) |
| 5 | ★ **وأي مخرَج تشغيل يُلصَق يُفحَص أنه بلا تسريب** — ⛔ **ومخرَجٌ يحمل السرّ يُوجِب تدويره فوراً** (`secrets-management-policy.md` §6 و§7) |

> ⚠️★ **والمقايضة معلَنة لا مسكوتٌ عنها:** الحساب **كامل الصلاحيات على
> التجريبية**، ⟵ **فتسريب اعتماده يعني وصولاً كاملاً لبيانات تلك البيئة.**
> ✅ **والمقبول أن التجريبية بياناتها وهمية حصراً** (§4) ⛔ **ولا بيانات
> إنتاجية تُنسَخ إليها أبداً** (§2 البند 4) — ⟵ **فالأثر محصور في بيانات
> لا قيمة لها.** ⛔⛔ **وهذا بالضبط ما يمنع تعميم الآلية على الإنتاج بلا
> بند `AM` جديد.**

---

## 2. ★ قواعد الفصل

| # | القاعدة |
|---|---|
| 1 | ★ **مشروع سحابي مستقل لكل بيئة** |
| 2 | ★ **أسرار مستقلة تماماً** — ⛔ **ولا سرّ إنتاجي على جهاز تطوير** |
| 3 | ⛔ **لا ربط تطبيق تطوير بمشروع الإنتاج — إطلاقاً** |
| 4 | ⛔ **لا نسخ بيانات إنتاجية لأي بيئة أخرى** |
| 5 | ★ **معرّف تطبيق مختلف للتجريبية** — ليتعايش مع الإنتاجي على نفس الجهاز |
| 6 | ★ **علامة بصرية واضحة في غير الإنتاج** — ★ **حتى لا يُخطئ أحد في التمييز** |

> ★ **البند 6 ليس تجميلاً:** الشاشة نفسها ببيانات مختلفة **تخدع أعين الجميع**،
> **وإدخال حركة حقيقية في التجريبية أو العكس خطأ لا يُلغى.**

## 3. المحاكي المحلي — ★ إلزامي

| ما يُشغَّل عليه |
|---|
| ★ **اختبارات قواعد الحماية** — **حاجب للدمج** |
| اختبارات العمليات السحابية |
| اختبارات التكامل |
| تجربة الترحيلات البنيوية قبل نشرها |

> ★ **لا يتصل بأي مشروع سحابي حقيقي** — وهذه ميزته الأمنية الأولى.

## 4. التجريبية

| البند | القاعدة |
|---|---|
| **البيانات** | ★ **وهمية مُولَّدة — بأسماء واضحة الوهمية** |
| **إعادة التهيئة** | دورياً — لا بيانات متراكمة عشوائية |
| **الاستخدام** | ★ **قبول المالك · اختبار الاسترجاع · اختبار الاختراق لو أُجري** |
| **مطابقة الإنتاج** | ★ **نفس قواعد الحماية ونفس الفهارس بالضبط** |

> ⚠️ **تجريبية بقواعد حماية مختلفة عن الإنتاج ⟵ لا تُثبت شيئاً.**

## 5. الإنتاج

| البند | القاعدة |
|---|---|
| **الوصول الإداري** | ★ **بأضيق صلاحية · واستثنائي · ⭐ ويُسجَّل في** [`../09-security/security-audit-log.md`](../09-security/security-audit-log.md) |
| **التغيير** | ★ **عبر الأتمتة والنشر الموثَّق — لا تعديل يدوي في اللوحة** |
| **البيانات** | ⛔ **لا تُنزَّل على جهاز شخصي** |
| **النسخ الاحتياطي** | [`../06-database/backup-and-recovery-policy.md`](../06-database/backup-and-recovery-policy.md) |
| **المراقبة** | [`../15-monitoring-and-observability/monitoring-strategy.md`](../15-monitoring-and-observability/monitoring-strategy.md) |

## 6. ما يختلف بين البيئات

| البند | يختلف؟ |
|---|:-:|
| معرّف المشروع السحابي | ✅ |
| الأسرار | ✅ |
| معرّف التطبيق | ✅ |
| العلامة البصرية | ✅ |
| ★ **قواعد الحماية** | ⛔ **لا — متطابقة** |
| ★ **الفهارس** | ⛔ **لا — متطابقة** |
| ★ **منطق الأعمال والمعادلات** | ⛔ **لا — متطابق** |
| ★ **تعطيل التخزين المؤقت المحلي** | ⛔ **لا — مُعطَّل في كل البيئات** |

## 7. المرتبطات

[`infrastructure-overview.md`](infrastructure-overview.md) ·
[`secrets-and-config-management.md`](secrets-and-config-management.md) ·
[`../07-development/environment-setup.md`](../07-development/environment-setup.md) ·
[`../12-deployment-and-release/deployment-guide.md`](../12-deployment-and-release/deployment-guide.md)
