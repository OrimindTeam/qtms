# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-29

**رقم تشغيل التنفيذ:** 40
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة** — شريحة رأسية كاملة)
**تقدّم التنفيذ:** 33.3٪ (9 منجَزة من 27)
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** **0** — ✅ **الثلاثةُ المفتوحة رُحِّلت في هذا التشغيل** (`IQ-028` · `IQ-029` · `IQ-030`)

---

## 1. ملخص تنفيذي

★★★ **رُحِّلت إجاباتُ المالك الثلاث** (`IQ-028` · `IQ-029` · `IQ-030`) إلى مستنداتها
والكودِ حيث وُجد، ⟵ **فلم يبقَ بندُ `IQ` مفتوحٌ واحد (30 من 30 محسومة).**
✅★★★ **وأُغلقت `WU-007`** بعد اجتياز **بوابة §د.2 الإلزامية على `Pixel_6_API_36`**:
**تسعةُ سيناريوهات + سجلٌّ نظيف.** ⛔⛔★★★ **وكشف التشغيلُ الحيُّ عطلاً ماليّاً
لم يكشفه أيٌّ من 1507 اختباراً آلياً** — **`DEBT-61`: «المتبقي» يبقى على قيمته
قبل القبض ⟵ فيُقبَض المبلغُ مرتين** — ✅ **سُدِّد، وله اختبارا ارتداد مُثبَتان
بطفرة، وأُثبت الإصلاحُ حيّاً بعد إعادة البناء.** ⛔ **وكشف عطلاً ثانياً خارج
النطاق** (`DEBT-62` — **شاشةُ التسعير تُرسَم فارغةً عند الدخول الثاني**)
⟵ **و`WU-005` نُقلت إلى `[يحتاج إعادة تحقق]`** ⛔ **ولم تُترك معلَّمةً منجَزة.**

---

## 2. بوابة الدخول — نتيجة الفحص

### 2.1 البوابة العامة (ثمانية شروط — `implementation-playbook.md` §1.1)

| # | الشرط | ✅ | المسار المفحوص |
|:-:|---|:-:|---|
| 1 | المنهجية مُصرَّح بها | ✅ | `docs/21-project-management/development-methodology.md` |
| 2 | العمارة والمكدس | ✅ | `03-architecture/architecture-overview.md` · `technology-stack.md` |
| 3 | أسلوب الكود والتسمية ومعالجة الأخطاء | ✅ | `04-design/coding-standards.md` · `naming-conventions.md` · `error-handling-strategy.md` |
| 4 | تعريف «منجَز» والتفرّع والالتزام والإصدار | ✅ | `07-development/*` · `12-deployment-and-release/versioning-scheme.md` |
| 5 | استراتيجية الاختبار | ✅ | `08-testing-and-qa/test-strategy.md` |
| 6 | سياسة الأسرار | ✅ | `09-security/secrets-management-policy.md` |
| 7 | `srs.md` بمتطلبات مرقّمة | ✅ | `02-requirements/srs.md` |
| 8 | **لا `ADR` بحالة «مقترح» يحكم ما نُفِّذ** | ✅ | **`ADR-0001`…`ADR-0022` كلُّها «مقبول»** — مقيسٌ بفحص حقل الحالة في المجلد كلِّه |

### 2.2 بوابة `WU-007` (خمسة شروط — §1.2)

| # | الشرط | ✅ | المسار |
|:-:|---|:-:|---|
| 1 | تصميم الوحدة | ✅ | `04-design/module-design/settlement-design.md` |
| 2 | المتطلب بمعايير قبول | ✅ | `02-requirements/functional-requirements/FR-M12.md` |
| 3 | عقود الواجهة | ✅ | `05-api/api-overview.md` · `authentication-and-authorization.md` |
| 4 | التخزين | ✅ | `06-database/erd.md` · `data-dictionary.md` |
| 5 | المصادقة والصلاحيات | ✅ | `09-security/authentication-policy.md` |

⚠️★ **وانحرافُ عدّادٍ مرصود:** ★ **`CLAUDE.md` يقول «كل ADR معتمد (20)»**
⛔ **والعدد الفعلي 22** (`ADR-0021` · `ADR-0022` أُضيفا لاحقاً) — ★ **والوصفُ
سليم والعدّادُ متأخر**، ✅ **وصُحِّح في الكتلة المُدارة في هذا التشغيل.**

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

| WU | العنوان | أبرزُ ما تغيّر فيها هنا | الفحوصات ونتيجتها الفعلية |
|---|---|---|---|
| ✅ **`WU-007`** | **المقبوضات وحساب المقوت من طرف إلى طرف** | ★ **الكودُ كان مكتوباً** — ★★ **وما أُضيف هنا: إصلاحُ `DEBT-61`** في [`receipt_screen.dart`](../../../lib/capabilities/sales_receivables/presentation/receipt_screen.dart) (`_refreshDerivedReads`) **واختبارا ارتداد** في [`receipt_screen_test.dart`](../../../test/capabilities/sales_receivables/presentation/receipt_screen_test.dart) | ★ `flutter analyze` ✅ **`No issues found!`** · ★ `flutter test` ✅ **400 · ٠ فاشلاً** (**398 قبل الإصلاح**) · ★ `dart analyze && dart test` (النطاق) ✅ **412 · ٠** · ★ (السحابة) ✅ **461 · ٠** · ★ `npm test` (الحماية) ✅ **236 · ٠** ⛔ **والقواعد لم تُمَسّ** · ★★★ **اختبار المحاكي §د.2** ✅ **تسعةُ سيناريوهات + سجلٌّ نظيف** · ★ `adb logcat` ✅ **نظيف** — ⟹ **المجموع 1509 · ٠ فاشلاً** |

**⛅ وإثباتاتٌ سحابية مقروءةٌ بعد الكتابة** ⛔ **لا ادّعاءُ شاشةٍ** (بروتوكول §و):

| ما أُثبت | القيمة المقروءة من التجريبية |
|---|---|
| `receipts/RCP-20260829-0001` | `surplusAmount 5000` · `surplusScope general` · `lines []` · `usedAutoAllocation false` |
| `dealer_surplus/MQT-0001_general` | `availableAmount 5000` ⟵ **ثم 0 بعد تطبيقه تلقائياً** |
| `distributions/MQT-0001_SRC-001_20260829/pricing/current` | `debtValue 10000` · `settledAmount 5000 ⟶ 8000 ⟶ 8800` · `remaining 5000 ⟶ 2000 ⟶ 1200` · `discountedAmount 0` |
| ★★★ **موضعُ المبالغ الثلاثة** | **في `pricing/current`** ⛔ **لا في الأب** — ⟹ **`IQ-027` و`ADR-0011` مُنفَّذان حيّاً لا على الورق** |

### 3.2 وحدات قيد التنفيذ

**لا توجد** — ⟵ `in_progress: 0`.

### 3.3 وحدات محجوبة (ولماذا)

**لا توجد** — ⟵ `blocked: 0`. ✅ **وكل بنود `IQ` محسومة.**

### 3.4 ⛔⛔ وحدةٌ نُقلت إلى `[يحتاج إعادة تحقق]`

| WU | العنوان | السبب | ما يلزم لإعادتها |
|---|---|---|---|
| ⚠️ **`WU-005`** | **التسعير اليومي** | **`DEBT-62`** — ★ **الشاشةُ تُرسَم فارغةً تماماً عند الدخول الثاني في نفس تشغيل التطبيق** (**بلا رأسٍ ولا هيكلٍ ولا حالةٍ فارغة ولا خطأ**) ⟵ **ولا تصحّ إلا بإقلاع**. ⛅ **مقيسٌ ومُكرَّرٌ ثلاث مرات** ⛔ **ولا أثرَ في `logcat`** | ★ **إصلاحُ فرعِ العرض ثم إعادةُ §د.2 لهذه الزيادة** |

⛔★★ **ولماذا لم يُصلَح هنا:** ★ **خارج نطاق `WU-007` صراحةً** — **وحدةٌ واحدةٌ
في كل مرة** (`implementation-playbook.md` §3 البند 5) ⟵ **وإصلاحُ شاشةِ وحدةٍ
أخرى داخل زيادةٍ جارية يخلط نطاقين ويُفسد حدودَ التحقق.**

---

## 4. مخالفات النزاهة المرصودة آلياً

✅ **لا مخالفات.** مخرجاتُ `track_implementation.py` حرفياً:
`integrity_violations: []` · `unparsed_lines: []` · `normalized_headings: []`.
**النتيجة:** 27 وحدة — **9 منجَزة · 0 قيد التنفيذ · 17 مخطَّطة · 0 محجوبة ·
1 تحتاج إعادة تحقق.**

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

⛔ **لم يُصعَّد بندٌ جديد في هذا التشغيل.** ✅★★★ **وثلاثةٌ رُحِّلت وأُغلقت:**

| IQ | القرار | إلى أين رُحِّل فعلاً |
|---|---|---|
| ✅ **`IQ-028`** | **الخيار ب** — **أزرقٌ رماديٌّ خافت لـ`info` وحدها** | **بندٌ جديد `DS-003`** في [`design-source-selection.md`](design-source-selection.md) · [`design-tokens.md`](../../18-ux-ui/design-tokens.md) §2.3 · [`design-system.md`](../../18-ux-ui/design-system.md) §3.4 · [`design_tokens.dart`](../../../lib/core/design/design_tokens.dart) · **و`infoBase` أُدرِج في حدّ الأيقونات 3:1** في [`design_gates_test.dart`](../../../test/core/design/design_gates_test.dart) |
| ✅ **`IQ-029`** | **الخيار أ** — **ضغطُ السبعِ مجموعات إلى خمسةِ تبويبات** | [`ui-guidelines.md`](../../18-ux-ui/ui-guidelines.md) **§4-أ** (**الخريطةُ كاملةً بمساراتها**) و**نمط 1** · [`design-system.md`](../../18-ux-ui/design-system.md) §6.و |
| ✅ **`IQ-030`** | **الخيار أ** — **سجلٌّ ثانٍ `owner_ledger_trends`** | [`owner-ledger-summary-design.md`](../../04-design/module-design/owner-ledger-summary-design.md) **§2.1** (**بقواعده الخمس**) و**§8** و**§10.2** · [`data-dictionary.md`](../../06-database/data-dictionary.md) §4 · [`erd.md`](../../06-database/erd.md) §5 · **ونطاقُ `WU-016` وُسِّع به** |

⛔⛔★★ **وشطران مؤجَّلان صراحةً — ⛔ لا مُغفَلان:**
① **`IQ-029` شطرُ الكود** (`router.dart` · `home_shell.dart`): ★ **لا شريطَ
سفليَّ مبنيٌّ اليوم أصلاً** (**الصدَفةُ `P1` بمداخلَ في أقسام** — `ADR-0021`)
⟵ **فالقرارُ رُحِّل إلى العقد الحاكم الذي يُبنى عليه**، ⛔ **وبناءُ الشريط
زيادةُ صدَفةٍ مستقلة لا تُقحَم في `WU-007`.**
② **`IQ-030` شطرُ الكود** (`firestore.rules` · `firestore.indexes.json` ·
باني الملخّص · مكوّنُ البطاقة): ★ **يُنفَّذ في `WU-016`** — ⛔ **وسجلٌّ بلا
بانٍ سجلٌّ فارغ**، ⟵ **وفتحُ قاعدةِ قراءةٍ لمجموعةٍ لا كاتبَ لها يُغيِّر
`firestore.rules` بلا نشرٍ يقابله** ⛔ **وهو ما تمنعه قاعدةُ ترتيب النشر الملزِمة.**

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

### 6.1 ✅ تعارضٌ حُسم بقرار المالك — الشريط السفلي

```text
⚠️ انحراف توثيقي مكتشَف
الموقع: docs/18-ux-ui/ui-guidelines.md §4 · design-system.md §6.و
نوع الانحراف: مستندان حاكمان يفرضان «خمسة عناصر كحد أقصى»
             والوثيقة الأصلية §4.7 على «سبع مجموعات» — تعارضٌ قائم منذ التوليد.
الإجراء المنفَّذ: حُسم بقرار المالك على IQ-029 (الخيار أ) — ضغطُ السبع إلى خمس،
             وكُتبت الخريطةُ المعتمَدة بمساراتها في ui-guidelines.md §4-أ.
             ⛔ ولم يُرفَع الحدُّ ولا خُفِّف مستندٌ حاكم.
```

### 6.2 ⚠️ عدّادُ `ADR` في `CLAUDE.md` متأخرٌ عن الواقع

★ **الكتلة كانت تقول «كل `ADR-0001`…`ADR-0020` معتمد (20)»** ⛔ **والفعليّ 22**
(`ADR-0021` أنماطُ الشاشات · `ADR-0022` ثوابتُ البناء العامة) — ★ **والقاعدةُ
الحاكمة نفسُها سليمة** (**لا تُعدَّل أيٌّ منها**)، ⟵ **والانحرافُ في العدد وحده**
✅ **وصُحِّح في الكتلة المُدارة.**

### 6.3 ⛔ ولا تعارضَ ثالث

⛔ **ولم يُعدَّل `srs.md` ولا أيُّ `ADR` معتمد ولا `uat-signoff.md`.**
★ **و`settlement-design.md` كان يصف `M12` منفَّذاً بالفعل** ⟵ **فلم يحتج تغييراً.**

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

- ⛔ **لم يُنشَأ `ADR` جديد ولا `RFC`** — ★ **و`IQ-028` كان الوحيد المرشَّح**،
  ⟵ **وقرارُ الهوية البصرية يعيش في سجل `DS` لا في `ADR`** (نصُّ البند نفسِه):
  ★ **فكُتب `DS-003`** ⛔ **ولم يُحرَّر `DS-002` ولا حُذف** (سجل تراكمي).
- ⛔⛔★★★ **ولا نشرَ إنتاجي — ولم يُطلَب ولم تُعرَض كلمةُ الاعتماد أصلاً:**
  ★ **الإنتاجُ لم يُمَسّ بحرف في هذا التشغيل.**
- ⛔⛔★★ **ولا `run deploy` ولا `*-iam-policy-binding`** (بروتوكول §ز) —
  ★ **ولم تُحتَجّ إليهما:** **الخدمةُ والفهارس منشورتان سلفاً بيد المالك**
  (`00019-b25`)، ⟵ **والاختبارُ الحيُّ جرى عليهما كما هما.**
- ⛔ **ولا عمليةَ Git خارجية** (`push` / `PR` / `merge`) — ★ **ولم تُطلَب.**

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

⛔⛔★★★ **`WU-007` حسّاسةٌ أمنياً — ونجاحُ كل ما سبق لا يُغني عن مراجعةٍ بشرية:**
★ **تحصيلٌ نقديٌّ وذمم** · ★ **ومسارُ إيداعٍ بنكي بصلاحيتين مستقلتين** ·
★ **وكتابةٌ في دفتر المقاوته وأرصدته وفائضه.**
⟹ ★★ **وما يزيد هذا تأكيداً في هذا التشغيل بالذات:** ⛔ **العطلُ الذي كُشف
(`DEBT-61`) كان ماليّاً ومسكوتاً عنه في 1507 اختباراً** — ⟵ **فالتحقق الآلي
شرطٌ ضروريٌّ لا كافٍ** (`implementation-playbook.md` §7).

⚠️★★ **وما لم يُختبَر حيّاً يُقال صراحةً** ⛔ **ولا يُدَّعى خلافُه:**
**مسارُ الإيداع البنكي** (`FR-M12-16` · `FR-M12-17`) · **وتعديلُ سندٍ وإلغاؤه**
(`receiptAmend` · `receiptCancel`) · **وسندٌ بتاريخٍ سابق** (`receiptBackdate`)
— ★ **الثلاثةُ لا واجهةَ لها بعد**، ★ **ومُغطّاةٌ باختبارات السحابة والنطاق**
⛔ **لا حيّاً.**

★ **والوحداتُ الحسّاسة السابقة تبقى على حالها:** `WU-000` · `WU-026` · `WU-001`
· `WU-002` · `WU-004` (⛔ **مراجعتُها الأمنية قائمةٌ لم تُمَسّ**) · `WU-006` · `WU-008`.

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| `18-ux-ui/design-tokens.md` §2.3 | ★ **قيمُ `info` النهائية** + **التباين 7.44** (`IQ-028`) |
| `18-ux-ui/design-system.md` §3.4 و§6.و | ★ **تفريقُ «معلومة» عن «لا حكم»** · **والتبويباتُ الخمسة** |
| `18-ux-ui/ui-guidelines.md` §4-أ ونمط 1 | ★★ **خريطةُ التبويبات الخمسة بمساراتها** (`IQ-029`) |
| `00-governance/upds-workflow/design-source-selection.md` | ★★ **بندٌ جديد `DS-003`** — استثناءُ اللون البارد بأربعة شروط |
| `04-design/module-design/owner-ledger-summary-design.md` §2.1 · §8 · §10.2 | ★★ **سجلُّ `owner_ledger_trends`** (`IQ-030`) |
| `06-database/data-dictionary.md` §4 · `erd.md` §5 | ★ **مجموعةٌ جديدة مخطَّطة لـ`WU-016`** (UPDS-03 §4: تغيّرُ المخطط) |
| `02-requirements/requirements-traceability-matrix.md` | ★ **عمودُ الكود لـ`FR-M12` صار «مُنفَّذ ومُتحقَّق كاملاً»** + **ذكرُ `DEBT-61`** |
| `03-architecture/risks-and-technical-debt-register.md` | ★★ **`DEBT-61`** (مُغلَق) · **`DEBT-62`** (مفتوح) |
| `00-governance/upds-workflow/implementation-plan.md` | ★ **`WU-007` منجَزة بجدول المحاكي** · **`WU-005` تحتاج إعادة تحقق** · **نطاقُ `WU-016` وُسِّع** |
| `00-governance/upds-workflow/implementation-questions.md` | ★ **الثلاثةُ `[تم الحل ✅]` بحقل `رُحِّل إلى`** |

⛔ **ولم يُحدَّث `openapi.yaml`** — ★ **لا `Endpoint` جديد:** **العملياتُ
المستدعاة الأربع كانت منشورةً سلفاً** (`00019-b25`) ⟵ **ولم تتغيّر عقودُها.**
⛔ **ولا `migrations-log.md`** — ★ **`owner_ledger_trends` مخطَّطٌ لم يُنشَأ بعد.**

---

## 10. حالة `CLAUDE.md`

✅ **حُدِّثت الكتلةُ المُدارة** — ★ **نقطةُ تحوّلٍ فعلية:** **اكتمالُ وحدة عمل**
(`claude-md-management.md` §2 البند 5) **وإغلاقُ آخر بندِ `IQ` مفتوح**.
★ **عبر `sync_claude_md.py` وحده** ⛔ **لا تحريرٌ يدوي** — راجع §12 أدناه للمخرَج.

---

## 11. الخطوة التالية الموصى بها

★★ **المرحلةُ الأولى لم تكتمل بعد** — ⟵ **فلا اختبارَ تكامليَّ للمرحلة (§د.3) الآن.**
⛔⛔★★★ **والأولويةُ المقترحة `DEBT-62` قبل أي زيادة جديدة:** ★ **`WU-005`
`[يحتاج إعادة تحقق]`، وشاشةُ التسعير بوابةُ التوزيع كلِّه** ⟵ **فعطلُها يُوقف
العملياتِ اليومية**، ★ **وهو أرخصُ ما يُصلَح الآن وأغلى ما يُؤجَّل.**
★ **ثم `WU-009` (مركز الإدخالات المعلّقة)** — **التالية في ترتيب المرحلة.**

⛔ **ولا بندَ `IQ` ينتظر المالك** — ✅ **الثلاثون كلُّها محسومة.**
⚠️★★ **وينتظر المالكَ شيئان لا ثالثَ لهما:** ① **المراجعةُ البشرية الأمنية
لـ`WU-007` و`WU-004` قبل أي نشر إنتاجي** · ② **قرارُ أولوية `DEBT-62`.**

---

## 12. مخرَج `sync_claude_md.py` (حرفياً)

```text
{
  "target_path": "E:/Projects/qtms\CLAUDE.md",
  "file_exists": true,
  "has_managed_block": true,
  "agents_md_exists": false,
  "agents_import_present": false,
  "human_content_lines_preserved": 20,
  "current_total_lines": 249,
  "recommended_max_lines": 200,
  "action": "managed-block-updated",
  "new_total_lines": 249,
  "exceeds_recommended_size": true,
  "warning": "تجاوز CLAUDE.md الحجم الإرشادي (200 سطر) — انقل القواعد المتخصصة إلى .claude/rules/ أو اكتفِ بالإشارة لمستندات docs/ بدل تكرار محتواها."
}

★ **والتحذيرُ متوقَّعٌ ومحكومٌ باستثناء `AM-005`** (**250 سطراً سقفاً**) —
✅ **و249 سطراً بعد التحديث** ⟵ **فالسقف لم يُخرَق**، ★ **والجلسةُ صافيةُ صفر:**
**قسمُ `IQ-027` استُبدل بقسمِ `WU-007` و`DEBT-61` في العدد نفسِه من الأسطر.**
✅ **و20 سطراً بشرياً محفوظةٌ حرفياً** (قسمُ `USER-ADDED`) ⛔ **لم تُمَسّ.**
```

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*

---

## 13. ⛔⛔★★★ ملحق — جلسةُ تشخيص `DEBT-62` (2026-08-29 · بطلب المالك)

**الطلب:** «**ابدأ بـ`DEBT-62` أولاً، بالبروتوكول القائم وبلا تخطّي أي تحقّق**».

**النتيجة الصريحة:** ⛔⛔ **الجذرُ لم يُحدَّد، ولم يُكتب إصلاح** — ★ **وأُعيدت
شجرةُ الكود إلى حالتها بالضبط.** ⛔ **ولا يُعَدّ هذا نجاحاً ولا فشلاً صامتاً:**
★ **هو توقّفٌ مقصودٌ عند حدّ المعرفة المُثبَتة** (`implementation-playbook.md` §5).

### 13.1 ما نُفِّذ فعلياً

★ **ستُّ دوراتِ بناءٍ وتثبيتٍ وتشغيلٍ حقيقي على `Pixel_6_API_36`**، ★ **بأدواتِ
تشخيصٍ مزروعةٍ في الكود ثم مُزالة:** **مجسّاتُ `build`** · **مجسُّ قيودِ التخطيط
(`LayoutBuilder`)** · **مجسُّ الهندسة (`RenderBox.size`/`localToGlobal`)** ·
**مجسُّ طَورِ الرسم (`CustomPainter.paint`) للجسم ولشريط التطبيق معاً** ·
**مجسُّ حالةِ المسار (`ModalRoute.isCurrent`/`GoRouter`)** · **ومَشيُ سلسلةِ
الأسلاف مع `debugNeedsPaint`.**

### 13.2 الخلاصة المقيسة — ثلاثةَ عشرَ قياساً

★ **الجدول الكامل في** [`risks-and-technical-debt-register.md`](../../03-architecture/risks-and-technical-debt-register.md)
**تحت `DEBT-62`.** ★ **وخلاصتُه:**

> ✅ **`build` و`initState` والتخطيطُ والإطاراتُ كلُّها سليمةٌ ببياناتٍ صحيحة**
> ⛔⛔ **والجسمُ لا يُرسَم ولا يُفحَص لمسُه، وأسلافُه عالقون على `debugNeedsPaint=true`**
> ★ **بينما شريطُ التطبيق في نفس `Scaffold` يُرسَم.**

⛔ **وأربعةُ أسبابٍ نُفيت بالقياس لا بالظنّ:** ① **المزوّدات والحالة والبيانات** ·
② **حجبُ مسارٍ فوقه** (`isCurrent=true · isActive=true · /home/pricing`) ·
③ **استثناءٌ مُبتلَع** (⛔ **لا أثرَ في `adb logcat`**) · ④ **خلفيةُ التصيير**
(⛔ **يتكرّر حرفياً بعد `EnableImpeller=false`**).

### 13.3 ⛔ وتصحيحُ وصفٍ سابق — لا يُترَك قائماً

⚠️★★ **وصفُ `DEBT-62` في تقرير هذا اليوم الأول قال «فرعٌ يُرجِع عدماً»** —
⛔⛔ **وهذا خطأ، أثبته القياس:** ★ **الفرعُ يُرجِع الشجرةَ كاملةً وتُخطَّط فعلاً**،
⟵ **والعطلُ في طَورِ الرسم لا في البناء.** ✅ **وصُحِّح البند كاملاً في سجل الديون**
⛔ **ولم يُترَك الوصفُ الأول قائماً** (بروتوكول §و).

⚠️★★ **وتصحيحٌ ثانٍ:** ★ **«عدّادُ `سُعِّر 0 من 1` خاطئ» — رُفع ولم يُسجَّل ديناً**:
✅ **وهو صحيحٌ تماماً** — `FR-M9-05` **يُعرِّف «تم التسعير» بالسعرين معاً**
(«**نصفُ التسعير ليس تسعيراً**»)، ★ **والحدُّ الأدنى كان فارغاً في القياس.**

### 13.4 ⛔ حدٌّ بنيويٌّ معلَن — الحارسُ الآلي عاجزٌ هنا

⛔⛔★★ **ولا اختبارَ ارتدادٍ كُتب لهذا العطل — ⛔ ولا يُدَّعى خلافُه:**
★ **جُرِّب مرتين ولم يتكرّر** — **بتبديلٍ مباشر للشجرة، وبملاحةِ `go_router`
متداخلةٍ مطابقةٍ للمسار الحقيقي** (`/home` ⟵ `/home/pricing`): ✅ **الشجرةُ
حاضرةٌ في الدخول الثاني داخل الاختبار.** ⟹ ★ **لأن اختبار الودجات لا يُشغّل
المُركِّبَ الحقيقي** ⛔ **فالعطلُ خارج مدى الحارس الآلي بنيوياً** — ⟵ **وهذا
بالضبط ما وُجدت بوابةُ §د لأجله.**

⚠️ **وسقّالةُ التشخيص حُذفت بعد انتهائها** (`debt62_repro_test.dart` — ★ **ملفٌّ
أنشأته هذه الجلسة نفسُها وعلَّمته «يُحذَف»**) ⛔ **ولم يُمَسّ ملفُّ اختبارٍ قائم.**

### 13.5 ✅ حالةُ الشجرة بعد الجلسة — مُتحقَّقةٌ لا مُدَّعاة

| الفحص | النتيجة |
|---|---|
| `daily_pricing_screen.dart` | ✅ **غيرُ معدَّل** — ⛔ **لا سطرَ تشخيصٍ باقٍ** (`git status`) |
| `AndroidManifest.xml` | ✅ **مُعاد** — ⛔ **ولا بندَ Impeller** |
| `flutter analyze` | ✅ **`No issues found!`** |
| `flutter test` | ✅ **400 · ٠ فاشلاً** |
| `dart test` (النطاق) | ✅ **412 · ٠** |
| `dart test` (السحابة) | ✅ **461 · ٠** |
| `npm test` (الحماية) | ⏭️ **لم يُعَد تشغيلُه في هذا الشطر** — ★ **و`firestore.rules` لم تُمَسّ** (**مقيس: `git status` فارغ**)، ★ **ونتيجتُه في هذه الجلسة 236 · ٠** |
| **الإجمالي** | ✅ **1509 · ٠ فاشلاً** — ⛔ **بلا تغيّر عن ما قبل الجلسة** |

### 13.6 ⛔ ولم يُمَسّ الإنتاج ولا IAM

⛔⛔★★★ **ولا نشرَ إنتاجيّ ولا تغييرَ IAM ولا `run deploy`** (بروتوكول §ز) —
★ **وكلُّ ما جرى بناءُ تصحيحٍ محليٌّ وتثبيتُه على المحاكي.** ⛅ **والتجريبيةُ
قُرئت فقط.** ⛔⛔ **و`WU-007` و`WU-004` تبقيان بحاجةِ مراجعةٍ بشريةٍ أمنيةٍ قبل
أي نشرٍ إنتاجي** — ★ **بلا تغيير.**
