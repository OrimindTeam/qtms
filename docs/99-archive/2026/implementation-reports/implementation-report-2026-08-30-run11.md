# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-30

**رقم تشغيل التنفيذ:** 11
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة** — شريحة رأسية كاملة)
**تقدّم التنفيذ:** 40.7٪ (11 منجَزة من 27)
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** **4** — `IQ-031` · `IQ-032` · `IQ-033` · `IQ-034` (**كلُّها فُتحت في هذا التشغيل**)

---

## 1. ملخص تنفيذي

⛔⛔★★★ **لم تُكتب سطرُ كودٍ واحدة في هذا التشغيل — وهذا هو المخرَجُ الصحيح
لا نقصاً فيه.** ★ **وحدتا المرحلة الأولى المتبقيتان** (`WU-010` الإرسال
والتصدير · `WU-011` تقارير المرحلة الأولى) **حُجبتا بأربعة بنودِ `IQ`**،
⟵ **وكلُّها فجواتُ عقدٍ في التوثيق الحاكم لا ترددٌ تنفيذي:** ⛔ **نصُّ قوالب
الرسائل غيرُ موجودٍ في أيِّ مستند** · ⛔ **ولا مفتاحَ صلاحيةٍ للتصدير في
الكتالوج المعتمد ولا كاتبَ لقيدِ تدقيقه** · ⛔ **ولا مفتاحَ تقريرٍ واحدٍ فيه**
· ⛔ **والطباعةُ الحرارية تطلب إذنَ بلوتوثٍ خارج القائمة المعتمدة في سياسة
الاعتماديات، ولا تُختبَر على محاكٍ أصلاً.**

✅★★★ **والعملُ الفعليُّ للتشغيل كان إصلاحَ نزاهةٍ في `implementation-plan.md`
نفسِه:** ★ **جسدُ `WU-006` كاملاً — 73 سطراً — كان منسوخاً حرفياً ومطابقاً
بايتاً ببايت داخل الوحدات الستَّ عشرةَ المخطَّطة كلِّها** (`WU-010` …
`WU-025`)، ⟵ **فكانت كلُّ واحدةٍ منها تحمل تسعةَ أسطرِ فحوصٍ «✅ نجح»
و`**نتيجة التحقق:** نجح` وجدولَ اختبارِ محاكٍ بمعرَّفِ مستندٍ حيّ** —
⛔ **لوحداتٍ لم يُكتب لها سطرُ كودٍ واحد.** ⛔⛔ **ولم يرصده الحارسُ الآلي
لأنه يفحص وحداتِ `[منجز ✅]` وحدَها** ⟵ **فبقيت `integrity_violations` فارغةً
وهي تحمل ستَّ عشرةَ شهادةَ نجاحٍ كاذبة.** ✅ **أُصلح كاملاً وسُجِّل `DEBT-69`.**

⚠️★★ **وأثرُ ذلك على حالة المشروع:** ★ **المرحلةُ الأولى لم تكتمل** —
⛔ **ولا اختبارَ تكامليَّ لها ولا بوابةَ انتقالٍ للمرحلة الثانية.**

---

## 2. بوابة الدخول — نتيجة الفحص

### 2.1 البوابة العامة (ثمانية شروط — `implementation-playbook.md` §1.1)

| # | الشرط | متحقق؟ | المسار المفحوص |
|:-:|---|:-:|---|
| 1 | المنهجية مُصرَّح بها | ✅ | `docs/21-project-management/development-methodology.md` |
| 2 | العمارة والمكدّس | ✅ | `docs/03-architecture/architecture-overview.md` · `technology-stack.md` |
| 3 | أسلوب الكود والتسمية ومعالجة الأخطاء | ✅ | `docs/04-design/coding-standards.md` · `naming-conventions.md` · `error-handling-strategy.md` |
| 4 | تعريف «منجَز» والتفرّع والالتزام والترقيم | ✅ | `docs/07-development/` · `docs/12-deployment-and-release/versioning-scheme.md` |
| 5 | استراتيجية الاختبار | ✅ | `docs/08-testing-and-qa/test-strategy.md` |
| 6 | سياسة الأسرار | ✅ | `docs/09-security/secrets-management-policy.md` |
| 7 | `srs.md` بمتطلبات مرقّمة | ✅ | `docs/02-requirements/srs.md` · `functional-requirements/FR-*.md` |
| 8 | ⛔ **لا `ADR` بحالة «مقترح» يحكم ما يُنفَّذ الآن** | ✅ | **22 `ADR` كلُّها معتمدة** |

⟹ ✅ **البوابة العامة نجحت — والتنفيذ بدأ فعلاً.**

### 2.2 بوابةُ كلِّ وحدة (`implementation-playbook.md` §1.2)

| الشرط | `WU-010` | `WU-011` |
|---|:-:|:-:|
| 1 — `module-design` للوحدة | ✅ `messaging-design.md` | ✅ `reporting-design.md` |
| 2 — متطلبٌ بمعيار قبولٍ واضح | ✅ `FR-M20` (16 بنداً) | ✅ `FR-M19` (8 بنودٍ عامة + 31 تقريراً) |
| 3 — عقود الواجهات إن انطبقت | ✅ **لا واجهةَ برمجيةً جديدة** | ✅ **لا واجهةَ برمجيةً جديدة** |
| 4 — التخزين (`erd` · `data-dictionary`) | ✅ | ✅ |
| 5 — المصادقة/الصلاحيات | ⛔⛔ **فشل** — ★ **`FR-M20` §3 يسمّي ثلاثَ صلاحيات ولا مفتاحَ لاثنتين منها في الكتالوج المعتمد** | ⛔⛔ **فشل** — ★ **`FR-M19` §4 يشترط «عرضَ تقريرٍ محدَّدٍ لكلِّ تقرير» ولا مفتاحَ تقريرٍ واحدٍ فيه** |

⟹ ⛔ **البند 5 هو ما حوّل الوحدتين إلى `[محجوب]`** — ★ **وقد صُعِّد بنداً
`IQ` لا خُمِّن في الكود** (`IQ-032` · `IQ-034`).

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

⛔ **لا وحدة.** ★ **ولم تُشغَّل أيُّ بوابةِ تحقّقٍ في هذا التشغيل** —
⟵ **فلا كودَ كُتب يُختبَر**، ⛔ **ولا يُدَّعى فحصٌ لم يجرِ** (البروتوكول §و).

### 3.2 وحدات قيد التنفيذ

⛔ **لا وحدة.**

### 3.3 وحدات محجوبة (ولماذا)

| WU | العنوان | محجوب بسبب | ما الذي يلزم لرفع الحجب |
|---|---|---|---|
| `WU-010` | زيادة: الإرسال والتصدير من طرف إلى طرف | `IQ-031` · `IQ-032` · `IQ-033` | ★ **نصُّ القوالب الأربعة** (أو إقرارُ مسودّتنا) · **مفتاحُ صلاحيةِ التصدير وعقدُ قيدِ تدقيقه** · **قرارُ الطباعة الحرارية** (تأجيلٌ · أو طرازٌ محدَّد · أو إخراجُها من النطاق) |
| `WU-011` | زيادة: تقارير المرحلة الأولى | `IQ-032` · `IQ-034` | ★ **مفاتيحُ صلاحيةِ عرض التقارير** (لكلِّ تقرير · أو لكلِّ عائلة · أو مفتاحٌ واحد) · **وعقدُ التصدير نفسُه** |

> ⚠️★★ **وحدٌّ يُقال صراحةً:** ★ **بقيةُ `WU-011` قابلةٌ للبناء اليوم** —
> **أحدَ عشرَ تقريراً تُبنى من الدفتر بلا عقبة** — ⛔ **لكن الزيادة لا
> تُعلَّم منجَزةً بشريحةٍ ناقصة** (`definition-of-done.md`: **من طرفٍ إلى
> طرف**)، ★ **ولا تُبنى شاشةٌ يُعرَض فيها رقمٌ ماليٌّ بلا مفتاحِ تفويضٍ
> يحكم من يراه.**

---

## 4. مخالفات النزاهة المرصودة آلياً

✅ **مخرَجُ `track_implementation.py` بعد الإصلاح:** `"integrity_violations": []`
· `"has_integrity_violations": false` · `"unparsed_lines": []`.

⛔⛔★★★ **ولا يُكتفى بهذا السطر — فهو نفسُه ما ستره العطلُ المُصلَح اليوم:**

| البند | قبل الإصلاح | بعد الإصلاح |
|---|---|---|
| **`integrity_violations`** | `[]` | `[]` |
| ★ **الواقعُ خلفها** | ⛔⛔ **16 وحدةً `[مخطط]` تحمل 9 فحوصٍ «نجح» و`**نتيجة التحقق:** نجح`** | ✅ **16 وحدةً تقرأ `checks: 0` و`verification_result: ''`** |
| **حجمُ الملف** | 4080 سطراً | 2992 سطراً (**−1088**) |

★ **والحارسُ لم يكن كاذباً بل أعمى:** ⟵ **نطاقُه وحداتُ `[منجز ✅]` وحدَها**
(`implementation-playbook.md` §7 البند 5) — ✅ **سُجِّل `DEBT-69`** بعلاجه
المقترح، ⚠️ **وهو تعديلٌ في `upds-docgen` نفسِها يُرفَع لمالك الإضافة.**

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

| IQ | السؤال | الوحدة المتعطّلة | الموضع | لماذا لم يكن قابلاً للاستنتاج |
|---|---|---|---|---|
| `IQ-031` | **نصوصُ القوالب الأربعة لرسائل `M20`** | `WU-010` | `lib/capabilities/oversight/domain/` (⛔ **لم يُكتب**) · `FR-M20.md` §2 | ★ **`FR-M20-06` «حرجة» تفرضها ثوابتَ كودٍ لا تُغيَّر إلا بإصدارٍ جديد**، ⟵ **والنصُّ التزامٌ تجاريٌّ يُرسَل باسم المحل لمقاوتة العميل** — ⛔ **ومصدرُه `M20 §⑤` ليس ضمن `docs/`**، ★ **و`translation-glossary.md` الذي تُحيل إليه `FR-M20` §4 لا يحوي منها حرفاً (فُحص فعلياً)** |
| `IQ-032` | **عقدُ «التصدير»: مفتاحُ الصلاحية وكاتبُ قيدِ التدقيق** | `WU-010` **و**`WU-011` | `permissions-catalog.md` §2.8 · `firestore.rules:714` · `functions/lib/src/callable.dart` | ⛔ **`FR-M20` §3 و`FR-M19` §4 يسمّيان صلاحيتَي تصديرٍ ولا سطرَ لأيٍّ منهما في الكتالوج المعتمد (74 مفتاحاً)** — ★ **واختراعُ مفتاحٍ ممنوعٌ نصّاً** (`RISK-09`) · ⛔ **و`FR-M19-04` يفرض قيدَ `export` بينما التطبيق ممنوعٌ من كتابة `audit_log` مطلقاً** ⟵ **فتلزم عمليةٌ سحابيةٌ جديدة نشرُها بيد المالك** (§ز) |
| `IQ-033` | **الطباعةُ الحرارية 58/80 مم: الحزمةُ والبروتوكولُ وتمثيلُ العربية وإذنُ بلوتوث** | `WU-010` | `technology-stack.md` §3 · `dependency-management-policy.md` §3 · `pubspec.yaml` | ⛔⛔ **تعارضٌ بين مستندَين حاكمَين:** **§3 من سياسة الاعتماديات تحصر أذونات الجهاز في «جهات الاتصال · التخزين»** ⛔ **وبلوتوث ليس منها** · ★ **وتمثيلُ العربية على `ESC/POS` قرارُ بنيةٍ للمخرَج** (نصٌّ أم صورةٌ نقطيّة) · ⛔⛔ **ولا طابعةَ على محاكٍ** ⟵ **فشرطُ §د.2 يتعذّر أصلاً** |
| `IQ-034` | **مفاتيحُ صلاحيةِ عرض التقارير** | `WU-011` | `FR-M19.md` §4 · `permissions-catalog.md` §2 · `security-requirements.md` §1 | ★ **«عرضُ تقريرٍ محدَّدٍ لكلِّ تقريرٍ على حدة» مع 31 تقريراً يعني 31 مفتاحاً** ⛔ **ولا مفتاحَ تقريرٍ واحدٍ في الكتالوج ولا في `security-requirements.md` §1 (فُحص فعلياً)** — ★ **والخيارُ بين 31 و6 و1 يغيّر شجرةَ الصلاحيات وبطاقةَ كلِّ مستخدم و62 اختبارَ تفويض** |

راجع التفاصيل الكاملة والخيارات في:
`docs/00-governance/upds-workflow/implementation-questions.md`
(**`IQ-031` … `IQ-034` — بكلِّ حقولها وخياراتها**).

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

### 6.1 ⛔⛔★★★ العطلُ الأكبر — سجلُّ الخطة نفسُه كان يشهد زوراً

★ **ماذا وجدنا:** ⛅ **جسدُ `WU-006` كاملاً (73 سطراً) منسوخٌ حرفياً ومطابقٌ
بايتاً ببايت في الوحدات الستَّ عشرةَ المخطَّطة** — `WU-010` … `WU-025`
(**قِيس بمقارنةِ المقاطع نصّاً لا بالعين**).

★ **ماذا كان يعرضه كلُّ واحدٍ منها زوراً:** **تسعةُ أسطرِ فحوصٍ «✅ نجح»
بتاريخ 2026-08-27** · **`**نتيجة التحقق:** نجح`** · **جدولُ اختبارِ المحاكي
بثمانيةِ سيناريوهات** ومعرَّفُ مستندٍ حيّ `DST-20260827-0001` · **قائمةُ
ملفاتٍ تخصّ التوزيع.**

★ **كيف عُولج:** ✅ **حُذف المقطعُ المنسوخ من الستَّ عشرةَ جميعاً وحلَّ محلَّه
حقلان صريحان** («⏳ **لم تُنشأ بعد**» · «⏳ **لم تُشغَّل بعد**») — ⛔ **بلا
مساسٍ بأيِّ حقلٍ خاصٍّ بوحدة** (النطاق · معيارُ القبول · المتطلباتُ المرتبطة ·
المستنداتُ الحاكمة · «يلمس نطاقاً حسّاساً» · الملاحظات). ★ **وسُجِّل قسمٌ
مؤرَّخ في `implementation-plan.md` نفسِه** («**إصلاحُ نزاهةٍ في هذا الملف
نفسِه — 2026-08-30**») **و`DEBT-69` في سجل الديون.**

⛔ **ولم يُحذَف ملفٌ ولا وحدةٌ ولا سطرٌ خاصٌّ بوحدة** — ★ **والمحذوفُ نسخةٌ
مكرَّرةٌ من محتوىً أصلُه باقٍ كاملاً تحت `WU-006`.**

### 6.2 ⚠️ تعارضٌ نصّيٌّ بين مستندَين حاكمَين — رُفع ولم يُحسَم ذاتياً

| المستند | ما يقوله |
|---|---|
| `technology-stack.md` §3 | «**الطباعة** \| إيصالات حرارية **58/80 مم عبر بلوتوث**» |
| `integration-architecture.md` §4 | «**الطابعة الحرارية** \| بلوتوث \| **تصدير PDF بديلاً دائماً**» |
| ⛔ `dependency-management-policy.md` §3 | «**أي حزمة تطلب صلاحيات جهاز غير مبرَّرة** ⛔ — **الأذونات المعتمدة: جهات الاتصال · التخزين** فقط» |

⟹ ★ **عُولج بـ`IQ-033`** وفق `implementation-playbook.md` §6 (**تعارضٌ بين
مستندَين حاكمَين ⟵ لا يُحسَم ذاتياً**) — ⛔ **ولا انحرافَ صامت.**

### 6.3 ⚠️ تعارضٌ ظاهرٌ داخل `M20` نفسِها

★ **`FR-M20-16`: «الإرسال إجراء واجهة فقط — لا كتابة في قاعدة البيانات
إطلاقاً»** · ★ **بينما `messaging-design.md` §9: «أما تصدير PDF فيُسجَّل
بالإجراء «تصدير»»**. ⟹ ★ **محلولٌ منطقياً** (الإرسالُ ≠ التصدير)
⛔ **وغيرُ محسومٍ نصّاً** — ★ **رُفع ضمن `IQ-032` البند 3** ⛔ **ولم يُعدَّل
`FR-M20` ذاتياً** (**متطلبٌ محكومٌ بحَوْكمة** — UPDS-05 §4.3).

### 6.4 ⚠️ ملاحظةُ اتّساقٍ في خريطة المراحل — ⛔ لا تعديلَ ذاتيّاً

★ **`product-roadmap.md` §1 يضع `M20` في المرحلة الأولى** ⟵ **بينما معيارُ
اكتمالها `AT-01`…`AT-37` · `AT-55`…`AT-62` · `AT-67`** ⛔ **لا يشمل
`AT-63`…`AT-64` الخاصَّين بـ`M20`** — ★ **وهما مذكوران في معيار اكتمال
**المرحلة الثالثة****، **بينما `requirements-traceability-matrix.md` §4
يُسنِدهما إلى `M20` في المرحلة **الأولى**.** ⚠️ **سُجِّل للعلم ولم يُحسَم:**
⛔ **لا أثرَ له على تنفيذٍ اليوم** (`WU-010` محجوبةٌ لأسبابٍ أخرى)، ★ **ويُرفع
عند بوابة المرحلة الأولى.**

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

| # | البند | الحالة |
|:-:|---|---|
| 1 | **الأسئلة الأربعة `IQ-031` … `IQ-034`** | ⛔ **المالك حصراً** (الفئة 2 — بروتوكول التشغيل §ب) — ★ **ولا تُنفَّذ بتخمين** |
| 2 | ⚠️ **`IQ-033` الخيار ج** (**إخراجُ الطباعة الحرارية من نطاق الإصدار**) | ⚠️ **إن اختيرَ فهو تعديلٌ على `technology-stack.md` §3 وقناةِ تكاملٍ معلنة** ⟵ ★ **يُوثَّق قراراً صريحاً** ⛔ **لا حذفاً صامتاً** |
| 3 | ⚠️ **`IQ-032` الخيار د** (**تأجيلُ قيدِ تدقيق التصدير**) | ⚠️ **مخالفةٌ صريحةٌ لـ`FR-M19-04`** ⟵ ★ **تُسجَّل ديناً معلَناً إن اختيرت** |
| 4 | ⛔⛔ **بوابةُ النشر الإنتاجي** | ⛔ **لم تُقترب** — ★ **ولا شيءَ نُشر على الإنتاج في هذا التشغيل ولا قبله** |
| 5 | ⛔ **أيُّ عمليةِ `git` خارجية** (`push` · `PR` · `merge`) | ⛔ **لم تُنفَّذ ولم تُطلَب** (UPDS-05 §9 البند 8) |

⛔ **ولم يُعتمَد أيُّ بندٍ ذاتياً** (UPDS-05 §2 و§4).

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

★ **الوحدتان المعالَجتان في هذا التشغيل موسومتان «يلمس نطاقاً حسّاساً: نعم»
— وكلتاهما `[محجوب]` فلا نشرَ لهما أصلاً:**

| WU | لماذا حسّاسة |
|---|---|
| `WU-010` | ⛔ **يُخرِج بيانات مالية وشخصية خارج التطبيق** — ★ **رسائلُ ومستنداتٌ تصل مقاوتة العميل** · ★ **وتنطبق عليها بوابةُ بصمة المطوّر الخماسية** (⛔ **لا إشارةَ لجهة التطوير في أيِّ مخرَجٍ يصل عملاء العميل** — `developer-identity.md` §5 · `FR-SYS-30`) |
| `WU-011` | ⛔ **تعرض أرقاماً يُبنى عليها قرارٌ مالي** ومحكومةٌ بنطاق مصادر القارئ (`FR-M19-02` · `AT-68`) |

⛔⛔★★ **وما لم يتغيّر بحرف:** ★ **`WU-004` تبقى محتاجةً مراجعةً أمنيةً بشريةً
قبل أيِّ نشرٍ إنتاجي** (`implementation-report-2026-08-26.md` §7–§8 ·
بروتوكول التشغيل §ب.1) — ⛔ **ولا يُغني عنها نجاحُ أيِّ فحصٍ آلي.**
★ **وبقيةُ الوحدات الحسّاسة المنجَزة كما هي في تقاريرها.**

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث | الإثبات |
|---|---|---|
| `docs/00-governance/upds-workflow/implementation-plan.md` | ★ **إصلاحُ نزاهةٍ** (§6.1) + **حجبُ `WU-010` و`WU-011`** + **إضافةُ مستندات الهوية البصرية الثلاثة لحقل «المستندات الحاكمة» فيهما** (بروتوكول التشغيل §ح) | ★ **قسمٌ جديد «إصلاحُ نزاهةٍ في هذا الملف نفسِه — 2026-08-30» قبل §0** · **`**محجوب بسبب:**` في الوحدتين** · **4080 ⟶ 2992 سطراً** |
| `docs/00-governance/upds-workflow/implementation-questions.md` | ★ **أربعةُ بنودٍ جديدة** | **`IQ-031` … `IQ-034`** — ★ **مقروءةٌ آلياً:** `track_completion.py` ⟵ `IQ: total 34 · open 4 · unparsed []` |
| `docs/03-architecture/risks-and-technical-debt-register.md` | ★ **دَينٌ جديد** | **`DEBT-69`** — **السطر 247**، ★ **بعد `DEBT-68` مباشرةً** |
| `CLAUDE.md` | ★ **تصحيحُ حقيقتين صارتا خاطئتين** | **السطر 6** (`IQ` مفتوحة: 4 · 30 من 34) · **السطر 149** (`WU-010` و`WU-011` محجوبتان) — راجع §10 |

⛔ **ولم يُحدَّث غيرُها — ولماذا صراحةً:**

| المستند | لماذا لم يُمَسّ |
|---|---|
| `requirements-traceability-matrix.md` | ⛔ **لا ميزةَ أُنجزت** ⟵ **فلا عمودَ «كود» يتغيّر** |
| `openapi.yaml` · `endpoints/` | ⛔ **لا واجهةَ برمجيةً جديدة** |
| `erd.md` · `data-dictionary.md` · `migrations-log.md` | ⛔ **لا تغييرَ في المخطَّط** |
| `module-design/messaging-design.md` · `reporting-design.md` | ⛔ **حالتُهما «مخطَّط» وتبقى** — ★ **ولا تُرفَع إلى «منفَّذ» بلا كود** |
| `firestore.rules` | ⛔ **لم تُمَسّ بحرف** |
| `troubleshooting-guide.md` | ⛔ **لا عطلَ تشغيليٍّ متكرِّرٍ حُلّ** — ★ **والعطلُ المُصلَح سجلُّ خطةٍ لا مشكلةُ بيئة**، ⟵ **وموضعُه `DEBT-69`** |

---

## 10. حالة `CLAUDE.md`

✅ **حُدِّث — ونقطةُ التحوّل مذكورةٌ صراحةً:** ⛔ **ليست «اكتمالَ وحدة»** بل
★ **أن الكتلة المُدارة كانت تحمل حقيقتين صارتا خاطئتين بنهاية هذا التشغيل**،
⟵ **وتركُهما يُضلِّل كلَّ جلسةٍ قادمة** (بروتوكول التشغيل §و).

| البند | القيمة |
|---|---|
| **الأداة** | ★ **`sync_claude_md.py` وحدَها** — ⛔ **ولا تحريرَ يدوي** (`claude-md-management.md` §5) |
| **`action`** | `managed-block-updated` |
| **`human_content_lines_preserved`** | **20** — ✅ **كما كانت قبل التشغيل بالضبط** |
| **`new_total_lines`** | **245** — ✅ **بلا زيادةِ سطرٍ واحد** (⟵ **استبدالٌ داخل سطرين قائمين**) · ✅ **ودون سقف `AM-005` البالغ 250** |
| **ما تغيّر — حرفياً** | **السطر 6:** «✅ **ولا بندَ `IQ` مفتوح — 30 من 30 محسومة**» ⟶ «⚠️ **وأربعةُ بنودِ `IQ` مفتوحةٌ من التشغيل 11 (`IQ-031`…`IQ-034`) — 30 من 34 محسومة**» · **السطر 149:** «⛔ **ولا محجوبةً ولا تحتاج إعادة تحقق**» ⟶ «⛔ **ولا تحتاج إعادة تحقق** — ⚠️ **ومحجوبتان بأربعة بنود `IQ`: `WU-010` و`WU-011`** ⟵ **فالمرحلة الأولى لم تكتمل**» |
| ⚠️ **تحذيرُ الحجم** | **قائمٌ كما كان** (245 > 200 الإرشادي) — ★ **استثناءٌ موثَّقٌ صراحةً بـ`AM-005`** ⛔ **لم يُوسَّع في هذا التشغيل** |

---

## 11. الخطوة التالية الموصى بها

⛔⛔★★★ **التنفيذُ متوقّفٌ على أربعة أجوبةٍ لا على وقتٍ ولا على كود.**

★ **① أجب البنود الأربعة** في
`docs/00-governance/upds-workflow/implementation-questions.md` —
★ **كلٌّ منها بخياراتٍ جاهزةٍ فاختيارُ حرفٍ يكفي:**

| البند | ★ أقصرُ جوابٍ ممكن |
|---|---|
| `IQ-031` | **«الخيار ب»** ⟵ **نُقرّ مسودّةَ القوالب المعروضة ونكملها على نسقها** · **أو ألصق نصَّ `M20 §⑤`** |
| `IQ-032` | **«الخيار أ»** ⟵ **مفتاحٌ واحدٌ `documentExport` + عمليةٌ سحابيةٌ `logExport`** |
| `IQ-033` | **«الخيار أ»** ⟵ **تُخرَج الطباعةُ الحرارية إلى زيادةٍ مستقلة**، ⟹ ✅ **فتُرفَع حجبةٌ من ثلاث عن `WU-010` فوراً** |
| `IQ-034` | **«الخيار ب»** ⟵ **ستةُ مفاتيحَ بعائلات `FR-M19` §2 نفسِها** |

★ **② ثم شغّل `/upds-docgen:implement`** — ⟵ **فتُرحَّل الأجوبةُ إلى الكود
والمستندات، ويُرفَع الحجبُ، وتُستأنف حلقةُ المرحلة الأولى من `WU-010`.**

⚠️★★ **وما يلزم توقّعه بعد الإجابة:** ★ **إن أُقرّ `IQ-032` بالخيار أ أو ب،
فستلزم عمليةٌ سحابيةٌ جديدة** ⟹ ⛔ **ونشرُها بيدك وحدك** (بروتوكول التشغيل
§ز) — ★ **وسيُسلَّم أمرُ النشر جاهزاً للنسخ** ⛔ **بلا تنفيذ.**

⚠️ **وبندٌ منفصلٌ لا يخصّ هذا المشروع:** ★ **`DEBT-69` يقترح حارساً ثانياً في
`track_implementation.py`** («**وحدةٌ ليست `[منجز ✅]` تحمل نتيجةَ تحقّقٍ أو
فحصاً ناجحاً ⟵ مخالفةُ نزاهة**») — ⚠️ **وهو تعديلٌ في `upds-docgen` نفسِها
يُرفَع لمالك الإضافة**، ⛔ **ولا يُنفَّذ من داخل المشروع.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
