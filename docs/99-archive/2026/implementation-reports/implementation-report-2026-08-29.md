# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-29

**رقم تشغيل التنفيذ:** 38
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة** — شريحة رأسية كاملة)
**تقدّم التنفيذ:** 33.3٪ (9 منجَزة من 27) — ★ **و`WU-007` قيد التنفيذ**
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** **0** — ✅ **و`IQ-027` حُسم في هذا التشغيل** (26 ⟵ 27 محلولاً)

---

## 1. ملخص تنفيذي

★★★ **رُفع الحجب عن `WU-007` وبُنيت كاملةً من طرف إلى طرف**: **`IQ-027` حُسم
بقرار المالك (الخيار أ)** ⟵ **ومبالغ تسوية الضمار الثلاثة نُقلت إلى
`pricing/current`** ⛔ **بلا هجرة بيانات ولا `ADR` جديد** (تطبيقٌ لـ`ADR-0011`
كما هو). ★ **ثم بُنيت المقبوضات كاملةً**: طبقةُ النطاق (**التوزيع التلقائي ·
الفائض ونطاقه · تحقّق السطر والتاريخ · الإيداع**) · **أربعُ عمليات سحابية**
(**37 عمليةً في الموجّه**) · **تطبيقُ الفائض عند إنشاء الضمار** (`FR-M12-11`) ·
**وشاشةُ المقبوضات** بمسار كتابةٍ سحابيٍّ خالص.

✅★★ **والبوابة الآلية كاملةٌ وناجحة: 1483 اختباراً · ٠ فاشلاً** ·
✅ **و`analyze` نظيفٌ في الحزم الثلاث** · ⛔⛔ **و`firestore.rules` لم تُمَسّ
بحرف** (**مقيسٌ بـ236 اختبار حماية**).

⛔⛔★★★ **ولم تُعلَّم `WU-007` منجَزة — والسبب مُسمّى لا عام:** **بوابةُ §د.2
(اختبار المحاكي) تشترط مسارات كتابةٍ منشورة**، ★ **والنشرُ حصرُ المالك**
(بروتوكول التشغيل §ز · 2026-08-28) — ⟵ **والمنشور اليوم `00018-78c` بـ33
عملية** (**مقيسٌ**) ⛔ **والأربع الجديدة ليست فيه.**

---

## 2. بوابة الدخول — نتيجة الفحص

### 2.1 البوابة العامة (ثمانية شروط — `implementation-playbook.md` §1.1)

| # | الشرط | متحقق؟ | المسار المفحوص |
|:-:|---|:-:|---|
| 1 | المنهجية مُصرَّح بها | ✅ | `docs/21-project-management/development-methodology.md` |
| 2 | العمارة والمكدس | ✅ | `docs/03-architecture/architecture-overview.md` · `technology-stack.md` |
| 3 | معايير الكود والتسمية ومعالجة الأخطاء | ✅ | `docs/04-design/coding-standards.md` · `naming-conventions.md` · `error-handling-strategy.md` |
| 4 | تعريف «منجَز» والتفرّع والالتزام والإصدار | ✅ | `docs/07-development/*` · `docs/12-deployment-and-release/versioning-scheme.md` |
| 5 | استراتيجية الاختبار | ✅ | `docs/08-testing-and-qa/test-strategy.md` |
| 6 | سياسة الأسرار | ✅ | `docs/09-security/secrets-management-policy.md` |
| 7 | `srs.md` بمتطلبات مرقّمة | ✅ | `docs/02-requirements/srs.md` |
| 8 | ⛔ **لا `ADR` بحالة «مقترح» يحكم ما يُنفَّذ** | ✅ | **الاثنان والعشرون كلها `مقبول`** — مقيسٌ بـ`grep` على `**الحالة:**` |

### 2.2 بوابة `WU-007` (خمسة شروط — §1.2)

| # | الشرط | متحقق؟ | المسار |
|:-:|---|:-:|---|
| 1 | تصميم الوحدة موجود | ✅ | `docs/04-design/module-design/settlement-design.md` |
| 2 | المتطلبات بمعايير قبول | ✅ | `docs/02-requirements/functional-requirements/FR-M12.md` (23 متطلباً) |
| 3 | عقود الواجهات | ✅ | `docs/05-api/api-overview.md` §3 — ⛔ **ولا نقاط نهاية** (`ADR-0001`) |
| 4 | مستندات التخزين | ✅ | `docs/06-database/erd.md` · `data-dictionary.md` · `schema/receipts.md` · `schema/dealer-ledger.md` |
| 5 | سياسة المصادقة | ✅ | `docs/09-security/authentication-policy.md` · `permissions-catalog.md` §2.5 |

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

**لا توجد.** ⛔⛔★★ **ولا تُدرَج `WU-007` هنا** — ★ **بوابةُ §د.2 لم تُجتَز**
(راجع §3.2)، ⟵ **وإدراجُها كان سيكرّر بالضبط العطلَ الذي صُحِّح في هذا التشغيل**
(نسخةُ حقول `WU-006` تحت بندٍ محجوب — راجع §6).

### 3.2 وحدات قيد التنفيذ

| WU | العنوان | ما أُنجز فعلياً | ما تبقّى |
|---|---|---|---|
| **`WU-007`** | زيادة: المقبوضات وحساب المقوت من طرف إلى طرف | ★ **الكود كاملاً في الطبقات الثلاث** + **الاختبارات** + **التوثيق التابع** — ✅ **وكل الفحوص الآلية ناجحة** | ⏳ **نشرُ الفهارس** ⟵ ⏳ **نشرُ خدمة العمليات** ⟵ ⏳ **اختبار المحاكي §د.2** — ⛔ **ثلاثتُها تحتاج المالك** |

**الفحوصات التي شُغِّلت فعلياً — بأرقامها المقاسة (2026-08-29):**

| الفحص | الأمر | النتيجة |
|---|---|---|
| طبقة النطاق — تحليل | `cd packages/qtms_domain && dart analyze` | ✅ **`No issues found!`** |
| طبقة النطاق — اختبار | `cd packages/qtms_domain && dart test` | ✅ **412 · ٠ فاشلاً** (**+47**) |
| السحابة — تحليل | `cd functions && dart analyze` | ✅ **`No issues found!`** |
| السحابة — اختبار | `cd functions && dart test` | ✅ **461 · ٠ فاشلاً** (**+69**) |
| التطبيق — تحليل | `flutter analyze` | ✅ **`No issues found!`** |
| التطبيق — اختبار | `flutter test` | ✅ **374 · ٠ فاشلاً** (**+14** · **وبوابات التصميم الأربع ضمنها**) |
| قواعد الحماية | `cd test/security-rules && npm test` | ✅ **236 · ٠ فاشلاً** — ⛔⛔ **ولم تُمَسّ القواعد** |
| **الإجمالي** | — | ★★ **1483 اختباراً · ٠ فاشلاً** |
| ⛅ **حالة التجريبية** | `gcloud run services describe qtms-callables` | ⛅ **`qtms-callables-00018-78c`** — ⛔ **بـ33 عملية، والأربع الجديدة ليست فيه** |
| ⛅ **المحاكي** | `adb devices` | ⛅ **`emulator-5554` · حالة `device`** — ★ **جاهزٌ ومتصل** |

⛔⛔★★★ **ولماذا لم يُشغَّل اختبار المحاكي رغم جاهزية الجهاز:** ★ **مسارات
الكتابة الأربع غير منشورة** — ⟵ **فتشغيلُه اليوم يقيس غيابَ نشرٍ لا صحةَ
كود**، ⛔ **ونتيجتُه لا تُثبت شيئاً عن الزيادة.**

### 3.3 وحدات محجوبة (ولماذا)

**لا توجد.** ✅★★★ **و`WU-007` كانت الوحيدة المحجوبة ورُفع حجبُها في هذا
التشغيل** — ⟵ **فعددُ المحجوبات صار ٠** (**مقيسٌ بـ`track_implementation.py`**).

---

## 4. مخالفات النزاهة المرصودة آلياً

**لا مخالفات** — `integrity_violations: []` · `unparsed_lines: []`
(**مقيسٌ بـ`track_implementation.py` بعد تحرير الخطة**).

⚠️⚠️★★★ **لكنّ التشغيل رصد مخالفةً حقيقيةً لم يرصدها السكربت** — راجع §6 ①:
★ **بندُ `WU-007` كان يحمل — وهو `[محجوب]` — نسخةً حرفية من فحوصات `WU-006`
و«نتيجة التحقق: نجح».** ⛔ **والسكربت لا يفحص إلا `[منجز ✅]`** — ⟵ **فالحارس
الآلي لا يُغني عن قراءة ما كُتب** (بروتوكول التشغيل §و).

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

**لم يُصعَّد بندٌ جديد في هذا التشغيل** — ★ **وكلُّ قرارٍ التبس كان محسوماً
في مستندٍ قائم أو في قاعدةٍ ملزِمة في `CLAUDE.md`** (راجع §6 ②).

✅★★★ **وبندٌ واحدٌ حُسم ورُحِّل:**

| IQ | السؤال | الوحدة | القرار | رُحِّل إلى |
|---|---|---|---|---|
| **`IQ-027`** | حقول تسوية الضمار في المستند الأب تكشف مبلغاً لمن لا يملك رؤيته | `WU-007` (**ومعها `WU-013`**) | ★★ **الخيار أ** — **نقلُ المبالغ الثلاثة إلى `pricing/current` وإبقاءُ `settlementStatus` في الأب** | `functions/lib/src/distribution_handler.dart` (`readStoredSettlement`) · `stored_settlement_test.dart` (**7 اختبارات ارتداد**) · `schema/distributions.md` · `data-dictionary.md` · `erd.md` · `distribution-design.md` · `DEBT-43` **مُغلَق** |

★★ **وأثرُه المقاس:** ⛔ **لا `ADR` جديد** (**تطبيقٌ لـ`ADR-0011` لا انحرافٌ
عنه**) · ⛔ **ولا مفتاحُ صلاحيةٍ جديد** (**الكتالوج 74 كما هو**) · ⛔ **ولا
هجرةُ بيانات** (**`WU-006` لم تكتب حقلَ تسويةٍ واحداً عمداً**) · ⛔ **ولا
تغييرَ في الفهرس القائم** (**`settlementStatus` لم يتحرك**).

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

### ① ⛔⛔★★★ عطلُ نزاهةٍ في `implementation-plan.md` نفسِه — وقد صُحِّح

```text
⚠️ انحراف توثيقي مكتشَف
الموقع: docs/00-governance/upds-workflow/implementation-plan.md (2001–2114)
نوع الانحراف: مستند لا يعكس واقع الكود — بأخطر صورة
التفصيل: بندُ WU-007 — وهو [محجوب] ولم يُكتب له سطرٌ واحد — كان يحمل نسخةً
         حرفية من حقول WU-006: «الملفات المنتَجة» و«فحوصات التحقق» بتواريخها
         وأرقامها، وجدولَ اختبار المحاكي بثمانية سيناريوهات، و«نتيجة التحقق:
         نجح». ⟵ أي أن وحدةً لم تبدأ كانت تُظهِر تحقّقاً ناجحاً كاملاً.
         ⛔ ولم يرصده track_implementation.py لأن فحص النزاهة يستهدف
         [منجز ✅] وحدها.
الإجراء المنفَّذ: أُزيلت النسخة كاملةً وأُعيدت كتابة البند بما قيس في هذا
         التشغيل وحده — والوحدة [قيد التنفيذ] لا [منجز ✅].
```

★★ **والدرسُ يُسجَّل:** ⛔ **الحارس الآلي يفحص ما وُسِم منجَزاً** — ⟵ **وبندٌ
محجوب يستطيع حمل أي ادّعاء بلا أن يُرصَد**، ★ **فقراءةُ ما كُتب شرطٌ لا
تكميل** (بروتوكول التشغيل §و).

### ② ⛔⛔★★★ تعارضٌ بين `indexing-strategy.md` وقواعد الحماية — وقد حُسم بقاعدةٍ قائمة

```text
⚠️ انحراف توثيقي مكتشَف
الموقع: docs/06-database/indexing-strategy.md (السطران 31 و112)
نوع الانحراف: مستند لا يعكس ما تفرضه قواعد الحماية فعلياً
التفصيل: يصف فهرس «الضمارات المفتوحة للقبض» بأنه
         `dealerId ↑ · settlementStatus ↑ · stockDate ↑` ويقول صراحةً
         «لكل مقوت عبر كل المصادر» — ⛔ واستعلامٌ كهذا (بلا تقييد sourceId)
         ترفضه القاعدة كاملاً لأن storedInScope() تقرأ
         resource.data.sourceId، ولو كان نطاق المستخدم شاملاً.
الإجراء المنفَّذ: أُضيف الفهرس المطابق للاستعلام الفعلي
         (`dealerId ↑ · sourceId ↑ · settlementStatus ↑ · stockDate ↑`)،
         وصار الاستعلام واحداً لكل مصدرٍ مع دمجٍ محلي، و«الكل» قائمةً
         مُعدَّدة يبنيها المزوّد — وحُدِّث المستند.
         ⛔ ولم تُخفَّف القاعدة بحرف.
```

⛔⛔★★★ **ولم يُصعَّد بنداً `IQ` عمداً — والسبب مُسمّى:** ★ **القرار محسومٌ
حرفياً في `CLAUDE.md`** («**والعلاج تقييدُ الحقل وفهرسٌ ببادئته ⛔ لا تخفيفُ
القاعدة**») **وفي ثلاث مقايسات سابقة** (`IQ-024` · `WU-008` · `DEBT-40`) —
⟵ **فهو استنتاجٌ من قاعدةٍ ملزِمة قائمة** ⛔ **لا اختيارٌ بين بدائل متكافئة**
(`implementation-playbook.md` §5).

⚠️⚠️★★ **وقيمةُ هذا الرصد أنه سبق المحاكي:** ★ **العطل نفسُه** (`DEBT-40`)
**كلَّف `WU-006` جولةَ اكتشافٍ حيّة** — ⟵ **وهنا رُصد بقراءة القاعدة قبل
كتابة الاستعلام**، ⛔ **ولا اختبارَ آليٌّ واحدٌ كان سيكشفه** (البدائل لا تُنفِّذ
قواعد حماية).

### ③ ⚠️ ثغرةٌ في `schema/distributions.md` — وقد سُدَّت

★ **كان يذكر `unitPrice` في السطور و`debtValue` في المستند الأب** — ⛔ **وهو
ما نقله `ADR-0011` إلى `pricing/current` منذ اعتماده**، ⟵ **والكود يتبع الـ`ADR`
و`data-dictionary.md` معاً.** ✅ **صُحِّح المستند وأُضيف له قسمُ المستند الفرعي**
— ★ **وهو مستندُ مخططٍ `AUTO`/`HYBRID`** ⛔ **لا بوابةَ حَوْكمة** (UPDS-05 §3.1).

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

⛔⛔★★★ **ولا `ADR` جديد ولا `RFC` ولا تعديلَ `srs.md` في هذا التشغيل** —
★ **ولم يُعتمَد شيءٌ ذاتياً.**

★★ **وما ينتظر المالك حصراً:**

| # | البند | الفئة | لماذا يملكه المالك |
|:-:|---|---|---|
| 1 | ⛔⛔ **نشرُ الفهارس على التجريبية** | **بيئة** | ★ **أمرُ نشر** — ⟵ **يُسلَّم جاهزاً** (§ج · §ز) |
| 2 | ⛔⛔ **نشرُ خدمة العمليات على التجريبية** | ★★ **الفئة 1 بحكم §ز** | ⛔ **لا `run deploy` من أي جلسة آلية** — **قرار 2026-08-28** |
| 3 | ⛔⛔★★ **مراجعةُ `WU-007` الأمنية قبل أي نشر إنتاجي** | **الفئة 1 البند 5** | ★ **تحصيلٌ نقدي وذمم ومسارُ إيداع بنكي** — راجع §8 |

⛔⛔ **ولم يُطلَب اعتمادُ نشرٍ إنتاجي في هذا التشغيل** — ★ **ولا مقتضى له**:
**لم يُنشَر شيءٌ على التجريبية بعد، فضلاً عن الإنتاج.**

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

★★★ **`WU-007` — المقبوضات وحساب المقوت** · **`يلمس نطاقاً حسّاساً: نعم`**:

| # | وجهُ الحساسية | ما يجب أن يُراجَع بشرياً |
|:-:|---|---|
| 1 | ★★ **تحصيلٌ نقدي فعلي** | **أن المبلغ الواصل يقع على ضماره ومصدره الصحيحين** — ⟵ **وحركةٌ في مصدرٍ خاطئ تُسدِّد ديناً ليس هو** |
| 2 | ★★★ **إسقاطُ دَينٍ من دفتر الذمم** | **أن الإلغاء يردّ المسدَّد ولا يُضاعفه** — ★ **والمعرّفات مشتقّة فإعادةُ الإرسال تكتب فوق الحركة لا بجانبها** |
| 3 | ★★ **مسارُ إيداعٍ بنكي بصلاحيتين** | **أن `receiptDepositConfirm` وحده لا يكفي بلا `receiptDepositView`** — ★ **ومُختبَرٌ آلياً** ⛔ **ويُراجَع بشرياً** |
| 4 | ★★★ **رصيدُ الفائض** | **أن تعديل السند يطرح مساهمتَه القديمة** — ⟵ **ومراكمةٌ بلا طرحٍ تمنح مقوتاً رصيداً لم يدفعه** (**له اختبارُ ارتداد**) |
| 5 | ★★ **نطاقُ المصادر** | **أن سند «الكل» يُفحَص على كل مصدرٍ مسّه لا على أولها** (`E-35`) |

⛔⛔★★ **والتحقق الآلي شرطٌ ضروري لا كافٍ لهذه الوحدة** —
`implementation-playbook.md` §7: ★ **1483 اختباراً لا تُغني عن عينٍ بشرية على
مالٍ يُقيَّد في دفتر ذمم.**

★ **والوحدات الحسّاسة السابقة كما هي** (`WU-000` · `WU-002`…`WU-006` ·
`WU-008`) — ⛔ **ولم تُمَسّ مراجعةُ `WU-004` الأمنية المعلَّقة.**

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث (UPDS-03 §4) | الإثبات |
|---|---|---|
| `docs/06-database/schema/distributions.md` | ★★ **نقلُ حقول** (`IQ-027`) + **سدُّ ثغرةِ `ADR-0011`** | **قسمٌ جديد `pricing/current`** · السطور 14–45 |
| `docs/06-database/data-dictionary.md` | **نقلُ حقول** + **حقولُ `dealer_surplus`** | §`distributions` · §4 (`dealer_surplus`) |
| `docs/06-database/erd.md` | **نقلُ الحقول الثلاثة بين الكيانين** | `DISTRIBUTION` ⟵ كيانُ التسعير |
| `docs/06-database/indexing-strategy.md` | ★★★ **فهرسٌ جديد + انحرافٌ مُصحَّح** | السطران 31 و112 |
| `docs/06-database/migrations-log.md` | ★★ **سطورٌ جديدة (12–16)** — **مجموعتان جديدتان وكاتبٌ ثانٍ وفهرس** | §3-ح |
| `docs/04-design/module-design/distribution-design.md` | **نقلُ حقول التسوية** | §2 |
| `docs/04-design/module-design/settlement-design.md` | ★ **الحالة: مخطَّط ⟵ منفَّذ جزئياً** | ترويسة المستند |
| `docs/05-api/api-overview.md` | ★★★ **أربعُ عمليات مستدعاة جديدة** | §3.1-ح (**جديد**) |
| `docs/02-requirements/requirements-traceability-matrix.md` | **عمود «الكود» لـ`FR-M12`** | صفُّ `FR-M12-01`…`23` |
| `docs/03-architecture/risks-and-technical-debt-register.md` | ✅ **`DEBT-43` مُغلَق** · ⚠️ **`DEBT-59` جديد** | السطران 177 و178 |
| `docs/00-governance/upds-workflow/implementation-questions.md` | ✅ **`IQ-027` ⟵ `[تم الحل ✅]`** | البند + **`رُحِّل إلى:`** |
| `docs/00-governance/upds-workflow/implementation-plan.md` | ★★★ **إعادةُ كتابة بند `WU-007`** + **تصحيحُ نزاهة** | السطور 2001–2125 |
| `firestore.indexes.json` | ★★ **فهرسٌ واحد جديد — والإجمالي 37** | **`JSON` صالحٌ مقيس** |

⛔⛔★★ **و`firestore.rules` لم تُمَسّ بحرف** — ★ **مقيسٌ لا مُدَّعى**:
**236 اختبار حماية ناجح.**

---

## 10. حالة `CLAUDE.md`

**لم يُحدَّث في هذا التشغيل.** ⛔⛔★★ **وهذا قرارٌ لا سهو:**
★ **نقطةُ التحوّل المعرَّفة هي «اكتمال وحدة عمل»** (`claude-md-management.md`
§2 البند 5) — ⟵ **و`WU-007` لم تكتمل بعد** (بوابةُ §د.2 معلَّقة)، ⛔ **وتحديثُه
اليوم كان سيُعلن اكتمالاً لم يقع.**

★ **ويُحدَّث فور اجتياز اختبار المحاكي** — **بسطرٍ واحد**: **الوحدة العاشرة
المنجَزة وعدّادُ الاختبارات** ⛔ **بلا توسيعٍ لحجم الملف** (`AM-005`: **السقف
250 سطراً**).

---

## 11. الخطوة التالية الموصى بها

### ★★★ ① أمرا البيئة المطلوبان من المالك (بروتوكول التشغيل §ج · §ز)

**بالترتيب الملزِم — ⛔ ولا يُعكَس:** ★ **الفهارس أولاً** (**فالاستعلام يفشل
بدونها**) **ثم الخدمة.**

⛔⛔★★★ **ولا تُنشَر `firestore.rules`** — ★ **لم تتغيّر أصلاً** (**مقيس**)،
⟵ **ونشرُها بلا داعٍ يُدخِل خطراً بلا مقابل.**

### ★★ ② بعد وصول المخرجات

★ **يُستأنف من حيث توقّف** (§ج بند 6): **اختبارُ المحاكي §د.2 لـ`WU-007`**
بسيناريوهات `AT-33`…`AT-37` (**مسارات النجاح والرفض معاً**) — ⟵ **ثم تُعلَّم
الوحدة `[منجز ✅]` ويُحدَّث `CLAUDE.md`** ⛔ **ولا قبل ذلك.**

⚠️⚠️★★ **وتوقُّعٌ معلَن لا تفاؤل:** ★ **`WU-006` كشفت عطلين لم يكشفهما 1265
اختباراً آلياً** (`DEBT-40` · `DEBT-41`) — ⟵ **ويُتوقَّع مثلُه هنا**،
★ **وهذه الزيادة تكتب في دفتر ذمم بأربعة مسارات جديدة.**

### ★ ③ ثم بقيةُ المرحلة الأولى

**`WU-009`** (مركز الإدخالات المعلّقة) · **`WU-010`** (الإرسال والتصدير) ·
**`WU-011`** (تقارير المرحلة الأولى) — ⟵ **ثم بوابةُ المرحلة** (`AT-01`…`AT-37` ·
`AT-55`…`AT-62` · `AT-67`) **واختبارُها التكاملي §د.3.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
