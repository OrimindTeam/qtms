# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-30

**رقم تشغيل التنفيذ:** 12 (اليوم) — ★ **التشغيل الثاني عشر في 2026-08-30**
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة** — شريحة رأسية كاملة)
**تقدّم التنفيذ:** ✅ **44.4٪ (12 منجَزة من 27)** — ✅★★★ **و`WU-010` أُغلقت بعد استيفاء §د.2 كاملةً (2026-08-31)**
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** **0** — ✅★★★ **34 من 34 محسومة** (⟵ **كانت أربعةٌ مفتوحة عند بدء التشغيل**)

---

## 1. ملخص تنفيذي

★★ **رُحِّلت أجوبةُ المالك الأربعة** (`IQ-031` … `IQ-034`) **فارتفع الحجب عن
`WU-010` و`WU-011` معاً**، ★ **وبُنيت `WU-010` (الإرسال والتصدير · `M20`)
كاملةً في الطبقات الثلاث:** **قوالبُ الرسائل الأربعة ثوابتَ نطاقٍ خالصة** ·
**راسمُ PDF عربيٌّ من التوكنز نفسِها** · **فاتحُ واتساب/الرسائل** · **مفتاحُ
صلاحيةٍ خامسٌ وسبعون `documentExport`** · **وعمليةٌ سحابية `logExport`** — ★ **وهي
أولُ عمليةٍ في النظام تكتب قيدَ تدقيقٍ بلا مستندٍ مصاحب.**

★ **البوابةُ الآلية 1689 ناجحاً · ٠ فاشلاً** (**+91**)، ⛔⛔ **و`firestore.rules`
لم تُمَسّ بحرف.** ⛅ **وسبعةُ سيناريوهاتٍ مقيسةٌ حيّاً على `Pixel_6_API_36`** —
⟵ ⛔★★ **وكشفت عطلاً حقيقياً لم يكشفه أيٌّ من 1685 اختباراً** (`https://wa.me`
يجعل «واتساب غير مثبَّت» فرعاً لا يقع أبداً).

⏳ **ولم تُعلَّم `WU-010` منجَزة:** ★ **ثلاثةُ سيناريوهات §د.2 (التصدير والمشاركة
والتسجيل) متعذّرةٌ بإجراءين يملكهما المالك وحده** — **نشرُ خدمة العمليات**
و**مزامنةُ صلاحيات حساب QA**.

✅★★★ **واعتُمد البندان الحَوْكميّان نصّاً بعد كتابة هذا التقرير (2026-08-30):**
`ADR-0023` (**الطباعة الحرارية خارج النطاق — PDF وحدها**) · `CR-004`
(**بحدٍّ ضيّق**) — ★ **وأثرُهما موثَّقٌ في §12 أدناه**، ⟵ **ومنه تصحيحُ كودٍ
فعلي:** **اسمُ المصدر أُعيد إلى ترويسة المستند المُصدَّر.**
⛅★★ **وبَنى المالكُ صورةَ `callables:wu010` بنجاح** — ⏳ ⛔ **والنشرُ لم يقع بعد.**

---

## 2. بوابة الدخول — نتيجة الفحص

### 2.1 البوابة العامة (٨ شروط · `implementation-playbook.md` §1.1)

| # | الشرط | متحقق؟ | المسار المفحوص |
|:-:|---|:-:|---|
| 1 | المنهجية مُصرَّح بها | ✅ | `docs/21-project-management/development-methodology.md` |
| 2 | `architecture-overview.md` و`technology-stack.md` | ✅ | `docs/03-architecture/` |
| 3 | `coding-standards` · `naming-conventions` · `error-handling-strategy` | ✅ | `docs/04-design/` |
| 4 | `definition-of-done` · `branching-strategy` · `commit-message-conventions` · `versioning-scheme` | ✅ | `docs/07-development/` · `docs/12-deployment-and-release/` |
| 5 | `test-strategy.md` | ✅ | `docs/08-testing-and-qa/test-strategy.md` |
| 6 | `secrets-management-policy.md` | ✅ | `docs/09-security/secrets-management-policy.md` |
| 7 | `srs.md` بمتطلبات مرقّمة | ✅ | `docs/02-requirements/srs.md` |
| 8 | ⚠️ **لا ADR بحالة «مقترح» يحكم ما يُنفَّذ الآن** | ⚠️ **مشروط** | راجع §7 — ★ **`ADR-0023` أُنشئ *أثناء* التشغيل ويحكم ما لم يُبنَ** |

> ⚠️★★ **وشرحُ البند 8 صراحةً — ⛔ لا تجاوزٌ صامت:** ★ **البوابةُ كانت خالصةً
> عند البدء** (⛔ **ولا `ADR` مقترح**). ⟵ **و`ADR-0023` نشأ من جواب المالك في
> `IQ-033` أثناء العمل**، ★ **ومضمونُه إخراجُ ميزةٍ من النطاق** — ⟵ **فما
> بُني لا يعتمد عليه في شيء:** ⛔ **ولا سطرَ كودٍ واحد يفترض قبولَه أو رفضَه**،
> ★ **ورفضُه يعني إضافةَ الطباعة زيادةً جديدة** ⛔ **لا إعادةَ كتابةِ ما بُني.**
> ★ **ولذلك بقيت `WU-010` `[قيد التنفيذ]`** ⛔ **ولم تُعلَّم منجَزة.**

### 2.2 بوابة الوحدة — `WU-010` (٥ شروط · §1.2)

| # | الشرط | متحقق؟ |
|:-:|---|:-:|
| 1 | `module-design/messaging-design.md` موجود | ✅ |
| 2 | `FR-M20` بمعايير قبول واضحة | ✅ (`FR-M20-01` … `FR-M20-16`) |
| 3 | الوحدة تعرض API ⟵ `api-overview.md` و`authentication-and-authorization.md` | ✅ (**§3.1-ط أُضيف**) |
| 4 | تمسّ التخزين ⟵ `erd.md` و`data-dictionary.md` | ✅ ⛔ **ولا حقلَ ولا مجموعةَ جديدة** — `audit_log` كما هي |
| 5 | تمسّ الصلاحيات ⟵ `authentication-policy.md` | ✅ |

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

✅★★★ **`WU-010` — الإرسال والتصدير (`M20`)** — **أُغلقت 2026-08-31** بعد أن
نشر المالكُ الخدمةَ وزامَن صلاحيات حساب QA:
**1689 اختباراً آلياً · ٠ فاشلاً** · ⛅ **وثلاثةَ عشرَ سيناريو حيّاً على
`Pixel_6_API_36`** مقابل `qtms-callables-00021-v7j` — ★ **راجع §12.4 و§12.5.**

### 3.2 ما كان قيد التنفيذ — ✅ واكتمل

| WU | العنوان | ما بُني فعلاً | الفحوصات ونتائجها الفعلية |
|---|---|---|---|
| `WU-010` | الإرسال والتصدير (`M20`) | **19 ملفاً جديداً و14 معدَّلاً** عبر الطبقات الثلاث — راجع `implementation-plan.md` | `dart analyze` ×2 ✅ · `dart test` (نطاق) **491** ✅ · `dart test` (سحابة) **506** ✅ · `flutter analyze` ✅ · `flutter test` **452** ✅ · بوابات التصميم **28** ✅ · `check:coverage` **75 مفتاحاً · 0 فجوة** ✅ · `npm test` **236** ✅ · **المحاكي: 7 من 10 سيناريوهات** ⏳ |

✅★★★ **وما كان ينقصها أُغلق — الستةُ الباقية مقيسةٌ حيّاً (2026-08-31):**
⑧ **ظهورُ زرّ التصدير بصلاحيةٍ مشروعة** · ⑨ **PDF عربيٌّ بأرقامٍ مطابقة وصفِّ
مصدر** · ⑩ **مشاركةٌ باسمِ ملفٍّ بلا اسم عميل** · ⑪ **قيدُ «تصدير» ظاهرٌ في
السجل بمصدره الصحيح** · ⑫ **رفضُ التسجيل يُعرَض ولا يُبتلَع ولا قيدَ وهمياً** ·
⑬ **ولا أثرَ للطباعة الحرارية.**

**وهذا ما كان ينقصها قبل ذلك — يُحفَظ للسجل:**

| # | السيناريو | لماذا تعذّر — ★ مقيسٌ لا مفترَض |
|:-:|---|---|
| ⑧ | **تصدير سندٍ إلى PDF** | ⛔ **زرُّ التصدير لا يظهر أصلاً**: `documentExport` **غيرُ مُسنَدٍ لحساب QA** — ★ **والمنحُ يتعذّر ذاتياً** لأن **لا مستخدم يعدّل صلاحيات نفسه** (`authentication-policy.md` §3 · `permission_sync.dart`) |
| ⑨ | **مشاركةُ الملف بورقة النظام** | ★ **تابعٌ للسيناريو ⑧** |
| ⑩ | **كتابةُ قيد «تصدير»** | ⛔ **الخدمةُ المنشورة لا تحمل `logExport`** — ★ **قِيس بنداءٍ فعلي:** `POST /logExport ⟵ 404 · ERR_CALL_404` **مقابل** `POST /createReceipt ⟵ 401 · ERR_AUTH_003` |

### 3.3 وحدات محجوبة (ولماذا)

⛔ **لا وحدة محجوبة** — ✅★★ **ارتفع الحجب عن `WU-010` و`WU-011` معاً**
بحسم `IQ-031` … `IQ-034`.

| WU | العنوان | الحالة الآن | الخطوة التالية |
|---|---|---|---|
| `WU-011` | تقارير المرحلة الأولى | `[مخطط]` — ✅ **غيرُ محجوبة** | ★ **تُبنى في التشغيل التالي** بقراري `IQ-034` (**ستةُ مفاتيح بالعائلات**) و`IQ-032` (**التصدير مبنيٌّ فعلاً**) |

---

## 4. مخالفات النزاهة المرصودة آلياً

✅ **لا مخالفات.** ★ **مخرَجُ `track_implementation.py`:**
`has_integrity_violations: false` · `integrity_violations: []` ·
`unparsed_lines: []` · `normalized_headings: []` ·
`planned 15 · in_progress 1 · done 11 · blocked 0`.

★ **ولا وحدةَ معلَّمة `[منجز ✅]` أُضيفت في هذا التشغيل** — ⟵ **فلا فرصةَ
لمخالفةٍ أصلاً**، ★ **وهو المقصود: الصدقُ في الحالة قبل الرقم.**

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

⛔ **لم يُصعَّد بندٌ جديد في هذا التشغيل.** ✅★★★ **وحُسمت الأربعةُ المفتوحة
كلُّها ورُحِّلت:**

| IQ | القرار | رُحِّل إلى |
|---|---|---|
| `IQ-031` | **الخيار ب بلا سطر المصدر** — نصوصُ القوالب الأربعة | `message_templates.dart` · `translation-glossary.md` §5-أ · `messaging-design.md` §3 · **`CR-004`** |
| `IQ-032` | **الخيار أ** — مفتاحٌ واحد `documentExport` + عملية `logExport` | الكتالوج §2.8 · `security-requirements.md` §1 · `permission.dart` · `export_log.dart` · `api-overview.md` §3.1-ط |
| `IQ-033` | **إزالةُ الطباعة الحرارية** والاكتفاءُ بالتصدير والإرسال | **`ADR-0023`** (مقترح) · `technology-stack.md` §3 · `integration-architecture.md` §4 |
| `IQ-034` | **الخيار ب** — مفتاحٌ لكلِّ عائلةِ تقارير (**ستة**) | `implementation-plan.md` (`WU-011`) · `reporting-design.md` §6 |

⚠️★ **وقرارٌ واحدٌ لم يُصعَّد عمداً — ويُقال صراحةً:** ★ **`FR-M20-07` يشترط
«الرصيد السابق» ولا يُعرِّفه**، ⟵ **والمتاح رصيدٌ تراكميٌّ واحد**: ★ **فاعتُمدت
القراءة «ما كان عليه قبل هذه التوزيعة»** (`balanceBeforeTodayDebt`) **بافتراضٍ
موثَّقٍ في الكود وفي هذا التقرير.** ★ **ولماذا لم يُصعَّد:** **الرسالة إجراء
واجهة لا يكتب شيئاً** (`FR-M20-16`) ⟵ ⛔ **فخطأُ القراءة لا يُفسِد مالاً ولا
مخزوناً**، ★ **والحدُّ المُعلَن أن قبضاً وقع اليوم نفسِه يجعل «السابق» أقلَّ
مما كان صباحاً.**

راجع التفاصيل الكاملة في:
`docs/00-governance/upds-workflow/implementation-questions.md`

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

| # | ماذا يقول المستند | ماذا فرض التنفيذ | كيف عُولج |
|:-:|---|---|---|
| 1 | ⛔⛔ **`FR-M20-09` «حرجة»:** «**اسم المصدر وتاريخ المخزون يظهران في كل رسالة**» | ★ **جوابُ المالك في `IQ-031` يحذف سطر المصدر** | ✅ **`CR-004` جديد بحالة «مقترح»** — ⛔ **و`FR-M20.md` لم يُمَسّ بحرف** (`UPDS-05` §4.3) |
| 2 | ⛔ **`technology-stack.md` §3 يفرض الطباعة عبر بلوتوث** · **و`dependency-management-policy.md` §3 يمنع إذناً خارج «جهات الاتصال · التخزين»** | ★ **تعارضٌ بين مستندَين حاكمَين** — ⛔ **ولا يُحسَم ذاتياً** | ✅ **`ADR-0023` جديد بحالة «مقترح»** · **والصفّان وُسِما «قيد المراجعة»** ⛔ **ولم يُحذَفا** (§4.5) |
| 3 | ★ **`auditEntityTypes` تقول «لا قيمة بلا كاتبٍ لها فعلاً»** | ⛔ **`receiptEntityType` له كاتبٌ منذ `WU-007` ولا سطرَ له في القائمة** | ✅ **أُضيف** — ★ **قرارٌ روتيني من الفئة 3**: **تنفيذٌ لقاعدة الملف لا انحرافٌ عنها** (⟵ **وكان حارسُ `logExport` يرفض تصدير كل سند قبض**) |
| 4 | ★ **بوابةُ الاتجاه ترفض «قيمة موضعية بيمين/يسار»** | ⛔ **`TableHelper.fromTextArray` في حزمة `pdf` يقبل `Alignment` وحدها** | ✅ **بُني الجدولُ يدوياً بـ`AlignmentDirectional.centerStart`** — ⛔ **ولم تُضعَّف البوابة ولم يُستثنَ الملف** |
| 5 | ★ **بوابةُ التوكنز ترفض حجمَ خطٍّ أو لوناً خارج طبقة التوكنز** | ⛔ **راسمُ الـPDF يحتاج `fontSize` و`PdfColor`** | ✅ **أُنشئ `lib/core/design/pdf_tokens.dart` داخل طبقة التوكنز** — ★ **مشتقٌّ من `TypeScale` و`SemanticColors`** ⛔ **لا سلّمٌ ثانٍ ولا استثناءٌ من البوابة** |
| 6 | ⛔ **`firestore.rules`: قراءةُ `dealer_balances` بـ`perm('dealerBalanceView')` وحدها** | ★ **`WU-010` أولُ قارئٍ للمجموعة** — ⟵ **والقيدُ النطاقي غائب** (`GR-23`) | ⚠️ **سُجِّل `DEBT-70`** ⛔ **ولم تُعدَّل القواعد**: ★ **تعديلٌ بلا نشرٍ يقابله يكسر التطابقَ البايتيَّ المُثبَت**، **والنشرُ حصرُ المالك** |

---

## 7. بنود بوابة الحَوْكمة — ✅ اعتُمدت نصّاً (2026-08-30)

⛔⛔ **ولم يُعتمَد أيٌّ منها ذاتياً** (`UPDS-05` §4.6 · بروتوكول التشغيل §ب
الفئة 1) — ★ **الاعتمادُ وقع بنصٍّ مكتوبٍ من المالك، وهو محفوظٌ حرفياً في
كلٍّ من الملفين.**

| البند | المسار | الحالة |
|---|---|---|
| ✅ **`ADR-0023`** — إخراجُ الطباعة الحرارية من النطاق | [`ADR-0023-drop-thermal-printing.md`](../../03-architecture/adr/ADR-0023-drop-thermal-printing.md) | ✅★★ **معتمد** — ⛔⛔ **لا دعمَ ولا اعتماديةَ ولا واجهةَ ولا بندَ قبولٍ للطباعة** · ★ **وصيغةُ المخرَج PDF وحدها** |
| ✅ **`CR-004`** — حذفُ اسم المصدر من ترويسة القوالب | [`CR-004-message-template-source-line.md`](../../02-requirements/change-requests/CR-004-message-template-source-line.md) | ✅★★★ **معتمد بحدٍّ ضيّقٍ منصوص** (§2.1) — ⛔ **ترويسةُ الرسالة وحدها** · ✅ **والمستندُ المُصدَّر والقيدُ والتقسيمُ بلا مساس** |

⛔⛔★★ **وليس منها النشرُ الإنتاجي:** ★ **ولا شيء في هذا التشغيل يقترب من
الإنتاج** — ⛔ **ولا `firestore.rules` مُسَّت، ولا خدمةٌ نُشرت.**

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

★ **كلُّ الوحدات المعلَّمة «يلمس نطاقاً حسّاساً: نعم» تبقى كما هي**
(`WU-000` · `WU-002` … `WU-008`)، ⏳ **ويُضاف إليها:**

| WU | لماذا حسّاسة | ما يجب أن يراجعه إنسان |
|---|---|---|
| ⏳ `WU-010` | ★ **تُخرِج بيانات مالية وشخصية خارج التطبيق** · ★★ **وتُضيف مفتاح تفويضٍ جديداً** (`documentExport`) | ① **نصُّ القوالب الأربعة حرفياً** — ★ **التزامٌ تجاريٌّ يصل مقاوتة العميل** ② **مَن يُمنَح `documentExport`** — ★ **ومن يُمنَحه يُخرِج المستند كاملاً** ③ **حدُّ `logExport` المُعلَن**: ⛔ **لا تتحقق من وجود المستند المُصدَّر** |
| ⏳ `WU-011` | ★ **تعرض أرقاماً يُبنى عليها قرار مالي** | ★ **توزيعُ مفاتيح العائلات الستّ على الأدوار** — ⟵ **ومن يرى «المالية» يرى `R-14` و`R-17`** |

⛔⛔★★ **والتحققُ الآلي شرطٌ ضروريٌّ لا كافٍ لهذه الوحدات** — ★ **ولا يُغني
عنه نجاحُ 1685 اختباراً ولا اختبارُ المحاكي.**

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| `09-security/permissions-catalog.md` §2.8 و§5 | ★ **مفتاحٌ جديد `documentExport`** — **74 ⟵ 75** |
| `09-security/security-requirements.md` §1 | ★ **صفُّ «تصدير السندات والتقارير»** (البند ② من §6 في الكتالوج) |
| `05-api/api-overview.md` §3.1-ط | ★ **عمليةٌ مستدعاة جديدة `logExport`** |
| `04-design/module-design/messaging-design.md` | ★ **الحالة ⟵ «منفَّذ جزئياً»** · §1 · §3 · §7 · §9 · §10 |
| `04-design/module-design/reporting-design.md` §6 | ★ **ترحيلُ قرار `IQ-034`** |
| `03-architecture/technology-stack.md` §3 | ⏳ **وسمُ «قيد المراجعة» على الطباعة** — ⛔ **لا حذف** |
| `03-architecture/integration/integration-architecture.md` §4 | ⏳ **نفسُ الوسم على صفّ «الطابعة»** |
| `03-architecture/risks-and-technical-debt-register.md` | ⚠️ **`DEBT-70` جديد** |
| `02-requirements/requirements-traceability-matrix.md` | ★ **عمودُ الكود لـ`FR-M20`** ⟵ **`🔷` صارت `🟩`** |
| `07-development/dependency-management-policy.md` §3 و§7 | ★ **أربعُ حزمٍ جديدة بمبرِّراتها** · **وتأكيدُ أن بلوتوث لم يُستثنَ** |
| `13-operations/troubleshooting-guide.md` §4-أ | ⛔⛔ **درسُ `https://wa.me`** — **رابطُ `https` لا يقيس وجود تطبيق** |
| `17-localization-and-i18n/translation-glossary.md` §5-أ | ★ **فهرسُ القوالب الأربعة** ⛔ **لا نسخةٌ ثانية من نصّها** |
| `00-governance/upds-workflow/implementation-plan.md` | ★ **`WU-010` و`WU-011`** |
| `00-governance/upds-workflow/implementation-questions.md` | ✅ **أربعةُ بنودٍ `[تم الحل ✅]`** |
| **جديدان:** `ADR-0023` · `CR-004` | ⏳ **بحالة «مقترح»** |

⛔⛔ **ولم يُحدَّث `erd.md` ولا `data-dictionary.md` ولا `migrations-log.md`** —
★ **ولا حقلَ ولا مجموعةَ جديدة:** **قيدُ «تصدير» قيدُ `audit_log` عاديٌّ
بحقوله المعروفة.** ⛔ **ولم يُحدَّث `openapi.yaml`** — ★ **ولا يوجد** (`ADR-0001`:
**بلا خادم وسيط**)، **والعقدُ في `api-overview.md` §3.1.**

---

## 10. حالة `CLAUDE.md`

✅ **حُدِّث — ونقطةُ التحوّل حقيقية** (`claude-md-management.md` §2 البند 5:
**تقدّمٌ جوهري في التنفيذ**).

| البند | القيمة |
|---|---|
| `action` | **`managed-block-updated`** |
| `human_content_lines_preserved` | **20** — ⛔ **ولم يُمَسّ سطرٌ بشريٌّ واحد** |
| الحجم | **245 ⟵ 243 سطراً** — ★ **صافي −2** ⛔ **تحت سقف 250** (`AM-005`) |
| `exceeds_recommended_size` | ⚠️ **`true`** — ★ **والاستثناء موثَّق بـ`AM-005`** ⛔ **ولا يُطبَّق صامتاً** |

★ **وما تغيّر:** **سطرُ المرحلة** · **أعدادُ الاختبارات في كتلة الأوامر**
(412 ⟵ 491 · 461 ⟵ 506 · 26 ⟵ 28) · **عددُ العمليات المستدعاة وحالةُ نشرها** ·
**أرقامُ حالة التنفيذ** · **قسمُ `WU-010` الجديد** (⟵ **بدمج قسمَي `WU-006`
و`WU-008`/`WU-009` في واحد** ⛔ **بلا حذفِ حقيقة**) · **والدرسُ السادس في
سلسلة `DEBT-37`.**

---

## 11. الخطوة التالية الموصى بها

### 11.1 ⛔⛔★★★ إجراءان يملكهما المالك وحده — ★ ولا يكتمل `WU-010` بدونهما

★ **يُنفَّذان بترتيبهما** — ⛔ **ولا يُعكَس** (`environments.md` §1.4):

**① نشرُ خدمة العمليات على التجريبية** (بروتوكول التشغيل §ز — ⛔ **`builds
submit` و`run deploy` حصرُ المالك**):

✅★★ **والخطوةُ الأولى تمّت — نفّذها المالك بنفسه (2026-08-30):**
`gcloud builds submit` بـ[`cloudbuild.callables.yaml`](../../../cloudbuild.callables.yaml)
⟵ **البناء `8b7076bf-bee1-411d-b3ea-fa35def2f9bb` · `SUCCESS` · مدّتُه `1M8S`**،
★ **والصورةُ في السجل:**
`me-central1-docker.pkg.dev/qtms-orimind-master/qtms/callables:wu010`.

✅★★★ **وتمّت خطوةُ النشر كذلك (2026-08-30) — نفّذها المالك بهذه الصيغة
حرفياً، والمراجعةُ الناتجة `qtms-callables-00021-v7j` تحمل 100٪ من المرور:**

```powershell
gcloud run deploy qtms-callables --image me-central1-docker.pkg.dev/qtms-orimind-master/qtms/callables:wu010 --region me-central1 --project qtms-orimind-master
```

⛔⛔★★★ **ولم تُكتَب هذه الرايات تخميناً — كلُّ قيمةٍ فيها مقروءةٌ من الخدمة
الحيّة نفسِها** (`gcloud run services describe` — ★ **قراءةٌ وحدها، وهي
المسموحة في بروتوكول §ز**):

| الراية | مصدرُ قيمتها | ما تفعله |
|---|---|---|
| `qtms-callables` | ★ **اسمُ الخدمة القائمة** (`environments.md` §1.4) | ⛔⛔ **تُحدِّث الخدمة القائمة** — ★ **ولا تُنشئ ثانية**: ⟵ **فالمسار (`https://qtms-callables-7stgg3ngga-ww.a.run.app`) لا يتغيّر**، ⛔ **ولا يحتاج التطبيقُ إعادةَ بناء** |
| `--image …:wu010` | ★ **مخرَجُ البناء أعلاه** | ★ **تُنشئ مراجعةً جديدة** (`00021-…`) **تحمل 34 عمليةً بدل 33** — ⟵ **ومنها `logExport`** |
| `--region me-central1` | ★ **موقعُ الخدمة** (`ADR-0014`) | ★ **تحدّد أيَّ خدمةٍ تُحدَّث** — ⛔ **ولا تنقلها** |
| `--project qtms-orimind-master` | ★ **التجريبية** | ⛔⛔ **وليست `qtms-orimind-c001`** — ★ **ولا نشرَ إنتاجياً في هذا التشغيل ولا إذنَ به** |

★★★ **وما لا تمسّه هذه الصيغة عمداً — وهو الأهمّ:**

| ما يبقى كما هو | قيمتُه الحيّة المقروءة |
|---|---|
| ★★ **متغيّرات البيئة الثلاثة** | `FUNCTION_TARGET=callables` · `GOOGLE_CLOUD_PROJECT=qtms-orimind-master` · `QTMS_OWNER_UID=fj7BDnSxHkfiSwU7SjNXp7gKgXv2` |
| ★★ **حسابُ الخدمة بأقلّ امتياز** | `qtms-callables@qtms-orimind-master.iam.gserviceaccount.com` |
| ★ **الموارد والحدود** | `cpu 1000m` · `memory 512Mi` · `maxScale 3` · `concurrency 80` · `timeout 300s` |
| ⛔⛔★★★ **وربطُ `allUsers` على `roles/run.invoker`** | ★ **لا تُمرَّر رايةُ مصادقةٍ إطلاقاً** ⟵ **فتحديثُ خدمةٍ قائمة لا يمسّ سياسةَ IAM** |

⛔⛔★★★ **ولا رايةَ IAM في هذا الأمر ولا حاجةَ لها** — ★ **الربطُ قائمٌ
ومقيسٌ منذ نشر `wu009`** (`get-iam-policy` ردّ `allUsers`)، ⟵ **وطلبُ تغييرِ
IAM هنا يخالف نصَّك ونصَّ البروتوكول معاً.**

> ⛔⛔★★ **ولا تُنشَر بـ`--no-allow-unauthenticated` إطلاقاً** — ★ **الخدمة
> تحرسُ نفسَها برمز Firebase**، ⟵ **والراية تُزيل `allUsers` فيسقط التطبيق كلُّه**
> (`troubleshooting-guide.md` §—). ⛔ **ولا تُمرَّر `--set-env-vars` ولا
> `--service-account`** — ★ **تمريرٌ جزئيٌّ لأيٍّ منهما يمسحُ البقية.**

> ⛔⛔★★ **ولا تُنشَر بـ`--no-allow-unauthenticated` إطلاقاً** — ★ **الخدمة
> تحرسُ نفسَها برمز Firebase**، ⟵ **والراية تُزيل `allUsers` فيسقط التطبيق كلُّه.**
>
### 11.1.1 ★★★ تسلسلُ التحقق بعد النشر — بالترتيب

★ **الأربعةُ الأولى قراءةٌ فقط وأُشغِّلها أنا** (بروتوكول §ز)، ★ **والخامسُ
حيٌّ على `Pixel_6_API_36`:**

| # | الفحص | الأمر / الفعل | المتوقَّع |
|:-:|---|---|---|
| ① | **المراجعةُ الجديدة تحمل المرور** | `gcloud run services describe qtms-callables --region me-central1 --project qtms-orimind-master --format="value(status.latestReadyRevisionName,status.traffic)"` | ★ **`00021-…` بـ100٪** — ⛔ **لا `00020-7hk`** |
| ② | ⛔⛔★★ **`logExport` لم تعد `404`** | `curl -s -o /dev/null -w "%{http_code}" -X POST https://qtms-callables-7stgg3ngga-ww.a.run.app/logExport -H "Content-Type: application/json" -d "{\"data\":{}}"` | ★ **`401`** (`ERR_AUTH_003`) — ⟵ **الموجّهُ يعرفها والحارسُ يطلب رمزاً**: ⛔ **و`404` تعني أن النشر لم يقع** |
| ③ | ★★ **ولا ارتدادَ في العمليات القائمة** | **نفسُ النداء على `createReceipt` و`writeDailyPrices` و`createDistribution` و`grantPermissions`** | ★ **`401` من كلٍّ منها** — ⛔ **لا `404` ولا `403`** |
| ④ | ⛔⛔★★ **والربطُ سليمٌ بعد النشر** | `gcloud run services get-iam-policy qtms-callables --region me-central1 --project qtms-orimind-master` | ★ **`allUsers` على `roles/run.invoker`** — ⛔ **وغيابُه يعني سقوطَ التطبيق كلِّه** |
| ⑤ | ⏳★★★ **ثم §د.2 حيّاً** | **بعد ② من §11.1 — مزامنةِ صلاحيات QA** | راجع §11.1.2 |

✅★★★ **والنتيجةُ الفعلية (2026-08-30 · بعد النشر مباشرةً):**

| # | ما قِيس | النتيجة |
|:-:|---|---|
| ① | `describe` | ✅ **`qtms-callables-00021-v7j` · الصورة `:wu010` · 100٪ من المرور** |
| ② | `POST /logExport` | ✅⛔⛔★★★ **`401`** — ⟵ **بعد `404` في القياس السابق**: ★ **العمليةُ منشورةٌ الآن فعلاً** |
| ③ | **كلُّ مسارات الموجّه** | ✅★★ **38 مساراً ردّت `401`** (**العددُ مستخرَجٌ آلياً من `CallableOperation`** ⛔ **لا مُقدَّراً**) · **ومسارٌ مجهول ردّ `404`** — ⟵ ⛔ **فلا ارتدادَ في عمليةٍ قائمة** |
| ④ | `get-iam-policy` | ✅⛔⛔ **`allUsers` على `roles/run.invoker` باقياً** — ★ **فالتطبيق لم يسقط** |

⚠️★★ **وتصحيحٌ عدديٌّ لازم:** ★ **كان يُكتَب «34 عملية»** — ⛔ **والمقيسُ 38**
(37 قبل `logExport`): ⟵ ★ **والعدد الآن مأخوذٌ من عدّ أعضاء `CallableOperation`
ومن ردّ الخدمة معاً** ⛔ **لا من ذاكرة تشغيلٍ سابق.**

### 11.1.2 ⏳★★★ مصفوفةُ قبول §د.2 — ما يبقى ليُعلَن `WU-010` منجَزة

| # | السيناريو | الحالة اليوم | شرطُ النجاح |
|:-:|---|---|---|
| ① … ⑦ | **الإرسال والقوالب والرقم والسند** | ✅ **مقيسةٌ حيّاً (2026-08-30)** | — |
| ⑧ | ★★ **زرُّ التصدير يظهر بصلاحيةٍ مشروعة** | ⏳ **متعذّر** — ⛔ **`documentExport` غيرُ مُسنَد** | ★ **يظهر بعد المزامنة** ⛔ **وبلا أي تخفيفٍ للصلاحية ولا تصعيدٍ ذاتي** |
| ⑨ | **تصديرُ PDF وفتحُه** | ⏳ **متعذّر** | ★ **مستندٌ عربيٌّ RTL** · ★★ **وأرقامُه تطابق الشاشة رقماً برقم** · ✅★★ **وصفُّ «المصدر» في ترويسته** (`CR-004` §2.1) · ⛔ **ولا بصمةَ جهة تطوير** |
| ⑩ | **مشاركتُه بورقة النظام** | ⏳ **متعذّر** | ★ **اسمُ الملف بلا اسم عميل** · ⛔ **ولا إرسالَ تلقائي** |
| ⑪ | ⛔⛔★★ **قيدُ «تصدير» يُكتَب فعلاً** | ⏳ **متعذّر** — **`logExport` غيرُ منشورة** | ★ **`200` من العملية** · ★★ **والقيدُ ظاهرٌ في شاشة سجل التدقيق بنوعه ومصدره** |
| ⑫ | ★ **ورفضُ التسجيل يُعرَض لا يُبتلَع** | ⏳ **متعذّر** | ★ **الملفُّ يصل المستخدم، ورسالةُ الرفض تظهر** — ⛔ **لا صمت** |
| ⑬ | ⛔⛔ **ولا أثرَ للطباعة الحرارية** | ✅ **مُتحقَّق آلياً** | ⛔ **لا حزمة ولا إذن ولا زرّ** — ★ **مقيسٌ بـ`pubspec.yaml` والمانفست** (`ADR-0023`) |

**② مزامنةُ صلاحيات حساب QA** — ★ **بحسابك أنت** (⛔ **والحساب لا يمنح نفسه**:
`authentication-policy.md` §3) · **الإجراء في**
[`RB-staging-qa-account`](../../13-operations/runbooks/RB-staging-qa-account.md) §4:
**استدعاءُ `grantPermissions` للمستخدم `mjmK8owk4ZaciYT2yj1711jRgWB3` بكل مفاتيح
§2 من الكتالوج (75 الآن)** — ★ **المجموعة كاملةً لا الفرق.**

★ **وبعدهما أُكمِل السيناريوهات الثلاثة الباقية وأُعلن `WU-010` منجَزة** —
⛔ **أو تُعلَن غيرَ ذلك بصدق.**

### 11.2 ✅ البندان الحَوْكميّان — اعتُمدا

★ **راجع §7 و§12.** ⛔ **ولم يبقَ بندُ حَوْكمةٍ واحدٌ معلَّقاً على `WU-010`**
— ★ **والباقي إجراءٌ تشغيليٌّ لا قرار.**

### 11.3 ★ ثم `WU-011` — تقارير المرحلة الأولى

✅ **غيرُ محجوبة الآن**، ★ **وقراراها محسومان** (`IQ-034` الخيار ب ·
`IQ-032`) — ⟵ **وهي آخرُ وحدةٍ في المرحلة الأولى.**

---

## 12. ✅★★★ اعتمادُ المالك (2026-08-30) — وأثرُه المقيس

### 12.1 `ADR-0023` — الطباعة الحرارية خارج النطاق

★★ **قرارُ نطاقٍ صريح** ⛔ **لا متطلبٌ مؤجَّل** — ★ **ونصُّ المالك محفوظٌ
حرفياً في** [`ADR-0023`](../../03-architecture/adr/ADR-0023-drop-thermal-printing.md)
§الاعتماد. ✅★★ **وأثرُه على الكود صفر — مقيسٌ لا مُدَّعى:** ⛔ **لا حزمةَ
طباعةٍ ولا بلوتوث في `pubspec.yaml` ولا إذنَ لهما في المانفست** ⟵ **فالبناءُ
كان مطابقاً للقرار قبل صدوره.** ★ **والمستنداتُ الأربعة وُسِمت لا حُذفت:**
`technology-stack.md` §3 · `integration-architecture.md` §4 ·
`messaging-design.md` §1 و§7 · `dependency-management-policy.md` §3.

### 12.2 `CR-004` — والحدُّ استلزم تصحيحاً فعلياً

⛔⛔★★★ **ولم يكن اعتماداً توثيقياً وحده:** ★ **نصُّ المالك يقصر الحذف على
ترويسة الرسالة**، ⟵ **بينما كان صفُّ «المصدر» محذوفاً من ترويسة المستند
المُصدَّر كذلك** — ★ **قياساً على الرسالة بمعيار «المستند يطابقها»**:
⛔ **وهو ما يستثنيه القرار نصّاً.**

| الملف | التغيير |
|---|---|
| [`export_documents.dart`](../../../packages/qtms_domain/lib/capabilities/oversight/domain/export_documents.dart) | ★★ **`sourceName` مُدخَلٌ إلزامي**، **وصفُّ «المصدر» أوّلُ الترويسة في سند التوزيع وسند القبض معاً** |
| [`master_data_providers.dart`](../../../lib/capabilities/master_data/application/master_data_providers.dart) | ★ **`sourceDisplayNameProvider`** — **الاسمُ من `sources`** · ⛔ **ولا اسمَ يُخترَع:** **المعرّفُ نفسُه عند الغياب**، **و«كل المصادر» لـ`auditAllSourcesId`** |
| `distribution_screen.dart` · `receipt_screen.dart` | ★ **تمرير الاسم** — ⛔ **بلا حسابٍ ولا نصٍّ محفور** |
| [`export_documents_test.dart`](../../../packages/qtms_domain/test/capabilities/oversight/domain/export_documents_test.dart) | ✅★★ **طرفا الحدّ مُختبَران:** **الرسالةُ بلا مصدر** · **والمستندُ به** |

✅ **والبوابةُ بعد التصحيح: 1687 ناجحاً · ٠ فاشلاً** (**النطاق 493** ⟵ **+2**)
· ✅ **و`analyze` نظيفٌ في الحزم الثلاث** · ⛔⛔ **و`firestore.rules` لم تُمَسّ.**

⚠️★ **ولم يُطبَّق القرارُ على غير موضعه** — ★ **ولا حُذف اسمُ المصدر من شاشةٍ
ولا قيدٍ ولا مرشِّح** (`CR-004` §2.1).

### 12.3 ★ أثرُ الجلسة على `CLAUDE.md` — مُعلَنٌ لا صامت

★★ **243 ⟵ 245 سطراً (`+2` صافية)** — ⛔ **ولا حذفَ حقيقةٍ واحدة:** ★ **سطرٌ
لـ`ADR-0023` في قائمة القرارات، وسطرٌ لحدّ `CR-004` تحت `WU-010`** — ⟵ **وكلاهما
حارسٌ لا يُختصَر بلا فقده.** ✅ **والسقف 250 قائمٌ لم يُخرَق** (`AM-005`)،
★ **والمحتوى البشري خارج الكتلة المُدارة محفوظٌ كما هو** (**20 سطراً** — **مقيسٌ
بمخرَج `sync_claude_md.py`**).

### 12.4 ⛅★★ مزامنةُ حساب QA — نُفِّذت بتصريحٍ صريح من المالك

★ **الإجراء:** [`tools/staging/qa_permission_sync.py`](../../../tools/staging/qa_permission_sync.py)
(**جديد** · يوثّقه [`RB-staging-qa-account`](../../13-operations/runbooks/RB-staging-qa-account.md) §4.1).

| الخطوة | النتيجة المقيسة |
|---|---|
| ① دخولُ مالك التجريبية | `200` · ★ **والمعرّفُ طابق المالكَ المسجَّل** ⛔ **وإلا لتوقّف السكربت** |
| ② `bootstrapOwnerPermissions` | `200` · `alreadyBootstrapped: false` — ★★ **وهو الجذر الوحيد الذي يُدخِل مفتاحاً جديداً** (`BR-M1-03` · `IQ-023`) |
| ③ `grantPermissions` لحساب QA | `200` · **75 مفتاحاً من `Permission` لحظةَ التشغيل** ⛔ **لا من قائمة محفوظة** |
| ④ قراءةُ بطاقة الحساب | **75 صلاحية · `documentExport: true` · `isActive: true` · `sourceScope: all`** |

⛔⛔★★ **ولم يعدّل أحدٌ صلاحيات نفسه** — ★ **الفاعلُ المالك والمستهدَفُ حسابُ
QA** (`authentication-policy.md` §3): ⟵ ⛔ **ولا تخفيفَ لقاعدةٍ ولا تصعيدَ ذاتي.**
⛔⛔ **ولا قيمةَ سرٍّ مرّت في سياق الأداة ولا في مخرَجها** — ★ **قراءةٌ داخل
العملية ومُنقٍّ ثانٍ على كل مخرَج** (`AM-004`).

⚠️⚠️★★★ **ورُصد أثناء ذلك أمرٌ أمنيٌّ يخصّ المالك — ولا تملك الأداة علاجَه:**
★ **`android/app/google-services.json` ملفُّ الإنتاج `qtms-orimind-c001`**
(⟵ **ونكهةُ التجريبية تقرأ `src/staging/`**)، ⛔⛔ **وتسجيلُ الدخول به باعتماد
مالك التجريبية نجح `200`** — ⟹ ★ **فالاعتمادُ نفسُه صالحٌ على الإنتاج**:
⛔ **وهو ما يمنعه `authentication-policy.md` §5 البند 2** (**لا إعادةَ استخدامٍ
لكلمة مرور**). ★ **ولم يُكتَب ولم يُقرَأ سرٌّ** — ⟵ **والقياسُ من رمز الحالة
ومعرّف الحساب وحدهما**، ⛔ **ولا كتابةَ واحدة وقعت على الإنتاج** (**العمليتان
التاليتان رُفضتا بـ`INVALID_ID_TOKEN` من خدمة التجريبية**). ⟹ ★ **يُسلَّم
للمالك: غيِّر إحدى كلمتَي المرور.**

### 12.5 ⛔⛔★★★ العطل السابع — كشفه الجهاز في المخرَج الذي يصل يدَ العميل

★★ **«مصدر الاختبار الأول» طُبعت «مصدر الاختبارالأول»** — ⟵ **حزمةُ `pdf`
تُقطّع النصّ كلماتٍ وتعكس ترتيبها للاتجاه، وتنسِب عرضَ الفراغ للفجوة الخطأ.**
⛔ **ولم يكشفه أيٌّ من 1687 اختباراً** — ★ **لأنه في تخطيط حزمةٍ لا في منطقنا.**

⛔⛔ **ومحاولةُ العلاج الأولى فشلت — وتُسجَّل كما وقعت:** ★ **فراغٌ غير فاصل
(`U+00A0`)** ⟵ **خرج الملفُّ بمواضع الكلمات نفسِها حرفاً بحرف** (**26 مقطعاً
في مواضعها**): ⟹ **الحزمة تُسوّيه فراغاً عادياً قبل التشكيل.**
★★ **والقياسُ هو ما أسقط الفرضية** — ⛔ **لا الشكل ولا الظن.**

✅ **والعلاج المُثبَت `spacedPdfValue`:** ★ **صفٌّ من كلماتٍ وفراغاتٍ نرسمها
بنسبةٍ من حجم الخط** ([`pdf_tokens.dart`](../../../lib/core/design/pdf_tokens.dart)
— `pdfWordGapRatio`) ⛔ **لا نقطةٌ ثابتة ولا رقمٌ حرّ خارج التوكنز.**
⛅ **ومُثبَتٌ حيّاً بعد الإصلاح:** **«المصدر: كل المصادر» · «رقم السند:
RCP-20260831-0001» · «التاريخ: 2026/08/31» · «المقوت: رعوي الاختبار»** —
**كلُّها بفراغاتها.** ★ **وله حارسان في** [`pdf_document_renderer_test.dart`](../../../test/capabilities/oversight/infrastructure/pdf_document_renderer_test.dart).

⛔⛔★★ **ودرسُه يُضاف لسلسلة `DEBT-37`:** ★ **اختبارُ «الملفُّ يُبنى» لا يقول
شيئاً عن «الملفُّ يُقرأ»** — ⟵ **وما يراه العميل يُفحَص بعينٍ على جهاز.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
