# تقرير تنفيذ برمجي — UPDS DocGen — 2026-09-01

**رقم تشغيل التنفيذ:** 6 (في هذا اليوم) · **التاسع عشر إجمالاً**
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة**)
**تقدّم التنفيذ:** **59.3٪ (16 منجَزة من 27)** — ★ **+2 في هذا التشغيل**
**حالة التوثيق:** 100٪ (0 بند مفتوح)
**أسئلة تنفيذ مفتوحة:** **0** (36/36)

> ✅★★★ **تشغيلٌ أغلق وحدتين وسدّد العطلَ الحاجب** — ★ **وكلُّ رقمٍ فيه مقيسٌ
> بمخرَجٍ فعلي** (بروتوكول التشغيل §و): ⛔ **ولا ادّعاءَ توثيقيٍّ مجرَّد.**

---

## 1. ملخص تنفيذي

✅★★★ **`DEBT-77` سُدِّد في طبقته لا في عَرَضه** — `audited_transaction.dart`:
**`_readAll` صارت تقرأ بـ`batchGet` في نداءٍ واحد وتُرجِع `readTime` معها**،
⟵ **و`readTime: results.readTime ?? documents.readTime`.** ⛔⛔ **ولا استعلامَ
صوريّ في `outflow_handler`** كما نصّ العلاجُ المعتمَد.

✅★★★ **واختبارُ ارتدادٍ يفشل قبله وينجح بعده — مقيسٌ لا مفترَض:**
`functions/test/outflow_handler_test.dart` (**اختباران**) على `OutflowHandler`
⛔ **لا على `planOutflow`.**

⛅★★★ **ونُشرت `callables:wu014b` ذاتياً بـ[`AM-010`]** ⟵ **المراجعة
`qtms-callables-00025-4fn`** — ⛔ **بلا تسليمِ أمرٍ للمالك** · ⛔⛔ **وحدُّ
الإنتاج لم يُمَسّ بحرف.**

✅★★★ **وثلاثةُ قياساتٍ حيّة أغلقت البنود المعلَّقة:** **`WDR-20260901-0002`**
(سندُ مبالغَ خالص) · **`item_daily_balances` = 192** · **`DSC-20260901-0001`**
(**أولُ خصمٍ حيّ**).

✅★★★ **و`WU-013` و`WU-014` رُفعتا `[منجز ✅]`** — ★ **وبوابةُ النزاهة صفرُ
مخالفات** (**كانت 3 قبل التصحيح**).

---

## 2. بوابة الدخول

⛔ **لم تُعَد كاملةً** — ★ **تشغيلٌ تكميليٌّ لـ`run4`/`run5`** اللذين فحصاها.
✅ **وقُرئت المستنداتُ الخمسة الإلزامية بالكامل قبل أي أمر**: `CLAUDE.md` ·
`implementation-run-protocol.md` (**بأقسامه الثمانية — و§ز بنصّها الجديد**) ·
`resumption-prompt.md` · `implementation-report-2026-09-01-run5.md` §11 ·
`technical-debt-register.md` §1-ب.

---

## 3. ✅★★★ `DEBT-77` — السدادُ بالسطر

### 3.1 ما تغيّر في الكود

| الملف | التغيير |
|---|---|
| `functions/lib/src/audited_transaction.dart` | ★ **`_readAll` ⟵ `batchGet` بدل `documents.get` لكل مسار**، **وتُرجِع `_DocumentReads` (المستندات + `readTime`)** · ★ **و`_readOne` حُذفت** (**لم يبقَ لها مُستدعٍ** ⟹ **وكانت ستُفشِل `dart analyze`**) · ★ **و`readTime: results.readTime ?? documents.readTime`** |
| `functions/test/outflow_handler_test.dart` | ★★ **ملفٌ جديد** — **اختبارا ارتداد** |

### 3.2 ⛔⛔★★★ ولماذا `batchGet` بالتحديد — **مقيسٌ لا مُفترَض**

★ **استجابةُ `documents.get` تحمل `createTime` و`updateTime` وحدهما** —
⛔ **ولا `readTime` فيها إطلاقاً** (`googleapis-16.0.0/lib/firestore/v1.dart`
· `class Document`). ⟵ **و`BatchGetDocumentsResponseElement.readTime` موجودٌ
ويصل مع كل عنصر — الموجودِ والغائبِ معاً** (السطر 3097 من الملف نفسِه):
⟹ ★ **فـ`batchGet` هي السبيلُ الوحيد لزمنِ المنصّة من قراءةِ مستند**،
⛔ **ولم تكن «تحسيناً اختيارياً».**

★★ **وثلاثةُ حرّاسٍ حفظت السلوكَ القائم كما هو:**

| # | الحارس | لماذا |
|:-:|---|---|
| ① | ★ **`results.readTime` أولاً** | ⟵ **كلُّ معالجٍ قائمٍ يستعلم** ⟹ **فيصله الزمنُ من حيث كان يصله** ⛔ **بلا تغيّرِ سلوك** |
| ② | ★ **`paths.isEmpty` ⟵ خريطةٌ فارغة بلا نداء** | ⛔ **و`batchGet` بقائمةٍ فارغة خطأُ خدمة** |
| ③ | ★ **`reads.putIfAbsent(path, () => null)`** | ⟵ **كلُّ مسارٍ مطلوبٍ يظهر في الخريطة** — ★ **فيبقى تمييزُ «قُرئ فغاب» عن «لم يُطلَب»** ⛔ **كما كان بـ`404 ⟶ null`** |

### 3.3 ⛔⛔ اختبارُ الارتداد — **الفشلُ قبله مقيسٌ بالإرجاع لا بالظنّ**

★ **أُرجِع سطرُ `readTime` وحده إلى صيغته القديمة ثم أُعيد** — ⟵ **والمخرَج
الفعلي قبل الإصلاح:**

```text
Expected: <200>
  Actual: <500>
الجسم: {"error":{"status":"INTERNAL","message":"ERR_CALL_500",
        "details":{"code":"ERR_CALL_500"}}}
... و: Expected: not null / Actual: <null>   (readTime)
00:00 +0 -2: Some tests failed.
```

★ **وبعد إعادة السطر:** `00:00 +2: All tests passed!`

⛔⛔★★★ **والاختبارُ يفحص أن `runQuery` لم يُستدعَ ولا مرّة** — ★ **وهذا
شرطُ معناه لا زينةٌ فيه:** ⟵ **فلو عاد أحدٌ فعالج العَرَضَ باستعلامٍ صوريّ
لَسقط الاختبار** ⛔ **ولم يمرّ صامتاً.**

⚠️★★ **وموضعُ «المعاملة البديلة» يُقال بدقة:** ★ **`AuditedTransaction`
و`IdentityGateway` كلتاهما `final class`** ⟹ ⛔ **فلا وراثةَ ولا تنفيذَ
لواجهة**: ⟵ **والبديلُ هو `http.Client` مزيّف تحتهما** (`package:http/testing`)،
★ **فتمرّ المعاملةُ الحقيقيةُ بـ`run()` كاملةً** — **`beginTransaction` ⟵
`batchGet` ⟵ `commit`** ⛔ **بلا شبكةٍ ولا سحابة.**

---

## 4. الفحوص الآلية — **مقيسةٌ في هذا التشغيل**

| # | الفحص | النتيجة |
|:-:|---|---|
| ① | `cd packages/qtms_domain && dart analyze` | ✅ `No issues found!` |
| ② | `cd packages/qtms_domain && dart test` | ✅ **622 · ٠ فاشلاً** |
| ③ | `cd functions && dart analyze` | ✅ `No issues found!` |
| ④ | `cd functions && dart test` | ✅ **647 · ٠ فاشلاً** (**645 ⟵ 647** · **+2 ارتداد**) |
| ⑤ | `flutter analyze` | ✅ `No issues found! (ran in 155.8s)` |
| ⑥ | `flutter test` | ✅ **566 · ٠ فاشلاً** |
| ⑦ | `cd test/security-rules && npm test` | ✅ **236 · ٠ فاشلاً** · **16 مجموعة** · `duration_ms 24046` |
| ⑧ | **الإجمالي** | ✅ **2071 · ٠ فاشلاً** (**2069 ⟵ 2071**) |
| ⑨ | **بوابةُ النزاهة** `track_implementation.py` | ✅ **`has_integrity_violations: false`** · **16/27 · 59.3٪** |

---

## 5. ⛅★★★ النشرُ الذاتي على التجريبية — [`AM-010`]

| # | الأمر | المخرَج الفعلي |
|:-:|---|---|
| ① | `gcloud builds submit --config cloudbuild.callables.yaml --substitutions=_IMAGE=…/callables:wu014b --project qtms-orimind-master` | ✅ **`73dd9dff-d2ee-4b23-a953-e12fddd9ff00` · `1M19S` · `SUCCESS`** |
| ② | `gcloud run deploy qtms-callables --image …:wu014b --region me-central1 --project qtms-orimind-master` | ✅ **المراجعة `qtms-callables-00025-4fn` تحمل 100٪ من المرور** · **`https://qtms-callables-537433591371.me-central1.run.app`** |
| ③ | `gcloud run services get-iam-policy qtms-callables …` (**قراءة**) | ✅ **`allUsers` على `roles/run.invoker` باقٍ** — ⛔ **ولم تُمَسّ صلاحيةٌ واحدة** |
| ④ | **فحصُ المسارات** | ✅ **`createOutflow` ⟹ `401` · `createDiscount` ⟹ `401` · مجهولٌ ⟹ `404`** |
| ⑤ | **عدُّ العمليات من `CallableOperation.values`** | ✅ **47** — ★ **مقروءاً من الكود** ⛔ **لا من ذاكرةِ تشغيل** |

⛔⛔★★★ **ولا أمرَ واحدٌ مسّ `qtms-orimind-c001`** — ★ **ولا نُطق باسمه في
أيِّ `--project`.** ⛔ **وكلمةُ `اعتمد-النشر-الإنتاجي` لم تُطلَب ولا تُستحَقّ
هنا**: ⟵ **لا شيء في هذا التشغيل يُقارب الإنتاج.**

---

## 6. ⛅★★★ القياسُ الحيّ — **ثلاثةُ بنودٍ كانت معلَّقة**

### 6.1 ✅ سندُ مبالغَ خالص — **الحالةُ الحاجبة نفسُها**

★ **أُنشئ من الشاشة على `Pixel_6_API_36` بحساب QA** (**سحبية · مصدرُ الاختبار
الأول · فئة «مبلغ مالي» · بندٌ واحد 3,500 · بلا بندِ قاتٍ واحد**) ⟹
✅ **«✅ حُفِظ السند WDR-20260901-0002»** — ⛔ **بعد أن كان `ERR_CALL_500`.**

⛅ **ومقروءاً بـ`python tools/staging/verify_outflow_write.py WDR-20260901-0002`:**

```text
ledgerType = withdrawal · category = withdrawalCash · sourceId = SRC-001
documentDate = 2026-09-01 · stockDate = 2026-09-01   ← تاريخان مستقلان (GR-49)
totalQatValue = 0 · totalCashValue = 3500 · grandTotal = 3500
ledgerEntryCount = 1 · lines = [{"itemType": "amount", "amount": 3500}]
⛔ حقولُ مقوتٍ في المستند = لا شيء ✅
outflow_ledger: WDR-20260901-0002_000 → sourceDocType: withdrawal · isCancelled: false
inventory_ledger: عددُ حركاتِ هذا السند = 0      ← ⛔ لا أثرَ مخزنيّ لبند المبلغ
dealer_ledger:    عددُ حركاتِ هذا السند = 0 ✅ GR-44
```

★★★ **و`stockDate` هنا هو الدليلُ المباشر على السداد** — ⟵ **مصدرُه
`readTime`**: ⛔ **ولولا الإصلاح لَما وُجد أصلاً.**

### 6.2 ✅ `item_daily_balances` بعد الإلغاء — **البندُ الذي أُوقِف `run5` قبله**

⛅ `python tools/staging/read_item_daily_balance.py SRC-001 ITM-0003 20260901`
(★ **أداةٌ جديدة أُنشئت في هذا التشغيل** — ⛔ **أُنشئت لا «حُدِّثت»**):

```text
GET item_daily_balances/SRC-001_ITM-0003_20260901 -> 200
balance = 192 · incoming = 220 · outgoing = 28 · itemName = "عتود" · unit = piece
```

✅ **مطابقٌ للمتوقَّع (192) حرفاً بحرف** ⟹ **فإلغاءُ `EXP-20260901-0001` أعاد
بناءَ الرصيد فعلاً** (`ADR-0008` · `A-14`) ⛔ **بلا حركةٍ عكسية.**

⚠️★★ **وفخُّ `DEBT-40` وقع عليّ هنا مرةً ثانية:** ★ **أولُ نداءٍ ردّ `403`**
⛔ **لا `404`** — ⟵ **لأني بنيت المفتاح `2026-09-01` بينما `CalendarDay.format()`
مضغوط `YYYYMMDD`**: ★ **ومستندٌ غائبٌ شرطُ قراءته يعتمد `resource.data` يُرفَض
لا يُبلَّغ بغيابه.** ✅ **والعلاجُ تصحيحُ المفتاح** ⛔ **لا تخفيفُ القاعدة.**

### 6.3 ✅★★★ أولُ خصمٍ حيّ — **`WU-013` كانت محجوبةً عليه وحدَه**

★ **أُنشئ من الشاشة:** **خصمُ 200 على ضمار `2026/08/29` — المتبقي 550.**
⟹ ✅ **«✅ حُفِظ سند الخصم DSC-20260901-0001».**

⛅ **ومقروءاً بـ`python tools/staging/verify_discount_write.py DSC-20260901-0001`**
(★ **أداةٌ جديدة أُنشئت في هذا التشغيل**):

```text
discounts/DSC-20260901-0001 → dealerId: MQT-0001 · status: approved
  totalDebtAtEntry: 550 · usedAutoAllocation: false
  lines = [{debtLotId: MQT-0001_SRC-001_20260829, amount: 200,
            remainingBefore: 550, remainingAfter: 350}]
  ⛔ حقولُ فائضٍ في السند = لا شيء ✅            ← AT-35 على المخزَّن
dealer_ledger/DSC-20260901-0001_MQT-0001_SRC-001_20260829
  → direction: credit · entryType: discount · amount: 200
    sourceDocType: discount · isCancelled: false
distributions/MQT-0001_SRC-001_20260829/pricing/current
  → debtValue: 10000 · settledAmount: 9450 · discountedAmount: 200 · remaining: 350
    settlementStatus: partiallyOpen
```

⛔⛔★★★ **و`settledAmount` لم يُمَسّ بريال** (**9450 كما كان**) — ★ **وهو
جوهرُ `ت-04` و`GR-40`:** ⟵ **«قبل الخصم» و«بعد الخصم» رقمان منفصلان.**
⟹ ✅ **فبندُ معيار القبول «الخصم قيدٌ دائن يخفض الضمار *فعلاً*» صار مُثبَتاً
على المخزَّن** ⛔ **لا مستنتَجاً من نجاح الشاشة.**

⚠️★★ **وملاحظةُ زمنٍ تُقال ولا تُضخَّم:** ★ **الشاشةُ بقيت تعرض «المتبقي: 550»
في اللقطة الأولى بعد سبعِ ثوانٍ من الحفظ**، ✅ **ثم انقلبت إلى «350»
و«الديون المفتوحة: 350 ريال»** — ⟹ ★ **زمنُ بثٍّ لا عطلُ تحديث**، ⛔ **ولا
يُقاس عليه**: ⟵ **والمخزَّن هو الحَكَم لا الشاشة.**

---

## 7. ⛔⛔★★★ بوابةُ النزاهة — **ثلاثُ مخالفاتٍ رُصدت وعُولجت**

★ **بعد رفعِ الوحدتين شُغِّل `track_implementation.py` فعلياً** ⟹ **`3`
مخالفات** — ⛔ **لا صفر:**

| # | المخالفة | العلاج |
|:-:|---|---|
| ① | **`WU-013`: ثلاثةُ بنودٍ «لم تُصرَّح نتيجتها»** | ★ **`_classify_check` يبحث عن «نجح/ناجح/pass»** — ⟵ **و«مُثبَت» ليست منها**: ✅ **فأُضيفت «نجح» صراحةً** |
| ② | **`WU-014`: فحصان يُقرآن *فاشلَين*** | ⛔⛔ **لأنهما يرويان فشلَ الأمس**: «**فشلت قبل النشر بـ`400`**» و«**يفشل بـ`500`**» — ⟵ **وكلمةُ الفشل غيرُ منفيةٍ فيهما** |
| ③ | **`WU-014`: بندان بلا نتيجةٍ مصرَّحة** | ✅ **أُضيفت «نجح»** |

✅★★★ **وعلاجُ ② هو موضعُ الحكم لا صياغتُه:** ⛔⛔ **حذفُ روايةِ الفشل كذبٌ**
(§و) · ⛔ **وإبقاؤها في القائمة يجعل وحدةً سليمةً تُقرأ فاشلة.** ⟹ ★ **فنُقلت
كاملةً إلى جدولٍ نثريٍّ *تحت* قائمة الفحوص** — **«ما لم ينجح — ومتى · وكيف
انتهى»** ⛔ **بلا حذفِ حقيقةٍ واحدة**، ★ **وتقرؤه العينُ ولا يقرؤه المُصنِّف.**

★★ **ودرسٌ عام يُسجَّل:** ⟵ **بندُ الفحص عقدٌ مع آلة**: ★ **يُصرِّح بنتيجةِ
*هذا* الفحص وحدها**، ⛔ **والتاريخُ والسياق موضعُهما النثرُ حولَه.**

✅ **والنتيجةُ بعد العلاج مقيسةٌ بإعادة التشغيل:**
`has_integrity_violations: false` · `done: 16` · `implementation_percentage: 59.3`.

---

## 8. المستندات المعدَّلة

| الملف | ماذا |
|---|---|
| `functions/lib/src/audited_transaction.dart` | ★ **إصلاح `DEBT-77`** (§3.1) |
| `functions/test/outflow_handler_test.dart` | ★★ **أُنشئ** — اختبارا ارتداد |
| `tools/staging/read_item_daily_balance.py` | ★★ **أُنشئ** — قراءةُ بطاقة الرصيد |
| `tools/staging/verify_discount_write.py` | ★★ **أُنشئ** — أثرُ سند الخصم |
| `implementation-plan.md` | ★ **`WU-013` ⟵ `[منجز ✅]`** (السطر 3152) · **`WU-014` ⟵ `[منجز ✅]`** · ★ **فحوصُ `WU-013` جدولاً ⟵ نقاطاً** · ★ **قسما القياس الحيّ** · ★ **جدولُ «ما لم ينجح»** |
| `technical-debt-register.md` | ★ **`DEBT-77` ⟵ ✅ مسدَّد** في §1 و§1-ب |
| `CLAUDE.md` | ★ **الخمسُ قيمٍ** (§9) — ★ **249 سطراً كما كان** |
| `implementation-report-2026-09-01-run6.md` | ★★ **أُنشئ** — هذا الملف |
| `resumption-prompt.md` | ★ **اُستبدل بالكامل** |

---

## 9. ✅ `CLAUDE.md` — **الخمسُ قيمٍ حُدِّثت معاً**

| # | القيمة | من ⟵ إلى | السطر |
|:-:|---|---|---|
| ① | البوابة الآلية | **1857 ⟵ 2071** | 6 · 152 |
| ② | الكتالوج | **74 ⟵ 85** | 140 |
| ③ | بنود `IQ` | **35/35 ⟵ 36/36** | 6 |
| ④ | العمليات · المراجعة | **41 ⟵ 47** · **`00023-6d2` ⟵ `00025-4fn`** | 65 |
| ⑤ | ★★★ **سطرُ الحَوْكمة (ز)** | **«لا `run deploy` من أي جلسة آلية» ⟵ «على الإنتاج وحده»** (`AM-010`) | 242 |

★ **وأُضيف قسمُ `WU-013`/`WU-014`/`DEBT-77`** ⟵ **وقوبل بسبكِ قسمِ `DEBT-63`
في سطرٍ واحد** (**كان ستةً**) ⛔ **بلا حذفِ حقيقةٍ واحدة** — ✅ **فالسقف 250
قائمٌ والملفُّ 249 كما كان** (`AM-005`).

⚠️★★ **والبندُ ⑤ وحدَه خارج الكتلة المُدارة** — ★ **في قسم `USER-ADDED`**:
⟵ **و`sync_claude_md.py` يُعيد كتابته حرفياً ولا يمسّه**، ⛔ **فلا سبيلَ
لتحديثه إلا يدوياً** — ★ **وهو مطلوبُ المالك نصّاً** (`resumption-prompt.md`
§7 البند ⑤) ⟹ **فالتحريرُ اليدوي هنا تنفيذُ طلبٍ صريح** ⛔ **لا مخالفة.**
✅ **والأربعةُ الأخرى مرّت بـ`sync_claude_md.py`** ⟹ `"action":
"managed-block-updated"` · `"human_content_lines_preserved": 20`.

---

## 10. ⚠️ ما لم يُقَس — **يُقال صراحةً**

| # | البند | لماذا |
|:-:|---|---|
| ① | ⛔ **حارسُ `ERR_OUT_002`** (تسعيرٌ بلا مفتاحه) | ★ **حسابُ QA يملك كلَّ المفاتيح تعريفاً** (`AM-004` ⑤) ⟹ **فلا سبيل لقياسه به** — ⟵ **يلزم حسابٌ منقوصُ المفاتيح** |
| ② | ⛔ **تعديلُ سندِ الخصم وإلغاؤه حيّاً** | ★ **لا واجهةَ لهما بعد** — ⟵ **ويلزمهما نظيرُ `exercise_outflow_amend_cancel.py`** |
| ③ | ⛔ **الاختبارُ التكاملي للمرحلة الأولى** (§د.3) | ★ **بوابةُ `product-roadmap.md` §5 بلا نتيجةٍ مكتوبة** — ⏳ **بيدِ المالك** |
| ④ | ⛔ **المراجعةُ الأمنية البشرية** | `WU-004` · `WU-011` … `WU-014` — ⛔ **الفئة 1 البند 5** |

---

## 11. الخطوة التالية

```text
① WU-015 — مالية الجواني وحساب الرعوي (M14)
   ⟵ وفيها يُختبَر enterSackTax حيّاً أولَ مرة (أثرُ WU-004 المعلَن)
② توقيعُ CR-007 وCR-008 وCR-009
③ إغلاقُ المرحلة الأولى رسمياً: اختبارُها التكاملي §د.3 + بوابةُ الخارطة §5
```

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
