# تقرير تنفيذ برمجي — UPDS DocGen — 2026-09-01

**رقم تشغيل التنفيذ:** 53 (الثالث في 2026-09-01)
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة** — شريحة رأسية كاملة)
**تقدّم التنفيذ:** 51.9٪ (14 منجَزة من 27) · ⏳ **و`WU-013` `[قيد التنفيذ]`**
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** **1** — `IQ-036` (**فُتح في هذا التشغيل**)

---

## 1. ملخص تنفيذي

⛔⛔★★★ **وأولاً — تغيُّرُ مسارٍ بقرار المالك أثناء التشغيل:** ★ **بدأ التشغيل
على إغلاق المرحلة الأولى** (§د.3 وبوابة `product-roadmap.md` §5) **كما توصي
خطوةُ ④ من تقرير التشغيل السابق** — ⟵ **ثم أفاد المالكُ صراحةً أن سيناريوهات
اختبار المرحلة الأولى على المحاكي اكتملت وطلب المضيَّ إلى المرحلة الثانية.**
⛔⛔★★ **ولم أرَ مخرَجاتِ تلك السيناريوهات فلا أصفها ولا أنسب إليّ قياسَها**
(بروتوكول التشغيل §و) — ★ **وما قِسْتُه بنفسي من المرحلة الأولى قبل التحويل
مذكورٌ في §3.3** ⛔ **ولا يُقدَّم بديلاً عن اختبارها التكاملي.**

★★★ **ثم بُنيت زيادةُ الخصومات (`M13` · `WU-013`) كاملةً من طرفٍ إلى طرف** —
**طبقةُ نطاقٍ ومستودعُ عقدٍ وعملياتٌ سحابيةٌ ثلاث ومستودعان وشاشةٌ ومسارٌ
ومدخلٌ في الرئيسية** — ★ **و85 اختباراً جديداً** (30 نطاق · 38 سحابة ·
17 تطبيق)، ⟹ **والبوابةُ الآلية 1942 ناجحاً · ٠ فاشلاً** ✅ **و`analyze`
نظيفٌ في الحزم الثلاث.**

⛔⛔★★★ **والزيادةُ لم تُعلَّم منجَزة** — ★ **وهذا هو الخبرُ المهم:**
⟵ **معيارُ قبولها ينصّ «الخصم قيد دائن يخفض الضمار *فعلاً*»**، ★ **وذلك لا
يُثبَت إلا بكتابةٍ حيّة**: ⛅ **وقد ردَّت `createDiscount` بـ`ERR_CALL_404`
مقيساً على `Pixel_6_API_36`** ⟵ **لأن الخدمة لم تُنشَر بعد**، ★ **والنشرُ
حصرُ المالك** (بروتوكول التشغيل §ز). ⟹ **فالوحدة `[قيد التنفيذ]` بانتظار
مخرَجات أمر البيئة** (§ج البند 5) — ⛔ **لا `[منجز ✅]` بشهادةٍ آلية وحدها.**

✅★★ **وخمسةَ عشرَ سيناريو على المحاكي قِسْتُها بنفسي — الكلُّ نجح**، ★ **ومنها
الأهمُّ ثلاثة:** ⛔⛔ **لا حقلَ فائضٍ في الشاشة إطلاقاً** (`AT-35`) ·
⛔⛔ **ورفضُ السحابة يُعرَض ولا يُبتلَع ولا حقلَ يُمحى** · ⛔⛔ **والباقي من
التوزيع التلقائي يُسمّى «لم تُوزَّع» لا «فائضاً»**.

⚠️★★ **ومفتاحٌ ثانٍ وثمانون أُضيف — `discountBackdate`** (`FR-M13-06` §4
يُسمّيه صراحةً ولم يكن في الكتالوج): ★ **وثلاثةُ حرّاس عدٍّ رصدوه وحُدِّثوا
عمداً** ⛔ **ولم يُخفَّف حارسٌ منها.**

---

## 2. بوابة الدخول — نتيجة الفحص

### 2.1 البوابة العامة (8 شروط — `implementation-playbook.md` §1.1)

| # | الشرط | متحقق؟ | المسار المفحوص |
|:-:|---|:-:|---|
| 1 | المنهجية مُصرَّح بها | ✅ | `docs/21-project-management/development-methodology.md` |
| 2 | العمارة والمكدس | ✅ | `docs/03-architecture/architecture-overview.md` · `technology-stack.md` |
| 3 | أسلوب الكود والتسمية ومعالجة الأخطاء | ✅ | `docs/04-design/coding-standards.md` · `naming-conventions.md` · `error-handling-strategy.md` |
| 4 | تعريف «منجَز» والتفرّع والالتزام والإصدار | ✅ | `docs/07-development/*` · `docs/12-deployment-and-release/versioning-scheme.md` |
| 5 | استراتيجية الاختبار | ✅ | `docs/08-testing-and-qa/test-strategy.md` |
| 6 | سياسة الأسرار | ✅ | `docs/09-security/secrets-management-policy.md` |
| 7 | `srs.md` بمتطلبات مرقّمة | ✅ | `docs/02-requirements/srs.md` |
| 8 | ⛔ **لا `ADR` بحالة «مقترح» يحكم ما يُنفَّذ** | ✅ | **`ADR-0001` … `ADR-0023` كلها «مقبول»** — ★ **مقيسٌ بفحص حقل الحالة في الملفات الثلاثة والعشرين** (⚠️ **و`ADR-0016` و`ADR-0017` تحملان كلمة «مقترح» داخل تعليق HTML يعدّد القيم الممكنة وحده** — ⛔ **لا في حقل الحالة**) |

### 2.2 بوابة الوحدة (`WU-013` — 5 شروط · §1.2)

| # | الشرط | متحقق؟ | المسار |
|:-:|---|:-:|---|
| 1 | تصميم الوحدة موجود | ✅ | `docs/04-design/module-design/settlement-design.md` §7 (**الفرق الملزم بين `M12` و`M13`**) |
| 2 | المتطلب بمعايير قبول | ✅ | `docs/02-requirements/functional-requirements/FR-M13.md` (13 بنداً) |
| 3 | عقود الواجهات | ✅ | `docs/05-api/api-overview.md` · `authentication-and-authorization.md` |
| 4 | عقود البيانات | ✅ | `docs/06-database/erd.md` · `data-dictionary.md` · `schema/discounts.md` |
| 5 | سياسة المصادقة | ✅ | `docs/09-security/authentication-policy.md` |

★ **وزيادةً على ما يطلبه `SKILL.md`** — بروتوكول التشغيل §ح: **قُرئت
`design-system.md` و`design-tokens.md` و`ui-guidelines.md` قبل كتابة الشاشة**
⛔ **لا `module-design` وحده** — ★ **وأُضيفت الثلاثةُ إلى «المستندات الحاكمة»
للوحدة** ⟵ **فيلتقطها كشفُ الانحراف تلقائياً.**

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

⛔ **لا شيء** — ★ **و`WU-013` لم تُرفَع** لسببٍ مذكورٍ صراحةً في §3.2.

### 3.2 وحدات قيد التنفيذ

| WU | العنوان | الحالة | ما يحجب الإكمال |
|---|---|---|---|
| **`WU-013`** | زيادة: الخصومات من طرف إلى طرف | ⏳ **`[قيد التنفيذ]`** | ⛔⛔★★★ **نشرُ خدمة العمليات على التجريبية** — ★ **المسارات الثلاثة تردّ `404`**، ⟵ **فبندُ «يخفض الضمار فعلاً» لم يُقَس** |

★ **التفصيلُ الكامل بأرقامه في** [`implementation-plan.md`](implementation-plan.md)
تحت `WU-013` — ★ **ومنه جدولُ اختبار المحاكي الخمسةَ عشر.**

### 3.3 ⚠️★★ ما قِسْتُه من المرحلة الأولى قبل التحويل — ⛔ **لا يُقدَّم إغلاقاً**

★ **قبل رسالة المالك، شُغِّل جزءٌ من سلسلة اليوم التشغيلي على المحاكي**،
✅ **وما يلي مقيسٌ بعيني على `Pixel_6_API_36` (2026-09-01):**

| # | ما قِيس | النتيجة |
|:-:|---|---|
| ① | **الدخول الآلي بحساب QA والشريطُ الموحّد** | ✅ **«متصل» بقياسٍ من بثِّ Firestore** ⛔ **بلا رجوعٍ ولا خروج** (`AM-008` · `AM-009` · `ADR-0003`) |
| ② | **الهويةُ البصرية** | ✅ **شعارُ العميل واسمُه وحدَهما** ⛔ **بلا بادئة `QTMS` وبلا شعار جهة التطوير** (`AM-002`) |
| ③ | **مركزُ الإدخالات المعلّقة** | ✅ **عدّادٌ حيٌّ = 5** (`FR-SYS-03`) |
| ④ | ★★ **وارد عدداً جديد** | ⛅ **`INC-20260901-0002` — «مصدر الاختبار الأول · 1 نوع · 120 حبة»** · ✅ **والتاريخ من الخادم ولا يُغيَّر** (`AT-55` · `GR-14`) · ✅ **وحُفِظ بلا سبب نصّي** (`ADR-0020`) |
| ⑤ | ★★★ **المخزون يُشتقّ من الدفتر** | ✅ **عتود: وارد 220 · صادر 20 ⟵ 200 حبة** (`ADR-0008`) — ★ **و120 منها من ④** |
| ⑥ | ⛔⛔★★★ **`AT-05` — عزلُ المخزون بالمصدر** | ✅ **«المخزن فارغ اليوم» على المصدر الثاني** بينما الأول 200 (`ADR-0005`) |
| ⑦ | ★★ **`AT-19` — التسعير يتبع المخزون** | ✅ **«لا شيء يُسعَّر بعد» على المصدر الثاني** |
| ⑧ | ★ **المعدود والوزني لا يُجمَعان** | ✅ **«إجمالي المعدود: 200 حبة» · «إجمالي الوزني: 0.000 كجم»** سطران منفصلان |

⛔⛔★★★ **وهذه ثمانيةُ سيناريوهات من سلسلةٍ لم تكتمل** — ★ **ولا تُساوي
اختبارَ §د.3 التكاملي للمرحلة كاملةً**: ⟵ **فالمالكُ أفاد أنه أتمّه**،
⛔ **ولم أرَ مخرَجاتِه.**

### 3.4 وحدات محجوبة (ولماذا)

⛔ **لا وحدةَ `[محجوب]`** — ⚠️★★ **و`IQ-036` لا يحجب `WU-013`:** ★ **يخصّ
`FR-M13-12` وحده** (بندٌ واحد من ثلاثة عشر)، ⟵ **ومعيارُ قبول الزيادة
نفسِه لا يذكر التصدير ولا الإرسال.** ⛔ **والوقوفُ الحالي وقوفُ بيئةٍ لا حجبُ
قرار** (بروتوكول التشغيل §ج البند 5: «**`[قيد التنفيذ]` لا `[محجوب]`**»).

---

## 4. مخالفات النزاهة المرصودة آلياً

**لا مخالفات** — ⛅ **مقيسٌ بـ`track_implementation.py`:**
`"has_integrity_violations": false` · `"integrity_violations": []` ·
`done: 14` · `in_progress: 1` · `planned: 12`.

⚠️★★ **وثلاثةُ حرّاس عدٍّ أفشلوا البناء عمداً وحُدِّثوا بوعي** — ★ **وهذا
عملُهم لا عطلُهم:**

| الحارس | كان | صار | لماذا |
|---|:-:|:-:|---|
| `permission_test.dart` (`catalog.length` و`Permission.values.length`) | 81 | **82** | ★ **`discountBackdate`** — راجع §6.1 |
| `permission_labels_test.dart` (`catalog.length`) | 81 | **82** | نفسُه |
| `callable_router_test.dart` (مجموعةُ المسارات) | 41 | **44** | ★ **ثلاثُ عمليات خصم** |

⛔⛔★★ **ولم يُخفَّف حارسٌ منها ولم يُحذَف** — ★ **بل حُدِّث رقمُه ومعه تعليقٌ
يذكر سبب التغيير**: ⟵ **فالحارسُ التالي يقرأ لماذا تغيّر العدد** ⛔ **لا
يجده مُعدَّلاً بلا أثر.**

⚠️★★ **وخطأٌ منهجيٌّ ارتكبتُه وأُصلح — يُقال لأنه كاد يُخفي فشلاً:**
★ **شُغِّلت الحزم أول مرة بـ`dart test 2>&1 | tail -2`** — ⛔ **ورمزُ خروج
الأنبوب رمزُ `tail` لا رمزُ `dart`**: ⟵ **فبدت الحزمتان ناجحتين وفيهما ثلاثةُ
إخفاقات.** ✅ **ورُصد بإعادة التشغيل بـ`grep` على سطر النتيجة** — ★ **والدرسُ
أن مخرَجَ الأنبوب لا يُقرأ رمزَ خروجٍ للأمر الأول.**

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

| IQ | السؤال | الوحدة | الملف/الدالة | لماذا لم يكن قابلاً للاستنتاج |
|---|---|---|---|---|
| **`IQ-036`** ⏳ **مفتوح** | **نصُّ إيصال سند الخصم وإرسالُه** (`FR-M13-12`) | `WU-013` (**جزئياً — بندٌ من 13**) | `message_templates.dart` · `export_documents.dart` · `discount_screen.dart` | ★ **`FR-M20-06` يجعل نصَّ القوالب قرارَ المالك حرفياً** (`IQ-031`) **و`FR-M20-07` يُسمّي أربعةَ قوالب لا خمسة** ⟹ **فصياغةُ نصٍّ اختلاقُ عقدٍ يصل يدَ العميل** · ★★ **وسابقتُه المباشرة `IQ-035` أجاب عنها المالك «لا إيصالَ إطلاقاً»** ⟹ [`CR-008`] — ⛔ **فافتراضُ «نعم كالتوزيعة» كان سيُنتج القالبَ الذي رُفض نظيرُه قبل يومٍ واحد** |

راجع التفاصيل الكاملة والخيارات الأربعة في:
`docs/00-governance/upds-workflow/implementation-questions.md`

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

### 6.1 ⚠️★★ مفتاحُ صلاحيةٍ يُسمّيه المتطلب ولا يوجد في الكتالوج — **أُضيف**

```text
⚠️ انحراف توثيقي مكتشَف
الموقع: docs/09-security/permissions-catalog.md §2.5
نوع الانحراف: متطلبٌ بلا مقابل في الكتالوج
التفصيل: `FR-M13-06` ينصّ «يقبل سابقاً بصلاحية **«خصم بتاريخ سابق»**»
         و`FR-M13` §4 يُدرِجها ضمن ثماني صلاحيات — ⛔ ولا مفتاح لها في
         الكتالوج، بينما نظيرُها `receiptBackdate` قائمٌ منذ `WU-007`.
الإجراء: ✅ أُضيف `discountBackdate` (الكتالوج **81 ⟵ 82**) — ★ ومستندُ
         الكتالوج ليس بوابةَ حَوْكمة (UPDS-05 §4)، ⛔ ولا `srs.md`
         يُمَسّ ولا `ADR` — ★ والقاعدة ⑤ من §6 في الكتالوج نفسِه تفرض
         إسنادَ كل مفتاحٍ جديد لحساب QA في نفس التشغيل.
```

⛔⛔★★ **ولماذا لم يُعَد استعمال `receiptBackdate`:** ★ **لأن الكتالوج §2.9
يُفرِد لكل نوع مستندٍ مفتاحَي تعديلٍ وإلغاءٍ مستقلَّين** ⟵ **فالنمطُ المعتمد
مفتاحٌ لكل عملية**، ★ **ولأن `IQ-020` و`IQ-021` حسما سابقاً أن عمليةً
مُسمّاةً مستقلةً تأخذ مفتاحَها** ⛔ **ولا يُغني عنها مفتاحٌ مجاور** — ⟵ **ومن
يُصحِّح تاريخ قبضٍ ليس بالضرورة من يُسقِط ديناً بأثرٍ رجعي.**

### 6.2 ⛔⛔★★ خطأُ نزاهةٍ ثانٍ في `implementation-plan.md` — **صُحِّح**

```text
⚠️ انحراف توثيقي مكتشَف
الموقع: implementation-plan.md — بند WU-013، حقل «معيار القبول»
نوع الانحراف: معيارُ قبولٍ يشير لاختبارات وحدةٍ أخرى
التفصيل: كان يذكر `AT-41` · `AT-42` — وتلك اختباراتُ `M22` السحبيات
         والخرجيات بنصّ requirements-traceability-matrix.md §4
         (`AT-38` … `AT-43` ⟵ `M22`). والصحيحُ `AT-33` … `AT-35` ⟵ `M13`،
         وهي منصوصةٌ في FR-M13 حرفياً (AT-33 في FR-M13-03 · AT-34 في
         FR-M13-08 · AT-35 في FR-M13-05).
الإجراء: ✅ صُحِّح بحاشيةٍ تُبقي الخطأ مقروءاً ⛔ لا بمحوٍ صامت.
```

⚠️⚠️★★★ **وهذا ثاني تكرارٍ للعطل نفسِه** — ★ **الأولُ في `WU-012`** (صُحِّح
في التشغيل السابق): ⟵ **فالنمطُ أن الأرقام نُسخت من وحدةٍ مجاورة** ⛔ **لا
استُخرجت من المصفوفة**، ★ **ويُوصى بفحص معايير قبول الوحدات الاثنتَي عشرةَ
المتبقية قبل بدء كلٍّ منها** ⛔ **لا بعده.**

### 6.3 ⚠️★★ تعارضٌ بين `product-roadmap.md` و`requirements-traceability-matrix.md` — **مُبلَّغٌ لا مَحسوم**

```text
⚠️ انحراف توثيقي مكتشَف
الموقع: docs/01-product/product-roadmap.md §1 و§2 و§3
        مقابل docs/02-requirements/requirements-traceability-matrix.md §4
نوع الانحراف: مستندان يختلفان في تعيين اختبارات القبول للمراحل
التفصيل: ① الخارطة تجعل معيارَ اكتمال المرحلة الأولى `AT-01` … `AT-37`،
           وهو مدىً متصلٌ يبتلع `AT-26`…`AT-28` (`M11` البيع النقدي)
           و`AT-33`…`AT-35` (`M13` الخصومات) — ★ وكلاهما وحدةُ **مرحلةٍ
           ثانية** بنصّ §2 من الخارطة نفسِها وبعمود المرحلة في المصفوفة.
         ② والخارطة تجعل `AT-63`…`AT-66` معيارَ المرحلة **الثالثة**،
           بينما المصفوفة تُسنِد `AT-63`…`AT-64` (`M20`) و`AT-65` (`M21`)
           للمرحلة **الأولى** — وكلاهما وحدةُ مرحلةٍ أولى بنصّ §1.
الأثر: ⛔ القراءة الحرفية للخارطة تجعل إغلاق المرحلة الأولى **مستحيلاً**
       قبل بناء `M13` — وهو نقيضُ ترتيب §5 من الخارطة نفسِها.
الإجراء المقترح: تصعيدٌ للمالك — ⛔ ولم يُحسَم ذاتياً: ★ فتعيينُ معيار
       إغلاق مرحلةٍ بوابةُ حَوْكمة (UPDS-04 §2.2)، ⛔ لا تفصيلٌ تنفيذي.
```

⛔⛔★★ **ولم يُعدَّل أيُّ مستندٍ من الاثنين** — ★ **ويُذكَر هنا لأنه سيصير
حاسماً عند إغلاق المرحلة الثانية**، ⟵ **وقد كان سيصير حاسماً في هذا التشغيل
لولا أن المالك أعلن اكتمال اختبار المرحلة الأولى.**

### 6.4 ★ قراراتٌ تفصيلية حُسمت ذاتياً (لا حَوْكمة فيها)

★ **مذكورةٌ بتمامها في** [`implementation-plan.md`](implementation-plan.md)
تحت `WU-013` — **الملاحظات ① … ⑤** (بروتوكول التشغيل §ب الفئة 3).

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

| # | البند | الحالة |
|:-:|---|---|
| ① | ⛔⛔★★★ **نشرُ خدمة العمليات على التجريبية** | ⏳ **مطلوبٌ الآن** — ★ **الأمرُ مُسلَّمٌ في الردّ النصّي** (§ج · §ز) · ⛔ **ولم يُنفَّذ أمرُ نشرٍ من الجلسة** |
| ② | **`IQ-036`** | ⏳ **ينتظر جوابك** — أربعةُ خيارات |
| ③ | ⛔⛔★★★ **[`CR-007`] — الحدُّ الأدنى منعٌ مطلق** | ⏳ **ما زال ينتظر توقيعك** — ⛔ **لم يُمَسّ في هذا التشغيل** |
| ④ | ⛔⛔ **[`CR-008`] — شطبُ إيصال البيع النقدي** | ⏳ **ما زال ينتظر توقيعك** |
| ⑤ | ⛔⛔★★★ **إغلاقُ المرحلة الأولى رسمياً** | ⚠️ **أفدتَ باكتمال §د.3** — ⛔ **وبوابةُ `product-roadmap.md` §5 لم تُوثَّق بنتيجةٍ مكتوبة**، ★ **ومعيارُها نفسُه محلُّ التعارض في §6.3** |
| ⑥ | ⛔⛔ **أيُّ نشرٍ إنتاجي** | ⛔ **لم يُطلَب ولم يُجهَّز** — ★ **ولا `uat-signoff` ولا `release-checklist` مُسّا** |

⛔⛔★★★ **ولا بندَ حَوْكمةٍ اعتُمد ذاتياً:** ⛔ **ولم يُعدَّل نصُّ `FR-M13`
بحرف** — ★ **والمتطلب معتمدٌ بـ`srs.md`.** ⛔ **ولا `ADR` جديدٌ ولا `RFC`.**
⛔ **ولا عمليةَ Git خارجية** (`push` / `PR` / `merge`) — ★ **ولم تُطلَب.**

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

| WU | لماذا هي حسّاسة |
|---|---|
| **`WU-013`** | ⛔⛔★★★ **إسقاطُ دَينٍ حقيقي** — ★ **`FR-M13-09` ينصّ أن صلاحيته «لا تُمنَح افتراضياً»**، ⟵ **فمن يملكها يُسقِط ذمماً بلا نقدٍ يدخل** · ★★ **وتُضيف مفتاحَ تفويضٍ جديداً** (`discountBackdate`) **يفتح الأثرَ الرجعي** ⟹ **فهي في الفئة 1 البند 5** (بوابةُ الأمن — §ب.1) |

⛔⛔ **ونجاحُ 1942 اختباراً وخمسةَ عشرَ فحصاً حيّاً *لا* يُغني عن المراجعة
البشرية** — ★ **شرطٌ ضروريٌّ لا كافٍ** (`implementation-playbook.md` §7).
⚠️ **وتبقى مراجعاتُ `WU-004` و`WU-011` و`WU-012` الأمنيةُ قائمةً كما هي** —
⛔ **لم تُمَسّ.**

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث | الإثبات |
|---|---|---|
| `docs/05-api/api-overview.md` | ★ **§3.1-ك جديد** — **ثلاثُ عملياتٍ مستدعاة** بمدخلاتها ومخرجاتها وشروطها ورموز فشلها (UPDS-03 §4 البند 1) | قسمٌ جديد قبل §3.1-ط |
| `docs/04-design/module-design/settlement-design.md` | **حالةُ `M13` من «لم يُنفَّذ» إلى «مبنيٌّ ومُختبَرٌ آلياً ⛔ غيرُ مُثبَتٍ حيّاً»** + **حاشيةُ §7** (UPDS-03 §4 البند 5) | السطر 5 و§7 |
| `docs/06-database/schema/discounts.md` | **حاشيةُ التنفيذ ومسارُ الكتابة والمفتاحان** | قبل «## الفهرس» |
| `docs/06-database/data-dictionary.md` | **جدولُ حقول `discounts` الفعلية** (UPDS-03 §4 البند 3) | §`discounts` |
| `docs/02-requirements/requirements-traceability-matrix.md` | **عمودُ «الكود» لـ`FR-M13-01`…`13`** بحالةٍ 🟨 (**مبنيٌّ · غيرُ منشور**) | السطر 53 |
| `docs/09-security/permissions-catalog.md` | ★★ **`discountBackdate` — 81 ⟵ 82** · **§2.5 (6) ⟵ (7)** · **عدّادُ البوابة ③** | §2.5 |
| `docs/00-governance/upds-workflow/implementation-plan.md` | **بندُ `WU-013` كاملاً** + **تصحيحُ معيار القبول** (§6.2) | بند `WU-013` |
| `docs/00-governance/upds-workflow/implementation-questions.md` | **`IQ-036` فُتح** | آخرُ الملف |

⛔⛔★★★ **و`firestore.rules` لم تُمَسّ بحرف** — ★ **`discounts` مغلقةٌ
للكتابة منذ `WU-026`**، ⟵ **فلا قاعدةَ كتابةٍ تُفتَح** (`ADR-0013` القاعدة 2).
⛔ **و`erd.md` لم يتغيّر** — ★ **لا مجموعةَ جديدة ولا علاقةَ جديدة**:
**`discounts` معرَّفةٌ فيه سلفاً.**
⛔ **و`migrations-log.md` لم يتغيّر** — ★ **لا هجرةَ بيانات** (**مجموعةٌ جديدةٌ
تبدأ فارغة** ⛔ **ولا حقلَ أُضيف لمجموعةٍ قائمة**).
⛔ **و`technical-debt-register.md` لم يتغيّر** — ★ **ولا دَينَ جديداً فُتح:**
★ **الثغرةُ الوحيدةُ المرصودة (ستُّ تسمياتٍ عربية ناقصة منذ `WU-007`) سُدَّت
في التشغيل نفسِه** ⛔ **ولم تُرحَّل.**

---

## 10. حالة `CLAUDE.md`

⏳ **لم يُحدَّث في هذا التشغيل** — ★ **ولا نقطةَ تحوّل وقعت بعد**
(`claude-md-management.md` §2): ⟵ **البندُ 5 «اكتمالُ وحدة عمل»**،
⛔ **و`WU-013` `[قيد التنفيذ]` لا `[منجز ✅]`.**

⚠️★★ **وثلاثُ قيمٍ فيه ستصير قديمةً حالما تُرفَع الوحدة** — ★ **وتُحدَّث
حينها في تشغيلٍ واحد**: **البوابة `1857 ⟵ 1942`** · **الكتالوج `81 ⟵ 82`** ·
**و`IQ` `35/35 ⟵ 35/36`** (⛔ **فالجملة «كل بنود `IQ` محسومة» لم تعد صادقة**).
★ **والملف اليوم 249 سطراً** — **والسقف 250** (`AM-005`).

---

## 11. الخطوة التالية الموصى بها

```text
① المالك ينشر خدمة العمليات على التجريبية (الأمر في الردّ النصّي)
   ⟹ ثم أُكمِل §د.2 لـWU-013: إنشاءٌ فعليٌّ برقم DSC- · حركةٌ دائنة
      بنوع «خصم» · discountedAmount في pricing/current · قيدُ تدقيق
      · تعديلٌ وإلغاء — ثم تُرفَع الوحدة [منجز ✅]
② ويُجيب IQ-036 (إيصال سند الخصم — أربعة خيارات)
③ ويوقّع CR-007 وCR-008 المعلّقين من التشغيل السابق
④ ويحسم تعارضَ §6.3 (أي اختبارات قبولٍ تُغلِق أي مرحلة)
⑤ ثم تُستأنف المرحلة الثانية: WU-014 (السحبيات والخرجيات) فما بعدها
```

⚠️★★ **وتوصيةٌ من §6.2:** ★ **تُفحَص معايير قبول الوحدات الاثنتَي عشرةَ
المتبقية مقابل `requirements-traceability-matrix.md` §4 قبل بدء كلٍّ منها** —
⟵ **فالخطأُ وقع مرتين متتاليتين** (`WU-012` ثم `WU-013`)، ⛔ **ولا يرصده
`track_implementation.py`.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
