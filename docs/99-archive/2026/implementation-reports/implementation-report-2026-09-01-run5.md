# تقرير تنفيذ برمجي — UPDS DocGen — 2026-09-01

**رقم تشغيل التنفيذ:** 5 (في هذا اليوم) · **الثامن عشر إجمالاً**
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة**)
**تقدّم التنفيذ:** 51.9٪ (14 منجَزة من 27) — ⛔ **بلا تغيير**
**حالة التوثيق:** 100٪ (0 بند مفتوح)
**أسئلة تنفيذ مفتوحة:** **0** — ✅ **`IQ-036` حُسم في هذا التشغيل**

> ⚠️ **هذا تشغيلٌ تكميليٌّ لـ`run4`** — ★ **لم يُبنَ فيه كودُ زيادةٍ جديد**:
> ⟵ **بل نُفِّذ ما كان محجوباً بالنشر، وقِيس حيّاً، وحُسم `IQ-036`، وعُدِّل
> بروتوكولُ التشغيل بطلب المالك.** ★ **وأُوقِف بطلبه صراحةً عند نقطةٍ محدّدة
> موصوفةٍ في §11.**

---

## 1. ملخص تنفيذي

⛅★★★ **نشرَ المالكُ خدمةَ العمليات** ⟵ **المراجعة `qtms-callables-00024-x48`**
(الصورة `callables:wu014` · البناء `685bf35b-…` · `SUCCESS` · `55S`) —
✅ **فانفكّ حجبٌ ثلاثيّ دفعةً واحدة.**

✅★★★ **و`WU-014` قِيست حيّاً من طرفٍ إلى طرف بأربعةَ عشرَ قياساً + ثلاثةِ
حرّاس** — **سندان أُنشئا من الشاشة على المحاكي وقُرئا من التجريبية حقلاً
بحقل**: `WDR-20260901-0001` · `EXP-20260901-0001`. ✅ **والتعديلُ والإلغاءُ
نجحا** ⟵ **وسطورُ الدفتر الرابع وُسمت `isCancelled`.**

⛔⛔★★★ **وعطلٌ حاجبٌ واحدٌ كشفه القياسُ الحيّ — `DEBT-77`:** **سندٌ بمبالغَ
فقط بلا بندِ قاتٍ يفشل بـ`ERR_CALL_500`** — ★ **والسببُ الجذري مُشخَّصٌ
بالسطر** (`readTime` من الاستعلامات وحدها)، ⛔ **والعلاجُ لم يُطبَّق بعد.**
⟹ **`WU-014` تبقى `[قيد التنفيذ]`.**

✅ **و`IQ-036` حُسم بالخيار ب** ⟹ [`CR-009`] **مُصاغٌ وينتظر التوقيع.**
✅★★★ **و[`AM-010`] طُبِّق** — **أوامرُ التجريبية تُنفَّذ ذاتياً من الآن**،
⛔ **وحدُّ الإنتاج لم يتغيّر بحرف.**

---

## 2. بوابة الدخول

⛔ **لم تُعَد** — ★ **تشغيلٌ تكميليٌّ لـ`run4`** الذي فحصها كاملةً
(8 شروط عامة + 5 للوحدة). ✅ **ولم يتغيّر شرطٌ منها.**

---

## 3. ما نُفِّذ فعلياً في هذا التشغيل

### 3.1 ⛅ عملياتُ بيئةٍ نُفِّذت — **بمخرَجاتها الفعلية**

| # | العملية | المخرَج المقيس |
|:-:|---|---|
| ① | **نشرُ المالك** (`builds submit` + `run deploy`) | ✅ `685bf35b-3064-42a4-8014-71c283972016` · `SUCCESS` · `55S` ⟹ **`qtms-callables-00024-x48` تحمل 100٪** |
| ② | ★ **قياسُ المسارات** (قراءةٌ فقط) | ✅ **`createOutflow` · `amendOutflow` · `cancelOutflow` ⟵ `401` الثلاثة** (**كانت `404`**) · **ومجهولٌ ردّ `404`** · ✅ **و`createDiscount` ⟵ `401`** |
| ③ | ⚠️★★ **عنوانان يعملان معاً** | **القديم `…-7stgg3ngga-ww.a.run.app`** و**الجديد `…-537433591371.me-central1.run.app`** ⟹ ✅ **فلا إعادةَ بناءٍ للتطبيق** ⛔ **ولا تغييرَ في `config/qtms-public-defines.env`** |
| ④ | ✅★★ **مزامنةُ حساب QA** (`AM-004` ⑤) | `qa_permission_sync.py` ⟹ **`grantPermissions -> 200`** · **`users/QA -> 200 · permissions_true = 85`** (**كان 81**) |
| ⑤ | ★ **بناءُ التطبيق وتشغيلُه على `Pixel_6_API_36`** | ✅ **`app-staging-debug.apk`** بنكهة التجريبية ودخولٍ آليٍّ بحساب QA |
| ⑥ | ⛅ **كتابةُ بياناتٍ تجريبية** (فئةٌ 3 بـ`AM-010`) | **سندان من الشاشة** + **تعديلٌ وإلغاءٌ وأربعةُ طلباتِ حرّاس** عبر المسار السحابي |

### 3.2 ✅ ما قِيس حيّاً لـ`WU-014` — **أربعةَ عشرَ قياساً**

★ **الجدولُ الكامل بأرقامه في** [`implementation-plan.md`](implementation-plan.md)
تحت `WU-014` §«القياسُ الحيّ بعد النشر». **وأخصُّه:**

| المحور | النتيجة |
|---|---|
| **الإنشاء** | ✅ `WDR-20260901-0001` (5 × 1,500 ⟹ 7,500) · `EXP-20260901-0001` (بندُ قاتٍ غير مسعَّر + 4,000) |
| ⛔⛔★★★ **`GR-44`** | ✅ **مقيسٌ على `dealer_ledger` نفسِه: صفرُ حركات** — ⛔ **ولا حقلَ مقوتٍ في المستند** |
| ★★★ **`GR-42`** | ✅ **`ERR_OUT_001` بالرمز المستقل** على سندٍ بمصدرٍ فارغ |
| ★★★ **`GR-43`** | ✅ **ادّعاءُ «خرجية» على سندِ سحبيةٍ رُفض** — ★ **من حارس بادئة الرقم قبل حارس المخزَّن**: **دفاعٌ في عمق** |
| ★★★ **`GR-49`** | ✅ **تاريخان مستقلان في المستند وسطر الدفتر** · ⛔ **وسطرُ المبلغ بلا `stockDate`** |
| ★★★ **`data-dictionary`** | ✅ **`lines[]` بحقل `itemType`** مقروءاً من المخزَّن — ⛔ **ولا `qatLines`/`cashLines`** |
| ★★★ **الأثر المخزني** | ✅ `balanceAfter: 195` ⟵ ثم `189` بعد التعديل · ✅ **وشاشةُ المخزون «وارد 220 · صادر 25 ⟹ 195»** |
| ★★★ **قيدُ التدقيق** | ✅ **بكلِّ حقولِه بالعربية** — ⛔ **ولا مفتاحٍ إنجليزيٍّ واحد** |
| ★★★ **المركزُ المعلّق** | ✅ **بندٌ واحد بعنوان «شُقى الشُقّات» بتاريخ المخزون** · ★ **ومرشِّح «سحبية أو خرجية» ظهر** |
| ★★★ **التعديل** | ✅ **`ledgerEntryCount: 1 ⟵ 2`** · **سطرٌ ثانٍ في الدفتر** · **رصيدٌ مُعاد بناؤه** |
| ★★★ **الإلغاء** | ✅ **`status: cancelled`** · **سطرا الدفتر وحركةُ المخزون `isCancelled: true`** · ✅ **`ERR_AMEND_006` على إلغاءٍ ثانٍ** |

### 3.3 ⛔ ما بقي غيرَ مقيس

| # | البند | لماذا |
|:-:|---|---|
| ① | ⛔⛔ **سندُ مبالغَ خالص** | ★ **يفشل بـ`500`** — `DEBT-77` |
| ② | ⚠️ **`item_daily_balances` بعد الإلغاء** | ★ **حركةُ `EXP` وُسمت ملغاة** ✅ ⛔ **ولم أقرأ مستندَ الرصيد بعدها** (المتوقَّع 192): ⟵ **أُوقِف التشغيل قبل القياس** |
| ③ | ⛔ **`ERR_OUT_002`** (تسعيرٌ بلا مفتاحه) | ★ **حسابُ QA يملك كلَّ المفاتيح** ⟹ **لا يمكن قياسُه به**: ⟵ **يلزم حسابٌ منقوصُ المفاتيح** |
| ④ | ⛔ **`FR-M22-16`** (تقرير `R-23` · `AT-42`) | **موضعُه `WU-018`** |

---

## 4. مخالفات النزاهة

**لا مخالفات** — `has_integrity_violations: false` · `integrity_violations: []`.

⚠️★★ **و`WU-013` ما زالت تكتب فحوصَها جدولاً** ⟹ **`checks = 0`** —
⛔ **فلا تُرفَع `[منجز ✅]` قبل تحويلها إلى نقاط** (رُصد في `run4` §4.1).

---

## 5. أسئلة التنفيذ

✅ **`IQ-036` — [تم الحل ✅]** · **الجواب: «الخيار ب»** ⟹ **رُحِّل إلى
[`CR-009`](../../02-requirements/change-requests/CR-009-drop-discount-receipt.md)**
⏳ **الذي ينتظر توقيعك.** ⛔ **ولا بندَ `IQ` مفتوحٌ الآن.**

---

## 6. تعارضات وثغرات

### 6.1 ⛔⛔★★★ `DEBT-77` — **العطلُ الحاجب** (التفصيل في §1-ب من سجل الديون)

★ **سندُ مبالغَ خالصٍ ⟹ `500`** · **السبب:** `readTime` من الاستعلامات وحدها
⟹ `platformDayOf` تردّ العدم. ✅ **والعلاجُ مُصمَّم** ⛔ **ولم يُطبَّق.**

### 6.2 ⚠️★★ `DEBT-40` وقع على أداتي أنا — **الرابعةَ في المشروع**

★ **`verify_outflow_write.py` استعلم بـ`sourceDocNumber` وحده ⟹ `403` ثلاثَ
مرات** — ⟵ **والاستعلامُ يجب أن يُقيّد حقلَ شرطِ قراءته** (`sourceId`
و`ledgerType`). ✅ **أُصلح والتصفيةُ برقم المستند صارت *بعد* الجلب.**

### 6.3 ⛔⛔★★ وحارسٌ نجح لسببٍ خاطئ — **في أداتي أيضاً**

★ **`403` طُبعت «✅ صفرُ حركات» في `dealer_ledger`** — ⟵ **وحارسٌ يمرّ لأن
القراءة فشلت أسوأ من غياب حارس.** ✅ **أُصلح: `403` تُطبَع الآن «لم يُفحَص».**

⟹ ★★ **ودرسُ الاثنين واحد:** **أدواتُ التحقق نفسُها تحتاج من يتحقق منها.**

---

## 7. بنود بوابة الحَوْكمة

| # | البند | الحالة |
|:-:|---|---|
| ① | **[`CR-009`]** — شطبُ `FR-M13-12` | ⏳ **مُصاغٌ وينتظر توقيعك** — ⛔ **ولا يُعدَّل `FR-M13.md` قبله** |
| ② | ⛔⛔ **[`CR-007`]** — الحدُّ الأدنى منعٌ مطلق | ⏳ **ما زال ينتظر** — ⛔ **لم يُمَسّ** |
| ③ | ⛔⛔ **[`CR-008`]** — شطبُ إيصال البيع النقدي | ⏳ **ما زال ينتظر** — ⛔ **لم يُمَسّ** |
| ④ | ⛔⛔ **إغلاقُ المرحلة الأولى رسمياً** | ⚠️ **ما زال معلَّقاً** |
| ⑤ | ⛔⛔ **أيُّ نشرٍ إنتاجي** | ⛔ **لم يُطلَب ولم يُجهَّز** |

✅★★★ **و[`AM-010`] طُبِّق بطلبك النصّي** — ⛔ **ولا يُعدّ اعتماداً ذاتياً**:
★ **طلبٌ صريحٌ من المالك وُثِّق ثم نُفِّذ.**
⛔⛔ **ولا `ADR` جديدٌ ولا `RFC` ولا عمليةَ Git خارجية.**

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

| WU | لماذا |
|---|---|
| **`WU-014`** | ⛔⛔★★★ **إخراجُ نقدٍ وبضاعة** · **وسحبياتُ المالك بياناتٌ حسّاسة** (`GR-43`) — ★ **وأخصُّ ما يُراجَع:** `outflowPermission` · `_existenceGate` (**مقابلةُ `ledgerType` المخزَّن**) · **شرطُ قراءة `outflows` في القواعد** |
| **`WU-013`** | ⛔⛔ **إسقاطُ دَينٍ حقيقي** |

⚠️ **وتبقى مراجعاتُ `WU-004` و`WU-011` و`WU-012` قائمةً** — ⛔ **لم تُمَسّ.**

---

## 9. مستندات UPDS التي حُدِّثت

| المستند | التغيير | الإثبات |
|---|---|---|
| `implementation-run-protocol.md` | ★★★ **§ز أُعيد صوغُه كاملاً** (`AM-010`) + **§ب.3 جديدة** — ⛔ **والنصُّ الأصلي محفوظٌ في §ز.4** | السطران 285 و457 |
| `amendment-log.md` | ★★★ **`AM-010` جديد** بستةِ أقسام | آخرُ الملف |
| `change-requests/CR-009-drop-discount-receipt.md` | ★★ **أُنشئ** (⛔ **أُنشئ لا «حُدِّث»**) | ملفٌّ جديد |
| `implementation-questions.md` | ✅ **`IQ-036` ⟵ [تم الحل ✅]** + `**رُحِّل إلى:**` | بندُ `IQ-036` |
| `implementation-plan.md` | ★★★ **قسمُ القياس الحيّ (14 قياساً + 3 حرّاس)** · **`DEBT-77`** · **الفحصان ⑪ و⑫ بنتائجهما** · **نتيجةُ التحقق أُعيد صوغُها** | بندُ `WU-014` |
| `technical-debt-register.md` | ★★★ **`DEBT-77` في §1 (الدين الحاجب) + §1-ب تفصيلُه** | السطران 22 و24 |

⛔⛔ **و`firestore.rules` و`firestore.indexes.json` لم تُمَسّا** — ★ **مقيسٌ
بـ`git status`.**

### 9.1 ★ أدواتٌ جديدة (`tools/staging/`)

| الأداة | الدور |
|---|---|
| `verify_outflow_write.py` | ⛅ **قراءةٌ محضة** — يقرأ المستند وسطورَ الدفتر وحركةَ المخزون ويفحص `GR-44` على `dealer_ledger` نفسِه |
| `exercise_outflow_amend_cancel.py` | ★ **يُشغِّل التعديلَ والإلغاءَ والحرّاس** — ⟵ **لأن الشاشة بلا واجهةِ تعديلٍ ولا إلغاء بعد** (`WU-018`) |

★ **كلاهما بضمانات `AM-004`** — ⛔ **ولا قيمةَ سرٍّ تمرّ ولا تُطبَع.**

---

## 10. حالة `CLAUDE.md`

⏳ **لم يُحدَّث** — ⛔ **ولا نقطةَ تحوّل**: **لا وحدةَ اكتملت** (`WU-014` ما
زالت `[قيد التنفيذ]` بـ`DEBT-77`).

⚠️⚠️★★★ **وخمسُ قيمٍ فيه قديمةٌ الآن — تُحدَّث في التشغيل الذي يُغلق الوحدتين:**
① **البوابة `1857 ⟵ 2069`** · ② **الكتالوج `74 ⟵ 85`** (**السطرُ الحالي أقدمُ
من ذلك**) · ③ **`IQ` `35/35 ⟵ 36/36`** · ④ **العملياتُ المنشورة `41 ⟵ 47`**
(**المراجعة `00023-6d2 ⟵ 00024-x48`**) · ⑤ ⛔⛔★★★ **وسطرُ الحَوْكمة (ز) صار
مخالفاً لـ`AM-010`** — ★ **يقول «لا `run deploy` من أي جلسة آلية»**،
⟵ **والصوابُ «على الإنتاج وحده».**
★ **والملف اليوم 245 سطراً** — **والسقف 250** (`AM-005`).

---

## 11. ⛔⛔★★★ نقطةُ التوقف — **بطلبِ المالك صراحةً**

★ **أُوقِف التشغيل عند سطرٍ محدَّد:** ⟵ **بعد تشخيص `DEBT-77` وتحديدِ
السطر المسؤول** (`audited_transaction.dart:413`…`423`) **وقبل كتابة حرفٍ
واحدٍ من الإصلاح.**

⛔⛔ **ولم يُعدَّل ملفُ `.dart` واحدٌ في هذا التشغيل** — ★ **التغييراتُ
كلُّها مستنداتٌ + أداتان جديدتان بلغة Python في `tools/staging/`.**
⚠️ **وينبغي التمييز:** ★ **`git status` يُظهر 23 ملفَ `.dart` معدَّلاً** —
⟵ **وكلُّها مخرَجُ `run4`** (بناءُ `WU-014`) ⛔ **لا هذا التشغيل**:
★ **فالمستودعُ لم يُلتزَم به بعدُ منذ بداية اليوم.**

★★ **والخطوةُ التالية موصوفةٌ بالكامل في `resumption-prompt.md`.**

---

## 12. الخطوة التالية الموصى بها

```text
① إصلاح DEBT-77 في audited_transaction.dart (readTime من batchGet كذلك)
   + اختبار ارتداد على OutflowHandler بلا استعلامات
② dart analyze && dart test في الحزمتين · flutter analyze && flutter test
③ نشرٌ ذاتيٌّ على التجريبية (callables:wu014b) — ✅ مسموح بـAM-010
④ إنشاءُ سندِ مبالغَ خالصٍ من الشاشة ⟹ قياسُه بـverify_outflow_write.py
⑤ قراءةُ item_daily_balances بعد الإلغاء (المتوقَّع 192)
⑥ تحويلُ فحوص WU-013 إلى نقاط ⟹ ثم رفعُ WU-013 وWU-014 [منجز ✅]
⑦ تحديثُ CLAUDE.md (نقطةُ تحوّل: اكتمالُ وحدتين) — الخمسُ قيمٍ في §10
⑧ توقيعُ CR-007 وCR-008 وCR-009 ⟵ ثم WU-015
```

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
