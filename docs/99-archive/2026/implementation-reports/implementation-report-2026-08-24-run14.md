# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-24

**رقم تشغيل التنفيذ:** 26
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة** — شريحة رأسية كاملة)
**تقدّم التنفيذ:** 7.4٪ (2 منجَزة من 27)
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** **1** (`IQ-018` — جديد في هذا التشغيل)

---

## 1. ملخص تنفيذي

**تشغيل استئناف** وجد **ثلاثة بنود `IQ` مُجابة** فطبّق إجاباتها في الكود
والقواعد والتوثيق: ✅ **`IQ-016`** (نصّا رفض الدخول — بنصٍّ اعتمده صاحب
المشروع) · ✅★★ **`IQ-017`** (**تعطيل الحساب صار مُنفَّذاً في `perm()` نفسها**
بترتيب النشر المُلزِم الذي اشترطته الإجابة) · ✅★★ **`IQ-015`** (**قراءة بطاقة
غيرك بـ`userView`** + **خمس عمليات سحابية لإدارة المستخدمين والأدوار** +
شاشة القائمة ونموذجها).

★★★ **وسُدِّد معها `DEBT-22` كاملاً و`DEBT-13` و`DEBT-21` ①** — والأخير هو
بندُ `CR-002` الذي كان ينتظر **أول دالة كاتبة**، ⟵ **وهذه أولُها.**

⛔ **و`WU-001` رُفع حجبها كاملاً ولم تُعلَن منجَزة** — أربعة بنود باقية
(§3.2)، **وبندٌ جديد صُعِّد بدل تخمينه** (`IQ-018` — حذف الدور).

**الأرقام:** **537 اختباراً ناجحاً · ٠ فاشلاً** (كانت 435) — **+102 اختباراً**،
★ **وأربع طفرات أسقطت اختبارات فعلاً ثم استُعيدت.**

---

## 2. بوابة الدخول — نتيجة الفحص

### 2.1 البوابة العامة (٨ شروط) — ✅ **8/8**

| # | الشرط | ✅ | المسار المفحوص |
|:-:|---|:-:|---|
| 1 | المنهجية مُصرَّح بها | ✅ | `docs/21-project-management/development-methodology.md` |
| 2 | العمارة والمكدّس | ✅ | `03-architecture/architecture-overview.md` · `technology-stack.md` |
| 3 | أسلوب الكود والتسمية ومعالجة الأخطاء | ✅ | `04-design/coding-standards.md` · `naming-conventions.md` · `error-handling-strategy.md` |
| 4 | تعريف «منجَز» والتفرّع والـCommit والإصدار | ✅ | `07-development/*` · `12-deployment-and-release/versioning-scheme.md` |
| 5 | استراتيجية الاختبار | ✅ | `08-testing-and-qa/test-strategy.md` |
| 6 | سياسة الأسرار | ✅ | `09-security/secrets-management-policy.md` |
| 7 | `srs.md` بمتطلبات مرقّمة | ✅ | `02-requirements/srs.md` |
| 8 | ★ **لا ADR بحالة «مقترح» يحكم ما يُنفَّذ** | ✅ | **17 ADR كلها «مقبول»** — فُحصت واحداً واحداً |

### 2.2 بوابة `WU-001` (٥ شروط) — ✅ **5/5**

| # | الشرط | ✅ | المسار |
|:-:|---|:-:|---|
| 1 | تصميم الوحدة («عقد التنفيذ») | ✅ | `04-design/module-design/identity-access-design.md` |
| 2 | المتطلبات بمعايير قبول | ✅ | `02-requirements/functional-requirements/FR-M1.md` |
| 3 | عقد الواجهات | ✅ | `05-api/api-overview.md` · `authentication-and-authorization.md` |
| 4 | عقد البيانات | ✅ | `06-database/erd.md` · `data-dictionary.md` |
| 5 | سياسة المصادقة | ✅ | `09-security/authentication-policy.md` |

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

⛔ **لا وحدة جديدة اكتملت في هذا التشغيل.** `WU-000` و`WU-026` كانتا
منجَزتين قبله، **ولم تُمَسّا**.

★ **والسبب مذكور صراحةً لا مطويّ:** `WU-001` تقدّمت تقدّماً كبيراً
**ورُفع حجبها كاملاً**، ⛔ **لكن أربعة بنود من معيار قبولها لم تُنفَّذ بعد**
(§3.2) — **وإعلانُها منجَزة كان سيكون تعليماً زوراً.**

### 3.2 وحدات قيد التنفيذ

| WU | العنوان | ما أُنجز في هذا التشغيل | ما تبقّى |
|---|---|---|---|
| `WU-001` | زيادة: الدخول والصلاحيات ونطاق المصادر | ✅ **رُفع الحجب كاملاً** · **`perm()` تشترط `isActive`** · **قراءة الدليل بـ`userView`** · **خمس عمليات سحابية للإدارة** · **عميل الاستدعاء** · **شاشة المستخدمين ونموذجها** · **سبب التعديل الإلزامي** | ① **حذف الدور** (`IQ-018`) · ② **شاشة تخصيص الصلاحيات** (`FR-M1-05` · `FR-M1-16`) · ③ **شاشات الأدوار** · ④ ⛔★★ **التشغيل على جهاز حقيقي** |

**فحوصات التحقق المُشغَّلة فعلياً (كلها بمخرَجاتها):**

| الفحص | النتيجة | العدد |
|---|:-:|---|
| `cd packages/qtms_domain && dart analyze` | ✅ | `No issues found!` |
| `cd packages/qtms_domain && dart test` | ✅ | **134** (كانت 107) |
| `cd functions && dart analyze` | ✅ | `No issues found!` |
| `cd functions && dart test` | ✅ | **137** (كانت 106) |
| `flutter analyze` | ✅ | `No issues found!` بإعدادات صارمة |
| `flutter test` | ✅ | **67** (كانت 38) |
| `npm test` (بوابتا الحماية) | ✅ | **199 · ٠ فاشلاً** · خرج بالرمز 0 (كانت 188) |
| `npm run check:coverage` | ✅ | ★★ **و`userView` صار مفحوصاً لا مستثنى** |
| `dart run build_runner build` | ✅ | `bin/server.dart` مولَّد بالعمليات الخمس |

**★★ أربع طفرات — كلٌّ أسقطت اختباراً فعلاً ثم استُعيدت:**

| # | الطفرة | الأثر المرصود |
|:-:|---|---|
| ① | `activeCard()` ⟵ `return true` | ⛔ **سقط اختباران** |
| ② | حذف `\|\| perm('userView')` من قاعدة `users` | ⛔ **فشلت بوابة التغطية** |
| ③ | حذف حارس `amendReasonMissing` | ⛔ **سقطت ثلاثة اختبارات** |
| ④ | `invalidCredentials` ⟵ رسالة أخرى | ⛔ **سقط اختبار `IQ-016`** |

⚠️★★ **وعطلٌ حقيقي رصده الاختبار لا القراءة:** شاشة المستخدمين كانت **تدور
بمؤشّر تحميل إلى الأبد** عند رفض القاعدة — لأن التدفّق المُخفِق يبقى
`AsyncLoading` **وهو يحمل الخطأ**، ⟵ فـ`when` تستدعي `loading`.
✅ **صُحِّح بفحص `hasError` أولاً.** ⛔ **ولولا الاختبار لَوصل الإنتاج بشهادة
«كل الاختبارات ناجحة»** — وهو نفس صنف العطل الذي يحذّر منه
`implementation-playbook.md` §7 البند 2.

### 3.3 وحدات محجوبة (ولماذا)

✅ **لا وحدة محجوبة.** `WU-001` كانت محجوبة بثلاثة بنود **ورُفع حجبها كاملاً**.

⚠️ **و`IQ-018` يحجب عمليةً واحدة داخل `WU-001` لا الوحدة كلها** — فبقية
عملياتها مبنيّة، ⟵ **والوحدة `[قيد التنفيذ]` لا `[محجوب]`.**

---

## 4. مخالفات النزاهة المرصودة آلياً

✅ **لا مخالفات** — `track_implementation.py` أرجع
`integrity_violations: []` و`has_integrity_violations: false`،
و`unparsed_lines: []`.

★ **وكل وحدة معلَّمة منجَزة تقابلها فحوصات ناجحة فعلياً**، ⛔ **ولم تُعلَّم
`WU-001` منجَزة رغم تقدّمها الكبير.**

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

### 5.1 ⛔ بندٌ **جديد** مفتوح

| IQ | السؤال | الوحدة المتعطّلة | ملف/دالة | لماذا لم يكن قابلاً للاستنتاج |
|---|---|---|---|---|
| ⛔★★ **`IQ-018`** | **`roleDelete` مفتاحٌ بلا مسارٍ ممكن** | `WU-001` — **عملية حذف الدور وحدها** | `functions/lib/src/user_admin.dart` → `UserAdminOperation` | ★★ **ثلاثة مستندات معتمدة تتعارض ولا مرجّح بينها:** الكتالوج §2 يُثبت `roleDelete` («حذف دور») · `security-requirements.md` §2 البند 1 يرفض **«الحذف في كل السجلات»** · `data-dictionary.md` §`roles` **لا يذكر أي حقل حالة** فلا مسار تعطيلٍ بلا **اختراع اسم حقل**. ⟵ **والتنفيذان مختلفان أثراً اختلافاً جوهرياً، وأحدهما غير قابل للتراجع.** |

⚠️ **والوضع الحالي مكشوف لا مطموس:** `UserAdminOperation` **لا تحوي
`deleteRole`** ⛔ **فالعملية غير موجودة أصلاً**، ★ **ويحرس ذلك اختبارٌ صريح**
كي لا يُرفَع الحجب سهواً.

### 5.2 ✅ بنود أُغلقت في هذا التشغيل

| IQ | ما طُبِّق | الدليل |
|---|---|---|
| ✅★★ **`IQ-017`** | **الخيار ج بترتيب الخيار أ حرفياً:** ① `isActive` تُكتب في كل مسار كتابة بطاقة **بقيمة مشتقّة من حالة الحساب** ⛔ لا `true` ثابتة ② **بوابة «تمّ سابقاً» في الإقلاع صارت تنظر إليه** فصار الإقلاع أداةَ ترحيل ③ **ثم** شرط `perm()` ④ **والتعطيل في خدمة المصادقة أيضاً** | **طفرة ①** · 4 اختبارات قواعد جديدة · رَنبوك ترحيل جديد |
| ✅★★ **`IQ-015`** | **الخيار أ:** شرط قراءة في القاعدة **مع** بقاء قراءة النفس بلا صلاحية · **خمس عمليات مستدعاة** ⛔ بلا فتح الكتابة المباشرة · **كشف خريطة الصلاحيات لصاحب `userView` مقبولٌ بقرار** ⛔ ولا `user_directory` مشتقّ | **طفرة ②** · 5 اختبارات قواعد · 31 اختبار سحابة · 27 اختبار نطاق · 16 اختبار واجهة |
| ✅ **`IQ-016`** | **الخيار أ:** `error-codes-catalog.md` **مرجع نصّ العرض**، و§4 مرجعٌ تاريخي ⛔ لا يُعدَّل · **`ERR_AUTH_008` و`ERR_AUTH_009` بنصٍّ اعتمده صاحب المشروع** | **طفرة ④** · اختبار الصمت المتعمَّد **قُلِب إلى اختبار ظهور** |

★★ **وشرطُ `IQ-016` احتُرم حرفياً:** الإجابة نصّت «⛔ **لا تُخترع صياغة
الرسالتين**… أقدّم نصّ الرسالتين لاعتمادهما، **أو أستخرج نصهما من مصدر مشروع
موثّق إن وُجد**». ✅ **فبُحث أولاً في كل `docs/` فلم يوجد نصّ**، ⟵ **ثم
عُرض النصّان للاعتماد قبل أي تعديل على الكتالوج.** ★ **وتكرّر الأمر مع
`ERR_SETUP_011` و`ERR_CALL_500`** — ⛔ **ولا رسالةَ واحدة صيغت اجتهاداً.**

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

| # | التعارض | ماذا يقول المستند | ماذا فرض التنفيذ | كيف عُولج |
|:-:|---|---|---|---|
| ① | ★★ **حذف الدور** | الكتالوج يُثبت `roleDelete` · `security-requirements.md` يرفض الحذف مطلقاً · القاموس بلا حقل حالة | **لا يمكن تنفيذ الاثنين معاً** | ⛔ **لم يُنفَّذ ولم يُخمَّن** — **`IQ-018`** بثلاثة خيارات، ★ **واثنان منها يمسّان مستنداً حاكماً** فقرارهما بشري |
| ② | ★ **`revokeAccessOnDisable`** | `api-overview.md` §3.2 يجعلها **عمليةً مشغَّلة بالكتابة** | التشغيل بالحدث **غير متزامن** ⟵ **تبقى نافذةٌ يعمل فيها رمزٌ صالح لحسابٍ عُطِّل** | ✅ **نُفِّذ أثرُه داخل `disableUser` مباشرةً** (بعد المعاملة) — ★★ **وأُعلن الانحراف صراحةً في `api-overview.md`** ⛔ **ولم تُلغَ العملية من §3.2**: إن بُنيت فهي **حزامٌ ثانٍ** لا بديل |
| ③ | ★ **مرجع نصّ العرض** | الكتالوج كان يقول إن نصّه «من §4 حرفياً» **وهو يحوي نصوصاً ليست فيه** | لا مرجع واحد قاطع | ✅ **حُسم بـ`IQ-016`** — الكتالوج (`LIVING`) هو المرجع، و§4 (`STATIC`) تاريخي ⛔ **ولم يُعدَّل مستند `STATIC`** |
| ④ | ★ **آلية بيان اعتماد المستخدم الجديد** | `authentication-policy.md` §5: ⛔ لا كلمة افتراضية مشتركة · «الإدارة تُعيد التعيين **لا تقرأ**» · آلية استرجاع آمنة | إنشاء الحساب يحتاج مساراً لكلمة المرور | ✅ **رابط استرجاع يضبطه صاحبُه** — ★ **وهو المسار الوحيد المتوافق مع القيود الثلاثة معاً**، ⛔ **فلم يُصعَّد بنداً**: الاستنتاج قاطع لا تخمين. **ووُثِّق التعليل في ترويسة `user_admin.dart`** |
| ⑤ | ★★ **سبب التعديل في عمليات الإدارة** | `FR-M1` **لا يذكره** · `ADR-0004` و`DEBT-21` ① **يفرضانه على كل تعديل** | `AuditEntry` **يرفض `amend` بلا سبب** — رصدَه التصريفُ فعلاً | ✅ **فُرض `amendReason` على `updateUser` و`updateRole`** — ★ **وهو تنفيذُ `CR-002` لا اجتهاد**، ومُثبَتٌ بالطفرة |

⛔ **ولا انحراف صامت واحد** — كل بند أعلاه إما مُصعَّد أو مُعلَن في مستنده.

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

| # | البند | الحالة |
|:-:|---|---|
| ① | ⛔ **`IQ-018`** — خياران من ثلاثة يمسّان **مستنداً حاكماً** (`data-dictionary.md` أو `security-requirements.md`) | ⏳ **ينتظر قرارك** — ⛔ **ولم يُعدَّل أيٌّ منهما ذاتياً** (`UPDS-05` §4) |
| ② | ⛔⛔ **نشر `firestore.rules` و`functions/`** | ⏳ **لم يُنفَّذ** — **النشر يحتاج طلباً صريحاً** ⛔ **ولم يُطلَب في هذا التشغيل** |
| ③ | ⛔ **تنفيذ رَنبوك الترحيل** `RB-backfill-user-isactive.md` | ⏳ **إجراء تشغيلي على بيئة حيّة** — قرارُه لك |

✅ **ولم يُنشأ ADR جديد** — ⛔ ولا عُدِّل أيٌّ من السبعة عشر.
✅ **ولم يُعدَّل `srs.md` ولا أي مستند `STATIC`.**

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

| WU | لماذا حسّاسة | ★ ما اتّسع في هذا التشغيل |
|---|---|---|
| ⚠️★★ **`WU-001`** | **مصادقة · صلاحيات · نطاق مصادر · بيانات شخصية** | ★★★ **واتّسع سطحها كثيراً:** صارت **تُنشئ حسابات في خدمة المصادقة وتعطّلها**، **وتفتح قراءة بطاقات المستخدمين لصاحب `userView` بما فيها خرائط صلاحياتهم**، ★ **وتغيّر `perm()` نفسها — الدالة التي يمرّ بها كل تفويض في النظام (66 موضعاً).** |
| ⚠️ **`WU-000`** | بوابة التأسيس والقواعد | (كما كانت) |

★★★ **والتحقق الآلي هنا شرط ضروري لا كافٍ.** ⛔ **وثلاثة بنود تحديداً
تحتاج عيناً بشرية قبل أي نشر إنتاجي:**

① ★★ **ترتيب نشر `IQ-017`** — ⛔ **نشرُ `perm()` قبل الترحيل يقفل المالك
خارج نظامه**، **ولا مسار برمجي لإصلاحه بعدها** (كل مسارات المنح تشترط
صلاحيةً لا تسري له).
② ★★ **توسيع سطح القراءة بـ`userView`** — **من يملكه يرى صلاحيات الجميع**.
✅ **قُبل بقرارك في `IQ-015`**، ★ **ويُراجَع من يُمنَح إياه فعلاً.**
③ ★ **مسار إنشاء الحساب** — ⛔ **لم يُشغَّل على خدمة مصادقة حقيقية بعد**،
والاختبارات تغطي **منطقنا** لا **وعد المنصة**
(`authentication-policy.md` §7 البندان 1 و2).

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| `05-api/api-overview.md` | ★ **§3.1-ج جديد: خمس عمليات مستدعاة** + **إعلان انحراف `revokeAccessOnDisable`** |
| `05-api/error-codes-catalog.md` | ★★ **صار مرجع نصّ العرض** + `ERR_AUTH_008` · `ERR_AUTH_009` · `ERR_SETUP_011` · `ERR_CALL_500` |
| `06-database/data-dictionary.md` | ★★ **`users.isActive` صار مقروءاً من `perm()`** — وأثرُ غيابه معلَن |
| `09-security/permissions-catalog.md` | ★★ **`userView` خرج من استثناء §2.10** — صار له شرط قراءة حقيقي |
| `04-design/module-design/identity-access-design.md` | **حالة الوحدة** + **عقود المستودعات الثلاثة** + **إبطال نصّ ثغرة `IQ-017`** |
| `02-requirements/requirements-traceability-matrix.md` | **عمود «الكود» لـ`FR-M1`** — وما يزال ناقصاً منه صراحةً |
| `03-architecture/risks-and-technical-debt-register.md` | ✅ **`DEBT-22` مسدَّد** · ✅ **`DEBT-13` مسدَّد** · ✅ **`DEBT-21` ① مسدَّد** |
| `13-operations/runbooks/RB-backfill-user-isactive.md` | ★★ **جديد** — إجراء الترحيل الحاجب للنشر |
| `22-onboarding/developer-onboarding.md` | **خمسة بنود يجب معرفتها قبل لمس التفويض** |
| `00-governance/upds-workflow/implementation-plan.md` | **`WU-001` ⟵ `[قيد التنفيذ]`** + فحوصها + **«ما تبقّى»** |
| `00-governance/upds-workflow/implementation-questions.md` | **`IQ-015` · `IQ-016` · `IQ-017` ⟵ `[تم الحل ✅]`** + **`IQ-018` جديد** |

---

## 10. حالة `CLAUDE.md`

✅ **حُدِّث** — **ونقاط التحوّل فعلية لا شكلية** (`claude-md-management.md` §2):
**تقدّم جوهري في التنفيذ** (البند 5) **وقرارات إنفاذ سرَت في الكود**.

| الحقل | القيمة |
|---|---|
| `action` | `managed-block-updated` |
| `human_content_lines_preserved` | **0** (لا محتوى بشري خارج الكتلة) |
| `new_total_lines` | **171** (كان 200) |
| `exceeds_recommended_size` | **`false`** |

★ **ولم يُحرَّر الملف يدوياً** — عبر `sync_claude_md.py` حصراً.
★★ **وأُضيفت إليه قاعدتا حَوْكمة جديدتان:** **سبب التعديل الإلزامي في كل
دالة كاتبة**، و**حظر نشر القواعد قبل الترحيل.**

---

## 11. الخطوة التالية الموصى بها

### ⛔ أولاً — قرارٌ ينتظرك

**أجب `IQ-018`** في `implementation-questions.md` (ثلاثة خيارات مشروحة
بمقايضاتها)، ثم أعد `/upds-docgen:implement`.
⚠️ **وخياران منه يمسّان مستنداً حاكماً** — ⛔ **فلا يُطبَّق أيٌّ منهما بلا قرارك.**

### ⛔⛔ ثانياً — قبل أي نشر

```text
① نفّذ RB-backfill-user-isactive.md على التجريبية
② تحقق: لا بطاقة فيها permissions بلا isActive
③ ثم — وثمّ فقط — انشر firestore.rules
④ ثم كرّر الترتيب نفسه على الإنتاج
```

⛔ **وعكسُ ① و③ يقفل المالك خارج نظامه.**

### ★ ثالثاً — لإغلاق `WU-001`

**شاشة تخصيص الصلاحيات** (`FR-M1-05` · `FR-M1-16`) · **شاشات الأدوار** ·
**وحذف الدور بعد حسم `IQ-018`** · ⛔★★ **وتشغيلٌ على جهاز أندرويد حقيقي** —
★ **وهذا الأخير لم يجرِ في أي تشغيل حتى الآن ولا يُدَّعى.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
