# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-30 (التشغيل 10)

**رقم تشغيل التنفيذ:** 48 (تشغيل التنفيذ العاشر في هذا اليوم)
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة** — شريحة رأسية كاملة)
**تقدّم التنفيذ:** ★ **40.7٪ (11 منجَزة من 27)** — ⛔ **ولا وحدةَ `[قيد التنفيذ]` ولا `[محجوب]`**
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** 0 — ⛔ **ولم يُفتَح بندٌ جديد في هذا التشغيل**

---

## 1. ملخص تنفيذي

✅ **أُنجزت `WU-009` — مركز الإدخالات المعلّقة — من طرف إلى طرف** عبر
الطبقات الثلاث: باني البنود دالّاتٍ خالصةً في طبقة النطاق، ومُنتِجُها في
السحابة مربوطاً في **أربعة معالِجات كاتبة داخل معاملة مستندها نفسِها**،
وشاشةُ المركز وعدّادُها الحيّ في التطبيق. ★ **والبوابة الآلية كلُّها خضراء:**
**1598 اختباراً · ٠ فاشلاً** (**+86 عن 1512**) · **28 بوابةَ تصميم** ·
**`analyze` نظيفٌ في الحزم الثلاث** · ⛔⛔ **و`firestore.rules` لم تُمَسّ بحرف.**

⛅★★★ **ونُشرت خدمة العمليات على التجريبية بيد المالك** (`callables:wu009` ·
**المراجعة `qtms-callables-00020-7hk`** · 100٪ من المرور · `allUsers` قائمٌ على
`roles/run.invoker`) — ★ **والأداةُ سلّمت الأوامر الثلاثة ولم تُنفِّذ منها
شيئاً** (بروتوكول التشغيل §ز)، ⟵ **ثم اكتفت بالقراءة بعده.**

✅★★★ **ثم نجح اختبار المحاكي §د.2 بأربعةَ عشرَ فحصاً** — ★ **ودورةُ حياة
البند قِيست كاملةً:** **يظهر عند ترك القيمة** (`E-06` · `E-10` · `FR-M9-10`
عند التوريد · `AT-23`) **ويختفي عند إدخالها فعلاً** (`AT-16` · `AT-24` ·
`BR-M7-12`)، ★ **والعدّاد الحيّ تتبّعها لحظةً بلحظة:** **لا شيء ⟵ 4 ⟵ 3 ⟵
2 ⟵ 3 ⟵ 4 ⟵ 3 ⟵ 2.**

⛔⛔★★★ **وكشف المحاكي عطلاً لم يكشفه 1594 اختباراً آلياً** (`DEBT-68`:
**شاشةُ التوزيع تسقط كاملةً عند فتح بندِ توزيعةٍ من المركز**) — ✅ **سُدِّد
وله أربعةُ حرّاسِ ارتدادٍ مُثبَتون بطفرة.**

★★ **وصُحِّح خللُ سجلٍّ خطير قبل البدء:** **جسمُ بند `WU-009` في خطة التنفيذ
كان نسخةً حرفيةً من `WU-006`** — بملفاتِه وفحوصِه وجدولِ محاكيه
و`**نتيجة التحقق:** نجح` — ⛔ **على وحدةٍ لم يُكتب لها سطرٌ واحد** (§4.1).

---

## 2. بوابة الدخول — نتيجة الفحص

### 2.1 البوابة العامة (٨ شروط — `implementation-playbook.md` §1.1)

| # | الشرط | متحقق؟ | المسار المفحوص |
|:-:|---|:-:|---|
| 1 | المنهجية مُصرَّح بها | ✅ | `docs/21-project-management/development-methodology.md` |
| 2 | العمارة والمكدّس | ✅ | `docs/03-architecture/architecture-overview.md` · `technology-stack.md` |
| 3 | معايير الكتابة ومعالجة الأخطاء | ✅ | `docs/04-design/coding-standards.md` · `naming-conventions.md` · `error-handling-strategy.md` |
| 4 | تعريف «منجَز» والتفرّع والالتزام والإصدار | ✅ | `docs/07-development/*` · `docs/12-deployment-and-release/versioning-scheme.md` |
| 5 | استراتيجية الاختبار | ✅ | `docs/08-testing-and-qa/test-strategy.md` |
| 6 | سياسة الأسرار | ✅ | `docs/09-security/secrets-management-policy.md` |
| 7 | `srs.md` بمتطلبات مرقّمة | ✅ | `docs/02-requirements/srs.md` |
| 8 | ⛔ **لا `ADR` بحالة «مقترح» يحكم ما يُنفَّذ** | ✅ | **الـ22 كلُّها «مقبول»** — مفحوصةٌ ملفاً ملفاً |

### 2.2 بوابة الوحدة `WU-009` (٥ شروط — §1.2)

| # | الشرط | متحقق؟ | المسار |
|:-:|---|:-:|---|
| 1 | `module-design` للوحدة | ✅ | `docs/04-design/module-design/pending-entries-design.md` |
| 2 | متطلبات بمعايير قبول | ✅ | `docs/02-requirements/functional-requirements/FR-SYS.md` §1 |
| 3 | عقود الواجهات (لا تعرض API خارجياً) | ✅ | `docs/05-api/api-overview.md` — ⛔ **ولا عمليةَ مستدعاة جديدة** |
| 4 | تمسّ التخزين ⟵ `erd` و`data-dictionary` | ✅ | `docs/06-database/erd.md` §5 · `data-dictionary.md` §4 |
| 5 | تمسّ الصلاحيات؟ | ✅ | ⛔ **لا** — **لا مفتاح للمركز في `permissions-catalog.md` §2**، ★ **والنطاق وحده يحكم** |

★★ **وزيادةً على ذلك — بروتوكول التشغيل §ح** (الوحدة تعرض واجهة): قُرئت
`docs/18-ux-ui/design-system.md` و`design-tokens.md` و`ui-guidelines.md`
**بالكامل قبل كتابة أي سطر عرض**، ★ **وأُدرجت الثلاثة في
`**المستندات الحاكمة:**` للوحدة** ⟵ **فيلتقطها كشفُ الانحراف تلقائياً.**

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

| WU | العنوان | الحالة |
|---|---|---|
| **`WU-009`** | **زيادة: مركز الإدخالات المعلّقة من طرف إلى طرف** | ✅ **`[منجز ✅]`** — **الطبقات الثلاث + الاختبارات + التوثيق + النشر + §د.2 كاملة** |

> ★★ **ولم تُرفَع إلا بعد أن قِيست دورةُ حياة البند حيّاً** —
> `implementation-playbook.md` §7 الخطوة 2 وبروتوكول التشغيل §د.2:
> ★ **أخصُّ سلوكِ هذه الزيادة (ظهورُ بندٍ واختفاؤه) لا يقع إلا بكاتبٍ
> سحابيٍّ حيّ**، ⛔ **ورفعُها بتغطيةٍ آلية وحدها كان سيخالف قاعدة «لا
> تُعلَن منجَزة بتغطية نصية»** — ★ **وقد أثبت `DEBT-68` أن الخطر حقيقي.**

### 3.2 تفصيل `WU-009`

#### 3.2.1 الملفات المنتَجة والمعدَّلة

| الطبقة | جديد | معدَّل |
|---|---|---|
| **النطاق** | `capabilities/oversight/domain/pending_entry.dart` · `pending_entry_builder.dart` | `inventory/domain/sack_intake.dart` (**استخراج `sackWeightStateOf`**) · `qtms_domain.dart` |
| **السحابة** | `functions/lib/src/pending_entries.dart` | `sack_intake_handler.dart` · `daily_pricing_handler.dart` · `inventory_handler.dart` · `distribution_handler.dart` · `sack_intake.dart` (**إتاحة `sackFinancePathOf`**) |
| **التطبيق** | `oversight/application/pending_entries_providers.dart` · `oversight/infrastructure/firestore_pending_entries_directory.dart` · `oversight/presentation/pending_entries_screen.dart` · `core/ui/needs_action_row.dart` | `app/router.dart` · `identity_access/presentation/home_shell.dart` · `main.dart` · `sales_receivables/presentation/distribution_screen.dart` |
| **الاختبارات** | `pending_entry_test.dart` (24) · `pending_entry_builder_test.dart` (20) · `functions/test/pending_entries_test.dart` (24) · `pending_entries_screen_test.dart` (14) · `test/support/fake_pending_entries.dart` | ★ `distribution_screen_test.dart` (**+4 حرّاسِ `DEBT-68`**) |
| **القواعد والفهارس** | ⛔⛔ **لا شيء** | ⛔⛔ **`firestore.rules` و`firestore.indexes.json` لم تُمَسّا بحرف** |

#### 3.2.2 الفحوص التي شُغِّلت فعلاً — بنتائجها المقاسة

| الفحص | النتيجة | العدد |
|---|:-:|---|
| `dart analyze` (النطاق) | ✅ | بلا ملاحظة |
| `dart test` (النطاق) | ✅ | **456** · ٠ فاشلاً (**+44**) |
| `dart analyze` (السحابة) | ✅ | بلا ملاحظة |
| `dart test` (السحابة) | ✅ | **485** · ٠ فاشلاً (**+24**) |
| `flutter analyze` | ✅ | ⚠️ **`info` واحدة خارج `lib/`** — راجع §6.3 |
| `flutter test` | ✅ | **421** · ٠ فاشلاً (**+18**) |
| `flutter test test/core/design/design_gates_test.dart` | ✅ | **28 بوابة** · ٠ فاشلاً |
| `npm test` (قواعد الحماية) | ✅ | **236** · ٠ فاشلاً |
| `flutter build apk --debug --flavor staging` | ✅ | `app-staging-debug.apk` |
| ⛅ **نشرُ الخدمة على التجريبية** | ✅ | ★ **بيد المالك** — `callables:wu009` · `qtms-callables-00020-7hk` · 100٪ |
| ✅ **اختبار المحاكي §د.2** | ✅ | ★ **14 فحصاً · ٠ فاشلاً** |
| **الإجمالي** | ✅ | ★★ **1598 اختباراً · ٠ فاشلاً** |

#### 3.2.3 ✅ اختبار المحاكي `Pixel_6_API_36` — أربعةَ عشرَ فحصاً · الكلُّ نجح

★ **جرى بحساب الاختبار على التجريبية** (`AM-004`) — ⛔ **ولم تمرّ قيمةُ
اعتمادٍ في أي مخرَج ولا سجلّ** (`environments.md` §1.5).

| # | الفحص | النتيجة |
|:-:|---|---|
| 1 | الإقلاع والدخول | ✅ **نجح** |
| 2 | ★★ **صفُّ «الإدخالات المعلّقة» في الصدَفة** (`FR-SYS-03` الموضع الرابع) | ✅ **ظاهرٌ بعدّاده «لا شيء» بالحالة الهادئة** ⛔ **بلا إخفاء** |
| 3 | النقر ينقل إلى المركز | ✅ **نجح** |
| 4 | شريط الفلاتر — مصدران + أربعُ شرائحِ نوع | ✅ **نجح** — ⛔ **ولا بطاقةَ «كل المصادر»** |
| 5 | ⛔⛔★★★ **الاستعلام لا يُرفَض من القاعدة** | ✅ **هيكلٌ عظمي ⟵ حالةٌ فارغة مصمَّمة** — ⛔ **لا `PERMISSION_DENIED` ولا `failed-precondition`** ⟵ **فشرطُ `storedInScope()` يقبل السردَ المقيَّد بالمصدر، والفهرس `sourceId ↑ · date ↓` منشورٌ فعلاً على التجريبية** |
| 6 | تبديل المصدر (الأول ⟵ الثاني) | ✅ **نجح** — **واستعلامُ الثاني قُبل كذلك** |
| 7 | شاشة التوزيع بعد تعديل `initState` | ✅ **تُفتَح كما كانت** ⛔ **بلا مقوتٍ مُخمَّن ولا انهيار** |
| 8 | ⛔⛔★★★ **`E-06`** — جونيةٌ (100 كجم · سكرب 10) بلا أنواع ولا ضريبة | ✅ **العدّاد «لا شيء» ⟵ «4»**: ثلاثةُ بنودِ `M7` + **بندُ تسعير السكرب** ⛔ **بلا رقم مستند** |
| 9 | ★★★ **`FR-SYS-04` لبند `M9`** | ✅ **فتح التسعير بالمصدر نفسِه ومرشِّح «لم يُسعَّر» نشطاً** ⟵ **والصفُّ الوحيد هو الحقلُ الناقص** |
| 10 | ★★ **تسعيرٌ ناقص** (400 بلا حدٍّ أدنى) | ✅ **البند باقٍ ونصُّه تبدّل إلى «الحد الأدنى»** — `FR-M9-10` «أو» |
| 11 | ★★★ **`AT-16`** — إكمال التسعير (380) | ✅ **اختفى: 4 ⟵ 3** |
| 12 | ★★★ **`BR-M7-12`** — تأكيدُ الوزن الضائع | ✅ **بندُه وحده اختفى: 3 ⟵ 2** |
| 13 | ★★★ **`FR-M9-10` عند التوريد** (100 حبة بلا سعر) | ✅ **2 ⟵ 3** — ★ **البند يُنشَأ لحظةَ دخول المخزون** |
| 14 | ★★★ **`AT-23` ثم `AT-24`** | ✅ **3 ⟵ 4 ببندٍ واحد للتوزيعة** · ✅ **تسعير «سلة» 4 ⟵ 3** · ✅ **تعديل التوزيعة 3 ⟵ 2** |

> ⚠️★ **وحدٌّ معلَنٌ واحد:** ⛔ **بندُ «ضريبة الكيلو» لم يُختبَر اختفاؤه** —
> ★ **لا واجهةَ لـ`enterSackTax` بعد** (أثرٌ معلَنٌ سلفاً في `WU-004`:
> «**يُبنى مع `M14`**») ⟵ **فالبندُ يظهر صحيحاً ويبقى**، ⛔ **ولا مسارَ في
> التطبيق لإدخال قيمته اليوم.**

### 3.3 وحدات محجوبة (ولماذا)

⛔ **لا وحدة محجوبة** — ★ **ولا بندَ `IQ` فُتح في هذا التشغيل.**
⚠️ **وكانت `WU-009` `[قيد التنفيذ]` بين مرحلتَي هذا التشغيل** (بانتظار أمر
بيئةٍ من المالك — §ج البند 5) ⛔ **لا `[محجوب]`**: ★ **فالحجب قرارٌ تنفيذي
معلَّق، وهذا كان توقّفاً بيئياً** ⟵ **وقد زال بالنشر.**

---

## 4. مخالفات النزاهة المرصودة آلياً

✅ **لا مخالفات** — `track_implementation.py` يُرجِع `integrity_violations: []`
و`has_integrity_violations: false` و`unparsed_lines: []`.

### 4.1 ⛔⛔★★★ لكنّ الحارس الآلي أخفى خللاً حقيقياً — درسٌ مُلزِم أول

★ **جسمُ بند `WU-009` في `implementation-plan.md` كان نسخةً حرفيةً من
`WU-006`**: ملفاتُ التوزيع · تسعةُ فحوصٍ بتواريخ 2026-08-27 ·
`**نتيجة التحقق:** نجح` · جدولُ محاكٍ بثمانية سيناريوهات · `DEBT-40`
و`DEBT-41` · قراران تنفيذيان — ⛔ **كلُّها على وحدةٍ لم يُكتب لها سطرٌ واحد.**

⛔⛔ **ولم يُبلِّغ عنه `track_implementation.py`** — ★ **لأن حارس النزاهة يفحص
الوحدات `[منجز ✅]` وحدها** (**«أي وحدة `[منجز ✅]` بلا فحوص … تظهر في
`integrity_violations`»**)، ★ **وهذه كانت `[مخطط]`.**

⟹ ★★★ **والدرس:** **الحارسُ يمنع «منجَزة زوراً»** ⛔ **ولا يمنع «مخطَّطة
تحمل دليلَ غيرها»** — ⟵ **وسجلٌّ كهذا كان سيُقرأ في أول مراجعة على أنه
إنجازٌ سابق**، ★ **وهو أخطر من غياب السجل: الغيابُ يُبقي النقص مرئياً
والادّعاءُ يُغلقه كذباً** (بروتوكول التشغيل §و حرفياً).

✅ **والعلاج:** استُبدل الجسم بسجلّ الوحدة فعلاً، ★ **وسُجِّل التصحيحُ نصّاً
في رأس البند** ⛔ **بلا مساسٍ ببند `WU-006` نفسِه**
([`implementation-plan.md:2403`](implementation-plan.md)).

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

⛔ **لم يُفتَح بندُ `IQ` جديد في هذا التشغيل** — **الرصيد 30 من 30 محسومة.**

★★ **وأقربُ ما بلغ حدَّ التصعيد وحُسم بالمستندات لا بالتخمين — يُذكَر
للشفافية:** **موضعُ كتابة بنود المركز** (مشغّلٌ بعد الالتزام أم داخل
المعاملة). ★ **ولم يُصعَّد لأن المستندات تحسمه صراحةً:**
`c3-component-diagram.md` §3 يضع «باني المركز المعلّق» في **«مشغَّلة
بالكتابة»**، ★ **والمشروع ينفّذ كلَّ مشتقّاته «المشغَّلة بالكتابة» داخل
المعاملة نفسها** (`dealer_balances` · `item_daily_balances` · سجل التدقيق —
`ADR-0013` القاعدة 1). ⛔ **ومسارُ المشغّل غيرُ متاحٍ أصلاً:** **فكُّ
protobuf لم يُنجَز** (`DEBT-16`) ⟵ **فالمشغّل لا يعرف قيمَ الحقول.**

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

### 6.1 ⛔ مدى اختبارات القبول في الخطة كان خاطئاً

**ماذا كان مكتوباً:** `**المتطلبات المرتبطة:** FR-* مركز الإدخالات المعلّقة ·
AT-58 … AT-60`.
**وماذا تقول المستندات الحاكمة:** `AT-58`…`AT-60` **اختباراتُ التعديل
والإلغاء والحذف** (`UC-007` · `FR-M6-14` · `FR-M10-18`) — ⛔ **لا علاقة لها
بالمركز**؛ ★ **ومداه `AT-16` · `AT-23` · `AT-24` · `AT-41`**
(`pending-entries-design.md` §11 · `FR-SYS-08`) **و`AT-68`** للنطاق
(`FR-SYS-07`).
**كيف عولج:** ✅ **صُحِّح في الخطة وحدها** — ★ **وهي سجلُّ حالةٍ تكتبه الأداة
لا مستندٌ محكومٌ بحَوْكمة** ⟵ **بنفس سابقة `WU-006` حرفياً** (قرار تنفيذي
روتيني · §ب الفئة 3)، ⛔ **بلا مساسٍ بأي `FR` ولا `AT` ولا مصفوفة تتبّع.**

### 6.2 ⚠️ `FR-SYS-08` يعدّ تسعة أسماءِ حقول — والمنفَّذ خمسة

**ماذا يقول المستند:** يعدّ لـ`M7`: «ضريبة الكيلو · الأنواع لم تُدخل ·
**وزن الحبة** · **سعر التوزيع** · **الحد الأدنى** · الوزن الضائع».
**وماذا فرض التنفيذ:** ★ **ثلاثةٌ لـ`M7` لا ستة** — ⟵ **والمبرّر من
المستندات نفسِها لا من اجتهاد:**
① **وزنُ الحبة لا يكون ناقصاً بعد الحفظ إطلاقاً** — `validateSackLine`
يُنتجه في حالاته الثلاث كلِّها (`FR-M7-13` · `FR-M7-14`) ⟵ **فبندٌ لا يظهر
أبداً ضجيجٌ في الكتالوج لا حارس.**
② **وسعرُ التوزيع والحدُّ الأدنى في الجونية هما حقلا `M9` نفسُهما** —
`FR-M7-21` («**من `M9`**») و`pending-entries-design.md` §9 («**نوع سُعِّر من
شاشة الجونية ⟵ يختفي بنده فوراً — المصدران يكتبان نفس الحقل**») ⟵ **وبندان
منفصلان لحقلٍ واحد كان سيبقى أحدهما بعد إدخال الآخر.**
**كيف عولج:** ✅ **وُثِّق في `pending-entries-design.md` §12.2** وفي ترويسة
`PendingMissingField` — ⛔ **بلا تعديلٍ على `FR-SYS.md`** (مستندُ متطلبات).

### 6.3 ⛔⛔★★★ عطلٌ حقيقي كشفه المحاكي وحده — `DEBT-68`

**ما وقع:** ★ **الضغطُ على [ إدخال ] في بندِ توزيعةٍ أظهر شاشةً حمراء**
بـ**«Tried to modify a provider while the widget tree was building»**
⛔ **بدل شاشة التوزيع.**
**الجذر:** `_DistributionBodyState.initState` **كان يستدعي
[`PendingFocusState.take`] مباشرةً** — ★ **وهي *تكتب* حالةً لا تقرؤها**،
⟵ **وRiverpod يمنع الكتابة في دورة حياة الويدجت.**
⚠️⚠️ **والمفارقة أن العلاج كان مكتوباً ثم أُزيل:** ★ **صيغةٌ أولى بمكوّنٍ
مستقل تستهلك الوجهة في `addPostFrameCallback`** ⛔ **استُبدلت بـ`initState`
لتقليل إعادة المحاذاة في ملفٍ قائم** ⟵ **فعاد العطلُ من حيث سُدَّ.**
**كيف عولج:** ✅ **`addPostFrameCallback` + `setState` + حارسُ `mounted`** ·
✅ **أربعةُ حرّاسِ ارتدادٍ مُثبَتون بطفرة** (`+19 -4` عند إعادة المُخالفة) ·
⛅ **وإثباتٌ حيٌّ بعد الإصلاح.**
⟹ ★★★ **وهو الوجهُ الخامس لدرس `DEBT-37`** (`DEBT-40` · `DEBT-41` ·
`DEBT-61` قبلَه): **اختبارُ الطبقة لا يُغني عن اختبار ما يعبر بينها** —
★ **ولم يكشفه أيٌّ من 1594 اختباراً آلياً** لأن اختبار الشاشة لا يصلها
ووجهةٌ مضبوطة.

### 6.4 ✅ وأثرٌ جانبيٌّ مقيس — يُغلق شطراً من `DEBT-65`

★★ **`AT-17` · `FR-M9-06`** (السكرب يُسعَّر بالكيلو ويظهر بوحدة «كجم») كان
**البندَ ① من ثغرة تغطية `DEBT-65`** — ⛅ **وقِيس حيّاً في هذا التشغيل:**
**«السكرب - جونية رقم 1 · 10.000 كجم»** بحقلَي **«سعر التوزيع لكل
كيلوجرام»** و**«الحد الأدنى لكل كيلوجرام»** ⟹ ✅ **البند ① مُغلَق**،
⚠️ **و② و③ باقيان.**

### 6.5 ⚠️ ملاحظة `flutter analyze` الوحيدة — خارج نطاق هذه الزيادة

`tools/debt63-repro/main.dart:12` — **استيرادٌ زائد** (`info`).
★ **مجلَّدٌ غيرُ متتبَّع في Git** (`git status`: `?? tools/`) **من جلسة
`DEBT-63`** ⛔ **ولم يُمَسّ**: ليس من نطاق هذه الزيادة ولا من `lib/`.
★ **ويُرفَع بحذف سطرٍ واحد متى قرّر المالك مصير المجلَّد.**

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

| # | البند | الحالة |
|:-:|---|---|
| 1 | ⛅ **نشرُ خدمة العمليات `qtms-callables` على التجريبية** | ✅ **نُفِّذ بيد المالك (2026-08-30)** — ★ **والأداةُ سلّمت الأوامر الثلاثة ولم تُنفِّذ منها شيئاً** (§ز)، ⟵ **ثم اكتفت بالقراءة** (`describe` · `get-iam-policy`) |
| 2 | ⛔ **أي نشرٍ إنتاجي** | ★ **لم يُطلَب ولم يُجهَّز** — ⛔ **ولا `uat-signoff` ولا `release-checklist` لُمِسا في هذا التشغيل** |

⛔ **ولا `ADR` جديد ولا `RFC` ولا تعديلٌ على `srs.md`** — ★ **ولم يُعتمَد
شيءٌ ذاتياً** (UPDS-05 §2 و§4).

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

★ **`WU-009` نفسُها ليست حسّاسة** — **قراءةٌ محضة في التطبيق**
(`pending_entries` **كتابتُها `if false`**) ⛔ **ولا مفتاحَ صلاحيةٍ جديد
ولا مسّ بقاعدة حماية.**

⚠️★★ **لكنها تُعدِّل أربعة معالِجات كاتبة** — ★ **بإضافة كتاباتٍ وحذوفاتٍ على
`pending_entries` وحدها**، ⛔ **بلا مساسٍ بأي قرارِ تفويضٍ ولا معادلةٍ مالية**
(⟵ **مُثبَتٌ بأن 461 اختبارَ سحابةٍ سابقاً لهذه الزيادة لم يتغيّر منها
اختبارٌ واحد**). ⟹ ★ **والمراجعةُ البشرية المطلوبة لها تنحصر في سؤالٍ واحد:
هل يُقبَل أن يُضيف كلُّ معالِجٍ كاتبٍ حذفاً على `pending_entries` داخل
معاملته؟** — ★ **وهو الحذفُ الثاني المسموح به في النظام بعد قالب الدور
غير المُسنَد** (`IQ-018`)، ★ **ومبرَّرٌ بأن البند مشتقٌّ بالكامل قابلٌ
لإعادة البناء** (`PAT-07` · `backup-and-recovery-policy.md`) ⛔ **لا سجلٌّ
ولا حركة.**

★★ **والوحدات الحسّاسة الثماني السابقة تبقى كما هي** (`WU-000` · `WU-002`…
`WU-008`) — ⛔ **ولم يتغيّر منها شيء في هذا التشغيل.**

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| [`docs/06-database/erd.md`](../../06-database/erd.md) §5 | ★ **شكلُ `PENDING_ENTRY` كما نُفِّذ** — المعرّف الحتمي والحقول العشرة |
| [`docs/06-database/data-dictionary.md`](../../06-database/data-dictionary.md) §4 | ★ **مفتاحُ `pending_entries` وحقولُه وحدودُ الكتابة عليه** |
| [`docs/04-design/module-design/pending-entries-design.md`](../../04-design/module-design/pending-entries-design.md) | **الحالة ⟵ «قيد التنفيذ»** · ★ **§12 جديد** (ما نُفِّذ · متى يُكتَب البند ويُمحى · ثلاثةُ حدودٍ معلَنة) |
| [`docs/02-requirements/requirements-traceability-matrix.md`](../../02-requirements/requirements-traceability-matrix.md) | ★ **عمود «الكود» لصفّ `FR-SYS-01`…`10`** ⟵ `🔷` صارت `🟩` |
| [`docs/03-architecture/risks-and-technical-debt-register.md`](../../03-architecture/risks-and-technical-debt-register.md) | ★ **`DEBT-66`** (وجهةُ زر [إدخال] لا تفتح ورقةَ الحقل) · **`DEBT-67`** (فلترُ التاريخ لم يُبنَ) |
| [`implementation-plan.md`](implementation-plan.md) | ★ **سجلُّ `WU-009` كاملاً** — ⛔ **بعد إزالة نسخة `WU-006` المُقحَمة** |

⛔ **ولم يُحدَّث `openapi.yaml` ولا `endpoints/`** — ★ **لا عمليةَ مستدعاة
جديدة**: **البنودُ أثرٌ جانبيٌّ للعمليات القائمة** (`api-overview.md` §3.1
بلا سطرٍ جديد). ⛔ **ولا `migrations-log.md`** — **لا هجرةَ بيانات**:
★ **المجموعةُ مشتقّةٌ وتُبنى من مستنداتها.**

---

## 10. حالة `CLAUDE.md`

✅ **حُدِّث — ونقطةُ التحوّل «اكتمال وحدة عمل»** (`claude-md-management.md`
§2 البند 5): ★ **`WU-009` صارت `[منجز ✅]`.**
★ **والتحديث عبر `sync_claude_md.py` وحده** ⛔ **لا تحريرٌ يدوي** — راجع
مخرَجه في §11.

---

## 11. الخطوة التالية الموصى بها

### 11.1 ✅ ما تمّ في هذا التشغيل

★ **`WU-009` مُغلَقة بالكامل** — كودٌ واختباراتٌ وتوثيقٌ ونشرٌ واختبارُ
محاكٍ. ⟵ **والمرحلة الأولى بقي فيها وحدتان:**

| WU | العنوان |
|---|---|
| `WU-010` | زيادة: الإرسال والتصدير من طرف إلى طرف |
| `WU-011` | زيادة: تقارير المرحلة الأولى |

⛔⛔ **ولم تُبدَأ أيٌّ منهما في هذا التشغيل** — ★ **بنصّ بروتوكول التشغيل §أ
الخطوة 5:** **لا تُبدأ وحداتُ المرحلة التالية في نفس التشغيل**، ⟵ **وهاتان
من المرحلة نفسها لكنّ تسلسلَ المرحلة يُستأنف عند الاستدعاء التالي.**

### 11.2 ⚠️ وما يُذكَر صراحةً قبل أي نشرٍ إنتاجي

⛔⛔ **ولا شيء نُشر على الإنتاج ولا جُهِّز له** — ★ **ولا `uat-signoff.md`
ولا `release-checklist.md` لُمِسا.** ★ **والشروط الأربعة السابقة للإنتاج
باقيةٌ بترتيبها** (`environments.md` §1.4): **① App Check ⟵ ② خدمة العمليات
⟵ ③ إقلاع المالك بمعرّفه ⟵ ④ ثم القواعد.**

### 11.3 ★ ودَينان مفتوحان من هذه الزيادة

`DEBT-66` (زرُّ [إدخال] لا يفتح ورقةَ الحقل نفسِه) · `DEBT-67` (فلترُ
التاريخ لم يُبنَ شريحةً) — ⚠️ **كلاهما منخفض**، ★ **ومسجَّلان بعلاجٍ مقترح.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
