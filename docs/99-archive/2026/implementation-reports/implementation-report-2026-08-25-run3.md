# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-25 (التشغيل الثالث)

**رقم تشغيل التنفيذ:** 38
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة** — شريحة رأسية كاملة)
**تقدّم التنفيذ:** 14.8٪ (**4 منجَزة من 27**) — ⛔ **ولم تُبدَأ زيادة جديدة في هذا التشغيل**
**حالة التوثيق وقت هذا التشغيل:** 100٪ (٠ بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** ✅ **صفر** — `IQ-020` حُسم وأُغلق (20 من 20)

---

## 1. ملخص تنفيذي

★★★ **تشغيلٌ لتصفية ذيول `WU-002` لا لبدء زيادة جديدة** — ثلاثة أوامر من
صاحب المشروع وُضعت موضع التنفيذ حرفياً:

| # | ما ورد | ما نُفِّذ |
|:-:|---|---|
| ① | **جواب `IQ-020` — الخيار أ** | ✅ **أُضيف `dealerDisableWithBalance` مفتاحاً مستقلاً** (73 لا 72) · **الشروط الثلاثة مجتمعة في الدالة الكاتبة** · ✅ **`DEBT-26` سُدِّد** |
| ② | **اعتماد مراجعة `master-data-design.md`** | ✅ **قرار إغلاق الكتابة صار معتمَداً لا مقترحاً** — ⟵ **ورُفع عنه بند «ينتظر مراجعة بشرية»** |
| ③ | **الإذن الصريح بالنشر** | ✅ **نُشر على التجريبية بالترتيب المُلزِم** — **الخدمة أولاً ثم القواعد** · ⛔ **والإنتاج لم يُمَسّ ولسببٍ مقيس** |

**الأرقام بعد التشغيل — مقاسة لا منقولة:**

| المجموعة | قبل | بعد |
|---|:-:|:-:|
| طبقة النطاق | 170 | ✅ **173** |
| العمليات السحابية | 207 | ✅ **209** |
| قواعد الحماية (المحاكي) | 222 | ✅ **224** |
| التطبيق | 136 | **136** |
| **المجموع** | **735** | ✅★★ **742 · ٠ فاشلاً** |

✅ **وكل أدوات الفحص الساكن بلا ملاحظة واحدة** · ✅ **وبوابة التغطية
`PASS ③`/`PASS ⑤`** · ✅ **و`integrity_violations` فارغة.**

★★★ **وطفرتان جديدتان قُتلتا** (36 تراكمياً) — تفصيلهما في §4.1.

---

## 2. بوابة الدخول — نتيجة الفحص

★ **البوابة العامة (٨ شروط) أُعيد فحصها: ✅ اجتازت** — ⛔ **ولا مستند حاكم
تغيّر منذ التشغيل السابق**، ★ **والـ17 `ADR` كلها «مقبول».**

⚠️ **وبوابة الوحدة (٥ شروط) لم تُطبَّق على وحدة جديدة** — ★ **لأن هذا
التشغيل لم يبدأ زيادة**، ⟵ **بل أتمّ بنداً داخل `WU-002` المنجَزة.**

---

## 3. ما نُفِّذ بالضبط — حسم `IQ-020` (الخيار أ)

### 3.1 القرار كما ورد ⟵ وكما نُفِّذ — بنداً ببند

| البند في جواب صاحب المشروع | التنفيذ |
|---|---|
| **مفتاح مستقل في `permissions-catalog.md` §2.1** | ✅ **`dealerDisableWithBalance`** — §2.1 صارت **(6)** والكتالوج **73 مفتاحاً** |
| **يُضاف إلى `Permission` والاختبار الثنائي الاتجاه والأسماء وشجرة الصلاحيات** | ✅ **الأربعة** — `permission.dart` · `permission_test.dart` (73) · `permission_labels.dart` (**اسمٌ عربي + مجموعة**) · `permission_labels_test.dart` (73) · ★ **والشجرة تقرأ `permissionGroups` فظهر فيها تلقائياً** |
| **سطرٌ مقابل في `security-requirements.md` §1** | ✅ **صفّ المقاوته أُعيد كتابته + بندٌ رابع ④ تحت الجدول** |
| **`dealerWrite` يبقى للإضافة والتعديل والتعطيل العادي** | ✅ **ولم يُمَسّ** — ★ **واختبارٌ صريح في الطبقتين يُثبت أن رصيد الصفر لا يشترط المفتاح الجديد** ⛔ **فليس بديلاً عنه** |
| **الشروط الثلاثة مجتمعة في العملية الكاتبة** | ✅ **① المفتاح** (`claims.has`) · **② إقرارٌ نصّي غير فارغ** · **③ رصيدٌ مقيسٌ من `dealer_balances` داخل المعاملة** ⛔ **لا من الحمولة** — **وغيابُ القياس رفضٌ صريح لا صمت** |
| **⛔ لا إعادة استعمال `dealerBalanceView`** | ✅ **لم يُمَسّ** — ★ **وبقي صلاحية قراءة** |
| **⛔ ولا تعديل `FR-M4-09` ولا `CR`** | ✅ **لم يُفتَح `srs.md` ولا أُنشئ `CR`** — ★ **استكمالُ كتالوج لا تعديلُ متطلب** |
| **شغّل فحوص التغطية والاتجاهين واختبارات الدالتين** | ✅ **الأربعة شُغِّلت فعلاً** — النتائج في §4 |

### 3.2 ★ قرارٌ تنفيذي صغير اتُّخذ ذاتياً — ويُعلَن

★★ **رمزا الرفض فُصلا عمداً:** نقصُ المفتاح يُعيد **`ERR_AUTH_001`**،
ونقصُ الإقرار يُعيد **`ERR_SETUP_006`** — ⛔ **ولا رمز جامع.**

★ **والمبرِّر:** الجواب نصّ على **«ثلاثة شروط»** متمايزة، ⟵ **ورمزٌ واحد
كان سيقول للمستخدم «اكتب إقراراً» بينما المشكلة أن الصلاحية ليست له.**
★ **والفرقة تُنفَّذ في طبقة النطاق بنوع الخطأ** — `PermissionError` مقابل
`ValidationError` — ⛔ **لا بسلسلة نصية تُقارَن.**

★ **وهو ضمن حدود الاعتماد الذاتي** (`implementation-run-protocol.md` ب):
**قرارٌ تنفيذي روتيني لا يمسّ بوابة حَوْكمة ولا يخالف نصّاً.**

---

## 4. التحقق — ★ شُغِّل فعلاً ⛔ ولا تغطية نصية

| # | الفحص | النتيجة |
|:-:|---|---|
| ① | `dart analyze` (النطاق) | ✅ **بلا ملاحظة** |
| ② | `dart test` (النطاق) | ✅ **173 · ٠ فاشلاً** (+3) |
| ③ | `dart analyze` (السحابة) | ✅ **بلا ملاحظة** |
| ④ | `dart test` (السحابة) | ✅ **209 · ٠ فاشلاً** (+2) |
| ⑤ | `npm run check:coverage` | ✅ **`PASS ③` و`PASS ⑤`** — **73 مفتاحاً · 13 مُنفَّذ في القواعد · 60 مستثنى بمبرر · ٠ فجوة** |
| ⑥ | `npm test` (المحاكي) | ✅ **224 · ٠ فاشلاً** (+2) |
| ⑦ | `flutter analyze` | ✅ **بلا ملاحظة** (والفحص الساكن صارم: كل ملاحظة حاجبة) |
| ⑧ | `flutter test` | ✅ **136 · ٠ فاشلاً** — ✅ **وبوابات التصميم الأربع ضمنها** |
| ⑨ | `track_implementation.py` | ✅ **`integrity_violations` فارغة** · **`IQ` مفتوحة: صفر** |

### 4.1 ★★★ الطفرتان — **الحارس يُثبَت بقتله**

| # | الطفرة | الأثر |
|:-:|---|---|
| ⑤ | **تعطيل شرط `canDisableWithBalance`** في `validateDealerDeactivation` | ⛔ **سقط اختبار** ⟵ ✅ **الشرط حارسٌ فعلي** — ثم أُعيد الأصل ونجحت 173 |
| ⑥ | **تمرير `true` ثابتةً** بدل `claims.has(...)` في `_planDealer` | ⛔ **سقط اختبار** ⟵ ✅ **المفتاح يُقرأ من البطاقة فعلاً** — ثم أُعيد ونجحت 209 |

> ★★ **ولماذا طفرتان لا واحدة:** الشرط مكتوب في **موضعين متعاونين** —
> **قاعدةُ العمل** في النطاق **وقراءةُ البطاقة** في السحابة. ⟵ ★ **وطفرةٌ
> على أحدهما لا تُثبت الآخر**، ⛔ **وأحدهما سليماً والآخر معطَّلاً يُنتج
> ثغرة صامتة.**

### 4.2 ⚠️ اختبارٌ قديم أُصلح — **والسبب يُقال لا يُطمَس**

★ **`⛔ ورصيدٌ غير صفري بلا إقرار ⟵ ERR_SETUP_006` سقط عند أول تشغيل** —
⛔ **ولم يكن خطأً في الاختبار بل نتيجةً صحيحة**: البطاقة الافتراضية في
مُساعِد الاختبار **لا تحمل المفتاح الجديد**، ⟵ **فصار الرفض لنقص الصلاحية
لا لنقص الإقرار.**

✅ **والإصلاح عزلُ المتغيّر:** مُنِح المفتاح صراحةً في ذلك الاختبار **ليبقى
الفرق الوحيد هو الإقرار** — ★ **وهو نفس مبدأ `permission-matrix.test.js`**:
⛔ **لا اختبارَ يمرّ لسببٍ خاطئ.**

★★ **وافتراضُ المُساعِد صار `canDisableWithBalance: false`** ⛔ **لا `true`** —
⟵ **فاختبارٌ ينسى ذكرَه يقع في الحالة الأقلّ صلاحية لا الأوسع.**

---

## 5. ✅★★★ النشر — بالترتيب المُلزِم لا بعكسه

⚠️ **بإذنٍ صريح من صاحب المشروع في هذا التشغيل.**

### 5.1 التجريبية `qtms-orimind-master` — ✅ **تمّ**

| # | الخطوة | النتيجة **المقيسة** |
|:-:|---|---|
| ① | **بناء الصورة** `callables:wu002` | ✅ **نجح** — 59 ثانية · سياق الجذر (`ADR-0012`) |
| ② | **نشر `qtms-callables`** | ✅ **المراجعة `qtms-callables-00006-8sn`** · **100٪ من المرور** · ★ **حساب الخدمة بأقل امتياز كما هو** ⛔ **ولم يُمنَح دورٌ جديد** |
| ③ | ★★★ **إثبات التوجيه حياً — 18/18** | ✅ **التسع الجديدة تُعيد `401 ERR_AUTH_003`** (⟵ **بلغت المعالج ورُفضت لغياب الرمز**) ⛔ **لا `404`** · ★ **ومسارٌ مجهول يُعيد `ERR_CALL_404`** |
| ④ | ★★ **جرد `users/`** (`RB-backfill-user-isactive` §4 ①) | ✅ **بطاقتان وكلتاهما `isActive: true`** — ⛔ **ولا بطاقة بـ`permissions` بلا `isActive`** ⟵ **فالترحيل لا محلّ له بالقياس** |
| ⑤ | ★★★ **نشر `firestore.rules`** | ✅ **ruleset `a516e4f6…`** · ★★ **مُتحقَّق بايتاً ببايت: 37799 بايت مطابقة للمستودع حرفياً** ⛔ **لا برسالة «Deploy complete»** |

> ★★ **ولماذا أُعيد الجرد ④ رغم وجود سجل سابق:** الرَنبوك **يُنفَّذ بخطوته
> الأولى ثم تُقرأ نتيجتها** — ⛔ **ولا يُفترَض أن نتيجة الأمس قائمة**،
> ★ **وقد كُتبت بطاقتان بين التشغيلين فعلاً** (كان الجرد السابق **صفراً**).

### 5.2 ⛔⛔★★ الإنتاج `qtms-orimind-c001` — **لم يُنشَر، وهذا قرارٌ مقيس**

★★★ **واجهة Cloud Run غير مُفعَّلة على مشروع الإنتاج أصلاً**
(`run.googleapis.com` معطَّلة — **مقيسٌ لا مفترَض**) ⟵ ★★ **فلا خدمة
عمليات هناك إطلاقاً.**

⛔⛔ **ونشرُ قواعد `WU-002` عليه كان سيكون عكسَ الترتيب المُلزِم حرفياً:**
القواعد **تُغلق كل مسارات الكتابة المباشرة فوراً**، **وبلا خدمةٍ تستقبلها
يبقى النظام بلا مسارٍ لكتابة بياناته المرجعية** — ⛔ **وهو العطل الذي
تحذّر منه `CLAUDE.md` نصّاً.**

★ **وقواعد الإنتاج اليوم على 36497 بايت** (إصدار `WU-026`) **وهي متسقة مع
حالته:** ★ **لا بيانات مرجعية عليه، ولا مالك أُقلع، ولا خدمة.**

⚠️ **وأربعة شروط تسبق أي نشر إنتاجي — بهذا الترتيب ⛔ لا بغيره:**

1. ★★ **تفعيل فرض App Check** — ⛔ **وهو أقدم بند معلَّق وأهمّها الآن**،
   لأن خدمة العمليات **عامة الوصول بالتصميم** (`ADR-0013` ③).
2. **تفعيل Cloud Run ونشر `qtms-callables`** بحساب خدمةٍ بأقل امتياز.
3. **إقلاع مالك الإنتاج بمعرّفه هو** — ⛔ **وخلطُ `QTMS_OWNER_UID` بين
   البيئتين تصعيدُ امتياز.**
4. ★ **ثم — وثمّ فقط — `firestore.rules`.**

---

## 6. مخالفات النزاهة المرصودة آلياً

✅ **لا شيء.** `track_implementation.py` ⟵ `integrity_violations: []`.

---

## 7. أسئلة التنفيذ

✅★★★ **`IQ-020` حُسم وأُغلق** — ⟵ **و`20/20` مغلقة** ⛔ **ولا بندَ `IQ`
مفتوح في المشروع.**

⛔ **ولم يُصعَّد بندٌ جديد في هذا التشغيل** — ★ **ولم يُخمَّن شيء.**

---

## 8. بنود بوابة الحَوْكمة

| البند | الحالة |
|---|---|
| **`IQ-020`** | ✅ **حُسم بجواب صاحب المشروع** (الخيار أ) ⟵ **ونُفِّذ حرفياً** |
| **قرار `master-data-design.md` §6.1** (إغلاق الكتابة) | ✅ **رُوجع واعتُمد صراحةً** |
| **إذن النشر** | ✅ **مُنِح صراحةً** ⟵ **ونُفِّذ على التجريبية بالترتيب المُلزِم** |
| ⚠️ **فرض App Check** | ⛔ **ما يزال مُطفأً** — ★ **وهو الشرط الأول لأي نشر إنتاجي** |
| ⚠️ **إقلاع مالك الإنتاج** | ⛔ **لم يُشغَّل** |

---

## 9. ⚠️ ما يحتاج مراجعة بشرية قبل النشر الإنتاجي

★★ **`WU-002` كاملةً تبقى حسّاسة** — بيانات شخصية + نطاق المصادر +
**إعادة كتابة طبقة الكتابة**. ✅ **وقد رُوجعت واعتُمدت**، ★ **والنشر
التجريبي أثبت مسارها حياً.**

⚠️⚠️ **وبندٌ يبقى غير مقيس ويُقال صراحةً:** ★ **لم يُشغَّل مسارٌ من طرف
إلى طرف من واجهة التطبيق نفسها بعد هذا النشر** — ⛔ **لأن ذلك يحتاج جلسة
مستخدمٍ حقيقية على جهاز**، ★ **وهي لا تُفتَح نيابةً عن صاحبها.**

★ **والمقيس فعلاً:** **الخدمة الحيّة تعرف المسارات التسعة وترفض بلا رمز** ·
**والقواعد المنشورة مطابقة للمستودع بايتاً ببايت**. ⟵ ★ **وهذان يُثبتان
النشر** ⛔ **ولا يُثبتان تجربة المستخدم** — والفرق يُقال لا يُطمَس.

★★ **والخطوة الموصى بها لصاحب المشروع:** **افتح التطبيق على التجريبية،
وأنشئ مصدراً، وتحقّق من ظهور قيد التدقيق** — ★ **وهو دليل نجاح الترتيب
كلّه** (`RB-backfill-user-isactive` §5).

> ⚠️ **ودرسُ `DEBT-24` يبقى قائماً:** ★ **عطلان سابقان مرّا من 581 اختباراً
> وكشفهما أول استدعاء حقيقي.** ⟵ ⛔ **فالتحقق الآلي شرط ضروري لا كافٍ.**

---

## 10. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | ما تغيّر |
|---|---|
| `docs/09-security/permissions-catalog.md` | **§2.1 ⟵ (6) و73 مفتاحاً** · **لافتة `IQ-020` بدل لافتة الانحراف** · ★ **§5 أُعيد قياسه كله** |
| `docs/09-security/security-requirements.md` | **صفّ المقاوته** · **البند ④ الجديد** |
| `docs/04-design/module-design/master-data-design.md` | **§6 الحالة الحدّية 3 ⟵ الشروط الثلاثة** |
| `docs/02-requirements/requirements-traceability-matrix.md` | **صفّ `FR-M4` ⟵ `FR-M4-09` اكتمل** |
| `docs/03-architecture/risks-and-technical-debt-register.md` | ✅ **`DEBT-26` سُدِّد** |
| `docs/00-governance/upds-workflow/implementation-questions.md` | ✅ **`IQ-020` ⟵ [تم الحل]** + **سجل الترحيل** |
| `docs/00-governance/upds-workflow/implementation-plan.md` | **أرقام التحقق أُعيدت** · **طفرتان جديدتان** · ★★ **سجل النشر** |
| `docs/11-devops-and-infrastructure/environments.md` | **§1.4 ⟵ 18 عملية والمراجعة والصورة** · ★★ **حقيقة الإنتاج المقيسة** |
| `docs/13-operations/runbooks/RB-backfill-user-isactive.md` | ★★ **§6.0 سجل تنفيذ ثانٍ** |
| `firestore.rules` | ★ **تعليقٌ يشرح لماذا لا شرط لـ`dealerDisableWithBalance` هنا** |

⛔ **ولم يُمَسّ `srs.md` ولا أي `ADR`** — ★ **ولا أُنشئ `CR`.**

---

## 11. حالة `CLAUDE.md`

★ **حُدِّث بـ`sync_claude_md.py` حصراً** ⛔ **ولا تحرير يدوي** — ★ **وثلاث
حقائق ثابتة تغيّرت:** **`IQ` المفتوحة صفر** · **742 اختباراً و36 طفرة** ·
★★ **وقواعد `WU-002` صارت منشورة على التجريبية** ⛔ **لا معلَّقة.**

---

## 12. الخطوة التالية الموصى بها

★★★ **`WU-003` — الوارد عدداً والمخزون اليومي.** ★ **أول زيادة تلمس المال
والكميات والتاريخين فعلياً**، ⟵ **وتبعياتها كلها منجَزة.**

⚠️ **والمرحلة الأولى ما تزال غير مستهلَكة:** `WU-003`…`WU-011` بحالة
`[مخطط]` **وكلها قابلة للبدء.**

★ **ولا بندَ `IQ` يحجب شيئاً** — ✅ **20/20 مغلقة.**
