# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-31

**رقم تشغيل التنفيذ:** 50
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة**)
**تقدّم التنفيذ:** 44.4٪ (12 منجَزة من 27)
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** 0

---

## 1. ملخص تنفيذي

★★ **بُنيت `WU-011` كاملةً برمجياً — أحدَ عشرَ تقريراً من `M19`** (`R-01` ·
`R-02` · `R-03` · `R-04` · `R-05` · `R-08` · `R-10` · `R-14` · `R-17` ·
`R-19` · `R-28`) بطبقة نطاقٍ خالصة تبني الجداول، ودليلِ قراءةٍ يُقيّد
`sourceId` في كل استعلام، وشاشتَي قائمةٍ وعرض، **وستةِ مفاتيح صلاحيةٍ
عائلية** (`IQ-034` الخيار ب) — ⟵ **فصار الكتالوج 81 مفتاحاً.**

✅★★★ **والبوابةُ الآلية كاملةٌ وخضراء: 1724 اختباراً · ٠ فاشلاً**
(236 حماية + 506 سحابة + 515 نطاق + 467 تطبيق) · **و`analyze` نظيفٌ في
الحزم الثلاث** · ⛔⛔ **و`firestore.rules` لم تُمَسّ بحرف.**

⏳⛔⛔★★★ **والوحدةُ باقيةٌ `[قيد التنفيذ]` ولم تُعلَن منجَزة** — ★ **وسببٌ
واحدٌ محدَّد:** **مزامنةُ حساب QA فشلت بـ`ERR_CALL_400`** لأن الخدمة المنشورة
تعرف 75 مفتاحاً لا 81، ⟵ **فاختبارُ المحاكي §د.2 يستحيل بحساب لا يملك
مفاتيحَ التقارير**، ⛔ **والنشرُ حصرُ المالك** (بروتوكول التشغيل §ز).

---

## 2. بوابة الدخول — نتيجة الفحص

| # | الشرط | متحقق؟ | المسار المفحوص |
|:-:|---|:-:|---|
| 1 | المنهجية مُصرَّح بها | ✅ | `docs/21-project-management/development-methodology.md` |
| 2 | العمارة والمكدّس | ✅ | `docs/03-architecture/architecture-overview.md` · `technology-stack.md` |
| 3 | أسلوب الكود والتسمية والأخطاء | ✅ | `docs/04-design/coding-standards.md` · `naming-conventions.md` · `error-handling-strategy.md` |
| 4 | تعريف «منجَز» والتفرّع والالتزام والإصدار | ✅ | `docs/07-development/*` · `docs/12-deployment-and-release/versioning-scheme.md` |
| 5 | استراتيجية الاختبار | ✅ | `docs/08-testing-and-qa/test-strategy.md` |
| 6 | سياسة الأسرار | ✅ | `docs/09-security/secrets-management-policy.md` |
| 7 | `srs.md` بمتطلبات مرقّمة | ✅ | `docs/02-requirements/srs.md` |
| 8 | لا `ADR` بحالة «مقترح» يحكم ما يُنفَّذ | ✅ | **23 ADR** — **كلُّها «مقبول»** (فُحص سطرُ الحالة في كلٍّ منها) |
| — | **بوابة الوحدة** (playbook §1.2) | ✅ | `reporting-design.md` · `FR-M19` · `indexing-strategy.md` · `permissions-catalog.md` · **وثلاثةُ مستندات الهوية البصرية** (بروتوكول §ح) |

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

⛔ **لا وحدة اكتملت في هذا التشغيل** — ★ **و`WU-011` وحدَها عُمل عليها**،
⟵ **وهي في §3.2 أدناه** ⛔ **ولا تُدرَج هنا لأن اختبارها الحيَّ لم يُشغَّل.**

### 3.2 وحدات قيد التنفيذ

| WU | العنوان | ما بُني | ما تبقّى |
|---|---|---|---|
| `WU-011` | زيادة: تقارير المرحلة الأولى | ★ **11 تقريراً** · **6 مفاتيح صلاحية** · **فهرسان** · **شاشتان** · **مكوّنان عامّان** · **35 اختباراً جديداً** · **7 مستندات محدَّثة** | ⏳ **نشرُ خدمة العمليات على التجريبية** ⟵ **مزامنةُ QA** ⟵ **اختبارُ §د.2** |

**الفحوصات المُشغَّلة فعلياً — بنتائجها المقيسة:**

| الفحص | النتيجة |
|---|---|
| `dart analyze` (النطاق) | ✅ `No issues found!` |
| `dart analyze` (السحابة) | ✅ `No issues found!` |
| `flutter analyze` | ✅ `No issues found!` |
| `dart test` (النطاق) | ✅ **515 · ٠ فاشلاً** |
| `dart test` (السحابة) | ✅ **506 · ٠ فاشلاً** |
| `flutter test` | ✅ **467 · ٠ فاشلاً** (★ **28 بوابةَ تصميم ضمنها**) |
| `npm run check:coverage` | ✅ **81 مفتاحاً · 13 في القواعد · 68 مستثنى بمبرر · 0 فجوة** |
| `npm run test:rules` | ✅ **236 · ٠ فاشلاً** |
| ★ **مزامنة QA** (`AM-004`) | ⛔ **`ERR_CALL_400`** — راجع §7 |
| ⏳ **اختبار المحاكي §د.2** | ⛔ **لم يُشغَّل** — محجوزٌ على ما سبق |

### 3.3 وحدات محجوبة (ولماذا)

⛔ **لا وحدة محجوبة** — ★ **ولا بند `IQ` جديد فُتح في هذا التشغيل**:
⟵ **كلُّ قرارٍ نُفِّذ كان مستنتَجاً من مستندٍ قائم أو محسوماً في `IQ-032`
و`IQ-034`.**

---

## 4. مخالفات النزاهة المرصودة آلياً

**لا مخالفات** — `track_implementation.py` ⟵ `integrity_violations: []`
(**مقيسٌ بعد تحديث الخطة**: `total 27 · done 12 · in_progress 1 · blocked 0`).

★★ **و`WU-011` باقيةٌ `[قيد التنفيذ]` بصدق** ⛔ **ولم تُرفَع إلى `[منجز ✅]`**
— ★ **رغم أن كل فحصٍ آليٍّ فيها أخضر**: ⟵ **لأن بروتوكول §د.2 شرطٌ إضافي
لا بديلٌ عن الآلي**، ⛔ **وتعليمُها منجَزةً الآن ادّعاءٌ غيرُ متحقَّق**
(§و من البروتوكول).

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

⛔ **لا بند `IQ` جديد.** ★ **والمحسومان اللذان نُفِّذا هنا:**

| IQ | القرار | أين نُفِّذ |
|---|---|---|
| `IQ-034` | **الخيار ب — مفتاحٌ لكلِّ عائلةِ تقارير** (**ستةُ مفاتيح**) | `permission.dart` · `permissions-catalog.md` §2.11 · `permission_labels.dart` · `check-permission-coverage.mjs` · `report_catalog.dart` |
| `IQ-032` | **التصدير بـ`documentExport` عبر `logExport`** ⛔ **ولا نظير يُبنى** | `report_table.dart` (`buildReportExport`) · `document_export_action.dart` · `audit_entity_types.dart` (`reportEntityType`) |

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

| # | ما يقوله المستند | ما فرضه التنفيذ | كيف عولج |
|:-:|---|---|---|
| 1 | `permissions-catalog.md` §5 البند 3: **«كل مفتاح له شرط في القواعد»** | ★ **لا مجموعةَ مستقلةَ لتقريرٍ تُحمى** — **التقريرُ مُجمَّعٌ من مجموعاتٍ لها شروطُها** | ✅ **استثناءٌ موثَّقٌ بمبرِّره** في §2.11 وفي بوابة التغطية — ★ **بنفس سابقة `sourceNetImpactView` حرفياً** (`ADR-0011` §5) · ⚠️ **والمخاطرةُ المتبقية معلَنةٌ في `DEBT-71`** ⛔ **لا مكتومة** |
| 2 | `reporting-design.md` §2 القاعدة 5: **وسمُ «⟳ مُحدَّث بأثر رجعي»** | ⛔ **لا كاتبَ لـ`retroUpdatedAt` في النظام اليوم** | ✅ **لم يُبنَ الوسم** — ★ **بقاعدة «لا قيمة بلا كاتبٍ لها فعلاً»**، ⟵ **ومُسجَّلٌ في `reporting-design.md` §7.1** ⛔ **بدل وسمٍ لا يظهر أبداً** |
| 3 | `FR-M19` §2: **فلاترُ الرعوي والجونية والمقوت والنوع والمستخدم والحدّ الأدنى** | ⛔ **لم تُبنَ في هذه الزيادة** | ✅ **مُعلَنةٌ في `DEBT-72`** — ★ **وسطرُ الفلاتر تحت كلِّ تقريرٍ في القائمة يذكر المتاحَ فعلاً** ⛔ **فلا يَعِد التطبيقُ بما لا يفعله** |
| 4 | ⛔ **اختبارُ `export_log_test.dart` كان يستعمل `'report'` مثالاً لنصٍّ حرٍّ مرفوض** | ★ **صار نوعاً معتمَداً في هذه الزيادة** | ✅ **استُبدل المثالُ بنصٍّ لا كاتبَ له** ⛔ **ولم يُحذَف الفحص** — ★ **والدرسُ مكتوبٌ في موضعه** |

⛔⛔ **ولا ADR جديد ولا تعديلَ ADR معتمد** — ★ **ولا `CR`**: ⟵ **قرارُ
`IQ-034` استكمالُ كتالوجٍ لتنفيذ متطلبٍ كما هو** ⛔ **لا انحرافٌ عنه.**

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

### 7.1 ⛔⛔★★★ نشرُ خدمة العمليات على التجريبية — **حصرُ المالك** (§ز)

★ **الحاجةُ مقيسةٌ لا مفترَضة:** ⛅ **`tools/staging/qa_permission_sync.py`
شُغِّل فعلاً وأعطى:**

```text
① signIn -> 200 · uid_matches_registered_owner = True
② bootstrapOwnerPermissions -> 200 (alreadyBootstrapped)
③ catalog keys = 81 · documentExport = True
④ grantPermissions -> 400 {"status":"INVALID_ARGUMENT","code":"ERR_CALL_400"}
⑤ users/QA -> 200 · permissions_true = 75 · isActive = True · sourceScope = all
```

⛔⛔★★ **والجذرُ سطرٌ واحد في الخدمة المنشورة:**
`permission_sync_handler.dart:159` — `_readPermissions` **تقرأ كلَّ مفتاحٍ
بـ`_byName`** ⟵ **و«مفتاحٌ مجهول يُبطل الطلب كله»** (تعليقُ الدالة نفسِه)،
★ **والستةُ الجديدة مجهولةٌ لخدمةٍ صُرِّفت قبلها.**

⟵ ⛔ **فلا مزامنةَ ولا اختبارَ حيّاً قبل النشر** — ★ **والأمرُ مُسلَّمٌ
للمالك في الردّ النصّي** (بروتوكول §ج) ⛔ **ولم يُنفَّذ من هذه الجلسة.**

### 7.2 ⛔⛔ ولا نشرَ إنتاجيَّ ولا اقترابَ منه

★ **لم يُطلَب اعتمادُ نشرٍ إنتاجي في هذا التشغيل** — ⛔ **ولا مستندَ اعتمادٍ
جُهِّز**: ⟵ **الوحدةُ لم تكتمل بعد**، ★ **والتجهيزُ نفسُه مشروطٌ بنجاح §د**
(§ب.2 البند ①).

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

| WU | لماذا | ما يجب أن يراه المراجع تحديداً |
|---|---|---|
| `WU-011` | ★★ **تُضيف ستةَ مفاتيح تفويض** ⟵ **الفئة 1 البند 5 (بوابةُ الأمن)** · ★ **وتعرض أرقاماً مالية محكومةً بنطاق المصادر** | ① **`DEBT-71`** — ★ **مفاتيحُ المخزنية والرقابية بواباتُ شاشةٍ لا حارسَ قراءة**: ⟵ **هل يُقبَل هذا الحدُّ أم يُفتَح قرارٌ معماري؟** · ② **`receiptAmountForSource`** — ★ **قاعدةُ «لا رقمَ من مصدرٍ خارج التقرير»** (`FR-M19-02`): ⟵ **تُراجَع بالعين على سندٍ يمسّ مصدرين** · ③ **عمودا القيمة والإيداع** — ★ **لا يُطلَبان أصلاً بلا مفتاحهما** (`ت-12` · `ADR-0017`) |
| **الوحدات الاثنتا عشرة المنجَزة سابقاً** | ⚠️ **مراجعتُها الأمنية البشرية قائمةٌ كما هي** — ★ **وأخصُّها `WU-004`** (`sackCreate` + أوزانٌ يُبنى عليها سعرُ الجونية) | ⛔ **لم تُمَسّ في هذا التشغيل** |

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| `docs/09-security/permissions-catalog.md` | ★★ **§2.11 جديد — ستةُ مفاتيح** · **والعدد 75 ⟵ 81** · **والمستثنى 62 ⟵ 68** |
| `docs/09-security/security-requirements.md` §1 | ★ **صفُّ «عرض التقارير» بمبرِّر الاستثناء والمخاطرة المتبقية** (البند ② من §6 من الكتالوج) |
| `docs/06-database/indexing-strategy.md` §2 | ★ **فهرسان جديدان بمبرِّرِ كلٍّ منهما** — ⛔ **ولا فهرسَ بلا استعلامٍ فعلي** |
| `docs/04-design/module-design/reporting-design.md` | ★★ **الحالة ⟵ «منفَّذٌ جزئياً»** · **§7.1 جديد: حالةُ كلِّ قاعدةٍ من §2 مقيسةً** |
| `docs/02-requirements/requirements-traceability-matrix.md` | ★ **عمودُ الكود لـ`FR-M19`** — ★ **بما نُفِّذ وما لم يُنفَّذ صراحةً** |
| `docs/03-architecture/risks-and-technical-debt-register.md` | ★ **`DEBT-71` · `DEBT-72` · `DEBT-73`** |
| `docs/07-development/troubleshooting-guide.md` | ★★ **§2-و: عثرتان مقيستان** (**إعادةُ محاولة Riverpod** · **ممرٌّ داخل ممر**) |
| `docs/00-governance/upds-workflow/implementation-plan.md` | ★ **بند `WU-011` كاملاً: الملفات · الفحوص بنتائجها · القرارات الذاتية · الديون · بند الانتظار** |

⛔ **ولم تُحدَّث `openapi.yaml` ولا `endpoints/` ولا `erd.md` ولا
`data-dictionary.md` ولا `migrations-log.md`** — ★ **ولا موجب**: ⟵ **لا
عمليةَ سحابية جديدة ولا حقلَ مخزَّنَ جديد**: **التقريرُ مشتقٌّ يُبنى عند
الطلب ولا يُخزَّن** (`ADR-0008`)، ★ **والحقلان المقروءان حديثاً**
(`stockDate` في الدفتر · `settlementStatus` في التوزيعة) **مكتوبان ومُوثَّقان
منذ `WU-003` و`WU-006`** ⛔ **ولم يُضَف حقلٌ واحد.**

---

## 10. حالة `CLAUDE.md`

**لم يُحدَّث — لا نقطة تحوّل في هذا التشغيل.** ★ **نقطةُ التحوّل المعرَّفة
هي «اكتمالُ وحدة عمل»** (`claude-md-management.md` §2 البند 5) — ⟵ **و`WU-011`
لم تكتمل**: ⛔ **وتحديثُ العدّادات الآن يجعل الملفَّ يقول ثلاثةَ عشرَ منجَزة
وهي اثنتا عشرة.**

★ **ويُحدَّث في التشغيل الذي يُغلقها** — ★ **بسطر المرحلة والعدّادات ومفاتيح
الكتالوج 81** ⛔ **لا قبله.**

---

## 11. الخطوة التالية الموصى بها

```text
① المالك يُشغِّل أمرَ نشرِ خدمة العمليات على التجريبية   (الأمر في الردّ النصّي)
② تُشغَّل مزامنةُ حساب QA: python tools/staging/qa_permission_sync.py
   ⟵ المتوقَّع: grantPermissions -> 200 · permissions_true = 81
③ يُشغَّل اختبارُ المحاكي §د.2 على Pixel_6_API_36
④ تُرفَع WU-011 إلى [منجز ✅] بفحوصها الحيّة، ثم تُغلَق المرحلة الأولى
   باختبارها التكاملي (§د.3) وبوابةِ product-roadmap.md §5
```

⛔⛔★★ **ولا تُفتَح المرحلة الثانية في التشغيل نفسِه** — ★ **البروتوكول §أ
البند 5**: ⟵ **تُعلَن المرحلةُ مكتملةً وتبدأ التالية عند الاستدعاء التالي.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
