# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-26 (تشغيل 2)

**رقم تشغيل التنفيذ:** 39
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة** — شريحة رأسية كاملة)
**تقدّم التنفيذ:** 22.2٪ (6 منجَزة من 27) — ⛔ **بلا تغيير عن التشغيل السابق**
**حالة التوثيق وقت هذا التشغيل:** 100٪ (٠ بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** ٠ (21 من 21 مغلقة)

---

## 1. ملخص تنفيذي

★★ **هدفُ التشغيل كان ثلاثةً — أُنجِز اثنان كاملَين، وتعذّر الثالث عند نقطةٍ
واحدةٍ محدَّدة لا لبس فيها.**

✅★★★ **① بند التدقيق ① — `sourceDocType` المحفور: أُغلِق بدليلٍ فعلي.**
★ **العطلُ نفسه كان مُصلَحاً في الكود منذ التشغيل السابق** ⛔ **لكن بلا اختبارٍ
يحرسه إطلاقاً** — ⟵ ★ **وهذه هي الفجوة الحقيقية التي أُغلقت:** كُشف المُحوِّل
للاختبار وكُتب **اختبار ارتدادٍ سلوكي بخمسة بنود**، ⛔⛔ **ولم يُكتفَ بكتابته:**
★★ **أُعيد العطلُ عمداً فأفشل الاختبار فعلاً** (`Expected: sack` /
`Actual: countedIntake`) **ثم أُزيل فعاد أخضر** — ⟵ **فالحارسُ مُثبَتٌ لا مُدَّعى.**

✅★★★ **② بند التدقيق ② — نشرُ `WU-003`/`WU-005`: أُغلِق بفحصٍ حيّ.**
**المراجعة `qtms-callables-00007-ctd`** · الصورة `callables:wu004` · **100٪ من
المرور** · ★★ **و30 عمليةً من 30 ردّت `401`** (منها الثلاث المخزونية
و`writeDailyPrices`) **ومسارٌ مجهول ردّ `404`** — ⟵ ★ **فالتوجيه سليمٌ والعمليتان
منشورتان فعلاً** ⛔ **لا مُعلَنتَين فقط.**

⛔⛔★★ **③ واختبارُ المحاكي الوظيفي لم يُنفَّذ — والسبب واحدٌ لم يتغيّر:**
★ **حسابُ الاختبار (QA) لم يُنشأ بعد.** ⛔⛔ **وإنشاءُ الحسابات وكتابةُ كلمات
المرور ممنوعان على الأداة بحدٍّ لم يُعدِّله `AM-004` ولا `AM-005`** — ★ **فما
أباحه `AM-004` هو **قراءةُ** اعتمادٍ **قائم** بلا اطّلاع** ⛔ **لا **إنشاؤه**،
★ **و`RB-staging-qa-account` §3.1 يُسنِد الإنشاء صراحةً إلى المالك في وحدة
التحكم.** ⟵ **فسُلِّمت الخطوة للمالك** (بروتوكول §ج).

⛔⛔ **و`WU-004` تبقى `[قيد التنفيذ]`** — ★ **بصدق لا بتساهل**: **السيناريوهات
الإحدى عشر لم يُنفَّذ منها واحد.**

---

## 2. بوابة الدخول — نتيجة الفحص

✅ **اجتِيزت كما هي** — ⛔ **ولا شرط سقط**: المنهجية مُصرَّح بها · مستندات
العمارة والتصميم والاختبار والأمن قائمة · ⛔ **ولا `ADR` بحالة «مقترح» يحكم ما
نُفِّذ.** ★ **والتشغيل لم يُنشئ وحدةً جديدة أصلاً** — **بل أغلق بندَي تدقيق على
وحدةٍ قائمة.**

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

⛔ **لا وحدة جديدة** — ★ **والستّ المنجَزة سابقاً بقيت كما هي** (`WU-000` ·
`WU-001` · `WU-002` · `WU-003` · `WU-005` · `WU-026`).

### 3.2 وحدات قيد التنفيذ

**`WU-004`** — ⏳ **`[قيد التنفيذ]`** · ⛔ **والحاجبُ واحد: حسابُ الاختبار**
(`DEBT-32`). ✅ **وكلُّ الفحوص الآلية نجحت** · ✅ **والخدمة منشورة ومُثبَتٌ
توجيهها حيّاً** · ⛔ **والسيناريوهات الإحدى عشر لم تُنفَّذ.**

### 3.3 وحدات محجوبة (ولماذا)

⛔ **لا وحدة محجوبة** — ★ **و`WU-006` فما بعدها `[مخطط]` بترتيب تبعياتها.**

---

## 4. مخالفات النزاهة المرصودة آلياً

✅ **`integrity_violations: []`** · **`unparsed_lines: []`** ·
**`has_drift: false`** — ⛔ **ولا وحدة معلَّمة منجَزة زوراً.**

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

⛔ **لا بند `IQ` جديد.** ★ **ولم يظهر سلوكٌ غير موصوفٍ في معيار قبول** —
**فالعملُ كلُّه كان إغلاقَ بندَي تدقيقٍ موصوفَين وقياساً.**

★★ **وقرارٌ تنفيذيٌّ روتينيٌّ واحد اتُّخذ ذاتياً** (بروتوكول §ب الفئة 3):
**كشفُ `_movementOf` للاختبار** بـ`@visibleForTesting` — ⟵ ★ **لأن العطل
المرصود كان في هذا التحويل نفسه**، ⛔ **وحراستُه بتغطيةٍ نصّية كانت ستُثبت أن
الشرط «مذكور» لا أنه «صحيح»** (`CLAUDE.md`). ⛔ **ولا تغيّر سلوكيٌّ واحد** —
★ **الدالة نفسها بالاسم نفسه والمنطق نفسه**، **وموضعُ استدعائها واحدٌ لم يتبدّل.**

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

### ⓵ ⚠️★★ **ثغرةٌ حقيقية: إصلاحٌ بلا حارس**

**ماذا وُجد:** إصلاحُ `sourceDocType` (تقرير التشغيل السابق §6 ⓵) **كان
مطبَّقاً في الكود فعلاً** ⛔ **ولا اختبارَ واحد يمسّه** — ★ **بحثٌ نصّي في
`test/` أعاد إشارةً واحدة يتيمة في `fake_inventory.dart`.**
**ولماذا هذا خطير:** ★ **العطل صنفُه «خطأ عرضٍ صامت»** — ⛔ **لا يُسقِط شيئاً
ولا يُنتج رسالة**، ⟵ **فعودتُه لن يكشفها إلا مراجعةُ عينٍ لسطرٍ واحد.**
**المعالجة:** ✅ **اختبارُ ارتدادٍ سلوكيٌّ بخمسة بنود** — ★ **يحرس الخريطة
كاملةً** (كلُّ قيمةٍ تُقرأ قيمتَها · والغائبُ يعود للافتراض الآمن · والمجهولُ
لا يُسقِط السطر).

### ⓶ ✅★★ **حقيقةٌ تشغيلية تأكّدت بالقياس لا بالنقل**

**التشغيل السابق أعلن** أن صورة `callables:wu004` تحمل `WU-003` و`WU-005`
**بعد أن كانت الخدمة على `callables:wu002`**. ✅ **وقيس ذلك حيّاً في هذا
التشغيل** — **30/30 عمليةً ردّت `401`** ⟵ ★ **فالإعلان صحيح.**

> ⚠️★ **وفائدةٌ منهجية:** أولُ مسحٍ أعاد `29/30` — **`updateSupplier` ردّ
> `000`** ⛔ **وهو انقطاعُ نقلٍ في `curl` لا ردُّ خادم**، ★ **وأعادت المحاولةُ
> الأولى `401`.** ⟵ ⛔ **فلا يُحسب فشلُ نقلٍ عطلاً في الخدمة، ولا يُبتلع صامتاً.**

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

**لا بند جديد.** ⛔ **ولم يُعتمَد شيء ذاتياً:** لا `ADR` · لا `CR` · ⛔ **ولا
تعديل على `srs.md` ولا على أي مستند `STATIC`.**

⛔⛔★★ **والبوابة القائمة كما هي بلا مساس:** **لا يُنشَر شيء على الإنتاج بلا أن
يكتب المالك حرفياً `اعتمد-النشر-الإنتاجي`** — ★ **ولم تُجهَّز `uat-signoff.md`
ولا `release-checklist.md` في هذا التشغيل** ⛔ **لأن شرطَ تجهيزهما المعلَن
(«نجاح كل السيناريوهات الإحدى عشر») لم يتحقق أصلاً.**

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

★★★ **`WU-004` حسّاسة أمنياً** (مفتاح `sackCreate` الجديد + أوزانٌ يُبنى عليها
سعرُ الجونية وصافي الرعوي) — ⛔ **وتحتاج مراجعةً بشرية صريحة قبل أي نشر إنتاجي.**

★★ **وخُطّاف `DEBT-31` بندٌ حسّاسٌ مستقل** (`AM-005`) — ⛔⛔ **لا يُغني عن
مراجعة `WU-004` ولا يقرّبها خطوة.**

⛔⛔★★ **والإنتاج لم يُمَسّ بحرف في هذا التشغيل** — ★ **وشروطه الأربعة باقية:**
**App Check** ⟵ **الخدمة** ⟵ **إقلاع المالك بمعرّفه هو** ⟵ **ثم القواعد.**

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| `docs/00-governance/upds-workflow/implementation-plan.md` | ★ **كتلةُ فحوص `2026-08-26-run2` في `WU-004`** — الأرقامُ المقيسة وبندا التدقيق وسببُ بقاء الحجب |
| `docs/03-architecture/risks-and-technical-debt-register.md` | ⚠️★★ **`DEBT-32`** — أن المحاولة جرت وتعذّرت عند النقطة نفسها، وما أُغلِق بدلاً عنها |

⛔ **ولا مستندَ آخر احتاج تحديثاً** — ★ **فلا Endpoint تغيّر ولا حقلَ قاعدةِ
بياناتٍ ولا سلوكَ موثَّقاً في `srs.md`.**

---

## 10. حالة `CLAUDE.md`

⛔ **لم يُحدَّث — ولم تحدث نقطةُ تحوّل** (`claude-md-management.md` §2):
★ **لا وحدةَ اكتملت، ولا منهجيةَ تغيّرت، ولا أمرَ بناءٍ جديد، ولا `ADR` انتقل.**
⚠️ **وعددُ الاختبارات وحده تغيّر** (**1032 ⟵ 1037**) — ⛔ **وهو تفصيلٌ متغيّرٌ
كثيراً موضعُه `implementation-plan.md` والتقارير** ⛔ **لا الكتلةُ المُدارة.**

---

## 11. الخطوة التالية الموصى بها

### ⛔⛔★★★ أولاً — **إنشاءُ حساب الاختبار** (فعلٌ بشري لا بديل عنه)

★ **نفّذ `RB-staging-qa-account` §3.1** في وحدة تحكم Firebase لمشروع
**`qtms-orimind-master`** ⛔ **لا الإنتاج** ⟵ ثم **§3.2** لمنح الصلاحيات
و`isActive: true`، ثم املأ القيمتين في `.secrets/qtms-staging-qa-account.env`.

### ثم — **السيناريوهات الإحدى عشر** كما هي في تقرير `2026-08-26` §11

```bash
flutter run --flavor staging --dart-define-from-file=.secrets/qtms-staging-qa-account.env
```

★★ **والخُطّاف سيملأ الحقلين تلقائياً** (`AM-005` · `DEBT-31`) ⛔ **بلا إرسالٍ
تلقائي** — ★ **فالضغط على «دخول» يبقى فعلاً صريحاً.**

★ **ولا بند `IQ` مفتوح يمنع أياً من ذلك.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
