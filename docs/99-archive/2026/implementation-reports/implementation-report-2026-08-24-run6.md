# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-24 (تشغيل 6)

**رقم تشغيل التنفيذ:** 18
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة**)
**تقدّم التنفيذ:** 0٪ (0 منجَزة من 27)
**حالة التوثيق:** 100٪ · **أسئلة تنفيذ مفتوحة:** ★ **0**
**المطلوب:** إكمال `WU-026` و`WU-000` حتى الإغلاق النهائي

---

## 1. ملخص تنفيذي

★★ **قيس معيار القبول 5 فعلاً — وكان آخر ما يمنع `WU-026` من الاكتمال.**
أُنشئت أداة قياس مخصّصة، ونتيجتها **أرقام مرصودة لا مُقدَّرة**:
**الحدّ 10 استدعاءات** · و**40 استدعاءً على نفس المسار نجحت** ⟵ **التخزين
المؤقت مُثبَت برهاناً قاطعاً**.

✅ ★ **و`DEBT-14` سُدِّد برمجياً بالكامل** — كود عيّنة `DEBT-09` واختباراته
ونقطة دخولها **أُزيلت**، ★ **فلم يبقَ في `bin/server.dart` إلا الأهداف
الثلاثة الحقيقية**.

⛔ **ولم تُغلَق الوحدتان — لبندين محجوبين بصلاحيات البيئة لا بنقص عمل:**
① **النشر السحابي مرفوض** من مُصنِّف الصلاحيات · ② **ولا جلسة مستخدم
مصادَقة** لقياس زمن الاستجابة.

★ **338 اختباراً ناجحاً · ٠ فاشلاً** · ✅ **والبوابتان معاً تنجحان (`exit 0`).**

---

## 2. ★★ القياس — معيار القبول 5

★ **أداة جديدة:** [`test/security-rules/measure-read-cost.mjs`](../../../test/security-rules/measure-read-cost.mjs)
تعمل داخل المحاكي عبر المُشغِّل نفسه (فتستفيد من معالجة `DEBT-10`).

| # | الرقم | ✅ **المقيس** |
|:-:|---|---|
| ① | الحدّ الفعلي لاستدعاءات الوصول في تقييم واحد | ★ **10** |
| ② | هل `get()` المكرّرة على نفس المسار تُحتسَب مرة واحدة؟ | ✅ ★ **نعم** |
| ③-أ | الفوترة | ★ **قراءة واحدة إضافية لكل طلب** — مشتقّة **حتمياً** من ② |
| ③-ب | زمن الاستجابة على قاعدة حقيقية | ⛔ **لم يُقَس — محجوب** |

### ★ لماذا قياس ② برهانٌ قاطع لا مؤشِّر

جُرّبت **40 استدعاءً على نفس المسار**. ⛔ **بلا تخزين مؤقت لكانت تستهلك 80
استدعاءً وتتجاوز الحدّ العاشر فتفشل حتماً.** ⟵ ★ **فنجاحها لا يحتمل تفسيراً
آخر**، والقياس **لا يعتمد على وثيقة منصّة بل على سلوكها المرصود**.

★ **والنتيجة العملية:** `perm()` تستهلك **استدعاءين ثابتين** (`exists` +
`get`) **مهما تكرّرت** — ⟵ **الهامش المتبقي 8 استدعاءات**. ✅ **وبهذا
تُحسَم ملاحظة `IQ-008` التنفيذية نهائياً.**

---

## 3. ما أُنجز أيضاً — `DEBT-14` (الشطر البرمجي)

| # | ما أُزيل | ✅ |
|:-:|---|:-:|
| 1 | `functions/lib/src/spike_probe.dart` | ✅ **حُذف** |
| 2 | خمسة اختبارات `buildSpikeTrace` | ✅ **أُزيلت** |
| 3 | نقطة الدخول `function` (مشغّل العيّنة) | ✅ **أُزيلت** |
| 4 | ترويسة «الحالة الآن: عيّنة إثبات» | ✅ **حُدِّثت** |

★ **والتحقق فعلي لا شكلي:** `dart analyze` نظيف · `build_runner` أُعيد
تشغيله ⟵ ★ **و`bin/server.dart` صار يحوي الأهداف الثلاثة الحقيقية وحدها** ·
و`dart compile exe` نجح.

⛔ **ويبقى الشطر السحابي** (مجموعة `spike_debt09` · مستند
`SRC-EVENTARC-PROBE` · مشغّل Eventarc · خدمة Cloud Run وصورتاها · حساب
الخدمة · عدّادات `SRC-CHECK6-*`) — **حذفٌ سحابي، ⛔ ولا يُنفَّذ بلا إذن صريح.**

---

## 4. ⛔ الحاجزان — ولماذا لم أتجاوزهما

### ① النشر السحابي مرفوض

```
firebase deploy --only firestore:rules --project qtms-orimind-master
⟵ Permission for this action was denied by the auto mode classifier
```

⛔ **ولم ألتفّ عليه** — والالتفاف على منع صلاحيات **مخالفة بذاتها**.

### ② لا جلسة مستخدم مصادَقة

قياس زمن الاستجابة على قاعدة حقيقية يحتاج **عميلاً مصادَقاً**. ⛔ **ولا
تتوفّر بيانات دخول**، ★ **ولا يجوز التعامل مع كلمات المرور أصلاً.**

> ⚠️ **وأثر الحاجزين مختلف عن «عملٍ ناقص»:** ما بقي **ليس كوداً لم يُكتب**
> بل **فعلٌ على بيئة سحابية**. ★ **وكل ما يُقاس محلياً قيس فعلاً.**

---

## 5. لماذا لم أبنِ `provisionAccountsOnSourceAdd` — ولم أخترعه

★ **بند من نطاق `WU-000` توقفتُ عنده بوعي:** «مُنشئ الحسابات» عمليتان
مشغَّلتان بالكتابة (`api-overview.md` §3.2).

⛔ **وثلاثة موانع تمنع بناءه الآن — أوّلها كافٍ:**

| # | المانع |
|:-:|---|
| 1 | ★★ **لا يمكن التحقق منه إطلاقاً بلا نشر** — وهو محجوب. **ودالة مشغَّلة لا تُشغَّل ليست منجَزة** (`implementation-playbook.md` §7 البند 2) |
| 2 | ⚠️ **«حساب» غير مُعرَّف بدقة في نموذج البيانات** — `dealer_balances/{dealerId}_{sourceId}` قائم، ⛔ **لكن «+ نسخة السكرب» عند إضافة مصدر بلا تعريف** |
| 3 | **`DEBT-16`** — تسليم أحداث القاعدة **protobuf** والفكّ الحالي JSON |

★ **وكتابته بتخمين المانع 2 مخالفة صريحة** لقاعدة «لا تخمّن قاعدة غامضة».
⛔ **ولم أُصعِّده بنداً `IQ` لأن المانع 1 يجعله سابقاً لأوانه** — يُصعَّد حين
يصير النشر ممكناً فيُبنى ويُتحقَّق منه معاً.

---

## 6. الفحوصات التي شُغِّلت فعلاً

| الفحص | النتيجة |
|---|---|
| ★★ `node run-rules-tests.mjs "node measure-read-cost.mjs"` | ✅ **الحدّ 10 · والتخزين المؤقت مُثبَت** |
| `npm test` (**البوابتان**) | ✅ **`exit 0`** · `PASS ③` · `PASS ⑤` · **188 · ٠ فاشلاً** |
| `cd functions && dart analyze` · `dart test` | ✅ **`No issues found!`** · **75 · ٠ فاشلاً** |
| `dart run build_runner build` | ✅ ★ **ثلاثة أهداف — ولا أثر للعيّنة** |
| `dart compile exe bin/server.dart` | ✅ **Dart وحده بلا Flutter** |
| النطاق · Flutter | ✅ **66** · **9** |

★ **المجموع: 338 اختباراً ناجحاً · ٠ فاشلاً.**

---

## 7. مخالفات النزاهة

✅ **لا مخالفات** — `integrity_violations: []` · `unparsed_lines: []`.
⛔ **ولا وحدة رُفعت إلى `[منجز ✅]`** — **الوحدتان `[قيد التنفيذ]` بصدق.**

---

## 8. بنود بوابة الحَوْكمة

| البند | الحالة |
|---|---|
| `ADR-0017` | ✅ **اعتمدته صراحةً** — والحالة «مقبول» مطابقة |
| **الـ17 ADR** | ✅ **كلها «مقبول»** ولا واحد «مقترح» |
| **حذف سحابي / نشر** | ⏳ **ينتظر إذنك** — راجع §4 و§9 |

---

## 9. ⚠️ ما تحتاجه لإغلاق الوحدتين — ثلاثة أفعال سحابية

| # | الفعل | يُغلِق |
|:-:|---|---|
| 1 | **السماح بالنشر** (`firebase deploy --only firestore:rules`) | ينشر قواعد `ADR-0016`/`ADR-0017` على البيئتين |
| 2 | **جلسة مستخدم على التجريبية** لقياس زمن الاستجابة | ★ **يُغلِق `WU-026` نهائياً** |
| 3 | **الإذن بحذف موارد العيّنة** (`DEBT-14` السحابي) + بناء مُنشئ الحسابات بعد ①| ★ **يُغلِق `WU-000`** |

⚠️ **وحتى تُنشَر القواعد الجديدة، البيئتان تعملان بالقواعد القديمة** —
⛔ **أي أن `ADR-0016` و`ADR-0017` سريان في المستودع لا في السحابة.**
★ **وهذه أهم جملة في هذا التقرير.**

---

## 10. حالة `CLAUDE.md`

✅ **حُدِّث** عبر السكربت — **172 سطراً**: عدد الاختبارات و`DEBT-14`.

---

## 11. الخطوة التالية

1. ⏳ **أذن بالنشر** — أو نفّذه بنفسك، **وهو أعجل ما في القائمة** لأن الفارق
   بين المستودع والسحابة **فارقٌ أمني حقيقي**.
2. ⏳ **جلسة على التجريبية** لقياس ③-ب ⟵ **تُغلِق `WU-026`**.
3. ⏳ **إذن حذف موارد العيّنة** ⟵ ثم **مُنشئ الحسابات** ⟵ **يُغلِق `WU-000`**.
4. ⛔ **ولا دالة كاتبة تُقبَل بلا استيفاء `DEBT-21`** — **أحد عشر قيداً**.

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
