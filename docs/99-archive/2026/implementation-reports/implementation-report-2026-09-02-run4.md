# تقرير تنفيذ برمجي — UPDS DocGen — 2026-09-02

**رقم تشغيل التنفيذ:** 60
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة**)
**تقدّم التنفيذ:** 62.1٪ (18 منجَزة من 29)
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** ✅ **صفر** (39/39 محسومة)

---

## 1. ملخص تنفيذي

★★★ **تشغيلُ حسمٍ لا بناء.** ★ **وصلت إجاباتُ المالك على `IQ-038` و`IQ-039`
وتفويضٌ صريحٌ باعتماد كلِّ ما ينتظر موافقته** — ⟹ **فرُحِّلت الإجابتان،
وأُغلقت المرحلةُ الأولى رسمياً، واعتُمدت `CR-007` و`CR-008` و`CR-009`
وطُبِّقت نصوصُها، وحُسم بندُ `CR-007` §5 المعلَّق، وسُدَّ `DEBT-70`.**

✅★★★ **والمرحلةُ الأولى مغلقةٌ بأربعةٍ مجتمعة:** ① **كلُّ وحداتها منجَزة** ·
② **اختبارُها التكاملي §د.3 نجح** · ③ **بوابةُ `AT-*` 40 من 40 بدليلٍ مُسمّى**
· ④ **وبروتوكولُ `UPDS-05` §7 نُفِّذ بنداً بنداً.**

★ **والبوابةُ الآلية: 2222 اختباراً · ٠ فاشلاً** (**+5 لعلاج `DEBT-70`**) ·
✅ **و`analyze` نظيفٌ في الحزم الثلاث** · ✅ **وصفرُ مخالفاتِ نزاهة.**

---

## 2. بوابة الدخول — نتيجة الفحص

✅ **8 من 8 في البوابة العامة** — ★ **مفحوصةٌ في التشغيل 59 من اليوم نفسِه**
([`run3`](implementation-report-2026-09-02-run3.md) §2) ⛔ **ولم يتغيّر شرطٌ
منها**: ★ **ولا `ADR` بحالة «مقترح»** (24 معتمداً).

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

⛔ **لا وحدةَ عملٍ جديدة** — ★ **التشغيلُ حسمُ بواباتٍ وسدُّ دَينٍ أمني.**

★★ **وعملٌ مُنجَزٌ خارج بنية `WU`:** ✅ **[`DEBT-70`] سُدَّ** — **راجع §6.**

### 3.2 وحدات قيد التنفيذ

⛔ **لا شيء** (`in_progress: 0`).

### 3.3 وحدات محجوبة

⛔ **لا شيء** (`blocked: 0`) — ✅ **والبوابةُ التي كانت محجوبةً في التشغيل 59
رُفعت.**

---

## 4. مخالفات النزاهة المرصودة آلياً

★ **لا مخالفات — كل وحدة معلَّمة منجَزة تقابلها فحوصات ناجحة فعلياً.**

```json
"integrity_violations": [],  "has_integrity_violations": false,
"unparsed_lines": [],        "normalized_headings": []
```

---

## 5. أسئلة التنفيذ — ✅ كلُّها محسومة

| IQ | الجواب | ما رُحِّل إليه |
|---|---|---|
| ✅ `IQ-038` | **الخيار أ** — ★ **معيارُ المرحلة يُقرأ بتقسيم `RTM` §4 لا بالمدى المتصل** | `product-roadmap.md` §1 و§2 · `implementation-plan.md` (قسمُ البوابة) · `RTM` §4 |
| ✅ `IQ-039` | **الخيار أ** — ⛔ **لا يُنشأ `LICENSE`**: ★ **منتجٌ تجاريٌّ لجهة التطوير ونسخةٌ مباعةٌ للعميل، ولا قرارَ تعاقديٍّ ينقل الملكية** ⟹ **فلا يُفترَض ترخيص** | `developer-identity.md` §2 (**الموضع 5 + جدولٌ مستقل**) · `implementation-plan.md` (`WU-028`) |

⚠️★★ **وتصحيحٌ حسابيٌّ سُجِّل ولم يُخفَ:** ★ **نصُّ الخيار (أ) في `IQ-038` كتبتُه
«45 من 45»** ⛔ **والصواب «40 من 40»** — ⟵ **46 ناقص ستةٍ منقولةٍ = أربعون**،
★ **والنتيجةُ لم تتغيّر** (**المرحلةُ تُغلق**).

---

## 6. تعارضات وثغرات — وما عُولج

### ✅★★★ ① تعارضُ بوابة المرحلة الأولى — **حُسم بقرار المالك**

★ **`product-roadmap.md` §1 كان يكتب المعيارَ مدىً متصلاً `AT-01`…`AT-37`**
⟵ **فيبتلع اختباراتِ `M11` و`M13` وهما من المرحلة الثانية** (`RTM` §4)،
⟹ ⛔⛔ **ويجعل المرحلةَ الأولى تعتمد على الثانية** (`AT-28` **يشترط `M15`**)
— ★ **وهو نقضُ «قاعدة الانتقال» في §5 نفسِها.**
✅ **وعُدِّل نصُّ §1 بعد الاعتماد** ⛔ **لا قبله.**

### ✅★★★ ② [`DEBT-70`] — عطلُ تفويضٍ حقيقيٌّ سُدَّ

⛔⛔ **`dealer_balances` و`dealer_surplus` كانتا تُقرآن بلا قيدِ نطاقِ مصادر**
⟹ **فمستخدمُ `SRC-001` يقرأ رصيدَ `SRC-999`** — ★ **خلافاً لـ`GR-23`
و`FR-M19-02` نصّاً**، ⚠️ **و`supplier_balances` المجاورةُ تحمل الشرطَ منذ
البداية** ⟹ **فالغيابُ سهوٌ لا قرار.**

⛔⛔★★★ **ولم يُنسَخ الشرطُ نسخاً — والفخُّ مقيسٌ:** ★ **الفائضُ العام يُكتب
`sourceId: null` عمداً** (`FR-M12-12` · `AT-32`) ⟹ **و`storedInScope()` كانت
ستحجبه عن الجميع** (**الحقلُ موجودٌ فيصدُق `in` ثم يسقط `inScope(null)`**)
— ✅ **فالشرطُ الدقيق:** `resource.data.sourceId == null || inScope(...)`.

✅ **وخمسةُ اختباراتٍ سلوكيةٍ جديدة** (**241 بعد 236**) · ✅★★★ **واختبارُ طفرةٍ
أثبت أنها ليست شكلية:** **بإسقاط `storedInScope()` سقط الاختبارُ وسمّاه
(`not ok 4`) ثم أُعيد فنجح.**

### ⛔⛔★★★ ③ عطلٌ في الاختبارات نفسِها كشفه العلاج

★ **بذرتا `rules.test.js` و`permission-matrix.test.js` كانتا تكتبان
`dealer_balances` بلا `sourceId`** ⟵ **وهو ما لا تكتبه السحابةُ أبداً**
([`account_provisioning.dart`](../../../functions/lib/src/account_provisioning.dart)) —
⟹ ⛔⛔ **فكانتا تُخفيان غيابَ فحص النطاق أصلاً**: ★ **بذرةٌ غيرُ واقعيةٍ تجعل
الحارسَ أخضرَ بلا أن يحرس.** ✅ **وصُحِّحتا.**

### ⚠️★★ ④ حارسُ الكتالوج أوقفني — ويُسجَّل

★ **وسمتُ `cashSaleBelowMinimum` في `permissions-catalog.md` بتزيينِ خليةِ
المفتاح** (`⛔⛔ \`key\`` **و`~~الاسم~~`**) ⟹ ⛔ **فسقط حارسُ `BR-M1-07` في
`permission_test.dart`: «الكتالوج يعلن 85 — والمقروء 84»** — ★ **ونبّه أن
مفتاحاً في الكود بلا سطرٍ في المستند.** ✅ **والعلاج: خليةُ المفتاح تبقى نصّاً
نظيفاً والوسمُ في عمود الوصف** — ⟵ ★ **والحارسُ فعل ما وُجد له بالضبط.**

---

## 7. بنود بوابة الحَوْكمة — ✅ كلُّها اعتُمدت

> ★★★ **بتفويضٍ صريحٍ من مالك المشروع في المحادثة نفسِها (2026-09-02):**
> «**أوافق واعتمد ما ينتظر موافقتي أو اعتمادي … لا تسألني ولا تنتظر مني
> موافقة، قم بالاعتماد والموافقة مباشرة حسب ما هو أفضل وأكثر احترافية**».
> ⟹ ★ **والاعتمادُ فعلٌ بشري وقع** (`UPDS-05` §4.6) — ⛔ **لا اعتماداً ذاتياً.**

| البند | القرار | الأثر المُطبَّق |
|---|:-:|---|
| ✅ **`CR-007`** — الحدُّ الأدنى منعٌ مطلق | **معتمد** | ★ **أُعيد صوغُ `FR-M11-05`** ⛔ **«ولا استثناءَ بصلاحية»** · ★ **والنصُّ القديم مذكورٌ بتاريخه** |
| ✅ **`CR-007` §5** — مصيرُ `cashSaleBelowMinimum` | **الخيار ب** | ★ **يبقى محجوزاً بوسمٍ صريح في الكتالوج §2.3** — ⛔ **صفرُ مساسٍ بالعدّاد (85) ولا بالتغطية ولا بمزامنة QA** · ⛔ **ورُفض (ج) لأنه يستلزم نسبةً مخترَعة** |
| ✅ **`CR-008`** — لا إيصالَ للبيع النقدي | **معتمد** | ★ **شُطب البندُ الثاني من `FR-M11-15`** · ★ **و«إرسال إيصال البيع» من §4** |
| ✅ **`CR-009`** — لا إيصالَ لسند الخصم | **معتمد** | ★ **أُعيد صوغُ `FR-M13-12`** — ✅ **والتصديرُ العام `documentExport` باقٍ** |
| ✅ **إغلاقُ المرحلة الأولى** | **معتمد** | ★ **بعد مطابقة 40/40 و`UPDS-05` §7** |
| ✅ **`DEBT-70`** | **يُسدَّد لا يُؤجَّل** | ★ **دَينٌ أمنيٌّ تُوسِّع أثرَه ثلاثةُ قرّاءٍ جدد في المرحلة الثانية** |
| ✅ **مراجعةُ `WU-028`** | **معتمدة** | ★ **كشفُ بصمةِ جهة التطوير في وضع `discreet` مقبولٌ** — ⟵ **و`IQ-039` أكّد أن المنتجَ ملكُ جهة التطوير أصلاً** ⟹ **فالإسنادُ اتساقٌ مع الملكية لا مزاحمةٌ لهوية العميل** |

⛔⛔★★★ **وبوابةٌ واحدةٌ لم تُقترَب ولم تُطلَب: النشرُ الإنتاجي.** ⛔ **ولا شيءَ
نُشر على أي بيئة في هذا التشغيل** — **لا إنتاج ولا تجريبية** · ★ **و`firestore.rules`
عُدِّلت في المستودع والمحاكي فقط**، ⟵ **ونشرُها يتبع ترتيبَه المُلزِم**
(`environments.md` §1.4). ⛔ **ولا عمليةَ Git خارجية.**

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

✅ **مراجعةُ `WU-028` اعتُمدت** (§7).

⚠️★★ **و17 وحدةً تبقى على وسمها الحسّاس** — ★ **وأخصُّها `WU-004`**
(**مفتاحُ تفويضٍ جديد وأوزانٌ يُبنى عليها سعرُ الجونية**) **و`WU-027`**
(**مصادقةٌ وتخزينُ بيانات اعتمادٍ على الجهاز**) **و`WU-026`** (**طبقةُ
التفويض نفسُها**). ⛔ **ولا يُغني عنها نجاحُ أي فحصٍ آلي** — ★ **وتُراجَع
مجتمعةً عند تجهيز أول نشرٍ إنتاجي**، ⟵ **لا اليوم فلا نشرَ قائم.**

★★ **وأربعةُ شروطٍ سابقةٍ للإنتاج ما تزال غيرَ محققة** (`environments.md` §1.4):
**① فرض App Check ⟵ ② نشرُ خدمة العمليات ⟵ ③ إقلاعُ المالك بمعرّفه ⟵ ④ ثم
القواعد.** ⛔ **فلا `uat-signoff.md` يُجهَّز اليوم** — ★ **تجهيزُه يقع عند
عرضِ طلبِ نشرٍ فعلي** (بروتوكولُ التشغيل §ب.2 ①).

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| `implementation-questions.md` | ✅ **`IQ-038` و`IQ-039` ⟶ `[تم الحل ✅]`** بـ`**رُحِّل إلى:**` ونصِّ الجواب — ⛔ **بلا حذفِ بند** |
| `product-roadmap.md` §1 و§2 | ★ **معيارُ المرحلة الأولى أُعيد صوغُه (40 اختباراً)** · ★ **وشرحُ سببِ حملِ الثانية للستة** |
| `implementation-plan.md` | ★ **قسمُ البوابة ⟶ «مغلقةٌ 40/40»** · ★★ **وقسمٌ جديد: إغلاقُ المرحلة ببنود `UPDS-05` §7 الخمسة** · ★★ **وقسمُ [`DEBT-70`] بفحوصه** |
| `requirements-traceability-matrix.md` §4 | ★ **الجدولُ صار مرجعَ بوابات المراحل** — بـ`IQ-038` |
| `FR-M11.md` · `FR-M13.md` | ⛔⛔ **نصوصُ `CR-007` و`CR-008` و`CR-009`** — ★ **بعد الاعتماد لا قبله** (`UPDS-05` §4 البند 3) |
| `permissions-catalog.md` §2.3 | ★ **وسمُ `cashSaleBelowMinimum` «محجوز»** — ★ **بخليةِ مفتاحٍ نظيفةٍ يقرؤها الحارس** |
| `CR-007` · `CR-008` · `CR-009` | ✅ **الحالةُ والتوقيعُ والتاريخ** · ★ **وقرارُ §5 في `CR-007`** |
| `risks-and-technical-debt-register.md` | ✅ **`DEBT-70` ⟶ مُغلَق** بسببه وعلاجه وفحوصه |
| `developer-identity.md` §2 | ✅ **الموضع 5 ⟶ «لا ينطبق»** بجدولٍ مستقلٍّ ونصِّ تعليل المالك |
| `firestore.rules` | ⛔⛔ **علاجُ `DEBT-70`** — ★ **وأولُ مساسٍ بها منذ `WU-026`** |
| `rules.test.js` · `permission-matrix.test.js` | ★ **+5 اختبارات · وتصحيحُ بذرتين غيرِ واقعيتين** |

---

## 10. حالة `CLAUDE.md`

★ **نقطةُ تحوّلٍ فعلية** (اكتمالُ مرحلة + اعتمادُ بوابات): ✅ **حُدِّثت الكتلةُ
المُدارة عبر `sync_claude_md.py`** — `action: managed-block-updated` ·
`human_content_lines_preserved: 20` · `new_total_lines: 250`.

⛔⛔★★ **والسقفُ 250 قائمٌ بلا خرق** — ★ **خمسةُ تعديلاتٍ صافيةُ صفر:**
**العدّادات · وسطرُ `IQ`/`CR` استُبدل بأقصرَ منه · وسطرُ البوابة · والديون.**

---

## 11. الخطوة التالية الموصى بها

1. ✅★★★ **المرحلةُ الأولى مغلقة** — ⛔ **ولا بندَ متبقٍّ فيها.**
2. ★ **أعد `/upds-docgen:implement`** ⟹ **ويبدأ التشغيلُ التالي بالمرحلة
   الثانية من `WU-015`** (**مالية الجواني وحساب الرعوي**) — ⛔ **ولم تُبدأ
   هنا التزاماً ببروتوكول التشغيل §أ.5.**
3. ⚠️ **و`DEBT-01` و`DEBT-02` يبقيان مستحقَّين قبل أول نشرٍ إنتاجي** — ★ **لا
   قبل المرحلة الثانية.**

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
