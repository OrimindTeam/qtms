# تقرير تشغيل التنفيذ — 2026-08-30 (التشغيل 44)

**الطلب:** **عيّنةُ عزلٍ صغرى خارج المستودع** لعطل `DEBT-63` — ★ **وتشغيلُها
بـ`--profile` وفحصُ حالة العنصر والرسم والتخطيط والمُجدوِل لحظةَ ظهور الابن**،
⛔ **بلا أي تغييرٍ تخميني على `QTMS` حتى تُعطي التجربة دليلاً.**

---

## 1. ✅ النتيجة — الاحتمال (2): ⛔ لا تتكرّر بالعزل

⛔⛔★★★ **العطلُ لا يتكرّر خارج المشروع** ⟹ ★★ **فليس عطلَ إطارٍ عاماً**،
⟵ **بل يعتمد على تركيب شاشة `QTMS` بعينها.**

✅ **والمستودع لم يُمَسّ:** ⛅ **مقيسٌ بـ`git status`** — ★ **`daily_pricing_screen.dart`
و`filter_bar.dart` خارج الفرق تماماً**، ★ **و`inventory_providers.dart` يحمل
تغييرَ `DEBT-62` وحدَه** (`+10` لا غير) ⛔ **بلا بقايا تجربة.**

---

## 2. العيّنة — أصغرُ ما يُحاكي السلسلة المقيسة

★ **موضعُها خارج المستودع** (**مجلّد العمل المؤقّت**) ⛔ **لا داخل `QTMS`.**
★ **وتُحاكي سلسلةَ الرسم المقيسة حرفاً بحرف:**

```
Scaffold > Column(kids=2)[رأسٌ بديل, Expanded]
  > Column(kids=3)[شريطٌ جماعي, Expanded(ListView), شريطٌ ثابت]
    > DecoratedBox > Padding > Row(kids=2)[Expanded(Text), ابنٌ يظهر بعد Future]
```

★ **والابنُ يردّ `SizedBox.shrink()` حتى تكتمل مهلةُ 3 ثوانٍ ثم `OutlinedButton.icon`**
— **وهو التبديلُ المرصود نفسُه** · ★ **وبالعربية و`RTL` كالأصل.**

---

## 3. ⛅ ثلاثُ طبقاتٍ جُرِّبت — والزرُّ يظهر في كلٍّ منها

| الطبقة | ما أُضيف | النتيجة |
|---|---|---|
| ① | **`setState` مجرَّداً** | ✅ `hasSize=true` — **والزرُّ ظاهرٌ في اللقطة** |
| ② | **`Riverpod`** — `StreamProvider.family` **بـ`isAutoDispose`** كنظيره تماماً | ✅ **`146.3974609375x48.0`** |
| ③ | **مسارٌ مدفوع** (`Navigator.push`) — **كالشاشة الحقيقية** | ✅ **`146.3974609375x48.0`** |

⛅★★★ **والمقارنةُ حاسمةٌ لأن العرض متطابق إلى آخر رقم:**

| | **الصفّ** | **الزرّ** |
|---|---|---|
| ⛔ **`QTMS` (المعطوب)** | **`379.42857142857144 × 22.0`** | ⛔ **`hasSize=false`** |
| ✅ **العيّنة** | **`379.42857142857144 × 48.0`** | ✅ **`146.3974609375 × 48.0`** |

⟹ ★★ **فسياقُ القياس واحدٌ تماماً** (**نفسُ الجهاز ونفسُ العرض المتاح**)،
⛔ **والفارقُ الوحيد أن الزرَّ في `QTMS` يُسهم بصفرٍ لأنه لم يُخطَّط.**

---

## 4. ⚠️ ملاحظةٌ منهجية على وضع `--profile`

⚠️ **`debugNeedsLayout` محروسٌ بـ`assert`** ⟵ **فلا يُقرأ في `--profile`**،
★ **والاعتمادُ على `hasSize` والمقاسات العددية بدلاً منه** — ⛅ **وهي كافيةٌ
للحكم.** ★ **و`flutter run --profile` بُني ونُشر فعلاً**، ⚠️ **والقراءةُ تمّت من
`logcat` بعد التثبيت** ⟵ **لأن ربطَ `adb` انقطع بمهلة التشغيل** ⛔ **والثنائيةُ
هي هي (`app-profile.apk`).**

---

## 5. ★ الخطوة التالية القائمة على الدليل

⛔⛔★★★ **الاتجاه انعكس:** ★ **لا يُبحَث عن عطلٍ في الإطار بعد اليوم** —
⟵ ★ **بل يُنصَّف من جهة `QTMS`:** **تُجرَّد الشاشة الحقيقية تدريجياً حتى يظهر
الزرُّ** ⟵ **فيَبين الفارقُ الأصغر بينها وبين العيّنة.**

★ **وأولُ المرشَّحين — بترتيب الاحتمال:**
① **`AsyncStateView`** — **انتقالُ الهيكل المتحرِّك (`SkeletonList`) إلى البيانات**
(⚠️ **وحركةٌ متكرِّرة رُصدت سابقاً وأوقفت `pumpAndSettle`**) ·
② **`_PricingForm`** — **`ConsumerStatefulWidget` بـ`ValueKey`** ·
③ **الثيمُ والخطوطُ المحمَّلة من الأصول** (`TypeScale` · Tajawal · IBM Plex).

⚠️★★ **وهذا يقتضي تعديلاً تشخيصياً مؤقّتاً على `QTMS` ثم ردَّه** — ⛔ **ولم
يُبدأ به**: ★ **فالمالك قيّد هذا التشغيل بالعيّنة وحدها** (**«لا تغييراتٍ
تخمينية حتى تُعطي التجربة دليلاً»**) ⟹ ★ **والدليلُ حصل، والإذنُ يُطلَب.**

---

## 6. ✅ القيود المفروضة — كلُّها محفوظة

✅ **لم يُعدَّل كودُ `QTMS` الإنتاجي** · ✅ **ولا حلٌّ التفافيٌّ جديد** ·
✅ **و`DEBT-62` كما هو** (⛅ **ومُتحقَّقٌ حيّاً في التشغيل 43**) ·
✅ **ولا نشرَ إنتاجي ولا مساسَ بصلاحيات IAM** ·
✅ **والمستودع على حالته النظيفة المردودة** ·
⛔ **و`WU-005` تبقى `[يحتاج إعادة تحقق]`** — **فالميزةُ لم تُستعَد.**
