# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-24 (تشغيل 5)

**رقم تشغيل التنفيذ:** 17
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة**)
**تقدّم التنفيذ:** 0٪ (0 منجَزة من 27)
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** ★ **0 — ولا بند واحد**
**المطلوب:** تنفيذ `IQ-010`

---

## 1. ملخص تنفيذي

✅ ★ **نُفِّذ `IQ-010` (الخيار أ) بالكامل** — عُزلت حقول الإيداع البنكي في
**`receipts/{id}/deposit/current`** بشرط قراءة **فعلي** `perm('receiptDepositView')`،
عبر **`ADR-0017`** جديد يشير إلى `ADR-0011` ⛔ **بلا مساس به**.

★★ **والنتيجة الأهم: بوابة التغطية نجحت لأول مرة منذ بدء `WU-026`** —
`PASS ③` · `PASS ⑤` · ★ **صفر فجوة**، ✅ **و`npm test` يخرج بالرمز 0**.

★ **343 اختباراً ناجحاً · ٠ فاشلاً** (188 حماية · 9 تطبيق · 66 نطاق · 80 سحابة).

⛔ **ولم تُعلَن `WU-026` منجَزة:** بقي **معيار القبول 5 وحده** — الأرقام
الثلاثة في `ADR-0016` §«كلفة القراءة» **لم تُقَس على قاعدة حقيقية**.

---

## 2. بوابة الدخول — نتيجة الفحص

✅ **البوابة العامة (٨ شروط) متحققة** · ✅ **وبوابة `WU-026` متحققة.**
★ **والـ17 ADR كلها «مقبول»** ولا واحد بحالة «مقترح».

---

## 3. ما نُفِّذ — بنداً بنداً من إجابتك

| ما نصّت عليه الإجابة | الحالة |
|---|---|
| حقول الإيداع تُنقَل إلى `receipts/{id}/deposit/current` | ✅ **نُفِّذ** — `data-dictionary.md` و`erd.md` |
| ★ يُفرَض الوصول بـ`perm('receiptDepositView')` **في `firestore.rules`** | ✅ **نُفِّذ ومُثبَت بالطفرة** |
| ⛔ **لا تُعدَّل دلالته إلى «صلاحية واجهة»** | ✅ **بقي شرط قراءة فعلياً** — والكتالوج §2.5 يوثّق موضع إنفاذه |
| بقية بيانات سند القبض **وفق قواعدها الحالية** | ✅ **لم تُمَسّ** — واختبارٌ مستقل يُثبت أن **الأب يبقى مقروءاً** |
| ★ **ADR جديد يشير إلى `ADR-0011`** ⛔ **ولا يُعدَّل `ADR-0011`** | ✅ **`ADR-0017`** — و`ADR-0011` **لم يُمَسّ بحرف** |
| تُحدَّث `data-dictionary.md` و`erd.md` | ✅ **نُفِّذ** — كيان `RECEIPT_DEPOSIT` جديد |
| تُحدَّث اختبارات القواعد وبوابة التغطية | ✅ **نُفِّذ** — **5 اختبارات جديدة** · الصفّ صار **قراءة** · **`PENDING_DECISION` أُفرغت** |
| يُحدَّث منطق الكتابة ليكتب الإيداع في المستند الفرعي | ⏳ ★ **لا دالة كاتبة بعد** — سُجِّل **قيدَ قبولٍ مُلزِماً** في `DEBT-21` |

> ⚠️ **البند الأخير يستحق التصريح:** سندات القبض تُبنى في `WU-007`، **ولا
> كود كتابة قائم اليوم** لأن `ADR-0013` أغلق الكتابة المباشرة. ⛔ **فلم
> أخترع دالة لأُطبّق عليها القيد**، بل **سجّلته شرطاً لا يُقبَل بدونه**.

---

## 4. الفحوصات التي شُغِّلت فعلاً — بنتائجها الحقيقية

| الفحص | النتيجة |
|---|---|
| `npm run test:rules` | ✅ **188 · ٠ فاشلاً** (183 قبل — **5 جديدة**) |
| ✅ ★★ `npm test` (**البوابتان معاً**) | ✅ **نجح — `exit 0`** · `PASS ③` · `PASS ⑤` · ★ **صفر فجوة** |
| ★★ **طفرة على شرط قراءة الإيداع** | ✅ بجعله `isSignedIn()` **سقطت 3 اختبارات**، ثم أُعيد فنجحت كلها |
| `cd functions && dart analyze` · `dart test` | ✅ **`No issues found!`** · **80 · ٠ فاشلاً** |
| `cd packages/qtms_domain && …` | ✅ **`No issues found!`** · **66 · ٠ فاشلاً** |
| `flutter analyze` · `flutter test` | ✅ **`No issues found!`** · **9 · ٠ فاشلاً** |

★ **المجموع: 343 اختباراً ناجحاً · ٠ فاشلاً.**

★ **واختبارٌ منها يستحق الإبراز** — «**والعزل مُستهدَف لا شامل**»: يُثبت أن
**السند الأب ما يزال مقروءاً** لمن لا يملك عرض الإيداع. ⛔ **وبدونه كان
اختبار المنع سينجح حتى لو أغلقنا السند كله بالخطأ.**

---

## 5. مخالفات النزاهة المرصودة آلياً

✅ **لا مخالفات** — `integrity_violations: []` · `unparsed_lines: []`.
⛔ **ولم تُرفَع `WU-026` إلى `[منجز ✅]`** رغم نجاح بوابتيها — **لأن معيار
القبول 5 لم يُستوفَ**، وإعلانها منجَزة كان سيكون ادّعاءً.

---

## 6. أسئلة التنفيذ

★ **لا بند مفتوح — والعشرة كلها محسومة.**

| IQ | الحالة |
|---|---|
| `IQ-001` … `IQ-009` | ✅ **حُلَّت** |
| ★ **`IQ-010`** | ✅ **حُلَّ ونُفِّذ في هذا التشغيل** ⟵ **`ADR-0017`** |

---

## 7. بنود بوابة الحَوْكمة

| البند | الحالة |
|---|---|
| ★ **`ADR-0017`** | ✅ **مُنشأ بحالة «مقبول»** — ★ **بناءً على اختيارك الصريح للخيار أ في `IQ-010` وأمرك بتنفيذه**. ⚠️ **وإن أردته «مقترح» ينتظر اعتماداً منفصلاً، قُل ذلك ويُعدَّل** |
| ⛔ **تعديل `ADR-0011`** | ✅ **لم يحدث** — وهو **شرط صريح في إجابتك**، ومُوثَّق في `ADR-0017` |
| **تعديل `srs.md`** | ⛔ **لم يحدث** |

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

★ **`WU-026` أخطر زيادة في المشروع** — والتحقق الآلي **شرط ضروري لا كافٍ**:

| # | ما يحتاج عيناً بشرية |
|:-:|---|
| 1 | ★★ **`DEBT-21`** — قيود الكتابة (ومنها **قيد الإيداع الجديد**) **غير مفروضة في أي مكان** حتى تُبنى الدوال الكاتبة |
| 2 | ★★ **كلفة القراءة لم تُقَس** — معيار القبول 5 معلَّق |
| 3 | ★ **تبعية `ADR-0016`** — غياب `users/{uid}` = فقدُ كل الصلاحيات فوراً |
| 4 | ★★ **`bootstrapOwnerPermissions`** · **`QTMS_OWNER_UID`** · **`DEBT-19`** |

> ⛔ **وثلاثة بنود لا تُنسى:** **App Check بفرض مُطفأ** · **حساب المالك بلا
> صلاحيات** حتى يُشغَّل الرَنبوك · **`DEBT-14`**.

---

## 9. مستندات UPDS التي حُدِّثت

| المستند | سبب التحديث |
|---|---|
| ★ `adr/ADR-0017-receipt-deposit-read-isolation.md` **(جديد)** | **العزل الثالث** — `IQ-010` الخيار أ |
| `06-database/data-dictionary.md` | حقول الإيداع **نُقلت** لمستندها الفرعي |
| `06-database/erd.md` | ★ **كيان `RECEIPT_DEPOSIT` جديد** |
| `09-security/permissions-catalog.md` §2.5 | **موضع إنفاذ `receiptDepositView`** |
| `risks-and-technical-debt-register.md` | **`DEBT-21`** — قيد كتابة الإيداع |
| `implementation-plan.md` · `implementation-questions.md` | `WU-026` · **إغلاق `IQ-010`** |

---

## 10. حالة `CLAUDE.md`

✅ **حُدِّث** عبر `sync_claude_md.py` حصراً — `managed-block-updated` ·
**171 سطراً** (تحت 200). ★ **نقطة تحوّل** (`claude-md-management.md` §2 البند
6): **ADR جديد مقبول** — وأُضيف `ADR-0017` وصار النصّ «كل `ADR-0001`…`ADR-0017`».

---

## 11. الخطوة التالية الموصى بها

★ **لا قرار ينتظرك — والمتبقي عملٌ تقني بحت:**

1. ★★ **قياس معيار القبول 5** — الأرقام الثلاثة في `ADR-0016` §«كلفة
   القراءة» على **قاعدة حقيقية**. ⚠️ **ويحتاج نشراً سحابياً**، ⛔ **ولم
   أُجرِه من تلقائي**. ⟵ **وبه وحده تُغلَق `WU-026`.**
2. **ثم `WU-000`:** مُنشئ الحسابات (`DEBT-16`) ← **`DEBT-14`** ← وتُغلَق.
3. **ثم `WU-001`** — أول زيادة تشغيلية بشاشات.
4. ⛔ **ولا دالة كاتبة تُقبَل بلا استيفاء `DEBT-21` كاملاً** — وقد صار
   **أحد عشر قيداً** بعد قيد الإيداع.

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
