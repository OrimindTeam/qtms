# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-27 (التشغيل الثاني)

> ★ **سجل تراكمي** — تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد.
> ⛔ **لا يُحذَف ولا يُستبدَل أيٌّ منها.**

**رقم تشغيل التنفيذ:** 2026-08-27 · التشغيل 2
**المنهجية المعتمدة:** تزايدية (Incremental) — وحدة العمل: **زيادة** (شريحة رأسية كاملة)
**تقدّم التنفيذ:** **29.6٪ (8 منجَزة من 27)** — ⟵ ★ **`WU-008` أُغلقت**
**حالة التوثيق وقت هذا التشغيل:** **100٪** (٠ بند توثيق مفتوح — `OQ` · `RD` · `RF`)
**أسئلة تنفيذ مفتوحة:** ★ **واحد** (`IQ-026`) — **26 بنداً · 25 محسوماً**

---

## 1. ملخص تنفيذي

★★ **بُنيت `WU-008` كاملةً من طرف إلى طرف** — **الشاشة المركزية لسجل التدقيق
والسجل السياقي 🕘 في ثماني شاشات قائمة** — ✅ **واختُبرت على المحاكي
`Pixel_6_API_36` ضدّ التجريبية الحيّة بثمانية سيناريوهات، والكلُّ نجح.**

⛔⛔★★★ **وحُجبت `WU-006` قبل كتابة سطرٍ واحد من كودها** (`IQ-026`): **قيمة
سطر التوزيع الوزني تُنتج كسراً، ولا قاعدةَ تحكمه** — ★ **والناتج يُقيَّد
ديناً على مقوت** ⛔ **فلم يُخمَّن.**

★★ **واكتشافٌ قِيس حيّاً أعاد تشكيل `WU-008` قبل بنائها:** **سردُ `audit_log`
بلا قيدٍ على `sourceId` يُرفَض من قاعدة الحماية كاملاً — ولو بنطاقٍ شامل.**
⟵ ✅ **وعُولج بتقييد المصدر في النوع نفسه وثلاثةِ فهارس** ⛔ **بلا أي مساسٍ
بقاعدة الحماية.**

★ **والمقاس بعد التشغيل:** **1146 اختباراً ناجحاً · ٠ فاشلاً** (1064 قبله)
· ✅ **و`analyze` نظيف في الحزم الثلاث.**

---

## 2. بوابة الدخول — نتيجة الفحص

### 2.1 البوابة العامة (٨ شروط — `implementation-playbook.md` §1.1)

| # | الشرط | متحقق؟ | المسار المفحوص |
|:-:|---|:-:|---|
| 1 | المنهجية مُصرَّح بها | ✅ | `docs/21-project-management/development-methodology.md` |
| 2 | العمارة والمكدس | ✅ | `docs/03-architecture/architecture-overview.md` · `technology-stack.md` |
| 3 | قواعد الكتابة ومعالجة الأخطاء | ✅ | `docs/04-design/{coding-standards,naming-conventions,error-handling-strategy}.md` |
| 4 | تعريف «منجَز» والتفرّع والالتزام والإصدار | ✅ | `docs/07-development/*` · `docs/12-deployment-and-release/versioning-scheme.md` |
| 5 | استراتيجية الاختبار | ✅ | `docs/08-testing-and-qa/test-strategy.md` |
| 6 | سياسة الأسرار | ✅ | `docs/09-security/secrets-management-policy.md` |
| 7 | `srs.md` بمتطلبات مرقّمة | ✅ | `docs/02-requirements/srs.md` |
| 8 | ⛔ **لا ADR بحالة «مقترح» يحكم ما يُنفَّذ** | ✅ | **18 ADR — كلُّها «مقبول»** |

### 2.2 بوابة الوحدة (`WU-008`) — ٥ شروط

| # | الشرط | متحقق؟ |
|:-:|---|:-:|
| 1 | عقد الوحدة | ✅ `docs/04-design/module-design/audit-log-design.md` |
| 2 | المتطلب بمعايير قبول | ✅ `FR-M18-01`…`16` |
| 3 | عقود الواجهات (إن انطبقت) | ✅ `docs/05-api/{api-overview,authentication-and-authorization}.md` |
| 4 | التخزين | ✅ `docs/06-database/{erd,data-dictionary}.md` · `schema/audit-log.md` |
| 5 | المصادقة والصلاحيات | ✅ `docs/09-security/authentication-policy.md` · `permissions-catalog.md` |

✅ **البوابتان اجتازتا — والتنفيذ بدأ بحق.**

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

| WU | العنوان | أبرز الملفات المنتَجة | الفحوصات ونتيجتها **الفعلية** |
|---|---|---|---|
| **`WU-008`** | زيادة: عرض سجل التدقيق من طرف إلى طرف | **النطاق:** `audit_log_repository.dart` · `audit_entity_types.dart` · `calendar_day.dart` (`tryParseCompact`) — **التطبيق:** `audit_log_screen.dart` · `audit_trail_view.dart` · `audit_log_providers.dart` · `firestore_audit_log_directory.dart` · `audit_labels.dart` · `date_labels.dart` — **الفهارس:** `firestore.indexes.json` (33 ⟵ 36) | ✅ `npm test` **236 · ٠ فاشلاً** (+9) · ✅ `dart test` (نطاق) **311 · ٠** (+31) · ✅ `dart test` (سحابة) **333 · ٠** · ✅ `flutter analyze` **نظيف** · ✅ `flutter test` **266 · ٠** (+42) · ✅ `flutter build apk` **نجح** · ⛅✅ نشر الفهارس + **تحقق مستقل** (36 حيّاً · 7 لـ`audit_log`) · ✅★★★ **اختبار المحاكي: ثمانية سيناريوهات — الكلُّ نجح** · ✅ `adb logcat` **نظيف** |

★ **التفصيل الكامل — بما فيه جدول سيناريوهات المحاكي وجدول الأعطال الثلاثة —
في بند `WU-008` من [`implementation-plan.md`](implementation-plan.md).**

### 3.2 وحدات قيد التنفيذ

⛔ **لا وحدة `[قيد التنفيذ]`** — ★ **ولا وحدةٌ تُركت بحالةٍ غامضة.**

### 3.3 وحدات محجوبة (ولماذا)

| WU | العنوان | محجوب بسبب | ما الذي يلزم لرفع الحجب |
|---|---|---|---|
| **`WU-006`** | زيادة: التوزيع والضمار من طرف إلى طرف | **`IQ-026`** | ★ **جوابُ المالك على قاعدة تقريب قيمة السطر الوزني** — ⟵ **ثم `ADR` جديد إن اختير (أ) أو (د)** |

⚠️★★ **وكلُّ ما بقي من المرحلة الأولى معلَّقٌ على هذا الحجب** — راجع §11.

---

## 4. مخالفات النزاهة المرصودة آلياً

✅ **لا مخالفات** — `track_implementation.py` يُرجِع `integrity_violations: []`
و`unparsed_lines: []`.

★ **وكل وحدة معلَّمة `[منجز ✅]` تقابلها فحوصات شُغِّلت فعلياً ونتائجُها
مسجَّلة بأرقامها** ⛔ **لا بعبارةٍ عامة.**

⚠️★ **وصُحِّح في هذا التشغيل بندٌ شكلي قائم:** `IQ-025` كان بحالة `[مغلق]`
**وهي ليست من الحالات الثلاث المسموحة** (`[مفتوح]` · `[تم الحل ✅]` ·
`[ملغى]`) — ⟵ **فكان السكربت يعدّه سطراً «غير قابل للتحليل» ويسقط من كل
إحصاء.** ✅ **صار `[تم الحل ✅]`** ⟵ **فصار العدّ 25 محسوماً بدل 24.**

---

## 5. أسئلة التنفيذ المُصعَّدة (لم تُخمَّن في الكود)

| IQ | السؤال | الوحدة المتعطّلة | الملف/الدالة | لماذا لم يكن قابلاً للاستنتاج |
|---|---|---|---|---|
| **`IQ-026`** | ⛔⛔ **قيمة سطر التوزيع الوزني تُنتج كسراً، والتقريب «موضعان لا ثالث لهما»** | `WU-006` | `…/sales_receivables/domain/distribution.dart` → `lineTotal` · `debtValue` (**لم يُكتَب — وهذا سببُ توقّفه**) | ★ **تعارضٌ صريح بين مستندين معتمدين:** `design-overview.md` §2.3 يجعل **قيمة السطر = الكمية × السعر**، و`FR-M10-15` يجعل **التوزيع بالكيلوجرام مدعوماً بثلاث خانات** — ⟵ **فالحاصل كسريّ**؛ ⛔ **بينما `ADR-0015` القاعدة 4 تحصر التقريب في موضعين** و§2.11 يقول **«موضعان اثنان لا ثالث لهما»**. ★ **وثلاثة سلوكيات معقولة بالتساوي ولا مُرجِّح** ⟵ **والناتج يُقيَّد ديناً على مقوت** |

★★ **والجواب يحسم معه `WU-012` (`M11`) و`WU-014` (`M22`)** — ⟵ **فنفس
الضرب يقع فيهما لأنواعٍ وزنية** ⛔ **ولا يُفتَح البند ثلاث مرات.**

راجع الخيارات الأربعة كاملةً في:
[`implementation-questions.md`](implementation-questions.md) → `IQ-026`.

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

### 6.1 ⛔⛔★★★ فهارس `audit-log-design.md` §7 لا تكفي التطبيق — **قياسٌ حيّ**

| البند | التفصيل |
|---|---|
| **ماذا يقول المستند** | §7 يذكر **أربعة فهارس**، أوّلُها `entityType ↑ · entityId ↑ · occurredAt ↓` **«يخدم السجل السياقي»** |
| **ماذا فرض التنفيذ فعلياً** | ★★ **قاعدة `audit_log` تشترط `storedInScope()`** — شرطٌ على `resource.data.sourceId`. ★ **وقُيس على المحاكي أن `getDocs` بلا قيدٍ على `sourceId` يُرفَض كاملاً — ولو لصاحب النطاق الشامل** (`false for 'list'`)، بينما `getDoc` لمستندٍ واحد يمرّ ⟵ **فشرطُ `resource.data` يُقيَّم على قيود الاستعلام لا على كل مستند.** ⛔ **فاستعلامُ السجل السياقي كان سيُرفَض قبل أن يبلغ الفهرس أصلاً** |
| **كيف عُولج** | ✅ **تسوية الحالة (ب)** — `state-detection-and-reconciliation.md`: **الكود صحيح والمستند ناقص**، ★ **وهو `module-design` غير محكومٍ بحَوْكمة** ⟵ **فحُدِّث** (playbook §6 الصف الثالث). ① **`sourceId` صار حقلاً مطلوباً في `AuditLogFilter` و`AuditEntityRef`** ⟵ **فاستعلامٌ يُرفَض حيّاً يستحيل بناؤه** · ② **ثلاثة فهارس ببادئته** · ③ **والأربعة الأصلية باقية** للقراءة الإدارية ⛔ **ولم تُحذَف** |
| ⛔⛔ **وما لم يُمَسّ** | **`firestore.rules` لم تُمَسّ بحرف ولم يُخفَّف شرطُها** — ★ **وهو المطلوب:** ⟵ **الحلّ كان في المُستعلِم لا في الحارس** |

★★★ **وهو حرفياً درس `IQ-024`** الذي ترك منتقيَ المصدر فارغاً في كل شاشة
حتى 2026-08-26 — ✅ **وسُجِّل الآن قاعدةً في `CLAUDE.md` وسطراً في
`troubleshooting-guide.md` §3** ⟵ **فلا يقع ثالثةً.**

### 6.2 ★ ثوابت `…EntityType` كانت في `functions/` وحدها — **ومصدرُ حقيقةٍ ثانٍ كان لازماً**

★ **القيمة تُكتب في السحابة وتُقرأ في التطبيق** (`FR-M18-10`) — ⟵ **ونسخةٌ
ثانية في التطبيق تخالف `coding-standards.md` §2.2**، ⛔ **وأثرُ انزلاقها
صامتٌ تماماً: سجلٌ سياقي فارغ أبداً لا خطأٌ ظاهر.**
✅ **نُقلت إلى `packages/qtms_domain/…/audit_entity_types.dart`** (`ADR-0012`)
⟵ ✅ **و333 اختبار سحابة بقيت كما هي** — ★ **وهو الدليل على أن النقل بلا
أثر سلوكي.**

### 6.3 ⚠️ مدى اختبارات القبول في بند `WU-006` كان خاطئاً في الخطة

**الخطة كانت تقول `AT-26` … `AT-32`** — ⛔ **بينما `requirements-traceability
-matrix.md` يُسنِد `AT-26`…`AT-28` إلى `M11` في المرحلة الثانية**، ★ **ومدى
`M10` هو `AT-20`…`AT-25`** (وهو ما يذكره `UC-003` و`FR-M10` و
`distribution-design.md` §8 معاً). ✅ **صُحِّح في الخطة وحدها** — ★ **وهي
سجلُّ حالةٍ تكتبه الأداة** ⛔ **ولم يُمَسّ مستندٌ محكومٌ بحَوْكمة.**

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

| البند | الحالة |
|---|---|
| ★★ **`IQ-026` — قاعدة تقريب قيمة السطر الوزني** | ⛔ **بيد المالك حصراً** (الفئة 2). ★ **وإن اختير الخيار (أ) أو (د) فيلزمه `ADR` جديد بحالة «مقترح»** يشير إلى `ADR-0015` ويُعدِّل إحصاء مواضع التقريب — ⛔ **ولا يُعدَّل `ADR-0015` نفسه** (UPDS-05 §4.1) |
| ⛔⛔ **النشر الإنتاجي** | **لم يُطلَب ولم يُجهَّز في هذا التشغيل** — ★ **ولا شيء نُشر على الإنتاج** |

⛔ **ولم يُعتمَد أيُّ بندٍ ذاتياً** (UPDS-05 §2 و§4.6).

★ **وقرارات الفئة 3 التي اتُّخذت ذاتياً — تُذكَر صراحةً** (بروتوكول التشغيل §ب):

| # | القرار | لماذا |
|:-:|---|---|
| 1 | ⛅ **نشر الفهارس على التجريبية** | **فئة 3** — ★ **بيئة تجريبية بعد تحقّقٍ مستقل** · ⛔ **ولا رَنبوك مرتبط** |
| 2 | **حدّ الصفحة 50 قيداً** (`auditLogPageSize`) | ★ **المستند يفرض وجود حدٍّ ولا يذكر رقمه** (§8) |
| 3 | **عرضُ الوقت بتوقيت UTC** | ★ **يومُ النظام كلُّه UTC** ⟵ **ووقتٌ محلّي كان يُظهر قيداً بتاريخٍ يخالف `stockDate` الخاص به** |
| 4 | **الحقل/القيمة المجهولة تُعرَض خاماً لا تُخفى** | ⛔ **إخفاءُ تغييرٍ وقع يُسقِط مبرر `ADR-0004`** (`RISK-05`) |
| 5 | **تصحيح صيغة حالة `IQ-025`** إلى `[تم الحل ✅]` | ★ **مواءمةٌ شكلية مع الحالات الثلاث المسموحة** ⛔ **بلا تغيير معنى** |

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

⛔⛔★★★ **`WU-008` — سجل التدقيق: «الحافظ الوحيد للتاريخ» بعد `ADR-0004`.**

★ **والتحقق الآلي شرطٌ ضروري لا كافٍ** (`implementation-playbook.md` §7) —
⟵ **وما يجب أن تراه عينٌ بشرية قبل أي نشر إنتاجي:**

1. ★★ **أن الزيادة قراءةٌ محضة فعلاً** — ⛔ **لا دالة كتابة في العقد ولا في
   الدليل ولا في المزوّدات** · ✅ **ومُثبَتٌ باختبارٍ يقرأ نصّ العقد نفسه.**
2. ★★ **أن `firestore.rules` لم تُمَسّ** — ✅ **مقيسٌ: الملف بلا تغيير.**
3. ★ **أن `sourceId` مُقيَّدٌ في كل استعلام** — ⟵ **فلا يتسرّب قيدُ مصدرٍ
   خارج النطاق.** ✅ **ومُثبَتٌ بأربعة اختبارات على محاكي القواعد.**
4. ⚠️ **أن عرض الحقول الخام مقبولٌ للمالك** — ★ **قرارٌ مُعلَن في §7 البند 4**،
   ⟵ **وهو ما يراه المدقّق فعلاً في الشاشة.**

⚠️ **والوحدات الحسّاسة السابقة تبقى كما هي** (`WU-000` · `WU-002`…`WU-005`)
— ★ **وأخصُّها مراجعةُ `WU-004` الأمنية** (`sackCreate` + أوزانٌ يُبنى عليها
سعرُ الجونية) ⛔ **لم تُمَسّ ولم تُغنِ عنها هذه الزيادة.**

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| `docs/04-design/module-design/audit-log-design.md` | ★ **الحالة «مخطَّط» ⟵ «منفَّذ»** · ★★ **و§7 الفهارس أُعيد بناؤه بالقياس الحيّ** (UPDS-03 §4) |
| `docs/06-database/schema/audit-log.md` | **فهارس المجموعة** — ★ **تمييزُ ما يستعمله التطبيق عمّا هو إداري** |
| `docs/06-database/indexing-strategy.md` | **ثلاثة فهارس جديدة ببادئة `sourceId`** + **سببُها المقيس** |
| `docs/02-requirements/requirements-traceability-matrix.md` | ★ **عمود «الكود» لـ`FR-M18`** · **الحالة `🔷 مُصمَّم` ⟵ `🟩 منفَّذ`** · ⛔ **وما لم يُنفَّذ منه مذكورٌ صراحةً** |
| `docs/07-development/troubleshooting-guide.md` | ★★ **سطرٌ لفخّ «السرد يُرفَض بينما المستند يمرّ»** — ⟵ **رُصد مرتين** |
| `docs/00-governance/upds-workflow/implementation-plan.md` | **`WU-008` منجَزة بفحوصها** · **`WU-006` محجوبة** · **تصحيح مدى `AT` فيها** |
| `docs/00-governance/upds-workflow/implementation-questions.md` | **`IQ-026` جديد** · **تصحيح صيغة حالة `IQ-025`** |

⛔ **ولا `openapi.yaml` ولا `erd.md` ولا `migrations-log.md` تغيّرت** —
★ **الزيادة لم تُضِف Endpoint ولا مجموعةً ولا حقلاً**: ⟵ **`audit_log`
قائمةٌ منذ `WU-000`.**

---

## 10. حالة `CLAUDE.md`

✅ **حُدِّث** — ★ **نقطة تحوّل فعلية:** **اكتمال وحدة عمل** (`claude-md
-management.md` §2 البند 5) **مع تغيّر الوحدة الجارية وفتحِ بندٍ حاجب.**

| البند | القيمة |
|---|---|
| `action` | `managed-block-updated` |
| `human_content_lines_preserved` | **16** — ✅ **وقسم «بروتوكول تشغيل إضافي» بقي كما هو حرفياً** |
| `new_total_lines` | **226** |
| `exceeds_recommended_size` | ⚠️ **نعم** (الحدّ الإرشادي 200) |

⚠️★ **واستثناء `AM-005` حُدِّث بصدق:** **226 بدل 212** — ★ **بعد حذفِ 13 سطراً
من تفاصيل `WU-004` المنقولة إلى الخطة والتقرير**، ⟵ **فالنموّ الصافي 14 سطراً
مقابل قاعدة حَوْكمةٍ جديدة وحالةِ حجبٍ يجب أن تُقرأ في كل جلسة.**
⛔ **ولم يُطبَّق صامتاً.**

---

## 11. الخطوة التالية الموصى بها

### ⛔⛔★★★ أولاً وقبل كل شيء — **أجب على `IQ-026`**

★ **افتح [`implementation-questions.md`](implementation-questions.md) →
`IQ-026`، واكتب حرف الخيار في حقل «إجابتك»** ثم أعد تشغيل
`/upds-docgen:implement`.

### ⚠️★★ ولماذا توقّف التشغيل هنا — بصدق

⛔ **كلُّ وحدةٍ متبقية في المرحلة الأولى معلَّقةٌ على `WU-006`:**

| WU | لماذا لا تبدأ الآن |
|---|---|
| `WU-007` (المقبوضات) | ★ **تقبض على الضمار** — ⛔ **والضمار يُنشئه `WU-006`** |
| `WU-009` (المركز المعلّق) | ★ **نطاقُه المكتوب يلاحق `M7` و`M9` و`M10` و`M22`** — ⛔ **و`M10` محجوب و`M22` في المرحلة الثانية** |
| `WU-010` (الإرسال والتصدير) | ★ **قوالبُه الأربعة كلُّها توزيع/قبض/خصم** (`FR-M20-07`) — ⛔ **ولا واحد منها قائم** |
| `WU-011` (تقارير المرحلة) | ★ **تُجمِّع ما لم يُبنَ بعد** |

⟵ ★ **وهذا هو شرط الإنهاء في بروتوكول التشغيل §أ.3 حرفياً:** «**ولا وحدة
متبقية `[مخطط]` قابلة للبدء فعلياً بلا تبعية معلَّقة**».

⚠️★ **وخيارٌ يملكه المالك وحده — يُعرَض ولا يُنفَّذ:** ★ **يمكن بناء `WU-009`
بنطاقٍ مُصغَّر** يلاحق **`M7` و`M9` وحدهما** (وكلاهما قائمٌ ويُنتج بنوداً
معلَّقة فعلاً اليوم)، **على أن تُضاف `M10` و`M22` مع زيادتيهما** — ★ **بنفس
نمط `SourceDocumentType`** («القائمة تُستكمَل في زيادة كل مستند»).
⛔ **ولم يُنفَّذ ذاتياً** — ★ **تصغيرُ نطاق زيادةٍ مخطَّطة قرارُ المالك.**

### ★ وتذكيرٌ لا يسقط

⚠️ **مراجعةُ `WU-008` و`WU-004` البشرية قبل أي نشر إنتاجي** (§8) —
⛔⛔ **ولا يُنشَر شيء على الإنتاج بلا الكلمة الحرفية من المالك** في نفس
المحادثة (بروتوكول التشغيل §ب.2).

---

*تقرير مولَّد بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
