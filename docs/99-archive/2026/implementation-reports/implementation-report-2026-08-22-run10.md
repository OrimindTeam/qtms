# تقرير تنفيذ برمجي — UPDS DocGen — 2026-08-22

**رقم تشغيل التنفيذ:** 10
**المنهجية المعتمدة:** تزايدية (Incremental) (وحدة العمل: **زيادة**)
**تقدّم التنفيذ:** 0٪ (0 منجَزة من 26)
**حالة التوثيق وقت هذا التشغيل:** 100٪ (0 بند توثيق مفتوح)
**أسئلة تنفيذ مفتوحة:** 1 (`IQ-006` — **جديد وحرج**)

---

## 1. ملخص تنفيذي

★ **نُشرت عيّنة `DEBT-09` فعلياً على `qtms-orimind-master` بإذنك، وثبتت ثلاثة
أرباع المسار بتشغيل حقيقي** — وانكشف **حاجز بنيوي لم يتوقّعه أي مستند**.

**ما ثبت:** الحاوية تُبنى من `dart:3.11` **بلا Flutter** · الخدمة تُنشَر وتعمل
على Cloud Run · تستقبل CloudEvent وتردّ `HTTP 200` · ★ **وتكتب في قاعدة
البيانات الحقيقية، والأثر مقروء فعلاً بقيمه الصحيحة**.

⛔ **وما لم يثبت — ولا يمكن أن يثبت كما هو:** **`me-central2` مغلقة** لـCloud
Run وEventarc وArtifact Registry وCloud Build (`LOCATION_POLICY_VIOLATED` —
«Access to the region is unavailable»)، **والقاعدة وحدها تعمل فيها**.
ومشغّل القاعدة **يجب أن يكون في منطقتها حرفياً** — جُرِّب من `me-central1`
فرُفض بنصّه. ⟵ ★ **فالعمليات المشغَّلة بالكتابة غير قابلة للنشر إطلاقاً**،
وصُعِّد ذلك في **`IQ-006`**.

✅ **وأجبتَ على `IQ-004` و`IQ-005` أثناء التشغيل فرُحِّلا:** الأول أنتج
**`ADR-0013` بحالة «مقترح»** كما طلبت، والثاني **نُفِّذ في الكود** بثلاثة
اختبارات جديدة.

**213 اختباراً ناجحاً · ٠ فاشلاً.** ⛔ **ولا وحدة أُعلنت منجَزة.**

---

## 2. بوابة الدخول — نتيجة الفحص

✅ **فُحصت في التشغيل 9 من هذا اليوم ولم يتغيّر شيء منها** — البوابة العامة
(٨ شروط) وبوابة `WU-000` (٥ شروط) كلها متحققة. راجع
`implementation-report-2026-08-22-run9.md` §2.

⚠️ **وملاحظة على الشرط 8 للتشغيل القادم:** صار في المستودع **ADR بحالة
«مقترح»** (`ADR-0013`)، **وهو يحكم نمط الكتابة الذي ستُبنى به الدوال**.
⛔ **فالشرط 8 سيفشل على أي وحدة تكتب — حتى تعتمده.**

---

## 3. وحدات العمل في هذا التشغيل

### 3.1 وحدات اكتملت وتم التحقق منها فعلياً

⛔ **لا شيء.** `WU-000` ما تزال `[قيد التنفيذ]` بصدق.

### 3.2 وحدات قيد التنفيذ

| WU | ما أُنجز | الفحوصات ونتيجتها الفعلية |
|---|---|---|
| `WU-000` | ★ **نشر عيّنة `DEBT-09` على التجريبية** · ★ **تنفيذ `IQ-005`** في حزمة النطاق · **`ADR-0013` مسودة** | Cloud Build ✅ · `run deploy` ✅ · استدعاء حقيقي **`HTTP 200`** ✅ · **قراءة الأثر من القاعدة** ✅ · `dart test` **16** ✅ + **20** ✅ · `flutter analyze/test` **9** ✅ · `npm test` **168/168** ✅ |

**المجموع: 213 اختباراً · 213 ناجحاً · ٠ فاشلاً.**

### 3.3 ★ ما نُشر فعلاً على `qtms-orimind-master` (سجل صريح)

| المورد | القيمة | ملاحظة |
|---|---|---|
| حساب خدمة | `qtms-functions-spike@…` | ★ **أقل امتياز**: `datastore.user` + `eventarc.eventReceiver` **فقط** |
| مستودع صور | `qtms` في **`me-central1`** | ⚠️ `me-central2` مرفوضة |
| صورة | `…/qtms/functions:debt09` | من `dart:3.11` — **بلا Flutter** |
| خدمة Cloud Run | `qtms-functions-debt09` في **`me-central1`** | ⛔ **بلا وصول عام** (`--no-allow-unauthenticated`) |
| مستند أثر | `spike_debt09/debt09-live-probe-001` | ⛔ **مرفوض لكل العملاء** بالرفض الافتراضي · يُزال بـ`DEBT-14` |
| مشغّل Eventarc | ⛔ **لم يُنشَأ** | **محجوب إقليمياً** — `IQ-006` |

⛔ **ولم يُمَسّ مشروع الإنتاج `qtms-orimind-c001` إطلاقاً.**

---

## 4. مخالفات النزاهة المرصودة آلياً

**لا مخالفات** — `integrity_violations: []` و`unparsed_lines: []`.

---

## 5. أسئلة التنفيذ — ما رُحِّل وما استُجدّ

### 5.1 ✅ رُحِّلت في هذا التشغيل

| IQ | إجابتك | أين رُحِّلت |
|---|---|---|
| `IQ-004` | **الخيار ب** — دالة مستدعاة تكتب المستند وقيده في معاملة واحدة، **مع ADR** | ★ **`ADR-0013` أُنشئ بحالة «مقترح»** — ⛔ **ولم يُكتب كود يعتمد عليه** |
| `IQ-005` | **الخيار ج** — تاريخ المستند لما يقبل السابق، واليوم لما عداه | ★ **نُفِّذ في الكود:** `allowsBackdating` و`documentNumberDay` + **3 اختبارات** · و**قاعدة العدّادات وُثِّقت** في `data-dictionary.md` §5-ب |

★ **وتصنيف `IQ-005` مشتقّ من `firestore.rules` حرفياً لا مخترَعاً:**
`countedIntake` و`sack` وحدهما مقيَّدان بـ`todayStockDate()`، والسبعة الباقية
تقبل تاريخاً سابقاً — كلٌّ بشرطه المكتوب في القواعد.

### 5.2 ⏳ استُجدّ

| IQ | السؤال | لماذا لم يكن قابلاً للاستنتاج |
|---|---|---|
| ★ `IQ-006` | **`me-central2` مغلقة لكل ما عدا قاعدة البيانات — فأين تعيش العمليات المشغَّلة؟** | ★ **لا مستند يذكر هذا ولا يمكن أن يذكره** — إتاحة إقليمية تُكتشف **بالتشغيل وحده**، والرسالة صريحة: «Contact our sales team». والخيارات الثلاثة **تنقض مستنداً مختلفاً كلٌّ منها** |

---

## 6. تعارضات وثغرات توثيق اكتشفها التنفيذ

| # | الثغرة | كيف عولجت |
|:-:|---|---|
| 1 | ★★ **`environments.md` يصف `me-central2` بأنه «نهائي لا رجعة فيه»** — والتشغيل أثبت أنه **لا يحمل العمليات السحابية أصلاً** | ⛔ **لم يُعدَّل المستند ذاتياً** · صُعِّد في **`IQ-006`** وسُجِّل **`DEBT-15`**. ★ **ونُبِّه صراحةً أن القاعدة فارغة الآن فالتصحيح شبه مجاني** |
| 2 | ★ **تصحيح ذاتي:** كنت وثّقت في `build-and-run-instructions.md` §2.2.3 أن المشغّل يُنشَأ بـ`--event-data-content-type=application/json` | ★ **خطأ ثبت بالتجربة وصُحِّح في نفس اليوم** — الصيغة **مرفوضة** لأحداث القاعدة، والمقبول `application/protobuf`. وسُجِّل **`DEBT-16`** لأن الفكّ الحالي بـJSON |
| 3 | **`gcloud` على Windows يفشل برفع المصدر** (`WinError 32`) بأي حجم | عولج **بتجهيز الأرشيف يدوياً ورفعه إلى GCS** ثم البناء منه · و`.gcloudignore` **بأنماط مُثبَّتة بشرطة بادئة** ⚠️ لأن `lib/` بلا شرطة كانت ستستبعد `functions/lib/` **فيفشل البناء بلا سبب ظاهر** |
| 4 | **Cloud Build غير متاح في `me-central2`** | بُني في `global` ودُفعت الصورة إلى مستودع في `me-central1` — ✅ **ونجح** |

⛔ **ولم يُعدَّل أي مستند محكوم بحَوْكمة** — ولا `api-overview.md` ولا
`environments.md` ولا أي ADR معتمد.

---

## 7. بنود بوابة الحَوْكمة (تنتظر موافقة بشرية صريحة)

| البند | الحالة |
|---|---|
| ★ **`ADR-0013`** | ⏳ **مقترح — ينتظر اعتمادك.** أُنشئ بطلبك الصريح في `IQ-004`. ⛔ **ولم يُعتمَد ذاتياً، ولا كود يعتمد عليه** |
| ★ **`IQ-006`** | ⏳ **مفتوح** — ★ **وخياراته الثلاثة كلها تحتاج ADR جديداً** |

⛔ **ولم أُنفِّذ أي عملية Git خارجية** (لا `push` ولا `PR` ولا `merge`).

---

## 8. ⚠️ وحدات حسّاسة تحتاج مراجعة بشرية قبل النشر الإنتاجي

| # | البند |
|:-:|---|
| ① | ★★ **`IQ-006` — قرار المنطقة.** ⚠️ **وهو الأعجل على الإطلاق**: القاعدة فارغة اليوم، **وكل يوم بيانات حقيقية يرفع كلفته** |
| ② | ★ **`ADR-0013` يضاعف سطح التفويض:** كل قاعدة صلاحية ستُكتب **مرتين** — في القواعد وفي الدالة. ⚠️ **نسختان تفترقان** — ويلزم اختبار يُشغَّل على الاثنين معاً |
| ③ | ★ **خدمة Cloud Run منشورة الآن بحساب خدمة له `datastore.user`** على التجريبية — ⛔ **تُراجَع وتُزال مع `DEBT-14`** |
| ④ | ★ **App Check بفرض مُطفأ** — إلزامي قبل أول نشر إنتاجي |
| ⑤ | ★ **`DEBT-13`** — رسالة «خدمات المنصة غائبة» ما تزال ناقصة |
| ⑥ | **حساب المالك بلا صلاحيات** حتى تُبنى `grantPermissions` |

---

## 9. مستندات UPDS التي حُدِّثت تبعاً للكود

| المستند | سبب التحديث |
|---|---|
| ★ `03-architecture/adr/ADR-0013-…md` | ★ **جديد — بحالة «مقترح»** ترحيلاً لإجابة `IQ-004` |
| `06-database/data-dictionary.md` | ★ **§5-ب جديد** — جدول اليوم الذي يدخل الرقم + **قاعدة العدّادات للتواريخ السابقة** (`IQ-005`) |
| `03-architecture/risks-and-technical-debt-register.md` | `DEBT-09` صار **ثلاثة أرباعه ثابتة** · ★ **`DEBT-15`** (الحاجز الإقليمي) · ★ **`DEBT-16`** (فكّ protobuf) |
| `07-development/build-and-run-instructions.md` | ★ **تصحيح §2.2.3** — protobuf لا JSON |
| `07-development/troubleshooting-guide.md` | ثلاثة أعراض سحابية حرفية رُصدت فعلياً |
| `00-governance/upds-workflow/implementation-plan.md` | **7 فحوص سحابية** بنتائجها · حالة المخرَج 7 · جدول البنود |
| `00-governance/upds-workflow/implementation-questions.md` | `IQ-004` و`IQ-005` **[تم الحل ✅]** · ★ **`IQ-006` جديد** |

⛔ **لم تُعدَّل `firestore.rules`** — ولا حاجة: الرفض الافتراضي يغطي
`spike_debt09`. ⚠️ **وستُعاد كتابتها عند اعتماد `ADR-0013`.**

---

## 10. حالة `CLAUDE.md`

★ **حُدِّث** — كتلته كانت تقول **«كل `ADR-0001`…`ADR-0012` معتمد»** و**«لا
حاجز حَوْكمة باقياً»**، **وصار الاثنان غير صحيحين**.

| الحقل | القيمة |
|---|---|
| `action` | `managed-block-updated` · `new_total_lines` **141** من حدّ 200 |
| `human_content_lines_preserved` | `0` |

---

## 11. الخطوة التالية الموصى بها

| # | الخطوة | مَن |
|:-:|---|---|
| **1** | ★★ **احسم `IQ-006`** — ⚠️ **الأعجل**: القاعدة فارغة، والكلفة ترتفع بأول بيانات | **أنت** |
| **2** | ★ **اعتمد `ADR-0013`** (أو ردّه) — ⛔ **ولا كود كتابة قبله** | **أنت** |
| 3 | حسب قرار `IQ-006`: نقل المنطقة · أو طلب الفتح · أو إسقاط المشغّلات | `/upds-docgen:implement` |
| 4 | ثم الدوال الخمس · ثم **إزالة العيّنة (`DEBT-14`)** · ثم اعتماد `WU-000` | `/upds-docgen:implement` |

---

*تقرير مولَّد آلياً بواسطة UPDS DocGen. تقارير التشغيلات السابقة محفوظة بأسمائها في نفس المجلد (سجل تراكمي — لا تُحذَف).*
